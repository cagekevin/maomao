# VA01 — vendor.js 导出标注任务（40 个）

> vendor.js 中 `内部名 as 导出名` 格式的 195 个导出，标注每个导出对应的真实 API。

## 方法

1. 对每个导出名，grep App.js 看它怎么被 import：
   ```
   import { Nr as le } from "./vendor/vendor.js"
   # Nr 在 App.js 中别名 le → grep "\ble\b" 看使用场景
   ```
2. 根据使用模式判断对应哪个库的哪个 API：
   - 如果是 JSX 工厂 → React.createElement
   - 如果是组件包装 → React.memo / forwardRef
   - 如果是 hook → useState / useEffect / useRef...
   - 如果是图标 → lucide-react 的具体图标名
   - 如果是工具 → lodash 的具体函数
3. 不确定标注 `(?)`

## 输出格式

```
导出名 = 库::API # 使用场景
Nr = React::createPortal # Un = Nr(), createPortal 调用
Ar = React::createElement # X = Ar(), jsx 工厂
Mr = React::memo # Y.memo(...) 包组件
Wt = lucide::X # 关闭按钮图标
```

| # | 导出名 | 推断结果 |
|---|--------|----------|
| 1 | `A` | three::RGBAFormat # THREE 纹理格式常量 1023（LT=1022/RT=1023… 同组为 THREE 像素格式），用于 Texture.format |
| 2 | `An` | lucide-react::Images # 图片图标（images），素材/图集节点 |
| 3 | `Ar` | react::createElement # JSX 编译工厂（X=Ar(...)），见任务参考示例 |
| 4 | `At` | react-dom::mediaEventTypes (?) # 媒体事件类型数组（abort/canplay/…/waiting），MediaEventPlugin 内部常量 |
| 5 | `B` | react-easy-crop::Cropper # 图片裁剪组件（extends PureComponent，evData/mouseDownOnCrop/docMoveBound 裁剪逻辑） |
| 6 | `Bn` | react::internal (?) # React 内部常量 0（hu=0，reconciler/scheduler 内部状态） |
| 7 | `Bt` | lucide-react::Search # 搜索图标（search） |
| 8 | `C` | three::LinearFilter # THREE 纹理过滤常量 1006（_T=1005/vT=1006… 同组为过滤模式），Texture.minFilter |
| 9 | `Cn` | lucide-react::LayoutTemplate # 布局模板图标（layout-template） |
| 10 | `Cr` | lucide-react::BookOpen # 书/文档图标（book-open） |
| 11 | `Ct` | lucide-react::Undo2 # 撤销图标（undo-2） |
| 12 | `D` | three::MeshStandardMaterial # THREE 标准 PBR 材质类（extends Material，isMeshStandardMaterial） |
| 13 | `Dn` | lucide-react::Keyboard # 键盘图标（keyboard） |
| 14 | `Dr` | lucide-react::ArrowDownUp # 上下箭头图标（arrow-down-up，排序用） |
| 15 | `Dt` | lucide-react::Ticket # 票/优惠券图标（ticket，特惠视频相关） |
| 16 | `E` | three::Matrix4 # THREE 4x4 矩阵类（isMatrix4，set/elements） |
| 17 | `En` | lucide-react::Lasso # 套索图标（lasso，图片分割/选区） |
| 18 | `Er` | lucide-react::ArrowLeft # 左箭头图标（arrow-left，返回） |
| 19 | `Et` | lucide-react::Trash2 # 删除图标（trash-2） |
| 20 | `F` | three::WebGLRenderTarget # THREE WebGL 渲染目标类（extends RenderTarget，isWebGLRenderTarget） |
| 21 | `Fn` | lucide-react::Hash # “#” 图标（hash） |
| 22 | `Ft` | lucide-react::ShieldCheck # 安全/校验图标（shield-check） |
| 23 | `G` | @xyflow/react::Handle # ReactFlow 连接点组件（forwardRef+memo，type/position/isValidConnection/isConnectable） |
| 24 | `Gn` | react::ErrorBoundary # 错误边界类组件（componentDidCatch / getDerivedStateFromError） |
| 25 | `Gt` | react::internal (?) # React 内部常量 0（ud=0，调度器内部） |
| 26 | `H` | @xyflow/react::Background # ReactFlow 背景组件（displayName Background，variant/gap/size/dots） |
| 27 | `Hn` | react::internal (?) # React 内部常量 null（pu=null） |
| 28 | `Ht` | lucide-react::ScanFace # 人脸识别图标（scan-face） |
| 29 | `I` | aws4::SignatureV4 (?) # AWS SigV4 请求签名器（constructor: accessKeyId/secretAccessKey/sessionToken/service/region/retries/initRetryMs） |
| 30 | `In` | react::internal (?) # React 内部常量 null（yu=null） |
| 31 | `It` | lucide-react::Settings # 设置齿轮图标（settings） |
| 32 | `J` | @xyflow/react::Panel # ReactFlow 面板组件（forwardRef，react-flow__panel，position top/left…） |
| 33 | `Jn` | react::internal (?) # React 内部（su=Se()，调度器 getCurrentTime 调用） |
| 34 | `Jt` | react::internal (?) # React 内部常量 false（sd=!1，调度器标志） |
| 35 | `K` | @xyflow/react::MiniMap # ReactFlow 缩略图组件（displayName MiniMap，nodeColor/nodeStrokeWidth…） |
| 36 | `Kn` | react::internal (?) # React 内部常量 null（lu=null） |
| 37 | `Kt` | react::internal (?) # React 内部常量 false（ld=!1，调度器标志） |
| 38 | `L` | gifenc (?) # GIF 编码库（GIFEncoder/quantize/applyPalette/nearestColor/nearestColorIndex…） |
| 39 | `Ln` | react::internal (?) # React 内部常量 0（vu=0） |
| 40 | `Lt` | lucide-react::Settings2 # 设置-2 图标（settings-2） |

## 参考
- App.js 导入行: `head -3 src/App.js` 看完整 import 映射
- 常见分组: $$/q/C/T 系→React hook/API，小写→图标，大写→组件
- rolldown-runtime.js 导出: `l`
