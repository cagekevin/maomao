# VA03 — vendor.js 导出标注任务（40 个）

> vendor.js 中 `内部名 as 导出名` 格式的 195 个导出，标注每个导出对应的真实 API。

## 方法

1. 对每个导出名，grep App.js 看它怎么被 import：
   ```
   import { Nr as le } from "./vendor/vendor.js"
   # Nr 在 App.js 中别名 le → grep "\ble\b" 看使用场景
   ```
2. 根据使用模式判断对应哪个库的哪个 API：
   - 如果是 JSX 工厂 → React.createElement
   - 如果是组件包装 → React.memo / forwardRef
   - 如果是 hook → useState / useEffect / useRef...
   - 如果是图标 → lucide-react 的具体图标名
   - 如果是工具 → lodash 的具体函数
3. 不确定标注 `(?)`

## 输出格式

```
导出名 = 库::API # 使用场景
Nr = React::createPortal # Un = Nr(), createPortal 调用
Ar = React::createElement # X = Ar(), jsx 工厂
Mr = React::memo # Y.memo(...) 包组件
Wt = lucide::X # 关闭按钮图标
```

| # | 导出名 | 推断结果 |
|---|--------|----------|
| 1 | `Xt` | `Xt = app::helper(?) # Ie(\`图片N\`) 媒体标签/复制类工具,真实用法被局部变量遮蔽` |
| 2 | `Y` | `Y = app::getMediaTypeLabel(?) # Le(e) 按 type 返回 '图片'/'视频' 等展示名` |
| 3 | `Yn` | `Yn = lucide-react::Icon(?) # Re size11/15 strokeWidth1.8 aria-hidden 图标` |
| 4 | `Yt` | `Yt = lucide-react::Icon(?) # ze X.jsx(ze,{…}) L15937` |
| 5 | `Z` | `Z = (?)? # Be 仅见局部尺寸变量,未定位真实 API(疑似被遮蔽)` |
| 6 | `Zn` | `Zn = (?)? # Ve 仅见局部尺寸变量,未定位真实 API(疑似被遮蔽)` |
| 7 | `Zt` | `Zt = lucide-react::Icon(?) # He X.jsx(He,{…}) 4 处` |
| 8 | `_` | `_ = (?)? # Ue 被局部 useRef 遮蔽,未定位真实 API` |
| 9 | `_n` | `_n = lucide-react::Icon(?) # We size11/15 strokeWidth1.8 图标(图片类按钮)` |
| 10 | `_r` | `_r = react-flow::Handle # Ge X.jsx(_r,{type:'target',…})` |
| 11 | `_t` | `_t = react-flow::Node(?) # Ke X.jsx(Ke,{…}) style:Ke 疑似节点/容器组件` |
| 12 | `a` | `a = app::component(?) # qe X.jsx(qe,{…}) onClick:()=>qe(id)` |
| 13 | `an` | `an = app::optionsArray(?) # Je=[{…}] 列表(切片 map 渲染)` |
| 14 | `ar` | `ar = app::component(?) # Ye function Ye(e) 在 map 中渲染` |
| 15 | `at` | `at = react-flow::Position # J.Left / J.Right 枚举(Position.Left/Right)` |
| 16 | `b` | `b = three::RepeatWrapping # Xe e.wrapS = Xe; e.wrapT = Xe(THREE 纹理环绕常量)` |
| 17 | `bn` | `bn = lucide-react::Icon(?) # Ze X.jsx(Ze,{…}) L33476` |
| 18 | `br` | `br = lucide-react::Loader(?) # Qe size + className animate-spin 旋转/加载图标` |
| 19 | `bt` | `bt = lucide-react::Icon(?) # $e X.jsx($e,{…}) L25164` |
| 20 | `c` | `c = app::component(?) # et X.jsx(et,{…}) L25869` |
| 21 | `cn` | `cn = app::component(?) # tt function tt(e) X.jsx(tt,{…})` |
| 22 | `cr` | `cr = (?)? # nt 仅见局部 style/state 对象,未定位真实 API` |
| 23 | `ct` | `ct = lucide-react::Crop(?) # rt X.jsx(rt,{size:14}) 旁 title='裁剪'` |
| 24 | `d` | `d = @react-three/fiber::PerspectiveCamera # it X.jsx(it,{makeDefault,position,fov})` |
| 25 | `dn` | `dn = lucide-react::Icon(?) # at X.jsx(at,{size:16})` |
| 26 | `dr` | `dr = lucide-react::Icon(?) # ot X.jsx(ot,{size:12,text-green-500 drop-shadow}) 成功/勾选图标` |
| 27 | `dt` | `dt = app::component(?) # st X.jsx(st,{…}) L35527` |
| 28 | `e` | `e = (不存在) # 非 vendor 导出,任务表噪声项` |
| 29 | `en` | `en = lucide-react::Icon(?) # ct X.jsx(ct,{…}) 9 处` |
| 30 | `er` | `er = lucide-react::Icon(?) # lt X.jsx(lt,{size:16})` |
| 31 | `et` | `et = React::memo # ut(Y.useMemo(…)) 包裹组件` |
| 32 | `expected` | `expected = (不存在) # 非 vendor 导出,任务表噪声项` |
| 33 | `explained` | `explained = (不存在) # 非 vendor 导出,任务表噪声项` |
| 34 | `f` | `f = drawing::Line(?) # dt X.jsx(dt,{color,lineWidth,points,onClick,opacity,transparent}) 绘图线条` |
| 35 | `fn` | `fn = lucide-react::Icon(?) # ft X.jsx(ft,{…}) 5 处` |
| 36 | `fr` | `fr = lucide-react::Icon(?) # pt X.jsx(pt,{…}) 20 处` |
| 37 | `ft` | `ft = lucide-react::Icon(?) # mt X.jsx(mt,{className:'w-4 h-4 …'})` |
| 38 | `g` | `g = React::(memo/forwardRef)(?) # ht(…) 包裹 X.jsx 的 HOC` |
| 39 | `gn` | `gn = lucide-react::Icon(?) # gt X.jsx(gt,{…})` |
| 40 | `gr` | `gr = lucide-react::Icon(?) # _t X.jsx(_t,{…}) 6 处` |

## 参考
- App.js 导入行: `head -3 src/App.js` 看完整 import 映射
- 常见分组: $$/q/C/T 系→React hook/API，小写→图标，大写→组件
- rolldown-runtime.js 导出: `l`
