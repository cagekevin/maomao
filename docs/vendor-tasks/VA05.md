# VA05 — vendor.js 导出标注任务（39 个）

> vendor.js 中 `内部名 as 导出名` 格式的 195 个导出，标注每个导出对应的真实 API。

## 方法

1. 对每个导出名，grep App.js 看它怎么被 import：
   ```
   import { Nr as le } from "./vendor/vendor.js"
   # Nr 在 App.js 中别名 le → grep "\ble\b" 看使用场景
   ```
2. 根据使用模式判断对应哪个库的哪个 API：
   - 如果是 JSX 工厂 → React.createElement
   - 如果是组件包装 → React.memo / forwardRef
   - 如果是 hook → useState / useEffect / useRef...
   - 如果是图标 → lucide-react 的具体图标名
   - 如果是工具 → lodash 的具体函数
3. 不确定标注 `(?)`

## 输出格式

```
导出名 = 库::API # 使用场景
Nr = React::createPortal # Un = Nr(), createPortal 调用
Ar = React::createElement # X = Ar(), jsx 工厂
Mr = React::memo # Y.memo(...) 包组件
Wt = lucide::X # 关闭按钮图标
```

| # | 导出名 | 推断结果 |
|---|--------|----------|
| 1 | `qn` | `lucide-react::FileBraces` # 文件括号图标 |
| 2 | `qt` | `lucide-react::Rotate3d` # 3D 旋转图标 |
| 3 | `r` | `three/SkeletonUtils::clone` # 深克隆含骨骼 SkinnedMesh 层级 |
| 4 | `rn` | `lucide-react::PenLine` # 笔线图标 |
| 5 | `rr` | `lucide-react::Copy` # 复制图标 |
| 6 | `rt` | `zustand::useStore (?) # 基于 context 的 store 选择器 hook (useContext + selector) |
| 7 | `s` | `@react-three/drei::Gizmo (?) # 3D 坐标轴/方位指示组件 (hideNegativeAxes/axisColors) |
| 8 | `sn` | `lucide-react::MoveVertical` # 垂直移动图标 |
| 9 | `sr` | `lucide-react::Circle` # 圆形图标 |
| 10 | `st` | `lucide-react::ZoomOut` # 缩小图标 |
| 11 | `t` | `@dagrejs/dagre (?) # 有向图布局 graphlib/layout/util.time |
| 12 | `the` | `N/A (vendor.js 导出列表中无此名) (?) |
| 13 | `tn` | `lucide-react::Pipette` # 吸管图标 |
| 14 | `tr` | `lucide-react::Download` # 下载图标 |
| 15 | `triangles` | `N/A (vendor.js 导出列表中无此名) (?) |
| 16 | `tt` | `react::useControllableState (?) # 受控值 hook，返回 [value,set,update] 三元组 |
| 17 | `u` | `@react-three/drei::OrbitControls` # 轨道控制器 (forwardRef, makeDefault, invalidate) |
| 18 | `un` | `lucide-react::MonitorPlay` # 监视播放图标 |
| 19 | `ur` | `lucide-react::CirclePlay` # 圆圈播放图标 |
| 20 | `ut` | `lucide-react::X` # 关闭/叉号图标 |
| 21 | `v` | `@react-three/fiber::useThree` # r3f store 选择器 hook (selector, equalityFn) |
| 22 | `vn` | `lucide-react::LockOpen` # 开锁图标 |
| 23 | `vr` | `lucide-react::ChevronDown` # 下箭头图标 |
| 24 | `vt` | `lucide-react::Users` # 用户组图标 |
| 25 | `w` | `constant/enum (?) # 连续枚举常量之一 (xT=1008，同组 1004~1020) |
| 26 | `wn` | `lucide-react::LayoutGrid` # 网格布局图标 |
| 27 | `wr` | `react::FiberFlags (?) # React 内部子树标志位常量 (jl=8192, subtreeFlags & jl) |
| 28 | `wt` | `lucide-react::Type` # 文本/字体图标 |
| 29 | `x` | `three::Color` # THREE.Color 颜色类 (isColor,r/g/b,set) |
| 30 | `xn` | `lucide-react::Link` # 链接图标 |
| 31 | `xr` | `lucide-react::Boxes` # Boxes 图标 |
| 32 | `xt` | `lucide-react::Upload` # 上传图标 |
| 33 | `y` | `three::Box3` # THREE.Box3 包围盒类 (isBox3,min,max,setFromArray) |
| 34 | `yn` | `lucide-react::LoaderCircle` # 加载圆圈图标 |
| 35 | `yr` | `lucide-react::Check` # 勾选图标 |
| 36 | `yt` | `lucide-react::User` # 用户图标 |
| 37 | `z` | `positioningUtil (?) # 居中定位工具，支持 %/px 单位 (box 居中到父容器) |
| 38 | `zn` | `lucide-react::Grid2x2` # 2x2 网格图标 |
| 39 | `zt` | `lucide-react::Send` # 发送图标 |

## 参考
- App.js 导入行: `head -3 src/App.js` 看完整 import 映射
- 常见分组: $$/q/C/T 系→React hook/API，小写→图标，大写→组件
- rolldown-runtime.js 导出: `l`
