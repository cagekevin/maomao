# VA04 — vendor.js 导出标注任务（40 个）

> vendor.js 中 `内部名 as 导出名` 格式的 195 个导出，标注每个导出对应的真实 API。

## 方法

1. 对每个导出名，grep App.js 看它怎么被 import：
   ```
   import { Nr as le } from "./vendor/vendor.js"
   # Nr 在 App.js 中别名 le → grep "\ble\b" 看使用场景
   ```
2. 根据使用模式判断对应哪个库的哪个 API：
   - 如果是 JSX 工厂 → React.createElement
   - 如果是组件包装 → React.memo / forwardRef
   - 如果是 hook → useState / useEffect / useRef...
   - 如果是图标 → lucide-react 的具体图标名
   - 如果是工具 → lodash 的具体函数
3. 不确定标注 `(?)`

## 输出格式

```
导出名 = 库::API # 使用场景
Nr = React::createPortal # Un = Nr(), createPortal 调用
Ar = React::createElement # X = Ar(), jsx 工厂
Mr = React::memo # Y.memo(...) 包组件
Wt = lucide::X # 关闭按钮图标
```

| # | 导出名 | 推断结果 |
|---|--------|----------|
| 1 | `gt` | lucide-react::volume-2 # U(`volume-2`) 图标组件（内部 zd） |
| 2 | `h` | @reactflow::BaseEdge (?) # 边渲染包装 function xW(e){jsx(iH,{children:jsx(bW,...)})} |
| 3 | `hn` | lucide-react::maximize-2 # U(`maximize-2`)（内部 Bu） |
| 4 | `hr` | lucide-react::chevron-up # U(`chevron-up`)（内部 Bl） |
| 5 | `ht` | lucide-react::volume-x # U(`volume-x`)（内部 Bd） |
| 6 | `i` | three::OBJLoader # three OBJ 加载器 class extends Loader, this.materials=null, load()（内部 jq） |
| 7 | `in` | lucide-react::pause # U(`pause`)（内部 Zu） |
| 8 | `ir` | lucide-react::code # U(`code`)（内部 Zl） |
| 9 | `it` | @reactflow::useUpdateNodeInternals # hook 返回更新节点内部尺寸的回调（内部 UC） |
| 10 | `j` | three::SRGBColorSpace # three 颜色空间常量字符串 'srgb'（内部 GE=`srgb`） |
| 11 | `jn` | lucide-react::image # U(`image`)（内部 wu） |
| 12 | `jr` | @reactflow::(?) (?) # 内部 d，处于 React Flow 上下文但未定位到公开 API |
| 13 | `jt` | lucide-react::star # U(`star`)（内部 wd） |
| 14 | `k` | three::Quaternion # three 四元数类 constructor(e,t,n,r){this.isQuaternion=!0}（内部 cO） |
| 15 | `kn` | lucide-react::inbox # U(`inbox`)（内部 Eu） |
| 16 | `kr` | react-dom::finalizeContainerChildren (?) # react-dom host config 内部变量（内部 ls=class{} 被赋 e.finalizeContainerChildren） |
| 17 | `kt` | lucide-react::store # U(`store`)（内部 Ed） |
| 18 | `l` | @react-three/drei::TransformControls (?) # forwardRef, domElement+onObjectChange+onMouseDown/Up（内部 VG） |
| 19 | `ln` | lucide-react::move-3d # U(`move-3d`)（内部 Ku） |
| 20 | `lr` | lucide-react::circle-question-mark # U(`circle-question-mark`) 帮助图标（内部 Kl） |
| 21 | `lt` | lucide-react::zap # U(`zap`)（内部 Kd） |
| 22 | `m` | @react-three/drei::Html # drei Html 组件 prepend/center/fullscreen/eps（内部 LW） |
| 23 | `mn` | lucide-react::maximize # U(`maximize`)（内部 Vu） |
| 24 | `mr` | react::(?) (?) # React 内部结构变量 Vl，非公开 API |
| 25 | `mt` | lucide-react::wand-sparkles # U(`wand-sparkles`)（内部 Vd） |
| 26 | `n` | three::GLTFLoader # three GLTF 加载器 dracoLoader/ktx2Loader（内部 Fq） |
| 27 | `nn` | lucide-react::pencil # U(`pencil`)（内部 $u） |
| 28 | `node` | (未找到) # vendor.js 导出块无此名（仅 170 处代码内 `node` 单词；疑属 rolldown-runtime 或其它 chunk） |
| 29 | `nr` | lucide-react::crop # U(`crop`)（内部 $l） |
| 30 | `nt` | @reactflow::useReactFlow # hook 返回画布 store 访问器 nodeLookup/nodeQueue/edgeQueue（内部 Rx） |
| 31 | `o` | @react-three/drei::Grid # drei 网格地面 gridMaterial/cellColor/sectionColor（内部 oK） |
| 32 | `on` | lucide-react::music # U(`music`)（内部 Yu） |
| 33 | `or` | lucide-react::clapperboard (?) # U(`clapperboard`)；与 React 内部名 Yl 同名遮蔽，取顶层 lucide 定义 |
| 34 | `ot` | @reactflow::getBezierPath # 贝塞尔连线路径计算 function Jv({sourceX,sourceY,sourcePosition,targetX,...}) |
| 35 | `p` | @reactflow::useStore (?) # zustand 式 store selector hook VW=e=>e?BW(e):BW |
| 36 | `perpendicular` | (未找到) # vendor.js 导出块无此名（仅 1 处 `direction as perpendicular` 噪声，未定位公开 API） |
| 37 | `pn` | lucide-react::mic # U(`mic`)（内部 Hu） |
| 38 | `pr` | lucide-react::chevrons-up # U(`chevrons-up`)（内部 Hl） |
| 39 | `pt` | lucide-react::waves # U(`waves`)（内部 Hd） |
| 40 | `q` | @reactflow::(memo component) (?) # React.memo(Dw) 记忆化组件（内部 Ow） |

## 参考
- App.js 导入行: `head -3 src/App.js` 看完整 import 映射
- 常见分组: $$/q/C/T 系→React hook/API，小写→图标，大写→组件
- rolldown-runtime.js 导出: `l`
