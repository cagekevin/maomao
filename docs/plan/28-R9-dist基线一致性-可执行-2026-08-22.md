# 28-R9 · dist/ 基线 & 构建产物一致性（可执行）

> 配套售后计划：`docs/27-API中转层售后收口计划-2026-08-22.md`（R9）。
> 定位：确认 `dist/` 构建产物与 `src/` 一致、`npm run check:health` 全量健康。提交前收尾保险。

---

## 1. 现状

- `dist/` 是 `src/` 的构建产物（Vite build）
- `npm run check:health` 内含：check:keys + check:events + check:node-types + build + test:all + TDZ + **dist 基线**
- 本次 API 收口改动后，`npm run build` 已单独验证通过（`b9dabad` 前），但未跑完整 `check:health`

---

## 2. 目标

跑 `npm run check:health`，确认 dist 基线、TDZ、全量健康 0 错 0 警。

---

## 3. 执行步骤

1. `npm run check:health`（全量编排，含构建+测试+TDZ+dist 基线）
2. 如有告警，逐项定位修复
3. 确认 `dist/` 与 `src/` 一致（dist 基线检查通过）

---

## 4. 验收

- [ ] `npm run check:health` 通过，0 错 0 警（或记录已知可接受告警）
- [ ] dist 基线一致
- [ ] `npm test` 全绿

---

## 5. 文件清单

| 文件 | 改动 |
|---|---|
| 无（运行验证） | 若 check:health 暴露问题，修复对应文件 |

---

## 5.1 代码审计修订（2026-08-22，code-explorer 实测）

> 实际读取 `package.json`/`scripts/health-check.cjs`/`vite.config.js` 后修正：

- `check:health` 真实定义：`package.json:26` → `node scripts/health-check.cjs`（**单脚本编排**，非多 npm 串联）。
- 该脚本实际串联 10 步：文件存在性 → npm scripts 完整性 → **build** → **test:all** → **check:keys** → **check:events** → **check:node-types** → **TDZ 扫描** → **dist 基线** → **⚠️决策渠道门禁（adr/CLAUDE.md，:183-194）**。文档列的 7 项子命令全部真实 ✅，但**漏列第 10 项"决策渠道门禁"**——提交前需确保 adr/CLAUDE.md 门禁通过，否则 check:health 会红。
- dist 基线逻辑：`scripts/health-check.cjs:139-172`，比对 `dist/` 与 `scripts/dist-snapshot.json`（首次运行自动生成），新增/删除/体积变化 >10%+1024B 即告警。
- `npm run build` → `vite build`（`vite.config.js:13-14` outDir `dist`）✅ 确产出 `dist/`。

## 6. 边界

- **纯验证**：非功能改动
- 若 check:health 报既有问题（非本次引入），记录不硬修，另立任务
