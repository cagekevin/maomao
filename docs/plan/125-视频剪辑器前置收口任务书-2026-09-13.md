# 125 · 视频剪辑器 · **前置收口任务书**（开工前的存量收口）

> **状态**：任务书（**未执行**） · **日期**：2026-09-13
> **定位**：这是视频剪辑器（`123`）的**开工前置**。**收口全部完成之前，不碰 `videoEditor/` 任何代码。**
> ⚠️ **执行请用 `docs/126-前置收口-执行任务书-交执行AI-2026-09-13.md`**（可脱手的执行版：只留**约束 + 验收**，怎么做由执行方定）。
> 本文保留为**分析依据**（能力清单 / 为什么 / 批次），两者冲突时以 **126** 为准。
> **上游**：`123` §0.5（第 0 步 · 开工前置）· `124`（能力重叠普查与时间轴真源裁决）
> **方法**：`架构师改码 7 步法`（Step 0 普查 → Step 1 证伪 → Step 2 归因 → Step 3 定位 → Step 4 定契 → Step 5 定权 → Step 6 施工 → Step 7 验证落账）
> **执行纪律**：
> 1. **一张任务卡一次收口**，做完一张再开下一张（用户要求"逐个慢慢来"）；
> 2. **每张卡的第一动作是只读取证**，不是改码（Step 1：不能假定"看起来像"就是重复）；
> 3. **收口优先"下沉 + 原处 re-export"**（消费方零改动，Step 3）——不动消费方是本任务书的第一原则；
> 4. 任何"份数""是否存在"的数字，**必须现查现写**，不许沿用本文件的数字（附录 A1/A2）。

---

## 〇、先定「我们需要什么能力」（从 `123` L0 + `120` 契约反推，不臆造）

> 口径：只列 **M1**（基座层：素材装配 + 导出）需要的能力。M2 专属（音量包络 / 转场 / 画中画 / 叠加轨 / 精剪）**不在本表**（`123` §一.4 里程碑过滤）。

### A 素材层

| # | 能力 | M1 用途（回指 123/120） | 现状 | 处置 |
| --- | --- | --- | --- | --- |
| A1 | 媒体判型（image/video/audio） | 分轨判据输入（C11.1） | `base/utils/assetType.ts`（**已收口**） | **复用** |
| A2 | 素材 URL 三源归一 + 断链判定 | 片段能不能读（120 C3 / C13） | 无 | **新建**（`videoEditor/data/sourceResolver.ts`） |
| A3 | 从画布节点提取媒体本体 URL | 入轨（B1 / 120 C11） | `base/canvas/nodeMedia.ts:49 getNodeMedia`（**已收口**） | **复用** |
| A4 | 素材元数据探测（时长/宽高/fps） | 入轨要时长（C11）；工程参数初值（C12） | `videoEngine.ts:176 readVideoMetadata`（**已收口**，**缺 codec**） | **复用 + 补 codec** |
| A5 | 图片默认停留时长 | 图片入轨（C11.6） | 常量 | 新建常量 |
| A6 | **引用登记（I6）** | 工程引用被文件生命周期看见（C13.1） | **无**（`13-文件生命周期` 是方案、`src/` 0 命中） | **与 13/122 合建**（不在本任务书内，另立） |

### B 帧/图像层

| # | 能力 | M1 用途 | 现状 | 处置 |
| --- | --- | --- | --- | --- |
| B1 | **从视频抽帧** | 缩略图胶片条（120 C11.10 **必需项**）· 定格（C15） | **多份**（见 **收口 1**） | **收口** |
| B2 | **音频波形 peaks** | 音频轨波形（120 C11.7b **必需项**） | **已落地**：`videoEditor/ui/.../timeline/audio-waveform.tsx`（wavesurfer.js 画真实波形） | **已交付**（原写「新建」，见下方更新条） |
| B3 | 音频解码（供波形） | B2 的输入 | 无独立实现；`videoEngine` 有 AudioBuffer 路径 | **借 `videoEngine`**，不引新依赖 |
| B4 | 多图层帧合成（`renderFrameAt`） | 导出合成重编码（C5/C8） | 无 | **新建**（`videoEditor/export/composite.ts`） |

> **★更新(2026-09-18) · B2 落地明细**：波形由 `videoEditor/ui/editor/panels/timeline/audio-waveform.tsx` 画（wavesurfer.js）。
> 峰值来源 = **该文件内联的 `extractPeaks`**，入参是 `timeline-element.tsx:484` 传来的 `element.buffer`（已解码的 `AudioBuffer`）。
> 当初另建的 `base/utils/audioPeaks.ts`（流式 `mergeIntoPeaks` + 一个只有 JSDoc 没有实现的顶层函数）**零生产消费** ⇒ 已删（TD-16-30）：
> 同一件事不留两份实现；其"流式解码省内存"卖点在这里**没有收益**（`element.buffer` 播放本身就要用，省不掉）。

### C 时间轴运算层

| # | 能力 | M1 用途 | 现状 | 处置 |
| --- | --- | --- | --- | --- |
| C1 | **源时间↔时间轴时间映射** | 预览=导出的地基（123 §一.1） | `VideoProcessNode.tsx:1356/1370/1552`（**3 处内联**） | **下沉**（收口 8） |
| C2 | **时间↔像素换算** | 工具带/轨道/播放头 | `VideoProcessNode.tsx:100`（固定比）· `director3d/panels/Timeline.tsx:120`（可变比） | **收口**（收口 2） |
| C3 | **刻度步长 / 抽稀** | 刻度尺 | `director3d/panels/Timeline.tsx:155-168` | **收口**（收口 2） |
| C4 | **吸附容差换算** | 吸附（C11/工具带） | `VideoProcessNode.tsx:797` · `Timeline.tsx:308` | **收口**（收口 3） |
| C5 | **框选矩形相交** | 片段多选（C11.8） | `Timeline.tsx:21/53/201-222` | **收口**（收口 4） |
| C6 | **rAF 播放钟** | 走带真出声（C11.7） | `director3d/App.tsx:1069` | **收口**（收口 5） |
| C7 | 时间轴编辑运算（压实/split/trim/删除二态/复制/定格） | 编辑动作面（C15） | **无**（A 参考实现有 `timelineOps`，**不可照搬**：见 123 §二.5 拒绝清单） | **新建**（`videoEditor/core/`） |

### D 交互层

| # | 能力 | M1 用途 | 现状 | 处置 |
| --- | --- | --- | --- | --- |
| D1 | 拖拽载荷 / 落点反馈 | 轨内拖序（M1）；N 有 `timelineDragPayload/timelineDropFeedback` | 无 | **新建**（`videoEditor/`） |
| D2 | **历史栈** | 工程级撤销（C6，MAX 50） | `base/canvas/historyStack.ts:15 HistoryStack<T>`（**MAX=15 写死**）· 另有 4 套 | **复用 + 改造**（收口 6） |
| D3 | 轨道状态（锁/隐/静音） | C7.1 | 无（`VTrack.muted` 有雏形） | **新建**（领域） |
| D4 | **键盘让位（激活位）** | C10（防画布误删/误撤销） | `useCanvasShortcuts.ts:80` + React Flow `deleteKeyCode`（**判据 2 份**） | **改造**（收口 9） |

### E 导出层

| # | 能力 | M1 用途 | 现状 | 处置 |
| --- | --- | --- | --- | --- |
| E1 | **编码器能力探针** | C9（启动期一次） | `director3d/App.tsx:1919` + `getFirstEncodableVideoCodec`（**散在 3D 域**） | **收口**（收口 7） |
| E2 | trim / concat | 直通导出（C5） | `videoEngine.ts:206/312`（**已收口**） | **复用** |
| E3 | 导出进度 + 取消 | C5（可中断） | `videoEngine.ts:153 ProgressController` + `:140 ConversionCanceled`（**已收口**） | **复用** |
| E4 | 合成重编码 | C5（直通外的常态） | 无（B4 的消费方） | **新建**（`videoEditor/export/`） |
| E5 | GIF | **M1 不需要**（120 §0.8） | `videoEngine.ts:519 videoToGif` | 不需要 |

### F 基础设施（**全部已收口，直接复用，不要碰**）

| # | 能力 | 现状 |
| --- | --- | --- |
| F1 | 落盘 / 上传唯一通道 | `base/api/filesApi.ts:246 uploadFileToLocal` |
| F2 | 产物回写画布唯一原语 | `base/canvas/deriveNodes.ts:63/135/152` |
| F3 | 工程持久化 | `core/contentStore.ts` + `contracts.ts:186 STORAGE_KEYS`（**G0 只需登记键**） |
| F4 | 全屏壳 / 模态层（M2 才用） | `base/panels/FullscreenShell.tsx` + `core/modalLayer.ts` |

**统计**：M1 需要 **27 项**能力 —— **已收口可直接复用 12 项** · **待收口 9 项** · **新建 5 项** · 不需 M1 1 项。

---

## 一、收口任务卡（逐个执行 · 每张先取证后动手）

### 收口 1 · 「从视频抽帧」统一原语 ⭐最高优先

| 项 | 内容 |
| --- | --- |
| **为什么** | 抽帧是**胶片条（M1 必需项）**与定格的前置；不先收口，剪辑器就是**第 N 份**实现 |
| **现状（需现查，数字以取证为准）** | ⚠️ **`124` 的"9 份"已自查为高估**：其中 `director3d/thumbnails.ts:12` 的输入是 **canvas**（不是 video）、`base/ui/VideoThumbnail.tsx:49` 是**显示侧**（`<video preload=metadata>`，根本不抽帧）→ 那 2 处**不是同类**。真正同机制（`<video>` seek → `drawImage` → 图）的是：`VideoProcessNode.tsx:142 captureFrame`（→`Blob`）· `useVideoPoster.ts:19`（→`dataURL`）· `scriptBoxEngine.ts:2089 captureVideoFrame`（→`dataURL`）· `clipboard.ts:150` 的 video→canvas helper（供 `ImageZoomDialog.tsx:194`）· `VideoExtractNode.tsx:188`（多模式节点） |
| **动作** | ① **只读取证**：逐份记 `输入(URL/File/元素) · 输出(Blob/dataURL/canvas) · 参数(时间/尺寸/质量) · 宿主耦合`；② **判种类**：区分**探测重复**（seek+drawImage 那段）与**判据不同**（各宿主何时抽、抽几张、拿去干什么）；③ 选最完整那份**下沉**到 `base/utils/`（候选 `captureFrame`，它已有 `crossOrigin`/超时/释放/`Blob` 输出），**原处 re-export**；④ 其余**先不迁**，标"待迁"。 |
| **验收** | `npm run check:arch && npm run type-check && npx vitest run tests/unit`；**探针**：把下沉函数临时改坏，确认原消费方测试变红（证明 re-export 真接到） |
| **风险 / 回退** | 低（纯下沉 + re-export）。回退 = `git checkout` 单文件 |
| **前置** | 无 |
| **批次** | **B** |

### 收口 2 · 「时间↔像素换算 + 刻度」共用原语

| 项 | 内容 |
| --- | --- |
| **为什么** | 剪辑器工具带/轨道件件都用；现在 2 份（一份固定比无缩放、一份可变比带刻度抽稀），剪辑器若自写 = 第 3 份 |
| **现状** | `VideoProcessNode.tsx:100 PX_PER_SEC=36`（固定，无缩放）· `director3d/panels/Timeline.tsx:120 pxPerFrame`（可变 + 滚轮缩放 `:178`）+ `:155-168` 刻度抽稀 |
| **动作** | 建 `src/components/base/utils/timeline/`：`timeToX/xToTime` · `clampZoom/fitZoom` · `pickTickStep/buildTicks/formatTickLabel`。**以 director3d 那份为准**（功能更全）。剪辑器用新版；**现有 2 份标"待迁"，本轮不改**（`director3d` 是克制域、`VideoProcessNode` 已上线） |
| **验收** | 新原语单测（`tests/unit/timeline/*.test.ts`）；`npm run check:arch`（新目录不得被 base 反向依赖） |
| **风险 / 回退** | 低（新增，不改存量）。风险 = 建了没人用 → 判据：**必须同时有 ≥2 个消费方使用**才建（当前：剪辑器 + 待迁 2 处） |
| **前置** | 无 |
| **批次** | **C** |

### 收口 3 · 「吸附容差换算」共用原语
- **现状**：`VideoProcessNode.tsx:796 snapTolerance / :797 snapTo` · `director3d/panels/Timeline.tsx:308-312`
- **动作**：与收口 2 同目录，收 `snapTime(t, candidates, pps, tolPx)`（容差以**像素**给、换算成秒，保证缩放手感一致）
- **验收 / 风险 / 批次**：同收口 2
- **前置**：收口 2（同目录）

### 收口 4 · 「框选矩形相交」共用原语
- **现状**：`director3d/panels/Timeline.tsx:21/53/201-222`（`MARQUEE_THRESHOLD` / `MarqueeState` / 拖超阈值框选）
- **动作**：收 `rectsIntersect(a,b)`（边缘接触也算相交）；**"框选什么、选中后做什么"保留各域**（判据不同）
- **验收 / 风险 / 批次**：同收口 2 · **前置**：收口 2

### 收口 5 · 「rAF 播放钟」共用原语
- **现状**：`director3d/App.tsx:1069`（`:1081` playStartRef、`:1097` rAF、`:405` currentFrame）— **全库唯一真播放钟**
- **动作**：抽成 `usePlaybackClock({duration, fps, onTick})`（纯计时 + 暂停/跳转），**不含媒体播放**（剪辑器要"出声"，3D 不要 → 那属判据层）
- **验收**：单测（给定假 rAF 推进，断言 tick 序列）；剪辑器接入时不得新写计时循环
- **风险**：动 `director3d` 需克制（CLAUDE.md §五·五）→ **抽新函数、让 director3d 调它**（re-export 思路）
- **批次**：**C** · **前置**：收口 2

### 收口 6 · 历史栈 `MAX` 参数化（**改造现有**，零新建）

| 项 | 内容 |
| --- | --- |
> **更新(2026-09-13，复验·由 126 执行 AI 回改)**：本表「MAX=15 写死」**不实** —— `historyStack.ts:22` 已是 `constructor({ max = 15 })`。**本卡实际无需改造**，交付缩小为「补契约锁用例（默认 15 / `{max:50}` 生效）+ 更正失实 JSDoc」；见 `daily/架构日志/21-视频剪辑器前置收口-2026-09-13.md`。

| **为什么** | 剪辑器要工程级撤销（C6 · `MAX_HISTORY=50`）；现有泛型 `HistoryStack<T>` ~~**MAX=15 写死**（画布用）~~（**上限本就可配，默认 15**）→ 复用即可，不必改造 |
| **现状** | `src/components/base/canvas/historyStack.ts:15 class HistoryStack<T>`（**默认上限 15，`{max}` 可配**）· 另有 `director3d/history.ts:9`(`HISTORY_LIMIT=50`) · `assistantTable/tableHistory.ts:29` · 4 个编辑器内部 |
| **动作** | 给 `HistoryStack` 构造增加 `max` 参数（**默认保持 15 → 画布零行为变化**）；剪辑器 `new HistoryStack({max:50})`。**不要求本轮迁移其余 3 套** |
| **验收** | 现有 `tests/unit` 里 HistoryStack 相关用例全绿（默认值不变）+ 一条新用例断言 `max:50` 生效。**探针**：把默认值改成 50 → 画布相关用例应红（证明默认值是真契约） |
| **风险 / 回退** | 极低。回退 = 单文件 |
| **前置** | 无 |
| **批次** | **A**（最易，先做） |

### 收口 7 · 编码器能力探针收口

| 项 | 内容 |
| --- | --- |
| **为什么** | 120 C9 要求启动期一次；现在这份逻辑**只存在于 3D 导出**里，剪辑器要用就是第 2 份 |
| **现状** | `src/components/director3d/App.tsx:1919`（`typeof VideoEncoder === 'undefined'`）+ `:1933 getFirstEncodableVideoCodec(['avc','av1','vp9'])`（mediabunny 封装） |
| **动作** | 抽到 `base/utils/`（如 `encoderProbe.ts`），返回 120 §一.6 的 `EncoderCapability` 判别联合；`director3d` 改为调用它（行为不变）；剪辑器共用。**结果按 UA/版本 memo**（C9/D10） |
| **验收** | 单测（mock `VideoEncoder` 缺失/存在两态）· 探针：注入"缺失"→ 3D 导出路径应给出明错而非静默 |
| **风险** | 中（动 3D 导出路径）→ 先只**抽函数 + director3d 调新函数**，不改 3D 的导出策略 |
| **前置** | 无 |
| **批次** | **B** |

### 收口 8 · 「源时间↔时间轴映射」下沉（`sourceTimeAt`）

| 项 | 内容 |
| --- | --- |
| **为什么** | 它是"预览=导出"的**最小充分条件**（123 §一.1）；现在**内联 3 处**，剪辑器再来一份 = 第 4 处 |
| **现状** | `VideoProcessNode.tsx:1356`（overlay 换算）· `:1370`（seek 落地）· `:1552`（源→时间轴反算） |
| **动作** | 抽 `sourceTimeAt(clip, timelineTime)` 与其反函数到 `videoEditor/core/`（**领域专属**，不进 base）；`VideoProcessNode` 的 3 处改调它 |
| **验收** | 单测（双向映射往返一致、EPS 内）· 现有 `VideoProcessNode` 相关用例绿 |
| **风险** | 低-中（动已上线节点）。回退 = 单文件 |
| **前置** | 无 |
| **批次** | **A** |

### 收口 9 · 「激活位」判据收成单点（G-3）

| 项 | 内容 |
| --- | --- |
| **为什么** | 120 C10 会让 `useCanvasShortcuts`（查询式）与 React Flow `deleteKeyCode`（声明式）**各写一份** `\|\| videoEditorOpen` 判据 → M3 母体 |
| **现状** | `src/hooks/useCanvasShortcuts.ts:80 if (hasModalLayer()) return;` · `App.tsx:1400 deleteKeyCode={modalLayerOpen ? null : DELETE_KEY_CODE}` · `:1130 modalLayerOpen = useSyncExternalStore(subscribeModalLayer, hasModalLayer)` |
| **动作** | 两处机制**无法统一**（`modalLayer.ts:48-54` 已有裁决），但**判据单点**：收成 `isCanvasSuppressed()`（= `hasModalLayer()` ∨ 剪辑器激活），查询式与订阅式都从它派生 |
| **验收** | 单测断言两处同源；grep 不到第二处 `videoEditorOpen` 判据 |
| **风险** | 中（动全局快捷键守卫）→ 必须在剪辑器存在前先做，否则两处判据会先长出来 |
| **前置** | 无（**但因它改的是"为剪辑器预留的让位"，必须在 G0 之前**） |
| **批次** | **D** |

### 收口 10 · 抽帧异类登记（**不是收口，是防止误并**）

- `director3d/thumbnails.ts:12 thumbnailFromCanvas(source: HTMLCanvasElement)` —— 输入是 **canvas**（3D 监视器截图），与"从视频抽帧"**不同类** → **不并入**
- `base/ui/VideoThumbnail.tsx` —— **显示侧**组件（`<video preload="metadata">` 原生首帧），**不抽帧** → **不并入**
- `videoEngine.ts:568` 的 `drawImage` —— GIF 逐帧编码路径，**不是抽帧 API** → **不并入**
- → 在 `base/utils/` 新原语的 JSDoc 里**写明这三条"为什么不并入"**（否则下一个人会再试图统一一次）

---

## 二、批次与顺序（先易后难 · 先独立后耦合）

| 批次 | 内容 | 特点 | 开始条件 |
| --- | --- | --- | --- |
| **A** | 收口 **6**（历史栈 MAX）· **8**（`sourceTimeAt` 下沉）· **10**（异类登记） | 单文件、低风险、无前置 | 立即可做 |
| **B** | 收口 **1**（抽帧统一）· **7**（编码器探针） | 收益最高；需先只读取证 | A 完成 |
| **C** | 收口 **2/3/4/5**（`base/utils/timeline/` 共用层） | 新建层；不改存量 | A 完成（可与 B 并行） |
| **D** | 收口 **9**（激活位判据单点） | 动全局快捷键守卫 | B/C 完成 |
| **E** | **存量迁移**：把现有 2 份（`VideoProcessNode` / `director3d Timeline`）迁入共用层 | 动已上线节点 + 克制域，**风险最高，可延后到剪辑器开工后** | D 完成 + 剪辑器已用新版 |

> **并行约束**：收口 9 **必须在 G0 之前**（否则 120 C10 的两份判据会先长出来）；其余批次与 G0 无依赖。

---

## 三、完成判据（Definition of Done · 全部达成才进 `123` G0）

> **更新(2026-09-13，按用户拍板修订 DoD 第 1 条)**：收口 2–5 已裁定**推迟到剪辑器 G0**（零真实消费方 = 假接缝，铁律 5）→ 原 DoD 1「份数达标」本轮**不可能也不应达成**。改判：
> **可收口的都已收；不可收口的（零消费方共用层）已裁定 + 已带 G0 硬约束**（`123` §二.3 G1 前置 4 条）。

1. **份数（按裁定修订）**：
   - **可收口的已收** —— 收口 6（历史栈：本无需改造，补契约锁）· 8（源时间映射 3 处 → 1 份）· 1 的 ①（抽帧最完整一份已下沉为唯一真源）· 7（编码器探针 1 处收口为唯一实现）· 9（激活位判据 2 处 + 1 处漏列 → 1 个谓词）。
   - **不可收口的已裁定递延** —— 收口 2–5（共用层，零真实消费方）→ **推迟到 G0，带 4 条硬约束**；收口 1 的 ②–⑤（抽帧其余 4 处）→ **下一批迁移**（不拖到 G0，避免与剪辑器新代码混进同一批文件、无法归因）。
2. **每张卡都有探针证据**（先红后绿）—— ✅ 已完成各卡均有；**例外且已诚实登记**：收口 1 的探针无法让原消费方变红（`VideoProcessNode.test.tsx` 不覆盖抽帧路径）→ **TD-21-2**。
3. **门禁全绿** —— ✅（`type-check` / `check:arch` / `check:catch` / `check:any` / `check:keys` / `test:smoke` / `build` / `lint` + 全量单测 199 文件 2556 例）。
4. **落账**（7.4，不删旧行）—— ✅ `daily/架构日志/债务.md` TD-21-1…4；`124` 份数表就地修正（含"按形态判定份数"的收官）；本文件各卡片就地标注；区域日志 `21-视频剪辑器前置收口-2026-09-13.md`。
5. **反向证明** —— 见 `126` §六 第 5 条（已按本轮范围收窄：已就位项 / 仍需新写项 / 待 G0 迁入项三分）。

---

## 四、本任务书与 `123` / `124` 的关系

```
124（普查：有什么、几份）  ──┐
                             ├─→ 125（本文件：该收哪些、怎么收、什么顺序、怎么验收）
123（施工图：怎么建剪辑器） ──┘        │
                                       └─ 全部收口完成 → 123 的 G0 → G0b(守卫) → G1(core)
```

> **执行顺序铁律**：**收口 → G0 → G1**。收口未完就开 G1，等于在一个已有 N 份实现的仓库上再添一份。
