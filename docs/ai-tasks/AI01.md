# AI01 — @ai-check 函数识别任务（42 个）

> ⚠️ 请勿查看其他 agent 的结果文件。每个函数另有 1 个 agent 独立识别，完成后交叉对比。

## 方法

1. 对每个函数，grep App.js 定义 + 调用点
2. 读懂功能，给出语义化英文名
3. 不确定标注 `(?)`

## 输出格式（提交到下方表格的结果列）

```
# 直接填表，一行一个：
| 1 | vr | processResourceItem | 遍历 resources，调 uploadFile |
| 2 | si | handlePromptApply(?) | 回写节点 data，存疑 |
```

| # | 函数 | 行号 | 你的结果 |
|---|------|------|----------|
| 1 | `vr` | ~L991 | makeThumbnail — 图片→缩略图 dataURL（默认最大边 200px、质量 .6，保持比例；调用点 vr(url,256,.7)） |
| 2 | `yr` | ~L992 | resizeImageToDataUrl — 图片缩放→dataURL（默认最大边 2048、质量 .85，保留 png/webp、jpeg 填白底） |
| 3 | `br` | ~L993 | getImageDimensions — 加载图片返回 {width,height}（naturalWidth/Height） |
| 4 | `xr` | ~L994 | compressImageIfNeeded — 超 max(1000) 则压缩为 blob，否则原样返回 {blob,compressed} |
| 5 | `si` | ~L1193 | EditableNodeTitle — 节点标题编辑器（双击改 label，回写节点 data.label） |
| 6 | `hi` | ~L1839 | RotatingTips — 提示语轮播组件（intervalMs/category，从 promptTips 随机抽一条淡入） |
| 7 | `vi` | ~L1887 | Modal — 通用弹窗组件（createPortal + open/title/onClose/children） |
| 8 | `Xa` | ~L3180 | TextContentViewer — 文本预览（data:text 解码 / http fetch / 原文展示，用于文本素材） |
| 9 | `Za` | ~L3216 | ResourcePicker — 素材选择浮层（素材/生成分页、收藏、文件夹导航、网格预览、音频判定） |
| 10 | `eo` | ~L4076 | CropNode — 图片裁剪节点（imageUrl + onCropComplete/onCancel） |
| 11 | `no` | ~L4179 | clamp — 数值钳制到 [min,max] |
| 12 | `ro` | ~L4180 | normalizeAxisValues — 值数组钳制 [.01,1]→四舍五入 4 位、去重、排序 |
| 13 | `io` | ~L4181 | buildSegments — 在 [0,...vals,1] 相邻值间生成区间对 [t[i],t[i+1]] |
| 14 | `ao` | ~L4182 | makeLassoId — 生成 lasso-<时间戳>-<随机> 形状 id |
| 15 | `oo` | ~L4183 | lassoCursor — 套索光标 SVG 的 CSS cursor 字符串常量 |
| 16 | `so` | ~L4184 | edgeSnapThreshold — 边吸附阈值常量 .04 |
| 17 | `co` | ~L4185 | snapPointToEdge — 点贴近某边时吸附到 top/bottom/left/right |
| 18 | `lo` | ~L4186 | completeLassoCorners — 补顺时针四角点形成闭合多边形路径 |
| 19 | `uo` | ~L4187 | pointsBoundingBox — 点集求包围盒 {minX,minY,maxX,maxY} |
| 20 | `fo` | ~L4188 | cropImageByMask — 按多边形 points 把图片裁剪为 png dataURL（含 onerror 回退） |
| 21 | `po` | ~L4189 | ImageSplitNode — 图片分割节点（grid/manual/lasso 三种模式切分并输出 extractedImages） |
| 22 | `mo` | ~L4190 | makeLayerId — 生成 layer-<时间戳>-<随机> id |
| 23 | `ho` | ~L4191 | loadImageElement — 加载图片为 Image 元素（带跨域 / onerror 回退） |
| 24 | `go` | ~L4192 | drawLayerWithMask — 图层绘到 canvas（有 maskUrl 用 destination-in 抠图） |
| 25 | `_o` | ~L4193 | mergeLayersToImage — 多图层按 zIndex/透明度/位置/旋转合成一张 png |
| 26 | `vo` | ~L4194 | renderLayersScaled — 按比例缩到 maxDim(800) 后合成（缩略图用） |
| 27 | `yo` | ~L5169 | LayerEditor — 图层编辑组件（增删/拖拽/zIndex 排序/橡皮擦/上游图入层） |
| 28 | `xo` | ~L5994 | clamp — 数值钳制到 [min,max]（与 no 同逻辑） |
| 29 | `Co` | ~L5996 | loadImageElement — 加载图片为 Image 元素（与 ho 同逻辑） |
| 30 | `wo` | ~L5997 | drawCanvasBackground — 填充底色/棋盘格并绘制网格线 |
| 31 | `Eo` | ~L5999 | sd2VideoAspectOptions — SD2 视频比例预设数组（16:9/9:16/3:2/2:3/1:1/自定义） |
| 32 | `Do` | ~L6000 | VideoNode — 视频生成节点（prompt/比例/时长/模型，读 VIDEO_* 配置） |
| 33 | `Oo` | ~L7732 | useUploadedAssets — 节点上传素材 hook（上传/重试/状态查询/清空失败） |
| 34 | `Ao` | ~L7799 | SD2VideoNode — SD2 视频生成节点（参考图/提示词/模型/秒数，见 [SD2VideoNode] 日志） |
| 35 | `jo` | ~L7800 | mediaTypeShortLabels — 媒体类型单字标签 {image:'图',audio:'音',video:'视'} |
| 36 | `rs` | ~L9242 | ModelPickerDialog — 选择 AI 模型弹窗（规格对比表格：价格/速度/稳定性/时长/能力） |
| 37 | `as` | ~L9428 | videoResolutionOptions — 视频分辨率选项数组（480p/720p/1080p） |
| 38 | `os` | ~L9429 | DiscountVideoNode — 特惠视频生成节点（discountVideoModel/discountVideoSpecs） |
| 39 | `ss` | ~L9430 | transcribeAudio — 调 Whisper /v1/audio/transcriptions 并按停顿切分时间段 |
| 40 | `cs` | ~L9431 | AudioTranscribeNode — 听音断句节点（连接音频→文本分段，prompt/maxDuration/pauseGap） |
| 41 | `fs` | ~L11362 | MediaVisualizer — 媒体播放器 + Web Audio 频谱可视化（playUrl/loading/error） |
| 42 | `ms` | ~L11525 | CustomApiNode — 自定义 HTTP 接口节点（模板变量 {{x}}/AI 辅助生成配置/resultPath） |

## 参考
- 已有映射格式：`docs/func-mapping.txt` / `docs/var-mapping.txt`
- 查看摘要：`node scripts/summarize.cjs <函数名>`
