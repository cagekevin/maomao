# AI05 — @ai-check 函数识别任务（43 个）

> ⚠️ 请勿查看其他 agent 的结果文件。每个函数另有 1 个 agent 独立识别，完成后交叉对比。

## 方法

1. 对每个函数，grep App.js 定义 + 调用点
2. 读懂功能，给出语义化英文名
3. 不确定标注 `(?)`

## 输出格式（提交到下方表格的结果列）

```
# 直接填表，一行一个：
| 1 | vr | processResourceItem | 遍历 resources，调 uploadFile |
| 2 | si | handlePromptApply(?) | 回写节点 data，存疑 |
```

| # | 函数 | 行号 | 你的结果 |
|---|------|------|----------|
| 1 | `vr` | ~L991 | generateThumbnail | 由 File/Blob/URL 生成 ≤200px JPEG dataURL 缩略图（强制 .6 质量），位于 CustomHandle 内 |
| 2 | `yr` | ~L992 | resizeImage | 等比缩放到 ≤2048px，保留 png/webp，白底填充，返回 dataURL |
| 3 | `br` | ~L993 | getImageDimensions | 加载图片返回 {width,height}，失败 reject |
| 4 | `xr` | ~L994 | compressImageIfNeeded | 超 1000px 才压缩（调 yr），返回 {blob, compressed} |
| 5 | `si` | ~L1193 | EditableNodeTitle | 节点标题栏：双击编辑，写回 node data.label（drag-handle） |
| 6 | `hi` | ~L1839 | RotatingTips | 循环展示提示词技巧（TIPS 轮播，intervalMs 切换） |
| 7 | `vi` | ~L1887 | FullscreenModal | 全屏可拖拽缩放弹窗（Portal，Esc/点空白关闭，默认"编辑输入"） |
| 8 | `Xa` | ~L3180 | UrlTextViewer | 加载 data:text/ 或 http(s) 文本并 whitespace-pre-wrap 展示 |
| 9 | `Za` | ~L3216 | ResourcePicker | 资源选择浮层（素材/生成 tab、收藏、文件夹导航、音频识别、分页） |
| 10 | `eo` | ~L4076 | CropNode | 裁剪节点组件（裁剪框 + 确认/取消，调 onCropComplete 输出裁剪结果）|
| 11 | `no` | ~L4179 | clamp | 数值钳制 Math.max(min, Math.min(max, e)) |
| 12 | `ro` | ~L4180 | normalizeLinePositions | 归一化分割线位置(0.01~0.99)并去重排序 |
| 13 | `io` | ~L4181 | toSegments | 由分割点生成相邻区间段 [[0..e],[e..1]...] |
| 14 | `ao` | ~L4182 | genLassoId | 生成 `lasso-${Date.now()}-${rand}` 套索 id |
| 15 | `oo` | ~L4183 | lassoCursorCss | 套索光标 SVG dataURL 字符串常量 |
| 16 | `so` | ~L4184 | edgeProximityThreshold | 边缘吸附阈值常量 .04 |
| 17 | `co` | ~L4185 | snapPointToEdge | 把 0~1 坐标吸附到最近边（top/bottom/left/right），否则返回原坐标 |
| 18 | `lo` | ~L4186 | insertRectangleCorners | 在起止角点间按矩形顺序补全四个角点 |
| 19 | `uo` | ~L4187 | getPointsBounds | 计算点集包围盒 {minX,minY,maxX,maxY} |
| 20 | `fo` | ~L4188 | cropImageByPolygon | 按多边形点集裁剪图片，返回 PNG dataURL |
| 21 | `po` | ~L4189 | ImageSplitNode | 图片切分节点（网格/手动/套索三种模式，输出子图、titlePattern）|
| 22 | `yg` | ~L28950 | ZoomIndicator | 读取视口缩放系数显示百分比 |
| 23 | `xg` | ~L29054 | EditorCanvas | 主画布编辑器（聚合所有 API 配置、节点、轮询、自定义模板等）|
| 24 | `zg` | ~L36180 | useTaskRefresh | 视频/SD2/特惠/rhWebapp 任务状态刷新轮询 hook，返回 handleRefreshTask |
| 25 | `Vg` | ~L36331 | LoginPanel | 登录面板（手机/密码登录，onLoginSuccess）|
| 26 | `Gg` | ~L36510 | buildLocalServiceUrl | `http://127.0.0.1:${port}${path}` 本地服务地址拼接 |
| 27 | `r_` | ~L36615 | useUpdateController | 检查插件更新 hook，返回 updateInfo/hasUpdate/checkUpdateNow/ignoreUpdateVersion 等 |
| 28 | `s_` | ~L37000 | EndpointSettings | 云端接入点设置面板（切换后重载页面）|
| 29 | `c_` | ~L37087 | LicenseManager | 许可证管理弹窗（创建/管理应用许可证，token 仅展示一次）|
| 30 | `l_` | ~L37348 | AppCenter | 应用中心（浏览已发布工作流应用，公开/私有、运行）|
| 31 | `u_` | ~L37474 | AppRunner | 应用运行视图（加载应用、填启动参数、注入画布运行）|
| 32 | `g_` | ~L37669 | ChangePasswordModal | 修改/设置密码弹窗（校验强度与两次一致）|
| 33 | `C_` | ~L37871 | ModelScheduleManager | 模型调度管理面板（列表 + 新建/编辑/删除/启停）|
| 34 | `w_` | ~L37872 | ScheduleCard | 单个调度组卡片（步骤链路、重试次数、启用/停用、编辑/删除）|
| 35 | `P_` | ~L38312 | taskTypeMeta | 任务类型 → {label, icon} 映射对象（text/image/video/sd2Video/...）|
| 36 | `F_` | ~L38313 | premiumVideoTypeSet | Set(['discountVideo','sd2Video']) 特惠/SD2 视频类型集合 |
| 37 | `H_` | ~L38597 | MemoTaskCard | TaskCard 的 Y.memo 包装（同 B_）|
| 38 | `av` | ~L39162 | TaskCenter | 任务中心抽屉（状态/类型/搜索筛选、分页、可拖拽宽度）|
| 39 | `hv` | ~L39584 | PublishAppModal | 发布为应用弹窗（名称/可见性/描述/启动参数筛选）|
| 40 | `gv` | ~L39992 | ShareAppModal | 分享应用弹窗（访问链接复制、私有应用生成许可证 token）|
| 41 | `_v` | ~L40196 | LocalEngineOfflineModal | 本地引擎未连接提示弹窗（安装指引 + 重试连接）|
| 42 | `jv` | ~L40280 | TextContentViewer | 加载并展示文本/URL 内容（与 Xa 逻辑近似重复）|
| 43 | `Nv` | ~L40321 | App | 根组件（聚合 toast、localTool、资源/设置/各面板状态）|

## 参考
- 已有映射格式：`docs/func-mapping.txt` / `docs/var-mapping.txt`
- 查看摘要：`node scripts/summarize.cjs <函数名>`
