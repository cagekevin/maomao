# AI02 — @ai-check 函数识别任务（42 个）

> ⚠️ 请勿查看其他 agent 的结果文件。每个函数另有 1 个 agent 独立识别，完成后交叉对比。

## 方法

1. 对每个函数，grep App.js 定义 + 调用点
2. 读懂功能，给出语义化英文名
3. 不确定标注 `(?)`

## 输出格式（提交到下方表格的结果列）

```
# 直接填表，一行一个：
| 1 | vr | processResourceItem | 遍历 resources，调 uploadFile |
| 2 | si | handlePromptApply(?) | 回写节点 data，存疑 |
```

| # | 函数 | 行号 | 你的结果 |
|---|------|------|----------|
| 1 | `mo` | ~L4190 | genLayerId — 生成 `layer-<ts>-<rand>` 图层 id |
| 2 | `ho` | ~L4191 | loadImage — 以 crossOrigin 加载 Image，失败重试无 CORS，返回 Image 元素 |
| 3 | `go` | ~L4192 | renderLayerWithMask — 加载图+mask，canvas 用 destination-in 合成蒙版，返回 canvas |
| 4 | `_o` | ~L4193 | composeLayers — 按 zIndex 合成所有可见图层+背景 → PNG dataURL |
| 5 | `vo` | ~L4194 | composeLayersPreview — 超 800px 下采样后调 _o 生成预览图 |
| 6 | `yo` | ~L5169 | LayerEditor — 图层编辑组件（移动/缩放/旋转/擦除/多图层） |
| 7 | `xo` | ~L5994 | clamp — 数值钳制 max(t,min(n,e))（与 `no` 同体重复） |
| 8 | `Co` | ~L5996 | loadImage — 同 `ho` 的图片加载（CORS+重试） |
| 9 | `wo` | ~L5997 | drawBackground — 填充背景色/透明棋盘格 + 可选网格线 |
| 10 | `Eo` | ~L5999 | videoAspectRatioOptions — 视频比例选项（16:9…/自定义，含 defaultSize） |
| 11 | `Do` | ~L6000 | VideoGenNode — 视频生成节点（prompt/size/seconds/model/apiFormat） |
| 12 | `Oo` | ~L7732 | useUploadedAssets — 上传素材状态 hook（upload/retry/status） |
| 13 | `Ao` | ~L7799 | Sd2VideoNode — SD2 图生视频节点（含 useUploadedAssets） |
| 14 | `jo` | ~L7800 | mediaTypeShortLabels — 媒体类型短标签 {image:'图',audio:'音',video:'视'} |
| 15 | `rs` | ~L9242 | ModelSelectModal — "选择 AI 模型"弹窗（表格展示规格/价格/速度） |
| 16 | `as` | ~L9428 | videoResolutionOptions — 视频分辨率选项（480p/720p/1080p） |
| 17 | `os` | ~L9429 | DiscountVideoNode — 优惠/折扣视频生成节点 |
| 18 | `ss` | ~L9430 | transcribeToSegments — Whisper 转写 word→按停顿/时长分镜字幕段 |
| 19 | `cs` | ~L9431 | SubtitleNode — 字幕生成节点（接上游音频，配 prompt/时长/停顿） |
| 20 | `fs` | ~L11362 | AudioPlayer — 音频播放器 + 频谱可视化（AnalyserNode + RAF） |
| 21 | `ms` | ~L11525 | HttpRequestNode — API/HTTP 请求节点（url/method/body/{{var}}/resultPath） |
| 22 | `hs` | ~L11526 | getNodeText — 从节点 data 提取文本表示（prompt/text/result…） |
| 23 | `gs` | ~L11527 | getNodeMedia — 从节点 data 提取 images/videos/audios URL（多节点类型） |
| 24 | `_s` | ~L11528 | urlToBase64 — fetch URL → FileReader 转 dataURL(base64) |
| 25 | `vs` | ~L11529 | rhWebappRunRequestEvent — 常量字符串 'rhwebapp-run-request'（事件/通道名） |
| 26 | `Ps` | ~L12771 | GifLibrary — gif.js 库对象（Ps.GIFEncoder/quantize/applyPalette），Ps=B() |
| 27 | `id` | ~L20205 | ObjectListPanel — 3D 导演台对象浏览器（角色/群众/几何/模型/相机分组） |
| 28 | `md` | ~L20555 | useUndoBatch — 包装 beginUndoBatch/endUndoBatch 的 hook |
| 29 | `_d` | ~L20638 | InspectorSelect — 属性面板下拉选择字段 |
| 30 | `yd` | ~L20735 | AxisDragInput — 属性面板可拖拽轴输入（X/Y/Z 拖动改值） |
| 31 | `bd` | ~L20797 | InspectorRangeField — 属性面板滑块+数字组合字段 |
| 32 | `Rd` | ~L21461 | CharacterInspector — 角色/群众属性面板（属性/姿势页签） |
| 33 | `Zd` | ~L21931 | SceneInspector — 3D 场景属性面板（scale/全景/地面高度） |
| 34 | `wf` | ~L22863 | RigPreview — 素体/骨架 3D 预览（BodyConfigPanel 内使用） |
| 35 | `Gf` | ~L22963 | TransformGizmo — 包装 TransformControls 变换控制器（带撤销） |
| 36 | `ap` | ~L23207 | NormalizedObject3D — 克隆对象并按包围盒归一化居中显示 |
| 37 | `dp` | ~L23345 | CompositeModel3D — 多 shape 组合模型渲染（composite-model） |
| 38 | `fp` | ~L23360 | CenterGroup — 将子元素按包围盒居中到原点的 group |
| 39 | `pp` | ~L23382 | Object3DItem — 场景单个对象 3D 渲染（模型/角色/道具+变换+标签） |
| 40 | `mp` | ~L23484 | CrowdTransform3D — 群众（crowd）变换控制器（Gf） |
| 41 | `hp` | ~L23518 | MultiObjectTransform3D — 多选对象统一变换控制器 |
| 42 | `_p` | ~L23582 | CameraObject3D — 相机对象 3D 渲染 + 变换（含 target 计算） |

## 参考
- 已有映射格式：`docs/func-mapping.txt` / `docs/var-mapping.txt`
- 查看摘要：`node scripts/summarize.cjs <函数名>`
