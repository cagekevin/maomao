# AI04 — @ai-check 函数识别任务（43 个）

> ⚠️ 请勿查看其他 agent 的结果文件。每个函数另有 1 个 agent 独立识别，完成后交叉对比。

## 方法

1. 对每个函数，grep App.js 定义 + 调用点
2. 读懂功能，给出语义化英文名
3. 不确定标注 `(?)`

## 输出格式（提交到下方表格的结果列）

```
# 直接填表，一行一个：
| 1 | vr | processResourceItem | 遍历 resources，调 uploadFile |
| 2 | si | handlePromptApply(?) | 回写节点 data，存疑 |
```

| # | 函数 | 行号 | 你的结果 |
|---|------|------|----------|
| 1 | `vp` | ~L23672 | DirectorSceneObjects \| 3D 导演台场景对象组：渲染 project 内 objects/crowds/cameras（含地面、选中、snapToGrid） |
| 2 | `Cp` | ~L23806 | ViewportAspectOverlay \| 视口比例取景遮罩（安全区/三分线，Sp 算 frame 尺寸） |
| 3 | `jp` | ~L24010 | AIModelGeneratorPanel \| 导演台 AI 模型生成面板（文本模型选择，ai-model-generator-panel） |
| 4 | `vh` | ~L25768 | useViewportCapture \| 注册视口捕获处理器 wd（当前/四方位/十二方位机位截图，gh 出图） |
| 5 | `yh` | ~L25831 | useControlsSnapshotSync \| director 模式下把 snapshot.target 同步到 OrbitControls |
| 6 | `bh` | ~L25847 | CameraSnapshotGizmo \| 相机快照指示 gizmo（写 snapshot.target，渲染 cn 轴标 et） |
| 7 | `Sh` | ~L25922 | DirectorViewport \| 3D 导演台视口：Canvas/全景/工具条/场景组容器 |
| 8 | `wh` | ~L26087 | Director3DContent \| 导演台主内容：载入工程/全景、键盘快捷键（删除/复制/粘贴/撤销）、捕获 |
| 9 | `Ih` | ~L26794 | ImageGalleryEditor \| 图片集节点编辑器：images 增删/缩略图/拖拽/粘贴/发送剪映，回写节点 |
| 10 | `Lh` | ~L26795 | bgColorPresets \| 背景色预设数组（含 rgba 半透明转换函数） |
| 11 | `zh` | ~L26797 | fontSizeOptions \| 字号选项数组 [10,12,14,18,24,32,48,64,80,96,128] |
| 12 | `Bh` | ~L26798 | emojiIcons \| 装饰 emoji 图标数组（➡️⬅️⬆️⬇️✅❌⭐…） |
| 13 | `Uh` | ~L27557 | TextOverlayEditor \| 文字/HTML 贴图编辑器：html/字号/加粗/颜色/底色/宽高，回写节点 |
| 14 | `Gh` | ~L27897 | ImageAnnotateEditor \| 图片标注/铅笔编辑器：pencil/颜色/笔刷/zoom/getImageData |
| 15 | `sg` | ~L28624 | ResourceLibraryModal \| 资源库浏览弹窗：mine/all、分类/关键词、拖拽入画布（ig 模板 MIME） |
| 16 | `lg` | ~L28797 | isBlockedError(?) \| 错误文案匹配（余额/算力/审核/违规/涉黄…），5 份同义副本 |
| 17 | `ug` | ~L28798 | isBlockedError(?) \| 同上（与 lg 字节相同） |
| 18 | `dg` | ~L28799 | isBlockedError(?) \| 同上 |
| 19 | `fg` | ~L28800 | isBlockedError(?) \| 同上 |
| 20 | `pg` | ~L28801 | isBlockedError(?) \| 同上 |
| 21 | `_g` | ~L28948 | GlobalStyles \| 全局样式注入：隐藏 react-flow 署名、LOD 降级、平移性能模式 |
| 22 | `yg` | ~L28950 | ZoomPercent \| 缩放百分比显示（sn(transform[2])→Math.round*100%） |
| 23 | `xg` | ~L29054 | CanvasEditor \| 主画布编辑器：ReactFlow、节点面板、工具栏（约 150k 字符） |
| 24 | `zg` | ~L36180 | refreshTaskStatus \| 任务状态刷新/轮询（sd2Video/video/discountVideo/rhWebapp） |
| 25 | `Vg` | ~L36331 | LoginForm \| 登录面板（手机验证码/账号密码切换，Ta 取用户信息） |
| 26 | `Gg` | ~L36510 | buildLocalUrl \| 本地服务 URL 构造 `http://127.0.0.1:${e}${t}` |
| 27 | `r_` | ~L36615 | useUpdateCheck \| 插件/版本更新检查 hook（Ng 返回 hasUpdate/checkUpdateNow/dismiss…） |
| 28 | `s_` | ~L37000 | EndpointSettings \| 云端接入点设置（Vn/Bn 选点，zn 保存后重载页面） |
| 29 | `c_` | ~L37087 | LicenseManagerModal \| 工作流应用许可证管理弹窗（/workflow-apps/licenses/mine） |
| 30 | `l_` | ~L37348 | AppCenter \| 应用中心：浏览已发布工作流应用（含许可证管理入口） |
| 31 | `u_` | ~L37474 | AppRunner \| 工作流应用运行器：拉取 schema、保存快照、派发运行事件 |
| 32 | `g_` | ~L37669 | ChangePasswordForm \| 修改密码表单（/user/change-password，m_/h_ 校验） |
| 33 | `C_` | ~L37871 | ModelSchedulePanel \| 模型调度面板（la 列表/新建/删除/启停，S_ 编辑） |
| 34 | `w_` | ~L37872 | ArrowSeparator \| 箭头分隔符组件（渲染 →） |
| 35 | `P_` | ~L38312 | serializeBlob \| Blob 序列化（转 {_blob,name,size,type}） |
| 36 | `F_` | ~L38313 | serializeBlob \| 同上（与 P_ 字节相同副本） |
| 37 | `H_` | ~L38597 | TaskListDrawer \| 任务列表抽屉：状态/类型筛选、搜索、分页（q_/rv/iv） |
| 38 | `av` | ~L39162 | TaskListDrawer \| 同上（与 H_ 字节相同副本） |
| 39 | `hv` | ~L39584 | WorkflowAppPublishModal \| 工作流应用发布弹窗：读当前画布图、可见性 private/public |
| 40 | `gv` | ~L39992 | AppLicenseManager \| 应用许可证签发/管理：创建 token、复制、按 appId 列出 |
| 41 | `_v` | ~L40196 | LocalEngineOfflineModal \| 本地引擎未连接提示弹窗（local-companion 安装/启动指引） |
| 42 | `jv` | ~L40280 | TextContentViewer \| 文本内容查看器：解码 data:URL / fetch 文本并 pre-wrap 展示 |
| 43 | `Nv` | ~L40321 | SettingsPanel \| 设置主面板：云存储配置、引擎状态、任务等（约 153k 字符） |

## 参考
- 已有映射格式：`docs/func-mapping.txt` / `docs/var-mapping.txt`
- 查看摘要：`node scripts/summarize.cjs <函数名>`
