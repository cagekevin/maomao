# 28-R8 · daily/ 历史 bug 兜底核对（可执行）

> 配套售后计划：`docs/27-API中转层售后收口计划-2026-08-22.md`（R8）。
> 定位：复查 `daily/` 里所有未闭合的契约/持久化风险，确认是否已解决或需跟进。低价值，非阻塞。

---

## 1. 现状

`daily/` 有 14 个实战 bug 记录（3 根目录 + 11 个 `1mao-daily/`）。此前 API 审计已确认**大部分已吸收进 PRD**（B0-B3 实施时）。**未闭合的少数项**：

| daily 文件 | 风险 | 状态 |
|---|---|---|
| `1mao-daily/2026-07-31.md`（upsertTask 伪 upsert 抹库） | PATCH 抹掉已落库诊断字段 | **已修复**（tasks.ts 已改真 UPSERT `ON CONFLICT ... DO UPDATE`，见 tasks.ts:70-83） |
| `1mao-daily/2026-08-02.md`（出站代理） | localTool 原生 fetch 不继承系统代理 | **已修复**（`netProxy.ts` fetchWithProxy） |
| `1mao-daily/2026-08-07.md`（垫图失败静默丢图） | 垫图失败静默 | 需核对是否已闭环 |
| `1mao-daily/2026-08-14.md`（生成图偶发不落盘） | 丢图 | 需核对 |

---

## 2. 目标

通读 `daily/` 14 个文件，对每个契约/持久化风险标注状态：`✅已修复 / 🔬待核 / ❌未解决`。

---

## 3. 执行步骤

1. 逐个读 `daily/*.md` + `daily/1mao-daily/*.md`
2. 每个风险点核对：当前代码是否已解决（grep 关键实现）
3. 输出状态表，未解决项单列待办

---

## 4. 验收

- [ ] 14 个文件全部通读
- [ ] 每个风险点有明确状态标注
- [ ] 未解决项列出（若无，则确认全闭合）

---

## 5. 文件清单

| 文件 | 改动 |
|---|---|
| `docs/27-...` R8 | 完成后标 ✅ |
| 本核对结果 | 可并入 CONTEXT 或单独记录 |

---

## 5.1 代码审计修订（2026-08-22，code-explorer 实测）

> 实际通读 `daily/` 14 文件 + 核查代码后修正状态：

- `daily/` 文件数 **14**（3 根 + 11 `1mao-daily/`）属实 ✅。
- **a. upsertTask 伪 upsert**（tasks.ts:70-83 `ON CONFLICT...DO UPDATE`）**✅ 已修复** 属实，行号准确。
- **b. 出站代理**（`netProxy.ts:276-321 fetchWithProxy`）**✅ 已修复** 属实。
- **c. 垫图静默丢图**（2026-08-07）：网关侧 `apimart-gateway/main.py:612/711-717` "垫图不齐即阻断 502" **已闭环（代码落地）**；但该 daily 另记 `resources.ts` 无列名白名单"暂不修"，且需重启网关 9004 生效。
- **d. 生成图偶发不落盘**（2026-08-14）：**❌ 仍未解决（仅日志留痕）**。根因 `files.ts:96 saveRemoteUrl` 远程 CDN 下载转存失败→降级保留远程 URL→磁盘无文件。可选修复（前端 toast / 后端重试）均**未实施**，仅 `files.ts:124-127` 加 `[download]` 日志 + `db-query.mjs --lost-check` 可追溯。**文档原"需核对"中性标注会误导，应标 ❌未解决 / 单列新任务**。

**修订后 4 项状态**：a ✅ / b ✅ / c ⚠️网关侧已闭环 / d ❌未解决（待实施）。执行 R8 时 d 必须单列跟进任务，不可并入"已闭合"。

## 6. 边界

- **低价值**：非阻塞，可在改相关模块时顺手核对
- 不主动修 bug，只核对状态；发现未解决项，单列新任务
