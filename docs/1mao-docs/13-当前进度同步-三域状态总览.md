# 13 · 当前进度同步 — 三域状态总览（git 实证版）

> 更新日期：2026-07-31
> 校验方式：本表每条「已修」均对应 `git log` 中的真实 commit（短哈希见各条）。
> 未提交的改动仅有 `daily/2026-07-31.md`（修改）与 `docs/12-*.md`（未跟踪），均为日志/文档，不含功能代码。
> 注：localTool 当前分支领先 origin/main 1 个 commit（`e15d502`，仅清理 script/ 调试产物 + 文档重命名），功能修复均在 `origin/main` 及更早 commit。

---

## 〇、证据来源（git 实据）

```
localTool/src:
  41bb2b8 fix(P0-1): /api/proxy 解除15s硬超时 → PROXY_TIMEOUT(默认300s)
  647c2dc fix(P0-2): /api/proxy 支持 SSE 流式转发
  5ee63db fix(P0-3): /api/proxy 协议翻译层 — 剥 {code,data} 信封
  d0ff986 fix(L1): kv/delete
  fd751fc fix(L3): 版本号统一 1.4.2
  4ae63a9 fix(S1): upload/thumbnail 返回绝对 URL
  6999993 fix(高-2+A2+L5): 画布宿主 + sync/default兜底 + assets别名 + 自动开浏览器
  2db5171 fix: 视频生成失败修复（system.ts，普通视频节点+sd2Video）
  891b7de feat: 网关原生同步/异步双模，localTool退化直传去轮询

apimart-gateway/main.py:
  20e7d42 fix(G1/G2/G3): 视频轮询 + draw 端点
  2db5171 fix: 视频生成失败修复（main.py，顶层 video_url 映射）
  891b7de feat: 网关同步/异步双模(wait开关)
  （chat/completions、images/generations|edits、G4 uploads、G5 gateway/*、G6 audio 在基线 035ed2c 已含，未见后续改动）

launch-all.ps1:
  b0d3bc6 新增 launch-all.ps1（合并启动器）

.gitignore:
  e2e25ef security: .env 从版本控制移除（含 AK/SK）
  localTool/dist 亦被 gitignore（构建产物）
```

---

## 一、A 域 · 官方平台（`www.1mao.cc` 等原版服务器）

| 断点 | 编号 | 现状 | 证据 / 说明 |
|---|---|---|---|
| 账号登录 | — | ✅ 通 | 真实一毛账号即走官方 |
| 云端同步按钮（上传/下载云端） | 10号文档 | ⚠️ 仍直连官方 | `/api/sync/upload\|download` 打 `www.1mao.cc`；自研未实现，需真实账号 |
| `/api/sync/default`（配置下发） | A2 | ✅ 本地兜底 | commit `6999993`（localTool 读基线 JSON 替换 `VITE_API_BASE_URL`） |
| 画布助手 agent chat | A1 | ⚠️ 仍直连官方 | 未走网关 |
| workflow-apps 市场 | A3/L6 | ⚠️ 可选 | 官方市场未接；本地 by-project 走 localTool 空壳（`6999993` 的 platform 处理） |
| 视频基址 kkidc | A5 | ✅ 已改道（经中继透传 kkidc，未统一到 Lovart 网关） | commit `2db5171`：local-tool 代理模式下视频经 `localTool/api/proxy`**原样透传 kkidc**（localTool 仅转发、不转 Lovart 协议；kkidc 字面量未动，但由 localTool 代发到 kkidc，故浏览器不直接命中；kkidc 可达即通。详见 docs/12 §4.3） |
| 硬编码兜底 IP `154.219.102.152` | A6 | ❌ 仍有 | 读 `dist/assets/endpointConfig-*.js`：`s()` 仍返回该兜底项，未见 commit 改动 |

---

## 二、B 域 · localTool（`127.0.0.1:18080`）

| 断点 | 编号 | 现状 | 证据 commit |
|---|---|---|---|
| `/api/kv/delete` | L1 | ✅ 已修 | `d0ff986` |
| 前端页面托管（画布宿主） | L2 | ✅ 已修 | `6999993` |
| manifest.version | L3 | ✅ 已修 | `fd751fc`（三处统一 1.4.2） |
| `/api/sync/default` | L4/A2 | ✅ 已修 | `6999993` |
| `/api/assets/upload` `/api/upload/app-asset` | L5 | ✅ 已修 | `6999993`（alias 到 handleUpload） |
| `/api/workflow-apps/by-project` | L6 | ✅ 已修（空壳） | `6999993` 附带 platform 处理 |
| 合并启动器 | I1 | ✅ 已修 | `b0d3bc6`（launch-all.ps1） |
| localTool 未预编译 | I3 | ⚠️ 见注 | `localTool/dist` 被 gitignore（构建产物）；磁盘现有 `dist/index.js`，但**是否反映最新 src 需重新 `npm run build` 确认** |
| `/api/proxy` 三缺陷（P0） | — | ✅ 全修 | `41bb2b8`(超时) + `647c2dc`(SSE) + `5ee63db`(信封翻译) |
| upload/thumbnail 相对路径 | S1 | ✅ 已修 | `4ae63a9` |
| ffmpeg 缩略图 | P2 | ⚠️ 未修 | `system.ts` 仍 `ffmpeg:false`，thumbnail 降级（非阻塞） |
| jianying 剪映发送 | — | ⚠️ stub | `index.ts` 空实现（非关键路径） |

---

## 三、C 域 · apimart-gateway（`127.0.0.1:9004`）

| 断点 | 编号 | 现状 | 证据 / 说明 |
|---|---|---|---|
| Lovart 凭据 | — | ✅ 已填真值 | `.env` 当前含真实 `ak_ff40…`/`sk_63a1…`，`OPEN_RELAY=true` 等；**但 `.env` 被 gitignore（commit `e2e25ef` 移出版控），此结论来自读当前文件，非 git 可溯** |
| `/v1/chat/completions`（流式） | — | ✅ 已实现 | 基线 `035ed2c` 已含，未见后续改动 |
| `/v1/images/generations` `/edits` | — | ✅ 已实现 | 含异步轮询补齐 `49d0f45`（P0-4） |
| `/v1/video/generations` + 别名 + `/v1/videos` | — | ✅ 已实现 | commit `2db5171` |
| `GET /v1/videos/{id}` | G1 | ✅ 已修 | `20e7d42` |
| `GET /v1/video/generations/{id}` | G2 | ✅ 已修 | `20e7d42` |
| `POST /v1/draw/completions` | G3 | ✅ 已修 | `20e7d42` |
| `POST /v1/uploads/images` + `/v1/gateway/upload` | G4 | ✅ 已实现 | 基线 `035ed2c` 已含 |
| `/v1/gateway/generate` `/v1/gateway/task/{id}` | G5 | ✅ 已实现 | 基线 `035ed2c` 已含；base 级 `/api/ai-apps` 应用市场仍缺失 |
| `/v1/audio/transcriptions` | G6 | ❌ 501 | 当前 `main.py` 返回 501，语音转写不可用 |
| webhook 监听 / confirm / project 缓存 | — | ✅ 已实现 | 基线 `035ed2c` 已含 |
| `verify_gateway.py` 覆盖度 | — | ⚠️ 待补 | 自检脚本未覆盖 G1/G2/G3（路由已实，仅脚本未跟进） |

---

## 四、剩余待办（按优先级）

### 阻塞/重要
1. **官方云端同步自研化**（可选）：需 localTool 实现 `/api/sync/upload\|download`；当前设计依赖官方账号。
2. **G6 语音转写 501**：`/v1/audio/transcriptions` 返回 501。
3. **base 级 `/api/ai-apps` 应用市场**：G5 网关级已通，根路径市场入口缺失。

### 次要 / 非阻塞
4. **A6 硬编码兜底 IP**：`endpointConfig` 仍含 `154.219.102.152:3012`，建议清理。
5. **I3 dist 重建**：`localTool/dist` 被 gitignore；当前磁盘有 `index.js`，但改动 src 后须 `npm run build` 才能生效（否则跑的是旧编译）。
6. **ffmpeg 缩略图（P2）**：`ffmpeg:false` 仅无缩略图，非阻塞。
7. **jianying 剪映发送**：stub 空实现，非主链路。
8. **verify_gateway.py 补 G1/G2/G3**：自检脚本跟进。

---

## 五、如何复核（每条都能 git 对账）

```powershell
# 核对 localTool 修复
git show 41bb2b8   # P0-1 超时
git show 647c2dc   # P0-2 SSE
git show 5ee63db   # P0-3 信封
git show d0ff986   # L1 kv/delete
git show 6999993   # L2/A2/L5 宿主/sync/assets
git show 2db5171   # 视频改道

# 核对网关修复
git show 20e7d42   # G1/G2/G3
git show 2db5171   # 视频 video_url 映射

# 核对凭证是否在版控（应被忽略）
git check-ignore apimart-gateway/.env   # 输出路径=已被忽略
```

---

## 附：与旧文档对照

- `02-断点三域梳理.md`：全部断点规划基线（含 P0-1~4、L1~6、A1~6、G1~6、I1~3、S1~2）。
- `10-头像云端同步-上传下载说明.md`：sync 按钮走官方，自研不实现（设计说明）。
- 本文（`13`）：**仅保留 git 实证过的状态**，每条「已修」附 commit 哈希，待办 8 项均为未提交/未实现项。
