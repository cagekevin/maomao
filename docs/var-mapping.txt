# App.js 混淆变量 → 可读名称 映射表
# 格式：混淆名 = 可读名（含义）
# 用途：后续脚本批量替换
# ⚠️ 跨包声明：本表已混入 engine 包符号。仅存在于 dist/assets/engine-COXkPi7B.js 的符号：
#    Bl(=defaultCameraConfig)、Oc(=offscreenCanvasB)、clPen(CSS @keyframes 动画名，非 JS 变量)、Vn(=getApiBaseUrl，疑在 engine)。
#    批量替换脚本须同时作用于 App.js 与 engine 两个包，否则这些符号会漏替换/报错。

# ── GAS 云端同步相关（本次改造）──
# ⚠️ 2026-07-23 更正（commit 176437b）：GAS push 函数已从 `ei` 语义化改名为 `syncToCloud`。
#    原文档"ei = pushToCloud"已作废：当前 App.js 内 `ei` 仅指"节点打组"函数，与 GAS 无关。
syncToCloud = pushToCloud    # 推送到云端（GAS push）🟢  [App.js L41365，原 `ei` 改名]
ti = pullFromCloud           # 从云端拉取（GAS pull）🟢  [同名遮蔽·GAS 义 App.js L41415]

# ⚠️ ei/ti 同名遮蔽真实状态（核对 App.js @176437b，行号随构建漂移，以语义名为准）：
#   App.js 内 `ei` 当前只有 1 个真身：节点打组 `ei = Y.useCallback`（≈L34671）。
#     —— 旧文档称"ei@L1853 = var ei = new Map() 缩略图缓存"经 grep 源码 0 命中，系旧构建快照误传，已删除该义。
#   App.js 内 `ti` 当前有 2 个真身：
#     ti@≈L34709 = Y.useCallback（清缓存，非 GAS 义）
#     ti@≈L41415 = async（GAS pullFromCloud，即上条 pullFromCloud）
#   凡引用必带 @Lnnnn 行号 + 语义名区分，勿只写 `ei`/`ti`。
ei = groupNodesCallback      # 节点打组 Y.useCallback（App.js ≈L34671，唯一真身）[同名遮蔽]
ti@L34709 = clearCacheCallback  # Y.useCallback（清缓存，非 GAS 义）[同名遮蔽·前端义]
ti@L41415 = pullFromCloud       # 从云端拉取（GAS pull）[见上条]

Qr = isSyncing             # 同步中 loading 状态 🟢
$r = setSyncing            # setSyncing setter 🟢

# ── 通用 UI ──
Br = showToast             # Toast 提示 🟢
X = jsxRuntime             # React JSX 工厂（jsx 函数） 🟢
Y = React                  # React 🟢

# ── 存储 ──
Q = StorageManager         # 存储管理器（编排器）：getConfig/setConfig/getObject/setObject/remove/getMultiple/has/syncToLocalTool/syncAllToLocalTool/saveCanvasState/loadCanvasState/saveCanvasStateWithVersion/migrate；后端 wr/Mr/Nr/Pr，@1441 🟢
Z = StorageKeys            # 存储键枚举：APP_SETTINGS/USERS/MEMBERSHIP/PROJECTS/CUSTOM_NODE_TEMPLATES/MODEL_SCHEDULES/CLOUD_STORAGE_CONFIG/TRANSIT_RESOURCES/GLOBAL_TASKS/CANVAS_STATE_PREFIX='canvas-state-v1-'/DEVICE_ID/CURRENT_USER_ID/AUTH_TOKEN…，@1233 🟢

# ── API 客户端 ──
Mg = apiClient             # 原版官方 API 客户端（post/get/getCurrentToken） 🟢

# ── 模型调度 ──
la = getModelSchedules      # 获取模型调度配置 🟢
Sa = setModelSchedules      # 设置模型调度配置 🟢
bi = modelCatalogData        # 模型目录/权益数据对象（Fi 返回；getModelPower/Unit/Entitlement/Currency/DurationSpec 等消费） 🟢

# ── 设置面板 ──
Ee = settingsTab           # 当前设置子 Tab（builtin/models/basic/data/upgrade/endpoint） 🟢
De = setSettingsTab        # setSettingsTab setter 🟢

# ── 版本升级相关 ──
Vr = updateController      # 升级控制器（r_ 函数返回值，含 hasUpdate/updateInfo 等） 🟢
i_ = UpgradeNavButton      # 设置左侧"版本升级"导航按钮组件 🟢
o_ = UpgradePanel          # 升级面板内容组件 🟢
a_ = UpdateBanner          # 顶栏"发现新版本"横幅组件 🟢
L = UpgradeIcon            # 升级图标（lucide） 🟢

# ── 图标 ──
jn = UploadIcon            # 上传图标 🟢
mn = DownloadIcon          # 下载图标 🟢
Nn = SpinnerIcon           # 加载旋转图标 🟢

# ── 登出 / 下载（同名遮蔽·双绑定）──
Zr = logout                # 登出函数（已改文案为"重置配置"）@L43893 🟢
Zr@L1827 = downloadAndSave # ⚠️ AI13 补录：下载/URL上传函数，resourceAdded@L43539 调用（var-mapping 原漏收此绑定；07-3/08-5 裁决）

# ── 账号 / 登录相关 ──
Ne = isLoggedIn             # 是否已登录（去登录改造后始终 true） 🟢
Ie = userInfo               # 用户信息对象（nickname/username/phone/avatar/balance） 🟢
Un = membershipInfo          # 会员信息（type/expiry） 🟢
Pe = avatarMenuOpen          # 头像菜单打开状态 getter 🟢
Le = setAvatarMenuOpen       # 头像菜单打开状态 setter 🟢
ke = openLoginModal          # 打开登录弹窗 🟢
je = openPasswordModal       # 打开修改密码弹窗 🟢
Mv = defaultAvatarDataUri    # 默认头像 SVG data URI 🟢
Fn = LoginIcon               # 登录图标（lucide） 🟢
We = PasswordIcon            # 修改密码图标（lucide） 🟢
we@L43015 = rescanResources   # ⚠️ 同名遮蔽：rescan 真身（非 We=PasswordIcon）；handleResourcesRescan 真函数 Ev@L42883 同义（AI13 M1/07-2）
kt = CrownIcon               # 会员皇冠图标（lucide） 🟢

# ── 节点组件 (Node components, Y.memo / forwardRef) ──
li = ImageNode             # 图片节点（imageUrl/thumbnailUrl，自动识别 image/video/audio/text） 🟢
Ya = PromptNode            # 提示词节点（prompt/aspectRatio/imageSize，调用 la() 模型） 🟢
Qa = TextNode              # 文本节点（prompt/text/autoSplit/presetPrompts/selectedModel） 🟢
ps = AudioNode             # 音频节点（audioUrl，上游取音频） 🟢
Ms = RhWebappNode          # AI 应用节点（webappId/aiAppApiUrl/schema/values） 🟢
Gs = VideoToGifNode        # 视频转 GIF 节点（fps/maxSize/colors/speed） 🟢
nc = ImageCompressNode     # 图片压缩节点（maxSize/quality/format/targetKB） 🟢
Cc = FaceMosaicNode        # 人脸打码节点（mode/strength/color） 🟢
Lc = GridSplitNode         # 图片切分节点（split/orientation） 🟢
Wc = UrlToImageNode        # URL 转图片节点（inputUrl，上游取 http 文本） 🟢
Jc = PanoramaNode          # 全景图节点（url/panoType/fov，Three.js 捕获） 🟢
_r = CustomHandle          # 自定义连线手柄（cust-handle，含 lodLevel/位置） 🟢
gi = AutoTextarea          # 自适应高度 textarea（forwardRef，强制 resize:none） 🟢
_i = ResizableBox          # 可拖拽缩放容器（targetRef/onResizeEnd/min-max 宽高） 🟢
Tf = ErrorBoundary         # React 错误边界类组件（getDerivedStateFromError） 🟢
Hg = ToastContainer        # Toast 容器（按 success/error/warning/info 渲染图标） 🟢
cg = ImageLightbox         # 图片灯箱/预览（缩放、Esc 关闭） 🟢
Wh = ImageEditor           # 图片标注编辑器（铅笔工具、颜色、笔刷） 🟢
Ph = genImageId            # 生成图片 id（'img-...'） 🟢
Fh = LazyImage             # 懒加载图片组件（Y.memo；IntersectionObserver rootMargin 120px 进视口才挂载 <img>，img 带 loading:'lazy'/decoding:'async'/draggable:false；props src/alt/className/onDoubleClick；@28875 声明，@29450 <Fh src={t.thumb||t.url}> 调用）注：'LazyImage' 是本表赋予的语义名，源码无此字符串字面量，故 dist 搜不到属正常 🟢
jh = CustomEdge            # 自定义连线组件（Y.memo(Ah)） 🟢
S_ = ModelCatalogEditor    # 模型目录/清单编辑器（catalog/initial/onSave） 🟢
M_ = ModelBrowser          # 模型浏览器（按分类筛选+搜索） 🟢
B_ = TaskCard              # 任务卡片（task/useThumbnail/onRefresh/onDelete） 🟢
gg = getDemoNodes          # 返回 demo 画布节点（promptNode 示例） 🟢
vg = LodObserver           # 视口 LOD 观察组件（监听 react-flow transform） 🟢

# ── 存储引擎 ──
wr = localToolBackend      # 存储后端：基于 window.localTool 的 getKV/saveKV，isAvailable=window.localTool?.status?.isConnected，@1262 🟢
Mr = chromeStorageBackend   # 存储后端：基于 chrome.storage.local 的 get/set/remove/getMultiple，@1329 🟢

# ── 事件名 / 样式 id / 常量字符串 ──
Zn = canvasAddResourceEvent # CustomEvent 名：画布添加资源请求 🟢
Rr = frameImageSuffix      # 帧图 URL 后缀 '_frame1.jpg'（缩略帧） 🟢
hr = customHandleStyleId   # <style> id：自定义手柄 hover 样式 🟢
ds = audioRangeStyleId     # <style> id：音频播放器滑条样式 🟢
Dh = customEdgeStyleId     # <style> id：自定义连线流动样式 🟢
ta = modelSchedulesKey     # KV key：'modelSchedules' 🟢
Ca = authTokenKey          # KV key：'auth_token' 🟢
wa = rememberedLoginKey     # 'remembered_login_credentials'（Da 删 / Ea 存，localStorage 记住的登录凭据） 🟢
Wa = promptRecentKey        # 'maomao:promptRecent'（Ga 读最近提示词数组，localStorage） 🟢
e_ = extensionUpdateDismissedKey # 'extension-update-dismissed-version'（扩展更新提示忽略版本，localStorage） 🟢
Va = openPromptSettingsEvent # CustomEvent 名：打开提示词设置 🟢
$g = extensionUpdateDistPathKey # KV key：'extension-update-dist-path' 🟢
ig = templateMimeType      # 模板拖拽 MIME：'application/x-maomao-template' 🟢
d_ = passwordSpecialChars  # 密码特殊字符白名单字符串 🟢

# ── 端点 / URL / 端口 ──
$n = apiBaseUrl             # 官方 API 基础地址（= Vn()，不含 /api） 🟢
Vn = getApiBaseUrl          # 返回官方 API 基础地址（var $n = Vn() @809；App 内未见声明，疑在 engine 包/外部） 🟢
ja = apiBasePath            # 官方 API 基址 = apiBaseUrl + '/api'（er='/api'；值同 qh） 🟢
er = apiPathSuffix          # '/api' 字符串（tr/ar 用它拼 /api 后缀；@810） 🟢
qh = apiBasePath            # 同 ja（值完全相同）= apiBaseUrl + '/api'，用于 /upload/asset 上传 🟢
Hr = localToolStatusUrl     # 'http://127.0.0.1:18080'（轮询/状态检测模块，Gr=3e3；值同 vv） 🟢
vv = localToolBaseUrl       # 'http://127.0.0.1:18080'（数据请求，用于 /api/resources 等；值同 Hr） 🟢
Gr = localToolPollIntervalMs # 3000（3e3），localTool 状态轮询间隔（module-level @1700） 🟢
Bc = localToolPort          # '18080'（localTool 端口字符串） 🟢
Wn = jianyingLocalPort       # '18080'（剪映本地 API 端口，Kn 用于 http://127.0.0.1:${Wn}/api/jianying/send；值同 Bc） 🟢
or = requestTimeoutMs       # 30000，默认请求超时(ms) 🟢

# ── 选项数组 (下拉/预设) ──
mi = promptTips             # 提示词技巧文案数组 🟢
Ua = promptCategoryOptions  # 提示词分类选项（全部/文本/生图/视频） 🟢
to = gridSplitLayoutOptions  # GridSplit 节点布局（2×2/3×3/4×4/1×5/5×1；值同 bo 但 owner 为 gridSplitNode） 🟢
bo = gridMergeLayoutOptions  # GridMerge 节点布局（2×2/3×3/4×4/1×5/5×1；模块含 mergeMode/merged-output，值同 to） 🟢
ko = aspectRatioOptions     # 比例选项 16:9/9:16/3:4/4:3/1:1 🟢
is = videoAspectRatioOptions # 视频比例选项（含 自定义） 🟢
zs = imageCompressWidthOptions # 图片压缩宽度预设 [240,360,480,640,720] 🟢
Zs = imageMaxSizeOptions    # 图片最大尺寸选项（原始/2048/1600...） 🟢
yc = mosaicEditorModeOptions # 马赛克编辑器(bc)打码模式（mosaic/bar/grid/blur；值同 xc） 🟢
xc = mosaicNodeModeOptions   # 马赛克节点(Cc)打码模式（mosaic/bar/grid/blur；值同 yc） 🟢
bc = MosaicEditor           # 马赛克编辑器组件（imageUrl/onSave/onClose；@17644） 🟢
fm = modelCategoryDirectories # 模型分类目录（便利生活/居家生活...） 🟢
yp = aspectRatioPresets     # 比例预设（auto/1:1/2:1/3:4/4:3/16:9/21:9） 🟢
Wo = mediaTypeLabels        # 媒体类型中文标签 {image/audio/video} 🟢
O_ = categoryMeta           # 分类元数据 {text/image/video/discount}，含 short/label/tone（用于 k_ 模型分类映射） 🟢
k_ = resolveCategoryMeta    # 由分类字符串解析 {key,label,tone}（空→misc/其他；消费 O_；@40264） 🟢
v_ = modelCategoryLabels    # 模型分类标签 {text/image/video}，仅 label（用于 y_ 模型列表；与 O_ 结构不同！） 🟢
y_ = getModelList           # 按 text/image/video 分类汇总模型清单（消费 v_ 标签；@39767） 🟢
Fp = imageFileRegex         # 图片文件正则 /\.(jpe?g|png|webp)$/i 🟢
Mp = modelFileRegex         # 模型文件正则 /\.(fbx|obj)$/i 🟢

# ── 3D 导演台 (Director3d) ──
tl = bodyTypePresets        # 素体预设（mannequin/female/broad...） 🟢
fl = mannequinGlbUrl        # 素体 glb 模型地址 'models/ue-mannequin-retopology.glb' 🟢
Al = defaultObjectScale     # 默认物体缩放 1 🟢
Il = defaultAspectRatio     # 默认相机比例 16/9 🟢
Gl = directorDefaultScene   # 导演台默认场景配置（scale/position/panorama...） 🟢
ru = activeModelLibraryId   # 当前 3D 模型库 id（storyai-3d-director...） 🟢
Im = directorDefaultCamera  # 导演台默认相机（var Im = Bl @27643） 🟢
Bl = defaultCameraConfig    # 导演台默认相机对象字面量 {fov:50,position,target}（Im 指向它；@20892） 🟢
wm = directorEditorLimits   # 导演台编辑器约束数值组（46/3/3/1.2/1/12/.1/10） 🟢
jf = directorAccentColor    # 导演台强调色 '#A9D8FF' 🟢

# ── 工具 / 杂项 ──
lr = defaultLodConfig       # LOD/上下文默认配置（useThumbnail/lodLevel...） 🟢
di = penIconSvgPath         # 加载笔图标 SVG path（clPen 动画） 🟢
ws = mediaTypeClassMap      # 媒体类型→类映射 {IMAGE/VIDEO/AUDIO/STRING} 🟢
Ds = allowedHtmlTags        # 富文本允许的 HTML 标签 Set 🟢
ic = faceDetectorInstance   # MediaPipe 人脸检测器单例缓存 🟢
Dc = offscreenCanvasA       # 离屏 canvas 缓存 A（diff 用，同 Oc） 🟢
Oc = offscreenCanvasB       # 离屏 canvas 缓存 B（kc() 中 e!=='a' 时赋值；@18324） 🟢
Cd = viewportCaptureHandler # 视口捕获处理器（wd 注册 / Ed 调用） 🟢
Op = modelGenerateHandler   # 导演台 AI 模型生成处理器（kp 注册） 🟢
kp = registerModelGenerateHandler # 注册模型生成处理器：kp(e){ Op = e }（@26080） 🟢
wd = registerViewportCaptureHandler # 注册视口捕获处理器：wd(e){ Cd = e }（@22986） 🟢
Ed = invokeViewportCapture  # 调用已注册视口捕获：Ed(e){ return Cd(e) }（@22992） 🟢
__ = cx                     # classnames 工具：(...a)=>a.filter(Boolean).join(' ')；与 E_ 完全相同（两处重复定义） 🟢
E_ = cx                     # 同 __（classnames 工具，值完全相同，两处独立定义） 🟢
$_ = taskPersistQueue       # 任务差异持久化 Promise 队列 🟢
nv = defaultPageSize        # 默认分页大小 20 🟢
rv = matchTaskStatus        # 任务状态过滤函数（all/running/...） 🟢
iv = matchTaskType          # 任务类型过滤函数（all/video/...） 🟢
ov = nodeDataFieldSet       # 节点可序列化字段白名单 Set 🟢
sv = nodeCallbackFieldSet   # 节点回调字段 Set（不序列化） 🟢
Ov = tasksSeededFlag        # KV/标记：'tasks_seeded_to_sqlite' 🟢
kv = resourcesSeededFlag    # KV/标记：'resources_seeded_to_sqlite' 🟢
Fg = getDeviceId            # 取设备 id（localStorage Z.DEVICE_ID） 🟢
Ig = syncTimeoutLongMs      # 30000，同步长超时 🟢
Lg = syncShortTimeoutMs     # 15000（15e3），同步短超时（同行 Ig/Rg；@37991） 🟢
Rg = syncRetryCount         # 3，同步重试次数（同行 Ig/Lg；@37991） 🟢

# ── 导演台节点 / 3D 常量（魔数） ──
Th = Director3DNode         # 3D 导演台节点（defaultTitle '3D 导演台'，含 directorProject/机位截图 onCaptureToBox；日志 [Director3DNode]） 🟢
od = sliderDragStepPx       # 10，自定义滑块拖拽：每 10 像素 = 1 步（(clientX-o)/od 取整） 🟢
Md = minZoom                # .25，导演台缩放滑块最小值（Math.min(Nd, Math.max(Md, e))） 🟢
Nd = maxZoom                # 5，导演台缩放滑块最大值 🟢
Pd = defaultZoom            # .25，导演台缩放滑块默认值（与 Md 同行声明） 🟢
Vd = minPanoramaRadius      # 10，全景半径最小值（panoramaRadius 钳制 + 滑块 min） 🟢
Hd = maxPanoramaRadius      # 300，全景半径最大值（滑块 max） 🟢
Ud = minPanoramaRotation    # -180，全景旋转最小值（度） 🟢
Wd = maxPanoramaRotation    # 180，全景旋转最大值（度） 🟢
ef = modelBBoxYOffset       # .18，3D 模型包围盒纵向留白（max.y + ef / 最高 y + ef） 🟢
Df = cameraAngleOffsetDeg   # 90，相机角度转弧度偏移：(e + 90) * π/180 🟢
Of = minClampThreshold      # .1，最小钳制阈值（Af: t ? Math.min(e, Of) : e） 🟢
Af = minClamp               # 钳制函数：t ? Math.min(e, Of) : e（@24983） 🟢

# ── 字体/图标名（补充） ──
be = FilmIcon               # lucide 图标（size 11，导演台节点标题图标） 🟢

# ── 主 store / 统计 / 分页常量 ──
$ = editorStore             # 主编辑器 zustand store（zustand create 工厂，含 trackUndo/persist/includePersistedScene/LocalAssets；Xt 为独立 tab 实例 ref，见 func-mapping） 🟢
Jo = minSampleCount         # 5，模型统计最小样本数（sampleCount < Jo 不显示耗时/失败率） 🟢
Dv = resourcePageSize       # 200，资源库分页大小（pageSize: Dv；区别于 nv=20 的默认列表分页） 🟢
Or = assetIdPrefixes        # ['img_','img_thumb_','video_thumb_'] 资源 id 前缀数组（@1303） 🟢
Pf = defaultUpVector        # new W(0,0,1) 默认上方向向量（Vector3，@24989） 🟢

# ── 存储子系统补充（聚焦子系统，2026-07-19）──
# 注：Q/Z/wr/Mr 已由本批核正命名（StorageManager/StorageKeys/localToolBackend/chromeStorageBackend，见 L20-21,92-93），此处不重复；仅补未收录的 Tr/Er/Dr/Sr
Tr = storageReadCache        # 读取缓存 Map<key,{value,expireAt}>（jr 用），@1300 🟢
Er = inFlightFetches         # 在途请求 Map<key,Promise>（防并发重复读），@1301 🟢
Dr = cacheTtlMs              # 600*1e3=600000ms 缓存 TTL（jr 用），@1302 🟢
Sr = localforageInstance     # Pr=localforageBackend 经 Sr.default 访问（Sr 为 localforage 实例，由 line2 导入别名 be；App.js 内 Sr 另在 1232/35134 为函数局部变量，非此对象）注：绑定源自外部模块，App.js 内非模块级声明 🟢

# ── 第六批补充（Q/R/S/T 大写常量/状态，2026-07-19）──
Qc = defaultBodyType        # 'mannequin'，3D 人体默认体型 id（bodyType 回退值，@20477） 🟢
Ql = directorDeskKeyPrefix   # 'storyai-3d-director-desk-demo'，3D 导演台存储键前缀（${Ql}: 构造，$l，@20938） 🟢
Qm = defaultTextRgb          # '255 255 255'，画布文字默认 RGB（--text-rgb 回退，@27659） 🟢
Qs = qualityPresets          # [{高.9,较高.8,中.6,低.4}] 质量档位选项（option 渲染，@17125） 🟢
Rf = lensSize                # [.4*Ll,.4*Ll,1*Ll] 镜头/包围盒尺寸向量（np('lens') center/size，@24993） 🟢
Rh = presetColors            # [{白,浅灰,黄,红,绿,蓝,橙,紫}] 预设颜色板（n.color??Rh[0]，@29626） 🟢
Rl = cameraDistance          # 5.2*Ll 相机距离标量（multiplyScalar，@20890） 🟢
Rm = axisColors              # ['#E56C5B','#6CDB7A','#7AA7FF'] 三轴颜色（红/绿/蓝，axisColors，@27645） 🟢
Rp = maxTextureSize          # 2048 纹理尺寸上限（Math.max 钳制，@26470） 🟢
Tm = defaultGridCount        # 3 默认网格行列数（useState(String(Tm))，@26913） 🟢
Ti = subscriberSet           # new Set() 模块级订阅者集合（Ti.add/forEach，zi 返回退订，@3110） 🟢
Si = responseCache           # null 模块级响应缓存（if(Si&&!t)return Si；t&&(Si=null) 失效，@3107） 🟢

# ── 第七批补充（U/V/W/X/Y/Z 大写常量，2026-07-19）──
U_ = localServiceBaseUrl    # 'http://127.0.0.1:18080' 本地插件/服务基址（同 Hr，@41195） 🟢
Ug = toastManager           # new class{listeners=[]…} 通知管理器单例（subscribe/removeToast/success/error/warning/info；Wg 用，@38438） 🟢
Vs = scalePresets           # [{0.5×.5,1×1,1.5×1.5,2×2,3×3}] 缩放档位选项（@16703） 🟢
W_ = customDataKeys         # ['customResultData','customRawResponse','requestData','responseData','mediaMeta'] 自定义数据持久化键（@41196） 🟢
Xc = posePresets            # [{stand站立,t-pose,T型,walk行走,run跑步…}] 3D 骨骼姿态预设（含 bone controls，@20144） 🟢
Zc = shapePresets           # [{box立方体,sphere球体,cylinder圆柱,torus环,cone圆锥,pyramid棱锥}] 3D 基本体预设（@20458） 🟢
Zl = localModelLibKey       # 'storyai-3d-director-local-model-library' 本地模型库键（@20937） 🟢
Zm = defaultPanelRgb        # '26 26 26' 面板默认 RGB（@27658） 🟢

# ── 审计补录（2026-07-19）──
# 审计原报 9 个"被注释引用但未收录"的悬空名。复核发现其中 8 个（Fi/Da/Ea/Ga/tr/ar/kc/Ah）
# 实为「函数」，早已收录在 func-mapping.txt（见该表 Fi@107/Ah@82/ar@10/tr@7/Da@93/Ea@98/Ga@112/kc@141），
# 此处不再重复，避免两表冲突。仅 Xt 是真正未收录且为组件局部，保留如下。
Xt = tabInstanceRef         # 组件内局部：Y.useRef(`tab-${Date.now()}-${Math.random()}`) 独立 tab 实例标识（@31467）⚠️ 局部，非模块级绑定，勿随表批量重命名

# ── ReactFlow 画布状态（鼠标交互相关，2026-07-20）──
Et = multiConnectMode     # 多连模式 state：let [Et, Dt] = Y.useState(null)（@31401）；kn 仅在此模式下建连
Dt = setMultiConnectMode  # Et 的 setter
K  = contextMenu          # 右键菜单 state：{x,y,type:'canvas'|'node'|'selection'|'connection',nodeId?,connection?}（菜单渲染 @37597）
ve = setContextMenu       # K 的 setter（Cn/wn/En/On/Fr 中 ve({...}) 设菜单）
Ue = closeMenuSetter      # 关菜单/退模式 setter：Ue(false) 在 Cn/wn/En/On 中调用（配合 Et）
W  = setNodes             # 节点 setState（Fr 中过滤 ghost-target；与 Qt 节点数组配套）
G  = setEdges             # 边 setState（Fr 中过滤/建 ghost-edge；与 $t 边数组配套）
Qt = nodesArray           # 当前节点数组/ref（On 中 Qt.current.filter(selected)；layout 用）
$t = edgesArray           # 当前边数组/ref（layout 用）