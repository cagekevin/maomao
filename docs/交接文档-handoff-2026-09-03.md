# 交接文档 · 前端统一与 relay 收口 · 任务进度 handoff（给下一个 AI）

> **定位**：本次会话正在执行「前端统一 + relay 迁移」（总纲见 `docs/交接文档-前端统一与relay-2026-09-03.md`）。本文件是**当前进度实况 + 未完成清单 + 关键坑 + 下一动作**，供接手的 AI 直接续作。**先读本文件，再读总纲**。
> **日期**：2026-09-03。本会话已在总纲的 Step 1/2/3/5 上大量落地，因换 AI 中断，未完成项见 §4。

## 0. TL;DR（一句话现状）

前端生成门面已全部切到 relay（`/api/generate` + `/api/relay`），`proxyGenerate` 已退役；**生图 / 文本 / 视频当前可出结果，任务能正常结束（进度 10→30→100）**。参考图已统一走 base64 内联。**尚未做**：chat 流式 config 驱动（Step 4）、后端统一 API（Step 6）、拆 9004（Step 8）、12 厂商（Step 9），以及**若干待真机验证点**（见 §4.1）。

## 1. 已完成（本次会话落地，门禁全绿）

- **Step 1 · 新时代服务商设置页**：`providerStore` 删 CRUD（add/update/remove），`save()` 改整组透传；`ApiSettings.tsx` 重写为「只读厂商列表 + 切当前使用 + 测试/拉模型」；删 `ProviderForm.tsx`/`ModelSection.tsx`。测试 `providerStore.test.ts` 改写。

- **Step 2 · 节点「选服务商→模型」两级 UI**：`ModelSelect.tsx` 按 providerId 分组为两级下拉（兼容旧扁平，消费节点零改动）。**未写 UI 测试**（用户明确不测 UI）。

- **Step 3 + Step 7 前端侧 · 门面直连 relay + 退役 proxy**：

  - `imageApi/videoApi` → `relayGenerate`(`/api/generate`)；`chatApi` → `relayChat`(`/api/relay`)，对外签名不变。

  - `proxyGenerate.ts` **整文件删除**；`/api/proxy` 契约项从 `contracts.ts` **删除登记**（不做 RESERVED，直接删）。

  - 后端 `lovart-chat` preset body 补 `temperature/response_format`（纯模板有传才进），`PROTOCOL_VARIABLE_NAMES` 补两变量，`relay.ts handleRelay` 透传。前端 `relayChat` 透传 `temperature/responseFormat`。

  - 旧测试：删 `proxyGenerate.test.ts`/`proxy-outbound.test.ts`/`pollTask.test.ts`；`chatApi/imageApi/videoApi` 测试重写为 relay 契约；`logger.test.ts` 引用修正；删 AgentPanel 过时 chip 断言、TaskCenter「刷新状态」过时断言。

- **Step 5 · 刷新恢复 R6（已完成大半）**：

  - 抽 `relayAttachUntilDone`（统一 attach 循环），`relayGenerate` 复用它。

  - `pollTask.ts` 恢复改走 `/api/generate/:id`（凭 `task.id`=frontTaskId attach），删旧 `/api/v1/gateway/task` 协议 + 前端落盘 + pollTaskId 依赖 + 死常量。

  - **关键 bug 修复**：`startRecoveryRound` 只接管 `type:image/video`，**不再对文本 attach**（文本走 /api/relay 同步无句柄，以前 attach 会导致 relayPoll 恒 running → 进度爬到 90 封顶任务永不结束）。→ 文本任务现在能正常 end（10→30→100）。

  - 进度三段：`TextNode run` 的 30% 挪到 `await chatCompletions` **之前**（否则被 100 覆盖不可见）。

- **参考图跨平台统一 base64（本次重点修复）**：`relay.ts` 对 `input.messages` 也做 `resolveLocalImages`（chat 参考图内嵌），`relay-poll.ts`/`relay.ts` 对 image/video 把 base64 补进 `body.image_urls`。**不用 localhost 绝对 URL**（外部平台访问不了本机），统一 data:base64。

## 2. 关键红线 / 契约（改动前必读）

- 前端门面只发意图（GenIntent：providerId/model/参数），不传 stream（流式由后端 config 决定，Step 4 待做）。出站细节全在后端 relay。

- **参考图一律走** **`resolveLocalImages`** **内联 base64**，禁止拼 `http://127.0.0.1:18080/files/` 绝对 URL 给上游（不通用）。

- `httpRequest` 默认 `parseJson:true` → 返回**纯 JSON 对象非 Response**，禁止 `res.json()`/`res.ok`（曾导致生图崩「res.json is not a function」）。

- 恢复轮询**只对 image/video** attach，文本绝不可 attach（会卡 90 永不结束）。

- 执行协议不绕 kit；落盘归 saveRemoteUrl；DB 写 result\_url 唯一 upsertTask。

## 3. 关键文件索引

- 前端 relay 客户端：`src/components/base/api/relayProxy.ts`（relaySubmit/relayPoll/relayCancel/relayAttachUntilDone/relayGenerate/relayChat）

- 门面：`src/components/base/api/{imageApi,videoApi,chatApi,index}.ts`

- 前端恢复：`src/components/base/api/pollTask.ts`

- 节点进度/生成契约：`src/components/nodes/TextNode.tsx`、`src/hooks/{useGenerateNode,useNodeGeneration}.ts`、`src/components/base/taskStore.ts`

- 后端 relay：`localTool/src/relay.ts`（同步 /api/relay）、`localTool/src/relay-poll.ts`（异步 /api/generate 句柄管理）、`localTool/src/routes/generate.ts`

- 后端 preset：`localTool/src/ai-relay/protocol/{presets,variables,engine}.ts`、`localTool/src/ai-relay/generate/index.ts`（有 kit `streamChat`）

- provider 配置型：`localTool/src/providerConfig.ts`、`localTool/src/routes/providers.ts`

- 统一契约：`src/components/base/api/genIntent.ts`、`src/components/base/contracts.ts`

## 4. 未完成 / 待办

### 4.1 待真机验证（本次改动后用户已确认大部分，剩余需再点一次确认）

- [x] 文本 / 图片 / 视频能出结果

- [x] 任务能结束（进度 10→30→100）

- [x] 文本带参考图成功（消息给 AI 能看图）

- [ ] **生图 / 视频带参考图**是否真通：改了 `relay-poll.ts`/`relay.ts` 补 `body.image_urls`(base64)，但未确认 9004 真机是否收图（报错逻辑已在）。**重启 localTool 后重点验证 PromptNode 生图参考图**。

### 4.2 Step 4 · chat 流式 config 驱动（未做，含待定细节）

- 目标：config/providers chat 模型加 `streaming` 标记 → 后端 /api/relay 按 providerId/model 读标记，决定走 kit `streamChat`（流式）还是现有协议（非流式）。前端 GenIntent 不传 stream（已达成：`chatApi` 不传 stream）。`streaming` 字段放 provider 级还是模型级、后端落点均未定（doc §8.5）。

- **风险**：9004 网关流式返回形态未真机实测（`streamChat` 用 OpenAI SSE 解析，9004 是双层信封 + 流式需实测）。当前 lovart-chat preset `stream:false` 是刻意保护 9004 兼容。别贸然切流式，先真机探 9004 流式响应。

- kit `streamChat` 在 `localTool/src/ai-relay/generate/index.ts`。

### 4.3 残留清洁（收尾时可做）

- `relay.ts` 有 `[relay-chat-image]` 排障埋点（`console.log`，§3 排障用）→ 确认 chat 参考图正常后**可删**。

- `taskStore.ts` done/fail 加了 `logger.debug('[任务] done/fail')` 排障埋点 → 确认后**可删或降级**。

- 存量卡死的旧文本任务（DB 里 `status:running progress:90`）不影响新任务，可手动删或留待清理。

### 4.4 总纲剩余步骤（Step 6-9，前端稳定后再做）

- **Step 6 · 后端统一 API**：把 `/api/relay` 与 `/api/generate` 收口成单一统一 API（doc §8.6 / docs/98）。前端稳定发 GenIntent 后做。

- **Step 7 剩余（后端侧）**：agentChat/其它旧链路照此思路，`/api/proxy` registry 已删（前端侧完成）；后续切割时确认无残留。

- **Step 8 · 拆 9004，relay 直连 Lovart 原生**：ai-relay 抽象 `mode:'agent-thread'`（方案 A）或独立客户端（方案 B），LMAC + /chat→thread\_id + status/result + confirm，key 读 .env（docs/99）。9004 作过渡保留到直连验证通过。

- **Step 9 · 其它 12 厂商生成协议**：补 config/providers JSON + ai-relay 预设/协议（docs/96）。

## 5. 验证命令

- 前端：`npm run type-check` · `npm run test:unit` · `npm run build`（含 check:api/check:events）· `npm test`（全门禁）

- 后端：`cd localTool && npm test`（176+ 用例，改 relay 必跑）

- 生图链路真机验证前需**重启 localTool**（改过后端 `.ts` 要 `npm run build && node dist/index.js`）

- 查生图/任务：`cd localTool && node scripts/task-inspect.mjs --lifecycle <task_id>`（注：脚本较旧，DB 字段/信号可能过时，重点看 DB 直接 `sqlite`）

## 6. 当前门禁状态

- `npm run test:unit`：2220 通过

- `npm run build`：API/事件契约校验通过

- `cd localTool && npm test`：177 通过

- 上述改动**均未 commit**（工作树含本会话 + 更早未提交迁移，见 git status）。提交前跑一次 `npm test` + `cd localTool && npm test` 兜底。

