# 测试覆盖补齐计划（2026-08-17）

> 基于前一轮「测试覆盖缺口梳理」结果制定。整体原则：**优先补齐高风险、纯逻辑、易落地的模块**，
> 对依赖浏览器重环境（mediabunny / mediapipe / canvas 渲染）的模块只测「可纯逻辑剥离」的部分，
> React 组件交互测试放到最后阶段（依赖 jsdom + RTL 环境搭建）。

## 背景

当前测试体系（`tests/unit/` 40 文件 + `localTool/test/` 3 文件 + `tests/e2e/` 4 spec）偏向纯函数/store 层。
经排查，以下大类**基本无测试**，按风险排序：

1. localTool 后端路由/工具（logs/projects/fileStore/logWriter/netProxy/index）
2. base 纯算法/逻辑模块（videoEngine/faceMosaic/promptManager/pollTask/resourcesApi 等）
3. 业务桥接 hook（useAssetDropPaste/useArrangeCanvas/useContextMenu 等）
4. settings 整目录
5. 大型业务节点组件（12+ 个零单测）

## 实施阶段

### 阶段一：base 纯逻辑模块单测（纯函数/store，node 环境可直接跑）✅ 本次实施
- [ ] `base/promptManager.js` → 预设增删改查/分组/广播
- [ ] `base/pollTask.js` → 结果 URL 提取/轮询节流/状态补回（mock fetch + taskStore）
- [ ] `base/resourcesApi.js` → 素材资源 API 封装（mock fetch）
- [ ] `base/faceMosaic.js` → 纯逻辑剥离：resolveAsset / 模式参数归一（不测模型推理，mock detector）
- [ ] `base/videoEngine.js` → 纯逻辑剥离：Sc/Cc 尺寸/帧率 clamp 工具（不测 WebCodecs，导出内部工具）

### 阶段二：localTool 后端路由单测
- [ ] `routes/logs.ts` → 日志写入/查询接口
- [ ] `routes/projects.ts` → 项目 CRUD/导入导出
- [ ] `utils/fileStore.ts` → 落盘/缩略图
- [ ] `utils/logWriter.ts` → 日志滚动/格式化
- [ ] `utils/netProxy.ts` → 代理转发协议翻译（补断言）

### 阶段三：业务 hook 单测（node + 部分 jsdom）
- [x] `inputStateMachine.js`（纯类，零依赖，**21 用例**）
- [x] `scriptBoxEngine.js`（纯函数 + `createScriptBoxEngine` 编排，mock chatApi/imageApi，**19 用例**）
- [ ] `useAssetDropPaste.js`、`useArrangeCanvas.js`、`useContextMenu.js`、`useVideoPoster.js`、`useLocalToolStatus.js`、`useFitNodeRatio.js`
- [ ] `useScriptBoxData.js`、`useScriptBoxEngine.js`（React hook，强依赖 `useReactFlow`，需 jsdom + RTL）

### 阶段四：settings 目录单测 ✅ 本次完成
- [x] `settings/agentModelStore.js`（load/save + 损坏配置兜底，**7 用例**）
- [x] `settings/accountsStore.js`（parseCookies + 表单/收藏/排序/删除状态机 + 浏览器端降级 saveEnvironment，**20 用例**）
- [x] `settings/providerStore.js`（本地动作 + 网络动作 mock settingsApi/kvStore，**15 用例**）
- [x] `settings/settingsApi.js`（request 封装，已有 `settingsApi.test.cjs`，**27 用例**，本轮确认仍绿）

### 阶段四（2026-08-17）✅ 完成
settings 目录四个文件补齐单测，**69 个用例全绿**（含既有 `settingsApi.test.cjs` 27）。

| 测试文件 | 覆盖点 | 用例数 |
|---|---|---|
| `tests/unit/agentModelStore.test.js` | `loadAgentChatModel` 读取/缺失降级；`saveAgentChatModel` 写回并用现存配置兜底 providerId/streamMode（非 `non-stream` 一律回落 `stream`）；损坏 JSON 不崩 | 7 |
| `tests/unit/accountsStore.test.js` | `parseCookies`（JSON 数组/单对象/key=val 退路/非法返回 null）；表单状态机 openCreate/Edit/Close/setName/Cookies；`toggleFavorite`/`moveEnv`/`requestDelete` 二次确认删除并清 activeId；`saveEnvironment` 浏览器端降级（写测试数据/手动添加/格式错误/auto 忽略表单） | 20 |
| `tests/unit/providerStore.test.js` | 本地动作 `add/update/select/setPrimary/remove`；网络动作 `load` 选主/`test` 成功失败/`fetchModels` 结构校验/`save` 清洗 payload（api_key 仅在 `_apiKey` 非空且无 `••` 时带、clear_key 规则、主供应商回写 kv）。`vi.mock` 隔离 `settingsApi.providerApi` 与 `kvStore.kvSet` | 15 |
| `tests/unit/settingsApi.test.cjs`（既有，确认） | `request` 封装、错误归一、auth header | 27 |

**踩坑与约束落实**：
- C2 零基建：三个 store 仅需内存 `localStorage`（`tests/setup.mjs` 已注入）与 `vi.mock('react', () => ({ useSyncExternalStore: (s,g) => g() }))` 让 `useXxx()` 在 node 下直接返回模块级快照，无需 React 运行时。
- C5 基于真实导出：`useXxx()` 返回的是 `getSnapshot()` 结果对象（**不是 hook**），测试直接读 `mod.useAccounts()` 而非 `.getSnapshot()`；`vi.mock` 工厂必须用 `vi.hoisted` 声明 spy，否则 hoist 到 `const` 之前捕获 undefined、导致 mock 不生效（`save` 表现成 `ok:false`）。
- `save` 内部对 `syncConfigBase`/`kvSet` 都走 `.then/.catch`，mock 须给二者 `mockReturnValue(Promise.resolve())`，否则 `undefined.then` 抛 TypeError 被 `save` 的 try/catch 吞成 `ok:false`。
- `accountsStore` 的 `openEditForm` 在 id 不存在时**不 setState**（保持原 `formOpen`），测试据此修正断言；`requestDelete` 二次确认依赖 3s `setTimeout`，测试在同同步 tick 内连调两次触发删除，无需 fake timers。

### 阶段五：大型业务节点组件 .test.jsx（需 jsdom + RTL 环境）✅ 完成
- [x] Group / Template / Text / Image / VideoProcess / VideoExtract / FaceMosaic / GridSplit / GridMerge / ImageBox / Panorama / Director3D / Loop（共 13 个，**28 用例**全绿）
- [x] `DiscountVideoNode.upstream.test.jsx` 既有（5 用例，确认仍绿）

### 阶段六：覆盖存疑模块补真实路径
- [ ] `useAgentChat.js` 工具循环编排、`*Api.js` 网络封装、`scriptBoxEngine.js` 真实调用链、`taskStore.runNodeGeneration` 真实落盘

## 验收方式

每阶段完成后跑 `npm run test:unit` 确认全绿，并更新 `TESTING.md` 的「已补」清单。
最终用 `vitest --coverage` 量化整体覆盖率提升。

---
## 实施记录

### 阶段一（2026-08-17）✅ 完成
模块 2「算法与逻辑层」5 个纯逻辑文件补齐单测，**54 个用例全绿**。

| 测试文件 | 覆盖点 | 用例数 |
|---|---|---|
| `tests/unit/promptManager.test.js` | 预设分组/预设 CRUD、本地持久化(`storageAdapter` 前缀)、最近使用、卡片映射/搜索/分类、eventBus 广播 | 13 |
| `tests/unit/resourcesApi.test.js` | `fetchResources/rescanResources/deleteResource/saveResource/renameResource/openLocalFolder/openFileDir/relativePathFromUrl`（fetch 全 mock） | 14 |
| `tests/unit/pollTask.test.js` | `pollOneTask` 状态机(completed/failed/error/running/无 pollTaskId)、结果 URL 提取(视频/图片/顶层 video_url)、网络异常、URL 编码（mock taskStore+fetch） | 12 |
| `tests/unit/faceMosaic.test.js` | `loadFaceDetector` 单例+失败重试(mock mediapipe)、`drawMosaicOnBox` 四模式+非法 mode(mock ctx + 离屏 canvas spy)、`MOSAIC_PALETTE` | 8 |
| `tests/unit/videoEngine.test.js` | `ProgressController` 取消状态/attach/attachOutput 取消传播、`ConversionCanceled` 类型（jsdom 环境） | 7 |

**踩坑与约束落实**：
- C1 可测性优先：`faceMosaic`/`videoEngine` 的重环境(WebCodecs/mediapipe/wasm/canvas 渲染)均 mock，只测纯逻辑剥离部分；`eyeLine/faceBox/Sc/Cc` 等未导出函数不强行测。
- C2 用例隔离：`storageAdapter` 会给 key 加 `yimao:` 前缀，测试统一经 `sGet/sSet` 读回，避免前缀不一致导致的假失败；`pollTask` mock 整个 `taskStore` 隔离副作用；`faceMosaic` 失败重试用例用 `vi.resetModules()` 隔离单例状态。
- C5 增量优先：每个文件先单独 `vitest run` 跑通再进入下一个，未基于猜测写 API（实测发现 promptManager 实际是扁平预设、faceMosaic 未导出 resolveAsset、resourcesApi 有独立 getResourceUrl 等，均按真实导出修正）。

**已知遗留（pre-existing，非本次引入）**：
- 完整套件 `vitest run` 现有 3 个失败，均位于既有文件、与阶段一无关：
  - `tests/unit/logger.test.js` ×2（`[log]` 文案断言、`spy` 上报去重次数）
  - `tests/unit/toastStore.test.js` ×1（`showToast` id 匹配对象）
  - 建议列入阶段六「覆盖存疑/既有失败修复」统一处理。

### 阶段二（2026-08-17）✅ 完成
localTool 后端路由/工具补齐单测，**26 个用例全绿**（`localTool/test/stage2-routes.test.js`），整包 `npm test` 107/107 通过、0 回归。

| 测试文件 | 覆盖点 | 用例数 |
|---|---|---|
| `localTool/test/stage2-routes.test.js` | `handleLogsPost`（空 body / level 分支 / detail 字符串·对象序列化·task/node tag / 未知 level 回落）；`handleProjectsGet/Save`（空库 / 全量 upsert + isLastOpened + lastOpened 返回 / 增量 upsert 改名保留 / 删除不在列表项 / 缺 projects→400）；`fileStore`（sanitizeFilename / resolveUploadTarget / writeUploadBuffer 时间戳前缀 / writeUploadBufferAt / ensureThumbnailTarget / resizeImage 真实缩放）；`netProxy`（fetchWithProxy 本地直连 / resolveProxy env 优先 / 无 env 探测回落 string\|null）；`logWriter`（仅导出 initLogWriter / 幂等接管）；`index.ts` 装配（路由注册 / catch-all 在 Not Found 前 / 前端托管在 passthrough 前 / 端口 18080） | 26 |

**踩坑与约束落实**：
- C3 指向最新产物：沿现有 `localtool.test.js` 做法 import `dist/` 产物（脚本 `tsc && node --test` 先 build 再测），未基于猜测写 API——实测发现多处与计划书/设想不符，均按真实导出修正：
  - `projects` 实际模型仅 `{id,name,is_last_opened,created_at}`，**不存 `data`/`lastOpenedAt`**；返回 `{projects:[{id,name,createdAt,isLastOpened}], lastOpened}`，`lastOpened` 是 id 字符串；save 为增量 upsert（UPDATE existing / INSERT 新 / DELETE 不在列表者），非全量覆盖。测试据此重写为真实语义。
  - `logs` 真实格式 `[frontend][level] <iso> detail`，对象 detail 输出为 `#taskId=t1 #nodeId=n2 {...}`（非 `[task:t1][node:n2]`）；`console[level]` 取对应方法（warn/error/log），故 stub 全部 console 方法。
  - `fileStore.sanitizeFilename(name)` 无 maxLen 参数，非法符→`_`；`resolveUploadTarget(subfolder, filename)` 参数顺序为 subfolder 在前；`writeUploadBuffer` 自动加 `Date.now()-` 前缀；`resizeImage(src, dst, {maxDim,quality})` 读文件→写文件（非 Buffer）。
  - `netProxy` 仅 `resolveProxy/resetProxyCache/fetchWithProxy` 导出；`isLocalTarget/requiresProxy/proxyFromEnv` 为内部函数，Lovart 判定通过对 `fetchWithProxy` 的可观测行为（本地走 `global.fetch` 直连、代理目标走原生 `http` 到代理服务器）间接覆盖，未强行测未导出符号。
- C5 增量优先：每个模块先 `node --test` 单独跑通再合并，所有失败均回到源码核对真实签名后修正，未用假设 API。
- 隔离：`MAOMAO_DATA_DIR` 指向 `os.tmpdir` 子目录避免污染 `~/.maomao-localtool`；`after` 钩子延迟 1.5s 等 `debouncedSaveDb` 异步 flush 后再删目录，消除 ENOENT 竞态。

### 阶段三（2026-08-17）✅ 完成（纯逻辑部分；React hook 部分留待阶段五 jsdom+RTL 基建）
entry 组件纯逻辑补齐单测，**40 个新用例全绿**，已并入 `vitest run tests/unit`（629 总用例中本次新增 40、全部通过）。

| 测试文件 | 覆盖点 | 用例数 |
|---|---|---|
| `tests/unit/inputStateMachine.test.js` | `InputStateMachine`：`action()` 推导（idle/send/stop/steer/retry/stopping，按 status+内容组合）；`load()` 按 conversationId 隔离与默认值规整；`setStatus()` 归一（completed/stopped→idle、空→idle）；`consume()` 去空白清草稿；`emit()` 回调（setDraft/start 触发 onChange） | 21 |
| `tests/unit/scriptBoxEngine.test.js` | 纯函数：`parseJsonText`（代码块/裸 JSON/非法/空→`{ok,data}`）、`useJsonObject`（deepseek/claude→false、gpt/gemini→true、空→true）、`dialogueLines`（说话者/旁白/普通格式→`完整原句`）、`assembleShotUser`（shot 字段+`@资源名`+统一风格+无资源回退）；引擎编排 `createScriptBoxEngine`：`onGenerateScript` 无剧情校验仅 toast 不调 chat / 调 chat 归一化写回 shots·assets·globalStyle·genMask / 非 JSON 回落 toast；`onStopScriptItem` 全停；`onConnectShot` 建下游 promptNode+自动连线（vi.mock 隔离 chatApi/imageApi/providerModels/toastStore） | 19 |

**踩坑与约束落实**：
- C5 基于真实导出：初稿按设想写 API 全部踩空，逐一对齐源码后修正——`parseJsonText` 返回 `{ok,data}` 包装（非直接对象）；`useJsonObject(modelId)` 返回 **boolean**（是否 json_object 模式）而非 schema 对象；`dialogueLines(dialogue)` 入参是**字符串**且输出 `说话者：X，完整原句：Y`/`旁白，完整原句：Y`；`assembleShotUser(shot, refAssets, globalStyle)` 的 shot 字段为 `index/duration/shotType/lighting/motion/description/dialogue/sound`，refAssets 为 `{name}` 数组、引用格式 `@名称`；引擎方法名为 `onGenerateScript/onGenerateShotPrompts/onStopScriptItem/onConnectShot`（非 `regenerate/clearAll/setStatus`），`onGenerateScript` 写回 `{genMask, story, projectName, globalStyle, shots, assets, pickedCount}`，shots 归一化为 `{id:'{nodeId}-shot-{i}', index, prompt, videoPrompt, ...}`。
- 隔离网络/React：`vi.mock` 屏蔽 `chatApi/imageApi/providerModels/toastStore`，仅保留纯提示词模块 `scriptBoxPrompts`；toastStore mock 需同时导出 `showToast` 与 `toastStore` 命名空间（引擎按 `import { toastStore }` 引用）。
- React hook 部分（`useScriptBoxData/Engine/useConnectedInputs/useFitNodeRatio/...`）强依赖 `useReactFlow`/jsdom，B 类不阻塞，留待阶段五 jsdom+RTL 基建后补。

**已知遗留（pre-existing，非本次引入，与阶段二同）**：完整套件曾有 `logger.test.js`×2、`toastStore.test.js`×1 三个 stale 失败（断言值写死、源已演进）。**已于本轮（2026-08-17 收尾）修复**：`toastStore` 的 `duration` 对齐分级默认 2500；`logger` 两处用例对齐 `[<level>][<category>]` 格式并加固 `postLogs`（`safeStringify` 降级 + 兜底 `.catch`，杜绝循环引用/fetch 同步异常导致的 unhandled rejection）。整包 `tests/unit` 现已 691 passed 全绿。

### 阶段五（2026-08-17）✅ 完成
13 个节点组件补齐 `.test.jsx` 单测，**28 个用例全绿**（含既有 `DiscountVideoNode.upstream.test.jsx` 5 仍绿）。整包 `tests/unit` 现 **65 files / 719 tests 全绿**（较阶段四末 +28）。

| 测试文件 | 覆盖点 | 用例数 |
|---|---|---|
| `GroupNode.test.jsx` | 展开/折叠态渲染、双击折叠（setNodes 更新 data.collapsed + 子节点 hidden/恢复尺寸） | 5 |
| `TemplateNode.test.jsx` | 挂载不崩、外壳标题透传、点击「生成」走 generateImage 链路 | 3 |
| `TextNode.test.jsx` | 挂载不崩、点击「生成」走 chatCompletions 链路 | 2 |
| `ImageNode.test.jsx` | 挂载不崩、点击生成按钮触发 generateImage | 2 |
| `VideoProcessNode.test.jsx` | 挂载不崩、根节点 data-node-id 正确 | 2 |
| `VideoExtractNode.test.jsx` | 挂载不崩（NodeTitle 透传） | 2 |
| `FaceMosaicNode.test.jsx` | 挂载不崩、点击主按钮不抛错 | 2 |
| `GridSplitNode.test.jsx` | 挂载不崩、点击「拆分为图片」调 setNodes（有输入时） | 2 |
| `GridMergeNode.test.jsx` | 挂载不崩、点击「合成为视频」调 setNodes | 2 |
| `ImageBoxNode.test.jsx` | 挂载不崩 | 1 |
| `PanoramaNode.test.jsx` | 挂载不崩、点击「送图片盒子」调 setNodes | 2 |
| `Director3DNode.test.jsx` | 挂载不崩 | 1 |
| `LoopNode.test.jsx` | 挂载不崩、点击「运行」调 setNodes 生成分段生图 | 2 |

**关键基建与踩坑（C2 大动作已落地、C5 基于真实导出）**：
- 复用现成 jsdom+RTL 基建（RTL 16 / jsdom 25 / vitest 2.1 已装）；`setup.mjs` 补 `ResizeObserver`/`matchMedia`/`canvas.getContext` polyfill（GridSplit/GridMerge 等用 ResizeObserver 自适应高度，jsdom 缺失会崩）。
- 共享 mock 模块 `tests/unit/_nodeMocks.mjs`：集中 stub 掉 `@xyflow/react`(useReactFlow→可控 setNodes/setEdges/addNodes/addEdges)、全部 `./base/*` 基座组件（NodeShell 暴露 `data-testid="shell"`、其余透传/空占位）、各 hooks、toastStore、filesApi、providerStore、providerModels、imageApi/chatApi、videoEngine、faceMosaic 等；并暴露 `mocks` 单一命名空间与 `resetNodeMockState()`。
- **命名冲突坑（重要）**：最初用 `import * as M` 导致 kit 内 `OverlayEditor`/`useStore` 等名被提升到测试模块作用域，与组件源码 `import { OverlayEditor }` 重名报 `Identifier already declared`。修正为 kit 只 `export const mocks`，测试用 `import { mocks }` 引用，彻底隔离。
- 交互断言贴近真实 UI：节点按钮文字非统一「生成」（FaceMosaic 用上传/打码按钮、VideoProcess 自渲染外壳无 NodeShell），故交互用例改为「查询按钮（模糊正则或 `.node-btn-primary`/首 button）→ 点击验证不抛错 + setNodes 被调用」，避免按假想文字查询失败。
- 挂载 smoke 作为底线断言，确保每个组件在 jsdom 下能渲染，后续可逐步加细粒度交互断言。
