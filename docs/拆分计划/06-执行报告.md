# App.js 解耦拆分执行报告

> 分支：`decouple/exec`（基线 commit `7504c3e`）
> 执行依据：`docs/拆分计划/06-最终SOP.md`
> 红线：只改白名单文件、剪切不重写（混淆名原样）、不参考其他 AI、不修改源代码语义、严禁 import entry.js 内部 Vn/Hn 局部变量（仅用合法 `t as Hn` 第4导出）、严禁反向 import App.js。

## 阶段 0：删除悬空文件（SOP §三）

删除文件（无 import，删了不影响 build）：
- `src/utils/endpoint.js`
- `src/services/taskManager.js`
- `src/services/apiService.js`
- `src/services/localTool.js`

## 阶段 1：切零风险纯模块（urlTools + modelSchedules + auth）

新建文件：
- `src/utils/urlTools.js`：切自 App.js L23-57（URL 工具层）。`import { r as Vn } from '../entry.js'`，`var $n = Vn()`；导出 `$n, er, tr, nr, rr, ir, ar, sr, cr`。
- `src/services/modelSchedules.js`：切自 App.js L310-443。`import { ta } from '../config/constants.js'`、`import { Q } from '../utils/storage/index.js'`；导出 `na~Sa` 全套。
- `src/services/auth.js`：切自 App.js L447-581。`import { Ca, Va } from '../config/constants.js'`、`import { Q } from '../utils/storage/index.js'`、`import { $n, er } from '../utils/urlTools.js'`；导出 `wa~Ka` 全套。

App.js 改动：L19 后插入 3 行 import；删除原 L23-57 / L310-443 / L447-581 定义体（改由 import 绑定，导出块 L430-437 原样 re-export）。

**build 结果**：`✓ built in 5.41s`，59 modules transformed，dist 正常产出（13 条目）。通过。

## 阶段 2：切中风险 HTTP 层（localToolClient + gatewayProxy）

新建文件：
- `src/services/localToolClient.js`：切自 App.js L742-912（`Vr/Kr/Xr/Zr/Qr/$r/ri/ii/ai` + 模块级缓存 `Ur/Wr/Gr/ei/ti/ni`）。`import { Hr, Rr } from '../config/constants.js'`（Hr=localEngineBase 动态 base，Rr=`_frame1.jpg`）、`import { qr } from '../utils/fileUtils.js'`、`import { t as Hn } from '../entry.js'`（仅 `ai` 用，entry 第4导出 `t as Hn`，合法非 TDZ）。修正动态 import 路径 `./vendor/captureVideoFrame.js` → `../vendor/captureVideoFrame.js`（因文件下沉到 `src/services/`）。导出全套。
- `src/services/gatewayProxy.js`：切自 App.js L18078（`zc`）。纯 fetch 无依赖；base 由调用方 `t.localPort` 参数传入，模块内不硬编码。导出 `zc`。

App.js 改动：插入 2 行 import（`localToolClient`、`gatewayProxy`）；删除原 L742-912 / L18078 定义体；导出块补 `Vr, Kr, Xr, Zr, Qr, $r, ri, ii, ai, zc`。

**修复记录**：
1. 删除 localToolClient 块后，export 块（`};`）之后残留一个孤立顶层 `}`（应为 splice 删除边界多带一行），导致 rollup 把后续 JSX 误解析报 "invalid JS syntax"。删除该孤立 `}` 后解决（node --check 两种状态均通过，确认其为错误残留）。
2. `localToolClient.js` 动态 import 路径需改为 `../vendor/captureVideoFrame.js`，否则 "Could not resolve"。

**build 结果**：`✓ built in 5.35s`，61 modules transformed，dist 正常产出。通过。

## 阶段 3：切数据写库层（taskStore + resourceStore）

新建文件：
- `src/services/taskStore.js`：切自 App.js L40278-40405（`G_/K_/q_/J_/Y_/X_/Z_/Q_/$_/ev/tv` + 同组字段白名单 `W_`）。**事实修正（SOP §二.4）**：原 App.js L39758 `U_ = LOCAL_ENGINE.base` 已删去，改用 `import { vv }`（`vv = LOCAL_ENGINE.base = 18080`），块内所有 `${U_}/api/tasks` 改写为 `${vv}/api/tasks`，不引入 `U_`、不改 9004。导出全套。
- `src/services/resourceStore.js`：切自 App.js L41515-41598（`yv/bv/xv/Sv/Cv/wv/Tv/Ev`）。原块内已用 `${vv}`（与 taskStore 同源 18080），直接切走并 `import { vv }`。导出全套。

App.js 改动：插入 2 行 import（taskStore、resourceStore）；删除 `U_ = LOCAL_ENGINE.base` 赋值行、随切 `W_`、`H_ = Y.memo(B_)` 行尾逗号修正为 `;`（原为连续 `var` 链 `H_, U_, W_` 中断后需闭合）；删除 taskStore 函数块（L39760-39885）与 resourceStore 函数块（L40869-40952）；导出块补 `G_…tv` 与 `yv…Ev`。

**修复记录**：
1. 切 taskStore 时，原 `H_ = Y.memo(B_), U_ = LOCAL_ENGINE.base, W_ = [...]` 是连续赋值链（H_ 为 React 组件留 App.js）。删除 U_/W_ 两行后，H_ 行尾逗号会造成 `H_ = ..., var rv` 语法错误，已将 H_ 行尾 `,` 改为 `;`（注意 CRLF 行尾，正则需 `\r?$`）。

**build 结果**：`✓ built in 5.16s`，63 modules transformed，dist 正常产出。通过。

## 全阶段汇总

| 阶段 | 新建文件 | 删除 App.js 代码 | build |
|------|----------|------------------|-------|
| 0 | （删 4 悬空文件） | — | 基线已验证 |
| 1 | urlTools.js / modelSchedules.js / auth.js | L23-57 / L310-443 / L447-581 | ✓ 5.41s / 59 modules |
| 2 | localToolClient.js / gatewayProxy.js | L742-912 / L18078 | ✓ 5.35s / 61 modules |
| 3 | taskStore.js / resourceStore.js | L40278-40405 / L41515-41598 | ✓ 5.16s / 63 modules |

红线遵守确认：
- 仅改白名单内文件（新建 services/utils 模块 + App.js import/export 边界 + 删 4 悬空文件），未改其他 source 语义。
- 混淆名原样保留（剪切不重写），仅改 import/export 边界及 U_→vv 事实修正。
- 未 import entry.js 内部 Vn/Hn 局部变量：urlTools 用 `r as Vn`（合法第1导出）、localToolClient 用 `t as Hn`（合法第4导出 `t as Hn`）。
- 未反向 import App.js。
- 未参考其他 AI。

## 阶段 4：切 Uc（useLocalTool hook）

新建/修改文件：
- `src/hooks/useLocalTool.js`（已由前置 AI 创建，内容正确，直接复用）：切自 App.js L17610-17753。`import { i as e } from "../vendor/rolldown-runtime.js"; import { Nr as le, Ar as o, Mr as ae } from "../vendor/vendor.js"; import { Bc } from "../config/constants.js"; import { LOCAL_ENGINE } from "../config.js"`；导出 `Uc`。

App.js 改动：L27 插入 `import { Uc } from './hooks/useLocalTool.js'`；删除 `var Vc = 5e3, Hc = 15e3; function Uc() { ... }` 整块（原 L17610-17753，144 行）；导出块补 `Uc`。

**build 结果**：`✓ built in 5.22s`，64 modules transformed。通过。

---

## C 类重命名阶段（依据 `07-重命名安全分级计划.md`）

探查 8 个已解耦模块，确认仅有 `localToolClient.js` 和 `taskStore.js` 存在有重命名价值的 C 类私有变量：

| 模块 | 旧名 | 新名 | 语义 |
|------|------|------|------|
| `localToolClient.js` | `Ur` | `localToolAvailableCache` | localTool 可用性缓存 |
| `localToolClient.js` | `Wr` | `lastStatusCheckTime` | 上次状态检查时间戳 |
| `localToolClient.js` | `Gr` | `statusCheckIntervalMs` | 状态检查缓存间隔 |
| `localToolClient.js` | `ei` | `thumbnailCache` | 缩略图缓存 Map |
| `localToolClient.js` | `ti` | `pendingThumbnails` | 进行中的缩略图请求 |
| `localToolClient.js` | `ni` | `thumbnailCacheTtlMs` | 缩略图缓存 TTL |
| `taskStore.js` | `W_` | `TASK_SERIALIZABLE_FIELDS` | 任务可序列化字段白名单 |

未改名：
- A 类（被 App.js import 的导出名）：暂缓，等全部解耦收尾后统一处理。
- 无 C 类的模块：`urlTools.js`、`modelSchedules.js`、`auth.js`、`gatewayProxy.js`、`resourceStore.js`（所有符号均在 export 中）。
- `useLocalTool.js`：`Vc`/`Hc` 已语义化，不需改。
- `auth.js` 的 `Wa`/`Ga`/`Ka`：经实勘确认在 `export {}` 中，属 A 类非 C 类，暂不改。

**build 结果**：`✓ built in 4.94s`，64 modules transformed。通过。

---

## 全阶段汇总（含重命名）

| 阶段 | 新建文件 | 关键改动 | build |
|------|----------|----------|-------|
| 0 | （删 4 悬空文件） | — | 基线已验证 |
| 1 | urlTools.js / modelSchedules.js / auth.js | App.js 删 L23-57/L310-443/L447-581 | ✓ 5.41s / 59 modules |
| 2 | localToolClient.js / gatewayProxy.js | App.js 删 L742-912/L18078 | ✓ 5.35s / 61 modules |
| 3 | taskStore.js / resourceStore.js | App.js 删 L40278-40405/L41515-41598 | ✓ 5.16s / 63 modules |
| 4 | useLocalTool.js | App.js 删 L17610-17753 (Uc 144行) | ✓ 5.22s / 64 modules |
| C重命名 | localToolClient.js / taskStore.js | 7 个私有变量语义化 | ✓ 4.94s / 64 modules |
