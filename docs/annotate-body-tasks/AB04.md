# AB04 — 节点函数体重写（3 个）

> 🚫 **禁止直接修改 src/App.js！** 只在本文件代码块中提交重写结果。运行 `node scripts/merge-rewrites.cjs` 统一写入。
> ⚠️ 铁律：改写后功能必须和原来完全一样。只改局部变量名和加注释，不改任何逻辑。

## 规则
1. 不改函数签名和模块级变量名
2. 重命名函数体内局部变量：a,b,c→loading,setLoading
3. 加段落注释：`// ── 状态 ──` `// ── 回调 ──` `// ── 渲染 ──`
4. 保持 vendor/React 用法不变：不动 Y.useState、X.jsx 等
5. 不改 JSX 结构

## 提交格式（重要！）
```
| 节点类型 | 组件变量名 |
```js
// ── 状态 ──
const [loading, setLoading] = Y.useState(false);
// ...
```
|
```

## 任务
1. **audioNode** (`AudioTranscribeNodeComp`)
2. **audioPlayerNode** (`AudioPlayerNodeComp`)
3. **customNode** (`CustomNodeComp`)

## 参考
- 节点契约：docs/节点组件契约表.md
- 函数摘要：`node scripts/summarize.cjs AudioTranscribeNodeComp`

| audioNode | AudioTranscribeNodeComp |
```js
// ── 状态 ──
let {
    updateNodeData: updateNodeData,
    getNodes: getNodes,
    getEdges: getEdges
  } = Gt(),
  nodeData = n,
  fileInputRef = Y.useRef(null),
  [audioFile, setAudioFile] = Y.useState(null),
  [showConfig, setShowConfig] = Y.useState(false),
  [prompt, setPrompt] = Y.useState(n.prompt || `请输出简体中文。`),
  [maxDuration, setMaxDuration] = Y.useState(n.maxDuration || 10),
  [pauseGap, setPauseGap] = Y.useState(n.pauseGap || .3);
Y.useEffect(() => {
  updateNodeData(e, {
    prompt: prompt,
    maxDuration: maxDuration,
    pauseGap: pauseGap
  });
}, [prompt, maxDuration, pauseGap, e, updateNodeData]);
let connectedSources = ut(t({
    handleType: `target`
  }).map(sourceId => sourceId.source)),
  prevAudioUrl = Y.useRef(``);
Y.useEffect(() => {
  if (audioFile) return;
  let sourceList = Array.isArray(connectedSources) ? connectedSources : connectedSources ? [connectedSources] : [],
    foundUrl = ``;
  for (let src of sourceList) if (src?.data) {
    if (src.data.videoUrl && typeof src.data.videoUrl == `string`) {
      let url = src.data.videoUrl;
      if (url.startsWith(`data:audio/`) || url.startsWith(`data:video/`) || /\.(mp3|wav|ogg|m4a|mp4|webm|mov)($|\?)/i.test(url)) {
        foundUrl = url;
        break;
      }
    }
    if (src.data.imageUrl && typeof src.data.imageUrl == `string`) {
      let url = src.data.imageUrl;
      if (url.startsWith(`data:audio/`) || url.startsWith(`data:video/`) || /\.(mp3|wav|ogg|m4a|mp4|webm|mov)($|\?)/i.test(url)) {
        foundUrl = url;
        break;
      }
    }
    if (src.data.text && typeof src.data.text == `string`) {
      let match = src.data.text.match(/(https?:\/\/[^\s"'`<>]+)|(data:(audio|video)\/[^\s"']+)/i);
      if (match) {
        foundUrl = match[0];
        break;
      }
    }
  }
  if (foundUrl && foundUrl !== prevAudioUrl.current) {
    prevAudioUrl.current = foundUrl;
    let fileName = `connected_audio.mp3`;
    if (foundUrl.startsWith(`data:audio/`)) fileName = `base64_audio.mp3`;else try {
      let urlObj = new URL(foundUrl),
        base = urlObj.pathname.split(`/`).pop();
      fileName = base && base.length > 0 && base !== `/` && base.includes(`.`) ? base + urlObj.search : foundUrl;
    } catch {
      fileName = foundUrl;
    }
    nodeData.audioUrl = foundUrl, nodeData.audioName = fileName, setPrompt(p => p), updateNodeData(e, {
      audioUrl: foundUrl,
      audioName: fileName,
      errorMessage: undefined
    });
  } else !foundUrl && prevAudioUrl.current && (prevAudioUrl.current = ``, audioFile || updateNodeData(e, {
    audioUrl: undefined,
    audioName: undefined
  }));
}, [connectedSources, audioFile, e, updateNodeData]);
Y.useEffect(() => {
  updateNodeData(e, {
    onGenerateAudio: handleGenerate
  });
}, [audioFile, nodeData.audioApiUrl, nodeData.audioApiKey, nodeData.audioModel, prompt, maxDuration, pauseGap]);

// ── 回调 ──
let handleFileChange = event => {
    let file = event.target.files?.[0];
    if (!file) return;
    setAudioFile(file);
    let objectUrl = URL.createObjectURL(file);
    nodeData.audioUrl = objectUrl, nodeData.audioName = file.name, setPrompt(p => p), updateNodeData(e, {
      audioUrl: objectUrl,
      audioName: file.name,
      errorMessage: undefined,
      chunks: undefined
    }), event.target.value = ``;
  },
  handleGenerate = async () => {
    let file = audioFile;
    if (!file) {
      let edges = getEdges(),
        nodes = getNodes(),
        matched = edges.filter(edge => edge.target === e),
        foundUrl = ``;
      for (let edge of matched) {
        let sourceNode = nodes.find(node => node.id === edge.source);
        if (sourceNode) {
          if (sourceNode.data.audioUrl && typeof sourceNode.data.audioUrl == `string`) {
            foundUrl = sourceNode.data.audioUrl;
            break;
          }
          if (sourceNode.data.videoUrl && typeof sourceNode.data.videoUrl == `string`) {
            let url = sourceNode.data.videoUrl;
            if (url.startsWith(`data:audio/`) || url.startsWith(`data:video/`) || /\.(mp3|wav|ogg|m4a|mp4|webm|mov)($|\?)/i.test(url)) {
              foundUrl = url;
              break;
            }
          }
          if (sourceNode.data.imageUrl && typeof sourceNode.data.imageUrl == `string`) {
            let url = sourceNode.data.imageUrl;
            if (url.startsWith(`data:audio/`) || url.startsWith(`data:video/`) || /\.(mp3|wav|ogg|m4a|mp4|webm|mov)($|\?)/i.test(url)) {
              foundUrl = url;
              break;
            }
          }
          if (sourceNode.data.text && typeof sourceNode.data.text == `string`) {
            let match = sourceNode.data.text.match(/(https?:\/\/[^\s"'`<>]+)|(data:(audio|video)\/[^\s"']+)/i);
            if (match) {
              foundUrl = match[0];
              break;
            }
          }
        }
      }
      if (foundUrl) {
        updateNodeData(e, {
          loading: true,
          errorMessage: `正在下载音频...`
        });
        try {
          if (foundUrl.startsWith(`data:audio/`) || foundUrl.startsWith(`data:video/`)) {
            let parts = foundUrl.split(`,`),
              mimeMatch = parts[0].match(/:(.*?);/),
              mime = mimeMatch ? mimeMatch[1] : `audio/mpeg`,
              binary = atob(parts[1]),
              len = binary.length,
              bytes = new Uint8Array(len);
            for (; len--;) bytes[len] = binary.charCodeAt(len);
            let fileName = `media_generated.${mime.split(`/`)[1] || `mp3`}`;
            file = new File([bytes], fileName, {
              type: mime
            }), updateNodeData(e, {
              audioUrl: URL.createObjectURL(file),
              audioName: fileName
            });
          } else {
            let controller = new AbortController(),
              timer = setTimeout(() => controller.abort(), 18e4),
              response = await fetch(foundUrl, {
                signal: controller.signal
              });
            if (clearTimeout(timer), !response.ok) throw Error(`下载失败: ${response.status}`);
            let blob = await response.blob(),
              fileName = foundUrl.split(`/`).pop() || `audio.mp3`;
            file = new File([blob], fileName, {
              type: blob.type || `audio/mpeg`
            }), updateNodeData(e, {
              audioUrl: URL.createObjectURL(file),
              audioName: fileName
            });
          }
        } catch (err) {
          updateNodeData(e, {
            loading: false,
            errorMessage: err.name === `AbortError` ? `音频下载超时 (3分钟)` : `音频下载失败: ${err.message}`
          });
          return;
        }
      }
    }
    if (!file) {
      nodeData.onShowToast?.(`请先上传音频文件或连接包含音频URL的节点`);
      return;
    }
    if (!nodeData.audioApiUrl || !nodeData.audioApiKey) {
      updateNodeData(e, {
        errorMessage: `请在设置中配置听音 API Key`
      });
      return;
    }
    updateNodeData(e, {
      loading: true,
      errorMessage: undefined
    });
    try {
      let chunks = await ss(file, nodeData.audioApiUrl, nodeData.audioApiKey, nodeData.audioModel || `whisper-1`, prompt, maxDuration, pauseGap);
      updateNodeData(e, {
        loading: false,
        chunks: chunks,
        text: JSON.stringify(chunks, null, 2)
      }), nodeData.onShowToast?.(`听音断句完成！`);
    } catch (err) {
      console.error(`Audio processing failed:`, err), updateNodeData(e, {
        loading: false,
        errorMessage: err.message || `处理失败，请重试`
      });
    }
  };

// ── 渲染 ──
return X.jsxs(`div`, {
  className: `relative flex flex-col group/node transition-all w-[360px] ${r ? `z-50` : `z-10`}`,
  children: [X.jsx(si, {
    id: e,
    data: n,
    defaultTitle: `听音断句`,
    icon: X.jsx(`span`, {
      className: `text-gray-500`,
      children: `🎙️`
    })
  }), X.jsx(`input`, {
    type: `file`,
    ref: fileInputRef,
    style: {
      display: `none`
    },
    accept: `audio/*,video/*`,
    onChange: handleFileChange
  }), nodeData.audioUrl && X.jsx(`div`, {
    className: `absolute -top-12 left-1/2 -translate-x-1/2 z-20 opacity-0 group-hover/node:opacity-100 transition-opacity pointer-events-none group-hover/node:pointer-events-auto nodrag pb-4`,
    children: X.jsxs(`div`, {
      className: `flex items-center gap-1 px-3 py-2 bg-[#1c1c1c]/90 backdrop-blur-md border border-[#333] rounded-full shadow-lg`,
      children: [X.jsx(Yn, {
        url: nodeData.audioUrl,
        fallbackExt: `mp3`,
        size: 13,
        onToast: msg => nodeData.onShowToast?.(msg)
      }), X.jsx(`button`, {
        className: `p-1.5 text-gray-400 hover:text-red-400 hover:bg-[#333] rounded-md`,
        onClick: () => {
          setAudioFile(null), updateNodeData(e, {
            audioUrl: undefined,
            audioName: undefined,
            chunks: undefined,
            errorMessage: undefined
          });
        },
        title: `清除`,
        children: X.jsx(S, {
          size: 14
        })
      })]
    })
  }), X.jsxs(`div`, {
    className: `relative bg-[#1c1c1c] rounded-xl overflow-visible border shadow-xl transition-all duration-300 flex flex-col
          ${r ? `border-[#555]` : `border-[#333] hover:border-[#444]`}
        `,
    style: {
      minHeight: `160px`
    },
    children: [X.jsx(_r, {
      type: `target`,
      position: J.Left
    }), X.jsx(_r, {
      type: `source`,
      position: J.Right
    }), X.jsxs(`div`, {
      className: `flex-1 p-3 overflow-y-auto bg-[#1a1a1a] custom-scrollbar relative min-h-[80px] max-h-[160px] rounded-t-xl`,
      children: [nodeData.loading && X.jsxs(`div`, {
        className: `absolute inset-0 flex flex-col items-center justify-center gap-2 text-gray-500 bg-[#1a1a1a]/80 backdrop-blur-sm z-10`,
        children: [X.jsx(ui, {
          size: 24
        }), X.jsx(`span`, {
          className: `text-xs`,
          children: `处理中...`
        })]
      }), nodeData.errorMessage && !nodeData.loading ? X.jsxs(`div`, {
        className: `text-red-400 text-[10px] p-2 border border-red-500/30 rounded bg-red-500/10 flex items-start gap-1.5`,
        children: [X.jsx(pt, {
          size: 12,
          className: `mt-0.5 flex-shrink-0`
        }), X.jsx(`span`, {
          className: `break-all leading-tight`,
          children: nodeData.errorMessage
        })]
      }) : nodeData.chunks ? X.jsxs(`div`, {
        className: `flex flex-col gap-1 nodrag`,
        children: [X.jsxs(`div`, {
          className: `flex justify-between items-center`,
          children: [X.jsxs(`span`, {
            className: `text-[10px] text-gray-500`,
            children: [`处理结果 (`, nodeData.chunks.length, ` 句)`]
          }), X.jsxs(`button`, {
            onClick: evt => {
              evt.stopPropagation(), nodeData.chunks && (navigator.clipboard.writeText(JSON.stringify(nodeData.chunks, null, 2)), nodeData.onShowToast?.(`JSON 已复制到剪贴板`));
            },
            className: `text-[10px] flex items-center gap-1 text-gray-400 hover:text-white transition-colors`,
            children: [X.jsx(on, {
              size: 10
            }), ` 复制 JSON`]
          })]
        }), X.jsx(`pre`, {
          className: `text-[10px] text-gray-400 font-mono whitespace-pre-wrap break-all nodrag select-text mt-1`,
          children: JSON.stringify(nodeData.chunks, null, 2)
        })]
      }) : X.jsx(`div`, {
        className: `flex items-center justify-center h-full text-gray-500 text-xs mt-8`,
        children: `等待上传并处理...`
      })]
    }), X.jsxs(`div`, {
      className: `p-3 bg-[#1a1a1a] flex flex-col gap-3 nodrag border-t border-[#2a2a2a] rounded-b-xl relative z-10`,
      onClick: evt => evt.stopPropagation(),
      children: [nodeData.audioUrl ? X.jsxs(`div`, {
        className: `w-full flex flex-col gap-2 bg-[#111] p-2 rounded-lg border border-[#333]`,
        children: [X.jsx(`div`, {
          className: `flex items-center justify-between`,
          children: X.jsxs(`div`, {
            className: `flex items-center gap-2 overflow-hidden`,
            children: [X.jsx(vn, {
              size: 14,
              className: `text-green-500 flex-shrink-0`
            }), X.jsx(`span`, {
              className: `text-xs text-gray-300 truncate`,
              title: nodeData.audioName,
              children: nodeData.audioName
            })]
          })
        }), nodeData.audioUrl.match(/\.(mp4|webm|mov|ogg)($|\?)/i) || nodeData.audioUrl.startsWith(`data:video/`) ? X.jsx(`video`, {
          src: nodeData.audioUrl,
          controls: true,
          className: `w-full h-24 object-contain outline-none nodrag bg-black rounded`
        }) : X.jsx(`audio`, {
          src: nodeData.audioUrl,
          controls: true,
          className: `w-full h-8 outline-none nodrag`
        })]
      }) : X.jsxs(`div`, {
        className: `w-full py-4 rounded-lg border border-dashed border-[#444] bg-[#111] hover:bg-[#1a1a1a] hover:border-[#666] flex flex-col items-center justify-center gap-2 cursor-pointer transition-colors group/upload`,
        onClick: () => fileInputRef.current?.click(),
        children: [X.jsx(jn, {
          size: 16,
          className: `text-gray-500 group-hover/upload:text-green-500 transition-colors`
        }), X.jsx(`span`, {
          className: `text-[10px] text-gray-500`,
          children: `点击上传音视频或连接含音频的节点`
        })]
      }), showConfig && X.jsxs(`div`, {
        className: `flex flex-col gap-3 bg-[#111] border border-[#333] rounded p-3 mt-1 animate-fade-in nodrag`,
        children: [X.jsxs(`div`, {
          className: `flex flex-col gap-1.5`,
          children: [X.jsx(`label`, {
            className: `text-[10px] text-gray-400`,
            children: `提示词 (Prompt)`
          }), X.jsx(`input`, {
            type: `text`,
            className: `w-full bg-[#222] border border-[#333] rounded px-2 py-1.5 text-xs text-gray-300 focus:outline-none focus:border-blue-500`,
            value: prompt,
            onChange: evt => setPrompt(evt.target.value),
            placeholder: `请输出简体中文。`
          })]
        }), X.jsxs(`div`, {
          className: `flex gap-2`,
          children: [X.jsxs(`div`, {
            className: `flex flex-col gap-1.5 flex-1`,
            children: [X.jsx(`label`, {
              className: `text-[10px] text-gray-400`,
              children: `换气停顿 (秒)`
            }), X.jsx(`input`, {
              type: `number`,
              step: `0.1`,
              min: `0`,
              className: `w-full bg-[#222] border border-[#333] rounded px-2 py-1.5 text-xs text-gray-300 focus:outline-none focus:border-blue-500`,
              value: pauseGap,
              onChange: evt => setPauseGap(parseFloat(evt.target.value) || .3)
            })]
          }), X.jsxs(`div`, {
            className: `flex flex-col gap-1.5 flex-1`,
            children: [X.jsx(`label`, {
              className: `text-[10px] text-gray-400`,
              children: `强制熔断 (秒)`
            }), X.jsx(`input`, {
              type: `number`,
              step: `1`,
              min: `1`,
              className: `w-full bg-[#222] border border-[#333] rounded px-2 py-1.5 text-xs text-gray-300 focus:outline-none focus:border-blue-500`,
              value: maxDuration,
              onChange: evt => setMaxDuration(parseFloat(evt.target.value) || 10)
            })]
          })]
        })]
      }), X.jsxs(`div`, {
        className: `flex justify-between items-center mt-1`,
        children: [X.jsxs(`button`, {
          className: `p-1.5 rounded flex items-center gap-1 transition-colors ${showConfig ? `text-blue-400 bg-[#333]` : `text-gray-400 hover:bg-[#333]`}`,
          onClick: evt => {
            evt.stopPropagation(), setShowConfig(!showConfig);
          },
          title: `参数配置`,
          children: [X.jsx(ne, {
            size: 14
          }), X.jsx(`span`, {
            className: `text-[10px]`,
            children: showConfig ? `收起配置` : `配置`
          })]
        }), X.jsx(`button`, {
          className: `px-4 py-1.5 rounded-full text-xs flex items-center gap-1.5 transition-all ${nodeData.audioUrl ? nodeData.loading ? `bg-blue-600/50 text-white cursor-wait` : `bg-blue-600 hover:bg-blue-500 text-white shadow-lg shadow-blue-500/20` : `bg-[#333] text-gray-500 cursor-not-allowed`}`,
          onClick: handleGenerate,
          disabled: !nodeData.audioUrl || nodeData.loading,
          children: nodeData.loading ? X.jsxs(X.Fragment, {
            children: [X.jsx(L, {
              size: 12,
              className: `animate-spin`
            }), `处理中...`]
          }) : X.jsxs(X.Fragment, {
            children: [X.jsx(vn, {
              size: 12
            }), `开始断句`]
          })
        })]
      })]
    })]
  })]
});
```
|

| audioPlayerNode | AudioPlayerNodeComp |
```js
// ── 状态 ──
let {
    updateNodeData: updateNodeData
  } = Gt(),
  nodeData = n,
  fileInputRef = Y.useRef(null),
  connectedSources = ut(t({
    handleType: `target`
  }).map(sourceId => sourceId.source)),
  resolvedUrl = (() => {
    let sourceList = Array.isArray(connectedSources) ? connectedSources : connectedSources ? [connectedSources] : [];
    for (let src of sourceList) {
      let url = ls(src?.data);
      if (url) return url;
    }
    return ``;
  })(),
  finalUrl = resolvedUrl || nodeData.audioUrl || ``,
  [playUrl, setPlayUrl] = Y.useState(``),
  [loading, setLoading] = Y.useState(false),
  [errorMsg, setErrorMsg] = Y.useState(``),
  objectUrlRef = Y.useRef(``);
Y.useEffect(() => {
  let cancelled = false,
    controller = new AbortController(),
    revokeUrl = () => {
      if (objectUrlRef.current) {
        try {
          URL.revokeObjectURL(objectUrlRef.current);
        } catch {}
        objectUrlRef.current = ``;
      }
    };
  if (!finalUrl) {
    revokeUrl(), setPlayUrl(``), setErrorMsg(``), setLoading(false);
    return;
  }
  if (finalUrl.startsWith(`data:`) || finalUrl.startsWith(`blob:`)) {
    revokeUrl(), setPlayUrl(finalUrl), setErrorMsg(``), setLoading(false);
    return;
  }
  return setLoading(true), setErrorMsg(``), (async () => {
    try {
      let trimmed = finalUrl.trim().replace(/^`+|`+$/g, ``),
        response = await fetch(trimmed, {
          signal: controller.signal
        });
      if (!response.ok) throw Error(`下载失败 HTTP ${response.status}`);
      let blob = await response.blob();
      if (cancelled) return;
      revokeUrl();
      let objectUrl = URL.createObjectURL(blob);
      objectUrlRef.current = objectUrl, setPlayUrl(objectUrl), setLoading(false);
    } catch (err) {
      if (cancelled || err?.name === `AbortError`) return;
      setPlayUrl(finalUrl), setErrorMsg(err?.message || `加载失败，尝试直接播放`), setLoading(false);
    }
  })(), () => {
    cancelled = true, controller.abort();
  };
}, [finalUrl]);
Y.useEffect(() => () => {
  if (objectUrlRef.current) try {
    URL.revokeObjectURL(objectUrlRef.current);
  } catch {}
}, []);

// ── 回调 ──
let handleFileSelect = Y.useCallback(file => {
    updateNodeData(e, {
      audioUrl: URL.createObjectURL(file),
      audioName: file.name
    });
  }, [e, updateNodeData]),
  handleClear = Y.useCallback(() => {
    updateNodeData(e, {
      audioUrl: undefined,
      audioName: undefined
    });
  }, [e, updateNodeData]),
  audioName = nodeData.audioName || (finalUrl ? decodeURIComponent(finalUrl.split(`/`).pop()?.split(`?`)[0] || `音频`) : ``),
  hasAudio = !!finalUrl,
  handleDownload = Y.useCallback(async evt => {
    if (evt.stopPropagation(), finalUrl) try {
      let link = document.createElement(`a`);
      if (finalUrl.startsWith(`blob:`) || finalUrl.startsWith(`data:`)) link.href = finalUrl;else {
        let blob = await (await fetch(finalUrl.trim().replace(/^`+|`+$/g, ``))).blob();
        link.href = URL.createObjectURL(blob);
      }
      link.download = audioName || `audio`, document.body.appendChild(link), link.click(), link.remove(), link.href.startsWith(`blob:`) && !finalUrl.startsWith(`blob:`) && setTimeout(() => URL.revokeObjectURL(link.href), 4e3);
    } catch {}
  }, [finalUrl, audioName]);

// ── 渲染 ──
return X.jsxs(`div`, {
  className: `relative flex flex-col items-center group/node transition-all ${r ? `z-50` : `z-10`}`,
  style: {
    width: `100%`,
    height: `100%`,
    minWidth: 320,
    minHeight: 200
  },
  children: [X.jsx(si, {
    id: e,
    data: n,
    defaultTitle: `音频`,
    icon: X.jsx(qt, {
      size: 11,
      className: `text-gray-500`
    })
  }), hasAudio && X.jsx(`div`, {
    className: `absolute -top-12 left-1/2 -translate-x-1/2 z-30 opacity-0 group-hover/node:opacity-100 transition-opacity pointer-events-none group-hover/node:pointer-events-auto nodrag pb-4`,
    children: X.jsxs(`div`, {
      className: `flex items-center gap-1 px-3 py-2 bg-[#1c1c1c]/90 backdrop-blur-md border border-[#333] rounded-full shadow-lg`,
      children: [X.jsx(`button`, {
        className: `p-1.5 text-gray-400 hover:text-white hover:bg-[#333] rounded-md`,
        title: `下载音频`,
        onClick: handleDownload,
        children: X.jsx(mn, {
          size: 14
        })
      }), !resolvedUrl && X.jsx(`button`, {
        className: `p-1.5 text-gray-400 hover:text-red-500 hover:bg-red-500/10 rounded-md`,
        title: `清除`,
        onClick: evt => {
          evt.stopPropagation(), handleClear();
        },
        children: X.jsx(yn, {
          size: 14
        })
      })]
    })
  }), X.jsxs(`div`, {
    className: `relative bg-[#1c1c1c] rounded-xl border shadow-xl transition-all duration-300 flex flex-col w-full h-full ${r ? `border-[#555]` : `border-[#333] hover:border-[#444]`}`,
    children: [X.jsx(_r, {
      type: `target`,
      position: J.Left,
      variant: `small`
    }), X.jsx(_r, {
      type: `source`,
      position: J.Right,
      variant: `small`
    }), X.jsx(`div`, {
      className: `flex-1 flex flex-col rounded-xl overflow-hidden min-h-0`,
      children: hasAudio ? X.jsx(fs, {
        playUrl: playUrl,
        loading: loading,
        error: errorMsg
      }) : X.jsxs(`div`, {
        className: `flex-1 flex flex-col items-center justify-center gap-2 text-gray-500 hover:text-blue-400 cursor-pointer bg-[#151515] transition-colors nodrag`,
        onClick: evt => {
          evt.stopPropagation(), fileInputRef.current?.click();
        },
        children: [X.jsx(jn, {
          size: 26
        }), X.jsx(`span`, {
          className: `text-[12px]`,
          children: `点击上传音频`
        }), X.jsx(`span`, {
          className: `text-[10px] text-gray-600`,
          children: `或从左侧连线接入`
        })]
      })
    }), X.jsx(`input`, {
      ref: fileInputRef,
      type: `file`,
      accept: `audio/*,.flac,.aac,.opus,.m4a,.wma,.aiff`,
      className: `hidden`,
      onChange: evt => {
        let file = evt.target.files?.[0];
        file && handleFileSelect(file), evt.currentTarget.value = ``;
      }
    }), X.jsx(ci, {
      minWidth: 320,
      minHeight: 200
    })]
  })]
});
```
|

| customNode | CustomNodeComp |
```js
// ── 状态 ──
let {
    updateNodeData: updateNodeData
  } = Gt(),
  nodeData = t,
  [configMode, setConfigMode] = Y.useState(nodeData.configMode === undefined ? true : nodeData.configMode),
  [variables, setVariables] = Y.useState(nodeData.config?.variables || {}),
  [extractedVars, setExtractedVars] = Y.useState([]),
  [config, setConfig] = Y.useState(nodeData.config || {
    apiUrl: ``,
    method: `POST`,
    headers: `{
  "Content-Type": "application/json"
}`,
    body: `{
  "prompt": "{{prompt}}"
}`,
    outputType: `text`,
    executionMode: `sync`,
    resultPath: `data.result`
  }),
  [aiPrompt, setAiPrompt] = Y.useState(``),
  [aiLoading, setAiLoading] = Y.useState(false);
Y.useEffect(() => {
  let text = (config.body || ``) + ` ` + (config.apiUrl || ``) + ` ` + (config.headers || ``),
    varRegex = /\{\{([^}]+)\}\}/g,
    match,
    vars = [],
    seen = new Set();
  for (; (match = varRegex.exec(text)) !== null;) {
    let name = match[1].trim();
    if (!seen.has(name)) if (seen.add(name), name.includes(`|`)) {
      let [optName, optList] = name.split(`|`);
      vars.push({
        name: optName.trim(),
        options: optList.split(`,`).map(opt => opt.trim())
      });
    } else vars.push({
      name: name
    });
  }
  setExtractedVars(vars);
}, [config.body, config.apiUrl, config.headers]);

// ── 回调 ──
let handleAiGenerate = async () => {
    if (aiPrompt.trim()) {
      if (!nodeData.onAIAssist) {
        nodeData.onShowToast?.(`AI辅助不可用，请检查API配置`);
        return;
      }
      setAiLoading(true);
      try {
        let result = await nodeData.onAIAssist(aiPrompt, config);
        try {
          let parsed = JSON.parse(result);
          setConfig(cfg => ({
            ...cfg,
            apiUrl: parsed.apiUrl || cfg.apiUrl,
            method: parsed.method || cfg.method,
            headers: parsed.headers || cfg.headers,
            body: parsed.body || cfg.body,
            outputType: parsed.outputType || cfg.outputType,
            executionMode: parsed.executionMode || cfg.executionMode,
            resultPath: parsed.resultPath || cfg.resultPath,
            taskIdPath: parsed.taskIdPath || cfg.taskIdPath,
            pollingUrl: parsed.pollingUrl || cfg.pollingUrl,
            pollingMethod: parsed.pollingMethod || cfg.pollingMethod,
            pollingHeaders: parsed.pollingHeaders || cfg.pollingHeaders,
            pollingBody: parsed.pollingBody || cfg.pollingBody,
            pollingResultPath: parsed.pollingResultPath || cfg.pollingResultPath,
            pollingCompletedValue: parsed.pollingCompletedValue || cfg.pollingCompletedValue,
            pollingFailedValue: parsed.pollingFailedValue || cfg.pollingFailedValue,
            pollingErrorPath: parsed.pollingErrorPath || cfg.pollingErrorPath,
            pollingProgressPath: parsed.pollingProgressPath === undefined ? cfg.pollingProgressPath : parsed.pollingProgressPath,
            pollingResultDataPath: parsed.pollingResultDataPath === undefined ? cfg.pollingResultDataPath : parsed.pollingResultDataPath,
            rawTextOutput: parsed.rawTextOutput === undefined ? cfg.rawTextOutput : parsed.rawTextOutput
          })), nodeData.onShowToast?.(`AI 生成配置成功`);
        } catch (parseErr) {
          console.error(`AI 返回的 JSON 解析失败`, parseErr, result), nodeData.onShowToast?.(`AI 生成格式错误，请重试`);
        }
      } catch (err) {
        nodeData.onShowToast?.(err.message || `AI 生成失败`);
      } finally {
        setAiLoading(false);
      }
    }
  },
  handleCompleteConfig = () => {
    updateNodeData(e, {
      config: {
        ...config,
        variables: variables
      },
      configMode: false
    }), setConfigMode(false);
  },
  handleSaveTemplate = () => {
    if (!config.apiUrl) {
      nodeData.onShowToast?.(`请至少填写 API URL`);
      return;
    }
    let name = window.prompt(`请输入自定义节点名称:`, t.label || `万能节点`);
    name && nodeData.onSaveTemplate && nodeData.onSaveTemplate(name, {
      ...config,
      variables: variables
    });
  },
  handleRun = evt => {
    if (evt.stopPropagation(), configMode) {
      nodeData.onShowToast?.(`请先完成配置`);
      return;
    }
    let cfg = {
      ...config,
      variables: variables
    };
    setConfig(cfg), updateNodeData(e, {
      config: cfg
    }), setTimeout(() => {
      console.log(`CustomNode handleRun triggered, calling onGenerateCustom`, nodeData.onGenerateCustom), nodeData.onGenerateCustom ? nodeData.onGenerateCustom(e) : nodeData.onShowToast?.(`未找到执行方法，请刷新页面重试`);
    }, 50);
  },
  urlifyAsset = async (varName, file) => {
    try {
      let uploaded = await ii(file, {
        subfolder: `canvas/upload`,
        preferThumbnail: file.type.startsWith(`image/`),
        thumbMaxDim: 480,
        thumbQuality: 75
      });
      if (uploaded.url && /^https?:\/\//i.test(uploaded.url)) {
        setVariables(vars => ({
          ...vars,
          [varName]: uploaded.url
        }));
        return;
      }
    } catch (err) {
      console.warn(`[CustomNode] urlifyAsset failed, fallback to base64:`, err);
    }
    let reader = new FileReader();
    reader.onload = evt => {
      evt.target?.result && setVariables(vars => ({
        ...vars,
        [varName]: evt.target.result
      }));
    }, reader.readAsDataURL(file);
  };

// ── 渲染 ──
return X.jsxs(`div`, {
  className: `flex flex-col items-center group/node transition-all ${n ? `z-50` : `z-10`}`,
  children: [X.jsx(si, {
    id: e,
    data: t,
    defaultTitle: `万能节点`,
    icon: X.jsx(He, {
      size: 11,
      className: `text-gray-500`
    })
  }), X.jsxs(`div`, {
    className: `relative bg-[#1c1c1c] rounded-xl overflow-visible border shadow-xl transition-all duration-300 flex flex-col
                ${n ? `border-[#555]` : `border-[#333] hover:border-[#444]`}
                `,
    style: {
      width: `400px`,
      minHeight: configMode ? `450px` : `250px`
    },
    children: [X.jsxs(`div`, {
      className: `absolute top-2 right-2 z-20 flex items-center gap-2 nodrag`,
      children: [nodeData.loading && X.jsx(L, {
        size: 12,
        className: `animate-spin flex-shrink-0`,
        style: {
          color: `rgb(210,2,7)`
        }
      }), X.jsxs(`div`, {
        className: `flex bg-[#0d0c0c]/90 rounded p-0.5 border border-[#333]`,
        children: [X.jsx(`button`, {
          className: `px-2 py-1 text-[10px] rounded transition-colors ${configMode ? `bg-[#333] text-white` : `text-gray-400 hover:text-gray-200`}`,
          onClick: () => {
            setConfigMode(true), updateNodeData(e, {
              configMode: true
            });
          },
          children: `编辑模式`
        }), X.jsx(`button`, {
          className: `px-2 py-1 text-[10px] rounded transition-colors ${configMode ? `text-gray-400 hover:text-gray-200` : `bg-[#333] text-white`}`,
          onClick: () => {
            setConfigMode(false), updateNodeData(e, {
              configMode: false
            });
          },
          children: `工作模式`
        })]
      })]
    }), X.jsxs(`div`, {
      className: `flex-1 flex flex-col p-3 bg-[#1a1a1a] relative drag-handle rounded-xl`,
      children: [nodeData.loading && X.jsxs(`div`, {
        className: `absolute inset-0 flex flex-col items-center justify-center gap-2 text-gray-500 bg-[#1a1a1a]/80 backdrop-blur-sm z-10`,
        children: [X.jsx(ui, {
          size: 24
        }), X.jsx(`span`, {
          className: `text-xs`,
          children: config.executionMode === `async` ? `请求中... ${nodeData.progress || 0}%` : `请求中...`
        }), X.jsxs(`button`, {
          onClick: evt => {
            evt.stopPropagation(), nodeData.onStop && nodeData.onStop(e);
          },
          className: `mt-2 bg-[#222]/80 hover:bg-[#333] border border-[#444] text-gray-400 hover:text-gray-200 px-3 py-1 rounded-full text-[10px] flex items-center gap-1.5 transition-colors backdrop-blur-sm nodrag`,
          children: [X.jsx(oe, {
            size: 10,
            fill: `currentColor`
          }), `停止`]
        })]
      }), nodeData.errorMessage && X.jsxs(`div`, {
        className: `text-red-400 text-[10px] p-2 mb-2 border border-red-500/30 rounded bg-red-500/10 flex items-start gap-1.5`,
        children: [X.jsx(pt, {
          size: 12,
          className: `mt-0.5 flex-shrink-0`
        }), X.jsx(`span`, {
          className: `break-all`,
          children: nodeData.errorMessage
        })]
      }), configMode ? X.jsxs(`div`, {
        className: `flex flex-col gap-3 nodrag text-xs`,
        children: [X.jsxs(`div`, {
          className: `flex flex-col gap-1`,
          children: [X.jsxs(`label`, {
            className: `text-gray-500 flex items-center gap-1`,
            children: [X.jsx(pe, {
              size: 12,
              className: `text-yellow-500`
            }), `AI 辅助配置`]
          }), X.jsxs(`div`, {
            className: `flex flex-col gap-2`,
            children: [X.jsx(`textarea`, {
              className: `flex-1 bg-[#0d0c0c] border border-[#333] rounded p-2 text-gray-200 focus:border-blue-500 outline-none custom-scrollbar text-[10px] resize-y nodrag nowheel nopan`,
              placeholder: `描述你想调用的API... (如：调用百度翻译)`,
              value: aiPrompt,
              onChange: evt => setAiPrompt(evt.target.value),
              onKeyDown: evt => {
                evt.key === `Enter` && (evt.ctrlKey || evt.metaKey) && handleAiGenerate();
              },
              onWheel: evt => evt.stopPropagation(),
              rows: 3
            }), X.jsxs(`button`, {
              onClick: handleAiGenerate,
              disabled: aiLoading,
              className: `py-1.5 w-full bg-blue-600/20 hover:bg-blue-600/40 text-blue-400 border border-blue-500/30 rounded transition-colors flex items-center justify-center gap-1`,
              children: [aiLoading ? X.jsx(L, {
                size: 12,
                className: `animate-spin`
              }) : `生成`, !aiLoading && X.jsx(`span`, {
                className: `text-[10px] text-blue-400/70`,
                children: `(Ctrl+Enter)`
              })]
            })]
          })]
        }), X.jsxs(`div`, {
          className: `flex gap-2`,
          children: [X.jsxs(`div`, {
            className: `flex flex-col gap-1 w-20`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `Method`
            }), X.jsxs(`select`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-1 py-1 text-gray-200 outline-none`,
              value: config.method,
              onChange: evt => setConfig({
                ...config,
                method: evt.target.value
              }),
              children: [X.jsx(`option`, {
                children: `GET`
              }), X.jsx(`option`, {
                children: `POST`
              }), X.jsx(`option`, {
                children: `PUT`
              })]
            })]
          }), X.jsxs(`div`, {
            className: `flex flex-col gap-1 flex-1`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `API URL`
            }), X.jsx(`input`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 focus:border-blue-500 outline-none`,
              value: config.apiUrl,
              onChange: evt => setConfig({
                ...config,
                apiUrl: evt.target.value
              })
            })]
          })]
        }), X.jsxs(`div`, {
          className: `flex flex-col gap-1`,
          children: [X.jsxs(`div`, {
            className: `flex justify-between items-center`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `Headers (JSON格式)`
            }), X.jsxs(`div`, {
              className: `flex gap-1`,
              children: [X.jsx(`button`, {
                onClick: () => setConfig({
                  ...config,
                  headers: `{
  "Content-Type": "application/json"
}`
                }),
                className: `text-[9px] bg-[#333] hover:bg-[#444] px-1.5 py-0.5 rounded text-gray-300 transition-colors`,
                children: `JSON`
              }), X.jsx(`button`, {
                onClick: () => setConfig({
                  ...config,
                  headers: `{
  "Content-Type": "multipart/form-data"
}`
                }),
                className: `text-[9px] bg-[#333] hover:bg-[#444] px-1.5 py-0.5 rounded text-gray-300 transition-colors`,
                children: `FormData`
              })]
            })]
          }), X.jsx(`textarea`, {
            className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 font-mono text-[10px] h-16 resize-y focus:border-blue-500 outline-none custom-scrollbar nodrag nowheel nopan`,
            value: config.headers,
            onChange: evt => setConfig({
              ...config,
              headers: evt.target.value
            }),
            onWheel: evt => evt.stopPropagation()
          })]
        }), X.jsxs(`div`, {
          className: `flex flex-col gap-1`,
          children: [X.jsx(`label`, {
            className: `text-gray-500 flex justify-between`,
            children: X.jsxs(`span`, {
              children: [`Body (支持变量: `, `{{prompt}}`, `, `, `{{image_1}}`, `)`]
            })
          }), X.jsx(`textarea`, {
            className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 font-mono text-[10px] h-24 resize-y focus:border-blue-500 outline-none custom-scrollbar nodrag nowheel nopan`,
            value: config.body,
            onChange: evt => setConfig({
              ...config,
              body: evt.target.value
            }),
            onWheel: evt => evt.stopPropagation()
          })]
        }), X.jsxs(`div`, {
          className: `flex gap-2`,
          children: [X.jsxs(`div`, {
            className: `flex flex-col gap-1 flex-1`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `输出类型`
            }), X.jsxs(`select`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-1 py-1 text-gray-200 outline-none`,
              value: config.outputType,
              onChange: evt => setConfig({
                ...config,
                outputType: evt.target.value
              }),
              children: [X.jsx(`option`, {
                value: `text`,
                children: `文本 (Text)`
              }), X.jsx(`option`, {
                value: `image`,
                children: `图片 (Image URL)`
              }), X.jsx(`option`, {
                value: `video`,
                children: `视频 (Video URL)`
              }), X.jsx(`option`, {
                value: `audio`,
                children: `音频 (Audio URL)`
              })]
            })]
          }), X.jsxs(`div`, {
            className: `flex flex-col gap-1 flex-1`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `执行模式`
            }), X.jsxs(`select`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-1 py-1 text-gray-200 outline-none`,
              value: config.executionMode,
              onChange: evt => setConfig({
                ...config,
                executionMode: evt.target.value
              }),
              children: [X.jsx(`option`, {
                value: `sync`,
                children: `同步 (立即返回)`
              }), X.jsx(`option`, {
                value: `async`,
                children: `异步 (需轮询)`
              })]
            })]
          })]
        }), X.jsxs(`div`, {
          className: `flex gap-2`,
          children: [X.jsxs(`div`, {
            className: `flex flex-col gap-1 flex-1`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `提取结果字段 (JSON Path, 如 data.url)`
            }), X.jsx(`input`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 focus:border-blue-500 outline-none`,
              value: config.resultPath,
              onChange: evt => setConfig({
                ...config,
                resultPath: evt.target.value
              }),
              placeholder: `如 choices[0].message.content`
            })]
          }), config.outputType === `text` && X.jsxs(`div`, {
            className: `flex flex-col gap-1 w-24`,
            children: [X.jsx(`label`, {
              className: `text-gray-500 text-center`,
              children: `纯文本输出`
            }), X.jsx(`div`, {
              className: `flex items-center justify-center h-full`,
              children: X.jsx(`input`, {
                type: `checkbox`,
                checked: config.rawTextOutput || false,
                onChange: evt => setConfig({
                  ...config,
                  rawTextOutput: evt.target.checked
                }),
                className: `w-4 h-4 accent-blue-500 cursor-pointer`
              })
            })]
          })]
        }), config.executionMode === `async` && X.jsxs(`div`, {
          className: `flex flex-col gap-2 p-2 bg-[#222] border border-[#333] rounded mt-1`,
          children: [X.jsxs(`div`, {
            className: `flex flex-col gap-1`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `提取 Task ID 字段`
            }), X.jsx(`input`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none`,
              value: config.taskIdPath || ``,
              onChange: evt => setConfig({
                ...config,
                taskIdPath: evt.target.value
              }),
              placeholder: `如 data.task_id`
            })]
          }), X.jsxs(`div`, {
            className: `flex gap-2`,
            children: [X.jsxs(`div`, {
              className: `flex flex-col gap-1 w-24`,
              children: [X.jsx(`label`, {
                className: `text-gray-500`,
                children: `轮询 Method`
              }), X.jsxs(`select`, {
                className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none h-[30px]`,
                value: config.pollingMethod || `GET`,
                onChange: evt => setConfig({
                  ...config,
                  pollingMethod: evt.target.value
                }),
                children: [X.jsx(`option`, {
                  value: `GET`,
                  children: `GET`
                }), X.jsx(`option`, {
                  value: `POST`,
                  children: `POST`
                })]
              })]
            }), X.jsxs(`div`, {
              className: `flex flex-col gap-1 flex-1`,
              children: [X.jsxs(`label`, {
                className: `text-gray-500`,
                children: [`轮询 API URL (支持 `, `{{task_id}}`, `)`]
              }), X.jsx(`input`, {
                className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none h-[30px]`,
                value: config.pollingUrl || ``,
                onChange: evt => setConfig({
                  ...config,
                  pollingUrl: evt.target.value
                }),
                placeholder: `如果与上方一致可留空`
              })]
            })]
          }), X.jsxs(`div`, {
            className: `flex flex-col gap-1`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `轮询 Headers (JSON格式, 留空同上)`
            }), X.jsx(`textarea`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 font-mono text-[10px] h-20 resize-y focus:border-blue-500 outline-none custom-scrollbar nodrag nowheel nopan`,
              value: config.pollingHeaders || ``,
              onChange: evt => setConfig({
                ...config,
                pollingHeaders: evt.target.value
              }),
              placeholder: `例如: {"Authorization": "Bearer xxx"}`,
              onWheel: evt => evt.stopPropagation()
            })]
          }), X.jsxs(`div`, {
            className: `flex flex-col gap-1 ${config.pollingMethod === `GET` || !config.pollingMethod ? `hidden` : ``}`,
            children: [X.jsxs(`label`, {
              className: `text-gray-500`,
              children: [`轮询 Body (JSON格式, 支持 `, `{{task_id}}`, `)`]
            }), X.jsx(`textarea`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 font-mono text-[10px] h-12 resize-y focus:border-blue-500 outline-none custom-scrollbar nodrag`,
              value: config.pollingBody || ``,
              onChange: evt => setConfig({
                ...config,
                pollingBody: evt.target.value
              }),
              placeholder: `例如: {"taskId": "{{task_id}}"}`,
              onWheel: evt => evt.stopPropagation()
            })]
          }), X.jsxs(`div`, {
            className: `flex gap-2`,
            children: [X.jsxs(`div`, {
              className: `flex flex-col gap-1 flex-1`,
              children: [X.jsx(`label`, {
                className: `text-gray-500`,
                children: `状态判断字段`
              }), X.jsx(`input`, {
                className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none`,
                value: config.pollingResultPath || ``,
                onChange: evt => setConfig({
                  ...config,
                  pollingResultPath: evt.target.value
                }),
                placeholder: `如 data.status`
              })]
            }), X.jsxs(`div`, {
              className: `flex flex-col gap-1 flex-1`,
              children: [X.jsx(`label`, {
                className: `text-gray-500`,
                children: `完成状态值`
              }), X.jsx(`input`, {
                className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none`,
                value: config.pollingCompletedValue || ``,
                onChange: evt => setConfig({
                  ...config,
                  pollingCompletedValue: evt.target.value
                }),
                placeholder: `如 completed`
              })]
            })]
          }), X.jsxs(`div`, {
            className: `flex gap-2`,
            children: [X.jsxs(`div`, {
              className: `flex flex-col gap-1 flex-1`,
              children: [X.jsx(`label`, {
                className: `text-gray-500`,
                children: `失败状态值`
              }), X.jsx(`input`, {
                className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none`,
                value: config.pollingFailedValue || ``,
                onChange: evt => setConfig({
                  ...config,
                  pollingFailedValue: evt.target.value
                }),
                placeholder: `如 failed`
              })]
            }), X.jsxs(`div`, {
              className: `flex flex-col gap-1 flex-1`,
              children: [X.jsx(`label`, {
                className: `text-gray-500`,
                children: `失败信息字段`
              }), X.jsx(`input`, {
                className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none`,
                value: config.pollingErrorPath || ``,
                onChange: evt => setConfig({
                  ...config,
                  pollingErrorPath: evt.target.value
                }),
                placeholder: `如 data.error`
              })]
            })]
          }), X.jsxs(`div`, {
            className: `flex flex-col gap-1`,
            children: [X.jsx(`label`, {
              className: `text-gray-500`,
              children: `进度判断字段`
            }), X.jsx(`input`, {
              className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none`,
              value: config.pollingProgressPath || ``,
              onChange: evt => setConfig({
                ...config,
                pollingProgressPath: evt.target.value
              }),
              placeholder: `如 data.progress (选填)`
            })]
          }), X.jsxs(`div`, {
            className: `flex gap-2`,
            children: [X.jsxs(`div`, {
              className: `flex flex-col gap-1 flex-1`,
              children: [X.jsx(`label`, {
                className: `text-gray-500`,
                children: `异步结果提取字段 (如轮询返回的 data.url)`
              }), X.jsx(`input`, {
                className: `bg-[#0d0c0c] border border-[#333] rounded px-2 py-1 text-gray-200 outline-none`,
                value: config.pollingResultDataPath || ``,
                onChange: evt => setConfig({
                  ...config,
                  pollingResultDataPath: evt.target.value
                }),
                placeholder: `留空则使用上方主请求提取字段`
              })]
            }), config.outputType === `text` && X.jsxs(`div`, {
              className: `flex flex-col gap-1 w-24`,
              children: [X.jsx(`label`, {
                className: `text-gray-500 text-center`,
                children: `纯文本输出`
              }), X.jsx(`div`, {
                className: `flex items-center justify-center h-full`,
                children: X.jsx(`input`, {
                  type: `checkbox`,
                  checked: config.rawTextOutput || false,
                  onChange: evt => setConfig({
                    ...config,
                    rawTextOutput: evt.target.checked
                  }),
                  className: `w-4 h-4 accent-blue-500 cursor-pointer`
                })
              })]
            })]
          })]
        }), X.jsxs(`div`, {
          className: `flex gap-2 mt-2`,
          children: [X.jsx(`button`, {
            onClick: handleCompleteConfig,
            className: `flex-1 py-1.5 bg-blue-600 hover:bg-blue-500 text-white rounded transition-colors text-xs`,
            children: `完成配置`
          }), X.jsxs(`button`, {
            onClick: handleSaveTemplate,
            className: `py-1.5 px-3 bg-[#333] hover:bg-[#444] text-white rounded transition-colors flex items-center justify-center gap-1 text-xs`,
            title: `保存为自定义节点供下次使用`,
            children: [X.jsx(De, {
              size: 12
            }), `保存模板`]
          })]
        })]
      }) : X.jsx(`div`, {
        className: `flex flex-col h-full nodrag`,
        children: X.jsxs(`div`, {
          className: `flex-1 flex flex-col min-h-[100px] pr-1`,
          children: [nodeData.resultData && X.jsxs(`div`, {
            className: `flex-1 bg-[#0d0c0c] border border-[#333] rounded p-2 mb-2 overflow-auto custom-scrollbar flex min-h-[60px] max-h-[250px] ${config.outputType === `text` ? `items-start justify-start` : `items-center justify-center`}`,
            children: [config.outputType === `text` && X.jsx(`div`, {
              className: `text-gray-300 text-xs whitespace-pre-wrap w-full align-top break-all`,
              children: nodeData.resultData
            }), config.outputType === `image` && X.jsx(`img`, {
              src: nodeData.resultData,
              loading: `lazy`,
              decoding: `async`,
              className: `max-w-full max-h-full object-contain`
            }), config.outputType === `video` && X.jsx(`video`, {
              src: nodeData.resultData,
              controls: true,
              preload: `metadata`,
              className: `max-w-full max-h-full`
            }), config.outputType === `audio` && X.jsx(`audio`, {
              src: nodeData.resultData,
              controls: true,
              className: `w-full`
            })]
          }), X.jsx(`div`, {
            className: `flex flex-col gap-3 mt-auto pt-2 pb-2`,
            children: extractedVars.length > 0 ? X.jsx(X.Fragment, {
              children: extractedVars.map(varDef => X.jsxs(`div`, {
                className: `flex flex-col gap-1 relative nodrag`,
                children: [X.jsx(`div`, {
                  className: `absolute top-1/2 -translate-y-1/2`,
                  style: {
                    left: `-12px`
                  },
                  children: X.jsx(_r, {
                    type: `target`,
                    id: `var-${varDef.name}`,
                    position: J.Left,
                    variant: `small`,
                    title: `连接到变量: ${varDef.name}`
                  })
                }), X.jsxs(`div`, {
                  className: `flex justify-between items-center mb-1`,
                  children: [X.jsx(`label`, {
                    className: `text-gray-400 text-[10px] ml-1`,
                    children: varDef.name
                  }), !varDef.options && !varDef.name.startsWith(`image`) && !varDef.name.startsWith(`audio`) && !varDef.name.startsWith(`video`) && !varDef.name.startsWith(`file`) && X.jsxs(`div`, {
                    className: `flex items-center gap-1 text-[9px]`,
                    children: [X.jsx(`span`, {
                      className: `${config.variableFormats?.[varDef.name] === `json` ? `text-gray-500` : `text-blue-400 font-bold`}`,
                      children: `Text`
                    }), X.jsx(`div`, {
                      className: `w-5 h-2.5 bg-[#333] rounded-full relative cursor-pointer`,
                      onClick: () => {
                        let fmt = (config.variableFormats?.[varDef.name] || `text`) === `text` ? `json` : `text`;
                        setConfig(cfg => ({
                          ...cfg,
                          variableFormats: {
                            ...cfg.variableFormats,
                            [varDef.name]: fmt
                          }
                        }));
                      },
                      children: X.jsx(`div`, {
                        className: `absolute top-[1px] w-2 h-2 rounded-full transition-all ${config.variableFormats?.[varDef.name] === `json` ? `bg-blue-400 right-[1px]` : `bg-gray-400 left-[1px]`}`
                      })
                    }), X.jsx(`span`, {
                      className: `${config.variableFormats?.[varDef.name] === `json` ? `text-blue-400 font-bold` : `text-gray-500`}`,
                      children: `JSON`
                    })]
                  })]
                }), varDef.options ? X.jsx(`select`, {
                  className: `w-full bg-[#0d0c0c] border border-[#333] rounded px-2 py-1.5 text-xs text-gray-200 outline-none focus:border-blue-500`,
                  value: variables[varDef.name] || varDef.options[0],
                  onChange: evt => setVariables(vars => ({
                    ...vars,
                    [varDef.name]: evt.target.value
                  })),
                  children: varDef.options.map(opt => X.jsx(`option`, {
                    value: opt,
                    children: opt
                  }, opt))
                }) : varDef.name.startsWith(`image`) || varDef.name.startsWith(`audio`) || varDef.name.startsWith(`video`) || varDef.name.startsWith(`file`) ? X.jsx(`div`, {
                  className: `flex items-center gap-2`,
                  children: variables[varDef.name] ? X.jsxs(`div`, {
                    className: `relative w-full h-12 rounded overflow-hidden border border-[#444] flex items-center justify-center bg-[#0d0c0c]`,
                    children: [varDef.name.startsWith(`image`) && X.jsx(`img`, {
                      src: variables[varDef.name],
                      loading: `lazy`,
                      decoding: `async`,
                      className: `w-full h-full object-cover`
                    }), varDef.name.startsWith(`audio`) && X.jsx(`audio`, {
                      src: variables[varDef.name],
                      controls: true,
                      className: `w-full h-full`
                    }), varDef.name.startsWith(`video`) && X.jsx(`video`, {
                      src: variables[varDef.name],
                      preload: `metadata`,
                      className: `w-full h-full object-cover`
                    }), varDef.name.startsWith(`file`) && X.jsx(`div`, {
                      className: `text-xs text-gray-400 break-all p-1 text-center line-clamp-2`,
                      children: `文件已上传`
                    }), X.jsx(`button`, {
                      onClick: () => setVariables(vars => {
                        let next = {
                          ...vars
                        };
                        return delete next[varDef.name], next;
                      }),
                      className: `absolute top-0 right-0 bg-red-500/80 text-white p-0.5 rounded-bl z-10`,
                      children: X.jsx(oe, {
                        size: 8,
                        fill: `currentColor`
                      })
                    })]
                  }) : X.jsxs(`label`, {
                    className: `flex-1 border border-dashed border-[#444] hover:border-blue-500 rounded p-2 flex items-center justify-center cursor-pointer text-gray-500 hover:text-blue-400 transition-colors text-xs`,
                    children: [X.jsx(jn, {
                      size: 12,
                      className: `mr-1`
                    }), varDef.name.startsWith(`image`) ? `上传图片` : varDef.name.startsWith(`audio`) ? `上传音频` : varDef.name.startsWith(`video`) ? `上传视频` : `上传文件`, X.jsx(`input`, {
                      type: `file`,
                      accept: varDef.name.startsWith(`image`) ? `image/*` : varDef.name.startsWith(`audio`) ? `audio/*` : varDef.name.startsWith(`video`) ? `video/*` : `*/*`,
                      className: `hidden`,
                      onChange: evt => {
                        evt.target.files?.[0] && urlifyAsset(varDef.name, evt.target.files[0]);
                      }
                    })]
                  })
                }) : X.jsx(`textarea`, {
                  className: `w-full bg-[#0d0c0c] border border-[#333] rounded px-2 py-1.5 text-xs text-gray-200 outline-none focus:border-blue-500 custom-scrollbar resize-y nodrag min-h-[30px]`,
                  placeholder: `输入 ${varDef.name}...`,
                  value: variables[varDef.name] || ``,
                  onChange: evt => setVariables(vars => ({
                    ...vars,
                    [varDef.name]: evt.target.value
                  })),
                  onWheel: evt => evt.stopPropagation()
                })]
              }, varDef.name))
            }) : X.jsxs(`div`, {
              className: `text-gray-500 text-xs text-center py-4 border border-dashed border-[#444] rounded`,
              children: [`当前配置未提取到变量。`, X.jsx(`br`, {}), `在编辑模式下使用 `, `{{变量名}}`, ` 添加变量。`]
            })
          }), X.jsx(`div`, {
            className: `mt-auto pt-2`,
            children: X.jsxs(`button`, {
              onClick: evt => {
                evt.stopPropagation(), handleRun(evt);
              },
              disabled: nodeData.loading,
              className: `w-full py-2 bg-white hover:bg-gray-100 text-black rounded-lg flex items-center justify-center gap-1.5 transition-colors shadow-sm disabled:opacity-50 font-medium text-sm`,
              children: [nodeData.loading ? X.jsx(L, {
                size: 14,
                className: `animate-spin`
              }) : X.jsx(ct, {
                size: 14,
                fill: `currentColor`
              }), nodeData.loading ? `处理中...` : `开始处理`]
            })
          })]
        })
      })]
    }), X.jsx(_r, {
      type: `source`,
      position: J.Right,
      variant: `small`
    })]
  })]
});
```
|

<!-- END_AB04 -->
