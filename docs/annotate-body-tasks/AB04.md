# AB04 — 节点函数体重写（3 个）

> 在 App.js 内重写函数体。不改文件结构，写坏只影响一个节点。

## 规则

1. **不改函数签名和模块级变量名**（ImageNodeComp 等已语义化）
2. **重命名函数体内局部变量**：`a,b,c` → `loading, setLoading`
3. **加段落注释**：`// ── 状态 ──` `// ── 回调 ──` `// ── 渲染 ──`
4. **保持 vendor/React 用法不变**：不动 `Y.useState`、`X.jsx` 等
5. **不改 JSX 结构**：不动 children 树
6. **整个函数体替换**：读 App.js 中完整 `Y.memo(({...}) => { ... })` 块，重写后粘贴回去

## 示例

```js
// 重写前
var ImageNodeComp = Y.memo(({ id, data, selected }) => {
  let [a,b] = Y.useState(false);
  let c = Y.useRef(null);
  let d = Y.useCallback(() => { b(true); upload(); b(false); }, []);
  return X.jsxs("div", { ... });
});

// 重写后
var ImageNodeComp = Y.memo(({ id, data, selected }) => {
  // ── 状态 ──
  const [loading, setLoading] = Y.useState(false);

  // ── 上传 ──
  const handleUpload = Y.useCallback(() => {
    setLoading(true);
    uploadImageToEngine();
    setLoading(false);
  }, []);

  // ── 渲染 ──
  return X.jsxs("div", { ... });
});
```

## 任务节点

1. **audioNode** (`AudioTranscribeNodeComp`)
2. **audioPlayerNode** (`AudioPlayerNodeComp`)
3. **customNode** (`CustomNodeComp`)

## 参考
- 节点契约：docs/节点组件契约表.md
- 函数摘要：`node scripts/summarize.cjs ImageNodeComp`
- 改完后：`npm run build && node scripts/check-build.cjs` 验证
