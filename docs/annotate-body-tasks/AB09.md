# AB09 — 节点函数体重写（3 个）

> 🚫 **禁止直接修改 src/App.js！** 只在本文件代码块中提交重写结果。运行 `node scripts/merge-rewrites.cjs` 统一写入。
> ⚠️ 铁律：改写后功能必须和原来完全一样。只改局部变量名和加注释，不改任何逻辑。

## 规则
1. 不改函数签名和模块级变量名
2. 重命名函数体内局部变量：a,b,c→loading,setLoading
3. 加段落注释：`// ── 状态 ──` `// ── 回调 ──` `// ── 渲染 ──`
4. 保持 vendor/React 用法不变：不动 Y.useState、X.jsx 等
5. 不改 JSX 结构

## 提交格式（重要！）
```
| imageNode | ImageNodeComp |
```js
// ── 状态 ──
const [loading, setLoading] = Y.useState(false);
// ...
```
|
```

## 任务
1. **stickyNoteNode** (`StickyNoteNodeComp`)
2. **group** (`GroupNodeComp`)
3. **ghostTarget** (`Nh`)

## 参考
- 节点契约：docs/节点组件契约表.md
- 函数摘要：`node scripts/summarize.cjs StickyNoteNodeComp`

---

| group | GroupNodeComp |
```js
// ── 状态 ──
let {
    updateNodeData: updateNodeData,
    setNodes: setNodes
  } = Gt(),
  [editing, setEditing] = Y.useState(false),
  [name, setName] = Y.useState(t?.name || `编组`),
  inputRef = Y.useRef(null),
  collapsed = t?.collapsed || false;

// ── 副作用：进入编辑态后自动聚焦并全选 ──
Y.useEffect(() => {
  editing && inputRef.current && (inputRef.current.focus(), inputRef.current.select());
}, [editing]);

// ── 回调：改名输入 / 提交 / 回车 / 折叠展开 ──
let onNameChange = e => {
    setName(e.target.value);
  },
  onNameCommit = () => {
    setEditing(false), updateNodeData(e, {
      name: name
    });
  },
  onNameKeyDown = e => {
    e.key === `Enter` && onNameCommit();
  },
  onToggleCollapse = t => {
    t.stopPropagation();
    let n = !collapsed;
    setNodes(t => t.map(t => {
      if (t.id === e) if (n) {
        let e = t.style?.width || t.measured?.width || 300,
          n = t.style?.height || t.measured?.height || 200;
        return {
          ...t,
          data: {
            ...t.data,
            collapsed: true,
            expandedWidth: e,
            expandedHeight: n
          },
          style: {
            ...t.style,
            width: `max-content`,
            height: 40,
            backgroundColor: `transparent`,
            border: `none`
          }
        };
      } else return {
        ...t,
        data: {
          ...t.data,
          collapsed: false
        },
        style: {
          ...t.style,
          width: t.data?.expandedWidth || 300,
          height: t.data?.expandedHeight || 200,
          backgroundColor: undefined,
          border: undefined
        }
      };
      return t;
    }));
  };

// ── 渲染：折叠态 / 展开态 ──
return collapsed ? X.jsxs(`div`, {
  className: `relative flex items-center justify-center bg-[#2a1f24] border border-dashed ${n ? `border-[#555]` : `border-[#444]`} rounded-xl px-4 py-2 shadow-lg min-w-[120px] h-[40px] cursor-pointer hover:bg-[#352a30] hover:border-gray-400 transition-all duration-300`,
  onClick: onToggleCollapse,
  children: [X.jsx(E, {
    type: `target`,
    position: J.Left,
    className: `!w-2 !h-2 !bg-gray-500 !border-gray-600 !opacity-0`
  }), X.jsx(_t, {
    className: `w-4 h-4 text-gray-400 mr-1`
  }), X.jsx(mt, {
    className: `w-4 h-4 text-[#8b92a5] mr-2`
  }), X.jsx(`span`, {
    className: `text-gray-300 text-sm select-none`,
    children: name
  }), X.jsx(E, {
    type: `source`,
    position: J.Right,
    className: `!w-2 !h-2 !bg-gray-500 !border-gray-600 !opacity-0`
  })]
}) : X.jsx(`div`, {
  className: `relative w-full h-full rounded-xl transition-all duration-300 ${n ? `border border-[#555]` : `border border-transparent hover:border-white/10`} bg-[#1e171b]/50 hover:bg-[#161214] group`,
  children: X.jsxs(`div`, {
    className: `absolute -top-8 left-0 flex items-center px-2 py-1`,
    onDoubleClick: () => setEditing(true),
    children: [X.jsx(`button`, {
      onClick: onToggleCollapse,
      className: `mr-1 hover:bg-white/10 rounded p-0.5 transition-colors`,
      children: X.jsx(Sn, {
        className: `w-4 h-4 text-gray-500 group-hover:text-gray-300`
      })
    }), X.jsx(mt, {
      className: `w-4 h-4 text-[#8b92a5] mr-1.5`
    }), editing ? X.jsx(`input`, {
      ref: inputRef,
      type: `text`,
      value: name,
      onChange: onNameChange,
      onBlur: onNameCommit,
      onKeyDown: onNameKeyDown,
      className: `bg-[#2a2a2a] border border-[#444] rounded outline-none text-gray-200 text-sm w-32 focus:border-blue-500 px-1 py-0.5`
    }) : X.jsx(`span`, {
      className: `text-gray-400 group-hover:text-gray-300 text-sm select-none cursor-text transition-colors`,
      children: name
    })]
  })
});
```
|
```

| ghostTarget | Nh |
```js
// ── 状态 ──
// (无局部状态)

// ── 渲染：透明 1x1 占位 handle，仅用于连线路由 ──
return X.jsx(`div`, {
  style: {
    width: 1,
    height: 1,
    opacity: 0,
    pointerEvents: `none`
  },
  children: X.jsx(E, {
    type: `target`,
    position: J.Left,
    style: {
      background: `transparent`,
      border: 0,
      width: 1,
      height: 1,
      opacity: 0
    },
    isConnectable: false
  })
});
```
|
```

| stickyNoteNode | StickyNoteNodeComp |
```js
// ── 状态 ──
let n = t,
  {
    updateNodeData: updateNodeData
  } = Gt(),
  [html, setHtml] = Y.useState(n.html ?? (n.text ? Hh(n.text) : ``)),
  [fontSize, setFontSize] = Y.useState(n.fontSize ?? 24),
  [bold, setBold] = Y.useState(n.bold ?? false),
  [color, setColor] = Y.useState(n.color ?? Rh[0].value),
  [bgColor, setBgColor] = Y.useState(n.bgColor ?? Lh[0].value),
  [width, setWidth] = Y.useState(n.width ?? 400),
  [height, setHeight] = Y.useState(n.height ?? 400),
  [isEditing, setIsEditing] = Y.useState(false),
  [menuPos, setMenuPos] = Y.useState(null),
  [showEmoji, setShowEmoji] = Y.useState(false),
  [showBg, setShowBg] = Y.useState(false),
  editorRef = Y.useRef(null),
  resizeRef = Y.useRef(null),
  commitData = Y.useCallback(t => {
    updateNodeData(e, t);
  }, [e, updateNodeData]);

// ── 副作用：任意字段变化即回写节点 data ──
Y.useEffect(() => {
  commitData({
    html,
    fontSize,
    bold,
    color,
    bgColor,
    width,
    height
  });
}, [html, fontSize, bold, color, bgColor, width, height, commitData]);

// ── 回调：进入编辑 / 选区样式 / 字号色值 / 拖拽缩放 ──
let k = bgColor === `transparent`,
  A = Vh(bgColor),
  j = e => {
    e.stopPropagation(), setIsEditing(true), setTimeout(() => {
      let e = editorRef.current;
      if (e) {
        e.innerHTML = html, e.focus();
        let t = document.createRange();
        t.selectNodeContents(e), t.collapse(false);
        let n = window.getSelection();
        n?.removeAllRanges(), n?.addRange(t);
      }
    }, 50);
  },
  M = () => {
    editorRef.current && setHtml(editorRef.current.innerHTML), setIsEditing(false), setMenuPos(null), setShowEmoji(false), setShowBg(false);
  },
  N = () => {
    M();
  },
  P = e => {
    if (!isEditing) return;
    e.preventDefault(), e.stopPropagation();
    let t = e.currentTarget.closest(`.react-flow__node`)?.getBoundingClientRect();
    setMenuPos({
      x: e.clientX - (t?.left ?? 0),
      y: e.clientY - (t?.top ?? 0)
    });
  },
  F = () => {
    editorRef.current && setHtml(editorRef.current.innerHTML);
  },
  I = () => {
    let e = window.getSelection();
    if (!e || e.rangeCount === 0 || e.isCollapsed) return false;
    let t = editorRef.current;
    return t ? t.contains(e.anchorNode) && t.contains(e.focusNode) : false;
  },
  L = e => {
    let t = window.getSelection();
    if (!t || t.rangeCount === 0 || t.isCollapsed) return;
    let n = t.getRangeAt(0),
      r = document.createElement(`span`);
    Object.assign(r.style, e);
    try {
      n.surroundContents(r);
    } catch {
      let e = n.extractContents();
      r.appendChild(e), n.insertNode(r);
    }
    let i = document.createRange();
    i.selectNodeContents(r), t.removeAllRanges(), t.addRange(i), F();
  },
  ee = e => {
    isEditing && I() ? L({
      fontSize: `${e}px`
    }) : setFontSize(e), setMenuPos(null);
  },
  R = e => {
    isEditing && I() ? L({
      color: e
    }) : setColor(e), setMenuPos(null);
  },
  z = () => {
    isEditing && I() ? L({
      fontWeight: `700`
    }) : setBold(e => !e), setMenuPos(null);
  },
  B = e => {
    let t = zh.indexOf(fontSize),
      n = t === -1 ? zh.findIndex(e => e >= fontSize) : t;
    ee(zh[Math.max(0, Math.min(zh.length - 1, (n === -1 ? zh.length - 1 : n) + e))]);
  },
  te = e => {
    let t = editorRef.current;
    t && (t.focus(), document.execCommand(`insertText`, false, e), F()), setShowEmoji(false);
  },
  ne = e => {
    e.preventDefault(), e.stopPropagation(), resizeRef.current = {
      startX: e.clientX,
      startY: e.clientY,
      startW: width,
      startH: height
    };
    let t = e => {
        if (!resizeRef.current) return;
        let t = Math.max(200, resizeRef.current.startW + e.clientX - resizeRef.current.startX),
          n = Math.max(150, resizeRef.current.startH + e.clientY - resizeRef.current.startY);
        setWidth(t), setHeight(n);
      },
      n = () => {
        resizeRef.current = null, window.removeEventListener(`mousemove`, t), window.removeEventListener(`mouseup`, n);
      };
    window.addEventListener(`mousemove`, t), window.addEventListener(`mouseup`, n);
  },
  re = (e => e >= 96 ? 1.08 : e >= 64 ? 1.12 : e >= 32 ? 1.16 : e >= 18 ? 1.22 : 1.28)(fontSize),
  ie = {
    fontSize,
    color,
    fontWeight: bold ? 700 : 400,
    lineHeight: re
  };

// ── 渲染 ──
return X.jsxs(X.Fragment, {
  children: [X.jsx(`style`, {
    children: `.sticky-editor:empty:before{content:attr(data-placeholder);color:#666;pointer-events:none;}`
  }), X.jsxs(`div`, {
    className: `relative group/sticky select-none ${isEditing ? `nodrag nopan nowheel` : ``}`,
    style: {
      width: width,
      minHeight: height,
      background: bgColor,
      borderRadius: k ? 0 : 8,
      padding: k ? `4px 0` : `16px 20px`,
      borderLeft: k ? `none` : `4px solid ${A}`
    },
    onDoubleClick: j,
    onClick: () => {
      setMenuPos(null);
    },
    onContextMenu: P,
    children: [!isEditing && X.jsx(`div`, {
      className: `w-full whitespace-pre-wrap break-words`,
      style: {
        ...ie,
        minHeight: 60,
        cursor: `grab`
      },
      children: html ? X.jsx(`div`, {
        dangerouslySetInnerHTML: {
          __html: html
        }
      }) : X.jsx(`span`, {
        style: {
          color: `#666`
        },
        children: `双击编辑...`
      })
    }), isEditing && X.jsxs(X.Fragment, {
      children: [X.jsx(`div`, {
        ref: editorRef,
        contentEditable: true,
        suppressContentEditableWarning: true,
        className: `sticky-editor w-full bg-transparent border-none outline-none whitespace-pre-wrap break-words nopan nowheel nodrag`,
        style: {
          ...ie,
          minHeight: Math.max(100, height - 80),
          overflow: `hidden`
        },
        onInput: F,
        onBlur: N,
        onKeyDown: e => {
          e.key === `Escape` && M();
        },
        "data-placeholder": `输入内容...`
      }), X.jsxs(`div`, {
        className: `flex items-center gap-2 mt-2 pt-2 border-t border-white/10 nodrag nopan`,
        onClick: e => e.stopPropagation(),
        onMouseDown: e => e.preventDefault(),
        children: [X.jsxs(`div`, {
          className: `relative`,
          children: [X.jsx(`button`, {
            className: `w-6 h-6 rounded border border-white/20 cursor-pointer`,
            style: {
              background: k ? `repeating-conic-gradient(#333 0% 25%, #555 0% 50%) 50%/6px 6px` : bgColor
            },
            onClick: () => {
              setShowBg(!showBg), setShowEmoji(false);
            },
            title: `便签底色`
          }), showBg && X.jsx(`div`, {
            className: `absolute bottom-8 left-0 z-50 bg-[#2a2a2a] border border-[#444] rounded-lg p-2 flex gap-1.5 shadow-xl`,
            children: Lh.map(e => X.jsx(`button`, {
              className: `w-6 h-6 rounded border transition-all cursor-pointer ${bgColor === e.value ? `border-white scale-110` : `border-transparent hover:border-white/40`}`,
              style: {
                background: e.value === `transparent` ? `repeating-conic-gradient(#333 0% 25%, #555 0% 50%) 50%/6px 6px` : e.value
              },
              onClick: () => {
                setBgColor(e.value), setShowBg(false);
              },
              title: e.name
            }, e.name))
          })]
        }), X.jsxs(`div`, {
          className: `relative`,
          children: [X.jsx(`button`, {
            className: `text-sm px-2 py-1 rounded bg-white/5 hover:bg-white/10 text-gray-400 cursor-pointer`,
            onClick: () => {
              setShowEmoji(!showEmoji), setShowBg(false);
            },
            children: `😀`
          }), showEmoji && X.jsx(`div`, {
            className: `absolute bottom-8 left-0 z-50 bg-[#2a2a2a] border border-[#444] rounded-lg p-2 grid grid-cols-6 gap-1 w-[180px] shadow-xl`,
            children: Bh.map(e => X.jsx(`button`, {
              className: `text-base w-7 h-7 flex items-center justify-center rounded hover:bg-[#444] cursor-pointer`,
              onClick: () => te(e),
              children: e
            }, e))
          })]
        }), X.jsx(`button`, {
          className: `w-7 h-7 rounded font-bold cursor-pointer transition-colors ${bold ? `bg-white/20 text-white` : `bg-white/5 text-gray-400 hover:bg-white/10`}`,
          onClick: z,
          title: `加粗（选中文字则只改选中部分）`,
          children: `B`
        }), X.jsxs(`div`, {
          className: `flex items-center gap-0.5`,
          children: [X.jsx(`button`, {
            className: `w-6 h-7 rounded bg-white/5 hover:bg-white/10 text-gray-400 text-xs cursor-pointer`,
            onClick: () => B(-1),
            title: `减小字号`,
            children: `A-`
          }), X.jsx(`button`, {
            className: `w-7 h-7 rounded bg-white/5 hover:bg-white/10 text-gray-300 text-sm cursor-pointer`,
            onClick: () => B(1),
            title: `增大字号`,
            children: `A+`
          })]
        }), X.jsx(`div`, {
          className: `flex items-center gap-0.5 ml-1`,
          children: Rh.slice(0, 5).map(e => X.jsx(`button`, {
            className: `w-4 h-4 rounded-full border transition-all cursor-pointer ${color === e.value ? `border-white scale-125` : `border-transparent hover:border-white/50`}`,
            style: {
              background: e.value
            },
            onClick: () => R(e.value),
            title: e.name
          }, e.name))
        })]
      })]
    }), menuPos && X.jsxs(`div`, {
      className: `absolute z-[9999] bg-[#222]/95 backdrop-blur border border-[#444] rounded-xl shadow-2xl p-2 min-w-[160px] nodrag nopan`,
      style: {
        left: menuPos.x,
        top: menuPos.y
      },
      onClick: e => e.stopPropagation(),
      onMouseDown: e => e.preventDefault(),
      children: [X.jsx(`div`, {
        className: `text-[10px] text-gray-500 px-2 mb-1`,
        children: `文字（选中后仅改选中部分）`
      }), X.jsxs(`div`, {
        className: `flex items-center gap-1 px-2 mb-2`,
        children: [X.jsx(`button`, {
          className: `w-6 h-6 rounded font-bold text-xs cursor-pointer ${bold ? `bg-white/20 text-white` : `text-gray-400 hover:bg-white/10`}`,
          onClick: z,
          title: `加粗`,
          children: `B`
        }), Rh.map(e => X.jsx(`button`, {
          className: `w-5 h-5 rounded-full border transition-all cursor-pointer ${color === e.value ? `border-white scale-125` : `border-transparent hover:border-white/50`}`,
          style: {
            background: e.value
          },
          onClick: () => R(e.value),
          title: e.name
        }, e.name))]
      }), X.jsx(`div`, {
        className: `text-[10px] text-gray-500 px-2 mb-1`,
        children: `字号`
      }), X.jsx(`div`, {
        className: `flex items-center gap-1 px-2 flex-wrap mb-2`,
        children: zh.map(e => X.jsx(`button`, {
          className: `text-[10px] px-1.5 py-0.5 rounded transition-all cursor-pointer ${fontSize === e ? `bg-white/20 text-white` : `text-gray-400 hover:bg-white/10`}`,
          onClick: () => ee(e),
          children: e
        }, e))
      }), X.jsx(`div`, {
        className: `h-px bg-[#444] my-1.5`
      }), X.jsx(`div`, {
        className: `text-[10px] text-gray-500 px-2 mb-1`,
        children: `便签底色`
      }), X.jsx(`div`, {
        className: `flex items-center gap-1 px-2`,
        children: Lh.map(e => X.jsx(`button`, {
          className: `w-5 h-5 rounded border transition-all cursor-pointer ${bgColor === e.value ? `border-white scale-110` : `border-transparent hover:border-white/40`}`,
          style: {
            background: e.value === `transparent` ? `repeating-conic-gradient(#333 0% 25%, #555 0% 50%) 50%/6px 6px` : e.value
          },
          onClick: () => {
            setBgColor(e.value), setMenuPos(null);
          },
          title: e.name
        }, e.name))
      })]
    }), X.jsx(`div`, {
      className: `absolute bottom-1 right-1 w-4 h-4 cursor-nwse-resize opacity-0 group-hover/sticky:opacity-60 transition-opacity nodrag`,
      onMouseDown: ne,
      title: `拖拽调整大小`,
      children: X.jsx(`svg`, {
        width: `16`,
        height: `16`,
        viewBox: `0 0 16 16`,
        fill: `none`,
        children: X.jsx(`path`, {
          d: `M14 2L2 14M14 6L6 14M14 10L10 14`,
          stroke: `#888`,
          strokeWidth: `1.5`,
          strokeLinecap: `round`
        })
      })
    })]
  })]
});
```
|
```
