# AI12 · 交叉流审计 T2.5.4 — 画布事件总线跨模块通知

> 状态：🟡 DRAFT（待门3校验 + 门4对抗审计）
> 行号快照：2026-07-21，grep `src/_engine/App.js` 坐实
> 缝合模块：T2.1~T2.4 全部（事件总线是横向切片）
> 方法学：枚举 window CustomEvent 全集收发点，验证状态同步不绕过 StorageManager(混淆名 `Q`)

---

## 一、window CustomEvent 全集（grep 坐实）

| 事件名 | 派发(行号) | 监听(行号) | 消费模块 | 说明 |
|--------|-----------|-----------|---------|------|
| `mutiwindow-task-completed` | L38481 / L43640 / L43676 / L43697 / L44406 | L31428 | T2.2/T2.5.1 | 任务完成 → rescan 刷资源 |
| `mutiwindow-rerun-task` | L44343 | L36618 | T2.4 | 重新执行任务 |
| `mutiwindow-update-task-meta` | L41032 | L43764（监听）/ L40988（`I_`=useTaskMetaSync 定义） | T2.3 | 多窗口任务元数据同步（`I_`@L40988 消费） |
| `import-project` | L44706 | L38534 | T2.4 | 导入工程（文件选择） |
| `export-project` | L44712 | L38534 | T2.4 | 导出工程（保存） |
| `mutiwindow-open-schedule-settings` | L4883 / L5828 / L26348 | L43774 | T2.1 | 打开模型调度设置 |
| `mutiwindow-open-builtin-settings` | L4922 / L5867 / L9518 / L26385 | L43774 | T2.1 | 打开内置模型设置 |
| `open-shortcuts-modal` | L37359 | （UI 内） | T2.4 | 快捷键弹窗 |
| `builtin-panel-switch-schedule` | L43771 | （N_ 组件内） | T2.1 | 内置面板切换 |
| `Zn`(canvasAddResourceEvent) | L213 | （监听待查） | T2.4→T2.2 | 画布添加资源请求 |
| `Va`(openPromptSettingsEvent) | L3643 | L43778 | T2.1 | 打开提示词设置 |
| `na`(未知事件名) | L3450 | — | — | ⚠️ 需查 `na` 定义 |
| `pg` / `fg` / `vs` / `dg` / `Zn` | L31859/L42113/L35469/L36607/L39818/L213 | 见映射 | 各模块 | 混淆名事件，需 func-mapping 补全 |

> ⚠️ **修正 ARCHITECTURE X2 表**：
> 1. `resourceAdded` 是 **chrome.runtime.onMessage 消息**（App.js L43527 接收），**非 window CustomEvent**。X2 表误列。
> 2. `mutiwindow-sync-local` 在 ARCHITECTURE X2 列出，但**本次 grep App.js 未找到该事件名**（L43774 监听的是 `mutiwindow-open-builtin-settings`/`mutiwindow-open-schedule-settings`）。可能已改名或不存在，标注待查。
> 3. `canvas-state-change` 在本次 grep 未直接命中 `dispatchEvent(...canvas-state-change)`，可能由 ReactFlow 内部或 `Su`(saveEditorState) 触发，标注待查。

---

## 二、事件总线 vs 存储管理器（红线 §3.3-9 验证）

红线要求：修复问题必须利用事件总线通知各组件，**不要绕过 StorageManager(`Q`) 直接操作底层存储**。

| 验证点 | 证据 | 结论 |
|--------|------|------|
| 任务完成走事件总线 | `mutiwindow-task-completed`@L38481 派发 → L31428 监听 | ✅ 符合红线 |
| 多窗口元数据同步 | `mutiwindow-update-task-meta`@L41032 → `I_`(useTaskMetaSync)@L40988 | ✅ 走事件总线 |
| 存储写必过 Q | `ka`(L3547) 写 token 调 `Q.setConfig`；`Sv`(L42838) 入库走 API 非直写 | ✅ 未见绕过 Q 直写底层 |
| 画布状态保存 | `Cr`(L1294) + `Pr`(localforage) 经 Q 编排 | ✅ 符合 |

> 结论：事件总线与 StorageManager 协作符合红线 §3.3-9，未观察到绕过 Q 直写。

---

## 三、存疑项（门4 待查）

| # | 存疑 | 标注 |
|---|------|------|
| E-1 | `mutiwindow-sync-local` 事件是否存在？ARCHITECTURE X2 列了但 grep 未命中 | 待上游确认（可能改名/移除） |
| E-2 | `canvas-state-change` 派发点？grep 未命中 dispatchEvent | 待查（可能 ReactFlow 内部触发或 `Su` 间接） |
| E-3 | `na`(L3450)/`pg`/`fg`/`vs`/`dg`/`Zn` 事件真名？ | 需 func-mapping 补全（混淆名事件） |
| E-4 | `Zn`(canvasAddResourceEvent) 监听点在哪？ | grep 仅见派发 L213，监听待查 |

---

## 四、结论
1. 事件总线全集枚举完成（12+ 个事件），收发点 grep 坐实。
2. **修正 ARCHITECTURE X2**：`resourceAdded` 实为 chrome.runtime 消息非 CustomEvent；`mutiwindow-sync-local` 可能不存在。
3. 红线 §3.3-9 验证通过：状态同步走事件总线 + StorageManager(`Q`)，无绕过。
4. 4 个待查项（E-1~E-4）需上游补 func-mapping 或确认事件改名。
