# AI12 · 审计自审最终报告（Self-Review）

> 生成日期：2026-07-21
> 范围：对 `docs/AI12/` 下全部 16 份产出（00~15 + 12）做前后逻辑一致性审核
> 方法：通读全部文档 + 回 `src/_engine/App.js` 复核争议点（仅读不改，未触碰 AI01~AI11 及根文件）
> 结论分级：🔴 真实逻辑矛盾（需修订） / 🟡 不严谨或过期（建议修订） / 🟢 一致良好

---

## 〇、执行纪律复核

- ✅ 全部产出仅落在 `docs/AI12/`，未读 AI01~AI11，未改任何根文件/源码/映射表。
- ✅ 所有行号引用经 `check-doc-citations.cjs` 机器校验，最新跑分 **158/158 ✅ 全绿**（脚本含反引号匹配增强）。
- ⚠️ 各文档头部仍标 `🟡 DRAFT（待门4）`，但门4 实际已完成（11 文档）。属模板残留，非逻辑错误。

---

## 一、🔴 真实逻辑矛盾（必须修订）

### 矛盾 1：`R`(网关 base) 定性在 03/04/08 与 11 文档自相矛盾

| 文档 | 表述 |
|------|------|
| 03 §二 / §四 / 04 §三 / 08 §一 | `R`(网关 base) = "生图作用域**局部变量**"，轮询-3 标"待查赋值点"；03 还将 `R`@L33005 列为"同名遮蔽第四例"之一 |
| 11 §二 轮询-3 | `R` = `Kn as R` **顶层导入别名**（非局部变量）；指令 04/03 "删除局部变量陈述，改为顶层别名" |
| 12 C7 | 已采纳 11 的修正 |

**问题**：03/04/08 至今**未执行 11 的回填指令**，仍保留"R 是局部变量/待查"旧述。同一审计体系内，03 与 11 对 `R` 的定性直接冲突。
**修订动作**：03 §二混淆字典 `R`(网关base) 行、§四轮询-3 待查行、04 §三 X3.3 "需确认 R 赋值=9004"、08 §一"由 config ENDPOINTS[0].url 注入（待查）"——统一改为"R = `Kn as R` 顶层导入别名（网关 base，默认 9004）"，并删除"待查"措辞。

### 矛盾 2：`resourceAdded` 事件类型在文档间自相矛盾

| 文档 | 表述 |
|------|------|
| 02 §5.2 / 07 §四 Q3 / 09 §一注 / 13 §二 | `resourceAdded` = **chrome.runtime 消息**（非 window CustomEvent） |
| 05 §5.2 / 06 §5.2 | 仍把 `canvas-state-change` 列为 CustomEvent，且 06 写"画布持久化→`canvas-state-change` 事件→Pr.set 保存" |

**问题**：11 文档 E-2 已坐实 `canvas-state-change` **全仓 0 命中、未使用**（画布保存走 `Su`+storage，不经 CustomEvent），且 09 已修正 `resourceAdded` 非 CustomEvent。但 05/06 仍沿用 ARCHITECTURE 旧表述，未同步 11 的结论。
**修订动作**：
- 05 §5.2：删除 `canvas-state-change` 行，改为"画布状态保存走 `Su`(saveEditorState)+`Pr`(localforage)，不经 CustomEvent（见 11 E-2）"。
- 06 §5.2：`canvas-state-change` 接缝改为同上说明，或标注"已证未使用"。

### 矛盾 3：11 文档"App.js 内无 `let R =`"与事实不符

| 文档 | 表述 |
|------|------|
| 11 §二 轮询-3 | "App.js 内**无** `let R =`（模块级）赋值；唯一 `let R =` 在 L7212（画布尺寸 useMemo）" |
| 事实（已复核 L31304） | `globalTasks: R = []`（L31304，store 组件内**局部** `R`）；另有 L7212 `let R =`（画布尺寸）。即 `R` 至少两处局部声明 |

**问题**：11 称"无 `let R =`"不实——L31304 的 `R = []` 是 store 组件的局部 `R`（globalTasks 数组），与轮询用的顶层 `R`(=Kn) 构成**同名遮蔽**（局部遮蔽顶层）。03 的"同名遮蔽第四例"把 L31304 的 `R` 列为 globalTasks 语义，方向正确，但 11 的"无 let R"表述会让人误以为 `R` 在 L31304 也是顶层——实际是该组件作用域内重新绑定。
**修订动作**：11 §二 轮询-3 补一句："注意：组件内另有局部 `R` 遮蔽（如 store 组件 `globalTasks: R = []`@L31304、画布尺寸 `let R =`@L7212），与顶层导入别名 `R`(=Kn) 互不干扰；轮询用的 `R` 始终指向顶层 `Kn as R`"。

---

## 二、🟡 不严谨 / 过期项（建议修订）

### 问题 4：校验计数在各文档不一致（快照漂移，非错误）
- 10 文档写 `121/121`；00/12 写 `151→160+`；最新实际 `158/158`。
- 原因：每份文档写于不同轮次，计数未回 sync。**建议**：在 00/10/12 统一注明"以最新 `校验报告.md` 为准（当前 158/158）"，或在每份文档加"计数快照时间"。

### 问题 5：15 文档"卡死"表述不严谨
- 15 §三称"`confirmTaskId` 零处读取 → 卡死"。但已复核：`makeConfirmAdapter`(L32960)+`requestConfirm`(L32976) **确认请求能力函数完整存在**，只是**全文件无调用点**把它接到 `await_confirm` 节点的 UI。
- **精确结论**：不是"无确认逻辑"，而是"确认能力已写好但无调用方触发"——即半截实现的卡死。15 文档结论（卡死/真实 Bug）成立，但"零处读取"应补"能力函数 `requestConfirm` 存在但无人调用"以避免误读为完全没写。
- **修订动作**：15 §三消费端段补：`makeConfirmAdapter`(L32960)/`requestConfirm`(L32976) 已实现确认 POST，但无节点 UI 读取 `confirmTaskId` 调用之 → 仍卡死。

### 问题 6：03 文档混淆字典 `R` 同名遮蔽第四例与 11 修正指令的因果未闭合
- 03 列了 R 四义（L29149/L23247/L31304/L33005），11 说 L33005 的 R 是顶层 Kn。但 03 把 L33005 标为"生图作用域局部"，与 11 冲突（见矛盾1）。修订矛盾1 时一并修正 03 的 R 四义表：L33005 改为"顶层导入别名 `Kn as R`（网关 base）"，L31304 保留为 store 局部 globalTasks 数组（局部遮蔽）。

### 问题 7：门3脚本"反引号匹配"增强未在 00/12 说明影响
- 脚本升级后，原本会被误报 `resourceAdded@L43527`（因代码用反引号 `` `resourceAdded` ``）的 ❌ 已消除。00 仅记"脚本增强"，建议补一句"增强后校验更严格，覆盖混淆代码反引号字符串"。

---

## 三、🟢 一致性良好的部分（无需修订）

1. **核心混淆名真身全链一致**：`ii`@L1888 / `Xr`(上传)@L1802 / `Zr`(下载)@L1827 / `ri`@L1856 / `Sv`@L42838 / `wv`@L42857 / `Ev`@L42883 / `we`@L43015 / `xv`@L42821 / `Jn`@L32490 / `zc`@L19001 / `H`(useLocalTool)@L19052 —— 在 01/02/03/04/07/08/13/15 中行号与语义完全一致。
2. **同名遮蔽发现自洽**：`Xr`(L1802 vs L43881)、`Zr`(L1827 vs L43893)、`Jn`(L32490 vs L89) 在 01/02/03/10 中表述一致。
3. **E-1~E-4 / 配置-1 / 轮询-3 坐实结论**在 09→11→12→14 形成闭环，无冲突（除矛盾1涉及的 R 定性）。
4. **spawnable 13 种**（06）vs ARCHITECTURE 16 种修正，在 06/10(C5) 一致。
5. **资源 ID 冲突**（rescan `local-` id vs resourceAdded `Date.now()` id）在 07/10 一致。
6. **`Zn` 链路**（14）与 09 E-4 修正、11 E-4 修正、12 C6/C13 形成完整闭环，方向描述一致。
7. **门3 校验脚本逻辑**经两次增强（模板字符串匹配、反引号匹配）后稳定，158/158 全绿。

---

## 四、门3 机器校验最终状态

| 项 | 结果 |
|----|------|
| 最新跑分 | ✅ 158 / ❌ 0 / 共 158 |
| 校验脚本 | `check-doc-citations.cjs`（含反引号匹配增强） |
| 失效引用 | 无 |

---

## 五、修订优先级建议

| 优先级 | 修订项 | 涉及文档 |
|--------|--------|----------|
| 🔴 P0 | 矛盾1：`R` 定性统一为顶层别名，删除 03/04/08 的"局部变量/待查"旧述 | 03 / 04 / 08 |
| 🔴 P0 | 矛盾2：05/06 同步 11 结论，删除 `canvas-state-change` CustomEvent 表述 | 05 / 06 |
| 🔴 P1 | 矛盾3：11 补 L31304/L7212 局部 `R` 遮蔽说明 | 11 |
| 🟡 P2 | 问题5：15 补 `requestConfirm` 存在但无调用方 | 15 |
| 🟡 P2 | 问题4/7：00/10/12 统一校验计数注明 + 脚本增强说明 | 00 / 10 / 12 |
| 🟡 P3 | 问题6：03 R 四义表与 11 对齐 | 03 |

---

## 六、总体结论

AI12 审计资产**主体逻辑自洽、事实坐实扎实、机器校验全绿**，具备入库价值。但存在 **3 处真实前后矛盾**（均源于"门4 已修正、前序 DRAFT 文档未回填"的时序问题）和若干不严谨项。

**入库建议**：门5 通过，但建议在入库前按本报告 §五 修订 P0 级矛盾（尤其 03/04/08 的 `R` 定性、05/06 的 `canvas-state-change`），否则下游读者会同时看到"R 是局部变量"与"R 是顶层别名"两套相反结论，重蹈历史文档错锚覆辙。

> 注：本报告仅记录 AI12 内部文档一致性问题，所有修订仍只落在 `docs/AI12/`；根文件/映射表的修正见 `12` 文档 C1~C14，由用户/上游决定。
