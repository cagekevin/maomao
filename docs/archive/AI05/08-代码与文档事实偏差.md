# 代码与文档事实偏差（AI05 / 深一度审计）

> 目的：把 `ARCHITECTURE.md` 的跨层描述与实际代码 grep 结果对照，找出"文档说有、代码没有"或"描述机制不符"的偏差。
> 范围：仅审计 `ARCHITECTURE.md` 与 `src/` 代码的偏差。**不改任何文件**，只记录（回填建议见 `07`）。

---

## 1. ❌ `canvas-state-change` 事件名在代码中不存在

| 文档说法 | 代码事实 |
|---------|---------|
| ARCHITECTURE X2：「`canvas-state-change` \| ReactFlow onChange \| → localforage 保存」；G4.4「画布持久化：ReactFlow onChange → canvas-state-change → localforage」 | grep `src/` 全目录 **0 命中** `canvas-state-change` |

**真相**：画布状态由 `Q.saveCanvasState(e,t)`(@L1642) 直接写 localforage，键 `Cr(e)=canvas-state-v1-{projectId}`(var L15/Z.CANVAS_STATE_PREFIX @L1283)。触发源是 ReactFlow `onNodesChange`/`onEdgesChange`(@L37123-37124) 经 `Q`(StorageManager) 编排，并非某个名为 `canvas-state-change` 的 CustomEvent。

**影响**：下一个 AI 若按文档去 grep `canvas-state-change` 会扑空，误以为持久化断链。属文档臆测/过时。

## 2. ❌ `mutiwindow-sync-local` 事件名在代码中不存在

| 文档说法 | 代码事实 |
|---------|---------|
| ARCHITECTURE X2：「`mutiwindow-sync-local` \| 资源面板按钮 \| → we() rescanSync」 | grep `src/` 仅 2 命中 `handleSyncLocal`(函数名 @L194/L45229)，**无 `mutiwindow-sync-local` CustomEvent** |

**真相**：资源面板"同步到本地"按钮绑定 `handleSyncLocal`(Oi, @L44426) → 调 `we()`/`Ev()` rescan，是函数调用而非事件总线广播。

## 3. ⚠️ `resourceAdded` 是跨进程消息，非 window CustomEvent

| 文档说法 | 代码事实 |
|---------|---------|
| ARCHITECTURE X2/G3：把 `resourceAdded` 归为「前端进程内 window CustomEvent」跨模块通信 | `background.ts`@L102 `chrome.runtime.sendMessage({action:'resourceAdded',resource})` → App.js@L43527 `if(e.action==='resourceAdded')` 走 **onMessage 监听**（chrome.runtime 消息，跨 SW→前端进程），非 `window.dispatchEvent` |

**真相**：`resourceAdded` 是 **Service Worker → 前端** 的 chrome.runtime 消息，不是前端进程内的 window CustomEvent。ARCHITECTURE G3 明确说"前端↔localTool/网关之间靠 HTTP，无直接事件总线"是对的，但 X2 又把 `resourceAdded` 列入"前端 window CustomEvent 全集"，自相矛盾。

**补充**：`resourceAdded` 处理(@L43527)内调用 `Zr(i.url,{subfolder:'migrated'})`(@L43539) 下载落盘——此处 `Zr` 是**下载函数**(L1827)，非 logout(L43893)，印证 `01` 节 `Zr` 双绑定发现。

## 4. ✅ `mutiwindow-task-completed` / `mutiwindow-update-task-meta` 确认存在（文档正确）

| 事件 | 代码位置 | 结果 |
|------|---------|------|
| `mutiwindow-task-completed` | 发送 @L38481/L43640/L43676/L43697/L44406；监听 @L31428 | ✅ 文档正确 |
| `mutiwindow-update-task-meta` | 发送 @L41032；监听 @L43764 | ✅ 文档正确（func-mapping L115 `I_`=useTaskMetaSync） |

## 5. ✅ ReactFlow onChange → 持久化 真实路径（修正文档描述）

```
ReactFlow onNodesChange/onEdgesChange (@L37123-37124)
  → fe/he handler → Q(StorageManager, var L20 @1441)
  → Q.saveCanvasState(e,t) (@L1642) → Cr(e)=canvas-state-v1-{id} → setLocalforage
  （恢复：Q.loadCanvasState @L1646）
```
无 `canvas-state-change` 事件中转。

## 6. 汇总：ARCHITECTURE X2 事件总线表修正建议

| 事件 | 文档归类 | 实际 | 建议 |
|------|---------|------|------|
| `canvas-state-change` | window CustomEvent | **不存在** | 从表中删除，改为"Q.saveCanvasState 直接写" |
| `mutiwindow-sync-local` | window CustomEvent | **不存在**（是 handleSyncLocal 函数） | 从表中删除 |
| `resourceAdded` | window CustomEvent | chrome.runtime 跨进程消息 | 改归类为"SW→前端消息"，移出 window CustomEvent 全集 |
| `mutiwindow-task-completed` | window CustomEvent | ✅ 正确 | 保留 |
| `mutiwindow-update-task-meta` | （未单列，见 X2 列表有误） | ✅ 正确 | 补入 X2 |
| `import-project`/`export-project` | AppShell L44644/L44650 | 实测：监听 @L38534，dispatch @L44706/L44712 | ✅ 存在（本次已实证） |

## 7. 校验

- `canvas-state-change` grep `src/` = 0 ✅（确认不存在）
- `mutiwindow-sync-local` grep = 0（仅 handleSyncLocal）✅
- `resourceAdded` background.ts@L102 + App.js@L43527 ✅
- `Zr`@L1827(下载) / @L43893(logout) ✅
- `Q.saveCanvasState`@L1642 / `Cr`@L1283 ✅
