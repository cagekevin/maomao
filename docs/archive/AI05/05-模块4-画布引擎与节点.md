# 模块4 · 画布引擎与节点（AI05 / T2.4）

> 边界：ReactFlow 画布交互 + 自定义节点组件 + 3D 导演台接入。
> 锚点：`func-mapping.txt`（第七批 ReactFlow handler + 3D 导演台）+ `var-mapping.txt` 节点组件。

---

## 1. 运转图景

主画布 ReactFlow 接线块 @L37125-37139，绑定全部鼠标交互 handler；节点渲染由 `nodeTypes`(lg)/`edgeTypes`(ug) 映射到自定义组件（ImageNode/PromptNode/.../Director3DNode）。

## 2. 核心混淆字典（模块4，带行号，func-mapping 已收）

### 2.1 ReactFlow 交互 handler（接线 @L37125-37139）
| 混淆名 | 可读名 | 行号 | 接线位置 |
|--------|--------|------|---------|
| `xn` | onConnect | @L31883 | L37125 |
| `Yr` | onEdgeDoubleClick | @L36570 | L37126 |
| `kn` | onNodeClick | @L31981 | L37131 |
| `Cn` | onPaneContextMenu | @L31933 | L37132 |
| `wn` | onNodeContextMenu | @L31947 | L37133 |
| `Ir` | onDragOver | @L36240 | L37137 |
| `Lr` | onDrop | @L36243 | L37138 |
| `Fr` | onConnectEnd | @L36195 | L37139 |
| `An` | onPaneClick | @L31994 | — |
| `En` | onSelectionContextMenu | @L31958 | — |
| `On` | onSelectionEnd | @L31968 | — |
| `jt` | onNodeDragStop | @L31514 | — |
| `bt`/`xt` | onMoveStart/End | @L31381/L31384 | — |

### 2.2 节点组件（var-mapping L63-89）
| 混淆名 | 可读名 | 行号 |
|--------|--------|------|
| `li` | ImageNode | — |
| `Ya` | PromptNode | — |
| `Qa` | TextNode | — |
| `ps` | AudioNode | — |
| `Lc` | GridSplitNode | — |
| `Wc` | UrlToImageNode | — |
| `Jc` | PanoramaNode | — |
| `Rc` | TextConcatNode | @L18812 |
| `To` | GridMergeNode | @L7906 |
| `Th` | Director3DNode | @L187 |
| `Ns` | FrameExtractNode | @L16031 |
| `Yc` | PanoNode | @L19516 |

### 2.3 3D 导演台（func-mapping L71-97 实测坐实）
| 混淆名 | 可读名 | 行号 | 证据 |
|--------|--------|------|------|
| `$d` | DirectorShell | @L24391 `function $d({children:e}` | L24391 定义 + L28382 `X.jsx($d,...)` 渲染，主布局（视口列+左场景栏+右属性栏） |
| `Bd` | ModelInspectorPanel | func-mapping L86 | 右侧属性面板（updateObjectName/Transform/UniformScale/Color） |
| `Dp` | PanoramaView | func-mapping L96 | 全景背景组件 |
| `Bu` | createSceneObject | func-mapping L89 | 由 asset 生成场景对象 `uu('obj_')`+name |
| `Du` | createDefaultDirectorProject | func-mapping L97 | 默认工程工厂 cam_1+char_default_a |
| `Th` | Director3DNode | var-mapping L187 @L187 | 画布节点组件，defaultTitle '3D 导演台'，含 directorProject/机位截图 onCaptureToBox |
| `Im`/`Bl` | directorDefaultCamera/defaultCameraConfig | var-mapping L153/L154 | 默认相机 {fov:50,position,target} |
| `Gl` | directorDefaultScene | var-mapping L151 | 默认场景配置 |
| `kp`/`Op` | registerModelGenerateHandler/modelGenerateHandler | var-mapping L168/L167 | 注册/调用 AI 模型生成 |

> 注：func-mapping 收录名 `$d`/`Bd`/`Dp`/`Bu`/`Du` 均真实（grep `function $d` @L24391 命中）。早期审计 grep 因 `$` 正则未转义误判 0 命中，已更正。

## 3. 关键数据流

### 3.1 拖放建节点（onDrop `Lr`）
```
Lr(e) @L36243
  → application/x-maomao-template → 批量导入节点
  → application/x-mutiwindow-task → 创建节点
  → dataTransfer.files → ii({subfolder:'canvas/drop'}) → POST /api/files/upload  (ARCHITECTURE L2.5 #3)
```

### 3.2 连线（onConnect `xn`）
```
xn(e) @L31883
  → 同侧校验(M 提示) + 左右反向纠正 + 选中节点批量连
  → setEdges（直接 setEdges，不经 onNodesChange，见 ARCHITECTURE §4.5 撤销/重做）
```

### 3.3 右键菜单（Cn/wn/En/On/Fr）
```
Cn(e) @L31933 → type:'canvas' → ve({...}) 设菜单
wn(e) @L31947 → type:'node'
Fr(e) @L36195 → 连线落空 → ghost-target/ghost-edge + connection 菜单
Ue(false) 关菜单（Cn/wn/En/On 中）
```

### 3.4 启动播种链（衔接模块1 → 本地数据层，App.js L43089-43135）

bootstrap 完成后主 `useEffect` 批量装配数据（与 02 节模块1 启动播种呼应）：

```43089:43135:src/_engine/App.js
Q.getObject(Z.USERS) → Be(e) 或 Be(Av)+Q.setObject(Z.USERS, Av)        // 用户列表
// 资源播种（幂等锁 kv='resources_seeded_to_sqlite' @L42895）
if (!await Q.getConfig(kv)) {
  let e = await Q.getObject(Z.TRANSIT_RESOURCES);                       // 旧 storage 资源
  let t = await Sr.default.getItem(Z.TRANSIT_RESOURCES);                // localforage 资源
  // 合并去重 → r = 过滤 source!=='local-tool'
  for (resource of r) {
    let localized = await Zr(resource.url, {subfolder:'migrated'});    // 下载落盘 uploads/migrated
    if (localized?.url) {
      let url = localized.url.startsWith('http') ? localized.url : `${Hr}${localized.url}`;
      await Sv({...resource, url, folder:'migrated', id:String(resource.id)});  // 入库 SQLite
    }
  }
  await Q.setConfig(kv, String(Date.now()));                            // 置播种锁
}
Q.getConfig(Z.TRANSIT_GRID_COLS) → re(t)                                // 网格列数
Q.getObject(Z.PROJECTS) → dr(...) + Q.getConfig(Z.LAST_OPENED_PROJECT)  // 项目恢复
Q.getObject(Z.PRESET_PROMPTS) → Mr(e)                                   // 预设提示词
Q.getObject(Z.GLOBAL_TASKS) →                                          // 历史任务播种（锁 Ov='tasks_seeded_to_sqlite' @L42894）
  if (!await Q.getConfig(Ov) && e?.length) { await Y_(e); Q.setConfig(Ov,...) }
```

- **幂等锁**：`kv`(资源)/`Ov`(任务) 确保只播种一次（L43104/L43132）。
- **资源迁移**：旧 `TRANSIT_RESOURCES`（chrome.storage/localforage）中 `source!=='local-tool'` 的资源，经 `Zr`(下载函数@L1827) 落盘 `uploads/migrated`，再 `Sv`(L42838) 入库——与 10/11 节「background 不下载、前端 resourceAdded 才下载」采集链路一致（此处是启动期一次性迁移，非实时采集）。
- **任务播种**：`Y_`(batch-save, L41696) → `POST /api/tasks/batch-save`（见 14 节），历史任务迁移到 SQLite。

> 详见 `16-启动播种与3D导演台实证.md`。

## 4. 边界契约（缝合点）

| 类型 | 名称 | 位置 | 说明 |
|------|------|------|------|
| ReactFlow props | onConnect/onDrop/... | @L37125-37139 | 主画布接线 |
| 持久化 | `Q.saveCanvasState` | @L1642 | **直接写 localforage，无 `canvas-state-change` 事件**（08 节证伪） |
| HTTP | `POST /api/files/upload` | localTool files.ts | 拖入文件落盘 |
| MIME | `application/x-maomao-template` | var ig @L108 | 模板拖拽 |
| config | `minZoom`(.05) | ARCHITECTURE §4.5 | React Flow 缩放下限魔改 |
| config | `.react-flow__pane {user-select:none}` | ARCHITECTURE §4.5 | 防框选蓝选 |

## 5. 存疑 Bug / 排雷

| 风险 | 结论 |
|-----|------|
| 新增自定义节点与混淆逻辑冲突 | ⚠️ 红线 §3.1：前端只改 `App.js`/`config.js`；新增节点组件须语义化命名（§3.1 #3），禁止模仿短名 |
| Ctrl+拖拽框选 | ✅ ARCHITECTURE §4.5：keydown 只认 Control/Meta，动态切 panOnDrag/selectionOnDrag |
| onConnect 直接 setEdges 不经 onChange | ✅ 设计如此（撤销/重做直接 setNodes/setEdges） |

## 6. 校验（门3 式）

- 接线块 `onConnect:xn`(L37125)/`onNodeClick:kn`(L37131)/`onPaneContextMenu:Cn`(L37132)/`onNodeContextMenu:wn`(L37133)/`onDragOver:Ir`(L37137)/`onDrop:Lr`(L37138)/`onConnectEnd:Fr`(L37139) 全部命中 ✅
- `xn`@L31883 / `Cn`@L31933 / `wn`@L31947 / `Fr`@L36195 / `Lr`@L36243 / `Ir`@L36240 全部命中 ✅
- 节点组件 `li`/`Ya`/`Qa`/`ps`/`Th`/`Rc`/`To` 在 var/func-mapping 收录 ✅
