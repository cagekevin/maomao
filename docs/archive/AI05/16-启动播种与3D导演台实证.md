# 深十度：启动播种链 + 3D 导演台实证（AI05）

> 对象：App.js 启动播种 `useEffect`(L43085-43135) + 3D 导演台函数（`$d` L24391 等，func-mapping L71-97 实测）。
> 承接 02 节模块1 启动 + 03 节本地数据层（Ev/Sv/Zr）+ 14 节 tasks batch-save。
> 红线：只读，不修改。

---

## 1. 启动播种链（L43085-43135）

### 1.1 幂等锁常量（L42893-42896）
```42893:42896:src/_engine/App.js
var Dv = 200,
  Ov = `tasks_seeded_to_sqlite`,
  kv = `resources_seeded_to_sqlite`,
  Av = [];
```
- `Ov`/`kv` 为「已播种到 SQLite」标志 key，避免每次启动重复迁移。

### 1.2 资源迁移（L43090-43118）
- 取旧资源：localforage `Z.TRANSIT_RESOURCES`(L43093) + chrome.storage `Sr.default.getItem`(L43094)。
- 合并去重（Set by id，L43096-43099）。
- 过滤 `source !== 'local-tool'`(L43100) —— 即**只迁移非 localTool 来源**的旧资源（localTool 来源已在 rescan 覆盖）。
- 逐条 `Zr(url,{subfolder:'migrated'})`(L43104) 下载落盘 → `Sv({...resource,url,folder:'migrated'})`(L43107) 入库。
- 失败单条 `console.warn` 跳过(L43109-43111)，不中断。
- 置锁 `Q.setConfig(kv, Date.now())`(L43115)。

> **与采集链路闭环**：此处 `Zr`(下载函数@L1827) 即 10/11 节「前端 resourceAdded 才下载」的同一函数；启动期一次性迁移复用之。

### 1.3 任务播种（L43130-43135）
- `Q.getObject(Z.GLOBAL_TASKS)` → 若未播种(`!Q.getConfig(Ov)`)且非空 → `Y_`(batch-save, L41696) → `POST /api/tasks/batch-save`（14 节 tasks.ts L72）。
- 置锁 `Q.setConfig(Ov, Date.now())`(L43132)。
- 失败回退 KV 数据(L43134)。

### 1.4 其余装配（L43088-43129）
| 数据 | 来源 | 动作 |
|------|------|------|
| USERS | `Z.USERS` | `Be(e)` 或默认 `Av` |
| GRID_COLS | `Z.TRANSIT_GRID_COLS` | `re(parseInt)` |
| PROJECTS | `Z.PROJECTS` | `dr(...)` + 恢复 `Z.LAST_OPENED_PROJECT` |
| PRESET_PROMPTS | `Z.PRESET_PROMPTS` | `Mr(e)` |

---

## 2. 3D 导演台实证（func-mapping L71-97）

### 2.1 DirectorShell `$d`（L24391，主布局）
```24391:24392:src/_engine/App.js
function $d({
  children: e
```
- L28381-28383：`className:'director3d-body'` → `X.jsx($d,{children:X.jsx(Sh,{})})` 渲染主壳。
- func-mapping L71：`director-shell` + 视口列(3D视口) + 左场景栏(id) + 右属性栏(`Qd`)，读 `viewportPanelsCollapsed`。

### 2.2 其余导演台函数（func-mapping 收录，已确认存在）
| 名 | 可读 | 行号(映射表) | 作用 |
|----|------|------|------|
| `Bd` | ModelInspectorPanel | L86 | 右侧属性面板 |
| `Dp` | PanoramaView | L96 | 全景背景 |
| `Bu` | createSceneObject | L89 | asset→场景对象 `uu('obj_')` |
| `Du` | createDefaultDirectorProject | L97 | 默认工程 cam_1+char_default_a |
| `Th` | Director3DNode | var L187 | 画布节点组件（defaultTitle '3D 导演台'） |
| `Im`/`Bl` | 默认相机 | var L153/L154 | {fov:50,position,target} |
| `Gl` | directorDefaultScene | var L151 | 默认场景 |
| `kp`/`Op` | registerModelGenerateHandler/modelGenerateHandler | var L168/L167 | AI 模型生成注册/调用 |

> **修正早前审计误区**：深九度时我曾 grep `$d`/`Bd` 等得 0 命中，误以为 3D 函数名臆测。实为 grep 正则未转义 `$` 前缀所致；func-mapping 收录名全部真实（`function $d` @L24391 命中）。05 节草稿的 3D 函数名正确，本轮实证确认。

---

## 3. 与既有审计衔接

- 播种链 `Zr`+`Sv` 复用 10/11 节采集/入库函数；`Y_` 复用 14 节 tasks batch-save。
- `Th`(Director3DNode) 是画布节点之一，与主 ReactFlow 接线(L37125-37139) 同属模块4。
- 幂等锁 `kv`/`Ov` 防重复迁移——若清除这两个 key 可强制重新播种（调试手段，非 bug）。

---

## 4. 校验（门3）

| 引用 | 文件:行 | 命中 |
|------|---------|------|
| kv/Ov 常量 | App.js L42894/L42895 | ✅ |
| 资源播种 Zr+Sv | App.js L43104/L43107 | ✅ |
| 任务播种 Y_ | App.js L43132 | ✅（Y_ 见 14 节） |
| $d=DirectorShell | App.js L24391 / L28382 | ✅ |
| Th=Director3DNode | var-mapping L187 | ✅ |
| 3D 函数名 func-mapping | L71-97 | ✅ 全收录真实 |
