# 文档纠错建议（AI05 / T4）

> 受众：文档维护者（你）。本文件**不直接改** `CLAUDE.md`/`ARCHITECTURE.md`/`TASKS.md`，只给回填清单。
> 核心发现：**`ARCHITECTURE.md` 在 2026-07-19 的第七批更新中已自我纠正**（`Jn` 双绑定、`ii`@L1888、`Zr`=注销均已正确）；**错误的集中在 `TASKS.md` 末尾的"架构审计计划"章节（T0/T2/T4）**，它没跟上映射表更新，仍标"待补/锚点有误"，会误导下一个 AI。

---

## 1. `TASKS.md` 审计计划章节的问题（优先级高）

| 位置 | 当前错误表述 | 实测真相 | 建议动作 |
|------|------------|---------|---------|
| T0 开头 L56 | "映射表缺口未补（见 T0），阶段二 prompt 不能直接用原版（原版 `Jn`/`Ev`/`Xr`/`Sv`/`Zr` 锚点有误）" | 映射表 2026-07-19 第七批已大幅补全；`Jn`/`Zr`/`Sv`/`sv` 均已正确（见 ARCHITECTURE L127/L388）；`Ev`/`we`/`Sv`(大写)/`wv`/`ii`/`ri` 六函数**身份已坐实但 func-mapping 漏收**（本次 01 节坐实） | **整段重写**：声明映射表已补，仅 6 个模块级函数需补录 func-mapping；删除"锚点有误"误导语 |
| T0.1 L89-95 | 7 项全标 ⬜ 待补 | 全部已坐实（详见 `01-映射表缺口核对.md`） | 改为 ✅ 已坐实，仅标注 func-mapping 漏收项 |
| T0.1 L91 | "AI 生成 N 分支派发非 `Jn`，待补函数名" | `Jn`(L32490) 就是生图主回调（imageNode）；与 `Jn`(L89)=LogoIcon 同名不同绑定。TASKS 说"非 Jn"是**错的** | 改为"`Jn`(L32490) 生图回调，与 LogoIcon `Jn`(L89) 同名不同绑定，引用须带行号" |
| T0.1 L94 | "上传/下载 疑似 `ii`/`R`" | `ii`@L1888=uploadFile；`R` 是多处局部回调（非上传函数） | 改"`ii`=uploadFile@L1888；`R` 为局部回调跳过" |
| T4.1 L132 | "修 ARCHITECTURE：`Jn` 实为 LogoIcon，非 AI 生成" | ARCHITECTURE L49/L127 **已正确**标注双绑定 | **关闭 T4.1**，无需改 ARCHITECTURE |
| T4.1 L132② | "Xr=openInTab(L43479)，非上传，改 L2.5" | ARCHITECTURE L388 已注"原记 Zr 误，Zr=注销@L43893" | **关闭**，ARCHITECTURE 已正确 |
| T4.2 L133 | "Sv/sv 厘清、Zr 下载改真名、wv/R/B/ii 回填" | `Sv`@L42838(入库)/`sv`@L178(序列化黑名单) 已厘清；`Zr`=注销已正确；`wv`/`ii`/`ri` 仅 func-mapping 漏收 | **关闭大部分**；仅保留"func-mapping 补录 6 函数" |

## 2. `func-mapping.txt` 需补录的 6 个模块级函数（来自本次 01 节）

> 这 6 个在 2026-07-19 更新时漏收，本次 grep 坐实。建议追加入 func-mapping.txt：

```
Ev = rescanTrigger          # async @L42883；fetch POST /api/resources/rescan（生成完成触发后端入库）
we = rescanResources        # Y.useCallback async @L43015；await Ev() + xv() 刷新资源面板
Sv = saveResource           # async @L42838；POST /api/resources/save（upsert 资源）
Cv = toggleFavorite         # async @L42851；Sv({...e,isFavorite:t})
wv = deleteResource         # async @L42857；POST /api/resources/delete?id=（只删DB不删盘，P2）
ii = uploadFile             # async @L1888；POST /api/files/upload 落盘 uploads/{subfolder}/（统一上传入口）
ri = getThumbnailCached     # async @L1856；内存 Map<TTL300s>+并发锁，回 /api/files/thumbnail
```

（注：`B`/`R` 为多处局部回调，按映射表规则跳过批量重命名，不补录。）

### 2.1 追加：`Zr` 双绑定补录（来自 `01` 节 / `08` 节）

```
Zr = downloadAndSave       # async @L1827；下载 URL 落盘 uploads/{subfolder}/（resourceAdded 处理 @L43539 调用）
```
> 注意：var-mapping L48 已收 `Zr=logout`@L43893。两处 `Zr` 同名不同绑定，**必须都收并标注行号**，否则下一个 AI 会把下载调用误判为 logout。

### 2.0 追加（高优）：`ei`/`ti` 四义绑定，var-mapping 严重漏收（来自 `10` 节）

var-mapping L9/L10 只收了 GAS 同步两处，漏收 3 处，且未标同名多绑定：

| 行号 | 真实身份 | 当前映射表 |
|------|---------|-----------|
| L1853 `var ei = new Map()` | 缩略图/缓存 Map | ❌ 漏收 |
| L1854 `var ti = new Map()` | 缓存 Map2 | ❌ 漏收 |
| L36784 `ei = Y.useCallback` | 打组 groupSelectedNodes | ❌ 漏收 |
| L36822 `ti = Y.useCallback` | 清理画布缓存 cleanCanvasCache | ❌ 漏收 |
| L43950 `ei = async()=>` | pushToCloud（GAS） | ✅ 已收 L9 |
| L43974 `ti = async()=>` | pullFromCloud（GAS） | ✅ 已收 L10 |

> 这是本次审计**最严重映射表缺陷**（4 义跨 Map/UI/云同步）。建议 func-mapping 补录 4 义并标注行号，或在 var-mapping L9/L10 加注「ei/ti 同名四义，引用必带行号」。

### 2.2.1 追加：`ARCHITECTURE.md` X1.2 采集链路描述过时（来自 `10` 节）

> X1.2 写「background.ts handleSaveToTransit → ... → Zr() 下载 → uploads/migrated/ → Sv() 入库」。实测：background.ts **L80 明确不再下载**，只存元数据 URL（`source:'extension'`）；真正下载在前端 `resourceAdded` 处理(@L43539 `Zr(url,{subfolder:'migrated'})`)，且失败整条丢弃。建议 X1.2 改为「background 只存元数据 → 前端接收后按需下载落盘」。

## 2.2 追加：`ARCHITECTURE.md` X2 事件总线表偏差（来自 `08` 节）

| 文档错误 | 真相 | 建议 |
|---------|------|------|
| X2 列 `canvas-state-change`(window CustomEvent) | 代码中不存在；持久化走 `Q.saveCanvasState`(@L1642) 直接写 | 删除该事件，改为"Q.saveCanvasState 直接写 localforage" |
| X2 列 `mutiwindow-sync-local`(window CustomEvent) | 不存在；是 `handleSyncLocal`(Oi @L44426) 函数调用 | 删除该事件 |
| X2 把 `resourceAdded` 归为 window CustomEvent | 实为 chrome.runtime 跨进程消息（background.ts@L102 → App.js@L43527） | 改归类为"SW→前端消息"，移出 window CustomEvent 全集 |
| X2 未列 `mutiwindow-update-task-meta` | 存在（发送@L41032/监听@L43764，func-mapping L115） | 补入 X2 |
| X2 漏列多窗口事件全集 | 17 节汇总 10+ 真实 window CustomEvent（mutiwindow-task-completed/update-task-meta/open-*-settings/rerun-task/nodes/images/import-project/export-project/open-shortcuts-modal/builtin-panel-switch-schedule） | 重写 X2 事件表，列全真实事件 |
| 多窗口跨窗口机制待查 | 17 节 §4：纯 window CustomEvent 不能跨扩展 window，代码未见 mutiwindow-* 的跨 window 广播实现 | 确认原版多窗口能力是否已弱化；若已弃用，文档应注明 |
| ARCHITECTURE §4.5 撤销重做表述不精确 | ⚠️ 文档缺口 | 18 节：实际是中央 store `r`(L21580) 统一接管撤销栈（快照/压栈/持久化 `Su` L21178），ReactFlow `setNodes/setEdges` 仅接线层即时交互；建议 §4.5 补「store.r 统一撤销栈」 |

## 2.3 追加：`TASKS.md` P0 修复方案锚点错误（来自 `09` 节）

| 位置 | 错误 | 建议 |
|------|------|------|
| P0 修复方案行33 | 「Sv() 保存前 `toAbsoluteFileUrl(i.url)` @L43462」——`toAbsoluteFileUrl` **实际存在于 localTool**（`localTool/src/routes/resources.ts` L31），不在 App.js；L43462 实为云存储 JSON 模板 `Lr` | 改为「localTool resources.ts `toAbsoluteFileUrl` L31（rescan 补全绝对路径）+ App.js host 硬编码 18080 兜底（d5d48dd）」；删除错误的 App.js L43462 指向 |
| P2 #6 行号 | `wv()` L42778 | 更新为 L42857（可选）；另注：删盘机制 localTool `handleResourcesClear` 已支持 `deleteFiles`，`wv` 未用 |

> 注：TASKS Bug 表其余行（P1 #2/#3、P2 #7/#8、统一 base URL）经核对均准确，仅行号有漂移。

## 2.4 追加：`apimart-gateway/README.md` 与 `main.py` 自相矛盾（来自 `12` 节，深五度）

| 位置 | README 表述 | 代码真相（main.py） | 建议 |
|------|-----------|------------------|------|
| README L42 `POST /v1/music/generations` | "chat（agent 处理音乐，异步 task）" | L655 `return err(501, "音乐/音频生成...", "not_supported_error", 501)` | README 改 "501 诚实不支持" |
| README L46 `POST /v1/audio/generations` | "chat（音乐 agent，别名，方便老调用方）" | L659 同样 501 | 同上 |
| README L43/L47 自洽 | "audio/speech、transcriptions 诚实 501" | L663/L667 确实 501 ✅ | 这两处 README 正确，但与前两条 music/audio 描述冲突 |

> 矛盾点：README 把 music/audio generations 写成走 chat agent，代码却直接 501 不转发。会让调用方误以为有音乐生成能力。建议统一 README 为「music/audio generations 与 TTS/转写一样，均为 501 诚实不支持」。

## 2.5 追加：网关 chat 路由「核心修复」点未见于权威文档（来自 `13` 节，深六度）

`main.py` `/v1/chat/completions`(L434-574) 含三处代码注释标「核心修复」，但 `ARCHITECTURE.md`/`TASKS.md` 均未记录：

| 位置 | 行为 | 文档现状 | 建议 |
|------|------|---------|------|
| L502/L521 | `abort` 状态抛 400（不再吞结果） | 未记录 | 补入 ARCHITECTURE 网关章节「chat 终态处理」 |
| L541 | 同步轮询超时抛 504（非静默空返回） | 未记录 | 同上 |
| L549 | SSE 每 2s `: heartbeat` 保活 | 未记录 | 同上 |
| L482-486 | `set_mode(unlimited=False)` + `CHAT_THREAD_MODE`(thinking) 强制线程模式 | 未记录 | 同上 |

> 另：`mutiwindow-update-task-meta` 已在 §2.2 建议补入 X2 事件表；其完整数据流（L41032 发 → L43734 监听 → `J_` L41611 `POST /api/tasks/save`）已在 `13` 节坐实，机制为纯前端 window CustomEvent（与 resourceAdded 的 chrome.runtime 跨进程不同）。

## 2.6 追加：localTool tasks clear 无删盘路径（残留债务，来自 `14` 节，深七度）

| 位置 | 行为 | 对比 resources 侧 | 建议 |
|------|------|------------------|------|
| `tasks.ts` L102 `handleTasksClear` | `DELETE FROM tasks` 整表清空，**不删文件**，无 `deleteFiles` 参数 | `resources.ts` L211 `handleResourcesClear` 已支持 `deleteFiles` | tasks clear 增加 `deleteFiles` 选项，或在 rescan 补「tasks 表无记录的文件」清理分支 |
| rescan 孤儿清理范围 | 仅清 `source='local-tool'`(resources.ts L120) | — | 任务产物若标其他 source，清空任务后其 `uploads/` 文件成孤儿且清不掉 |

> 这是比 P2 #6（resources 删不删盘）更深一层的残留：P2 #6 的删盘解法（deleteFiles）**仅 resources 侧有，tasks 侧完全没有**。建议在 TASKS Bug 表新增一条 P2 级残留（或升 P1）。另 `upsertTask`(L42) 为 DELETE+INSERT 非事务伪 upsert，理论并发竞态，前端 1s 防抖缓解，实际风险低，可记一笔低危。

## 2.7 追加：GAS 部署权限硬约束未见于文档（来自 `15` 节，深八度）

| 位置 | 行为 | 文档现状 | 建议 |
|------|------|---------|------|
| `CloudSyncEngine.callGateway` L43914 | 响应含 `<html` → 抛「权限拦截，请确保 GAS 设为了【所有人】访问」 | ARCHITECTURE/TASKS/部署文档均未强调 GAS 必须部署为「任何人可访问」 | 在部署文档补「GAS 部署权限 = 任何人（甚至匿名），否则同步必失败」 |

> 另：`mutiwindow-task-completed` 与 `mutiwindow-update-task-meta` 为**双轨并行广播**（前者驱 `Ev()` rescan 入库 resources，后者驱 `J_` 写 tasks mediaMeta），均在 X2 事件表漏列（§2.2 已建议补）。CloudSyncEngine 内部逻辑代码注释标「原样保留，勿改」，审计仅只读未动。

## 3. `ARCHITECTURE.md` 行号微调建议（非错误，仅漂移）

| 位置 | 当前 | 建议 | 原因 |
|------|------|------|------|
| L115 `ii` | ~L36284 | L1888（定义）/ L36285（onDrop 调用） | 实测定义 @L1888，L36284 是调用点 |
| L128 生图轮询 | L32910 | L32987（轮询分支起点）/ L33005（pollUrl） | 实测轮询逻辑在 L32987-33071 |
| L129 统一同步 effect | L44246 | L44246（实测命中） | ✅ 无需改 |

## 4. `TASKS.md` Bug 表（P0-P2）与本次审计一致性

| Bug | 本次代码坐实 | 结论 |
|-----|------------|------|
| P0 URL 破图 | d5d48dd 修复，host 硬编码 18080（Hr/vv/U_）| ✅ 残留项仍待修（模块1 X4） |
| P1 #2 URL 拖入不落盘 | `B`@L29188 只入内存 | ✅ 属实 |
| P1 #3 文件上传不入库 | `R`@L29149 仅预览，不调 Sv | ✅ 属实 |
| P2 #6 删除不删磁盘 | `wv`@L42857 只删 DB | ✅ 属实 |
| P2 #7 孤儿清理只清 local-tool | localTool rescan | ✅ 属实（localTool 侧） |
| P2 #8 缩略图伪复制 | localTool files.ts copyFileSync | ✅ 属实（localTool 侧） |

> Bug 表本身准确，无需改；错误仅在末尾"审计计划"章节。

## 5. 校验

- ARCHITECTURE L49/L127/L388 已正确 ✅
- 6 函数行号均 grep 命中（见 `01`/`03`）✅
- TASKS 审计计划章节与映射表 2026-07-19 状态不同步 ❌（需维护者更新）
