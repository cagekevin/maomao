# 一毛AI画布 · 映射表缺口核对（AI05 / T0 前置阻塞项）

> 目标：把 `docs/TASKS.md` 审计计划 T0.1/T0.2 列出的"悬空/错锚点"逐个 grep 代码坐实。
> 方法：实测 `src/_engine/App.js` 行号 + 对照 `func-mapping.txt`/`var-mapping.txt` 已收录项。
> 结论：**映射表在 2026-07-19 已补录第七批，T0 缺口基本已坐实；TASKS.md 的 T0 文本未同步，且其"错锚点"表述已证伪。**

---

## 1. T0.1 解码项核对

| 混淆名 | TASKS.md T0.1 说法 | 实测代码 | 映射表收录 | 结论 |
|--------|-------------------|----------|-----------|------|
| `Ev` | "rescan 触发器，待补" | `async function Ev()` @L42883；L31426 `生成完成触发后端 rescan`、L43017 `await Ev()` | 未单列函数级（仅在 ARCHITECTURE L2.4 引为 rescan） | ✅ 坐实：`Ev` = rescan 触发器（async，调 `/api/resources/rescan`）。**映射表 func-mapping 漏收 `Ev` 函数级条目，建议补录** |
| `we` | "rescanResources 主函数，待补" | `we = Y.useCallback(async () => {...})` @L43015；内部 `await Ev()` + `xv()` | ❌ 未收录 | ✅ 坐实：`we` = rescanResources 主函数（rescan+查询）。**映射表漏收，建议补录 `we = rescanResources`** |
| AI 生成 N 分支派发 | "非 `Jn`，待补函数名" | `Jn` 实为两处：L89 `function Jn({`=LogoIcon 组件（func-mapping 已收）；L32490 `let Jn = Y.useCallback(async (e,r,o,...)=>{` 是 **imageNode 生图回调**（被 L34789/L35463/L36589 调用）。真正的"异步轮询派发"在 `Jn` 内部 fetch → 轮询 `/v1/tasks/{id}` | func-mapping: `Jn = LogoIcon` ✅；L32490 的 `Jn` 是**局部生图回调**，未在映射表单列 | ✅ 坐实：TASKS.md 说"`Jn` 非生图"是**错的**——`Jn`(L32490) 正是 imageNode 的生图主回调；但 `Jn`(L89) 是 LogoIcon。同名的**两个不同绑定**，必须带行号区分 |
| 资源入库 | "疑似 `Sv` 大写，待定" | `async function Sv(e)` @L42838（upsert resource） | ❌ 未单列（ARCHITECTURE L1.4 引 `Sv` @L42759 行号偏旧） | ✅ 坐实：`Sv` = 资源 upsert/入库函数 @L42838。**映射表漏收 `Sv`，建议补录** |
| 资源删除 | "`wv()`，待补" | `async function wv(e)` @L42857 | ❌ 未收录 | ✅ 坐实：`wv` = 资源删除（只删 DB，P2 见 TASKS）。**映射表漏收 `wv`，建议补录 `wv = deleteResource`** |
| 上传/下载 | "疑似 `ii`/`R`，待补" | `async function ii(e,t={})` @L1888（uploadFile 统一入口，落盘 uploads/）；`R` 是**多处的局部回调**（L15146/L18687/L29149 等，非统一上传函数） | func-mapping 未单列 `ii`；var-mapping 未单列 | ✅ 坐实：`ii` = uploadFile 统一上传入口 @L1888（与 ARCHITECTURE L1.2 `ii`@L36284 行号漂，实为 L1888）；`R` 是上下文局部回调，**不是上传函数** |
| 缩略图缓存 | "前端 `ri()`，待补" | `async function ri(e,t={})` @L1856（内存缓存 Map + TTL + 并发锁） | ❌ 未收录 | ✅ 坐实：`ri` = 缩略图内存缓存 @L1856。**映射表漏收 `ri`，建议补录 `ri = getThumbnailCached`** |

## 2. T0.2 易混项核对

| 项 | TASKS.md 说法 | 实测 | 结论 |
|----|-------------|------|------|
| `Zr = logout` | "被旧文档当下载函数误用" | `async function Zr(e,t={})` @L1827；`Zr = async () => {...}` @L43893（logout/重置配置）；var-mapping L48 `Zr = logout` ✅ | ✅ 映射表已纠正：`Zr` = logout（重置配置），**非下载函数**。TASKS.md T4.2 仍要"改 Zr 下载表述"——但 ARCHITECTURE L2.5 已无 Zr 下载锚（见 L43479 `Xr = openInTab`），**T4.2 残留任务可关闭** |
| `Sv`(大写) vs `sv`(小写) | "易混，待厘清" | `Sv` @L42838 = 资源入库（函数）；`sv` var-mapping L178 = `nodeCallbackFieldSet`（节点回调字段 Set，不序列化） | ✅ 两者完全不同：`Sv`=入库函数、`sv`=序列化黑名单 Set。映射表已分清楚，TASKS.md T4.2 该任务可关闭 |
| `Hr` / `vv` | "base URL 两处，待补" | var-mapping L117 `Hr = localToolStatusUrl`('http://127.0.0.1:18080')；L118 `vv = localToolBaseUrl`(同值)。值相同，用途略分（状态轮询 vs 数据请求） | ✅ 映射表已收。TASKS.md P2"统一 base URL"指 `Hr`/`vv` 两配置源——但二者值恒等，真问题是 `USE_LOCAL_ENGINE=false` 时走 9004 无上传路由（见 ARCHITECTURE X3.3） |
| `B` / `R` / `ii` / `wv` / `ri` | "模块2 回调/函数，未收录" | `ii`/`wv`/`ri` 见上（建议补录）；`B`/`R` 是**多处局部回调**（如 L29188 `B`=URL 拖入回调、L29149 `R`=文件上传回调），**非模块级绑定**，按映射表规则跳过批量重命名 | ✅ 不需要补录 `B`/`R`（局部）；需补录 `ii`/`wv`/`ri`/`Sv`/`Ev`/`we` 六个模块级函数 |

## 3. 纠错汇总（回填建议，详见 07）

1. **TASKS.md T0.1 "AI 生成 N 分支派发非 `Jn`" 表述错误**：`Jn`(L32490) 就是 imageNode 生图主回调；TASKS 应改为"生图回调为 `Jn`(L32490)，与 LogoIcon `Jn`(L89) 同名不同绑定"。
2. **映射表 func-mapping 漏收 6 个模块级函数**：`Ev`(rescan触发@42883)、`we`(rescanResources@43015)、`Sv`(资源入库@42838)、`wv`(资源删除@42857)、`ii`(uploadFile@1888)、`ri`(缩略图缓存@1856)。
3. **TASKS.md T4.2 三项可关闭**：Zr 下载、Sv/sv 厘清、B/R/ii/wv/ri 回填——其中 Zr/Sv/sv 已正确，仅 B/R 不需补，ii/wv/ri 改到映射表补录而非文档改锚。
4. **ARCHITECTURE L1.2 `ii`@L36284 行号漂移**：实测 `ii` 定义 @L1888；L36284 是 `onDrop` 调用点。建议文档标"定义 L1888 / 调用 L36285"。

## 3. 新增发现：`Zr` 双绑定（原 T0.2 未坐实的真缺口）

| 绑定 | 行号 | 真实身份 | 证据 |
|------|------|---------|------|
| `Zr`(L1827) | `async function Zr(e,t={})` @L1827 | **下载/落盘函数**：被 `resourceAdded` 处理(@L43539) 调用，`Zr(i.url,{subfolder:'migrated'})` 把 extension 来源 URL 下载到 `uploads/migrated/` | L43539 `let localized = await Zr(i.url,{subfolder:'migrated'})` |
| `Zr`(L43893) | `Zr = async () => {...}` @L43893 | **logout/重置配置**：清 auth_token、关菜单 | L43894 `Q.remove(Z.AUTH_TOKEN)` |

> ⚠️ 结论：var-mapping L48 只收了 L43893 的 `Zr=logout`，**漏收 L1827 的 `Zr`=下载函数**。这正是 TASKS T0.2 "Zr 被旧文档当下载函数误用" 的根因——实际 `Zr` 同名两义，映射表只收了一半。
> 建议 func-mapping 补录：`Zr = downloadAndSave`（下载落盘，@L1827），并标注与 logout `Zr`(@L43893) 同名不同绑定，引用须带行号。

## 4. 校验（门3 式）

以下行号均已在 `src/_engine/App.js` grep 命中 ✅：
- `Ev` @L42883 ✅ `/ `we` @L43015 ✅ `/ `Sv` @L42838 ✅ `/ `wv` @L42857 ✅ `/ `ii` @L1888 ✅ `/ `ri` @L1856 ✅
- `Zr` 双绑定 @L1827(下载) & @L43893(logout) ✅ `/ `Jn` @L89(LogoIcon) & L32490(生图回调) ✅

> 注：以上为"符号/行号存在性"校验，逻辑语义由模块审计（02~05）继续坐实。
