# 猫猫AI画布 · 审计整理总览（AI05）

> 审计执行区：`docs/AI05/`（2026-07-21）
> 依据：先读 `CLAUDE.md` + `docs/` 全量文档，再实地 grep 代码核对行号。
> 红线约束：本次只产出**审计文档**，不修改任何运行代码（`App.js`/`config.js`/`localTool`/`apimart-gateway` 一律只读）；权威文档（`CLAUDE.md`/`ARCHITECTURE.md`）不当作重写对象，仅作为真理参照。

---

## 1. 本次审计的范围

| 项 | 内容 |
|----|------|
| 输入 | `CLAUDE.md`、`docs/PROJECT_ORIGIN.md`、`docs/ARCHITECTURE.md`、`docs/TASKS.md`、`docs/FUNCTION_MAP.md`、`docs/func-mapping.txt`、`docs/var-mapping.txt`、`docs/PROJECT_LOG.md` |
| 代码核对 | `src/_engine/App.js`（~46252 行）、`src/_engine/config.js`、`localTool/`、`apimart-gateway/` |
| 产出 | 仅 `docs/AI05/` 下的 md（本文件 + 01~18 + 校验报告，共 20 个文件） |
| 不触碰 | `dist/`、`reference/`、`src/v2/`、`scripts/`、任何运行代码 |

## 2. 关键结论（先说结果）

1. **映射表已大幅领先于 TASKS.md 的审计计划**：`func-mapping.txt` / `var-mapping.txt` 在 2026-07-19 已补录第七批（含 `Ev`/`we`/`Sv`/`Zr`/`ii`/`wv`/`ri` 的真实身份）。但 `TASKS.md` 末节的「架构审计计划 T0」仍标「待补/未补」，且引用了**已证伪的错锚点**（如称 `Jn`=生图、`Zr`=下载、`Sv`/`sv` 未厘清）。
2. **T0 缺口实际已坐实**：本次 grep 复核，T0.1/T0.2 列出的 9 个悬空名中，8 个在映射表已有真身，本次用代码行号再确认一遍（见 `01-映射表缺口核对.md`）。
   - 注：模块文档 02 原标「待产」，本轮已据 `config.js`+`App.js` L43489/L1642 实证重写（修正草稿中 `canvas-state-change` 误述，与 08 节证伪一致）。
3. **真正的审计债务是**：TASKS.md 的 T0/T4 文本没跟上映射表，会误导下一个 AI 继续"补已补过的表"。模块文档（T2.1~T2.4）和交叉流（T2.5）**本轮已全部产出**（02/03/04/05/06），不再遗漏。
4. **深一度发现（08 节）**：`ARCHITECTURE.md` X2 事件总线表有 3 处与代码不符——`canvas-state-change` 和 `mutiwindow-sync-local` 在代码中**根本不存在**；`resourceAdded` 实为 chrome.runtime 跨进程消息而非 window CustomEvent。这是比 T0 更深的文档事实偏差。
5. **`Zr` 双绑定（01/08 节）**：`Zr` 既有 L1827 下载函数、又有 L43893 logout，var-mapping L48 只收了后者，是 TASKS T0.2 "Zr 误用" 的真根因。
6. **TASKS P0 修复方案锚点错误（09 节）**：`toAbsoluteFileUrl(i.url)` 全代码 0 命中，且 L43462 实为云存储 JSON 模板字符串（`Lr`），并非 Sv 调用点。P0 破图实际由 `d5d48dd`（host 硬编码 18080 兜底）修复。
7. **`ei`/`ti` 四义绑定（10 节，最严重映射表缺陷）**：var-mapping L9/L10 只收 GAS 同步 2 处，漏收 L1853(Map)/L36784(打组)/L36822(清缓存) 3 处；且 background.ts 采集链路已改为"不下载只存元数据"，ARCHITECTURE X1.2 描述过时。
8. **`toAbsoluteFileUrl` 真身在 localTool（11 节，修正 09）**：该函数不在 App.js，而在 `localTool/src/routes/resources.ts` L31（rescan 补全绝对路径）。TASKS P0 标"App.js L43462"是文件/行号错误。P0 破图 = localTool `toAbsoluteFileUrl` + App.js host 硬编码兜底共同修复。localTool 侧 rescan 孤儿清理、删除不删盘(P2 #6)、删盘解法(handleResourcesClear deleteFiles)均已实证。
9. **localTool 文件/缩略图实证 + 网关 README 自相矛盾（12 节，深五度）**：`files.ts` 全文实证——上传落盘 `uploads/{subfolder}/`、`tryGenerateThumbnail`(L121) 与 `handleThumbnail`(L223) 均为 `fs.copyFileSync` 原样复制（P2 #8 缩略图伪复制属实，maxDim/quality 未参与生成）；返回 URL 全为无 host 相对路径（`/files/...`），呼应 P0 破图根因。新增发现：`apimart-gateway/README.md` L42-47 称 music/audio generations 走 chat agent，但 `main.py` L655/L659/L663 实际直接 501 `not_supported_error`——**README 与代码自相矛盾**。
10. **网关 chat/SSE + task-meta 流（13 节，深六度）**：`main.py` L434-574 chat 路由为「网关内同步轮询 Lovart + SSE 包装」，含 abort→400(L502/L521)、超时→504(L541)、heartbeat(L549) 三处核心修复；与 04 节 `Jn`(L32490) 前端异步轮询 `GET /v1/tasks/{id}` 是**两套独立机制**但汇于同一查询端点(L873)。`mutiwindow-update-task-meta` 完整数据流坐实：媒体卡片 L41032 发 → 全局 store L43734 监听(变更检测+1s 防抖) → `J_`(L41611) `POST /api/tasks/save`@18080 落库；是纯前端 window CustomEvent（非跨进程）。
11. **localTool Tasks 落库实证（14 节，深七度）**：`tasks.ts` 全文实证——`upsertTask`(L42) 为「DELETE+INSERT」伪 upsert（非事务，理论并发竞态，前端 1s 防抖缓解）；`mediaMeta` 经 `taskToRow` 序列化为 `media_meta` 列，与 13 节写库闭环。关键残留：`handleTasksClear`(L102) 整表清空**无删盘路径**（resources 侧有 `deleteFiles` 参数，tasks 侧无），且 rescan 仅清 `source='local-tool'`，任务产物孤儿可能永久滞留——比 P2 #6 更深的残留债务。
12. **CloudSyncEngine GAS + task-completed 流（15 节，深八度）**：`CloudSyncEngine`(L43896-43948) 真身坐实——`callGateway` POST `text/plain` 到 `GAS_CLOUD_SYNC_URL`，含「任何人访问」权限硬约束(L43914 `<html` 拦截)；`ei`/`ti` 云同步双义(L43950 push/L43974 pull) 收集 9 类配置、pull 后 1s `reload`。`mutiwindow-task-completed` 触发链坐实：4 处发送(L38481/L43640/L43676/L43697/L44406) → 监听端 L31426 → `Ev()` rescan 入库；与 13 节 `mutiwindow-update-task-meta`(写 tasks) 是**双轨并行广播**（前者驱 resources 入库）。
13. **启动播种链 + 3D 导演台实证（16 节，深十度）**：bootstrap 后主 `useEffect`(L43085-43135) 资源/任务播种到 SQLite，幂等锁 `kv`/`Ov`(L42894/L42895)；资源迁移复用 `Zr`(下载)+`Sv`(入库)，任务播种复用 `Y_`(batch-save)。3D 导演台函数 `$d`(DirectorShell@L24391)/`Bd`/`Dp`/`Bu`/`Du`/`Th`(Director3DNode) 经 func-mapping L71-97 **全收录真实**（早前审计 grep `$` 未转义误判 0 命中，已更正）；`Th` 为画布节点之一，与 L37125-37139 主接线同属模块4。05 节草稿 `canvas-state-change` 误述已据 L1642 修正为 `Q.saveCanvasState` 直接写。
14. **background.ts 消息图 + 多窗口事件全集（17 节，深十一度）**：`background.ts` 全文实证——采集链路 L80「不再经过本地引擎上传」只存 `source:'extension'` 元数据（与 10 节闭环），经 `chrome.runtime.sendMessage({action:'resourceAdded'})`(L101) 跨进程通知前端(L43527 接收)。`import/export-project` 实证：发送 L44706/L44712 → 监听 L38534（调 `r.current()` 执行）。多窗口事件全集汇总 10+ 个真实 window CustomEvent（mutiwindow-task-completed/update-task-meta/open-*-settings/rerun-task/nodes/images/import/export 等），X2 事件表漏列这些且误列 canvas-state-change/mutiwindow-sync-local（08 节）。**待查**：纯 window CustomEvent 不能跨扩展 window，多窗口真实跨窗口机制是否仍存。
15. **撤销/重做 + rerun-task 重跑流（18 节，深十二度）**：撤销栈中央 store 实证——`undoStack`(L21219)+写入函数 `r`(L21580 快照/压栈/持久化 `Su` L21178)+`ed` 相等判定(L21569)+`td` 截断(L21572)+`undo` 执行(L22304)+批量 `begin/endUndoBatch`(L21615)。**修正 ARCHITECTURE §4.5**：实际是 store.r 统一接管（非直接 setNodes/setEdges），ReactFlow setEdges 仅接线层即时交互。rerun-task 完整流：发送 `Ei`(L44343)→监听 L36618→按 type 分派 `tr`/`er`/`Qn`/`$n`/`_r`(customNode L35078)/`vs`(rhWebapp)，复用 04 节生图链路。

## 3. 产出清单（本目录）

| 文件 | 内容 | 状态 |
|------|------|------|
| `00-审计总览.md` | 本文：范围 / 结论 / 方法论 | ✅ |
| `01-映射表缺口核对.md` | T0 缺口逐项 grep 坐实，给出 ✅/❌ | ✅ |
| `02-模块1-初始化与配置.md` | T2.1：bootstrap(L43489)/config.js 全量导出/存储装配（实证版，修正 canvas-state-change 误述） | ✅ |
| `03-模块2-本地数据层与Rescan.md` | T2.2：`Ev`/`we`/资源入库/删除/上传/缩略图 + P1/P2 排雷 | ✅ |
| `04-模块3-AI生成与网关.md` | T2.3：真实 AI 派发函数 + 轮询 + 落盘 + 事件总线 | ✅ |
| `05-模块4-画布引擎与节点.md` | T2.4：ReactFlow 交互 handler + 自定义节点 + 3D 导演台（实证版） | ✅ |
| `06-交叉流审计.md` | T2.5：4 条端到端流沿边界契约缝合 | ✅ |
| `07-文档纠错建议.md` | T4：给文档维护者的错锚点回填清单（不动权威文件） | ✅ |
| `08-代码与文档事实偏差.md` | 深一度：ARCHITECTURE X2 事件总线表 vs 代码实际偏差 | ✅ |
| `09-TASKS_Bug表锚点纠错.md` | 深二度：TASKS P0 修复方案 `toAbsoluteFileUrl`@L43462 锚点错误 | ✅ |
| `10-ei_ti多绑定与采集链路.md` | 深三度：`ei`/`ti` 四义绑定 + background 采集链路 vs ARCHITECTURE | ✅ |
| `11-localTool与网关实证.md` | 深四度：localTool rescan/删除实证 + `toAbsoluteFileUrl` 真身(localTool) + 网关 501 | ✅ |
| `12-localTool文件与缩略图.md` | 深五度：files.ts 全实证（P2 #8 伪复制坐实）+ 网关 README/代码矛盾 | ✅ |
| `13-网关chat与task-meta流.md` | 深六度：chat SSE 同步轮询核 + mutiwindow-update-task-meta 全流 + J_ 落库 | ✅ |
| `14-localTool任务落库.md` | 深七度：tasks.ts upsert(DELETE+INSERT) + clear 无删盘残留 | ✅ |
| `15-云同步与task-completed流.md` | 深八度：CloudSyncEngine GAS + mutiwindow-task-completed 双轨广播 | ✅ |
| `16-启动播种与3D导演台实证.md` | 深十度：播种链幂等锁 + 3D 导演台函数实证（修正早前 $ 误判） | ✅ |
| `17-background与多窗口事件.md` | 深十一度：background 采集不下载 + resourceAdded 跨进程 + 多窗口事件全集 | ✅ |
| `18-撤销重做与重跑流.md` | 深十二度：undoStack store + rerun-task 分派流 | ✅ |
| `校验报告-AI05.md` | 门3 式机器校验：grep 文档内 `App.js:Lnnnn` 是否存在 | ✅ |

## 4. 方法论（防幻觉，取自 TASKS.md 五关流水线）

- **门1 锚点门**：只用映射表已收录名 + 自 grep 行号；新名先入表再写。
- **门2 强制引用**：每条事实带 `文件:L行号`。
- **门3 机器校验**：产出后跑正则抽取文档内 `App.js:L\d+`，回源码 grep 确认符号/字符串存在，出校验报告。
- **门4 对抗审计 / 门5 入库**：本次为单人单会话，采用"写手→自 grep 复核→列 ❌ 项"替代。
- **防污染**：模块 doc 只当草稿线索，最终回代码核对；映射表是唯一可信锚层。

## 5. 与既有文档的边界（避免重复造轮子）

- `CLAUDE.md` §3 红线、`ARCHITECTURE.md` 架构事实：**不重写**，直接引用。
- `TASKS.md` 的 Bug 表：**不复制**，审计中发现的"错锚点"在 `07` 给回填建议。
- 本目录是**派生审计区**，权威事实以代码 + 映射表为准；若本文与代码冲突，以代码为准并回写 `07`。
