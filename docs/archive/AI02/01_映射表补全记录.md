# 映射表补全记录（AI02 · 不污染原表）

> 对应 `docs/TASKS.md` 阶段0 T0.1–T0.3。本文件**只追加坐实结果**，不回写 `docs/func-mapping.txt` / `docs/var-mapping.txt`。
> 所有行号来自 2026-07-21 对 `src/_engine/App.js` 的 `grep` 复核（当前快照 **46397 行**；早期记 46252 为初始快照，差异属正常漂移）。
> 状态图例：🟢 已 grep 坐实 · 🟡 推断待复核 · ⬜ 待补。

---

## 一、T0.1 解码并补入（原 func-mapping 缺口）

| 混淆名 | grep 证据（App.js 行号） | 真实语义 | 状态 | 备注 |
|--------|--------------------------|----------|------|------|
| `ri` | `L1856 async function ri(e, t = {})` | 前端缩略图内存缓存（Map<TTL> + 并发锁），返回 thumbnailUrl 字符串 | 🟢 | 对应 ARCHITECTURE L2.4「前端 `ri()`」；func-mapping 未收录（仅有 `pr=useThumbnail` hook，是两回事） |
| `ii` | `L1888 async function ii(e, t = {})` | 落盘上传统一入口（`uploadFile` 风格），写 `uploads/` 子目录 | 🟢 | ARCHITECTURE L2.5 上传入口①；func-mapping 未收录此 `ii`（别与 `Ii=fetchBuiltinModels` 混） |
| `Sv` | `L42838 async function Sv(e)` | 资源入库 upsert（POST /api/resources/save） | 🟢 | 大写 `Sv` ≠ 小写 `sv=nodeCallbackFieldSet`（func-mapping L178）。资源写库真身 |
| `wv` | `L42857 async function wv(e)` | 资源删除（POST /api/resources/delete），**只删 DB 不删磁盘**（TASKS P2） | 🟢 | 已坐实只删 DB |
| `Ev` | `L42883 async function Ev()` | rescan：扫磁盘 → resources 表（POST /api/resources/rescan） | 🟢 | 对应 TASKS T0.1「Ev=rescan 触发器」✅ |
| `we` | `L4176 we = (t, n = false) => {...}`（组件内 callback，PromptNode 作用域）；另有组件内 `L43015 we = Y.useCallback(async () => {...})`（资源面板作用域） | ⚠️ **L4176 的 `we` 实为 PromptNode 的 `@提及` 文本插入回调**（往 textarea 插入 `@xxx`），**不是**资源加载函数。真正"加载资源(rescan+查询)"是 `L43015 we = Y.useCallback` | 🟢 | 推翻 func-mapping 隐含假设与 ARCHITECTURE L141「we=加载资源」；两处 `we` 同名不同组件作用域（均组件内，非模块级），引用必须带行号+组件上下文 |
| `Xr` | `L1802 async function Xr(e,t={})`；另有组件内 `L43881 Xr = async e=>` | ⚠️ **双重定义**：模块级 `Xr`(L1802)=**上传落盘实现**（内部 L1812 `fetch('/api/files/upload')`，名为 openInTab 实为 uploadToLocalTool）；`ii`(L1888) 在 L1898 调它完成上传。组件内 `Xr`(L43881)=**openInTab**（活动标签页打开 URL）。原 ARCHITECTURE/TASKS 把"模块级 Xr 非上传"是**错误**——模块级 Xr 确为上传环节 | 🟢 | 纠正 02 文档初版误判；上传链 = `ii`(L1888)→`Xr`(L1802)→POST /api/files/upload |
| `Zr` | `L1827 async function Zr(e,t={})`；另有组件内 `L43893 Zr = async () =>` | ⚠️ **双重定义**：模块级 `Zr`(L1827)=`uploadRemoteUrlToLocalTool`（远程 URL 上传落盘，L1832 POST /api/files/upload）；组件内 `Zr`(L43893)=**注销/重置配置**（logout）。原文档把 `Zr` 当"下载函数"是错的——模块级 Zr 实为上传（远程URL），组件内 Zr 为注销 | 🟢 | 纠正 ARCHITECTURE L2.5 / TASKS L34 误判 |
| 资源入库（右侧栏"发送到资源"） | `L43436–L43467`（原记 `Zr()`，已证伪→实为 `Zr` 注销）；真实写库调用 `Sv()` + 上传 `ii()`/`Xr` 体系 | 见 02_文档错误修正 §T4.1 | 🟢 | 非单一 `Zr`，是多函数协作 |
| AI 生成派发 | `L32490 let Jn = Y.useCallback(async (e, r, o, s, c, d) => {...})` 为组件内局部生图回调；N 分支分流在 L32957 附近（`task_id` 检测 + `pollUrl=${R}/v1/tasks/${taskId}`） | 生图主派发=组件内 `Jn`(L32490)，**非模块级 `Jn`(L89, LogoIcon)** | 🟢 | TASKS T0.1「AI 生成 N 分支派发非 `Jn`」✅ 坐实 |

---

## 二、T0.2 核对 var-mapping 易混项

| 项 | 现状 | 动作 | 状态 |
|----|------|------|------|
| `Zr = logout`（var-mapping L48） | grep `L43893 Zr = async () =>` 在组件内，确为注销/重置配置；ARCHITECTURE L2.5 与 TASKS 行34 误把它当"下载函数" | 纠正：下载非 `Zr` | 🟢 |
| `sv = nodeCallbackFieldSet`（var-mapping L178） | grep `L42002 sv = new Set(\`...\`)` 节点回调字段白名单 | 与 `Sv`(大写,资源入库) 厘清：大小写不同、语义不同 | 🟢 |
| `Hr` / `vv` | var-mapping L117/L118：`Hr=localToolStatusUrl`、`vv=localToolBaseUrl`，值均 `http://127.0.0.1:18080` | 两处 base URL 同源；ARCHITECTURE X3 跨层风险3 提及 `Hr` vs `vv` 不一致仅在 `USE_LOCAL_ENGINE=false` 触发，属预期分支 | 🟢 |
| `B` / `R` / `ii` / `wv` / `ri` | `ii`/`wv`/`ri` 已补上；`B`/`R` 为组件内局部回调（资源面板 URL 拖入 L29176、文件上传 L29165），非模块级绑定 | `B`/`R` 标注"局部，勿随表批量重命名" | 🟡 |

---

## 三、T0.3 回写建议（待人工确认后并入原表）

以下条目建议并入 `docs/func-mapping.txt`（新增行）或 `docs/var-mapping.txt`：

```text
# 建议新增（func-mapping.txt）
ri = thumbnailCache         # 前端缩略图内存缓存 Map<TTL> + 并发锁，@1856
ii = uploadFileLocal        # 落盘上传统一入口，写 uploads/ 子目录，@1888

# 建议新增（var-mapping.txt）
Sv = saveResource           # 资源入库 upsert（POST /api/resources/save），@42838（大写，区别于 sv）
wv = deleteResource         # 资源删除（只删 DB），@42857
Ev = rescanResources        # 扫磁盘→resources 表，@42883
```

> ⚠️ 不改原表：上方仅作建议，避免污染现有已验证映射。合并由原作者在 `docs/` 根执行。

---

## 四、审计闭环补充（已坐实的关键澄清）

- **`Jn` 双重身份**：模块级 `function Jn({size})`（L89）= LogoIcon 品牌图标；组件内 `let Jn = Y.useCallback(...)`（L32490）= 生图主回调。两者同名不同作用域，**引用生图逻辑必须带 L32490**，不能只说"`Jn`"。
- `func-mapping.txt` L65 `Jn = LogoIcon` 正确；但 `ARCHITECTURE.md` L1.3 / `TASKS.md` 把生图派发说成 `Jn` 是**错误**，应改为"组件内 `Jn`@L32490"。

### 新增发现（模块7 审计，2026-07-21）

- **`Lr` 双重身份**：模块级 `function Lr(e,t,n)`（L1700）= 缩略图 URL 构造器（拼 `_resize_N.jpg`）；组件内 `Lr = Y.useCallback`（L36293）= 画布 onDrop 投放处理器（解析模板/任务/文件三类拖入）。同名不同作用域，引用须带行号。建议映射表补 `Lr=thumbnailUrlBuilder`@1700 并标注"画布 Lr@36293 为 onDrop，非此函数"。
- **`Ir` 同名不同义**：模块级 `function Ir(e)`（L1695）= 缩放档位选择器（配合 `Fr` 数组）；画布 `onDragOver: Ir`（L37137）= 组件内 dragover 处理器。不冲突（作用域隔离），但文档引用须带行号区分。
- **`R` 双局部**：资源面板 `function R(e)`（L23247，拖拽起点）vs 上传回调 `R`（L29165 附近，`await R(files)`）；另有 `z`(L23255)/`B`(L23258) 为同组件内拖拽/目标选择局部回调。均非模块级绑定，引用须带行号。
- **`Z` 未收录**：组件内节点工厂 `Z(nodeType, pos, props)`（L36318 等）负责模板/任务拖入落点；建议补 `Z=createNode`。
- 累计双重定义清单（审计并集）：`Jn`(L89/L32490)、`we`(L4176 组件内@提及/L43015 组件内资源加载)、`Lr`(L1700/L36293)、`Ir`(L1695/L37137)、`Xr`(L1802 模块级上传/L43881 组件内openInTab)、`Zr`(L1827 模块级远程URL上传/L43893 组件内注销)、`Sv`(大写资源入库 L42838 vs 小写 `sv` 字段白名单 L42002)。`01_` 已记 Jn/we/Xr/Zr/Lr/Ir/R/Z。
