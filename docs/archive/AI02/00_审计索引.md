# 一毛AI画布 · 审计整理索引（AI02 工作区）

> **本目录为审计专属工作区**。所有产物仅存放于 `docs/AI02/`，不污染 `docs/` 根与其他目录。
> 审计对象：当前运行的 **V1 引擎**（`src/_engine/App.js` 等），V2 已暂停不纳入。
> 方法：遵循 `docs/TASKS.md` 末尾《架构审计计划》的五关流水线 + 四段式模块输出。
> 快照日期：**2026-07-21**（行号会漂移，文档内引用以「L行号 + grep 复核」为准；当前源码快照 App.js **46397 行**，早期文档记 46252 为初始快照，差异属正常漂移）。

---

## 审计原则（红线对齐）

1. **只出文档，不改代码**：本次为审计整理，不修改 `App.js` / `config.js` / `dist/` / `localTool` / 网关 任何源码。
2. **锚点门**：每条事实必须带 `file:line`，且经 `grep` 在源码坐实；映射表未收录的不猜测，先补映射表再写。
3. **映射表是唯一可信锚层**：`docs/func-mapping.txt` + `docs/var-mapping.txt` 为权威字典；本工作区产出的补全单独放 `01_映射表补全记录.md`，**不回写原表**（避免污染，待人工确认后并入）。
4. **文档派生不可信**：现有 `ARCHITECTURE.md` / `TASKS.md` 的锚点已知有误，已在 `02_文档错误修正.md` 列出并修正建议。

---

## 文件清单（本工作区）

| 文件 | 内容 | 状态 |
|------|------|------|
| `00_审计索引.md` | 本文件：范围/原则/清单 | ✅ 建 |
| `01_映射表补全记录.md` | T0 缺口坐实结果（不污染原表） | ✅ 建 |
| `02_文档错误修正.md` | ARCHITECTURE/TASKS 错误锚点修正建议 | ✅ 建 |
| `10_模块1_系统初始化与配置层.md` | 模块1 四段式审计 | ✅ 建 |
| `11_模块2_本地数据层与Rescan.md` | 模块2 四段式审计 | ✅ 建 |
| `12_模块3_AI生成与网关层.md` | 模块3 四段式审计 | ✅ 建 |
| `13_模块4_画布引擎与节点.md` | 模块4 四段式审计 | ✅ 建 |
| `14_模块5_事件总线与跨窗口通信.md` | 模块5 四段式（边界契约专精） | ✅ 建 |
| `15_模块6_配置开关与运行时链路.md` | 模块6 四段式（Q 封装 + rescan 触发链） | ✅ 建 |
| `16_模块7_画布投放与节点拖拽边界.md` | 模块7 四段式（onDrop + 双重定义陷阱） | ✅ 建 |
| `17_模块8_PromptNode与文本节点系统.md` | 模块8 四段式（we@L4176 @提及坐实 + 文本节点） | ✅ 建 |
| `20_交叉流审计.md` | 阶段2.5 横切流缝合 | ✅ 建 |
| `30_审计自检复查.md` | 阶段4 一致性自检 + 门3 回填 | ✅ 建 |
| `99_审计总报告.md` | 阶段3 汇总 + Bug 回填建议 | ✅ 建 |
| `scripts/check-doc-citations.cjs` | 门3 机器校验脚本（T0.4，只读源码） | ✅ 建 |
| `校验报告.md` | 门3 校验输出（✅3 🟡1 ❌0） | ✅ 建 |

---

## 已坐实的关键事实（门1 锚点门预览）

- `Jn` 模块级（L89）= 品牌 SVG 图标组件（viewBox `0 0 1389 1024`），即 `LogoIcon`。**生图主回调是组件内局部变量 `let Jn = Y.useCallback(...)`（L32490），非模块级 `Jn`**——这是原文档把 `Jn` 误当生图派发的根因。
- `Xr` 为**双重定义**：模块级 `Xr`(L1802)=**上传落盘实现**（`ii` L1898 调它 POST /api/files/upload）；组件内 `Xr`(L43881)=`openInTab`（活动标签页打开 URL）。原"模块级 Xr 非上传"为审计初版误判，二审已纠正（见 `02`/`99`）。
- `Zr` L43893 = 组件内注销/重置配置（logout），非下载函数。
- 模块级真实函数行号（已 grep 坐实）：`ri` L1856（缩略图缓存）、`ii` L1888（落盘上传）、`Sv` L42838（资源入库）、`wv` L42857（资源删除·只删DB）、`Ev` L42883（rescan）。

详见 `01_映射表补全记录.md` 与 `02_文档错误修正.md`。
