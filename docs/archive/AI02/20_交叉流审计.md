# 交叉流审计（阶段2.5 · 横向切片 · AI02）

> 在 B1–B4 完成后，沿各模块「边界契约」的接缝（HTTP 路由 / CustomEvent / config 开关）追踪端到端流。
> 所有接缝均回 `App.js` / `config.js` grep 复核，带 `file:line`。对照 ARCHITECTURE.md X1–X3 修正/补全。

---

## C1 · AI 生成 → 落盘 → 资源面板 全流程（缝合 B3+B2+B4）

```
节点 onGenerate (PromptNode/ImageNode 等)
  → Jn(nodeId,...) 组件内生图主回调          App.js L32490（注意：非模块级 Jn@L89）
  → 网关 POST /v1/images|videos/generations  :9004（SSE 或异步 task_id）
  → 异步: 轮询 GET /v1/tasks/{task_id}         L33005（≥3s/退避/15min deadline L33003）
  → status=completed → 提取 result.images[0].url[0]   L33031（数组[0]陷阱）
  → ii(u,{subfolder:'tasks'}) 落盘 18080        L33049 → Xr(L1802) → POST /api/files/upload
  → 更新节点 data.imageUrl/videoUrl
  → 统一同步 effect 遍历 globalTasks:
      本地化判定 L44369（/files/ 或 127.0.0.1/files/tasks 跳过）
      → n.uploadFile(blob,'tasks') 落盘          L44377
      → dispatchEvent('mutiwindow-task-completed')  L44406
  → Ev()(L42883) POST /api/resources/rescan → 资源面板刷新
  → Sv()(L42838) 资源入库（若走"发送到资源"路径 L43436）
```
**接缝校验**：网关路由（L33005）/ 落盘（L33049,L44377）/ 事件总线（L44406）/ rescan 触发 `Ev()`（L42883）全部 grep 坐实。✅

---

## C2 · Rescan 孤儿清理与资源入库一致性（缝合 B2 内部 + localTool）

```
Ev()(L42883) → POST :18080/api/resources/rescan
  → localTool resources.ts rescan 扫 uploads/ 子目录（排除 .thumbnails）
  → 孤儿清理：只清 source='local-tool' 且磁盘不存在（TASKS P2）
  → source='extension' 记录不被清理（跨层风险7）
资源入库 Sv()(L42838) vs rescan 重复：
  - 右键"发送到资源" L43436 → ii 落盘 + Sv 入库 + Ev rescan（双写：Sv 显式 + rescan 扫盘）
  - 潜在重复：rescan 用 id=`local-{folder}-{name}`，resourceAdded 用 Date.now()（跨层风险2 资源ID冲突）
```
**接缝校验**：`Ev`(L42883)/`Sv`(L42838) 坐实；孤儿清理逻辑在 localTool 侧（非前端），归 localTool 审计。✅

---

## C3 · 配置加载与双服务 base URL 桥接（缝合 B1+B2+B3）

```
config.js:
  USE_LOCAL_ENGINE(config.js L36)=true → localEngineBase()=LOCAL_ENGINE.base=http://127.0.0.1:18080 (L42/L14)
  DEFAULT_ENDPOINT=:9004 (L30) → AI 网关
前端变量（var-mapping）：
  Hr=localToolStatusUrl=18080 (L117) → ri(L1871)/状态轮询
  vv=localToolBaseUrl=18080 (L118) → Sv/wv/Ev(L42840/L42859/L42885)
  Bc=localToolPort='18080' (L120) → ii 兜底前缀
  $n=apiBaseUrl / er='/api' (L112/L115) → ar/tr/cr 解析网关 base
桥接分支（USE_LOCAL_ENGINE=false）：
  localEngineBase()→REMOTE_BASE=:9004 (L39) → 文件请求走 9004 无 /api/files/upload → 失败（跨层风险3）
```
**接缝校验**：config.js L36/L42/L30 + var-mapping Hr/vv/Bc/$n/er 坐实。✅ 发现：`Hr`/`vv` 值相同=18080，仅在 false 分支分歧。

---

## C4 · 画布事件总线跨模块通知（缝合 B1+B2+B3+B4）

| CustomEvent | 触发(行号) | 消费(模块) | 说明 |
|-------------|-----------|-----------|------|
| `mutiwindow-task-completed` | L38430/L43640/L43676/L43697/**L44406** | B2 Ev rescan | 资源刷新（生成完成） |
| `resourceAdded` | background.ts → L43436 | B2 ii 落盘 + Sv 入库 | 右键采集 |
| `mutiwindow-update-task-meta` | I_(useTaskMetaSync) | 多窗口任务元数据 | func-mapping |
| `import-project`/`export-project` | AppShell L44644/L44650 | 文件选择/保存 | B4 画布导入导出 |
| `yimao:openPromptSettings`(Va) | Ha() L3641（内部 L3643 dispatch Va） | 提示词设置面板 | B1 |

> ⚠️ 已删除原 C4 表中 `canvas-state-change` 行：经 grep 复核 App.js **无此事件名**（原表凭空列出，违反"每条事实带 file:line"原则）。画布持久化实际走 `Q`(StorageManager, L1476) + `Cr`(L1294, localforage key 构造)，非 CustomEvent。
>
> ⚠️ 已删除原 C4 表中 `mutiwindow-sync-local` 行：经 grep 复核 App.js / 全仓 **0 命中**（已由 AI06/09/10/12 实锤），其并非 CustomEvent 事件总线成员。资源面板"手动同步"真实实现为 `handleSyncLocal` 函数（App.js L44426，非事件总线）；资源面板的同步推进实际走 `rescanSync`(we，L43015) 由 `mutiwindow-task-completed` 触发，而非独立事件。该假事件名已按 AI13 裁决表 06-2 自清。
>
> [2026-07-21 据 AI13 裁决表修订：C4 表删 mutiwindow-sync-local 残留行]

**状态同步红线校验**：所有通知经事件总线，未绕过 `StorageManager`(Q，@1441)；`ii`/`Sv`/`Ev` 均经 Q/window.localTool。✅（符合红线§3.3）

---

## 交叉流审计结论

1. **四流接缝全部 grep 坐实**，无悬空锚点。
2. **TASKS 已知 Bug 在流中定位明确**：P0 破图(C3 host 硬编码)、P1 URL 不落盘(C1 经 ii 的 http 分支短路 L1889)、P2 删不删磁盘(C2 wv L42857 无 deleteFiles)。
3. **新发现跨流风险**：
   - `mutiwindow-task-completed` 在 L44406（统一同步 effect）触发，但**生成完成即时刷新**也在 L38430/L43640 等——多入口 dispatch，需确认是否重复 rescan。
   - `resourceAdded`(L43436) 与统一同步 effect 循环（L44360）双路径都可能落盘同一文件 → 潜在重复（ID 冲突跨层风险2）。
4. Ctrl 框选动态切换（B4 存疑）不影响交叉流，单独标注。

> 校验状态：🟢 接缝全 grep 坐实；🟡 两处重复 dispatch/rescan 风险建议运行时验证（非代码盲猜）。
