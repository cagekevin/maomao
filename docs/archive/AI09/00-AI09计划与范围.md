# AI09 · 审计整理计划与范围

> 执行区：`docs/AI09/`（2026-07-21）
> 依据：先读 `CLAUDE.md` + `docs/`（`PROJECT_ORIGIN.md` / `ARCHITECTURE.md` / `TASKS.md` / `FUNCTION_MAP.md` / `func-mapping.txt` / `var-mapping.txt` / `PROJECT_LOG.md`），再实地 grep 代码核对行号。
> 红线约束：本次只产出**审计文档**，不修改任何运行代码（`App.js`/`config.js`/`localTool`/`apimart-gateway` 一律只读）；权威文档（`CLAUDE.md`/`ARCHITECTURE.md`/`func-mapping.txt`/`var-mapping.txt`）不当作重写对象，仅作为真理参照。

---

## 0. 隔离声明（硬约束）

- 本会话所有文件**只落在 `docs/AI09/`**，不新建/修改 `docs/AI01~AI06`、`docs/*.md`、任何运行代码、映射表。
- 不读别人目录的"推理过程"，只取已沉淀的权威事实（代码 + 映射表 + 总览声明）。AI05 的 `00-审计总览.md` 作为范围参照，但其"待产"清单由本会话独立承接，不复制其未验证结论。

## 1. 本次审计的范围

| 项 | 内容 |
|----|------|
| 输入 | `CLAUDE.md`、`docs/PROJECT_ORIGIN.md`、`docs/ARCHITECTURE.md`、`docs/TASKS.md`、`docs/FUNCTION_MAP.md`、`docs/func-mapping.txt`、`docs/var-mapping.txt`、`docs/PROJECT_LOG.md`；以及 `docs/AI05/00-审计总览.md`（范围参照） |
| 代码核对 | `src/_engine/App.js`（~46k 行）、`src/_engine/config.js`、`localTool/src/index.ts` 及子路由、`apimart-gateway/main.py` |
| 产出 | 仅 `docs/AI09/` 下的 md（01~03 + 校验报告 + 本文件） |
| 不触碰 | `docs/AI01~AI06` 他人文件、`dist/`、`reference/`、`src/v2/`、`scripts/`、任何运行代码、权威文档与映射表 |

## 2. 承接关系（与 AI05 的边界）

AI05 已完成「T0 缺口坐实」（映射表缺口 grep 复核）并产出 `00-审计总览.md`，但其 T2.1~T2.5 模块文档、T4 纠错建议、门3 校验报告均**标"待产"**。

本会话**不重复** AI05 的 T0 缺口核对，而是承接其待产部分，聚焦两项最易漂移、最需门3 机器复核的内容：
1. **边界契约实测**（T2 硬要求）：把 HTTP 路由 / `window` CustomEvent / config 开关 / 混淆名行号**全部重新 grep 坐实**，因为 AI05 给的行号会漂移。
2. **交叉流沿接缝缝合**（T2.5）：用实测锚点串 1~2 条端到端流，验证与 `ARCHITECTURE.md` 一致。

## 3. 方法论（防幻觉，取自 TASKS.md 五关流水线）

- **门1 锚点门**：只用映射表已收录名 + 自 grep 行号；新名先 grep 定位再写。
- **门2 强制引用**：每条事实带 `文件:L行号`。
- **门3 机器校验**：产出后跑 `scripts/check-doc-citations.cjs`，正则抽文档内 `App.js:L\d+` / `函数名()`，回源码 grep 确认存在，出 `校验报告-AI09.md`。❌ 项打回重写。
- **门4/门5**：本次单人单会话，用"写手→自 grep 复核→列 ❌/⚠️ 项"替代对抗审计。
- **防污染**：行号会漂移，AI05/AI01 的行号快照仅作线索，**最终以本次实际 grep 为准**。

## 4. 产出清单（本目录）

| 文件 | 内容 | 状态 | 门3 |
|------|------|------|------|
| `00-AI09计划与范围.md` | 本文：隔离声明 / 范围 / 承接关系 / 方法论 | ✅ | — |
| `01-边界契约核对.md` | 4 类接缝实测：HTTP 路由 / CustomEvent / config 开关 / 混淆名行号（全 grep 坐实） | ✅ | 8 引用 6✅/2⚠️/0❌ |
| `02-交叉流审计.md` | 沿边界契约缝合 2 条端到端流（AI生成→落盘→资源面板；Rescan 孤儿清理） | ✅ | 5 引用 全✅ |
| `03-文档纠错清单.md` | 给文档维护者的错锚点回填建议（不动权威文件） | ✅ | — |
| `04-模块1-初始化与配置层.md` | T2.1：bootstrap/config 加载/鉴权/base URL 桥接/存储后端 | ✅ | 12 引用 全✅ |
| `05-模块2-本地数据层与Rescan.md` | T2.2：资源入库/删除/rescan/缩略图/上传落盘 + P1/P2 排雷 | ✅ | 11 引用 全✅ |
| `06-模块3-AI生成与网关层.md` | T2.3：Jn 生图主回调/轮询/落盘/事件总线 + 轮询陷阱排雷 | ✅ | 4 引用 全✅ |
| `07-模块4-画布引擎与节点.md` | T2.4：ReactFlow handler/创建节点/节点类型/性能降级 + 3D 导演台真实锚点（Director3D） | ✅ | 20 引用 全✅ |
| `08-交叉流补充.md` | T2.5.3 配置桥接流 + T2.5.4 事件总线流 + 3D 导演台**真锚更正**（第三轮撤错锚） | ✅ | 5 引用 4✅/1⚠️ |
| `09-Bug表交叉核对.md` | T3.2：逐条核对 `TASKS.md` P0–P2 Bug 与修复清单锚点真伪 | ✅ | 门3 5✅ |
| `10-映射表缺口分析.md` | AI09 实测符号 × 映射表收录对照；列出 6 个未入表模块级符号 + 歧义项 | ✅ | 门3 6✅ |
| `11-门4对抗审计.md` | 5 项存疑反向质询（持久化/节点回填/Sv大小写/删磁盘/toAbsoluteFileUrl）| ✅ | 无阻断项 |
| `12-门5入库与总览.md` | 五关汇总 + 给维护者回填清单 | ✅ | — |
| `13-深挖盲区审计.md` | T2.6：剪映发送/`Kn`误植/`Qn`碰撞/pending_confirmation/双上传/`zc`原语 | ✅ | 门3 5✅/1⚠️(软) |
| `14-云端同步与跨进程盲区.md` | T2.7：CloudSyncEngine 真身 / var-mapping L9–L12 误植 / background.ts resourceAdded / 端口一致 | ✅ | 门3 4✅/1⚠️(软) |
| `15-资源回流全链路审计.md` | T2.8：Ev/Sv/wv/xv/we 函数体 + 生成→rescan→refetch 回流 glue | ✅ | 门3 5✅ |
| `校验报告-AI09.md` | 门3 总报告（01+02 汇总） | ✅ | — |
| `校验报告-0N.md` | 各模块门3 细分报告（04~08） | ✅ | — |
| `校验报告-09-10.md` | 09/10 门3 细分报告 | ✅ | — |

> **第三轮更正（2026-07-21）**：撤回第二轮对 `Th`=Director3DNode 的"错锚"误判（见 03/07/08 更正）。实测 `Th`=Y.memo@L28388、[Director3DNode]@L28449、`director3dNode:Th`@L31141、`type:'director3dNode'`@L35724，确证其为真；3D 导演台节点形态 + viewMode 视口形态并存。

## 5. 与既有文档的边界

- `CLAUDE.md` §3 红线、`ARCHITECTURE.md` 架构事实：**不重写**，直接引用。
- `TASKS.md` Bug 表：**不复制**，发现的错锚在 `03` 给回填建议。
- 本目录是**派生审计区**，权威事实以代码 + 映射表为准；若本文与代码冲突，以代码为准并回写 `03`。
