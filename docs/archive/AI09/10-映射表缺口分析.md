# AI09 · 映射表缺口分析（func-mapping.txt / var-mapping.txt）

> 把 AI09（T2.1–T2.5）实测到的模块级符号，对照两份权威映射表，找出**未收录**与**收录但歧义**项。
> 红线：只列出，不回写映射表（除非用户要求）。门1 要求"新发现函数先入映射表再写"，故缺口即下一批待补项。
> 实测基准 2026-07-21，行号会漂移。

---

## 一、AI09 已用符号 × 映射表收录状态

| 符号 | 语义 | 实测位置 | func-mapping | var-mapping | 状态 |
|------|------|------|------|------|------|
| `Oa` | getAuthToken | L3543 | ✅ L14 | — | 已收录 |
| `Ba` | hasAuthToken | L3637 | ✅ L85 | — | 已收录 |
| `Jh` | authHeaders | L30712 | ✅ L13 | — | 已收录 |
| `ar` | resolveApiUrl | (模块1) | ✅ L10 | — | 已收录 |
| `tr` | withApiSuffix | (模块1) | ✅ L7 | — | 已收录 |
| `jr` | cachedGet | L1345 | ✅ L157 | — | 已收录 |
| `Nr` | localStorageBackend | L1418 | ✅ L152 | — | 已收录 |
| `Pr` | localforageBackend | L1446 | ✅ L153 | — | 已收录 |
| `Cr` | canvasStateKey | L1294 | ✅ L15 | — | 已收录 |
| `Qn` | TransitPanel | L169 | ✅ L66 | — | 已收录 |
| `Jn` | LogoIcon | L89 | ✅ L65 | — | 已收录（与模块3 局部 `Jn`=生图回调同名人，引用须带行号）|
| `Lr` | onDrop | L36293 | ✅ L188 | — | 已收录 |
| `xn` | onConnect | L31883 | ✅ L180 | — | 已收录 |
| `Cn` | onPaneContextMenu | L31933 | ✅ L181 | — | 已收录 |
| `wn` | onNodeContextMenu | L31947 | ✅ L182 | — | 已收录 |
| `kn` | onNodeClick | L31980 | ✅ L185 | — | 已收录 |
| `Fr` | onConnectEnd | L36245 | ✅ L186 | — | 已收录 |
| `Ir`(小写) | onDragOver | L36290 | ✅ L187 | — | 已收录 |
| `Z` | StorageKeys | L1268（`Z = {...}`，模块级）| — | ✅ L21 | 已收录（⚠️ 注意：组件级 L35536 的 `let Z=(...)` 是 `addNodeWithData` 创建节点函数，与模块级 `Z`=StorageKeys 同名不同物，引用须带行号）|
| `Hr` | localToolStatusUrl | L1732 | — | ✅ L117 | 已收录 |
| `vv` | localToolBaseUrl | L42808 | — | ✅ L118 | 已收录 |
| `U_` | localServiceBaseUrl | L41570 | — | ✅ L233 | 已收录 |
| `Th` | Director3DNode | L28388 | ⚠️ 标注"跳过(用户早期已收录)"L160 | ✅ L187 | 已收录（第三轮更正确认真身）|
| `Zr` | **双绑定**：①logout（真身 `App.js:L43893`，`Q.remove(Z.AUTH_TOKEN)`）②下载函数（`App.js:L1827 async function Zr(e,t={})`，由 `resourceAdded@L43539` 调 `await Zr(i.url,...)` 落盘）| — | — | ✅ L48（仅收 logout，漏收 L1827 下载绑定）| 已收录但**漏收 L1827 下载双绑定**（var-mapping 缺口，AI05 断言正确；旧「Zr=logout(L1827)」行号错锚，logout 真身 L43893）|

---

## 二、🟡 缺口：AI09 实测到、两份映射表均未收录的模块级符号

这些符号 AI09 已 grep 坐实（模块级 `async function` / `function`），但 func-mapping.txt / var-mapping.txt 都**没有**，违反门1「先入表再写」。属下一批待补项：

| 符号 | 语义（推断/实测）| 实测位置 | 建议收录类别 |
|------|------|------|------|
| `Ev` | rescan 触发器：`fetch(${vv}/api/resources/rescan)` 发起者 | App.js **L42883** `async function Ev()` | func-mapping（资源 rescan）|
| `Sv` | 资源入库（写库）：`async function Sv(e)` | App.js **L42838** | func-mapping（⚠️ 与 var-mapping `sv`=nodeCallbackFieldSet 小写易混，须显式区分大小写）|
| `wv` | 资源删除（只删 DB）：`async function wv(e)` | App.js **L42857** | func-mapping（删除，P2-6）|
| `xv` | 资源刷新/列表：`async function xv(e={})` | App.js **L42821** | func-mapping（资源查询）|
| `ii` | 统一下载/落盘：`async function ii(e,t={})` | App.js **L1888** | func-mapping（上传/下载，TASKS P1-2 真身）|
| `ri` | 缩略图缓存：`async function ri(e,t={})` | App.js **L1856** | func-mapping（内存缓存 Map）|
| `Cv` | 资源面板辅助（实测 L42851 一带）| App.js L42851 | 待坐实语义后补 |
| `Sv`/`wv`/`xv`/`Ev` 的 localTool 后端对应 | `/api/resources/*` 路由 handler | localTool resources.ts | 建议单独「localTool 处理函数表」|

> 注：`we`（TASKS 记的 rescan 主函数）经核实**非模块级**，是组件作用域变量，故**不计入缺口**（见 09-Bug表交叉核对 §三）。

---

## 三、🟡 收录但歧义 / 风险项

| 符号 | 歧义 | 影响 | 建议 |
|------|------|------|------|
| `Sv`(大写) vs `sv`(小写) | var-mapping L178 `sv`=nodeCallbackFieldSet；func-mapping 未收录大写 `Sv` | 下一批写入 `Sv` 时须显式标注"大写 Sv=资源入库，小写 sv=nodeCallbackFieldSet"，防混 | 补 func-mapping `Sv` 并加注大小写区分 |
| `Jn` 同名 | func-mapping L65 `Jn`=LogoIcon；模块3 局部 `Jn`=生图主回调(L32490) | 引用 Jn 必须带行号，否则混淆 | 映射表已对模块级收；局部 Jn 引用须带行号 |
| `Lr` 双义 | func-mapping L38 `Lr`=withThumbSuffix（URL 后缀函数）；L188 `Lr`=onDrop（事件 handler）| 同一名两处不同语义 | 引用须带行号；建议批量替换时确认两处都覆盖 |
| `Th` 收录状态 | func-mapping L160 标"跳过(用户早期已收录)"；var-mapping L187 已收 | 第三轮更正确认真身存在，收录正确；但 func-mapping 标注措辞易让人以为"未验证" | 建议 func-mapping 改"已收录见 var-mapping L187，真身 L28388" |

---

## 四、给映射表维护者的回写建议（不代改）

1. **func-mapping.txt 新增**：`Ev`(L42883)/`Sv`(L42838)/`wv`(L42857)/`xv`(L42821)/`ii`(L1888)/`ri`(L1856) 六条模块级资源/下载函数；其中 `Sv` 须与 `sv` 大小写区分。
2. **localTool 处理函数表**（新建或并入 func-mapping）：`index.ts` 中央 if 分发各 handler、`resources.ts` 的 rescan/save/delete、`files.ts` 的 upload/thumbnail。
3. **func-mapping.txt `Th` 条目措辞**：由"跳过"改为指向 var-mapping L187 + 真身 L28388，消除"未验证"歧义。
4. **var-mapping.txt**：`B`/`R` 多处组件内局部重名，建议加注「非模块级，随组件隔离」（已在 03 提出）。

> 缺口汇总：本次审计新确认 **6 个模块级符号未入表**（Ev/Sv/wv/xv/ii/ri），是门1 要求"先入表"但此前缺失的部分；补入后 ARCHITECTURE/TASKS 中 `B`/`R`/`we`/`Zr` 错名可由这些真名替换。

[2026-07-21 据 AI13 裁决表修订：Zr=logout 改 L43893，补 L1827 下载双绑定]
