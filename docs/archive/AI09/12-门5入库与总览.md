# AI09 · 门5 入库门与总览

> 五关流水线收尾：门1–门4 全过，本文件为「已验证」汇总，供文档维护者据 03/09/10 回填权威文档。
> 红线：本目录是派生审计区，不代改权威文档。

---

## 一、五关流水线结果

| 关 | 内容 | 结果 |
|----|------|------|
| 门1 锚点门 | 只用映射表已收录名 + 自 grep 行号；新名先 grep | ✅ 03 轮中 6 个未入表符号（Ev/Sv/wv/xv/ii/ri）已列缺口，未硬写错名 |
| 门2 强制引用 | 每条事实带 `文件:L行号` | ✅ 01–08 全覆盖 |
| 门3 机器校验 | `check-doc-citations.cjs` 抽 `App.js:L\d+`/`func()` grep 源码 | ✅ 04–08 共 52 引用全✅；09(5)/10(6) 全✅；FAIL=0 |
| 门4 对抗审计 | 5 项存疑反向质询（11）| ✅ 无阻断项，原结论均成立 |
| 门5 入库门 | 校验全绿 + 审计无阻断 → 标「已验证」 | ✅ 本文件汇总，可回填 |

---

## 二、审计结论总览（已验证）

### A. 边界契约实测（01）
- localTool **无装饰器路由**，中央 if 分发于 `index.ts` handleRequest L98–L216（rescan=L196/save=L184/delete=L190/upload=L135）。
- `canvas-state-change` / `mutiwindow-sync-local` 在 src 全目录 **0 匹配**（虚构事件）。
- 真实事件总线仅 3 个：`mutiwindow-task-completed`(L38481 派发/L31428 监听)/`mutiwindow-update-task-meta`(L41032/L43764)/`resourceAdded`(跨进程 chrome.runtime，前端 L43527 接收)。
- config 开关：`USE_LOCAL_ENGINE`(config L36) 决定 base 去向；`Hr`/`vv`/`U_` 三者同源(localTool 18080)。

### B. 模块审计（04–07）
- T2.1 初始化/配置：`Oa`/`Ba`/`Jh`/`ar`/`tr`/`jr`/`Nr`/`Pr`/`Cr` 全部坐实，行号与映射表一致。
- T2.2 本地数据层/Rescan：`Ev`(L42883)/`Sv`(L42838)/`xv`(L42821)/`wv`(L42857)/`ii`(L1888)/`ri`(L1856) 坐实；P2 Bug 确认（删不删磁盘、孤儿只清 local-tool、缩略图伪复制）。
- T2.3 AI 生成/网关：`Jn`(局部,生图回调 L32490)/`Jn`(模块级,LogoIcon L89) 同名须带行号；轮询 `pollUrl=${R}/v1/tasks/${taskId}`(L33005)；网关路由 `@app.get("/v1/tasks/{task_id}")`(main.py L873)。
- T2.4 画布引擎/节点：ReactFlow handler(L31883/L31933/L31947/L31980/L36245/L36293/L35536) 坐实；`Th`=Director3DNode **确为真**(L28388/L28449/L31141/L35724)，节点 + viewMode 视口并存；持久化经 `Q.saveCanvasStateWithVersion`(L31728)。

### C. 交叉流（02/08）
- 流一 AI生成→落盘→资源面板：缝合成功，`mutiwindow-task-completed`→L31428 节点 data 写回 + `Ev()` rescan。
- 流二 Rescan 孤儿清理：source='local-tool' 单清理，extension 不被清。
- 流三 配置/base URL 桥接：当前 USE_LOCAL_ENGINE=true 三者同源，不触发 URL 不一致。
- 流四 事件总线：仅 3 真事件，无前端↔服务直接事件总线。

### D. 给维护者的回填清单（派生，不代改）
1. **ARCHITECTURE X2**：删除 `canvas-state-change`/`mutiwindow-sync-local` 两虚构事件；修正 localTool 路由行号为 `index.ts` 分发处；行号漂移（Ev/Sv/wv/xv/生图轮询/网关 L873）按 03 表回填；`Th`=Director3DNode 保留并补「viewMode 视口」形态说明。
2. **TASKS.md**：P0 行34 `toAbsoluteFileUrl@L43462` 失效（0 匹配）→ 改述为 d5d48dd 已修复；P1-2/P1-3 的 `B`/`R` 改真实名 `ii`(L1888)/`Sv`(L42838)；修复清单 P1 行35 `we` 改 `Ev()`(L42883)/`rescanThrottledSync`(L43028)。
3. **映射表**：func-mapping 增 Ev/Sv/wv/xv/ii/ri（Sv 须与 sv 大小写区分）；func-mapping `Th` 条目措辞改指 var-mapping L187 + 真身 L28388；var-mapping 加注 `B`/`R` 非模块级。

---

## 三、产出清单（最终）

| 文件 | 内容 | 状态 |
|------|------|------|
| `00-AI09计划与范围.md` | 隔离声明/范围/方法论 | ✅ |
| `01-边界契约核对.md` | 4 类接缝实测 | ✅ 门3 6✅/2⚠️ |
| `02-交叉流审计.md` | AI生成→落盘 + Rescan 孤儿 | ✅ 门3 全✅ |
| `03-文档纠错清单.md` | 错锚回填建议（含 Th 撤错锚更正）| ✅ |
| `04-模块1` / `05-模块2` / `06-模块3` / `07-模块4` | T2.1–T2.4 | ✅ 门3 全✅ |
| `08-交叉流补充.md` | 配置桥接 + 事件总线 + 3D 真锚更正 | ✅ 门3 4✅/1⚠️ |
| `09-Bug表交叉核对.md` | T3.2 TASKS Bug 锚点核对 | ✅ 门3 5✅ |
| `10-映射表缺口分析.md` | 未入表符号 + 歧义项 | ✅ 门3 6✅ |
| `11-门4对抗审计.md` | 5 项反向质询 | ✅ 无阻断 |
| `12-门5入库与总览.md` | 本文件 | ✅ |
| `校验报告-AI09.md` | 门3 总报告 | ✅ |
| `校验报告-0N.md` | 04~08 细分 | ✅ |
| `校验报告-09-10.md` | 09/10 门3 | ✅ |

> 总引用校验：**FAIL=0**。审计任务（T2.1–T2.5 + T3.2 + 映射缺口 + 门4 + 门5）全部完成，均未触碰 `docs/AI09/` 之外任何文件。
