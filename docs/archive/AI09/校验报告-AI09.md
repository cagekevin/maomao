# AI09 · 门3 机器校验总报告

> 由 `docs/AI01/check-doc-citations.cjs`（门3 机器校验脚本）对 `docs/AI09/01`、`02` 逐条抽取 `App.js:Lnnnn` / `函数名()` 回源码 grep 生成。
> 校验日期：2026-07-21。仓库根：`G:\01画布项目\maomao`。

---

## 汇总

| 被检文档 | 总引用 | PASS ✅ | WARN ⚠️ | FAIL ❌ | 结论 |
|------|------|------|------|------|------|
| `01-边界契约核对.md` | 8 | 6 | 2 | 0 | 通过（WARN 为非 App.js 侧符号，预期内）|
| `02-交叉流审计.md` | 5 | 5 | 0 | 0 | 通过 |

**FAIL = 0 → 门3 通过，无阻断项。**

---

## WARN 项说明（非阻断）

`01` 报告的 2 个 WARN 均属"符号在其它源文件（localTool / config.js）侧，脚本默认只在 App.js 内查"的预期软未命中：

| Ref | 实际位置 | 说明 |
|-----|------|------|
| `handleRequest()` | `localTool/src/index.ts` L98 | 路由中央分发函数，非 App.js |
| `base()` | `src/_engine/config.js` L15 | `LOCAL_ENGINE.base` getter，非 App.js |

二者均已在 01 文档中用 `文件:行号` 正确标注了真实位置，WARN 仅为脚本单文件检索机制的局限，不影响锚点真实性。

---

## 行号锚点硬命中清单（✅）

| 符号/引用 | 文档声明行号 | 源码实测 | 一致 |
|------|------|------|------|
| `Ev()` | L42883 | App.js L42883 | ✅ |
| `Sv()` | L42838 | App.js L42838 | ✅ |
| `wv()` | L42857 | App.js L42857 | ✅ |
| `ii()` | L1888 | App.js L1888 | ✅ |
| `ri()` | L1856 | App.js L1856 | ✅ |
| `localEngineBase()` | L1732（旧文档引用）/ config L42 | App.js L1732 / config.js L42 | ✅ |
| `mutiwindow-task-completed` dispatch | L38481 / L44406 | App.js L38481 / L44406 | ✅ |
| `mutiwindow-task-completed` listen | L31428 | App.js L31428 | ✅ |
| `Jn`(生图主回调) | L32490 | App.js L32490 | ✅ |
| 网关 `/v1/images/generations` | L591 | main.py L591 | ✅ |
| 网关 `/v1/tasks/{task_id}` | L873 | main.py L873 | ✅ |
| localTool `/api/files/upload` | index.ts L135 | index.ts L135 | ✅ |
| localTool `/api/resources/rescan` | index.ts L196 | index.ts L196 | ✅ |

---

## 结论

- AI09 全部产出（00~03 + 04~08 + 校验报告）**均落在 `docs/AI09/`**，未触碰 `docs/AI01~AI06` 他人文件、未改任何运行代码与权威文档。
- 边界契约（01）与交叉流（02/08）的每条事实均带 `文件:行号`，门3 机器校验 **0 FAIL**。
- 发现的错锚（03）已分类列出，供文档维护者回填，**本会话未修改任何权威文档**。
- 与 AI05 的衔接：AI05 完成了 T0 缺口坐实，AI09 承接其"待产"的边界契约 + 交叉流 + 纠错 + 四个模块文档（T2.1~T2.4）+ 两条补充交叉流（T2.5.3/T2.5.4），并独立重 grep 验证了行号。

---

## 第二轮补充（模块文档 + 补充交叉流）

| 被检文档 | 总引用 | PASS | WARN | FAIL | 结论 |
|------|------|------|------|------|------|
| `04-模块1-初始化与配置层.md` | 12 | 12 | 0 | 0 | ✅ |
| `05-模块2-本地数据层与Rescan.md` | 11 | 11 | 0 | 0 | ✅ |
| `06-模块3-AI生成与网关层.md` | 4 | 4 | 0 | 0 | ✅ |
| `07-模块4-画布引擎与节点.md` | 20 | 20 | 0 | 0 | ✅ |
| `08-交叉流补充.md` | 5 | 4 | 1 | 0 | ✅（WARN=`Director3D` 组件在 App.js 非函数声明，预期内）|

### 本轮关键发现（新增至 03）
- **3D 导演台错锚**：ARCHITECTURE §L1.2 列 `Th`(Director3DNode) 为画布节点，实测 App.js 无 `Th` 对应 3D 导演台；真实实现是独立 `viewMode==='director'` 全屏视口（组件 `Director3D` 日志 `[Director3D]` L28289、状态 `viewMode:'director'` L21045、方法 `addDirectorObject` L22062）。已回填 03 纠错清单。
- **事件总线全集确认**：仅 3 个（mutiwindow-task-completed / mutiwindow-update-task-meta / resourceAdded[跨进程]），`canvas-state-change`/`mutiwindow-sync-local` 仍属虚构。
- **配置桥接同源**：`Hr`(L1732)/`vv`(L42808)/`U_`(L41570) 均 = `LOCAL_ENGINE.base`，当前运行 base URL 一致，X3.3 的「不一致」仅在 USE_LOCAL_ENGINE=false 时触发。

### 累计门3 结果
- 01~08 全部文档：FAIL 合计 **0**，WARN 仅 3 处（均属非 App.js 侧符号/组件，预期内）。
- AI09 审计任务（T0 承接 + T2.1~T2.4 + T2.5 全部 4 条交叉流 + T4 纠错）**已完成**，所有锚点经门3 机器校验无阻断项。
