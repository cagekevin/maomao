# 04 · 模块3 — AI 生成与网关层

> **四段式**：① 运转图景 ② 核心混淆字典 ③ 关键数据流 ④ 存疑 Bug
> **边界契约**：发 `POST /v1/images|videos/generations`(网关9004) → 轮询 `GET /v1/tasks/{id}` → 落盘 `POST /api/files/upload`(18080) → 事件 `mutiwindow-task-completed`。

---

## ① 运转图景

**重要纠正**：生图主回调**不是 `Jn`**（`Jn`=LogoIcon, L89）。真实派发逻辑在 **`App.js:L32987` 区域的一个大 `useCallback` 内**（无独立 function 名，依赖 `R`/`Jr`/`zc`/`ii` 等），内部含 `task_id` 检测 + 异步轮询分支（L32990-L33071）。

流程：节点触发 → `POST /v1/images|videos/generations`(`R`=网关基址, L32796) → 若返回 `task_id` → `while` 轮询 `GET ${R}/v1/tasks/${taskId}`(L33005, 经 `zc` L19001) → `completed` 提取 `images[].url`(L33031) → `ii(u,{subfolder:'tasks'})`(L33049) 落盘 18080 → 回填节点 → 派发 `mutiwindow-task-completed`(L38481)。

## ② 核心混淆字典（grep 坐实）

| 混淆名 | 行号 | 真身 | 证据 |
|--------|------|------|------|
| 生图派发 useCallback | `App.js:L32987` | 异步生成+轮询主逻辑（无独立名） | L32987 `// A-C1: task_id 检测 + 分流` |
| `R` | import映射 `R=ge` | 网关基址变量（=DEFAULT_ENDPOINT 9004） | L32796 `I=${R}/v1/images/generations` |
| `zc` | `App.js:L19001` | 带 localPort 代理 fetch 封装（轮询用） | L33007 `await zc(pollUrl,...)` |
| `ii` | `App.js:L1888` | 结果 URL 持久化落盘 | L33049 `await ii(u,{subfolder:'tasks'})` |
| `Jr` | `App.js:L1788` | `mimeToExtension` | func-mapping L40 |
| `uploadFile` | `App.js:L19098` | 组件级落盘（统一同步effect用） | L34699/L35279/L44377 调用 |
| `mutiwindow-task-completed` | `App.js:L38481` | 生成完成事件 | L38481 `dispatchEvent(new CustomEvent('mutiwindow-task-completed'` |
| `Jn` | `App.js:L89` | `LogoIcon`（**非**生图） | func-mapping L65 ✅（纠正 TASKS/ARCH 误用） |

## ③ 关键数据流（端到端）

```
节点生成回调(L32987)
  → POST ${R}/v1/images|videos/generations  (R=9004, L32796)
  → 返回 task_id? → while 轮询:
       GET ${R}/v1/tasks/${taskId}  (L33005, zc L19001)
       间隔 (ie||3)*1e3, 429退避≤15s, 15min deadline (L32998-L33003/L33067-L33069)
       completed → imgUrl=result.images[0].url[0] (L33031)
       ii(imgUrl,{subfolder:'tasks'}) 落盘 18080 (L33049)
  → 回填节点 data.imageUrl/videoUrl
  → dispatchEvent('mutiwindow-task-completed') (L38481) → Ev() rescan → 资源面板刷新
```

**7 陷阱覆盖**（对照 PRD_TASK_POLLING）：deadline(L32997)、AbortController(L32993)、429退避(L33067)、progress回写(L33024)、终态判定(L33028)、结果提取[0](L33031)、pending_confirmation(L33058)。

## ④ 存疑 Bug

| 优先级 | 问题 | grep 证据 | 现状 |
|--------|------|-----------|------|
| 已知限制 | `pending_confirmation` 卡轮询 | L33058-33063 `markNeedsConfirm`→`return`（AUTO_CONFIRM=false 时退出不重试） | 独立问题，未修（PROJECT_LOG 2026-07-20 13:07 遗留） |
| 红线§3.2 | CDN直链违规风险 | L33046-33051 已 `ii()` 持久化（合规）；但 catch 失败仍用原始URL(L33051) | 已大部分合规，失败兜底用原始URL属降级 |
| 已知噪音 | 网关未实现API 404 | 网关内存任务库重启即丢 | 无害（PROJECT_LOG 2026-07-20 11:50） |

## 边界契约

- **出（HTTP）**：`POST /v1/images/generations`(L32796) · `POST /v1/videos/generations` · `GET /v1/tasks/{id}`(L33005) · `POST /api/files/upload`(L33049经ii)
- **出（事件）**：`mutiwindow-task-completed`(L38481) · `mutiwindow-update-task-meta`(L41032)
- **config 开关**：`DEFAULT_ENDPOINT`(config L30=9004) → `R`；`LOCAL_ENGINE.port`(config L14) → 落盘 18080

## 对抗审计（门4）

**Q：生图派发真没有独立函数名？那审计怎么锚？** A：grep `32987` 上下文，是 `useCallback((...) => { ... if(N){...} })` 闭包，无 `function Xn`。审计须锚**行号 L32987** 而非混淆名。func-mapping 未收录，建议补「L32987 生图派发闭包」。✅

**Q：pending_confirmation 真会卡死吗？** A：L33058 `resolveNeedsConfirm(taskInfo)` 命中 → `markNeedsConfirm`(L33080) 置 `await_confirm` 并 `return` 退出轮询；若 `AUTO_CONFIRM=false` 且无手动确认 UI 触发，节点永久 `await_confirm`。PROJECT_LOG 已记该遗留。✅

**Q：ii 落盘失败会怎样？** A：L33051 `catch` 仅 warn，仍用原始 CDN URL 回填节点 → 刷新/离线即丢（违反红线§3.2 但属降级）。建议失败时应阻断而非降级。✅（存疑，记 TASKS 线索）
