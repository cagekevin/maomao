# 05 · 模块4 — 画布引擎与节点

> **四段式**：① 运转图景 ② 核心混淆字典（含同名遮蔽警告）③ 关键数据流 ④ 存疑 Bug
> **边界契约**：ReactFlow props 块(L37125-L37139) 接线；消费 `mutiwindow-task-completed`(生成完成回填)。注：画布持久化走 `Q.saveCanvasState`(L1642)/`saveCanvasStateWithVersion`(L1650/L31728) 直接写 localforage，**无 `canvas-state-change` 事件**（源码 grep 0 命中）。

---

## ① 运转图景

画布基于 ReactFlow（`@xyflow/react`）。主画布 `<ReactFlow>` props 在 **L37125-L37139** 接线：`onConnect:xn` / `onNodeClick:kn` / `onPaneContextMenu:Cn` / `onNodeContextMenu:wn` / `onDragOver:Ir` / `onDrop:Lr` / `onConnectEnd:Fr`。

节点类型（ARCH L1.2 spawnable 集合）：`promptNode/imageNode/imageBoxNode/videoNode/sd2VideoNode/.../textNode/audioNode/textConcatNode` 等。自定义节点组件如 `Th`(Director3DNode)/`Yc`(PanoNode L19516)/`Ns`(FrameExtractNode L16031)/`Rc`(TextConcatNode L18812)/`To`(GridMergeNode L7906)。

## ② 核心混淆字典（grep 坐实 + 同名遮蔽警告）

⚠️ **同名遮蔽警告**：反编译产生多处同名 `Lr`/`z`，不同作用域内语义不同，**必须带行号+作用域**，不能只说「改 Lr」。

| 混淆名 | 行号 | 真身 | 作用域 | 证据 |
|--------|------|------|--------|------|
| `xn` | `App.js:L31883` | `onConnect` | 模块级 | func-mapping L180 ✅（行号吻合） |
| `kn` | `App.js:L31980` | `onNodeClick` | 模块级 | func-mapping L185 行号漂移（记31981→实测31980） |
| `Cn` | `App.js:L31933` | `onPaneContextMenu` | 模块级 | func-mapping L181 ✅（行号吻合） |
| `wn` | `App.js:L31947` | `onNodeContextMenu` | 模块级 | func-mapping L182 ✅（行号吻合） |
| `Fr` | `App.js:L36245` | `onConnectEnd` | 模块级 | func-mapping L186 **行号漂移**（记36195→实测36245） |
| `Ir`(onDragOver) | `App.js:L36290` | `onDragOver`（拖拽悬停设 dropEffect） | 模块级 | func-mapping L187 **行号漂移**（记36240→实测36290） |
| `Lr`(onDrop) | `App.js:L36293` | `onDrop`（模板/任务/文件→建节点） | **组件内局部** | L36293 `Lr=Y.useCallback(e=>{`；L37138 `onDrop:Lr` |
| `Lr`(withThumbSuffix) | `App.js:L1700` | `withThumbSuffix`（URL缩略图后缀） | **模块级** | func-mapping L38；**与 onDrop 的 Lr 同名遮蔽，是两不同函数** |
| `Th` | `App.js`(Director3DNode) | 3D导演台节点 | 模块级 | func-mapping L187 ✅ |
| `Yc` | `App.js:L19620` | `PanoNode` | 模块级 | func-mapping L174 **行号漂移**（记19516→实测19620） |
| `Ns` | `App.js:L16111` | `FrameExtractNode` | 模块级 | func-mapping L147 **行号漂移**（记16031→实测16111） |
| `Rc` | `App.js:L18892` | `TextConcatNode` | 模块级 | func-mapping L162 **行号漂移**（记18812→实测18892） |
| `To` | `App.js:L7972` | `GridMergeNode` | 模块级 | func-mapping L165 **行号漂移**（记7906→实测7972） |
| `Pr`(gridSplit) | `App.js:L36215` | 创建 gridSplitNode 的 handler | 组件内局部 | L36215 `Pr=()=>{...gridSplitNode...}` |
| `z`(创建节点) | 多处局部 `z=Y.useCallback` | 创建节点（不同作用域不同实现） | 组件内局部 | L7224/L15287/L23255/L29159；func-mapping L113 `Z~L36215` **需纠正**：L36215 是 `Pr` 非 `Z` |

## ③ 关键数据流

- **拖放建节点**：`onDrop:Lr`(L36293) 处理 `application/x-yimao-template`(批量导入) / `x-mutiwindow-task`(建节点) / `dataTransfer.files`(→`ii({subfolder:'canvas/drop'})` L36285 落盘)。
- **连线**：`xn`(L31883) 同侧校验 + 左右反向纠正 + 多选批量连。
- **画布持久化**：`onChange` → `Q.saveCanvasState`(L1642)/`saveCanvasStateWithVersion`(L1650/L31728) 直接写 localforage(`Cr` L1294 拼 key)；**`canvas-state-change` 事件源码 0 命中，系误造**。
- **撤销/重做**：直接 `setNodes/setEdges`，不经 `onNodesChange`（PROJ_ORIGIN §4.5）。

## ④ 存疑 Bug

| 优先级 | 问题 | 说明 |
|--------|------|------|
| 文档错误 | func-mapping `Lr=onDrop`(L188) 与 `Lr=withThumbSuffix`(L38) 同名冲突 | 两处是不同函数（L36293 局部 vs L1700 模块级），改代码时若只搜 `Lr` 会误改。审计须带行号 L36293 vs L1700 |
| 文档错误 | func-mapping `Z~L36215 创建节点` | L36215 实际是 `Pr`(gridSplit handler)；`Z` 是局部 `z=Y.useCallback`（多作用域），无稳定语义 |
| **门3抓出** | **func-mapping.txt 行号整体漂移**（2026-07-19 快照 vs 2026-07-21 实测） | 以下行号已漂移，须以实测为准：`kn`(记31981→**31980**)、`Fr`(记36195→**36245**)、`Ir`(记36240→**36290**)、`Yc`(记19516→**19620**)、`Ns`(记16031→**16111**)、`Rc`(记18812→**18892**)、`To`(记7906→**7972**)；`xn/Cn/wn` 行号吻合 |
| 红线§3.1 | 新增节点/连线规则勿与混淆逻辑冲突 | 改画布交互必须定位到 L37125 接线块 + 对应 handler 行号 |

## 边界契约

- **入（事件）**：`mutiwindow-task-completed`(L38481) → 节点 data 更新；`import-project`/`export-project`(AppShell L44644/L44650)
- **出（事件）**：`mutiwindow-task-completed`(生成完成回填)。注：`canvas-state-change` 事件源码 grep 0 命中，系误造，已删。
- **出（HTTP）**：`ii({subfolder:'canvas/drop'})`(L36285) → `POST /api/files/upload`

## 对抗审计（门4）

**Q：onDrop 的 Lr 和缩略图的 Lr 是同一函数吗？** A：否。grep `function Lr` 仅 L1700(withThumbSuffix 模块级)；`onDrop:Lr` 是 L36293 组件内 `Y.useCallback`，作用域不同，反编译同名遮蔽。改 onDrop 必须锚 L36293，改缩略图锚 L1700。✅

**Q：Z 创建节点在哪？** A：无稳定 `Z`。func-mapping L113 记 `Z~L36215` 错（那是 `Pr`=gridSplit）。创建节点逻辑分散在多处局部 `z=Y.useCallback`（L7224 等），按 ReactFlow 上下文定位。✅（建议 func-mapping 删 `Z~L36215` 条）

[2026-07-21 据 AI13 裁决表修订：删两假事件；Hr 改判 localEngineBase() 动态]
