# AI04 · 猫猫画布架构审计（全套 6 步）

> **审计对象**：一毛 AI 画布魔改本地版（V1 引擎 `src/_engine/App.js` + `localTool/` + `apimart-gateway/`）。
> **审计方法**：源自 `docs/TASKS.md` 末尾《架构审计计划》的五关流水线（锚点门 / 强制引用 / 机器校验 / 对抗审计 / 入库门）+ 防污染规则。
> **铁律**：引用必须带 `file:line`；混淆名先过映射表再 grep 确认；不盲猜；不改任何代码；不碰 `dist/`/`vendor`/`App.original.js`；不引入 V2。
> **文件位置约束**：本目录 `docs/AI04/` 是本次审计唯一产出区，**所有文档与校验脚本只放这里**，不写入 `TASKS.md`/`ARCHITECTURE.md` 等其他文档（发现的问题仅记录于此）。

---

## 文档地图

| 文件 | 阶段 | 内容 |
|------|------|------|
| `README.md` | — | 本总览 |
| `00-审计计划与进度.md` | 总纲 | 五关流水线落地、进度看板、方法说明 |
| `01-阶段0映射表缺口.md` | 阶段0 | 坐实 `Ev/we/ii/R/B/wv/ri/Sv/Zr/zc` 等未收录/误记混淆名（含行号 + 真身） |
| `02-模块1系统初始化与配置层.md` | 阶段2.1 | `Oa/Jh/Ba/ar/tr/Nr/Pr/jr/Cr` + config.js 加载降级 |
| `03-模块2本地数据层与Rescan.md` | 阶段2.2 | `Ev/we/Sv/wv/ii/R/ri` + 扩展名→类型 + P1/P2 排雷 |
| `04-模块3AI生成与网关层.md` | 阶段2.3 | 真实生图派发函数 + L32987 轮询分支 + `zc` + `uploadFile` 落盘 + 事件总线 |
| `05-模块4画布引擎与节点.md` | 阶段2.4 | `Lr/ xn/ kn/ Fr/ Cn/ wn/ Th/ Yc/ Ns` + ReactFlow 魔改点 |
| `06-交叉流审计.md` | 阶段2.5 | 4 条端到端横向切片（沿边界契约接缝追踪） |
| `07-审计总报告.md` | 阶段3 | 合并总结 + 存疑 Bug 回填线索（仅记录） |
| `08-模块5同步与采集与存储键.md` | 阶段2.6 | 统一同步 effect(`Se`锁) + GAS `ei`/`ti` + `background.ts` 仅存 URL + 存储键 |
| `09-模块6网关侧路由与轮询陷阱对照.md` | 阶段2.7 | 网关路由表 + `_check_and_fire_task` 的 `done`→`completed` 归一 + `AUTO_CONFIRM` 默认 true 闭环 |
| `10-模块7导出恢复与同名遮蔽风险.md` | 阶段2.8 | 导出 `Ri`(L44505)/导入 `Bi`(L44544) 真身 + `Ri`/`Bi` 同名遮蔽新告警 |
| `11-模块8三维导演台与节点渲染.md` | 阶段2.9 | 3D 导演台 `Ap`/`Op`/`kp` 处理器链 + `Th`(L28388) 实为图片节点澄清 |
| `12-模块9画布节点类型全表.md` | 阶段2.10 | 节点注册表 `Tr`(L35671) 17 种节点 + 分类 `Er` + 渲染组件映射 |
| `13-模块10事件总线全量.md` | 阶段2.11 | 三类通信（CustomEvent/BroadcastChannel/localStorage剪贴板/iframe）+ 事件全量表 |
| `14-模块11资源新增事件跨边界追踪.md` | 阶段2.12 | `resourceAdded` 全仓仅 1 处接收、0 处发送 → 外部扩展注入（闭合 B15） |
| `15-模块12万能节点模板注入专项.md` | 阶段2.13 | `customNode` 模板 L1276/L43152/L31307 + `_r` 同名遮蔽（标记 B14/B18/B19，专项见 17） |
| `16-模块13GAS三向不一致深挖.md` | 阶段2.14 | 推送9键白名单 vs 拉取全量无校验 + modelSchedules 旁路（B20/B21/B22） |
| `17-模块14万能节点模板Schema专项.md` | 阶段2.15 | `Si`(L44302) 按id累加无name去重 + `_r`失败不触发节点失败态（B14/B18/B19 闭环） |
| `校验报告.md` | 门3产物 | `audit-check.cjs` 输出：当前 ✅113 ❌0（含 App.js/config.js/main.py 三方引用） |
| `audit-check.cjs` | 门3脚本 | 机器校验：抽 `App.js:Lnnnn`/`config.js:Lnnnn`/`main.py:Lnnnn` → 去源码 grep 确认存在 |

---

## 进度看板

| 阶段 | 状态 | 说明 |
|------|------|------|
| 阶段0 补映射表 | ✅ 完成 | 缺口已全部 grep 坐实（见 01） |
| 模块1 系统初始化 | ✅ 完成 | 见 02 |
| 模块2 本地数据层 | ✅ 完成 | 见 03 |
| 模块3 AI生成网关 | ✅ 完成 | 见 04 |
| 模块4 画布引擎 | ✅ 完成 | 见 05 |
| 阶段2.5 交叉流 | ✅ 完成 | 见 06 |
| 阶段3 总报告 | ✅ 完成 | 见 07 |
| 模块5 同步/采集/存储键 | ✅ 完成 | 见 08（GAS 三向不一致、本地化 `/files/` 判定） |
| 模块6 网关路由/轮询 | ✅ 完成 | 见 09（`done`→`completed` 闭环、`AUTO_CONFIRM` 默认 true） |
| 模块7 导出/恢复+遮蔽 | ✅ 完成 | 见 10（`Ri`/`Bi` 双重定义告警，B9/B10） |
| 模块8 3D导演台/节点 | ✅ 完成 | 见 11（`Ap`/`Op` 前置契约、`Th` 误解澄清，B11/B12） |
| 模块9 节点类型全表 | ✅ 完成 | 见 12（`Tr` 17 种 + 分类，B13/B14） |
| 模块10 事件总线全量 | ✅ 完成 | 见 13（三类机制 + 事件表，B15/B16/B17） |
| 模块11 资源新增追踪 | ✅ 完成 | 见 14（`resourceAdded` 外部注入，B15 闭环） |
| 模块12 万能节点模板 | ✅ 完成 | 见 15（模板注入 `_r` 遮蔽，标记 B14/B18/B19 待 17 专项） |
| 模块13 GAS三向不一致 | ✅ 完成 | 见 16（白名单vs全量+旁路，B20/B21/B22） |
| 模块14 模板Schema专项 | ✅ 完成 | 见 17（`Si` 按id累加、失败不触发节点态，B14/B18/B19 闭环） |
| 门3 机器校验 | ✅ 完成 | 脚本迭代支持 main.py + 模块7~14，已跑 ✅113 ❌0，见 `校验报告.md` |

> 行号快照日期 2026-07-21，会漂移，任何事实以 `grep` 当前源码为准。
