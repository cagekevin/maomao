# 07 · 审计总报告（阶段3 合并 + 存疑 Bug 回填线索）

> 合并 `01`–`17` 结论（阶段0 + 模块1~14 + 4 交叉流 + 派生专项）。所有发现**仅记录于此目录**，未改动 `TASKS.md`/`ARCHITECTURE.md`/`func-mapping.txt`（遵守文件位置约束）。
> 门3 机器校验：✅ 全绿（见 `校验报告.md`，当前 ✅113 ❌0，EXIT=0）。

---

## 一、审计结论摘要

猫猫画布 V1 本地模式由三大进程组成，代码事实经 grep 全部坐实：
- **前端**：`src/_engine/App.js`（46397 行），混淆代码，引用必带行号。
- **localTool**：`localTool/src/routes/*`（:18080, sql.js WASM）。
- **网关**：`apimart-gateway/main.py`（:9004, FastAPI, 内存任务库）。

审计坐实了 4 个基础模块 + 4 条交叉流 + 10 个派生专项（模块5~14），纠正了原文档 4 类锚点错误。

## 二、阶段0 映射表缺口（已坐实，建议回写 func-mapping.txt）

| 混淆名 | 真身 | 行号 |
|--------|------|------|
| `Zr` | uploadRemoteUrlToLocalTool（远程URL落盘） | L1827 |
| `ii` | 统一上传入口 | L1888 |
| `ri` | 缩略图内存缓存 | L1856 |
| `Sv` | saveResource | L42838 |
| `wv` | deleteResource（只删DB） | L42857 |
| `Ev` | rescanResources | L42883 |
| `zc` | 带localPort代理fetch封装（轮询用） | L19001 |
| `vv` | LOCAL_ENGINE.base（固定18080） | L42808 |
| `Hr` | localEngineBase()（随开关变） | L1732 |
| `we` | rescan闭包（Ev+xv刷新） | L43015 |

## 三、原文档锚点错误纠正（已在 AI04 纠正，原文档未动）

1. **`Zr` 不是上传函数**：原 `TASKS.md`/`ARCHITECTURE.md` 误用 `Zr()` 当上传；实为「远程URL→本地」下载器（L1827）。
2. **`Jn` 不是生图主回调**：`Jn`(L89)=LogoIcon（func-mapping 正确）；真实生图派发在 L32987 区域的 `useCallback` 闭包，无独立名。
3. **func-mapping.txt 行号整体漂移**（门3 抓出）：`kn`/`Fr`/`Ir`/`Yc`/`Ns`/`Rc`/`To` 行号需以实测为准（见 `05` ④）。
4. **同名遮蔽（不止 `Lr`/`z`）**：`Lr`(L1700 vs L36293)、`Z`(多处 `z=Y.useCallback`)、`Pr`(L1446 vs L36215)、`Ri`(L44505导出 vs L3269模型系列)、`Bi`(L44544导入 vs L3294折扣快照)、`_r`(L1103渲染 vs L35078执行)——共 6 组，见 `10`/`11`/`17` 文档。

## 四、存疑 Bug 回填线索（仅记录，待你决定是否回写 TASKS.md）

| # | 优先级 | 问题 | 证据(file:line) | 建议修复方向 |
|---|--------|------|----------------|--------------|
| B1 | P1 | 资源面板拖入URL不落盘 | L29182 只存state `O([{url:r}])` 无 `Zr()`/`Sv()` | 拖入URL时调 `Zr()` 下载或提示 |
| B2 | P1 | 文件上传不入库 | L29160 `R(e)` 后只 `O(...)` 无 `Sv()` | 上传成功调 `we()`/`Sv()` |
| B3 | P2 | 删除不删磁盘 | `wv`(L42857) 只 `POST /api/resources/delete`；`Tv`(L42866) `deleteFiles` 可选 | `wv()` 加 `deleteFiles` 参数联动 |
| B4 | P2 | rescan孤儿只清 local-tool | `resources.ts` L123 `WHERE source='local-tool'` | 评估是否扩展清理 scope |
| B5 | P2 | USE_LOCAL_ENGINE=false 上传破 | `Hr`(L1732) 变9004 但无 `/api/files/upload` | 统一 base URL 源（Hr/vv 合并） |
| B6 | 已知 | pending_confirmation 卡轮询 | L33058 `markNeedsConfirm`→`return`（AUTO_CONFIRM=false） | 增加手动确认 UI 或默认 AUTO_CONFIRM=true |
| B7 | 设计 | 缩略图伪复制 | `ri`(L1856)+`localTool/files.ts` copyFileSync | 接入 sharp 真实缩放 |
| B8 | 红线 | ii 落盘失败用原始URL降级 | L33051 catch 仅 warn 仍用 CDN URL | 失败应阻断而非降级（违红线§3.2） |
| B9 | 待评估 | 导入时 kvStore 恢复依赖连接态 | `Bi`(L44544) 需 `n.status.isConnected` | 断连时提示配置未恢复 |
| B10 | 待评估 | 导出排除 `img_` 致图片断链 | L44517 排除 `img_` 前缀 | 配合 `imageUrlRef` 回填 |
| B11 | 待评估 | `Ap` 强依赖 `Op` 注册（须从画布开导演台） | L26188 抛错提示 | 入口层加保底注册 |
| B12 | 已知限制 | 导出排除 `img_` → 3D 场景图断链 | 见 10/11 | 同 B10 |
| B13 | 待评估 | `Tr` 菜单注册表不含运行时 `imageNode`/`promptNode` | L35671 vs L10106 | 反推全节点会漏 |
| B14 | 待评估→已专项 | `customNode` 与 `customNodeTemplates` 关系 | L1276/L31307 | 专项见 17（schema 闭环） |
| B15 | 已闭环 | `resourceAdded` 发送源缺失（仅收无发） | L43527 仅接收 | 外部扩展注入，见 14 |
| B16 | 待评估 | `mutiwindow-task-completed` 5发2收，`nodeId` 缺失静默丢 | L38481/L43640/.../L31428 | 失败任务加兜底通知 |
| B17 | 已知限制 | BroadcastChannel 仅置脏标记非实时同步 | L31544 `Yt(true)` | 设计如此 |
| B18 | 已闭环 | `customNode` 重名模板**并存**非覆盖 | `Si`(L44302) 按 id 累加 | 修复：加 name 查重（见 17） |
| B19 | 已闭环 | `customNode` 失败不触发节点失败态 | `_r`(L35078) 仅 toast | 修复：补 `mutiwindow-task-completed`(L43697) |
| B20 | 建议 | 拉取无键白名单校验→云端残留污染本地 | `ti`(L43974) 全量回写 | 拉取加白名单 |
| B21 | 建议 | `modelSchedules` 走 la()/Sa() 旁路 kvStore | L43955/L43994 | 统一走 kvStore |
| B22 | 待评估 | 推送缺 `lastOpenedProject`/画布态 | L43954 白名单 | 确认是否故意设计 |

> 状态图例：B1–B8 基础模块线索；B9–B13/B16/B22 待评估；B14/B18/B19 已专项坐实；B15 已闭环；B20/B21 整改建议。编号跨文档全局唯一，详见各模块文档。

## 五、方法学收获（供后续审计参考）

1. **门3 校验脚本价值**：自动抓出 func-mapping.txt 行号漂移 + 锚点误用，比人工可靠。建议每次审计后跑 `node docs/AI04/audit-check.cjs`。
2. **混淆名必须带行号+作用域**：同名遮蔽（`Lr`/`z`）证实「只说改 ii()」是危险指令。
3. **派生文档不可信**：`TASKS.md`/`ARCHITECTURE.md` 的 `Jn`/`Zr` 锚点错误被本次审计纠正，证明计划 T0 阻塞项确实存在。
4. **不污染其他文档**：本次所有产出仅在 `docs/AI04/`，符合你的文件位置约束。

## 附录：门3 校验报告（摘要）

```
校验源码: App.js(46397行) / config.js(75行) / main.py(940行)
汇总: ✅ 113  ❌ 0  (EXIT=0)
所有 App.js:Lnnnn / config.js:Lnnnn / main.py:Lnnnn 引用均在源码命中预期片段
```

> 完整报告见同目录 `校验报告.md`（机器生成）。
