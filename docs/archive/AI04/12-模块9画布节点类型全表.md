# 12 · 模块9 画布节点类型全表（派生）

> 阶段2.10（派生）。节点菜单注册表在 `App.js:L35671–L35820`（`Tr` 数组），分类在 `Er`(L35821)。
> 引用带 `App.js:Lnnnn`。仅记录于此目录，未改动任何外部文档。

---

## 一、节点注册表（`Tr`，L35671 起）

| type | label | cat | 备注 |
|------|-------|-----|------|
| `textConcatNode` | 文本拼接 | text | L35671 |
| `audioNode` | 听音断句 | text | L35679 |
| `imageBoxNode` | 图片盒子 | image | L35687，默认 `data:{images:[],activeIndex:0,expanded:false}` |
| `gridSplitNode` | 图片切分 | image | L35700 |
| `gridMergeNode` | 图片拼图 | image | L35708 |
| `panoramaNode` | 全景图 | image | L35716 |
| `director3dNode` | 3D导演台 | image | L35724（见 11 模块链路） |
| `imageCompressNode` | 图片压缩 | image | L35732 |
| `faceMosaicNode` | 人脸打码 | image | L35740 |
| `compareNode` | 对比工具 | image | L35748 |
| `urlToImageNode` | 网址转图片 | image | L35756 |
| `videoNode` | 其他视频 | video | L35764，默认 `data:{prompt:''}` |
| `videoExtractNode` | 视频抽帧 | video | L35775 |
| `videoToGifNode` | 视频转GIF | video | L35783 |
| `customNode` | 万能节点 | other | L35791 |
| `stickyNoteNode` | 便签 | other | L35799，默认 `data:{text:'',fontSize:24,bgColor,textColor}` |
| `fileToUrlNode` | 文件转链接 | other | L35813 |

> 共 17 种。其中 `promptNode`(L31169)、`imageNode`(散布于 L10106/L11843/L15035/...)、`audioPlayerNode`(L15018)、`videoExtractNode`/`audioNode` 在多处作为运行时生成/默认节点出现，但**菜单注册表 `Tr` 仅含上表 17 种**。

## 二、分类（`Er`，L35821）
`text`(文本工具) / `image`(图片工具) / `video`(视频工具) / 及隐式 `other`（万能/便签/文件转链接未显式列于 `Er`，但 `cat:'other'` 已在 `Tr` 标注）。

## 三、节点渲染组件（摘录，混淆名→真身）
| 组件 | 行号 | 说明 |
|------|------|------|
| `Th` | L28388 | 图片类节点渲染器（`imageUrl` + `target` handle + 上游取图）— 见 11 |
| `Fr` | L36245 | 节点容器/Wrapper（ReactFlow nodeTypes 绑定点附近）— 见 05 |
| `xn` | L31883 | onConnect 连线回调 — 见 05 |
| `Cn` | L31933 | 节点添加/创建 — 见 05 |
| `wn` | L31947 | 节点删除 — 见 05 |
| `kn` | L31980 | 拖拽放置 onDrop — 见 05 |

> 节点类型 → 渲染组件并非 1:1 映射；多数节点共用 `Th`/`Fr` 等通用渲染器，差异由 `data` 驱动。

## 四、门3 待登记锚点
| 行号 | 预期片段 | 语义 |
|------|----------|------|
| L35671 | `type: \`textConcatNode\`` | 节点注册表起点 |
| L35724 | `type: \`director3dNode\`` | 3D导演台节点 |
| L35791 | `type: \`customNode\`` | 万能节点 |
| L35813 | `type: \`fileToUrlNode\`` | 文件转链接（注册表末项） |

## 五、存疑点（仅记录）
- **B13（待评估）**：`Tr` 注册表与散落的 `imageNode`/`promptNode`/`audioPlayerNode` 不完全一致——`imageNode` 在运行时大量生成（L10106 等）却不在 `Tr` 菜单中，说明存在"非菜单节点"（程序化创建）。若有人按 `Tr` 反推全节点集会漏掉这些。
- **B14（待评估）**：`customNode`(万能节点) 与 `customNodeTemplates`(L1276 存储键 / L31307 / L45280) 的关系未在此文档展开——模板如何注入万能节点待专项审计。
