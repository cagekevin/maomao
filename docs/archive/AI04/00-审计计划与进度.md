# 00 · 审计计划与进度（五关流水线落地）

> 把 `docs/TASKS.md` 末尾《架构审计计划》落到本次执行。本文件只描述「怎么做」，不重复模块内容。

## 一、五关流水线（本次执行方式）

1. **门1 锚点门（写前）**：每个混淆名必须先查 `docs/func-mapping.txt` / `docs/var-mapping.txt`；未收录的，先 grep 源码坐实函数体语义再写，禁止边写边猜。
   - 阶段0 已坐实 `Zr`/`ii`/`ri`/`Sv`/`wv`/`Ev`/`we`/`zc`/`vv`/`Hr` 等（见 `01-阶段0映射表缺口.md`）。
2. **门2 强制引用（写中）**：每条事实带 `App.js:Lnnnn` 或 `localTool/...:Lnnn` 或 `config.js:Lnn`，禁止无出处断言。
3. **门3 机器校验（写后·脚本）**：`audit-check.cjs` 正则抽文档内 `App.js:L\d+` / `函数名()`，去源码 grep 确认符号/行号存在 → 输出逐条 ✅/❌。
4. **门4 对抗审计（写后·逻辑质询）**：对存疑 Bug / 数据流做反向质询（"A 调 B，B 返回在哪被消费？grep 给我看"），由本文档作者切「审计模式」自问自答，记录于各模块「对抗审计」小节。
5. **门5 入库门**：门3 全绿 + 门4 无阻断项 → 模块标「已验证」。本次所有模块均过门3（报告见 `07` 附录），阶段0 缺口为强证据。

## 二、防污染规则（本次遵守）

- 模块草稿不写 `_DRAFT`（直接产出最终稿），但仍遵循「文档是派生，错了改文档别改代码」。
- 后续模块审计不把前序模块 doc 当真理，只当线索，最终回代码核对（见各模块「对抗审计」）。
- 映射表是唯一可信锚层；本次发现 `func-mapping.txt` 中 `Jn=LogoIcon` 正确但 `TASKS.md`/`ARCHITECTURE.md` 误用 `Jn` 当生图主回调，已在本目录纠正，**未改动原文档**（遵守文件位置约束）。

## 三、模块划分（沿用计划 T2.1–T2.4）

- 模块1：系统初始化与配置层
- 模块2：本地数据层与 Rescan
- 模块3：AI 生成与网关层
- 模块4：画布引擎与节点
- 阶段2.5：交叉流（横向切片 4 条）

## 四、边界契约（每个模块必含子节）

记录本模块**接收/发出**的接缝：HTTP 路由、`window` CustomEvent、`config` 开关。这是阶段2.5 的缝合点，均带 `file:line`。

## 五、已知文档锚点错误（已在 AI04 内纠正，原文档未动）

| 原文档表述 | 错误 | 真相（grep 坐实） |
|-----------|------|------------------|
| `TASKS.md` 行34 / `ARCHITECTURE.md` L2.5：`Zr()` 是上传函数 | ❌ | `Zr`(App.js L1827) = `uploadRemoteUrlToLocalTool`（下载远程URL落盘），非上传 |
| `TASKS.md`/`ARCHITECTURE.md`：`Jn` = 生图主回调 | ❌ | `Jn`(App.js L89) = `LogoIcon`（品牌SVG）；真实生图派发在 L32987 区域 `useCallback` 内（非独立 function） |
| `ARCHITECTURE.md` 称 `Xr = openInTab` 在 L43479 | ⚠️ 行号漂移 | `Xr` 真身 L1802（uploadFile 内部 openInTab 重载 L43881）；`func-mapping.txt` L173 语义正确 |
| `func-mapping.txt` 未收录 `Zr`/`ii`/`ri`/`Sv`/`wv`/`Ev`/`zc`/`vv`/`Hr`/`we` | ⚠️ 缺口 | 见 `01` 补全 |
