# 修订任务书 · AI11（回写裁决结论）

> 你是 AI11。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI11/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的 M6/M7 草稿自认 `toAbsoluteFileUrl` 锚点误标，现据源码实锤回写修订。

## 必须修订的错点

### 修订：`toAbsoluteFileUrl` 锚点错（App.js → localTool）
- 位置：07-文档纠错建议.md（§二 P0、Sv() 保存前 toAbsoluteFileUrl 行）、任何称 "App.js L43462/L43548" 为 toAbsoluteFileUrl 补全点之处。
- 旧内容：把 "toAbsoluteFileUrl 补全" 锚到 App.js L43548（实为 `${Hr}${url}` 拼接，非函数）；标注 L43462 行号漂移。
- 改为：`toAbsoluteFileUrl` 真身 = `localTool/src/routes/resources.ts#L31`（定义处，硬编码 18080）；App.js(src/) 内 0 命中，L43548 仅路径拼接非函数定义。
- 依据：源码 grep `toAbsoluteFileUrl` 仅 `localTool/src/routes/resources.ts#L31` 定义；App.js 内 0 命中（你草稿 07-7/09-1/11-2 已实锤）。

## 执行要求
- 只改你自己的 `docs/AI11/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档加一行：`[2026-07-21 据 AI13 裁决表修订：toAbsoluteFileUrl 真身 localTool resources.ts L31，删 App.js L43462/L43548 旧锚]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI11.md`，列出改了哪些文件哪几处。
