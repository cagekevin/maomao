# 修订任务书 · AI08（回写裁决结论）

> 你是 AI08。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI08/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的 M4 草稿自认误标 `Th` 失效，现据源码实锤回写修订。

## 必须修订的错点

### 修订：`Th` 误标"失效" → Director3DNode
- 位置：13_模块4 §② / §④（凡称 "Th grep `function Th`/`Th =` 无结果→待废弃" 之处）。
- 旧内容：`Th` 失效/待废弃，未收录为节点。
- 改为：`Th = Director3DNode`，注册点 `App.js:L31141 director3dNode: Th`（nodeTypes 注册表）；`function $d`@L24391 = DirectorShell（布局壳），与 `Th`(节点壳@L28388) 同属 3D 系统两层封装，均真实。
- 依据：源码 L31141 注册、L28449 `[Director3DNode]` 注释、L24391 `$d`(DirectorShell)（你草稿 05-8 已实锤）。
- 说明：你 grep 用 `Th =`/`function Th` 漏检了对象属性值用法（nodeTypes 里是 `director3dNode: Th`），故误判。

## 执行要求
- 只改你自己的 `docs/AI08/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档加一行：`[2026-07-21 据 AI13 裁决表修订：Th 实为 Director3DNode(L31141 注册)，非失效]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI08.md`，列出改了哪些文件哪几处。
