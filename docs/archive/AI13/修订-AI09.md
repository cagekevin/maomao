# 修订任务书 · AI09（回写裁决结论）

> 你是 AI09。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI09/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的 M6/M7 草稿自认 `Zr` 错锚，现据源码实锤回写修订。

## 必须修订的错点

### 修订：`Zr` 双绑定错锚（logout 行号 + 漏收下载绑定）
- 位置：03-文档纠错清单.md（第二节）、10-映射表缺口分析.md（Zr 相关）。
- 旧内容：`Zr`=logout(L1827)（行号错），且未收"Zr@L1827=下载函数"双绑定。
- 改为：
  - `Zr`=logout 真身 `App.js:L43893`（var-mapping L48 已正确）；
  - `Zr`@L1827 = downloadAndSave 下载函数（由 resourceAdded@L43539 调 `await Zr(i.url,...)` 落盘），与 L43893 logout 构成**双绑定**，var-mapping 漏收 L1827。
- 依据：logout 真身 L43893；L1827 `async function Zr(e,t={})` 为下载函数（你草稿 07-3/08-5 已实锤）。

## 执行要求
- 只改你自己的 `docs/AI09/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档加一行：`[2026-07-21 据 AI13 裁决表修订：Zr=logout 改 L43893，补 L1827 下载双绑定]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI09.md`，列出改了哪些文件哪几处。
