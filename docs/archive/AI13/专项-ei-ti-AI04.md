# 专项任务书 · AI04（补录 ei/ti 四义）

> 你是 AI04。本文件是你的唯一任务指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI04/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成（裁决见 `docs/AI13/裁决表.md`），AI05 断言清单将 `ei/ti` 四义漏收列为「最严重缺陷」（10-1 / 07-4）。你的 M6/M7 草稿自认仅记录了 ei/ti 的 GAS 两义，漏收前端两义。

## 事实锚点（已多 AI 回源码实锤，直接采用）
`ei` / `ti` 为同名遮蔽重灾区，App.js 内至少五处绑定：
- `ei`@`App.js:L1853` `var ei = new Map()` —— 缩略图缓存 Map
- `ei`@`App.js:L36784` `ei = Y.useCallback(...)` —— 节点打组
- `ei`@`App.js:L43950` `ei = async () =>` —— GAS pushToCloud
- `ti`@`App.js:L36822` `ti = Y.useCallback(...)` —— 清缓存
- `ti`@`App.js:L43974` `ti = async () =>` —— GAS pullFromCloud
（var-mapping 通常只收后两义 GAS 绑定，漏收前三个前端绑定 → 这就是「最严重缺陷」）

## 你要做的修订
1. 打开 `docs/AI04/01-阶段0映射表缺口.md`：在映射表缺口处补录 `ei`/`ti` 四义（五处绑定）说明，标注「var-mapping 只收 GAS 两义，漏收 L1853/L36784/L36822 三处前端绑定」。
2. 打开 `docs/AI04/08-模块5同步与采集与存储键.md` 与 `docs/AI04/16-模块13GAS三向不一致深挖.md`：凡仅列 ei/ti 为 GAS push/pull 之处，补注「另有前端三义（L1853 Map / L36784 打组 / L36822 清缓存），同名遮蔽，引用必带行号」。
3. 文末追加标记行：`[2026-07-21 据 AI13 裁决表专项：补录 ei/ti 四义（L1853/L36784/L36822/L43950/L43974）]`。

## 铁律
- 只改你自己的 `docs/AI04/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 不杜撰行号；上面五行均来自多 AI 已 grep 实锤，直接引用即可。
- 完成后在 `docs/AI13/草稿/` 新建 `专项回执-ei-ti-AI04.md`，列出改了哪些文件哪几处。
