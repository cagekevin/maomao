# 修订任务书 · AI04（回写裁决结论）

> 你是 AI04。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI04/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的草稿中已自认误造假事件 + Hr 锚点需修正，现据源码实锤结论回写修订。

## 必须修订的错点

### 修订 1：删除 `canvas-state-change` / `mutiwindow-sync-local` 两假事件
- 位置：06 X4 表、05 边界契约、其他事件相关处。
- 旧内容：把两事件列为真实 CustomEvent（canvas-state-change: ReactFlow onChange→localforage；mutiwindow-sync-local: L44435）。
- 改为：删除两行；补 `mutiwindow-task-completed`(L38481) / `mutiwindow-update-task-meta`(L41032/L43764)；注明两假事件源码 0 命中。
- 依据：源码 grep 两事件名均 0 命中（你草稿 05-7/06-2/06-5 已实锤）。

### 修订 2：`Hr` 改判为动态（非硬编码）
- 位置：06 X3 及任何称 "Hr 硬编码 18080" 之处。
- 旧内容：`Hr/vv/U_ 三处硬编码 18080`。
- 改为：`Hr = localEngineBase()(App.js L1732)，随 USE_LOCAL_ENGINE 开关动态取 18080/9004；vv(L42808) 才是字面值写死 18080`。
- 依据：App.js L1732 `var Hr = localEngineBase()`（你草稿 06-4 已实锤）。

## 执行要求
- 只改你自己的 `docs/AI04/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档加一行：`[2026-07-21 据 AI13 裁决表修订：删两假事件；Hr 改判 localEngineBase() 动态]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI04.md`，列出改了哪些文件哪几处。
