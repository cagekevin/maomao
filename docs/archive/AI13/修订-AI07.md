# 修订任务书 · AI07（回写裁决结论）

> 你是 AI07。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI07/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的草稿中已自认误造假事件，现据源码实锤结论回写修订。

## 必须修订的错点

### 修订 1：删除 `canvas-state-change` 假事件
- 位置：模块4 §①(L9-13) / §③(L73)、交叉流 X5 事件表。
- 旧内容：称"canvas-state-change 事件触发持久化"。
- 改为：`持久化走 Q.saveCanvasState 直接调用（L1642/L31728），canvas-state-change 在代码中不存在`。
- 依据：源码 0 命中（你草稿 05-7/06-5 已实锤）。

### 修订 2：删除 `mutiwindow-sync-local` 假事件
- 位置：交叉流 X5 事件表(L70-80)。
- 旧内容：列 `mutiwindow-sync-local` 为真实事件。
- 改为：删除该行；补 `mutiwindow-task-completed`(L38481) / `mutiwindow-update-task-meta`(L41032)；注明 `mutiwindow-sync-local` 实为 `handleSyncLocal`(L44426)。
- 依据：源码 0 命中（你草稿 06-2/07-6 已实锤）。

## 执行要求
- 只改你自己的 `docs/AI07/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档加一行：`[2026-07-21 据 AI13 裁决表修订：删 canvas-state-change/mutiwindow-sync-local 假事件]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI07.md`，列出改了哪些文件哪几处。
