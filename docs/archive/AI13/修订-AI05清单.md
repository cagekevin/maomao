# 修订任务书 · AI05断言清单（基准微调）

> 你是负责维护 `docs/AI13/AI05断言清单.md` 的角色。本文件是你的唯一修订指令，读完即执行。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。各 AI 独立 ↔ 源码双重印证 AI05 基准方向正确，但 3 处措辞/锚点需微调以保持严谨（非事实错误，是表述精度）。

## 必须微调的 3 处

### 微调 1：`Hr` 硬编码 → 动态
- 位置：01-10、02-9、06-4。
- 旧措辞："Hr/vv/U_ 三处硬编码 18080"、"host 硬编码 18080：Hr/vv/U_ 三处"。
- 改为："Hr = localEngineBase()(App.js L1732) 动态随开关取 18080/9004；vv(L42808) 字面值写死 18080；U_ 同 vv。false 走 9004 无 /api/files/upload 路由"。

### 微调 2：`Xr` 语义明确
- 位置：M1 关注项 / 已知冲突速查表（Xr 行）。
- 旧："Xr 上传 vs openInTab｜AI02/03/07 vs AI08/10/12"。
- 改为："Xr = uploadToLocalTool（App.js L1802 → POST /api/files/upload），非 openInTab（func-mapping L173 错锚）。裁决：上传函数成立"。

### 微调 3：`we` 同名遮蔽说明
- 位置：01-2、M1 关注项（we 行）。
- 旧："we=rescanResources @L43015（漏收）"、"we rescan vs insertMention｜AI05 vs ?"。
- 改为："we 同名遮蔽：组件内 `we=(t,n=false)=>{}`@L4176 为 insertMention；rescan 真身为 `Ev`@L42883 / `we`@L43015（资源 rescan 触发）。两者不同绑定，AI05 01-2 指 rescan 真身 L43015 正确；旧文档把 we 当 rescan 主函数属误称"。

## 执行要求
- 只改 `docs/AI13/AI05断言清单.md`，不碰他人文档、不改源码、不改 `裁决表.md`。
- 改完在文件顶部或修订记录加一行：`[2026-07-21 据 AI13 裁决表微调：Hr 动态性 / Xr 语义 / we 同名遮蔽]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI05清单.md`，列出改了哪几处。
