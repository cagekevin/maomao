# 修订任务书 · AI02（残留清理）

> 你是 AI02。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI02/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的 M5 核对草稿(06-2) 指出 20 C4 事件表仍残留 `mutiwindow-sync-local` 旧行（其余假事件已删），现清理之。

## 必须修订的错点

### 修订：清理 `mutiwindow-sync-local` 残留行
- 位置：20 交叉流审计 C4 事件表（第 3 行附近，标注 "mutiwindow-sync-local 资源面板按钮→we(L43015)"）。
- 旧内容：C4 表仍列 `mutiwindow-sync-local` 为真实事件。
- 改为：删除该行。注明 `mutiwindow-sync-local` 源码 0 命中（已由 AI06/09/10/12 实锤），实为 `handleSyncLocal` 函数(L44426)，非事件总线；资源面板同步走 `rescanSync`(we) 由 `mutiwindow-task-completed` 触发。
- 依据：你草稿 06-2 待实锤队列已记"回 App.js grep 若 0 命中则删"——现确认 0 命中，执行删除。
- 注意：canvas-state-change 你已删，勿重复。

## 执行要求
- 只改你自己的 `docs/AI02/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档加一行：`[2026-07-21 据 AI13 裁决表修订：C4 表删 mutiwindow-sync-local 残留行]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI02.md`，列出改了哪些文件哪几处。
