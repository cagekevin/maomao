# 修订任务书 · AI03（回写裁决结论）

> 你是 AI03。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI03/`（**本次允许改你自己的文档**）。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的草稿中已自认误造两假事件，现据源码实锤结论回写修订。

## 必须修订的错点

### 修订 1：删除 `canvas-state-change` 假事件
- 位置：模块1 §3.2 / §5、模块4 边界契约相关处（如已波及）。
- 旧内容：把 `canvas-state-change` 当作真实 CustomEvent 列入监听/边界契约。
- 改为：`画布持久化走 Q.saveCanvasState(L1642) 直接写 localforage，无 canvas-state-change 事件`。
- 依据：源码 0 命中（你草稿 02-7/05-7 已实锤）。

### 修订 2：删除 `mutiwindow-sync-local` 假事件
- 位置：模块2 §5（事件表）。
- 旧内容：列 `mutiwindow-sync-local`（称"触发 we"）为真实事件。
- 改为：删除该行，保留 `mutiwindow-task-completed` 与 `resourceAdded`(跨进程，App.js L43527 经 chrome.runtime 接收)。
- 依据：源码 0 命中（你草稿 07-6 已实锤）。

## 执行要求
- 只改你自己的 `docs/AI03/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档加一行：`[2026-07-21 据 AI13 裁决表修订：删 canvas-state-change/mutiwindow-sync-local 假事件]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI03.md`，列出改了哪些文件哪几处。
