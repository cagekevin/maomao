# 修订回执 · AI09（据 AI13 裁决表回写）

> 修订指令：`docs/AI13/修订-AI09.md`
> 裁决依据：`docs/AI13/裁决表.md` 条目 07-3 / 08-5（✅ 双绑定成立，AI09 旧文档错锚）
> 修订日期：2026-07-21

## 修订清单

### 1. `docs/AI09/03-文档纠错清单.md` — 第二节 TASKS.md 错锚表
- 位置：原「建议清单行34」行（旧述 `Zr=logout(L1827)，非下载函数（AI05 已纠正）`）。
- 改为：
  - 标注 `Zr` 为**双绑定**：①logout 真身 `App.js:L43893`（`Q.remove(Z.AUTH_TOKEN)`）；②下载函数 `App.js:L1827 async function Zr(e,t={})`，由 `resourceAdded@L43539` 调 `await Zr(i.url,...)` 落盘。
  - 建议：保留真实下载/落盘函数名 `Zr` 并区分两绑定；旧「Zr=logout(L1827)」行号错锚，logout 真身在 L43893。
- 文末追加修订标记行。

### 2. `docs/AI09/10-映射表缺口分析.md` — 第一节符号×映射表收录状态表
- 位置：原 `Zr` 行（旧述 `logout | — | — | ✅ L48 | 已收录（非下载函数，TASKS 误用）`）。
- 改为：
  - 语义列改为**双绑定**（logout L43893 + 下载函数 L1827）。
  - 状态列改为「已收录但**漏收 L1827 下载双绑定**（var-mapping 缺口，AI05 断言正确；旧行号错锚，logout 真身 L43893）」。
- 文末追加修订标记行。

## 未改动项（按要求）
- 未触碰他人文档（`docs/AI01~AI12` 等）。
- 未改源码 `App.js`。
- 未改 `docs/AI13/裁决表.md` 与 `docs/AI13/AI05断言清单.md`。

## 两文档新增标记
- `docs/AI09/03-文档纠错清单.md`：末尾 `[2026-07-21 据 AI13 裁决表修订：Zr=logout 改 L43893，补 L1827 下载双绑定]`
- `docs/AI09/10-映射表缺口分析.md`：末尾 `[2026-07-21 据 AI13 裁决表修订：Zr=logout 改 L43893，补 L1827 下载双绑定]`
