# 修订回执 · AI05断言清单

> 依据 `docs/AI13/修订-AI05清单.md` 执行，仅改 `docs/AI13/AI05断言清单.md`，未碰他人文档、未改源码、未改 `裁决表.md`。
> 顶部已加修订记录行：`[2026-07-21 据 AI13 裁决表微调：Hr 动态性 / Xr 语义 / we 同名遮蔽]`

## 本次改动清单（3 处微调）

### 微调 1 · Hr 硬编码 → 动态（3 处）
- **01-10**：旧"Hr/vv 值恒等18080" → 改为"Hr = localEngineBase()(App.js L1732) 动态随开关取 18080/9004；vv(L42808) 字面值写死 18080；U_ 同 vv。false 走 9004 无 /api/files/upload 路由"。
- **02-9**：旧"host硬编码18080：Hr/vv/U_三处；false走9004无上传路由" → 同上动态表述。
- **06-4**：旧"X4 Hr/vv/U_硬编码18080恒等；false走9004无路由" → 同上动态表述。

### 微调 2 · Xr 语义明确
- **已知冲突速查表（Xr 行）**：旧"Xr 上传 vs openInTab｜AI02/03/07 vs AI08/10/12" → 改为"Xr = uploadToLocalTool（App.js L1802 → POST /api/files/upload），非 openInTab（func-mapping L173 错锚）。裁决：上传函数成立"。

### 微调 3 · we 同名遮蔽说明（2 处）
- **01-2**：旧"we=rescanResources @L43015（漏收）" → 改为"we 同名遮蔽：组件内 `we=(t,n=false)=>{}`@L4176 为 insertMention；rescan 真身为 `Ev`@L42883 / `we`@L43015（资源 rescan 触发）。两者不同绑定，AI05 01-2 指 rescan 真身 L43015 正确；旧文档把 we 当 rescan 主函数属误称"。
- **已知冲突速查表（we 行）**：旧"we rescan vs insertMention｜AI05 vs ?" → 改为"we 同名遮蔽：组件内 `we`@L4176=insertMention，rescan 真身 `we`@L43015（AI05 01-2 指 L43015 正确）"。

## 源码锚点实锤（本次核实）
- `src/_engine/App.js` L1732：`var Hr = localEngineBase()` ✅
- `src/_engine/App.js` L42808：`var vv = LOCAL_ENGINE.base` ✅
- `src/_engine/App.js` L1802：`async function Xr(e, t = {})`（上传函数）✅
- `docs/func-mapping.txt` L173：`Xr = openInTab`（错锚，已证伪）✅
- `src/_engine/App.js` L4176：`we = (t, n = false) => {}`（insertMention）✅
- `src/_engine/App.js` L43015：`we = Y.useCallback(async () => {...})`（资源 rescan）✅
- `src/_engine/App.js` L42883：`Ev`（rescan 触发器）✅
