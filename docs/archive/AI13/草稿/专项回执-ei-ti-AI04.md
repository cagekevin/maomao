# 专项回执 · ei/ti 四义补录（AI04）

> 依据 `docs/AI13/专项-ei-ti-AI04.md` 执行，仅改 `docs/AI04/` 自身文档，未碰他人文档、未改源码、未改 `裁决表.md` / `AI05断言清单.md`。

## 改动清单

### 1. `docs/AI04/01-阶段0映射表缺口.md`
- 新增 §1.5「`ei` / `ti` 同名遮蔽四义补录」，列出五处绑定（L1853 缩略图缓存 Map / L36784 节点打组 / L43950 GAS push / L36822 清缓存 / L43974 GAS pull）。
- 明确标注：`var-mapping` 旧稿仅收后两义 GAS（L43950/L43974），漏收前三处前端绑定（L1853/L36784/L36822）。
- 附四义对照表（含「是否入 var-mapping」列）。
- 文末追加标记行：`[2026-07-21 据 AI13 裁决表专项：补录 ei/ti 四义（L1853/L36784/L36822/L43950/L43974）]`。

### 2. `docs/AI04/08-模块5同步与采集与存储键.md`
- 在 ② 核心混淆字典 `ei`(GAS push)/`ti`(GAS pull) 两行下方补注「同名遮蔽补注」，列出前端三义（L1853 Map / L36784 打组 / L36822 清缓存），并注明旧稿漏收、引用必带行号。
- 文末追加标记行（接在既有修订标记之后）。

### 3. `docs/AI04/16-模块13GAS三向不一致深挖.md`
- 在「一、同步入口」`ei`(L43950)/`ti`(L43974) 两行下方补注「同名遮蔽补注」，列出前端三义（L1853 Map / L36784 打组 / L36822 清缓存），并注明旧稿漏收、引用必带行号。
- 文末追加标记行。

## 铁律自检
- ✅ 仅改 `docs/AI04/` 文档。
- ✅ 未改源码、未改 `裁决表.md` / `AI05断言清单.md`。
- ✅ 五行绑定均直接引用任务书已 grep 实锤行号，未杜撰。
- ✅ 文末标记行格式与任务书要求一致。
