# 核对草稿 - AI04 - 模块7(M7 深一度盲区)

> 基准：docs/AI13/AI05断言清单.md 的【08~18】相关条目（重点 10-1/10-2/10-4、15-2、08-3/17-2、13-4、15-5）
> 纪律：只写本文件；禁止改 裁决表.md 与他人草稿；❌必须附 file:line 证据
> 交叉核对对象：AI04 08/13/16 / AI09 14 / AI10 16 / AI11 08
> 我的基准文档：docs/AI04/08-模块5同步与采集与存储键.md、13-模块10事件总线全量.md、16-模块13GAS三向不一致深挖.md

| 条目ID | 我的判定(✅/❌/➖) | 我的依据(本AI文档引用或源码file:line) | 备注 |
|--------|------------------|--------------------------------------|------|
| 10-1   | ➖ | AI05称 `ei/ti` 四义 L1853(Map)/L36784(打组)/L36822(清缓存)/L43950(push)/L43974(pull)，只收后2。实锤：`App.js:L1853 var ei=new Map()`、`L36784 ei=Y.useCallback`(打组)、`L43950 ei=async`(push)、`L36822 ti=Y.useCallback`(清缓存)、`L43974 ti=async`(pull) → 四义全存在。我08/16仅记 ei(push L43950)+缩略图Map(L1853)、ti(pull L43974)，**漏收** L36784/L36822 两义。我未否认AI05，仅缺失。 | ➖我文档缺陷；ei/ti 四义实锤存在 |
| 10-2   | ✅ | 我08 3.1：`background.ts:L65 handleSaveToTransit` 只存元数据(L81-89)，L80注释「不再经过本地引擎上传」，落盘靠前端 → 与AI05「background.ts L80不再下载」完全一致。 | 强一致 |
| 10-3   | ➖ | AI05称前端 `resourceAdded@L43539` 才调 `Zr` 下载，失败整条丢弃。我13 B15 记 resourceAdded 为跨进程事件(L43527收)，未记 Zr 下载失败丢弃细节。 | ➖缺失 |
| 10-4   | ✅ | 我08 3.3 / 16二：`ei`(push L43950) 收集9类配置，`ti`(pull L43974) 后 `window.location.reload()`(L43997,延迟1s)，与AI05一致。 | 一致 |
| 08-3   | ✅ | 我13 B15：`resourceAdded` 仅在 `App.js:L43527` 接收，发送源不在 App.js（跨进程/外部扩展注入），与AI05「resourceAdded是跨进程消息非CustomEvent」一致。 | 一致 |
| 17-2   | ✅ | 同08-3，`resourceAdded` 跨进程 L43527，与AI05一致。 | 一致 |
| 13-4   | ✅ | 我13一.1：纯前端 CustomEvent 经 window，与AI05「纯前端CustomEvent非跨进程」一致（区别于 resourceAdded 跨进程）。 | 一致 |
| 15-2   | ➖ | AI05称 GAS 权限硬约束响应含 `<html` 抛错 L43914。实锤 `App.js:L43915 throw new Error("权限拦截，请确保 GAS 设为了【所有人】访问")`。我08/16未记录此硬约束 → 缺失（同07-11）。 | 实锤存在L43915；我➖缺失 |
| 15-5   | ✅ | 我13二表：`mutiwindow-task-completed`(L38481等5发) 与 `mutiwindow-update-task-meta`(L41032发/L43764收) 双轨并行，与AI05一致。 | 一致 |

## 待实锤队列（所有标❌的条目）
| 条目ID | 我认为冲突的点 | 我附的源码证据(file:line) | 建议实锤方向 |
|--------|--------------|--------------------------|------------|
| （本模块无❌；均为✅或➖缺失） | — | — | ei/ti 四义(10-1)与 GAS权限(15-2)为➖缺失，建议回写我08/16文档补齐 L36784/L36822 绑定与 L43915 硬约束 |

## 补充：ei/ti 四义实锤明细（供裁决参考）
- `ei`: `App.js:L1853` `var ei = new Map()`(缩略图缓存) · `L36784` `ei = Y.useCallback`(节点打组) · `L43950` `ei = async () =>`(GAS push)
- `ti`: `App.js:L36822` `ti = Y.useCallback`(清缓存) · `L43974` `ti = async () =>`(GAS pull)
- 我文档现状：仅记 L1853(Map)+L43950(push) 与 L43974(pull)，漏 L36784/L36822。
