# 核对草稿 - AI11 - 模块M6（文档纠错）
> 基准：docs/AI13/AI05断言清单.md 的【07 · 文档纠错】各条
> 纪律：只写本文件；禁止改 裁决表.md 与他人草稿；❌必须附 file:line 证据
> 交叉核对对象（去这些 AI 的同主题文件比对自己说法）：AI02 02/AI03清单/AI04 07/AI06§4/AI07清单/AI08 99/AI09 03/AI10 99/AI12 12（我方：docs/AI11/07-文档纠错建议.md）

| 条目ID | 我的判定(✅/❌/➖) | 我的依据(本AI文档引用或源码file:line) | 备注 |
|--------|------------------|--------------------------------------|------|
| 07-1   | ✅ | 07 §二 TASKS 错误锚点表（Zr/P0/P1-2/P1-3/Sv 行号）已列"审计计划章节过时"迹象；我方明确"不修改权威文件，仅列错锚点供维护者回填" | 与 AI05 一致（TASKS 审计计划过时须重写） |
| 07-2   | ✅ | 07 §三 补录建议表：`Tv`@L42866/`Cv`@L42851/`xv`@L42821 补 func-mapping；Ev/we/Sv/wv/ii/ri 在 03 §二已全列 | 与 AI05 一致（func-mapping 漏收 6 函数） |
| 07-3   | ✅ | 07 §一/§四 同名遮蔽总表：`Zr`@L1827=uploadRemoteUrlToLocalTool + `Zr`@L43893=logout 双绑定，已建议"拆两个 Zr 并标作用域" | 与 AI05 一致（Zr 双绑定补录 downloadAndSave@L1827） |
| 07-4   | ✅ | 08 §四 `ei`(局部)L36784=pushToCloud + `ti`(局部)L36822=pullFromCloud + `ei`(CloudSyncEngine)L43950 + `ti`(CloudSyncEngine)L43974，共四义；07 §三建议 var-mapping 标注"多义引用必带行号" | 与 AI05 "ei/ti 四义漏收，最严重缺陷"完全对齐 ✅ |
| 07-5   | ✅ | 07 §一 ARCHITECTURE L1.2/L2.5 采集链路错锚已订正（入口1 实为 Zr 非 ii）；08 §一 bootstrap 流说明 background 不下载仅存 source:'extension'（08 未单列 background，但 03/13 资源采集走 Zr 落盘，符合"background 不下载"） | 与 AI05 一致 |
| 07-6   | ✅ | 08 全量事件清单（12.1）已证 `mutiwindow-task-completed`/`mutiwindow-update-task-meta` 存在，`canvas-state-change`/`mutiwindow-sync-local` 不存在（05-7 已实锤 0 命中）；07 未直接列 ARCH X2 事件表，但 12.1 事件清单与 AI05 07-6 方向一致 | 与 AI05 一致（删 2 假事件 + resourceAdded 改跨进程） |
| 07-7   | ❌ | AI05 断言"toAbsoluteFileUrl 真身在 localTool L31，非 App.js L43462"。我方 07 §二记"绝对路径补全在 resourceAdded@L43548，改 L43462 行号漂移"——**未识别 localTool L31 真身，且把 App.js 内 L43548 当补全点（实际 L43548 只是 `${Hr}${url}` 拼接，非 toAbsoluteFileUrl 函数）**。本次 grep 实锤：`toAbsoluteFileUrl` 仅在 localTool/src/routes/resources.ts#L31 定义；App.js 内 0 命中 | 见待实锤队列 |
| 07-8   | ✅ | 09 §二 网关路由 `main.py` L655/L659/L663 返回 501（music/audio/speech）；README 与 main.py 矛盾（09 未直列 README 行，但 12/网关实证同结论） | 与 AI05 一致（网关 README 矛盾） |
| 07-9   | ✅ | 09 §二/§四 网关 chat L434-574 同步轮询+SSE（abort→400 L502/L521、超时→504 L541、heartbeat L549）；三处核心修复已在 09/13 坐实 | 与 AI05 一致（未见于权威文档） |
| 07-10  | ✅ | 09 §一 `tasks clear` L176→handleTasksClear@index.ts L176；localTool tasks.ts clear 无删盘路径（13.5 记"tasks clear 无删盘"）；03 §六 同 | 与 AI05 一致（残留债务） |
| 07-11  | ✅ | 15 未单列 GAS 权限，但 08 §四 GAS URL 来自 config.GAS_CLOUD_SYNC_URL；AI05 锚 App.js L43914（GAS 权限硬约束响应含 <html 抛错）——我方未单独立项，但不冲突 | 与 AI05 一致（GAS 须全员访问硬约束） |
| 07-12  | ✅ | 18 未单列，但 10.2 撤销重做 `r`(store) 接管；AI05 锚 ARCH §4.5 store.r 接管——我方 10 持久化层与 AI05 同立场 | 与 AI05 一致 |
| 07-13  | ✅ | 03 §六/§七、09 §四 均确认 Bug 表 P0-P2 准确仅行号漂移，已逐条给正确行号 | 与 AI05 一致（仅行号漂移） |

## 待实锤队列（所有标❌的条目）
| 条目ID | 我认为冲突的点 | 我附的源码证据(file:line) | 建议实锤方向 |
|--------|--------------|--------------------------|------------|
| 07-7   | 我方 07 文档误把 "toAbsoluteFileUrl 补全" 锚到 App.js L43548（实为该处 `${Hr}${url}` 拼接，非函数定义）；未识别真身在 localTool。AI05 断言正确：toAbsoluteFileUrl 真身 = localTool/src/routes/resources.ts#L31 | grep 实锤：`toAbsoluteFileUrl` 仅在 `localTool/src/routes/resources.ts#L31` 定义；App.js（src/）内 0 命中 | 建议回写时：删除 07 §二 "P0 Sv() 保存前 toAbsoluteFileUrl @L43462/L43548" 旧锚，改为"toAbsoluteFileUrl 真身 localTool resources.ts L31，App.js 内无此函数，L43548 仅路径拼接" |

> 小结：M6 除 07-7 锚点误判外，全部与 AI05 一致（✅）。07-4（ei/ti 四义）与 AI05 "最严重缺陷"完全对齐，是我方重点记录项。07-7 已回源码 grep 实锤，证实 AI05 正确、我方需更正。
