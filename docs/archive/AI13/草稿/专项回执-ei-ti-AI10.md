# 专项回执 · ei/ti 四义补录（AI10）

> 据 `docs/AI13/专项-ei-ti-AI10.md` 指令执行。仅改 `docs/AI10/` 自有文档与 `docs/var-mapping.txt`（AI10 引用的映射表，任务书允许同步）。未碰他人文档、未改源码、未改 `裁决表.md`/`AI05断言清单.md`。

## 修订清单

### 1. `docs/AI10/16-模块5-认证与配置开关层.md`
- 文末新增「§5 补录 · ei/ti 同名遮蔽四义缺口」小节：
  - 列出 `ei`/`ti` 同名遮蔽五处绑定表（@L1853 / @L36784 / @L43950 / @L36822 / @L43974），均带行号。
  - 缺口说明：`var-mapping.txt` 仅收 GAS 两义，漏收前端三义。
  - 关联提醒：`Ar(e)` 清的缩略图缓存本体即 `ei`@L1853 的 Map，须与 GAS 义 `ei`@L43950 区分。
  - 文末追加标记行。

### 2. `docs/AI10/99-审计总报告.md`
- §8.1 表格 M2 行下方补注：`ri`@L1856 缩略图缓存本体即 `ei`@L1853 的 `Map`；点明 `ei`/`ti` 五义与 var-mapping 仅收两义缺口，引用必带行号。
- §8.1 `Q` 方法表处改写：`Ar(e)` 清缩略图缓存本体即 `ei`@L1853（非 GAS 义 `ei`@L43950）。
- 文末追加标记行。

### 3. `docs/var-mapping.txt`（AI10 引用的映射表，任务书第 2 条允许同步）
- GAS 段 `ei`/`ti` 两行补注 `[同名遮蔽·GAS 义 @L43950/@L43974]`。
- 新增「ei/ti 四义缺口补录」注释块：列出漏收的三义（ei@L1853 缩略图缓存、ei@L36784 节点打组、ti@L36822 清缓存），并注明 ri@L1856 / Ar(e) 与 ei@L1853 的归属关系。

## 事实锚点（均来自多 AI grep 实锤，未杜撰）
- `ei`@L1853 `var ei = new Map()` —— 缩略图缓存 Map
- `ei`@L36784 `ei = Y.useCallback(...)` —— 节点打组
- `ei`@L43950 `ei = async () =>` —— GAS pushToCloud
- `ti`@L36822 `ti = Y.useCallback(...)` —— 清缓存
- `ti`@L43974 `ti = async () =>` —— GAS pullFromCloud

[2026-07-21 据 AI13 裁决表专项：补录 ei/ti 四义（L1853/L36784/L36822/L43950/L43974）]
