# 核对草稿 - AI01 - 模块M5（交叉流）
> 基准：docs/AI13/AI05断言清单.md 的【06 · 交叉流】各条
> 纪律：只写本文件；禁止改 裁决表.md 与他人草稿；❌必须附 file:line 证据
> 交叉核对对象：AI01 T2.5、AI02 20、AI03交叉流、AI04 06、AI06 05、AI07交叉流、AI08 20、AI09 02、AI10 20、AI11 06、AI12 04

| 条目ID | 我的判定(✅/❌/➖) | 我的依据(本AI文档引用或源码file:line) | 备注 |
|--------|------------------|--------------------------------------|------|
| 06-1   | ✅ 一致 | T2.5 X1：AI 生成→落盘→资源面板全链 `Jn`(L32490)→网关→`ii`(L33049)→`Ev`(L42883) rescan→`xv`(L42821) 刷新。与 AI05 一致。 | 一致 |
| 06-2   | ❌ 冲突 | AI05 称 `task-completed`/`update-task-meta` 存在，`canvas-state-change`/`mutiwindow-sync-local` 不存在。我 T2.5 X4 事件表却把 `canvas-state-change`（标注"ReactFlow onChange"）和 `mutiwindow-sync-local`（标注"资源面板按钮"）列为真实 CustomEvent。源码实锤：`App.js` grep `canvas-state-change` → **0 命中**；`mutiwindow-sync-local` → **0 命中**；真实只有 `handleSyncLocal` 函数(L194/L45229)，非事件。真实存在的是 `mutiwindow-task-completed`(L31428/38481/43640/43676/43697/44406) 与 `mutiwindow-update-task-meta`(L41032 dispatch / L43764 listen)。我文档误造了两事件名。 | **已实锤冲突**：我 X4 表含两个不存在的事件，应删除并更正为真实事件名（带 `mutiwindow-` 前缀） |
| 06-3   | ✅ 一致 | T2.5 X2：孤儿清理只清 `source='local-tool'`，`source='extension'` 不清理(resources.ts L120)。与 AI05 一致。 | 一致 |
| 06-4   | ✅ 一致 | T2.5 X3：`Hr=localEngineBase()(L1732)`、`vv=LOCAL_ENGINE.base`(L42808) 指向 18080；`ot`=9004(L43084)；`USE_LOCAL_ENGINE=false` 时 `/api/files/upload` 仅 localTool(18080) 无路由(X3.3)。与 AI05 一致。 | 一致（注意 AI05 01-10 也确认 Hr@L1732=localEngineBase） |
| 06-5   | ❌ 冲突 | AI05 称"canvas-state-change→localforage 实为 Q.saveCanvasState"。我 T2.5 X4 表写 `canvas-state-change` 事件"→ localforage 保存(Cr 键)"。但 `canvas-state-change` 在 App.js 0 命中，真实持久化为 `Q.saveCanvasState`(L1642) 直接写。与 05-7 同源错误。 | **已实锤冲突**：同 05-7，事件名误造 |

## 待实锤队列（所有标❌的条目）
| 条目ID | 我认为冲突的点 | 我附的源码证据(file:line) | 建议实锤方向 |
|--------|--------------|--------------------------|------------|
| 06-2   | 我 X4 事件表列了 `canvas-state-change` 与 `mutiwindow-sync-local` 两个不存在的事件；真实为 `mutiwindow-task-completed`(L31428 等) 与 `mutiwindow-update-task-meta`(L41032/L43764)。`mutiwindow-sync-local` 实为 `handleSyncLocal` 函数(L194/L45229)，非事件总线。 | `App.js` grep `canvas-state-change`→0 命中；`mutiwindow-sync-local`→0 命中；`handleSyncLocal`→`App.js:L194`/`L45229`；`mutiwindow-task-completed`→`App.js:L31428/L38481/43640/43676/43697/44406`；`mutiwindow-update-task-meta`→`App.js:L41032/L43764` | 建议：我 T2.5 X4 表删除 `canvas-state-change`/`mutiwindow-sync-local` 两行，补入真实事件 `mutiwindow-task-completed` 与 `mutiwindow-update-task-meta`（统一带 `mutiwindow-` 前缀） |
| 06-5   | 同 05-7：`canvas-state-change` 不存在，画布持久化走 `Q.saveCanvasState`(L1642) 直接写 localforage。 | `App.js` grep `canvas-state-change`→0 命中；`App.js:L1642 async saveCanvasState(e,t)`；`L1650 saveCanvasStateWithVersion` | 建议：X4 表中 `canvas-state-change` 行改为"画布变更 → `Q.saveCanvasState`(L1642) 直接写 localforage（`Cr` 构造键）"，与 AI05 06-5 对齐 |

> 备注：M5 两处 ❌（06-2、06-5）均源于我 T2.4/T2.5 误造 `canvas-state-change` 与 `mutiwindow-sync-local` 两事件名，已回源码 grep 0 命中实锤。06-1/06-3/06-4 与 AI05 一致。另：我 X4 未列出 `mutiwindow-update-task-meta`（真实事件），属表述不全，但不构成与 AI05 的冲突（AI05 称其存在，我未否认）。
