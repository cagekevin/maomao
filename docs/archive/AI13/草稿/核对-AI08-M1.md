# 核对草稿 - AI08 - 模块M1（初始化配置）

> 基准：docs/AI13/AI05断言清单.md 的【02 · 模块1】条目 02-1~02-10
> 纪律：只写本文件；禁止改 裁决表.md 与他人草稿；❌必须附 file:line 证据
> 交叉核对对象（去这些 AI 的同主题文件比对自己说法）：AI01 T2.1/AI02 10/AI03模块1/AI04 02/AI06 01/AI07模块1/AI08 11/AI09 04/AI10 10/AI11 02/AI12 05
> 我方比对文档：docs/AI08/11_模块1_初始化与配置层.md + docs/AI08/00_AI08索引.md

| 条目ID | 我的判定(✅/❌/➖) | 我的依据(本AI文档引用或源码file:line) | 备注 |
|--------|------------------|--------------------------------------|------|
| 02-1   | ➖ | 我文档 `11_模块1` 未读 `main.tsx`、未断言 `React.lazy` 导入 App.js；仅引用 `config.js`。与 AI05 同标 ➖（未读源码） | 一致缺失，非冲突 |
| 02-2   | ➖ | 我文档 `11_模块1` 未记录 `bootstrap@L43489` 的 `Chrome.runtime?.id` 判定（仅 `Oa`/`Ba` 鉴权层） | 未专门记录该断言 |
| 02-3   | ➖ | 我文档 `11_模块1` 字典录 `wr`(L1297)/`Mr`(L1364) 两后端，并注 `Nr`/`Pr` 真身未出现；但未记录 `bootstrap`/`getStatus` L1476/L1480 行号 | 存储编排 Q 一致，行号断言未覆盖 |
| 02-4   | ➖ | 我文档 `11_模块1` 未记录启动播种 `L43089-43135` 行号 | 未专门记录 |
| 02-5   | ✅ | 我文档 `11_模块1` ②字典：`Oa`(L3543)=`getAuthToken` 返固定 `local-mode-token`；`ka`(L3547)/`Aa`(L3552)；`Ba`(L3637)=`hasAuthToken`=`!!Oa()` 本地模式恒 true（门4 30_ A.3 读体确认） | 与 AI05 一致 |
| 02-6   | ✅ | 我文档 `11_模块1` ①：`USE_LOCAL_ENGINE=true`(config.js:L36)→18080；`LOCAL_ENGINE.base`(L12-18)；`DEFAULT_ENDPOINT`(L30)=9004 | 与 AI05 一致 |
| 02-7   | ✅ | 我文档 `11_模块1` §④ 标 `canvas-state-change` 待交叉流核对；本轮源码 grep `canvas-state-change` 于 `src/_engine/App.js` **0 命中**（与 08-1 互证） | AI05 断言"代码不存在"成立，方向一致（依据以源码 0 命中为实锤） |
| 02-8   | ✅ | 我文档 `11_模块1` ②字典：`Jh`(L30712)=`authHeaders`(主) | 与 AI05 实锤一致 |
| 02-9   | ✅ | 我文档 `11_模块1` 流C + §④：P0 根因 `Hr`/`vv` 硬编码 `18080`(var-map)，`U_` 同硬编码；且 `Hr`/`vv` 未引用 `config.js LOCAL_ENGINE.base` → 割裂根因坐实 | 与 AI05 一致（含 01-10 冲突点） |
| 02-10  | ➖ | 我文档 `11_模块1` 未记录 `setConfig` 仅写 localTool+localforage(L1501) 的专门断言 | 未专门记录 |

## 待实锤队列（所有标❌的条目）
| 条目ID | 我认为冲突的点 | 我附的源码证据(file:line) | 建议实锤方向 |
|--------|--------------|--------------------------|------------|
|        |              |                          |            |

> 小结：M1 范围内我方文档与 AI05 无 ❌ 冲突；02-1/02-2/02-3/02-4/02-10 为 ➖（我方未专门覆盖该断言，属文档覆盖缺口，非事实冲突）。重点冲突关注 02-7（canvas-state-change）已源码实锤 0 命中，与 AI05 一致。
