# 修订回执 · AI03

> 依据 `docs/AI13/修订-AI03.md` 与 `docs/AI13/裁决表.md` 执行。
> 执行时间：2026-07-21。仅改 `docs/AI03/` 自有文档，未碰他人文档/源码/裁决表/AI05断言清单。

## 改动清单

### 文件 1：`docs/AI03/模块1-初始化与配置层.md`

| 修订点 | 位置 | 旧内容 | 新内容 |
|--------|------|--------|--------|
| 修订1 | §3.2 启动初始化链（原 L53） | `→ 注册监听（mutiwindow-task-completed / resourceAdded / canvas-state-change）` | 删 `canvas-state-change`；补注「画布持久化走 `Q.saveCanvasState`(App.js L1642) 直接写 localforage，无 `canvas-state-change` 事件（源码 grep 0 命中）」 |
| 修订1 | §5 边界契约表（原 L81） | `CustomEvent \| mutiwindow-task-completed / resourceAdded / canvas-state-change \| 见交叉流审计` | 删除 `canvas-state-change`；`resourceAdded` 标注为跨进程（App.js L43527 经 chrome.runtime 接收）；新增「持久化」行说明 `Q.saveCanvasState`(L1642) 直接写 localforage，无该事件 |
| 标记 | 文件末尾 | — | 追加 `[2026-07-21 据 AI13 裁决表修订：删 canvas-state-change/mutiwindow-sync-local 假事件]` |

### 文件 2：`docs/AI03/模块2-本地数据层与Rescan.md`

| 修订点 | 位置 | 旧内容 | 新内容 |
|--------|------|--------|--------|
| 修订2 | §5 边界契约表（原 L102） | `CustomEvent \| mutiwindow-task-completed(触发 Ev) / mutiwindow-sync-local(触发 we) \| 见交叉流审计` | 删除 `mutiwindow-sync-local` 行；保留 `mutiwindow-task-completed`(App.js L38481) 与 `resourceAdded`(跨进程, chrome.runtime L43527)；新增「持久化」行说明无 `canvas-state-change`/`mutiwindow-sync-local` 事件（`mutiwindow-sync-local` 实为 `handleSyncLocal` 函数 App.js L44426） |
| 标记 | 文件末尾 | — | 追加 `[2026-07-21 据 AI13 裁决表修订：删 canvas-state-change/mutiwindow-sync-local 假事件]` |

## 未波及说明

- 模块4（画布引擎与节点）文档经 grep 确认未列两假事件，故无需修订。
- 交叉流审计.md 经 grep 确认未列两假事件（其事件表仅含 `mutiwindow-task-completed`/`resourceAdded`），故无需修订。

## 依据

- 修订1：裁决表 02-7 / 05-7 / 06-5 —— `grep canvas-state-change` 源码 0 命中，持久化真身为 `Q.saveCanvasState`(L1642)/`saveCanvasStateWithVersion`(L1650/L31728)。
- 修订2：裁决表 06-2 / 07-6 —— `grep mutiwindow-sync-local` 源码 0 命中，实为 `handleSyncLocal`(L44426)；真实保留 `mutiwindow-task-completed` 与 `resourceAdded`(跨进程)。
