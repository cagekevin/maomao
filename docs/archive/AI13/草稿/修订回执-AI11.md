# 修订回执 · AI11（据 AI13 裁决表）

> 修订日期：2026-07-21
> 任务来源：`docs/AI13/修订-AI11.md`
> 核心结论：`toAbsoluteFileUrl` 真身 = `localTool/src/routes/resources.ts#L31`（定义处，硬编码 18080）；App.js 内 0 命中，L43548 仅 `${Hr}${url}` 拼接非函数定义；L43462 实为 `Lr = `{`，与 toAbsoluteFileUrl 无关。旧锚删除改正。

## 改动清单

| 文件 | 位置 | 改动 |
|------|------|------|
| `docs/AI11/07-文档纠错建议.md` | §二 表格 P0 行（原 L30） | 旧「Sv() 保存前 toAbsoluteFileUrl @L43462 ⚠️ 行号漂移 / 绝对路径补全在 resourceAdded@L43548 / 改行号」→ 改为 ❌ 误标，删 App.js L43462/L43548 旧锚，改指 `localTool/src/routes/resources.ts#L31`；文件头加修订标记行 |
| `docs/AI11/03-模块2-本地数据层与Rescan.md` | §四 数据流图 L48 / §六 排雷表 L75 | 两处 L43548 补全绝对路径表述，补注「仅 `${Hr}${url}` 拼接，真身在 localTool resources.ts:L31」；文末加修订标记行 |
| `docs/AI11/校验报告-AI11.md` | L30 / L71 两校验行 | L43548 行补注「非 toAbsoluteFileUrl，真身在 resources.ts:L31」；L43462 行状态由 ✅ 改为 ⚠️撤销（旧草稿误标）；文件头加修订标记行 |
| `docs/AI11/README.md` | 第 3 条红线合规说明（原 L48） | 补注「L43548 仅 `${Hr}${url}` 拼接，真正 toAbsoluteFileUrl 定义在 localTool resources.ts:L31」；文末加修订标记行 |

## 未改动（遵守边界）
- 未碰他人文档（AI01–AI10、AI13 等）。
- 未改源码（localTool / App.js 等）。
- 未改 `裁决表.md` / `AI05断言清单.md`。
- 仅改 `docs/AI11/` 自有文档。

## 验证依据
- grep `toAbsoluteFileUrl`：`localTool/src/routes/resources.ts#L31` 定义；App.js 内 0 命中（草稿 07-7/09-1/11-2 已实锤）。
- `localTool/src/routes/resources.ts#L31`：`function toAbsoluteFileUrl(relativePath: string): string {`（硬编码 18080）。
