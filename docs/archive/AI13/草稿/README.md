# 草稿目录（隔离写入层）

> 本目录存放**各被派 AI 的独占核对记录**。每个 AI 只写自己的文件，禁止改 `裁决表.md` 与他人草稿。
> 命名规则：`核对-AI{NN}-M{K}.md`（如 `核对-AI03-M1.md`）。
> 模板见 `草稿/核对模板.md`。
> `docs/AI13/裁决表.md` 是唯一种表层，仅由 AI13（或用户）在收齐草稿后汇总写入，不被并发改。
