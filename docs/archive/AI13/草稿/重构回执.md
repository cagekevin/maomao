# 重构回执 · 六层理解文档层

> 执行依据：`docs/AI13/任务表-重构docs理解文档层.md`（2026-07-21）。
> 事实源唯一：`docs/AI13/交叉验证最终报告.md` + `docs/AI13/裁决表.md`。
> 铁律遵守：AI01~AI12 / AI13 / 源码 均未触碰；所有 `file:line` 引自最终报告实锤值，无杜撰。

---

## 一、新建文件清单

| 文件 | 角色 | 内容来源 | 状态 |
|------|------|---------|------|
| `docs/glossary.md` | 术语表（最高优先级） | 裁决表同名遮蔽全集 + 已证伪项 + 跨包真身 + 3D 系统 + 事件 payload | ✅ 重建 |
| `docs/01-concept.md` | 产品与需求 | 最终报告反推用户故事 + 边界 | ✅ 新建→增强 |
| `docs/02-architecture.md` | 架构主文件 | 拓扑 + ADR(含 ADR-4 双轮询) + 已裁决事实段（全引实锤锚点） | ✅ 新建→增强 |
| `docs/03-database.md` | 数据模型 | AI08/AI11 实证 + AI05-03/11 + 最终报告 + 债务解法 | ✅ 新建→增强 |
| `docs/04-api/api-reference.md` | 接口契约 | AI05-03/11/13/14 实证 + 已补 tasks 落库/删盘解法/payload | ✅ 新建→增强 |
| `docs/05-runbook.md` | 运维 | `启动项目.ps1` + `package.json` 抽取 | ✅ 新建→增强 |
| `docs/06-integration.md` | **对外能力接口文档（开发者/AI 首选）** | AI13 最终报告 + AI05 全 19 文件深审提炼 | ✅ **新增** |

---

## 二、各文件改动明细

### `docs/glossary.md`
- 重写为统一黑话表，含 7 组同名遮蔽（ei/ti 四义、Jn、Zr、we、R、Th）带 `绑定A@行 / 绑定B@行`。
- 动态配置：`Hr`@L1732（localEngineBase 动态）/ `vv`@L42808（硬编码 18080）。
- 已证伪项红字：`canvas-state-change`（0 命中，`Q.saveCanvasState`@L1642）、`mutiwindow-sync-local`（实为 `handleSyncLocal`@L44426）。
- 跨包真身：`toAbsoluteFileUrl`@`localTool/src/routes/resources.ts#L31`。
- 3D：`$d`=DirectorShell@L24391 / `Th`=Director3DNode@L31141（非失效）。
- 事件总线真实事件名（mutiwindow-task-completed / mutiwindow-update-task-meta / resourceAdded 跨进程）。

### `docs/01-concept.md`
- 核心价值一段话（画布 + AI 生图 + localTool + 网关 + GAS 闭环，本地优先）。
- 4 条用户故事（拖入 / 采集 / 生图 / 同步），均引真实事件与函数。
- 边界（不做云端协作 / 不内置推理 / 不做多用户权限 / 不做音频生成 / 不做剪映真实发送）。

### `docs/02-architecture.md`
- 系统拓扑图（前端 ↔ localTool ↔ 网关 ↔ GAS，background 跨进程 resourceAdded）。
- 3 则 ADR（localTool 独立进程 Why / 双 base 开关 Why / mutiwindow-* + chrome.runtime 双轨 Why）。
- 已裁决事实段 6 节：画布持久化（`Q.saveCanvasState`，无 canvas-state-change）、事件总线真实事件（证伪两项）、资源落盘（Zr@L1827 / toAbsoluteFileUrl 真身）、3D（Th/$d）、撤销重做（store.r 接管）、AI 生图流（Jn@L32490 → 轮询 L33005 → 落盘 L33049）。

### `docs/03-database.md`
- 存储总览（sql.js WASM / 磁盘 / 浏览器内）。
- 三表字段语义：resources（含 source 孤儿清理只清 local-tool）、tasks（media_meta 序列化）、kv。
- upsert = DELETE+INSERT（无 ON CONFLICT）。
- 6 条已知债务（wv 只删 DB、tasks clear 无删盘、孤儿清理局限、缩略图伪复制、ID 双源冲突、网关内存态）。
- 落盘路径表（tasks/migrated/drop/paste/.thumbnails，URL 拖入不落盘）。

### `docs/04-api/api-reference.md`（小幅补全，原稿保留）
- 网关端点补：`/v1/chat/completions` POST SSE（main.py L434–574，abort→400 L502/L521，超时→504 L541，heartbeat L549）。
- 其余 localTool 端点 / tasks / resources / kv / 跨进程 resourceAdded 原稿已正确，保留。
- music/audio 501 偏差原稿已记，保留。

### `docs/05-runbook.md`
- 环境依赖（Node 18+ / Python / Chrome / max-old-space-size=1024）。
- 三步启动：localTool(:18080) → 网关(`--port 9004`) → 前端 build+加载。明确注明 `启动项目.ps1` **只启动 localTool**，网关需另行启动。
- 配置说明（config.js 集中；网关见 .env.example）。
- 排查入口（三处日志位置）+ 常见故障（18080/9004/破图/不落盘）。

---

## 三、重定向处理（仅顶部加重定向行，正文未改）

| 文件 | 顶部新增行 |
|------|-----------|
| `docs/ARCHITECTURE.md` | `> ⚠️ 本文为旧版架构描述，已迁移至 docs/02-architecture.md（理解文档主文件，含交叉验证实锤事实）。旧版保留仅供追溯。` |
| `docs/TASKS.md` | `> ⚠️ 本文为旧版任务/审计计划，审计结论已汇总至 docs/AI13/交叉验证最终报告.md；理解文档见 docs/02-architecture.md。旧版保留仅供追溯。` |

---

## 四、未触碰项（铁律验证）

- ✅ AI01~AI12 审计草稿：未删未改。
- ✅ AI13 全部文件（裁决表 / 最终报告 / 任务表 / 修订稿 / 草稿）：未删未改。
- ✅ 所有源码（App.js / localTool / 网关 / config.js）：未修改。
- ✅ func-mapping.txt / var-mapping.txt / FUNCTION_MAP.md / PROJECT_ORIGIN.md 等：保留不动。

---

## 五、验收对照（任务表 §5）

- [x] 六层文件全部新建/补全，内容来自最终报告实锤锚点，无杜撰行号。
- [x] glossary.md 含全部同名遮蔽 + 已证伪项。
- [x] ARCHITECTURE.md / TASKS.md 仅顶部加重定向行，正文未动。
- [x] AI01~AI12 / AI13 / 源码 均未触碰。
- [x] 重构回执写毕，列明每个文件改动。
