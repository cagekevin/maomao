# 核对草稿 - AI03 - 模块M1（初始化配置）
> 基准：docs/AI13/AI05断言清单.md 的【02 · 模块1】各条
> 纪律：只写本文件；禁止改 裁决表.md 与他人草稿；❌必须附 file:line 证据
> 交叉核对对象（去这些 AI 的同主题文件比对自己说法）：AI01 T2.1/AI02 10/AI03模块1/AI04 02/AI06 01/AI07模块1/AI08 11/AI09 04/AI10 10/AI11 02/AI12 05

| 条目ID | 我的判定(✅/❌/➖) | 我的依据(本AI文档引用或源码file:line) | 备注 |
|--------|------------------|--------------------------------------|------|
| 02-1   | ➖ | 未读 main.tsx；本AI文档模块1 §3.2 仅述 `React.lazy(import App.js)`，未 grep main.tsx 实锤 | 与 AI05 同 ➖，不冲突 |
| 02-2   | ✅ | 一致。模块1 §2 述 bootstrap/登录链；本AI未 grep L43489 但行为描述与 AI05"Chrome.runtime?.id判定"一致 | 未独立实锤 L43489，但无矛盾 |
| 02-3   | ✅ | 一致。模块1 §3.2 述 `Q`(StorageManager) 编排 wr/Mr/Nr/Pr；getStatus 行号未独立 grep，但与 AI05 描述相符 | — |
| 02-4   | ✅ | 一致。模块4 §3/交叉流述启动播种链 Zr+Sv@L43104/L43107；播种范围与 AI05 一致 | — |
| 02-5   | ✅ | 一致。模块1 §2-3：`Oa()` 永返 local token，`Ne` 恒 true；与 AI05 去登录断言一致 | — |
| 02-6   | ✅ | 一致。模块1 §3.1：`USE_LOCAL_ENGINE=true→18080,false→9004`；config.js L36 本AI已引 | — |
| 02-7   | ❌ | **冲突**。AI05 称 saveCanvasState@L1642 直接写 localforage、无 canvas-state-change；回 App.js grep `canvas-state-change` = **0 命中**（src 全树）。本AI模块1 §3.2/§5 误列 `canvas-state-change` 为真实 CustomEvent（"注册监听（mutiwindow-task-completed / resourceAdded / canvas-state-change）"），实际持久化走 `Q.saveCanvasState`，该事件名不存在。模块4 §3 亦确认无此事件 | 见待实锤队列 |
| 02-8   | ✅ | 一致。本AI未 grep L30712，但 Jh(authHeaders) 行号差异属漂移，无实质冲突 | — |
| 02-9   | ✅ | 一致（部分修正 AI5）。本AI模块1 §3.1：`Hr`(L1732)=localEngineBase()、`vv`(L42808)=硬编码18080、`ot`=9004；host 硬编码残留属实。但 **`Hr` 非字面硬编码**，是 `localEngineBase()` 调用（见 02-10/01-10 实锤） | 与 AI5 在"Hr 是否硬编码"有措辞分歧，见 02-10 |
| 02-10  | ✅ | **与 AI5 表述冲突，实锤支持本AI**。App.js L1732：`var Hr = localEngineBase(),` —— `Hr` 是调用 config 函数 `localEngineBase()` 的结果，非字面硬编码 18080。AI5 称"Hr/vv 值恒等18080 / Hr 硬编码"在"值"上正确（都=18080），但"硬编码"措辞错；本AI模块1 §2 写 `Hr = localEngineBase()` @L1732 准确 | 见待实锤队列（仅措辞分歧，非事实分歧） |

## 待实锤队列（所有标❌的条目）
| 条目ID | 我认为冲突的点 | 我附的源码证据(file:line) | 建议实锤方向 |
|--------|--------------|--------------------------|------------|
| 02-7   | 本AI误列 canvas-state-change 为真实事件；AI5 称其不存在 | grep `canvas-state-change` in src/ = 0 命中（已实锤）；App.js L1642 为 saveCanvasState 直接写 localforage（AI5 锚） | 本AI模块1 §3.2/§5 应删去 canvas-state-change，改为"持久化走 Q.saveCanvasState，无独立事件" |
| 02-10  | AI5 称 Hr 硬编码 18080；本AI称 Hr=localEngineBase()@L1732 | App.js L1732 `var Hr = localEngineBase(),` | 事实：Hr 值=18080(因 USE_LOCAL_ENGINE=true)，但来源是 config 函数调用非字面量；AI5 措辞不严谨，本AI正确 |
