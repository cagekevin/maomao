# 修订回执 · AI04（据 AI13 裁决表回写）

> 执行指令：`docs/AI13/修订-AI04.md`
> 执行日期：2026-07-21
> 范围：仅改 `docs/AI04/` 自有文档，未碰他人文档 / 源码 / 裁决表.md / AI05断言清单.md。

## 修订 1：删除 `canvas-state-change` / `mutiwindow-sync-local` 两假事件

| 文件 | 位置 | 改动 |
|------|------|------|
| `05-模块4画布引擎与节点.md` | 顶部边界契约(L4)、③关键数据流(L40)、边界契约出事件(L55) | 删除 `canvas-state-change`(localforage) 3 处；改为 `Q.saveCanvasState`(L1642)/`saveCanvasStateWithVersion`(L1650/L31728) 直接写 localforage，注明该事件源码 grep 0 命中系误造 |
| `06-交叉流审计.md` | X4 事件表(L47/L48) | 删除 `canvas-state-change`(ReactFlow onChange→localforage) 与 `mutiwindow-sync-local`(资源面板按钮) 两行；补 `mutiwindow-update-task-meta`(L41032/L43764)；新增「已删两假事件」说明块（含 handleSyncLocal@L44426 真身） |
| `06-交叉流审计.md` | 对抗审计 Q(L55) | `mutiwindow-sync-local`(L44435) 改为真实函数 `handleSyncLocal`(L44426)，注明原事件名 0 命中 |
| `08-模块5同步与采集与存储键.md` | 边界契约出事件(L74) | 删除 `mutiwindow-sync-local`(L44435)；补 `mutiwindow-update-task-meta`(L41032/L43764)，注明原事件名 0 命中、真实为 `handleSyncLocal`(L44426) |

依据：裁决表 02-7 / 05-7 / 06-2 / 06-5；源码 grep 两事件名均 0 命中。

## 修订 2：`Hr` 改判为动态（非硬编码）

| 文件 | 位置 | 改动 |
|------|------|------|
| `01-阶段0映射表缺口.md` | L21 | 已为正确表述：`Hr = localEngineBase()`（USE_LOCAL_ENGINE?18080:9004）；无需改动，确认无误 |
| `06-交叉流审计.md` | 交叉风险点3(L35-39)、对抗审计 Q(L57) | 已明确 `Hr = localEngineBase()`(L1732) 随开关变 18080/9004，`vv`(L42808) 才是字面值写死 18080；措辞与裁决表一致，确认无误 |
| `07-审计总报告.md` | L29 | 已为「Hr = localEngineBase()（随开关变）」；确认无误 |

结论：AI04 旧文档从未将 `Hr` 表述为「硬编码 18080」（vv 才是硬编码），Hr 动态性表述自始正确，本次据裁决表复核确认，无实质误述需改；但在 05/06/08 三文件均追加了统一修订标注行。

## 追加修订标注行

在 `05-模块4画布引擎与节点.md`、`06-交叉流审计.md`、`08-模块5同步与采集与存储键.md` 末尾各追加：
`[2026-07-21 据 AI13 裁决表修订：删两假事件；Hr 改判 localEngineBase() 动态]`

## 检查项

- [x] 仅改 `docs/AI04/` 文档
- [x] 未改源码 / 裁决表.md / AI05断言清单.md / 他人文档
- [x] 删除两假事件并补真实事件（`mutiwindow-task-completed`/`mutiwindow-update-task-meta`）
- [x] Hr 动态性措辞核实（原判正确，复核确认）
- [x] 三文件加修订标注行
- [x] 本回执写入 `docs/AI13/草稿/修订回执-AI04.md`
