# 修订回执 · AI07

> 据 `docs/AI13/修订-AI07.md` 与 `裁决表.md`（条目 05-7 / 06-2 / 06-5 / 07-6）执行。
> 日期：2026-07-21。

## 修订内容

### 修订 1：删除 `canvas-state-change` 假事件（源码 0 命中，持久化走 `Q.saveCanvasState` 直接调用 L1642/L31728）

| 文件 | 位置 | 改动 |
|------|------|------|
| `docs/AI07/模块4-画布引擎与节点.md` | §① 运转图景 (原 L10) | 原「画布状态经 Q → localforage 持久化，`canvas-state-change` 事件触发」改为「走 `Q.saveCanvasState` 直接调用，代码中不存在 `canvas-state-change` 事件」；事件总线列表删除 `canvas-state-change` 条目 |
| `docs/AI07/模块4-画布引擎与节点.md` | §③ 3.3 画布状态持久化 (原 L73) | 删除 `ReactFlow onChange → dispatchEvent('canvas-state-change')` 链，改为直接 `Q.saveCanvasState` 调用，并加注「代码中不存在 canvas-state-change 事件（grep 0 命中）」 |
| `docs/AI07/模块1-初始化与配置层.md` | §③ 3.4 画布状态保存/恢复 (原 L72) | 删除「→ canvas-state-change 事件 → localforage 持久化 (ARCHITECTURE X2)」一行，改为说明持久化走直接调用、事件不存在 |
| `docs/AI07/交叉流审计.md` | X5 事件总线表 (原 L77) | 删除 `canvas-state-change` 事件行 |

### 修订 2：删除 `mutiwindow-sync-local` 假事件（实为 `handleSyncLocal` L44426，非 CustomEvent）

| 文件 | 位置 | 改动 |
|------|------|------|
| `docs/AI07/交叉流审计.md` | X5 事件总线表 (原 L76) | 删除 `mutiwindow-sync-local` 假事件行；补真实事件 `mutiwindow-update-task-meta`(L41032 / L43764)；表下加修订说明注明 `mutiwindow-sync-local` 实为 `handleSyncLocal`(L44426) |

## 标记行

已在以下文件末尾追加：
`[2026-07-21 据 AI13 裁决表修订：删 canvas-state-change/mutiwindow-sync-local 假事件]`
- `docs/AI07/模块4-画布引擎与节点.md`
- `docs/AI07/交叉流审计.md`
- `docs/AI07/模块1-初始化与配置层.md`

## 未触碰

- 未改他人文档、未改源码、未改 `裁决表.md` / `AI05断言清单.md`。
- 模块4 §③ `mutiwindow-task-completed`(L38481) 真实事件保留；X5 表真实事件 `mutiwindow-task-completed` / `resourceAdded` / `import-project` / `export-project` 均保留。
