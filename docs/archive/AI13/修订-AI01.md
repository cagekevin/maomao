# 修订任务书 · AI01（回写裁决结论）

> 你是 AI01。本文件是你的唯一修订指令，读完即执行，无需询问。
> 工作区：你的旧审计文档 `docs/AI01/`（**本次允许改你自己的文档**），以及本目录。
> 背景：交叉验证已完成，裁决见 `docs/AI13/裁决表.md`。你的草稿 `docs/AI13/草稿/核对-AI01-M*.md` 中已自认以下错误，现据源码实锤结论回写修订。

## 必须修订的错点（均源自你误造假事件名）

### 修订 1：删除 `canvas-state-change` 假事件
- 位置：T2.4（模块4 §3.4 边界契约、§流4）、T2.5（X4 事件表、§5）。
- 旧内容：把 `canvas-state-change` 当作真实 CustomEvent，称"画布变更 → canvas-state-change → localforage 保存"。
- 改为：`画布变更 → Q.saveCanvasState(L1642) 直接写 localforage（Cr 构造键），无 canvas-state-change 事件`。
- 依据：源码 `grep canvas-state-change` in App.js = **0 命中**（你草稿 05-7/06-5 已实锤）。

### 修订 2：删除 `mutiwindow-sync-local` 假事件，补真实事件
- 位置：T2.5（X4 事件表）。
- 旧内容：列 `mutiwindow-sync-local`（标注"资源面板按钮"）为真实 CustomEvent。
- 改为：删除该行；补入 `mutiwindow-task-completed`(L38481 等多处) 与 `mutiwindow-update-task-meta`(L41032/L43764)，统一 `mutiwindow-` 前缀。注明 `mutiwindow-sync-local` 实为 `handleSyncLocal` 函数(L44426)，非事件总线。
- 依据：源码 `grep mutiwindow-sync-local` = 0 命中（你草稿 06-2 已实锤）。

## 执行要求
- 只改你自己的 `docs/AI01/` 文档，不碰他人文档、不改源码、不改 `裁决表.md` / `AI05断言清单.md`。
- 改完在对应文档顶部或修订记录里加一行：`[2026-07-21 据 AI13 裁决表修订：删 canvas-state-change/mutiwindow-sync-local 假事件]`。
- 完成后在 `docs/AI13/草稿/` 新建 `修订回执-AI01.md`，列出改了哪些文件哪几处（一句话即可）。
