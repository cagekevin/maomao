# T2.11 · 3D 导演台全链路闭环（$d / Sh 展开，_DRAFT · 未验证）

> 续 `T2.10_wh与In图片盒子深挖_DRAFT.md`，展开 `$d`(L24391) 与 `Sh`(L28112)，完成 3D 导演台全链路审计。
> 目标：确认 3D 渲染子树内部局部符号是否含模块级 R/B/we 重名风险（T2.7 原担忧）。
> 行号快照 2026-07-21，会漂移，以 grep 为准。归属 V1。未改任何源码。

---

## 结论（高置信）

**3D 导演台全链路已展开：`Th`(L28388) → `wh`(L28276) → `$d`(L24391) → `Sh`(L28112)，截图经 `onCaptureToBox`(`Kn` L32367) → `In.current`=`qn`(L32370) 推入 imageBoxNode。**
**3D 导演台渲染/截图链路相关组件（`Th`/`wh`/`$d`/`Sh`/`qn`）局部符号均干净，无模块级 `R`/`B`/`we` 重名冲突 —— T2.7 原担忧「重名高发指向 wh/Kn 子组件」被推翻。**
> 注：3D 导演台**属性面板区**（非渲染链路）另有局部重名 `R`(L23247 zoom 拖拽)/`B`(L23258 目标模式切换)，属 README 问题4 记录的"3D导演台区 R/B"，与渲染链路 `Th→wh→$d→Sh` 隔离、不影响本结论。真正重名高发区是画布主组件资源面板（L29149 R / L29159 z / L29188 B）。

---

## 一、`$d`(布局包裹, L24391–24413)

```
function $d({ children: e }) {
  let t = $(e => e.viewportPanelsCollapsed);
  return <div className="director-shell ...">
    <section className="viewport-column">{e}</section>   // 3D 视口（放 Sh）
    <aside>{id /* 场景面板 */}</aside>
    <aside>{Qd /* 属性面板 */}</aside>
  </div>
}
```
- 局部仅 `t`/`e`，**无模块级冲突**
- `id`(场景面板)/`Qd`(属性面板) 为子组件，超出 3D 导演台核心截图链路，非重名高发区

---

## 二、`Sh`(3D 场景渲染, L28112–28272)

react-three-fiber 画布组件（Canvas=`yt`）：
- Zustand store 读取：`viewMode`/`openSceneInspector`/`project.scene`/`project.assets`/`project.panoramaAssetId`/`activeCamera`/`viewportAspectRatio` 等
- 渲染子树：`Dp`(场地+全景球) / `ambientLight` / `directionalLight` / `Kt`(网格) / `gn`(OrbitControls) / `yh`(相机辅助) / `it`(机位相机) / `vh`(安全区/构图) / `vp`(Suspense 资源) / `Cp`(三分线 UI) / `xh`(快照 UI) / `Fm`(工具栏)
- `onCreated`：初始化相机快照 `c.current`/`f(c.current)`
- `onChange`(OrbitControls)：节流 32ms 写相机快照 `O({fov,position,target})`
- 局部符号：`e`/`t`/`n`/`r`/`i`/`a`/`o`/`s`/`c`/`l`/`u`/`d`/`f`/`p`/`m`/`h`/`g`/`_`/`v`/`y`/`b`/`x`/`S`/`C`/`w`/`T`/`E`/`D`/`O`/`k`/`A` —— **全组件局部，无模块级 R/B/we 冲突**

---

## 三、3D 导演台全链路状态机（闭环）

```
点击节点「打开导演台」(Th L28469)
  → s(true) 开弹窗
  → Un.createPortal(wh, document.body)        // wh = 3D 编辑器 (L28503)
      wh:
        replaceProject(initialProject ?? Du())       // 载入/默认工程
        if(initialPanoramaUrl) dm(url) → addImportedAsset({kind:'panorama'})  // 全景导入
        渲染 $d → Sh（3D 场景）
        快捷键：Delete 删物 / Ctrl+C/V/Z/Y 复制/粘贴/撤销/重做
        点「返回画布」→ c():
          s() = Ed({preset:'current', source:'capture-panel'})  // 取机位截图 dataUrl
          n({ project, thumbnailDataUrl })            // 调 onExit
  → Th.f(onExit):
      遍历 cameras[].captures → onCaptureToBox?.(id, [{url:dataUrl, label}])  // = Kn(L32367)
        → In.current(e,t) = qn(e,t)                   // 推入/新建 imageBoxNode
      写回 directorProject/syncedCaptureIds
      if(thumbnailDataUrl) ii(..., {subfolder:'tasks'})  // 缩略图落盘 18080（Th 内唯一落盘点）
  → 节点 imageUrl = 缩略图 http URL（成功后）/ base64（失败仅 warn）
```

**落盘点清点**：
- `Th.f` 内 `ii(thumbnailDataUrl)`（L28438）—— 仅缩略图落盘 ✅
- `qn` 内截图 dataURL 直进 imageBoxNode —— **未落盘** ❌（P1 同源，见 T2.10）
- `wh`/`Sh`/`$d` —— 纯渲染/交互，无落盘逻辑

---

## 四、与既有文档一致性

- **彻底闭合 T2.7 待补项**：`wh`/`Kn` 子组件已展开（T2.10），`$d`/`Sh` 本批展开，**全链路无重名冲突**，原担忧推翻。
- **闭合 T2.10 待补项**：`$d`/`Sh`(Three.js 子树) 已展开，确认非重名风险点；`Sh` 内部 `id`/`Qd` 子组件为 sidebar 面板，超出核心链路，未展开但评估非重名高发。
- 与 README 问题4（`R`/`B` 局部重名）无关：3D 导演台链路全部组件局部符号隔离，真正冲突在画布主组件。

---

## 五、审计收尾说明

3D 导演台（模块4 特殊节点）审计已**全链路覆盖**：`Th`(T2.7) → `wh`+`Kn`+`In`/`qn`(T2.10) → `$d`+`Sh`(本批)。
全模块4 审计完成度：onDrop/onConnect/右键菜单/持久化（T2.4）+ 3D 导演台全链路（T2.7/T2.10/T2.11）+ 双服务 URL（T2.8 含上传 base）。

---

## 待回写映射表（本会话未改）

- `$d` = DirectorShell 布局包裹（L24391）—— 建议 func-mapping 补注
- `Sh` = Director3DScene 渲染（L28112）—— 建议 func-mapping 补注
- `id`/`Qd`（sidebar 面板）未展开，若后续审计需可单列
