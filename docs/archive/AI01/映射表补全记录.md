# 映射表补全记录（阶段 0 · T0.1/T0.2 坐实）

> 本文件是 `TASKS.md` 架构审计计划「阶段 0 补映射表」的实测产出。
> 方法：对每个待补符号，先用 `search_content` 在 `src/_engine/App.js` 定位当前行号，再读函数体坐实语义。
> 实测日期：2026-07-21（行号快照，会漂移，后续引用以「grep 确认」为准）。
> 铁律：源码 = 唯一权威；本记录为派生草稿，凡与旧文档冲突处以本记录 + 实际代码为准。

---

## 一、T0.1 解码补入 func-mapping.txt（实测坐实）

| 混淆名 | 实测位置 | 真实语义 | 与旧文档差异 |
|--------|----------|----------|--------------|
| `Ev` | `App.js` **L42883** `async function Ev()` | **rescanResources 前端触发**：`fetch(${vv}/api/resources/rescan, POST)` → 返回 count。caller: L31426/L43017/L44435 | ✅ 与 TASKS/T0.1 猜的一致 |
| `we` | **两处重名，必须区分**：<br>① L4176 `we=(t,n=false)=>` 局部：PromptNode 内插入 `@mention` 文本<br>② L43015 `we=Y.useCallback(async()=>{ await Ev(); ... xv() })` 局部：资源面板「同步/刷新」主函数（rescan+查询） | ②才是 T0.1 说的「rescanResources 主函数」，但它**不是模块级 function，是组件内 useCallback**，名为 `we` 但 func-mapping 未收录此局部绑定 | ⚠️ T0.1 把 `we` 当模块级 rescan 主函数，实为**资源面板组件局部 useCallback**（与 L4176 的 `we` 重名不同物） |
| `Sv` | `App.js` **L42838** `async function Sv(e)` | **资源 upsert（save）**：`fetch(${vv}/api/resources/save, POST, body=e)` | ✅ 与「资源入库」猜测一致（大写 Sv ≠ 小写 sv=nodeCallbackFieldSet） |
| `wv` | `App.js` **L42857** `async function wv(e)` | **资源删除**：`fetch(${vv}/api/resources/delete?id=..., POST)`；**只删 DB，不删磁盘**（P2 已知） | ✅ 一致 |
| `ii` | `App.js` **L1888** `async function ii(e,t={})` | **统一上传落盘**：`fetch(${Hr}/api/files/upload, ...)` → 返回绝对路径（已 d5d48dd 修复后补 `http://127.0.0.1:${Bc}` 前缀） | ✅ 与「落盘上传函数」一致 |
| `ri` | `App.js` **L1856** `async function ri(e,t={})` | **缩略图缓存**：内存 Map<TTL=300s> + 并发锁，返回 thumbnailUrl | ✅ 一致（ARCHITECTURE L2.4 引用正确） |
| `Xr` | `App.js` **L1802** `async function Xr(e,t={})` | **uploadToLocalTool（上传落盘核心）**：`ii`(L1888) 内部调用；`fetch(${Hr}/api/files/upload)` | ⚠️ **已纠偏（见 `符号冲突深度核查_DRAFT.md` 问题2）**：func-mapping L173 记 `Xr=openInTab` 是**错锚**——openInTab 真实在 **L43881**（组件内局部 `Xr = async e=>`），非 L1802。L1802 的 `function Xr` 实为 uploadToLocalTool。 |
| `Zr` | `App.js` **L1827** `async function Zr(e,t={})` | **uploadHelper 姐妹函数**（与 Xr=L1802 同组，落盘/处理） | ⚠️ **已纠偏（见 `符号冲突深度核查_DRAFT.md` 问题5）**：var-mapping 记 `Zr=logout` 指的是 **L43893**（组件内局部 `Zr=async()=>{...}` 清配置），非 L1827。L1827 另有一个同名上传组函数。两处 `Zr` 重名不同物，grep 须带行号。 |
| AI 生成派发（N 分支） | **L32490** `Jn = Y.useCallback(async(e,r,o,s,c,d)=>{...})` 局部 | **生图/生视频主回调**：L32987 `task_id 检测+分流` → L32990 异步轮询分支（`pollUrl=${R}/v1/tasks/${taskId}`，L33005 `zc(pollUrl)`）→ L33076 原同步解析 | ⚠️ **重大纠偏**：`Jn` 在 `func-mapping.txt` L65 被记为 `LogoIcon`（L89 `function Jn({` 组件），但**生图主回调是另一个同名局部变量 Jn（L32490）**——两个 `Jn` 重名不同物！T0.1 说「非 Jn」是对的，但真身是 L32490 的 `Jn` useCallback，不是某个未命名函数。ARCHITECTURE/TASKS 把生图入口写作 `Jn(~L32731)` 行号也偏（实际 useCallback 起 L32490，提交端点在 L32731 `I=${R}/v1/images/generations`）。 |

### 资源入库调用链（坐实）
```
L31426  i==='completed' → Ev().then(...)         // 生成完成触发 rescan
L42838  Sv(e)  → POST /api/resources/save        // 入库
L42857  wv(e)  → POST /api/resources/delete?id=  // 删（只DB）
L42883  Ev()   → POST /api/resources/rescan      // 扫盘入库
L43015  we()   → await Ev(); xv(...)             // 资源面板「同步」按钮（rescan+查询）
L43028  rescanThrottledSync → 3s 节流防循环
```

---

## 二、T0.2 核对 var-mapping.txt 易混项

| 项 | 实测 | 动作 |
|----|------|------|
| `Zr` 双身 | `Zr` 有**两处重名**：L1827 `async function Zr`=uploadHelper 姐妹（落盘/处理）；L43893 `Zr=async()=>{...}`=logout（重置配置，清本地数据）。var-mapping 记的 `Zr=logout` 指 L43893 那个 | ⚠️ **已纠偏（见 `符号冲突深度核查_DRAFT.md` 问题5）**：原写「L1827 确认 Zr 是 logout」**错误**，L1827 实为 uploadHelper，logout 是 L43893。TASKS 行34 旧文「Zr() 下载」**错误，须改**。 |
| `Sv`(大写) vs `sv`(小写) | `Sv`=资源入库(L42838)；`sv`=nodeCallbackFieldSet（var-mapping L178，不序列化字段 Set） | ✅ 厘清，大小写不同物 |
| `Hr` / `vv` | `Hr`=localToolStatusUrl `http://127.0.0.1:18080`（L1732 `var Hr=localEngineBase()`）；`vv`=localToolBaseUrl 同址（L42808 `var vv=LOCAL_ENGINE.base`） | ✅ 两处同源（均指向 18080），P2「统一 base URL」实际**已同源**，差异仅在 `localEngineBase()` 动态 vs `LOCAL_ENGINE.base` 常量，切换 USE_LOCAL_ENGINE=false 时才分歧（见 X3.3） |
| `B` / `R` / `ii` / `wv` / `ri` | ⚠️ **关键纠偏**：T0.2 把 `R`/`B` 当「模块2 资源面板上传/URL拖入回调」，实测它们是**局部函数且多处重名**：<br>• 资源面板区 L29159 `z=useCallback(await R(e)...)` 的 `R` 是**该组件内**读文件→FileReader 转 dataURL 的局部函数（约 L2914x，未收录）；<br>• L29188 `B=useCallback(...)` 同组件内处理拖放释放的局部回调；<br>• 另有 L23247 `function R(e)`（3D 导演台拖拽开始）、L23258 `function B(e)`（3D 目标模式切换）——**与资源面板无关**。<br>故 `R`/`B` 不能作为模块级符号补入映射表，须标注「组件局部，随组件隔离」。 | ⚠️ 修正 T0.2：资源面板"上传不入库"真实锚点是 L29159 的 `z`（paste/上传回调）只 `O(...)` 写内存 transitItems，**未调 Sv()/Ev()**，故刷新丢（P1）。TASKS 把 `z` 误记为 `R`。 |

---

## 三、对旧文档的纠正清单（回写前须改）

1. **TASKS.md 行34**：`Sv()` 保存前 `toAbsoluteFileUrl` 锚点 `App.js L43462` → 实测 `Sv` 在 **L42838**，L43462 已是别的逻辑，需重 grep。
2. **TASKS.md P1「R 回调 L29165–29166 上传后不调 Sv()」** → 实为局部 `z`（L29159）只写内存，符号名 `R` 是同组件内文件读取局部函数，非模块级。
3. **TASKS.md P1「B 回调 L29176 只存状态不下载」** → L29176 是 `source:'paste'` 赋值，URL 拖入落盘缺失真实锚点在 L29181–29184（paste 的 text/plain 分支只 `O([{url:r}])` 写内存）。
4. **ARCHITECTURE.md / TASKS.md「生图主回调 Jn(~L32731)」** → 真身是 L32490 `Jn=Y.useCallback` 局部（与 L89 `Jn=LogoIcon` 组件重名），提交端点 L32731 `I=${R}/v1/images/generations`。
5. **func-mapping.txt L65 `Jn = LogoIcon`** → 保留（L89 组件正确），但须加注「L32490 另有同名局部 Jn=生图主回调 useCallback，二者不同物」。

---

## 四、回写映射表的待办（T0.3）

- [ ] `func-mapping.txt` 追加：`we`(资源面板 rescan+查询 useCallback, L43015, 局部)、`ii`(上传落盘, L1888)、`ri`(缩略图缓存, L1856)、`Sv`(资源入库, L42838)、`wv`(资源删除, L42857)、`Ev`(rescan触发, L42883)
- [ ] `func-mapping.txt` `Jn` 条加注重名警示
- [ ] `var-mapping.txt` `R`/`B` 条加注「多处局部重名，资源面板区为组件内文件读取/拖放回调，非模块级」
- [ ] 纠正 TASKS.md / ARCHITECTURE.md 上述 5 处错锚（属 T4.1/T4.2，见 TASKS.md 末尾）

> 注意：本记录仅作审计字典，**不改 `App.js`**。回写映射表属文档维护，可后续执行。
