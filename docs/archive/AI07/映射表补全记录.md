# 映射表补全记录（AI07）

> 落实 `TASKS.md` T0 阻塞项：grep 实敲坐实核心混淆名真实身份与**当前行号**（2026-07-21 快照）。
> 旧文档（`ARCHITECTURE.md` / `FUNCTION_MAP.md` / `TASKS.md`）存在多处**错锚**，已逐条标注修正。
> 行号仍会漂移，后续引用以本记录为起点再 grep 复核。

---

## 1. 重大错锚修正（对照旧文档）

| 符号 | 旧文档说法（ARCHITECTURE / TASKS / FUNCTION_MAP） | 实敲结果（2026-07-21） | 结论 |
|------|--------------------------------------------------|------------------------|------|
| `Jn` | 生图主回调（L32731） | `function Jn({ size })` @ **L89**，返回 svg，是 **LogoIcon 品牌图标组件** | ❌ 旧文档错。`Jn` 非生图回调。生图派发逻辑另在 L32779 起的异步块（外层为某节点 handleGenerate，未用 `Jn` 名） |
| `Xr` | `openInTab`（活动标签打开 URL） | `async function Xr(e, t={})` @ **L1802**，POST `${Hr}/api/files/upload`，是 **blob 上传到 localTool** | ❌ 旧 TASKS T0 说"Xr=openInTab"错；实为上传函数 |
| `Zr` | `logout`（重置配置） | `async function Zr(e, t={})` @ **L1827**，POST `${Hr}/api/files/upload`(fileUrl)，是 **URL 上传到 localTool** | ❌ 旧 TASKS T0 说"Zr=logout"错；实为上传函数。logout 真身是另一个符号（见 §3） |
| `we` | rescanResources 主函数（L42759/L42980） | **存在两个作用域不同的 `we`**：① `we = (t,n=false)=>{...}` @ **L4176** = prompt 文本框 **@提及插入**；② `we = Y.useCallback(async()=>{ await Ev(); await xv(...) })` @ **L43015** = 组件层 **rescan 主函数**（调 `Ev` L42883 + 刷新列表）。旧文档把两者混为一谈 | ⚠️ 旧 ARCHITECTURE 把 `we` 单称作 rescan 不严谨；rescan 真入口是 `Ev`(L42883)，组件层 `we`(L43015) 是其封装。L4176 的 `we` 是 @提及，与 rescan 无关 |
| `Ev` | rescan 前端定义（行号待 grep） | `async function Ev()` @ **L42883**，POST `${vv}/api/resources/rescan`，是 **rescan 唯一真函数** | ✅ 确认（旧文档行号漂移，现 L42883） |
| `Sv` | 资源入库（大写，与 `sv` 混淆） | `async function Sv(e)` @ **L42838**，POST `${vv}/api/resources/save` | ✅ 确认。混淆变量 `sv`(小写) 是别处 nodeCallback 字段集，需注意大小写区分 |
| `wv` | 资源删除 | `async function wv(e)` @ **L42857**，POST `${vv}/api/resources/delete?id=` | ✅ 确认（旧文档 L42778 已漂移到 L42857） |
| `ii` | 上传/下载（疑似） | `async function ii(e, t={})` @ **L1888**，是 **uploadFile 统一入口**，字符串 URL 直接返回、File/Blob 调 `Xr` 落盘 | ✅ 确认。注意 `ii` 对 http(s) 字符串**不下载**直接透传（CDN 直链风险，见问题清单） |
| `Oa` | 去登录核心（L3511） | `function Oa()` @ **L3543**，返回 `localStorage.getItem(Ca) \|\| 'local-mode-token'`，是 **本地模式 token** | ✅ 确认（行号 L3511→L3543 漂移） |
| `R`（资源面板） | 资源面板 file input 上传回调 | `R = Y.useCallback(async e => {...})` @ **L29149**，是 **File→base64 读取**（FileReader），非入库；真正入库走 `z`(L29159)→`O`(资源 setter) | ⚠️ 旧 TASKS 说"R(files) 上传后不调 Sv()"需重核：`R` 只读数，入库在 `z`/`O` 路径 |
| `B`（资源面板） | 资源面板 URL 拖入回调 | `B = Y.useCallback(async e => {...})` @ **L29188**，是 **onDrop 处理器**，内部 `R(e.dataTransfer.files)` | ⚠️ 旧 TASKS 行号 29160–29179 已漂移到 29188 起 |

---

## 2. 当前行号坐实表（grep 实敲）

| 符号 | 当前行号 | 真实语义 | 端点/行为 |
|------|----------|----------|-----------|
| `Jn` | L89 | LogoIcon 品牌图标组件（svg） | 纯 UI，无网络 |
| `Oa` | L3543 | 去登录 token 获取 | `localStorage` 读 `Ca` 键 |
| `Aa` | L3552 | **真·logout**：移除 `auth_token` | 导出 `clearAuthToken`(L38324) |
| `ka` | L3547 | 登录/保存 token | `localStorage.setItem(Ca, e)` |
| `ji` | L3181 | 去登录态接管（本地模式分支） | `!t \|\| t==='local-mode-token'` → 清权益 |
| `Da` | L3540 | 清除"记住的登录凭据" `wa` | 非 token，仅清记住密码 |
| `Xr` | L1802 | blob 上传 localTool | POST `/api/files/upload` |
| `Zr` | L1827 | URL 上传 localTool | POST `/api/files/upload`(fileUrl) |
| `ii` | L1888 | uploadFile 统一入口 | 调 `Xr`；字符串 URL 透传 |
| `Ev` | L42883 | rescan 资源 | POST `/api/resources/rescan` |
| `xv` | L42821 | 资源列表查询 | GET `/api/resources?...` |
| `Sv` | L42838 | 资源入库 upsert | POST `/api/resources/save` |
| `Cv` | L42851 | 切换收藏 | 调 `Sv({...isFavorite})` |
| `wv` | L42857 | 资源删除 | POST `/api/resources/delete?id=` |
| `Tv` | L42866 | 资源清空 | POST `/api/resources/clear` |
| `we`(①) | L4176 | prompt @提及插入 | 文本框插入 `@name` |
| `we`(②) | L43015 | 组件层 rescan 主函数 | 调 `Ev()`(L42883) + `xv()`(L42821) 刷新列表 |
| `R`(资源面板) | L29149 | 文件→base64 读取 | FileReader |
| `B`(资源面板) | L29188 | onDrop 处理器 | 调 `R` |
| `uploadFile`(useLocalTool hook) | L19098 | hook 内落盘回调 | POST `http://127.0.0.1:${Bc}/api/files/upload` |

> 注：`Bc = LOCAL_ENGINE.port`(18080) 在 `config.js`；`Hr = localEngineBase()`(L1732)、`vv = 'http://127.0.0.1:18080'`(L42808 `var vv = LOCAL_ENGINE.base`，硬编码残留)。

---

## 3. 待补/未坐实项（2026-07-21 续 · 见 `待核项补遗.md`）

- **logout 真身**（TODO-2）：✅ **已坐实**（2026-07-21 续）。真身 = `Aa`(L3552) 移除 `auth_token`，导出 `clearAuthToken`(L38324)，由 401 响应 L38256 触发，`ji`(L3181) 本地模式分支接管登出态。旧称 `Zr=logout` 双重错误（`Zr` L1827 是 URL 上传）。详见 `待核项补遗.md` TODO-2（补）。
- **生图派发外层函数名**（TODO-1）：⚠️ **无法坐实**。L32779 块外层反编译嵌套深，未暴露稳定名；引用统一用「`App.js:L32779 起生图派发块`」，不臆造名称。
- **小写 `sv`**（TODO-3）：⚠️ **部分说明**。小写 `sv` 0 稳定匹配；统一引用大写 `Sv`(L42838)，不引用小写，避免大小写歧义。
- ✅ **已坐实并移出**：`ei`/`ti`(GAS 云同步) → L43950 / L43974；`spawnable` 集 → L4141（13 种）；`Th`(Director3D) → L31141；统一同步 `/files/` 判断 → L44369；性能策略 `pan-performance-mode` L31231 / minZoom .05 L37149 / selectionKeyCode L37165。

---

## 4. 下游文档修正建议（回填）

1. **`ARCHITECTURE.md`**：
   - L1.3 表 `Jn ~L32731 生图主回调` → 改为「`Jn`(L89) 是 LogoIcon，非生图；生图派发见 L32779 块」。
   - L2.4 / L2.5 中 `we` 当作 rescan / `Xr` 当作 openInTab / `Zr` 当作 logout 的表述 → 全部按 §1 更正。
   - `Sv()`/`Zr()`/`Xr()` 上传锚点 → 真名回填（`Xr`=blob 上传、`Zr`=URL 上传、`ii`=uploadFile 入口）。
2. **`TASKS.md`**：
   - 行34 `Zr()` 下载 → 实为 `Zr`=URL 上传；"R(files) 上传后不调 Sv()" 需按 §1 `R`/`z`/`O` 路径重核。
   - T0 阻塞项已部分推翻（Jn 确为 LogoIcon；但 Xr/Zr 的"openInTab/logout"判定反而是错的）。
3. **`FUNCTION_MAP.md`**：§0.5 `Jn = 生图主回调` 表述错误，应改为 LogoIcon。
