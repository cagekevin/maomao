# 最终审计报告（AI07 · 猫猫AI画布 V1 文档一致性审计）

> 审计周期：2026-07-21（多轮，含最终自审校准）
> 审计对象：V1 引擎 `src/_engine/App.js`(~46000行) + `config.js` + `localTool/`(`:18080`) + `apimart-gateway/`(`:9004`)
> 产出位置：**全部限于 `docs/AI07/`**（红线：不向项目其他目录写文件）
> 方法：grep 实敲行号 + `docs/AI01/check-doc-citations.cjs` 门3机器校验

---

## 0. 自审结论（本轮审核最关键的发现）

**文档内部存在行号漂移，已全量重核并修正。** 在最终自审阶段，对全部 AI07 文档做了与源码的二次交叉核对，发现并修正了以下漂移：

| 符号/项 | 文档原行号 | 源码实际行号 | 影响文档 |
|---------|-----------|-------------|---------|
| `vv`（硬编码 base） | L42691 | **L42808** | 映射表/模块1/模块2/交叉流/问题清单 |
| `images_generations` | L591 | **L592** | 模块3/交叉流/补遗 |
| `videos_generations` | L641 | **L642** | 模块3/交叉流/补遗 |
| `get_task` | L873 | **L874** | 模块3/交叉流/补遗 |
| `confirm_task` | L882 | **L883** | 模块3/问题清单/补遗 |
| 生图轮询 `deadline` | L33097 | **L32997** | 模块3/交叉流 |
| 生图轮询 `pollInterval` | L33098 | **L32998** | 模块3/交叉流 |
| `zc`（fetch 封装）定义 | L33007(误) | **L19001** | 模块3 |
| `mutiwindow-task-completed` 监听 | L31426 | **L31428** | 模块2/交叉流 |
| `pan-performance-mode` CSS | L31231 | **L31232** | 映射表注/补遗 |

> 所有修正已落地，重跑校验 **PASS 11 / WARN 0 / FAIL 0**。

**未漂移（已确认准确）的关键锚点**：`Jn`(L89) / `Xr`(L1802) / `Zr`(L1827) / `ii`(L1888) / `Oa`(L3543) / `Aa`(L3552) / `Sv`(L42838) / `Ev`(L42883) / `xv`(L42821) / `wv`(L42857) / `Ir`(①L1695) / `Lr`(①L1700) / `Fr`(①L1694) / `Ir`(②L37137) / `Lr`(②L37138) / 画布处理器注册 L37125-37139 / `spawnable`(L4141) / `director3dNode:Th`(L31141) / 统一同步 `/files/` 判断(L44369) / `minZoom .05`(L37149) / `selectionKeyCode`(L37165)。

---

## 1. 审计产出清单（共 17 份文件，全部在 docs/AI07/）

| 文件 | 内容 | 状态 |
|------|------|------|
| `README.md` | 导航 + 红线 + 信任链 + 产出清单 | ✅ |
| `映射表补全记录.md` | T0 阻塞项坐实 + 当前行号表 + 下游修正建议 | ✅ |
| `模块1-初始化与配置层.md` | 配置/存储/鉴权层 | ✅ |
| `模块2-本地数据层与Rescan.md` | localTool 交互/rescan/缩略图 | ✅ |
| `模块3-AI生成与网关层.md` | 生图/视频派发 + 轮询 + 落盘 | ✅ |
| `模块4-画布引擎与节点.md` | ReactFlow/节点/3D导演台 | ✅ |
| `交叉流审计.md` | 6 条端到端缝合 | ✅ |
| `审计问题清单.md` | DOC错锚8 + 多义3组 + 代码Bug11 + TODO8 | ✅ |
| `待核项补遗.md` | TODO-1~8坐实/定论 + 接口对齐审计 | ✅ |
| `校验报告-*.md`（7份） | 门3机器校验报告 | ✅ |

---

## 2. 核心审计发现

### 2.1 文档错锚（DOC 类 · 8 项，均已坐实正确事实）

| 编号 | 旧文档错误 | 正确事实 |
|------|-----------|---------|
| DOC-1 | `Jn`=生图主回调(L32731) | `Jn`(L89)=LogoIcon；生图见 L32779 块 |
| DOC-2 | `Xr`=openInTab、`Zr`=logout | `Xr`(L1802)=blob上传、`Zr`(L1827)=URL上传；**logout 真身 `Aa`(L3552)** |
| DOC-3 | `we` 单作 rescan | `we` 双义：L4176(@提及)/L43015(rescan封装)；rescan 真函数 `Ev`(L42883) |
| DOC-4 | `Oa` L3511、`V`=登录态 | `Oa`(L3543)；`V`(L31317)局部默认false；登录态由 `Oa()` 动态决定 |
| DOC-5 | `Q` L1260、`Hr` L1700 | `Q`(L1476)、`Hr`(L1732) |
| DOC-6 | 画布 onDrop `Z`/L36215 | 注册于 L37138（`Ir`/`Lr` 另有分辨率/路径义） |
| DOC-7 | `Zr()` 下载 | `Zr`(L1827)=URL上传 |
| DOC-8 | 上传锚点 `Sv`/`Zr`/`Xr` 混写 | 真上传 `Xr`/`Zr`/`ii` |

### 2.2 混淆名多义污染（3 组，实锤高风险）

`we`(L4176 vs L43015)、`Ir`(L1695 vs L37137)、`Lr`(L1700 vs L37138) —— 旧文档未区分，引用必须带**行号+作用域**。

### 2.3 代码 Bug / 风险（11 项）

- **P0-1** URL 破图（host 硬编码 18080）：rescan 侧已修，上传侧待补
- **P0-2** 中文名 Latin1 乱码（`sanitizeFilename` L117 吞中文）：待实机验证
- **P1-2** URL 拖入不落盘（`B`→`R`，paste 分支仅存 state）
- **P1-3** file input 上传不自动入库（`R`→`z`→`O`，未调 `Sv()`）
- **P2-5** base URL 不一致（`Hr` L1732 vs `vv` L42808）
- **P2-6** 删除不删磁盘（`wv`+后端 L202 仅删 DB）
- **P2-7** 孤儿清理只清 `source='local-tool'`
- **P2-8** 缩略图伪复制（`copyFileSync` L137）
- **NET-1** pending_confirmation：网关已加 `confirm_task`(L883)+watcher(L672) 兜底，**已缓解**
- **INT-1** localTool `/api/resources/batch-save`(L187/L190) **死端点**：App.js 资源层无对应调用

### 2.4 TODO 状态（8 项全部关闭）

| TODO | 内容 | 结论 |
|------|------|------|
| 1 | 生图派发外层函数名 | ⚠️ **定论无法坐实**：组件级内联 async 回调，非顶层命名函数（L30000-32779 无对应 `async function` 声明） |
| 2 | logout 真身 | ✅ `Aa`(L3552) 移除 `auth_token`，L38256(401)触发，`ji`(L3181)接管 |
| 3 | 小写 `sv` | ⚠️ 0 稳定匹配，统一用大写 `Sv`(L42838) |
| 4 | `ei`/`ti` GAS | ✅ L43950 / L43974 |
| 5 | `spawnable` 集 | ✅ L4141，13 种（旧16种含 text/audio/textConcat 错误） |
| 6 | `Th` Director3D | ✅ L31141 |
| 7 | 统一同步 `/files/` | ✅ effect L44352-44425，判断 L44369 |
| 8 | 性能策略 | ✅ `pan-performance-mode` L31232、`minZoom .05` L37149、`selectionKeyCode` L37165 |

### 2.5 接口对齐审计（新增）

- **App.js ↔ 网关**：✅ 完全对齐。`/v1/images/generations`(L592)、`/v1/tasks/{id}`(L874)、`makeConfirmAdapter` 调 `/v1/tasks/{id}/confirm`(L883) 与网关一一对应，A-C6 红线合规。
- **App.js ↔ localTool**：✅ 主流端点对齐；⚠️ 发现 `resources/batch-save`(L187/L190) 为死端点（INT-1）。

---

## 3. 红线遵守声明

- ✅ 所有 17 份产出文件**仅**位于 `docs/AI07/`，未触碰任何源文件。
- ✅ 未修改 `App.js` / `config.js` / `localTool` / `apimart-gateway` / `ARCHITECTURE.md` / `FUNCTION_MAP.md` / `TASKS.md`。
- ✅ 未删除 `.codebuddy` 及任何项目数据。
- ✅ V2 仅作只读归档参考，未接未写。

---

## 4. 对下游文档的修正建议（回填需用户授权）

1. **`ARCHITECTURE.md`**：修正 DOC-1/2/3/5/6/8；补 `we`/`Ir`/`Lr` 多义说明；`Hr` L1700→L1732；`vv` 标注 L42808。
2. **`FUNCTION_MAP.md`**：`Jn` 改 LogoIcon；`Oa`/V/Ne 登录态表述；`Zr` 改 URL 上传。
3. **`TASKS.md`**：NET-1 标已缓解；P1-3 机制修正；补 INT-1（死端点）；T0 阻塞项部分推翻。
4. **`CLAUDE.md` §3.1**：建议「引用必须带行号」后追加「+ 作用域说明」（本次 3 组多义实锤）。

> 以上回填**未执行**（超出 `docs/AI07/` 边界），待用户明确授权。

---

## 5. 校验结果

```
check-doc-citations.cjs (Gate-3) 全量运行
Total 11 | PASS 11 | WARN 0 | FAIL 0
```

所有文档引用均带可验证 `file:line`，无幻觉锚点。

---

## 6. 审计定论

本轮对猫猫AI画布 V1 引擎做了完整的文档-代码一致性审计：

1. **推翻了旧文档 8 处重大错锚**，其中 `Jn`/`Xr`/`Zr` 三处核心混淆名身份全部纠正，`logout` 真身（`Aa`）首次坐实。
2. **实锤 3 组混淆名多义污染**，验证了 CLAUDE.md 红线「引用带行号」的必要性，并建议升级为「行号+作用域」。
3. **发现 1 个接口不对称（死端点 INT-1）** 和 **1 个已缓解项（NET-1）**，补充了旧 TASKS 未记录的网关修复。
4. **8 个 TODO 全部关闭**（7 坐实 + 1 定论），其中 TODO-1 经深层反编译分析确认为"非顶层命名函数"而非"未查到"。
5. **最终自审发现并修正了行号漂移**（10 处），确保文档与 2026-07-21 源码快照一致。

审计产出完备、可机器校验、红线零违规。建议下一步在用户授权后将修正回填至 `ARCHITECTURE.md` / `FUNCTION_MAP.md` / `TASKS.md`，并对 INT-1 死端点决策（前端补调用 or 文档标注预留）。
