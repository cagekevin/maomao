# 待核项补遗（AI07 · 2026-07-21 续）

> 承接 `审计问题清单.md` 第四节「待核/待补项」与 `映射表补全记录.md` 第三节。
> 本次对 TODO-3~8 进行 grep 坐实，结果如下。所有条目均 grepped 实敲，未做代码修改，文件仅落于 `docs/AI07/`。

---

## A. 已坐实条目（TODO-3 / 4 / 5 / 6 / 7 / 8）

### TODO-4 · `ei` / `ti`（GAS 云同步）行号 —— 已坐实 ✅

| 符号 | 行号 | 定义 | 行为 |
|------|------|------|------|
| `ei` | **L43950** | `ei = async () => {` | `CloudSyncEngine.push`，上行本地资源到 GAS |
| `ti` | **L43974** | `ti = async () => {` | `CloudSyncEngine.pull`，拉取 GAS 云同步 |

- 依赖 `gasUrl: GAS_CLOUD_SYNC_URL`（L43899 附近定义，`config.js` 导出）。
- 旧 `FUNCTION_MAP` 称 ~L43760，实际在 **L43950 / L43974**，行号已漂移约 +190。

### TODO-5 · `spawnable` 节点类型集 —— 已坐实 ✅

- 定义：`new Set([...])` @ **L4141**。
- 实敲集合（共 **13** 种）：
  `promptNode, imageNode, imageBoxNode, videoNode, sd2VideoNode, discountVideoNode, gridSplitNode, gridMergeNode, cropNode, urlToImageNode, fileToUrlNode, panoramaNode, videoExtractNode`
- **旧文档声称 16 种（含 `textNode` / `audioNode` / `textConcatNode`）错误**：这三种不在 `spawnable` 集中，不能从画布节点菜单直接生成。

### TODO-6 · `Th`（Director3DNode）定义 —— 已坐实 ✅

- 注册：`director3dNode: Th` @ **L31141**（节点类型映射表）。
- 引用：`[Director3D]` @ L28289、`[Director3DNode]` @ L28449（节点实现区）。
- 结论：`Th` = Director3D 节点实现，可在 `spawnable` 之外的特殊节点表注册。

### TODO-7 · 统一同步 effect 的 `/files/` 判断行 —— 已坐实 ✅

- effect 区间：**L44352 – L44425**。
- 关键判断 @ **L44369**：
  ```js
  if (!e || typeof e != 'string' || e.startsWith('/files/') || e.startsWith('http://127.0.0.1') && /\/files\/tasks(\/|$)/.test(e)) return e;
  ```
- 含义：本地落盘路径（`/files/...`）与 127.0.0.1 任务文件直链被豁免同步改写，避免把本地资源 URL 误当远程覆盖。

### TODO-8 · 性能策略 `pan-performance-mode` / `is-large-canvas` —— 已坐实 ✅

| 项 | 行号 | 证据 |
|----|------|------|
| `pan-performance-mode` CSS 类 | **L31231** | 画布性能模式样式类定义 |
| `minZoom` | **L37149** | `minZoom: .05`（最小缩放 0.05） |
| `selectionKeyCode` | **L37165** | `selectionKeyCode: [Control, Meta]`（框选键 = Ctrl/Meta） |
| `panOnDrag` / `selectionOnDrag` | **L37163-37164** | `panOnDrag: true` / `selectionOnDrag: false`（拖拽平移、非框选） |

- 结论：性能策略走 ReactFlow 原生 `pan-performance-mode` + `minZoom .05`，无自建节流层；框选需按住 Ctrl/Meta。

### TODO-2（补）· logout 真身 —— 已坐实 ✅（追加于二次审计）

旧文档称 `Zr=logout`，但 `Zr`(L1827) 实为 URL 上传，属双重错误。本次定位到真正的登出链路：

| 符号 | 行号 | 语义 | 证据 |
|------|------|------|------|
| `Aa` | **L3552** | **真·logout**：`localStorage.removeItem(Ca)` + `Q.remove(Ca)`，移除 `auth_token` | 导出名 `clearAuthToken: Aa`(L38324) |
| `ka` | L3547 | 登录/保存 token：`localStorage.setItem(Ca, e)` | 与 `Aa` 成对 |
| `Ca` | L3524 | 常量 `auth_token`（token 键名） | `Oa`(L3543) 读此键 |
| `ji` | L3181 | 去登录态接管：当 `!t \|\| t==='local-mode-token'` → `xi=[]; bi.entitlements=undefined; Di()` | 本地模式分支即"登出态" |
| `Da` | L3540 | 清除"记住的登录凭据" `wa`(`remembered_login_credentials`) | 仅清记住密码，非 token |
| 触发点 | **L38256** | `u.status === 401 && !a && Aa()` | 401 未授权自动登出 |

- **结论**：logout 真身 = `Aa`(L3552)，由 401 响应（L38256）触发，并经 `ji`(L3181) 本地模式分支进入登出态。与 `Zr` 毫无关系。
- `Ta`(L3526)/`Ea`(L3537)/`Da`(L3540) 管理的是"记住密码"(`wa`)，非登出核心。

### TODO-3 · `sv`（小写）混淆变量语义 —— 部分说明

- 独立 grep `sv`（小写）作变量名返回 **0 稳定匹配**（多为字符串/属性片段误命中）。
- 现状：`Sv`（大写，L42838 资源入库）已确证；小写 `sv` 旧文档称「nodeCallback 字段集」，本轮仍未坐实稳定定义，降级为「暂不引用」。
- **建议**：凡引用一律用大写 `Sv`(L42838)，避免大小写歧义。

---

## B. 仍无法坐实条目（TODO-1 / TODO-2）

| 编号 | 内容 | 结论 | 说明 |
|------|------|------|------|
| TODO-1 | 生图派发外层函数名 | **无法坐实（已定论）** ⚠️ | 见本节末尾「TODO-1 最终结论」。该生图主逻辑是**组件级内联 async 回调**，非顶层命名函数，故无稳定外层名。审计引用统一用「`App.js:L32779 起生图派发块`」描述，不臆造名称。 |
| TODO-2 | logout 真身（旧称 `Zr=logout` 已证伪） | **已坐实 ✅** | 真身 = `Aa`(L3552) 移除 `auth_token`，导出 `clearAuthToken`(L38324)，由 401 响应 L38256 触发，`ji`(L3181) 本地分支接管登出态。详见 A 节「TODO-2（补）」。 |

---

## B-2. TODO-1 最终结论（生图派发外层函数名）

经二次深层追溯（L30000–L33000 区间全部 `async function` 声明 + 函数体边界分析），结论如下：

- L32779 `if (N) {...}` 生图发起块 与 L32986 `if (N) {...}` 响应分流块，同属**一个巨型生图 async 函数体**。
- 该函数在 30000–32779 区间内**无顶层 `async function` 命名声明**；最近的上层命名函数 `Ri`(L3269) 仅负责拉取模型列表（`/public/platform/models`），于 L3280 前后即 return，**不包含** L32779 生图块。
- 生图主逻辑实为**某个 React 组件内的内联 `async () => {...}` 或 `useCallback`**，其外层函数名在反编译产物中未被保留为稳定顶层符号。
- 函数体内含局部 `makeConfirmAdapter`(L32960) / `requestConfirm`(L32976) / `markNeedsConfirm`(L32980) 等嵌套闭包，进一步证明其为组件级回调。
- **定论**：TODO-1 无法坐实，非"未查到"而是"它本就不是顶层命名函数"。审计引用统一用「`App.js:L32779 起生图派发块`」，不臆造 `handleGenerate` 等名称。

---

## B-3. 接口对齐审计（App.js ↔ localTool ↔ apimart-gateway）

### B-3.1 网关契约对齐（App.js ↔ apimart-gateway/main.py）✅

| 契约点 | App.js 侧 | 网关侧 (main.py) | 一致性 |
|--------|-----------|------------------|--------|
| 生图提交 | `${R}/v1/images/generations`(L32796) | `@app.post("/v1/images/generations")` L592 | ✅ |
| 图编辑 | `${R}/v1/images/edits`(L32788) | `@app.post("/v1/images/edits")` L595 | ✅ |
| 任务轮询 | `GET ${R}/v1/tasks/${taskId}`(L33005) | `@app.get("/v1/tasks/{task_id}")` L874 | ✅ |
| **确认回调** | `zc(\`${gatewayBase}/v1/tasks/${taskId}/confirm\`)`(L32963, `makeConfirmAdapter`) | `@app.post("/v1/tasks/{task_id}/confirm")` L883 | ✅ **A-C6 对齐** |
| 后台兜底 | `AbortController` + 转入后台(L32847) | `_background_webhook_watcher` L672 | ✅ 已缓解 pending_confirmation |
| 15min deadline | `Date.now() + 9e5`(L32997) | 无硬 deadline，靠 webhook | ✅ 前端自建兜底 |

> 结论：网关契约与 CLAUDE.md 红线「异步轮询 + 确认」完全一致，旧 TASKS 标"限制"的 pending_confirmation 已缓解。

### B-3.2 localTool 契约对齐（App.js ↔ localTool/src）⚠️ 发现死端点

| 端点 | localTool (index.ts) | App.js 调用方 | 一致性 |
|------|---------------------|---------------|--------|
| `/api/files/upload` | L135 | `Xr`(L1802)/`Zr`(L1827)/`ii`(L1888) | ✅ |
| `/api/resources`(GET) | L181 | `xv`(L42821) | ✅ |
| `/api/resources/save` | L184 | `Sv`(L42838)/`Cv`(L42851) | ✅ |
| `/api/resources/delete` | L190 | `wv`(L42857) | ✅ |
| `/api/resources/clear` | L193 | `Tv`(L42866) | ✅ |
| `/api/resources/rescan` | L196 | `Ev`(L42883) | ✅ |
| `/api/tasks/batch-save` | L167 | `Y_`(L41624 taskStore) | ✅ |
| **`/api/resources/batch-save`** | **L187** | **无对应调用** | ⚠️ **死端点** |

- **新发现（接口不对称）**：localTool 暴露 `/api/resources/batch-save`(L187 → `handleResourcesBatchSave`)，但 App.js 资源层只有单条 `Sv`(L42838) 与 `Cv`(L42851)，**全局未检索到对 `resources/batch-save` 的 fetch 调用**。该端点为预留/未接线功能，或待实现批量入库（如多图一次生成后批量落库）。建议：要么前端补调用，要么文档标注为"预留端点"，避免误认为已支持批量入库。
- 区分澄清：`/api/tasks/batch-save`(L167) 是 **tasks 域**（任务持久化），与 `resources/batch-save`(L187) 是 **resources 域**（资源批量），二者独立，不混淆。

### B-3.3 错锚补充

- 旧 `ARCHITECTURE` 称生图主回调 = `Jn`(L32731) ❌（实为 LogoIcon L89）。生图派发块确实在 L32779 附近，但函数名非 `Jn`，且非顶层命名（见 B-2）。

---

## C. 对 `审计问题清单.md` / `映射表补全记录.md` 的修订建议（仅记录，回填入下游需用户授权）

1. **TODO-1**：定论为「组件级内联回调，无顶层名，无法坐实」，引用统一用 `App.js:L32779 起生图派发块`。
2. **TODO-2**：已坐实 → `Aa`(L3552) 真·logout，下游 ARCHITECTURE/TASKS 的 `Zr=logout` 表述需推翻。
3. **TODO-3**：小写 `sv` 不引用，统一用 `Sv`(L42838)。
4. **TODO-4/5/6/7/8**：已坐实，行号见 A 节。
5. **新增接口发现**：`localTool` 的 `/api/resources/batch-save`(L187) 为死端点，建议下游文档标注"预留"；或后续前端补 `批量 Sv` 调用。
6. 下游文档（`ARCHITECTURE.md` / `FUNCTION_MAP.md` / `TASKS.md`）若需回填，须经用户明确授权（红线：仅可在 `docs/AI07/` 建文件）。

---

## D. 本轮 grep 命令留痕（可复现）

```text
grep "ei = async () => {"      App.js  → L43950
grep "ti = async () => {"      App.js  → L43974
grep "new Set(["              App.js  → L4141 (spawnable)
grep "director3dNode: Th"     App.js  → L31141
grep "pan-performance-mode"   App.js  → L31231
grep "minZoom"                App.js  → L37149
grep "selectionKeyCode"       App.js  → L37165
grep "startsWith('/files/')"  App.js  → L44369
```
