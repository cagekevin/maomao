# 审计问题清单（AI07）

> 汇总本轮（2026-07-21）审计发现的可疑 Bug / 错锚 / 待核项。
> 对照 `TASKS.md` P0–P2，并补充文档一致性问题（DOC 类）。
> 所有条目均 grep 实敲，未做代码修改。

---

## 一、文档错锚（DOC 类 · 高优先回填）

| 编号 | 文档位置 | 错误 | 正确事实（file:line） | 严重度 |
|------|----------|------|----------------------|--------|
| DOC-1 | ARCHITECTURE L1.3 / FUNCTION_MAP §2 | `Jn` 是生图主回调(L32731) | `Jn`(L89)=LogoIcon 组件，非生图；生图派发见 L32779 块 | 高 |
| DOC-2 | TASKS T0 / ARCHITECTURE | `Xr`=openInTab、`Zr`=logout | `Xr`(L1802)=blob 上传、`Zr`(L1827)=URL 上传 | 高 |
| DOC-3 | ARCHITECTURE L2.4/L2.5 | `we` 单称作 rescan 主函数 | `we` 有2义：L4176(@提及) / L43015(rescan 封装)；rescan 真函数 `Ev`(L42883) | 高 |
| DOC-4 | FUNCTION_MAP §1 | `Oa` L3511、`V`=L31251 登录态 | `Oa`(L3543)；`V`(L31317)是局部默认 false，`Ne` 多义，登录态由 `Oa()` 动态决定 | 中 |
| DOC-5 | ARCHITECTURE L1.1/L1.4 | `Q`(StorageManager) L1260、`Hr` L1700 | `Q`(L1476)、`Hr`(L1732) | 中 |
| DOC-6 | ARCHITECTURE L1.2 / FUNCTION_MAP §L1.2 | 画布 onDrop `Z`/L36215、`Ir`/`Lr`=onDragOver/onDrop | 画布处理器注册于 L37124-37140（`Ir` L37137 onDragOver、`Lr` L37138 onDrop）；`Ir`/`Lr` 另有分辨率/路径函数义 | 中 |
| DOC-7 | TASKS 行34 | `Zr()` 下载 | `Zr`(L1827)=URL 上传，非下载 | 中 |
| DOC-8 | ARCHITECTURE L2.5 | 上传锚点 `Sv()`/`Zr()`/`Xr()` 混写 | 真上传 `Xr`(L1802)/`Zr`(L1827)/`ii`(L1888) | 中 |

---

## 二、混淆名多义污染（新发现 · 高风险）

本轮实锤 **3 组同名不同作用域变量**，旧文档未区分，引用极易误读：

| 符号 | 义①(file:line) | 义②(file:line) | 影响 |
|------|----------------|----------------|------|
| `we` | @提及插入 L4176 | rescan 封装 L43015 | 旧文档把 rescan 全归因 `we` 误导 |
| `Ir` | 分辨率档位 L1695 | 画布 onDragOver L37137 | 旧文档 `Ir`=onDragOver 仅对画布层正确 |
| `Lr` | URL 路径归一化 L1700 | 画布 onDrop L37138 | 同上 |

> 建议：后续所有混淆名引用必须带 **行号 + 作用域说明**（CLAUDE.md 红线 §3.1 已强调，本次实锤）。

---

## 三、代码 Bug / 风险（对照 TASKS P0–P2）

| 编号 | 等级 | 问题 | 证据 | 状态 |
|------|------|------|------|------|
| P0-1 | P0 | URL 格式不统一破图（host 硬编码 18080） | rescan 已补全绝对路径(resources.ts L31)；`/api/files/upload` 返回相对路径(files.ts L42) | 部分已修，上传侧待补 |
| P0-2 | P0 | 中文目录/文件名 Latin1 乱码 | `sanitizeFilename`(files.ts L117)吞中文；Windows 读盘乱码待验 | 待核（未跑实机） |
| P1-2 | P1 | 资源面板 URL 拖入不落盘 | onDrop `B`(L29188)→`R`(L29149)；纯 URL 走 paste `O([{url,source:'paste'}])`(L29181) 仅存 state | 确认 |
| P1-3 | P1 | file input 上传不自动入库 | `R`(L29149)读 base64→`z`(L29159)→`O`(state)，未调 `Sv()` 写 DB | 确认（机制与旧文档措辞略异） |
| P2-5 | P2 | base URL 不一致（Hr vs vv） | `Hr`(L1732)按开关；`vv`(L42808)硬编码 18080，`USE_LOCAL_ENGINE=false` 时文件走错端口 | 确认 |
| P2-6 | P2 | 资源删除不删磁盘 | `wv`(L42857)+后端 L202 仅删 DB | 确认 |
| P2-7 | P2 | 孤儿清理只清 source='local-tool' | resources.ts L123 | 确认 |
| P2-8 | P2 | 缩略图伪复制 | files.ts L137 copyFileSync | 确认 |
| NET-1 | 已缓解 | pending_confirmation 卡轮询 | 网关 `confirm_task`(L883)+`_background_webhook_watcher`(L672) 兜底；App.js `makeConfirmAdapter`(L32960) 调 `/v1/tasks/{id}/confirm` 对齐 | 旧 TASKS 标"限制"，现已缓解，文档待更新 |
| INT-1 | 低 | localTool `/api/resources/batch-save`(L187) 死端点 | App.js 资源层仅有单条 `Sv`(L42838)/`Cv`(L42851)，全局未检索到对该端点的 fetch 调用 | 确认（预留/未接线，建议标注或补调用） |

---

## 四、待核/待补项（本轮未坐实）

| 编号 | 内容 | 结果（2026-07-21 续） |
|------|------|----------------------|
| TODO-1 | 生图派发外层函数名 | ⚠️ **无法坐实（已定论）**：生图主逻辑是组件级内联 async 回调，非顶层命名函数（30000–32779 区间无对应 `async function` 声明，外层 `Ri` L3269 仅拉模型列表）。引用统一用「App.js:L32779 起生图派发块」，不臆造名称 |
| TODO-2 | logout 真身（旧称 Zr=logout 已证伪） | ✅ **已坐实**：真身为 `Aa`(L3552) 移除 `auth_token`，导出 `clearAuthToken`(L38324)，由 401 响应 L38256 触发，`ji`(L3181) 本地分支接管登出态 |
| TODO-3 | `sv`(小写) 混淆变量语义 | ⚠️ **部分说明**：小写 `sv` 0 稳定匹配；统一引用大写 `Sv`(L42838)，不引用小写 |
| TODO-4 | `ei`/`ti`(GAS 云同步) 行号 | ✅ **已坐实**：`ei` L43950 / `ti` L43974（旧称 L43760 漂移） |
| TODO-5 | `spawnable` 节点类型集 | ✅ **已坐实**：L4141，共 13 种（旧称 16 种含 textNode/audioNode/textConcatNode 错误，不在集内） |
| TODO-6 | `Th`(Director3DNode) 定义 | ✅ **已坐实**：`director3dNode: Th` L31141 |
| TODO-7 | 统一同步 effect 的 `/files/` 判断具体行 | ✅ **已坐实**：effect L44352-44425，关键判断 L44369 |
| TODO-8 | 性能策略 `pan-performance-mode`/`is-large-canvas` | ✅ **已坐实**：CSS L31231、minZoom .05 L37149、selectionKeyCode [Ctrl/Meta] L37165 |

> 详细证据见 `待核项补遗.md`。下游文档回填须经用户授权（红线：仅可在 `docs/AI07/` 建文件）。

---

## 五、对下游文档的修正总建议

1. **`ARCHITECTURE.md`**：修正 DOC-1/2/3/5/6/8（见上表），补 `we`/`Ir`/`Lr` 多义说明。
2. **`FUNCTION_MAP.md`**：修正 DOC-1/4/6/7，`Jn` 改 LogoIcon，`Oa`/V/Ne 登录态表述。
3. **`TASKS.md`**：NET-1 标注已缓解；P1-3 机制修正（R→z→O 路径）；T0 阻塞项部分推翻（Jn 确为 LogoIcon，但 Xr/Zr 的"openInTab/logout"判定反错）。
4. **`CLAUDE.md` 红线 §3.1**：本次 3 组多义变量实锤，建议在「引用必须带行号」后追加「+ 作用域说明」。
