# 一毛AI画布 · 审计整理（AI08 工作区）

> **本目录为审计专属工作区**。所有产物**仅存放于 `docs/AI08/`**，不向项目其他目录（含 `docs/` 根、`docs/AI02`、`docs/AI03`）写入任何文件。不修改任何源码（`App.js`/`config.js`/`dist/`/`localTool`/网关）。
> 审计对象：当前运行的 **V1 引擎**（`src/_engine/App.js` + `config.js` + `localTool/` + `apimart-gateway/`）。**V2 永久暂停，只读归档，不碰、不接、不写。**
> 快照日期：**2026-07-21**（行号会漂移，文档内引用以「L行号 + grep 复核」为准）。

---

## 0. AI08 与 AI02 / AI03 的关系（防重复劳动）

- **AI02 / AI03**：已立好审计方法学（五关流水线 + 四段式）、门3 校验脚本（`docs/AI01/check-doc-citations.cjs`）、并给出"已坐实关键事实"预览。但其模块文档（`10_*`～`99_*`）基本未产出（状态 ⬜/⏳）。
- **AI08 定位**：承接审计计划，把方法学转化为**带门3 校验的真实模块审计资产**。不做重复框架，直接出内容。
- **信任链（从高到低）**：
  1. 实际代码 + grep 实敲（唯一权威）；
  2. 映射表 `docs/func-mapping.txt` + `docs/var-mapping.txt`；
  3. 项目文档 `CLAUDE.md`(红线)/`PROJECT_ORIGIN.md`/`ARCHITECTURE.md`/`TASKS.md`/`FUNCTION_MAP.md`；
  4. 历史笔记 `docs/reference/`/`reference/`：可能过时，仅作线索，引用前以代码/git 为准。

> ⚠️ AI02 `00_审计索引.md` / `AI03/README.md` 中若干"已坐实事实"经 AI08 grep 复核**被推翻或需修正**（见 `01_已坐实锚点快照.md` 第 4 节）：
> - AI02 称 `Xr = openInTab`（L1802）**非上传函数** —— 实测 `Xr`(L1802) 即 `uploadToLocalTool`（`/api/files/upload`），**就是上传函数**。AI02 错。
> - AI02 称 `Zr = logout`（L1827）**非下载函数** —— 实测 `Zr`(L1827) 是**按 URL 上传到 localTool**（`fileUrl` 参数），也不是 logout（logout 真身另在别处）。
> - AI02 把 `B`/`R` 当模块级函数（L29160/L29188）—— 实测是**组件内局部回调**，同名陷阱。

---

## 1. 红线（动手前必读，来自 CLAUDE.md §3）

- **修改范围**：只改 `App.js`(核心) / `config.js`(配置)。**本审计不修改任何源码**，纯产出文档。
- **禁止触碰**：`dist/`、vendor / runtime / `*.css`、`App.original.js`、`reference/App.original.js`。
- **版本锁定**：严格 V1；V2 当只读归档。
- **命名**：混淆名(`ii`/`Xr`/`U_`/`W_`/`G_`)无稳定语义，**引用必须带行号**；新增变量用语义化命名（本审计不新增代码）。
- **已知噪音不修**：9004 的 404、`RootErrorBoundary` null、`18080` 连不上。
- **先证明后修改**：本次为审计不修改；若后续要改，必须先在回答首句确认红线未触碰。

---

## 2. 审计方法（四段式 + 校验门，来自 TASKS.md 审计计划）

- **门1 锚点门（写前）**：只用 `func-mapping.txt`/`var-mapping.txt` 已收录名 + 自 grep 确认行号；新发现先补 `01_已坐实锚点快照.md` 再写。
- **门2 强制引用（写中）**：每条事实带 `App.js:Lnnnn` 或 `localTool/...:Lnnn`。
- **门3 机器校验（写后）**：跑 `docs/AI01/check-doc-citations.cjs <md>` 输出 `校验报告.md`，逐条 ✅/❌，❌ 打回。
- **门4 对抗审计（写后·逻辑质询）**：对存疑 Bug 做反向质询（"A 调 B，B 返回在哪被消费？grep 给我看"）。
- **门5 入库门**：校验全绿 + 审计无阻断 → 标「已验证」；否则打回。

每个模块文档四段式：**①运转图景 ②核心混淆字典 ③关键数据流（带 file:line）④存疑 Bug（对照 TASKS P0–P2）+ 边界契约子节**。

---

## 3. 文件清单（本工作区）

| 文件 | 内容 | 状态 |
|------|------|------|
| `00_AI08索引.md` | 本文件：范围/红线/信任链/与 AI02·AI03 关系 | ✅ 建 |
| `01_已坐实锚点快照.md` | 通用锚点快照：模块2 相关混淆名真身 + 当前行号 + 对 AI02/AI03 错锚的修正 | ✅ 建 |
| `10_模块2_本地数据层与Rescan.md` | 模块2 四段式审计（资源入库/删除/rescan/上传/缩略图） | ✅ 建 |
| `10_模块2_本地数据层与Rescan_校验报告.md` | 门3 机器校验报告（14/14 PASS） | ✅ 建 |
| `11_模块1_初始化与配置层.md` | 模块1 四段式审计（鉴权/配置层/base URL 桥接） | ✅ 建 |
| `11_模块1_初始化与配置层_校验报告.md` | 门3 机器校验报告（9/9 PASS） | ✅ 建 |
| `12_模块3_AI生成与网关层.md` | 模块3 四段式审计（生图派发/轮询/落盘/确认） | ✅ 建 |
| `12_模块3_AI生成与网关层_校验报告.md` | 门3 机器校验报告（8/8 PASS） | ✅ 建 |
| `13_模块4_画布引擎与节点.md` | 模块4 四段式审计（ReactFlow 交互/节点注册/3D 导演台） | ✅ 建 |
| `13_模块4_画布引擎与节点_校验报告.md` | 门3 机器校验报告（4/4 PASS） | ✅ 建 |
| `20_交叉流审计.md` | 阶段2.5 横切流缝合（4 条端到端流） | ✅ 建 |
| `20_交叉流审计_校验报告.md` | 门3 机器校验报告（9/9 PASS） | ✅ 建 |
| `99_审计总报告.md` | 阶段3 汇总 + Bug 回填建议 + 文档修正清单 | ✅ 建 |
| `30_门4对抗审计与localTool跨审.md` | 门4 对抗审计 + localTool 侧跨审（P2 三项落地） | ✅ 建 |
| `30_门4对抗审计与localTool跨审_校验报告.md` | 门3 机器校验报告（8/8 PASS） | ✅ 建 |
| `31_事件总线全集与已知限制实锤.md` | CustomEvent 全集 + `resourceAdded` 派发源 + extension 瞬态 + 噪音项实锤 | ✅ 建 |
| `31_事件总线全集与已知限制实锤_校验报告.md` | 门3 机器校验报告（4/4 PASS） | ✅ 建 |
| `32_网关侧审计.md` | 模块5 四段式审计（apimart-gateway：路由/AUTO_CONFIRM/轮询/确认/音频501/内存态） | ✅ 建 |
| `32_网关侧审计_校验报告.md` | 门3 机器校验报告（33/33 PASS） | ✅ 建 |
| `33_模块6_localTool服务端.md` | 模块6 四段式审计（启动/路由/持久化/KV/系统/任务；剪映空实现等） | ✅ 建 |
| `33_模块6_localTool服务端_校验报告.md` | 门3 机器校验报告（52 行号 PASS / 0 FAIL，3 WARN 为脚本局限） | ✅ 建 |
| `34_模块7_网关验证与工具层.md` | 模块7 四段式审计（verify_gateway.py 验证套件 + helpers.ts 工具层） | ✅ 建 |
| `34_模块7_网关验证与工具层_校验报告.md` | 门3 机器校验报告（37 行号 PASS / 0 FAIL，2 WARN 为脚本局限） | ✅ 建 |
| `35_模块8_文件与资源路由.md` | 模块8 四段式审计（files.ts 文件仓库 + resources.ts 资源存储，P2 三项落地） | ✅ 建 |
| `35_模块8_文件与资源路由_校验报告.md` | 门3 机器校验报告（33 行号 PASS / 0 FAIL，1 WARN 为脚本局限） | ✅ 建 |

> 图例：✅ 已完成 · 🔜 进行中/待做

---

## 4. 已坐实的关键事实（门1 锚点门预览，grep 复核 2026-07-21）

| 混淆名 | 当前行号 | 真身 | 说明 |
|--------|----------|------|------|
| `ii` | L1888 | `uploadFile`(落盘上传封装) | 调 `Xr` 上传；http(s) 直链直接返回 `{url}` |
| `Xr` | L1802 | `uploadToLocalTool` | **实为上传** `/api/files/upload`（Blob 版）；AI02 错标为 openInTab |
| `Zr` | L1827 | `uploadFileByUrl` | **按 URL 上传** `/api/files/upload`（`fileUrl` 版）；非 logout |
| `ri` | L1856 | `getThumbnailUrl`(缩略图缓存) | 内存 Map + WASM 缓存，调 `/api/files/thumbnail` |
| `Sv` | L42838 | `saveResource` | POST `/api/resources/save` |
| `wv` | L42857 | `deleteResource` | POST `/api/resources/delete?id=` **只删 DB** |
| `Tv` | L42866 | `clearResources` | POST `/api/resources/clear`，**带 `deleteFiles` 参数**（可删磁盘） |
| `Ev` | L42883 | `rescanResources` | POST `/api/resources/rescan`，返回 count |
| `Hr` | var-mapping | `localToolStatusUrl` | `'http://127.0.0.1:18080'` |
| `vv` | var-mapping | `localToolBaseUrl` | `'http://127.0.0.1:18080'`（值同 Hr） |
| `Jn` | L89 | `LogoIcon`(品牌 SVG) | 非生图；生图回调为 L32490 组件内 `let Jn` |
| `B`/`R` | L29188/L29160 | 组件内局部回调 | 拖拽/粘贴/文件上传处理；**同名陷阱，非模块级函数** |

详见 `01_已坐实锚点快照.md`。
