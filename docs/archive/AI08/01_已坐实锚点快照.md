# AI08 · 已坐实锚点快照（模块2 相关）

> 本文件为 AI08 的可信锚层。所有行号经 `grep` 在 `src/_engine/App.js` 实敲复核（2026-07-21）。
> 用途：① 作为后续模块文档的引用基准；② **修正 AI02/AI03 已有的错锚**（避免错传错）。
> 规则（CLAUDE.md §3 / TASKS 审计计划门1）：映射表是唯一可信字典；本文件坐实结果**不回写** `docs/func-mapping.txt`/`var-mapping.txt`，待人工确认后并入。

---

## 1. 模块2 核心函数真身（grep 复核）

| 混淆名 | 当前行号 | 真身（可读名） | 调用去向 | 证据（代码摘录） |
|--------|----------|----------------|----------|------------------|
| `ii` | L1888 | `uploadFile`（落盘上传封装） | 调 `Xr`(L1898) | `async function ii(e, t={})`；http(s) 直链 `return {url:e}`，否则 `Xr(e,{subfolder,generateThumb})` |
| `Xr` | L1802 | `uploadToLocalTool`（Blob 上传） | POST `${Hr}/api/files/upload` | `async function Xr(e,t={})`；`qr(e)` 取 blob → FormData `file` → fetch upload |
| `Zr` | L1827 | `uploadFileByUrl`（URL 上传） | POST `${Hr}/api/files/upload` | `async function Zr(e,t={})`；FormData `fileUrl` 参数 |
| `ri` | L1856 | `getThumbnailUrl`（缩略图缓存） | GET `${Hr}/api/files/thumbnail` | 内存 `ei`(Map) + `ti`(Promise缓存)；`fetch(thumbnail?url=...)` |
| `Sv` | L42838 | `saveResource`（资源入库） | POST `${vv}/api/resources/save` | `body: JSON.stringify(e)` |
| `wv` | L42857 | `deleteResource`（删资源） | POST `${vv}/api/resources/delete?id=` | **只发 delete 请求，无 deleteFiles 参数 → 只删 DB** |
| `Tv` | L42866 | `clearResources`（清空资源） | POST `${vv}/api/resources/clear` | **带 `deleteFiles: t` 参数**（可删磁盘，与 `wv` 不同） |
| `Ev` | L42883 | `rescanResources`（重扫） | POST `${vv}/api/resources/rescan` | 返回 `count` |
| `Cv` | L42851 | `saveResourceFavorite`（收藏切换） | 调 `Sv` | `Sv({...e,isFavorite:t})` |

> `Hr` = `localToolStatusUrl` = `http://127.0.0.1:18080`（var-mapping L117）
> `vv` = `localToolBaseUrl` = `http://127.0.0.1:18080`（var-mapping L118，值同 Hr）
> `Kr` = localTool 连通性检测（被 `Xr`/`ri`/`Zr` 前置调用，`if(!(await Kr())) return null`）

---

## 2. 关键的"同名陷阱"（门4 对抗审计重点）

反编译残留导致**模块级函数**与**组件内局部变量**大量同名。审计时**必须带行号**，否则必错：

| 名字 | 模块级（真身@行号） | 组件内局部（@行号） | 风险 |
|------|---------------------|---------------------|------|
| `Jn` | `LogoIcon` 品牌 SVG @L89 | `let Jn = useCallback(...)` 生图主回调 @L32490 | AI02 把模块级 `Jn` 当生图派发 → 错 |
| `ri` | `getThumbnailUrl` @L1856 | `let ri = () => {...}` @L37007（疑似另一局部） | 引用 `ri` 须带行号 |
| `R` | （模块级未定义同名函数） | `let R = Y.useCallback(...)` 文件→资源 @L29160；`let R = useMemo` @L7212 | TASKS 把 `R` 当"上传回调"——实为组件内局部 |
| `B` | （模块级未定义同名函数） | `let B = Y.useCallback(...)` 拖拽处理 @L29188 | TASKS 把 `B` 当"资源面板回调"——实为组件内局部 |

**结论**：`B`/`R` 在 TASKS.md 的 P1 描述（"拖入 URL 不落盘 `B` 回调 L29160`、`文件上传不入库 R 回调 L29165`"）指向的是**组件内局部回调**，语义上大致对（确实处理拖拽/上传），但其"只存状态不下载""上传后不调 Sv"的指控需回到该组件上下文复核（见 `10_模块2` 第4节）。

---

## 3. 对 AI02 / AI03 错锚的修正（重要）

| 条目 | AI02/AI03 说法 | AI08 grep 复核结论 | 修正 |
|------|---------------|-------------------|------|
| `Xr` 身份 | AI02：「`Xr` L1802 = `openInTab`，**非上传函数**」 | `Xr`(L1802) 函数体为 `qr(e)` 取 blob → FormData `file` → `fetch(${Hr}/api/files/upload)` = **标准 Blob 上传到 localTool** | ❌ AI02 错。`Xr` 就是上传函数（Blob 版）。`openInTab` 真身在别处，需另行定位 |
| `Zr` 身份 | AI02：「`Zr` L43893 = 注销/重置配置(logout)，非下载」；TASKS：「`Zr()` 下载」 | `Zr`(L1827) 函数体为 FormData `fileUrl` → `fetch(${Hr}/api/files/upload)` = **按 URL 上传到 localTool**；与 logout 无关 | ❌ 两头都错。`Zr` 是"按 URL 上传"，既非 logout 也非下载。注：App.js 中 `Zr` 仅在 L1827 定义，L43893 是 AI02 错误引用的行号，并非代码第二处定义 |
| `B`/`R` 层级 | AI02/AI03 视为模块级回调 | 实测为组件内 `let B`/`let R` 局部 useCallback/useMemo（L29188/L29160/L7212） | ⚠️ 层级标注错，但局部功能描述（拖拽/上传处理）方向对 |
| `Jn` 身份 | AI02 已纠正 `Jn`=LogoIcon≠生图 | 复核一致：`Jn` 模块级 @L89=LogoIcon；生图回调为 L32490 组件内 `let Jn` | ✅ AI02 对 |
| `Sv`/`wv`/`Ev` 行号 | AI02：Sv L42838 / wv L42857 / Ev L42883 | grep 复核一致 | ✅ 对 |

> 注：AI02 把 `Xr` 误判为 `openInTab`、把 `Zr` 误判为 `logout`，根因是引用了错误的行号（AI02 标 `Xr` L43881、`Zr` L43893）。经 grep 复核，App.js 中 `Xr`/`Zr` **仅在 L1802/L1827 各定义一处**，并无 L43881/L43893 的镜像定义——那两个行号是 AI02/var-mapping 的**错锚引用**，不代表代码存在第二处。再次印证"同名陷阱 + 必须带行号 + 以 grep 实敲为准"的铁律。

---

## 4. 待 AI08 后续定位的开放项（不猜测，列此待补）

- `openInTab` 真身（模块级 `Xr` 被误认为它，需另搜）。
- `logout`/`重置配置` 真身（var-mapping 标 `Zr=logout` @L48，但 `Zr`(L1827) 实为上载；说明 var-mapping 的 `Zr=logout` 与代码 L1827 **冲突**，需复核 var-mapping 是否把另一处 `Zr` 当真身）。
- `we`：TASKS T0.1 列为 rescan 主函数，但 grep 未见模块级 `function we`；rescan 实为 `Ev`(L42883)。`we` 可能为不存在或已被重命名 → 标记待废弃。
- 真正的 AI 生成派发函数（T0.1 待补，非 `Jn`）：指向 L32490 `let Jn = useCallback(async(e,r,o,s,c,d)=>...)` 生图回调，及其上游 task_id 轮询分流（L32957 区）。

→ 见 `11_模块1` 与后续模块补完。
