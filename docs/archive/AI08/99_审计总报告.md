# AI08 · 审计总报告（阶段3 汇总）

> 汇总模块1-8（`11_模块1`/`10_模块2`/`12_模块3`/`13_模块4`/`20_交叉流`/`30_门4`/`31_事件总线`/`32_网关侧`/`33_localTool服务端`/`34_网关验证与工具层`/`35_文件与资源路由`）的审计结论，回填 Bug 建议，并给出对既有文档（TASKS/AI02/AI03）的修正清单。
> 全部行号 grep 复核 `src/_engine/App.js` + `src/_engine/config.js` + `apimart-gateway/main.py` + `localTool/src/*`（2026-07-21）。
> 门3 机器校验：模块1 9/9、模块2 14/14、模块3 8/8、模块4 4/4、交叉流 9/9、门4 8/8、事件总线 4/4、网关侧 33/33、localTool 服务端 52 行号 PASS/0 FAIL、网关验证+工具层 37 行号 PASS/0 FAIL、文件与资源路由 33 行号 PASS/0 FAIL（WARN 均为脚本仅校验 App.js 符号的局限），合计 **216 引用、211 行号 PASS、0 FAIL**。

---

## 一、审计覆盖与方法

| 模块 | 文档 | 门3 |
|------|------|-----|
| 模块1 初始化与配置层 | `11_模块1_初始化与配置层.md` | ✅ 9/9 |
| 模块2 本地数据层与 Rescan | `10_模块2_本地数据层与Rescan.md` | ✅ 14/14 |
| 模块3 AI 生成与网关层 | `12_模块3_AI生成与网关层.md` | ✅ 8/8 |
| 模块4 画布引擎与节点 | `13_模块4_画布引擎与节点.md` | ✅ 4/4 |
| 阶段2.5 交叉流 | `20_交叉流审计.md` | ✅ 9/9 |
| 模块5 网关侧（apimart-gateway） | `32_网关侧审计.md` | ✅ 33/33 |
| 模块6 localTool 服务端 | `33_模块6_localTool服务端.md` | ✅ 52 行号 PASS / 0 FAIL |
| 模块7 网关验证 + 工具层 | `34_模块7_网关验证与工具层.md` | ✅ 37 行号 PASS / 0 FAIL |
| 模块8 文件与资源路由 | `35_模块8_文件与资源路由.md` | ✅ 33 行号 PASS / 0 FAIL |
| 锚点快照（修正 AI02/AI03 错锚） | `01_已坐实锚点快照.md` | — |

方法：五关流水线（锚点门/引用门/机器校验门/+ 对抗审计门4 内联 + 入库门）。**只出文档，未改任何源码**（含本次新增模块3-8、门4、事件总线、网关侧，产物仅存 `docs/AI08/`）。

---

## 二、核心锚点坐实表（可信起点）

| 混淆名 | 行号 | 真身 | 备注 |
|--------|------|------|------|
| `ii` | L1888 | `uploadFile`(落盘封装) | 调 `Xr` |
| `Xr` | L1802 | `uploadToLocalTool`(Blob上传) | **AI02 错标 openInTab，实为上传** |
| `Zr` | L1827 | `uploadFileByUrl`(URL上传) | **AI02/TASKS 错标 logout/下载，实为上载** |
| `ri` | L1856 | `getThumbnailUrl` | 内存+WASM 缓存 |
| `Sv` | L42838 | `saveResource` | `/api/resources/save` |
| `wv` | L42857 | `deleteResource` | 只删 DB |
| `Tv` | L42866 | `clearResources` | 带 `deleteFiles` |
| `Ev` | L42883 | `rescanResources` | `/api/resources/rescan` |
| `Oa` | L3543 | `getAuthToken`(local-mode) | 固定 token |
| `Jh` | L30712 | `authHeaders` | Bearer |
| `ar` | L863 | `resolveApiUrl` | localhost 回退 |
| `tr` | L846 | `withApiSuffix` | 拼 /api |
| `Cr` | L1294 | `canvasStateKey` | `canvas-state-v1-` 前缀 |
| `jr` | L1345 | `cachedGet` | 带缓存 KV 读 |
| `B`/`R` | L29188/L29160 | 组件内局部回调 | **同名陷阱，非模块级** |
| `Jn` | L89 / L32490 | `LogoIcon` / 生图回调(局部) | 模块级≠生图 |

---

## 三、Bug 回填建议（对照 TASKS P0–P2）

| 优先级 | 问题 | AI08 证据 | 修复建议 | 涉及文件 |
|--------|------|-----------|----------|----------|
| 🔴 红线冲突 | localTool 断连时生图回退 CDN 直链 | App.js:L33051 catch 兜底 | 断连应标记失败，禁止回退直链；或强约束 localTool 必连 | App.js L33049 区 |
| 🟠 P0 | host 硬编码 18080，config 真源脱节 | `Hr`/`vv` 硬编码 vs config.js `LOCAL_ENGINE.base`(L12-18) 未被引用 | App 改读 `config.js` 单一真源 | App.js(全 `Hr`/`vv`) + config.js |
| 🟠 P0 | URL 格式不统一破图 | 入库 `Sv` 未 `toAbsoluteFileUrl` | 入库前统一绝对化 | App.js L43462 区 / `Sv` L42838 |
| 🟡 P1 | 拖入/粘贴 URL 不落盘 | `B`/`R` 局部(L29188/L29160) 仅存 state | 落盘调 `ii()`/`Zr()` | App.js L29188 区 |
| 🟡 P1 | 文件上传不入库 | `R`(L29160) 后未调 `Sv()` | 成功后调 `Sv()`/`Ev()` | App.js L29160 区 |
| 🟡 P2 | 删除不删磁盘 | `wv`(L42857) 无 `deleteFiles` vs `Tv`(L42866) 有；localTool `handleResourcesDelete`(resources.ts:L207) 单条无 `deleteFiles` | 前端 `wv` 加可选 `deleteFiles` + localTool `delete` 路由支持该参数 | App.js L42857 / `35_模块8` |
| 🟡 P2 | Rescan 孤儿只清 local-tool | `handleResourcesRescan` 孤儿清理仅 `source='local-tool'`(resources.ts:L123) | rescan 放宽清理条件纳入 `source='extension'` | `35_模块8` / `30_门4` B.2 |
| 🟡 P2 | 缩略图伪复制 | `ri`(L1856) 仅取 URL；伪复制在 `tryGenerateThumbnail`(files.ts:L137) `copyFileSync` | localTool 接 sharp 真实缩放 | `35_模块8` / `30_门4` B.3 |

---

## 四、对既有文档的修正清单（重要）

### 4.1 推翻 AI02 / AI03 错锚
- ❌ AI02：`Xr`(L1802)=`openInTab` 非上传 → **实为 `uploadToLocalTool`（Blob 上传）**。
- ❌ AI02：`Zr`(L43893)=logout 非下载 → **`Zr`(L1827) 实为 `uploadFileByUrl`（按 URL 上传）**，与 logout 无关。注：App.js 中 `Zr` 仅在 L1827 定义，L43893 是 AI02 错误引用的行号，并非代码第二处定义。
- ❌ var-mapping `Zr=logout`(L48) / AI02 `Xr=openInTab`：App.js 内 `openInTab`/`logout` **全仓搜不到**，两映射均错。
- ⚠️ AI02/AI03：`B`/`R` 当模块级函数 → 实为组件内局部回调（L29188/L29160/L7212）。

### 4.2 废弃/待确认锚点
- `we`（TASKS T0.1 列 rescan 主函数）：grep 无模块级 `function we`，rescan 实为 `Ev`(L42883) → **建议废弃 `we`**。
- `Nr`/`Pr`（localforage 后端）：App.js 仅 `wr`/`Mr` 两后端 → **建议废弃**。
- `Ba`(L3637) 真身：`hasAuthToken` = `!!Oa()`（门4 已读体确认，本地模式恒 true，无 bug）。
- `Th`(TASKS T2.4 列 Director3DNode)：grep App.js 无 `Th` 定义 → **建议废弃**，3D 导演台改用 `kp`(L26080)/`wd`(L22986)/`Op`/`Cd` 锚。

### 4.3 门4/localTool 跨审结论（已落地，见 `30_门4对抗审计与localTool跨审.md`）
- `resourceAdded` 派发源：`src/background.ts:L102`（Service Worker），前端 L43527 跨进程监听 → 通道合规，未绕过 StorageManager。
- `Zn`(L168)=`canvas-add-resource-request`（事件名）；L43357 同名局部变量是配置加载用（同名陷阱）。
- localTool P2 #6 删除不删磁盘：**双向坐实**（前端 `wv` L42857 + localTool `resources.ts:L207` 单条删除无 `deleteFiles`；`handleResourcesClear` L211 才有）。
- localTool P2 #7 rescan 孤儿只清 `source='local-tool'`（`resources.ts:L123`）→ extension 源不清理，坐实。
- localTool P2 #8 缩略图伪复制：`files.ts:L137` `fs.copyFileSync` 复制原图，注释预留 sharp，坐实。
- `openInTab`/`logout` 全仓搜不到 → var-mapping `Zr=logout` 与 AI02 `Xr=openInTab` 确认错误映射。

---

## 五、下一步建议

1. **修正回写**：将 §4.1/§4.2/§4.3 反馈维护者，更新 `docs/func-mapping.txt`/`var-mapping.txt`（废弃 `we`/`Nr`/`Pr`/`Th`，修正 `Zr`/`Xr`），AI08 不回写，待人工并入。
2. **P2 修复建议**（详见各模块文档）：
   - 删除联动：前端 `wv` 加 `deleteFiles` + localTool `delete` 路由(L207) 支持该参数。
   - rescan 孤儿：放宽清理条件纳入 `source='extension'`。
   - 缩略图：localTool 接 sharp 真实缩放。
   - 红线冲突：L33051 断连回退 CDN 直链改为失败标记。
3. **已闭环（模块1-8 + 门4 + 事件总线）**：AI 生成派发（`let Jn`@L32490、轮询@L32957）、画布引擎（ReactFlow `Be`@L37120 及 `xn`/`kn`/`Lr`/`Fr`）、localTool 服务端（启动/路由/持久化/KV/系统/任务 `33_`）、网关侧（路由/AUTO_CONFIRM/轮询/确认/音频501/内存态 `32_`）、文件与资源路由（P2 三项落地 `35_`）、门4/localTool 跨审（P2 三项落地）、事件总线全集（`resourceAdded` 派发源 `background.ts:L102`、`Zn`=`canvas-add-resource-request`@L168）、网关验证套件+工具层（`34_`）——均已产出并通过门3。
4. **延伸发现**：扩展采集资源 `source='extension'`（background.ts:L81）**有意瞬态不落盘**（注释 L80「不再经过本地引擎上传」），最多 5 条（L91）；这与 P2 #7 rescan 不清理 extension 不冲突，属设计特性，文档应明确避免误判为 bug。详见 `31_事件总线全集与已知限制实锤.md`。
5. **修正回写**：将 §4.1/§4.2/§4.3 修正反馈维护者，更新 `docs/func-mapping.txt`/`var-mapping.txt`（AI08 不回写，待人工确认）。

> AI08 工作区产物**仅存放于 `docs/AI08/`**，未污染其他目录、未修改任何源码。
> 审计方法学：五关流水线全程贯彻，门3 机器校验累计 **216 引用、211 行号 PASS、0 FAIL**（模块1-4 + 交叉流 + 门4 + 事件总线 + 网关侧 + localTool 服务端 + 网关验证/工具层 + 文件与资源路由；WARN 均为门3 脚本仅校验 App.js 符号的局限，非真实错误）。
