# 模块10 · 自定义节点模板系统（customNode）

> 模块6（`17-模块6-节点注册表全景.md`）列出 `customNode` → `ms`@L31130 已在 `lg` 注册表。本篇坐实自定义节点的**模板存储/增删/导入导出**机制，闭合"用户可扩展节点"的最后一公里。
> 方法：grep `App.js` `CUSTOM_NODE_TEMPLATES` / `ms` / `customNodeTemplates` 坐实，行号 2026-07-21 快照。
> 纯文档审计，未触源码。

---

## 1. 自定义节点渲染器 `ms` @L13662

`ms = Y.memo(({id, data, selected}) => {...})` @L13662 是 `customNode` 类型的 React 组件（绑定 `lg.customNode` = `ms` @L31130）。其具体渲染逻辑（用户模板驱动的 UI）在 @L13662 起的函数体内，属用户可配置节点外壳。

---

## 2. 模板存储键与状态

| 符号 | 行号 | 语义 |
|------|------|------|
| `CUSTOM_NODE_TEMPLATES` | @L1276 | store key = `'customNodeTemplates'` |
| `customNodeTemplates` (store) | @L31307 (`te=[]`) / @L45280 (`wn`) | 全局模板数组状态 |
| `onAddCustomNodeTemplate` | @L31308 (`ne`) / @L45281 (`Si`) | 新增模板回调 |
| `onDeleteCustomNodeTemplate` | @L31309 (`re`) / @L45282 (`Ci`) | 删除模板回调 |

---

## 3. 模板持久化与导入导出

| 场景 | 位置 | 调用 |
|------|------|------|
| 设置加载 | @L43355 | `e.customNodeTemplates && Tn(e.customNodeTemplates)`（载入到 store） |
| 备份导出 | @L43954-43957 | 遍历 key 含 `customNodeTemplates`，`Q.getObject('customNodeTemplates')` 收集进备份 |
| 备份导入 | @L44551 / @L44555 | 老格式 `t.customNodeTemplates` → 映射 `Z.CUSTOM_NODE_TEMPLATES` |
| 恢复读取 | @L44512 | `Q.getObject(t)` 对 `customNodeTemplates` 等 key 批量读 |

> ✅ 自定义节点模板经 `Q.getObject/setObject`（模块7 坐实 `Q` → `wr` localTool）持久化，符合红线 §3.3「不绕过 `Q`」。导入导出（`importData` @L44547）兼容老格式映射，健壮。

---

## 4. 审计新发现（模块10）

1. **自定义节点是"用户模板驱动"节点**：`customNode` 类型已在 `lg`@L31130 注册（`ms`@L13662 渲染），模板数据存 `customNodeTemplates` key，经 `Q` 持久化，支持增删（`onAdd/DeleteCustomNodeTemplate`）与导入导出。
2. **存储键一致**：`CUSTOM_NODE_TEMPLATES='customNodeTemplates'`@L1276 与备份/导入导出所用字符串一致，无 key 漂移。
3. **与节点注册表衔接**：`customNode` 是 `lg` 27 节点之一（模块6 已确认非孤儿），模板系统是其"内容来源"——二者构成"注册表(壳) + 模板(内容)"双层结构。
4. **导入兼容**：`importData`@L44547 对老格式 `customNodeTemplates` 做 `Z.CUSTOM_NODE_TEMPLATES` 映射(@L44555)，避免旧备份失效。

> 本篇为模块6 的 customNode 内容层补强；与 `17-模块6-节点注册表全景.md` 合并即构成"节点系统"完整切面（壳 + 内容）。
