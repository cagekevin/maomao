# 交叉流审计 · 端到端横向切片（T2.5）

> 在模块1–4 之后，沿边界契约的接缝（HTTP 路由 / window CustomEvent / config 名）追踪 4 条端到端流。
> 方法：每条事实带 `file:line`，经 grep 坐实（2026-07-21 快照，行号会漂移）。
> 信任链：实际代码 > 映射表 > 文档。

---

## 1. 流一：AI 生成 → 落盘 → 资源面板刷新（缝合 模块3 + 模块2 + 模块4）

```
[模块4] 节点触发生图 → Jn(e,...)  @L32490
  → [模块3] fetch 网关 /v1/images/generations → 返回 task_id
  → 轮询 GET ${R}/v1/tasks/${taskId}  @L33005 (zc@L19001)
  → 终态 completed → 提取 result.images[0].url[0]  @L33031
  → ii(u,{subfolder:'tasks'})  @L33049   // [模块2] 落盘 18080
  → POST /api/files/upload (files.ts L14) → uploads/tasks/{name}
  → 节点回填 resultUrl=u (绝对本地路径)  @L33118
  → dispatchEvent('mutiwindow-task-completed', {nodeId,resultUrl})  @L44406 (统一同步effect) / L38481(任务清单)
  → 监听侧: window.addEventListener('mutiwindow-task-completed')  @L31428
  → Ev() rescan (模块2 @L42883) → POST /api/resources/rescan (resources.ts L37)
  → 扫盘入库 + 孤儿清理 (resources.ts L120-130) → 资源面板刷新
```
> 红线 §3.2 达标：结果经 `ii()` 落盘，未直接用 CDN URL。

---

## 2. 流二：资源采集（background.ts → 前端 → localTool）

```
[Service Worker] background.ts handleSaveToTransit()
  → chrome.storage.local.set('transitResources')  (≤5, 内存易失)
  → sendMessage({action:'resourceAdded'})
[前端] onMessage: e.action==='resourceAdded'  @L43527
  → 追加 transitItems(内存) + Zr()(L1827, 远程URL上传) 下载 → uploads/migrated/
  → Sv()(L42838) POST /api/resources/save 入库
  → dispatchEvent('mutiwindow-task-completed')  @L43640/L43676/L43697
  → Ev() rescan → 资源面板刷新
```
> 已知限制（TASKS P1）：采集只存元数据 URL，下载走 `Zr`@L1827 远程上传；双数据源 `transitItems`(内存) vs `resources`(持久)（ARCHITECTURE X3.1）。

---

## 3. 流三：画布拖放 → 落盘 → 建节点（缝合 模块4 + 模块2）

```
[模块4] onDrop Lr(e)  @L36293
  ├─ 文件拖入 → ii(e,{subfolder:'canvas/drop'})  @L36363
  │     → [模块2] POST /api/files/upload → uploads/canvas/drop/{name}
  │     → Z('imageNode'|'audioPlayerNode', {url})  @L36370-36378
  ├─ 任务拖入 → getData('application/x-mutiwindow-task') → 按 mediaType Z()建节点 @L36318-36331
  └─ 模板拖入 → sn.current(e,t) 导入子图 @L36303
```
> 拖入即落盘，符合本地闭环（ARCHITECTURE X1.3）。

---

## 4. 流四：统一同步 effect（前端 ↔ localTool，防死循环）

```
依赖 [globalTasks(i), isConnected, port]  (L44324 / L44360)
Se.current 锁: 同步中跳过  @L44354-44358
遍历 globalTasks(status==='completed'):
  a(url, type)  @L44368:
    // ★ 已本地化跳过（防死循环关键）
    if url.startsWith('/files/') || (url.startsWith('http://127.0.0.1') && /\/files\/tasks(\/|$)/.test(url))
        return url;  @L44369
    // 远程 http/data/blob → 落盘
    blob = url.startsWith('http') ? url : await (await fetch(url)).blob();
    return await n.uploadFile(blob, name, 'tasks')  @L44377
  有变更 → ev(i,t) 持久化 globalTasks + dispatchEvent('mutiwindow-task-completed')  @L44406
```
> 死循环防护双保险：`Se.current` 锁 + 已本地化 URL 跳过（L44369）。与 ARCHITECTURE X1.4「修复前缺 /files/ 条件 → 死循环」对照：**实测已修复**（L44369 含 `/files/` 与 `127.0.0.1/files/tasks` 判断）。

---

## 5. 跨层事件总线全集（window CustomEvent，grep 坐实）

| 事件 | dispatch 位置 | 监听位置 | 作用 |
|------|--------------|----------|------|
| `mutiwindow-task-completed` | L38481 / L43640 / L43676 / L43697 / L44406 | L31428 | rescan + 资源刷新 |
| `resourceAdded` | background.ts（SW） | L43527（前端 onMessage） | 采集入库 |
| `mutiwindow-rerun-task` | L44343 | — | 重新执行任务 |
| `mutiwindow-sync-local` | （资源面板按钮，ARCHITECTURE X2） | — | rescanSync |
| （画布持久化） | **无 CustomEvent**；ReactFlow onChange 内联调 `Pr.set`(localforage) | — | 画布落盘 |

> ✅ `canvas-state-change` 待查项已结案（2026-07-21 复 grep）：App.js 全文 `canvas-state-change` 字符串 **0 命中**，确认该事件**不存在**。画布持久化路径为：
> - 常量 `CANVAS_STATE_PREFIX = 'canvas-state-v1-'` @L1283（localStorage key 前缀）
> - ReactFlow `onChange` → `Pr.set(e, t)` @L1637（localforage 持久化层，见模块1 `Pr`@L1446）
> - 删除：`Sr.default.removeItem('canvas-state-v1-${e}')` @L44299
> - 结论：画布以 `canvas-state-v1-{key}` 为键经 `Pr`(localforage) 直接落盘，**不发任何 window 事件**。旧文档假设的事件总线条目应删除。

---

## 6. 跨层一致性风险（审计复核，对照 ARCHITECTURE X3）

| 风险点 | 实测结论 |
|--------|----------|
| 双数据源 transitItems vs resources | ✅ 仍存在（采集走内存 transit，落盘走 resources）；P1 待修 |
| 资源 ID 冲突：resourceAdded(Date.now()) vs rescan(local-{folder}-{name}) | ✅ 仍可能两条记录（不同 id 体系） |
| base URL 不一致 Hr vs vv | ✅ 两者均 18080（config.js），仅 USE_LOCAL_ENGINE=false 分歧（当前 true 未暴露），ARCHITECTURE X3.3 已识别 |
| rescan URL 格式 | ✅ localTool 后端已实现 `toAbsoluteFileUrl`(resources.ts L31)，前端兜底保留 |
| 缩略图路径 | ✅ `/files/{sub}/.thumbnails/thumb_*`（files.ts L132/L248） |

---

## 7. 审计新发现汇总（交叉流层面）

1. **统一同步死循环已修复**：ARCHITECTURE X1.4 称"修复前缺 /files/ 条件"，实测 L44369 已含 `/files/` + `127.0.0.1/files/tasks` 跳过判断 → **修复已落地**，文档可标注"已完成"。
2. **`mutiwindow-task-completed` 是 rescan 主触发器**：5 处 dispatch（L38481/L43640/L43676/L43697/L44406）统一驱动资源刷新，事件总线设计健康。
3. **`canvas-state-change` 事件名消失 → 已结案**：实测画布持久化走 ReactFlow onChange → `Pr.set`(localforage, `canvas-state-v1-` 前缀 @L1283/@L1637/@L44299)，无 CustomEvent。旧文档事件总线假设作废。
4. **采集流双数据源割裂**（P1）：`resourceAdded` 走内存 transit + `Zr`@L1827 远程上传，与 rescan 的 `local-{folder}-{name}` id 体系不统一，是资源面板偶现重复/丢失的根因。
