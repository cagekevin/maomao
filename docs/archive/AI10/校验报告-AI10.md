# 文档引用校验报告（AI10 · 门3 机器校验 · 累计）

> 由 `01-audit-check.cjs` 自动生成，逐模块跑后汇总。
> 仓库根: `G:\01画布项目\maomao`，行号快照 2026-07-21。
> 校验范围：App.js（主）+ config.js + localTool/* + apimart-gateway/main.py。
> 说明：`background.ts`（Service Worker）函数不在脚本 grep 范围，相关引用标 ⚠️ 属**已知误报**，不影响结论。

---

## 累计汇总

| 模块 | 总引用 | ✅ | ⚠️ | ❌ |
|------|--------|----|----|----|
| 10-模块1 | 14 | 14 | 0 | 0 |
| 11-模块2 | 12 | 12 | 0 | 0 |
| 12-模块3 | 7 | 7 | 0 | 0 |
| 13-模块4 | 2 | 2 | 0 | 0 |
| 14-模块3b网关 | 1 | 1 | 0 | 0 |
| 15-模块2b P1 | 3 | 3 | 0 | 0 |
| 16-模块5认证 | 2 | 2 | 0 | 0 |
| 17-模块6节点 | 0* | 0 | 0 | 0 |
| 18-模块7存储 | 5 | 5 | 0 | 0 |
| 19-模块8任务 | 6 | 6 | 0 | 0 |
| 20-交叉流 | 8 | 7 | 1 | 0 |
| 20b-模块9事件 | 0* | 0 | 0 | 0 |
| 21-模块10自定义 | 0* | 0 | 0 | 0 |
| 22-模块11蒙版 | 2 | 2 | 0 | 0 |
| 23-模块12错误 | 4 | 4 | 0 | 0 |
| **合计** | **66** | **65** | **1** | **0** |

> *模块6 / 模块17 / 模块9 / 模块10 以 `事件常量`/`type:` 字符串 + `Lnnnn` 行号形式呈现，脚本需 `file:Lnnn` 格式才抽取；这些引用已通过手动 grep 全文复核（如 `lg`@L31117、事件字符串 0 命中 `canvas-state-change` 等）。

---

## ⚠️ 唯一项说明（非阻断）

`20-交叉流审计.md` 中 `handleSaveToTransit()` 标 ⚠️：该函数定义于 `src/background.ts`（Service Worker），不在本脚本默认 grep 的源文件集（App.js / config.js / localTool / main.py）内，属**脚本范围误报**。文档已正确注明其位于 `background.ts`（流二采集流起点），引用无误。

---

## 结论

- 全 60 条脚本抽取引用 0 硬失败（❌），门3 入库校验通过。
- 先前 `canvas-state-change` 待查项已结案：复 grep 确认 App.js 全文 0 命中，画布持久化走 ReactFlow onChange → `Pr.set`（localforage, `canvas-state-v1-` 前缀 @L1283/@L1637/@L44299），无 CustomEvent。
- 本轮新增模块5（认证/开关）、6（节点注册表 27+1）、7（存储管理器 `Q`）、8（任务引擎双轮询），并修正了三处初版误判（`Ca` 同值；audio/custom 节点已注册；`R` 轮询实为本地网关非官方云）。
- 累计审计文档：模块1–8 + 模块2b(P1) + 模块3b(网关) + 交叉流 = **11 篇模块/专项** + 索引 + 总报告 + 脚本/报告，全部位于 `docs/AI10/`。

> 各模块独立报告见同目录：`校验-10.md` … `校验-19.md` / `校验-20.md`。
