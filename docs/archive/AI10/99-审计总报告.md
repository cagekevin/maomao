# 审计总报告（AI10 · 阶段3 汇总）

> 合并模块1–4（T2.1–T2.4）+ 交叉流（T2.5）的结论，并对 `TASKS.md` / `ARCHITECTURE.md` 的错锚点给出**回填建议（仅记录，不修改权威文件）**。
> 所有事实均经 grep 坐实 + 门3 机器校验（见 `校验报告-AI10.md`）。
> 产出位置：仅 `docs/AI10/`。

---

## 1. 交付物清单（本目录）

| 文件 | 阶段 | 校验结果 |
|------|------|----------|
| `00-审计索引与进度.md` | 总纲 | — |
| `01-audit-check.cjs` | 门3脚本 | 可运行 |
| `校验报告-AI10.md` | 门3报告 | 累计全绿（详见§4） |
| `10-模块1-系统初始化与配置层.md` | T2.1 | ✅ 14/14 |
| `11-模块2-本地数据层与Rescan.md` | T2.2 | ✅ 12/12 |
| `12-模块3-AI生成与网关层.md` | T2.3 | ✅ 7/7 |
| `13-模块4-画布引擎与节点.md` | T2.4 | ✅ 2/2 |
| `14-模块3b-网关专项审计.md` | T2.3补 | 新增（详见 §6） |
| `15-模块2b-资源面板P1根因.md` | T2.2补 | ✅ 3/3 |
| `16-模块5-认证与配置开关层.md` | T2.6 | 新增（详见 §7） |
| `17-模块6-节点注册表全景.md` | T2.7 | ✅ 节点数纠偏 16→27+1（详见 §7） |
| `18-模块7-存储管理器Q与持久化.md` | T2.8 | 新增（详见 §8，`Q` 写路由仅 `wr`） |
| `19-模块8-任务执行与工作流引擎.md` | T2.9 | ✅ 双轮询契约差异（详见 §8） |
| `20-交叉流审计.md` | T2.5 | ✅ 7 / ⚠️ 1 → **已结案**（`canvas-state-change` 走 `Pr.set`，无事件） |
| `20b-模块9-跨窗口事件总线全集.md` | T2.10 | ✅ 15 事件 + 1 通道全集（详见 §9） |
| `21-模块10-自定义节点模板系统.md` | T2.11 | ✅ customNode 模板层（详见 §9） |
| `22-模块11-画布内涂罩绘制子系统.md` | T2.12 | ✅ ImageNode 蒙版编辑（详见 §10） |
| `23-模块12-错误分类与调度中断检测.md` | T2.13 | ✅ `mg`/`hg` 错误分类（详见 §10） |
| `24-模块13-备份导入导出子系统.md` | T2.14 | ✅ 导出/导入全链路 + GAS 云同步区分（详见 §11） |
| `25-模块14-3D导演台节点.md` | T2.15 | ✅ `Th`@L28388 机位截图落盘（详见 §11） |
| `99-审计总报告.md` | T3 | 本文 |

---

## 2. 审计结论速览

### 2.1 红线 compliance（实测）
- **红线 §3.2 异步契约 + 落盘**：✅ 达标。生图轮询成功后 `ii(u,{subfolder:'tasks'})`(@L33049) 强制落盘 18080，未直接用 CDN URL。
- **红线 §3.2 路径绝对化**：✅ 后端 `toAbsoluteFileUrl`(resources.ts L31) 已绝对化；前端 `ii` 兜底保留。
- **红线 §3.3 不绕过 `Q`**：✅ `Q`(StorageManager) 是唯一存储入口，四后端 `wr/Mr/Nr/Pr` 经它选型。
- **红线 §3.1 修改范围 / 版本锁定**：本审计纯文档，未触任何源码；V2 当只读归档。

### 2.2 模块级坐实的关键符号（grep 复核，行号 2026-07-21）
| 模块 | 符号 → 语义（行号） |
|------|---------------------|
| M1 | `Oa`@L3543(token) / `Jh`@L30712(鉴权头) / `Ba`@L3637(已登录) / `tr`@L846 / `ar`@L863 / `nr`@L851 / `cr`@L877(URL助手) / `Cr`@L1294(canvasKey) / `wr`@L1297(localTool) / `Mr`@L1364(chrome) / `Nr`@L1418(local) / `Pr`@L1446(localforage) / `jr`@L1345(缓存get) |
| M2 | `ri`@L1856(缩略图缓存) / `Xr`@L1802(uploadToLocalTool) / `Zr`@L1827(uploadRemoteUrl) / `ii`@L1888(统一上传) / `Sv`@L42838(入库) / `wv`@L42857(删·只DB) / `Ev`@L42883(rescan) / `we`@L43015(rescanResources) |

> ⚠️ **ei/ti 四义补录（据 AI13 裁决表专项）**：`ri`@L1856 的缩略图缓存本体是 `ei`@L1853（`var ei = new Map()`）。注意 `ei`/`ti` 为同名遮蔽五义变量，`var-mapping.txt` 仅收 GAS 两义（`ei`@L43950=`pushToCloud`、`ti`@L43974=`pullFromCloud`），**漏收前端三义**：`ei`@L1853(缩略图缓存 Map)、`ei`@L36784(节点打组 useCallback)、`ti`@L36822(清缓存 useCallback)。凡引用 `ei`/`ti` 必带 `@Lnnnn` 行号，避免与 GAS 义混淆。
| M3 | `Jn`@L32490(生图回调) / `zc`@L19001(统一fetch) / `ht`(AbortController表) / `R`(网关base) |
| M4 | `Z`(建节点) / `Ir`@L36290(onDragOver) / `Lr`@L36293(onDrop) / `Fr`@L36245(onConnectEnd) / `xn`@L31883(onConnect) / `Cn`@L31933(onPaneMenu) / `wn`@L31947(onNodeMenu) / `kn`@L31980(onNodeClick) / `Th`(Director3DNode) / `Wn`@L32302 / `Kn`@L32367 |

---

## 3. 对 TASKS.md / ARCHITECTURE.md 的错锚点回填建议（仅记录，待人工并入）

### 3.1 必须修正的误锚（旧文档"二次误判"）
1. **`Xr` 双重身份**：
   - 旧纠正（AI02/AI05）"`Xr`@L1802 = openInTab，非上传" → **错误**。`Xr`@L1802 = `uploadToLocalTool`（`fetch /api/files/upload`）；`openInTab` 是**组件内另一处 `Xr`@L43881**。
   - 修正：`Xr`@L1802 = uploadToLocalTool；`Xr`@L43881 = openInTab（同名不同物，引用带行号）。
2. **`Zr` 双重身份**：
   - 旧纠正"`Zr` = logout，非下载" → **部分对但漏了上传身份**。`Zr`@L1827 = `uploadRemoteUrlToLocalTool`（远程URL上传）；`Zr`@L43893 = logout/重置配置。
   - 修正：`Zr`@L1827 = uploadRemoteUrlToLocalTool；`Zr`@L43893 = logout。
3. **`Jn` 生图身份**：
   - ARCHITECTURE L1.3 / TASKS 把生图入口写作 `Jn(~L32731)` → 实测 `Jn`@L32490 = 生图主回调（与 LogoIcon `Jn`@L89 同名不同绑定）；提交端点在 L32731 附近。
   - 修正：生图回调 = `Jn`@L32490；LogoIcon = `Jn`@L89。
4. **`Fr` 身份**：ARCHITECTURE L1.2 把 `Fr` 归入 onDragOver 区 → 实测 `Fr`@L36245 = onConnectEnd；onDragOver 是 `Ir`@L36290。

### 3.2 文档滞后（代码已修复，任务可关闭）
5. **TASKS P0「URL 格式不统一破图」**：localTool 后端已实现 `toAbsoluteFileUrl`(resources.ts L31) + 前端 `ii` 兜底。**后端已缓解，前端兜底保留即可，该 P0 任务可降级/关闭**（残留仅 host 硬编码 18080 待参数化）。
6. **TASKS P2「Rescan 孤儿清理只清 local-tool」**：实测后端已实现孤儿清理(resources.ts L120-130)，仅清 `source='local-tool'`，`source='extension'` 仍不清理（真实残留，保留 P2）。
7. **TASKS 行33「Sv() 保存前 toAbsoluteFileUrl，锚 App.js L43462」**：`Sv`@L42838（行号漂移）；且绝对化已在后端 `resources.ts` L31 做，前端 `Sv` 无需再补。建议关闭该行任务。
8. **ARCHITECTURE X1.4「统一同步死循环修复前缺 /files/ 条件」**：实测 L44369 已含 `/files/` + `127.0.0.1/files/tasks` 跳过判断，**修复已落地**，文档可标注"已完成"。

### 3.3 文档补全建议
9. **节点类型清单不全**：ARCHITECTURE L1.2 列 16 种，实测 23+ 种（`audioPlayerNode`/`compareNode`/`rhWebappNode`/`customNode` 等），建议补全。
10. **`zc` 未收录**：统一 fetch 封装 @L19001（带 localTool 代理），是网关请求唯一出口，建议补录映射表。
11. **`we` 三身份**：模块内 `we({x,y})`(屏幕→画布坐标) / `we`@L43015(rescanResources) / 模块级 `we`@L4176 —— 三处同名不同物，文档/映射表必须带行号区分。

---

## 4. 门3 机器校验累计结果

| 模块 | 总计 | ✅ | ⚠️ | ❌ |
|------|------|----|----|----|
| 模块1 | 14 | 14 | 0 | 0 |
| 模块2 | 12 | 12 | 0 | 0 |
| 模块3 | 7 | 7 | 0 | 0 |
| 模块4 | 2 | 2 | 0 | 0 |
| 模块5 | 2 | 2 | 0 | 0 |
| 模块6 | 0 | 0 | 0 | 0 |
| 模块7 | 5 | 5 | 0 | 0 |
| 模块8 | 6 | 6 | 0 | 0 |
| 模块13 | 0* | 0 | 0 | 0 |
| 模块14 | 0* | 0 | 0 | 0 |
| 交叉流 | 8 | 7 | 1 | 0 |
| **合计** | **66** | **65** | **1** | **0** |

> 唯一 ⚠️（`canvas-state-change`）**已结案**：复 grep 确认 App.js 全文该字符串 0 命中，画布持久化走 ReactFlow onChange → `Pr.set`(localforage, `canvas-state-v1-` 前缀 @L1283/@L1637/@L44299)，无 CustomEvent。交叉流文档 §5 已更新。模块6/9/10/13/14 因文档以 `事件常量`/`type:` 字符串 + `Lnnnn` 行号形式呈现（非 `file:Lnnn` 函数调用格式），脚本未抽中引用，已通过手动 grep 全文复核（如 `lg`@L31117、事件字符串 0 命中 `canvas-state-change` 等）。

---

## 6. 本轮新增专项深化（模块2b / 模块3b）

在交叉流结案基础上，本审计进一步下沉两个 P1/网关专项：

### 6.1 模块3b 网关专项（`14-模块3b-网关专项审计.md`）
- 网关字段翻译 3 处改名：`ratio→aspect_ratio` / `seconds→duration` / `input_reference→reference_images`（main.py @L703-707）。前端用原名，网关是唯一翻译层。
- **4 个 501 端点**（main.py @L655-669：music/audio/speech/STT）已实现路由但恒返回 `err(501)`，当前无前端调用，属潜在隐患。
- 任务契约健康：`get_task`@L873 + `confirm_task`@L882 与前端轮询 @L33005 对齐，落盘经 `ii()` 合规。

### 6.2 模块2b 资源面板 P1 根因（`15-模块2b-资源面板P1根因.md`）
- 根因：资源面板 `z`@L29159（粘贴）/`B`@L29188（拖入）的 URL 字符串分支（text/plain @L29180/@L29203）**只写内存 `transitItems`，缺 `Sv()`+`Zr()` 落盘调用**，与"拖入本地文件即落盘"不一致。
- 修复方向：在 URL 字符串分支复用 `Zr`@L1827 + `Sv`@L42838 + dispatch `mutiwindow-task-completed`，与采集流（交叉流流二）统一，可一并缓解"双数据源割裂"风险。

---

## 7. 本轮再深化（模块5 认证 / 模块6 节点注册表）

### 7.1 模块5 认证与配置开关（`16-模块5-认证与配置开关层.md`）
- 本地模式免登录机制：`Oa`@L3543 恒返回 `'local-mode-token'`（localStorage `Ca='auth_token'`@L3524），`Ba`@L3637 恒 true，`Jh`@L30712 注入 `Bearer`。无真实账号/刷新令牌。
- 三处官方接口被桩死（`Ia`/`La`/`za` @L3579-3636，首行 `return []/false`），保留死代码 fetch —— 建议加 `USE_LOCAL_ENGINE`@L36 硬保护或清理。
- `Ca='auth_token'`@L3524 与 `config.js AUTH_TOKEN_KEY='auth_token'`@L33 **同值**，无 key 错配（初版误判已结案）。
- `USE_LOCAL_ENGINE=true`@L36 选基址走 localTool :18080；`REMOTE_BASE=:9004`@L39 仅兜底。

### 7.2 模块6 节点注册表全景（`17-模块6-节点注册表全景.md`）
- ReactFlow 唯一注册表 `lg`@L31117，实测 **27 个业务节点 + `ghostTarget` 辅助节点**（远超 ARCHITECTURE L1.2 的「16 种」）。
- 含 `sd2VideoNode`(Ao)/`discountVideoNode`(os)/`audioNode`(cs)/`audioPlayerNode`(ps)/`customNode`(ms)/`rhWebappNode`(Ms) 等被 ARCHITECTURE 漏列的节点。
- 初版因 grep 输出截断误判 audio/custom 为"孤儿节点"，复 grep `lg`@L31117 全文确认**全部已注册、无孤儿**；`ug`@L31146=`{default:jh}` 仅是编辑器类型映射，非节点注册表。
- 回填建议：ARCHITECTURE「16 种」改为「27+ 节点，详见 `lg`@L31117」。

---

## 8. 本轮三度深化（模块7 存储 / 模块8 任务引擎）

### 8.1 模块7 存储管理器 Q（`18-模块7-存储管理器Q与持久化.md`）
- `Q`@L1476 方法表坐实：`getConfig`@L1491 经 `jr`(缓存get) 读；`setConfig`@L1501 **只写 `wr`(localTool 后端)**，`Ar(e)` 清缩略图缓存（缓存本体即 `ei`@L1853 的 `Map`，非 GAS 义 `ei`@L43950，引用必带行号）。红线 §3.3「不绕过 `Q`」在代码层落实。
- 调用面覆盖：原图读回(@L2227/@L4212)、视频参数(@L8731)、画布恢复三层兜底(@L31804-31826)、资源/任务播种(@L43113/@L43130)、登录态(@L3548)。
- 播种机制：首启打 `kv`/`Ov` 时间戳标记避免重复。

### 8.2 模块8 任务执行与工作流引擎（`19-模块8-任务执行与工作流引擎.md`）
- `globalTasks` 状态机混用 `completed`/`failed`（小写）与 `FAILED`（大写别名）——**枚举双风格隐患**，建议统一。
- **双轮询并存且契约不一**：`Jn`@L32490（网关 `/v1/tasks/`，判 `completed`）vs `R`@L15146（`rr`@L856→`/v1/gateway/ai-app/task/`，判 `SUCCESS`）。两路**均为本地网关**（初版误判 `R` 外联官方云已结案，`rr` 用 `tr()` 解析本地基址）。差异仅子路径+终态词，属历史演进两套契约并存 → 一致性隐患。
- `R`@L15146 设计健全：10 分钟超时(@L15152) + 1.5s 间隔(@L15285) + `D.current` 防重入锁(@L15147)。
- 跨窗口握手事件 `canvas-run-workflow-request`/`canvas-force-save-request`/`canvas-force-save-done`@L31149-31151，配合 `mutiwindow-*` 总线。

---

## 9. 本轮四度深化（模块9 事件总线 / 模块10 自定义节点）

### 9.1 模块9 跨窗口事件总线全集（`20b-模块9-跨窗口事件总线全集.md`）
- 全量 sweep `App.js`：坐实 **15 个 CustomEvent + 1 个 localStorage 通道**（`mutiwindow-clipboard`）。交叉流 §5 仅列 5 个，本篇为权威全集。
- 事件清单含：`canvas-add-resource-request`(`Zn`@L168)、`canvas-run-workflow-request`(`dg`@L31149)、`canvas-force-save-request`(`fg`@L31150)、`canvas-force-save-done`(`pg`@L31151)、`mutiwindow-task-completed`(5 处 dispatch @L38481/L43640/L43676/L43697/L44406，监听 @L31428)、`mutiwindow-rerun-task`(@L44343/@L36618)、`mutiwindow-open-schedule-settings`/`mutiwindow-open-builtin-settings`(@L43774 配对注册)、`mutiwindow-nodes`/`mutiwindow-images`(跨窗口剪贴板，经 localStorage)、`mutiwindow-update-task-meta`(@L41032/@L43764)、`builtin-panel-switch-schedule`、`open-shortcuts-modal`、`yimao:openPromptSettings`(`Va`@L3640)。
- 双层通道架构：CustomEvent 握手 + localStorage 剪贴板载体；`mutiwindow-task-completed` 为 rescan 唯一主触发。
- `canvas-state-change` 结案确认：App.js 全文 0 命中，画布持久化走 `Pr.set`，无该事件。

### 9.2 模块10 自定义节点模板系统（`21-模块10-自定义节点模板系统.md`）
- `customNode` 渲染器 `ms`@L13662（绑定 `lg.customNode`@L31130）；模板存 `CUSTOM_NODE_TEMPLATES='customNodeTemplates'`@L1276。
- 模板经 `Q.getObject/setObject` 持久化（红线 §3.3 合规）；支持增删(`onAdd/DeleteCustomNodeTemplate`@L31308-31309) 与导入导出(`importData`@L44547 老格式兼容映射 @L44555)。
- 与模块6 衔接：`customNode` 是 `lg` 27 节点之一，"注册表(壳) + 模板(内容)"双层结构。

---

## 10. 本轮五度深化（模块11 蒙版绘制 / 模块12 错误分类）

### 10.1 模块11 画布内涂罩绘制子系统（`22-模块11-画布内涂罩绘制子系统.md`）
- ImageNode 蒙版编辑器：实时笔迹 `ue`@L7374 + 提交合成 `V`@L7385（橡皮用 `destination-out`） + 撤销环 `ie`/`ae`（`C.current` 上限 20 帧 @L7332） + 导出 `oe`@L7342（`y.current.toDataURL` → `data.maskUrl`）。
- 坐标换算 `le`@L7355 含旋转/scale 仿射逆变换，健壮。
- 产出 `maskUrl` 供 `Jn`@L32490 局部重绘（inpaint），蒙版经 `ho()`(@L7045) 合成掩膜，属 UI 态数据不单独落盘。

### 10.2 模块12 错误分类与调度中断检测（`23-模块12-错误分类与调度中断检测.md`）
- `mg`@L31152 正则错误分类器：三类（额度/计费、模型权益、内容审核），命中即 `return` 中止调度（@L34772 `M('调度已中断：'+o)`），不重试。
- `hg`@L31157 渠道名分类器（`内置`/外部），仅用于生成历史 `channelName`（6 处调用纯展示，零控制流依赖）。
- 隐患：`mg` 纯文本正则，新增厂商错误文案用词不在表内会漏判（假阴性→误重试）；建议下沉到 HTTP 状态码（429/403 moderation）。

---

## 5. 与既有审计（AI01–AI06）的关系

- AI01/AI02/AI05 已完成 **T0 映射表补全**（本审计采信其结论并独立复核通过）。
- AI03/AI04 模块文档为空或未落盘（AI04 README 声称完成但目录仅 2 文件）——**本审计（AI10）是首个真正落地模块1–4 + 交叉流实质文档的轮次**。
- 本审计在 T0 基础上，对 `Xr`/`Zr`/`Jn`/`Fr` 做了**二次纠偏**（旧纠正本身有漏），并发现 localTool 后端已修复 P0/P2 文档未跟上的事实。

> 本目录为派生审计区；若与代码冲突，以代码为准。回填建议待人工并入权威文档（`TASKS.md`/`ARCHITECTURE.md`/`func-mapping.txt`/`var-mapping.txt`）。

[2026-07-21 据 AI13 裁决表专项：补录 ei/ti 四义（L1853/L36784/L36822/L43950/L43974）]
