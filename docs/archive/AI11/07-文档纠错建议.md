# 07 · 文档纠错建议（给文档维护者 · 不动权威文件）

> 本文件**不修改** `CLAUDE.md`/`ARCHITECTURE.md`/`TASKS.md`/`func-mapping.txt`/`var-mapping.txt`。
> 仅列「审计发现的错锚点」，供维护者回填。所有结论均经 grep 坐实（2026-07-21）。

[2026-07-21 据 AI13 裁决表修订：toAbsoluteFileUrl 真身 localTool resources.ts L31，删 App.js L43462/L43548 旧锚]

---

## 一、ARCHITECTURE.md 错误锚点

| 位置 | 原表述 | 错误 | 真相（grep） | 建议 |
|------|--------|------|------------|------|
| L1.2 / L2.5 | `ii`@~L36284 = uploadFile 统一入口 | 行号错（实际 L1888） | `ii`@L1888 | 改行号 |
| L2.5 表 | 入口1「右键发送到资源 `ii()`→POST」 | ❌ 实为 `Zr`(模块级 uploadRemoteUrlToLocalTool) | `Zr`@L1827，调用点 L5364/L8772/L43104/L43539 均为 `Zr(...,{subfolder:'migrated'})` | 改 `ii`→`Zr` |
| L2.5 表 | 入口2「AI 结果 `uploadFile()`→`ii()`」 | 部分对，`ii` 对 http URL 直返不落盘 | `ii`@L1889 直返；落盘需 `ii(u,{subfolder:'tasks'})`@L33049 | 注明 subfolder 必传 |
| L2.5 | `Xr`=openInTab 在 L43479 | ⚠️ 行号错+身份混 | 模块级 `Xr`@L1802=uploadToLocalTool；组件内 `Xr`@L43881=openInTab | 拆两个 `Xr` 并标作用域 |
| L1.2/L2.5 | `Zr`=下载/注销函数混用 | ❌ 同名遮蔽 | 模块级 `Zr`@L1827=uploadRemoteUrlToLocalTool；组件内 `Zr`@L43893=logout | 拆两个 `Zr` 并标作用域 |
| L1.3 | `Jn`=生图主回调 | ⚠️ 实为局部遮蔽 | `Jn`@L89=SVG Logo 图标组件（本地定义，vendor `Jn` 已改名 `I`）；生图是组件内 `let Jn`@L32490 | 注明局部+行号，勿称 vendor 导入 |
| L2.4 | `we`@L42936 模块级 rescan 主函数 | ❌ 实为组件局部 | `we`@L43015=loadResources(useCallback)；另有 `we`@L4176(视口比例)/`we`@L36253(坐标helper) | 改标注为局部 |
| L1.4 | `Sv`(大写)@L42759；实际 L42838 | ⚠️ 行号漂移 | `Sv`@L42838 | 改行号 |
| L1.2 节点集 | 含 `textNode`/`audioNode`/`textConcatNode` | ⚠️ 未出现在 spawnable Set | spawnable@L4141/L5446 仅 13 种，无此三者 | 核实是否可拖放生成 |

## 二、TASKS.md 错误锚点

| 位置 | 原表述 | 错误 | 真相 | 建议 |
|------|--------|------|------|------|
| 行34(注释/P0) | `Zr()` 下载函数 | ❌ 模块级 `Zr`=uploadRemoteUrlToLocalTool(实为上传) | `Zr`@L1827 | 改「uploadRemoteUrlToLocalTool」 |
| P1-2 | `B`回调(L29160–79) URL不落盘 | ✅ 属实 | 与 resourceAdded 走 `Zr` 落盘不一致 | 统一落盘路径 |
| P1-3 | `R`回调(L29165) 上传不入库 | ✅ 属实 | `R` 为组件局部 handler，非模块级 | 注明局部 |
| 修复清单 P2 | wv()@L42857 | ✅ 属实 | 行号对 | 保留 |
| 修复清单 P0 | Sv() 保存前 toAbsoluteFileUrl @L43462 | ❌ 误标 | `toAbsoluteFileUrl` 真身在 `localTool/src/routes/resources.ts#L31`（定义处，硬编码 18080）；App.js 内 0 命中，L43548 仅 `${Hr}${url}` 路径拼接非函数定义 | 删 App.js L43462/L43548 旧锚，改指 resources.ts:L31 |

## 三、func-mapping.txt / var-mapping.txt 补录建议（不回写，待人工并）

| 名 | 行号 | 身份 | 建议 |
|----|------|------|------|
| `Tv` | L42866 | clearResources | 补 func-mapping |
| `Cv` | L42851 | toggleFavorite | 补 func-mapping |
| `xv` | L42821 | listResources | 补 func-mapping |
| `we`(四义) | L3(`Tr as we`)/L4176/L43015/L36253 | vendor Tr/视口比例/localResources/坐标helper | var-mapping 标注「多义，引用必带行号」 |
| `R`(gatewayBase) | 作用域内 | 网关基址=config.DEFAULT_GATEWAY_URL（L43212 `ot`），非 DEFAULT_ENDPOINT 直接绑定 | 注明来源 |

## 四、同名遮蔽总表（最高优先级警告）

> ⚠️ **重要更正（基于 import 映射复核）**：`App.js` L3 的 import 显示 `Xr`/`Zr`/`Jn`/`we`/`Q`/`Mr`/`Nr` 等名字，部分是 vendor 导入后**重命名**的结果。下表区分「vendor 导入原名 → App.js 内别名」与「App.js 本地定义」。
> - vendor `Jn` 在 App.js 内已改名为 `I`（`Jn as I`），故 App.js 内的 `Jn`@L89/L32490 是**本地重新定义**，并非 vendor 导入。
> - vendor `Q` 在 App.js 内已改名为 `me`（`Q as me`），故 App.js 内的 `Q`@L1476(StorageManager) 是**本地定义**，`me` 才是 vendor 的 Q。
> - vendor `Tr` 在 App.js 内改名为 `we`（`Tr as we`），故 `we` 除本地三处外，还有 vendor 导入版。

| 名字 | 身份1@行号 | 身份2@行号 | 身份3@行号 | 备注 |
|------|-----------|-----------|-----------|------|
| `Xr` | uploadToLocalTool@L1802（本地） | openInTab@L43881（局部） | — | import 无 `Xr`，L1802 为本地定义 |
| `Zr` | uploadRemoteUrlToLocalTool@L1827（本地） | logout@L43893（局部） | — | import 无 `Zr`，L1827 为本地定义 |
| `Jn` | SVG Logo 图标组件@L89（本地） | 生图主回调@L32490（局部） | — | vendor `Jn` 已改名 `I`，非 vendor 导入 |
| `we` | vendor `Tr`@L3(`Tr as we`) | 视口比例@L4176 | loadResources@L43015 | 坐标helper@L36253 |
| `Q` | StorageManager@L1476（本地） | vendor `Q` 以 `me` 存在 | — | 07 旧表"Q=vendor 导入"有误，已更正 |

> **给下一个 AI 的硬提醒**：引用以上符号**必须带作用域+行号**，绝不可只写名字或照搬旧文档。且写 `Jn`/`Q` 时勿称"vendor 导入"——二者在 App.js 内均为本地定义。
