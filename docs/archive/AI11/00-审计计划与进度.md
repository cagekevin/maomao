# 00 · AI11 审计计划与进度（五关流水线落地）

> 审计执行区：`docs/AI11/`（2026-07-21）
> 依据：先读 `CLAUDE.md` + `docs/` 全量文档，再实地 grep 代码核对行号。
> 红线约束：本次**只产出审计文档**，不修改任何运行代码（`App.js`/`config.js`/`localTool`/`apimart-gateway` 一律只读）；权威文档（`CLAUDE.md`/`ARCHITECTURE.md`）仅作真理参照，不当作重写对象。
> **文件位置约束**：本工作区所有产物**仅存放于 `docs/AI11/`**，不放入 `docs/` 根或其他目录；不修改 `AI01`–`AI10` 任何文件。

---

## 一、本轮定位（与既有 AI 的关系）

- AI02/AI04/AI05 已建立方法论并产出「总览 / 索引 / 映射缺口核对」，**但模块文档 T2.1–T2.4 与交叉流 T2.5 始终未真正产出**（AI05 的 `02~06` 标「待产」、AI03 仅 README、AI01 仅测试脚本）。
- 本轮目标：**实际落地模块审计 + 交叉流 + 机器校验**，把前面空转的部分补出来。
- 关键态度：**不轻信已有文档锚点**（行号会漂移、且已知有错锚点如 `Jn`/`Zr`），每条事实自己 grep 坐实。

## 二、五关流水线（本次执行方式）

1. **门1 锚点门（写前）**：每个混淆名先查 `func-mapping.txt` / `var-mapping.txt`；未收录的自己 grep 源码坐实语义再写，禁止边写边猜。
2. **门2 强制引用（写中）**：每条事实带 `App.js:Lnnnn` / `localTool/...:Lnnn` / `config.js:Lnn` / `网关main.py:Lnnn`，禁止无出处断言。
3. **门3 机器校验（写后）**：用 `search_content` 复核文档内引用的行号/符号在源码中确实存在，输出 `校验报告-AI11.md` 逐条 ✅/❌（单会话用脚本化 grep 替代）。
4. **门4 对抗审计 / 门5 入库**：单人单会话，用「写手→自 grep 复核→列 ❌ 项」替代。
5. **防污染**：模块 doc 只当草稿线索，最终回代码核对；映射表是唯一可信锚层，发现新名单独记录不回写原表。

## 三、模块划分（沿用 TASKS.md T2.1–T2.4 + 阶段2.5）

| 模块 | 内容 | 产出文件 |
|------|------|---------|
| 计划 | 本文件 | `00-审计计划与进度.md` |
| 映射复核 | T0 缺口自 grep 坐实（不盲信 AI05） | `01-映射表缺口复核.md` |
| 模块1 | 系统初始化与配置层 | `02-模块1-初始化与配置层.md` |
| 模块2 | 本地数据层与 Rescan | `03-模块2-本地数据层与Rescan.md` |
| 模块3 | AI 生成与网关层 | `04-模块3-AI生成与网关层.md` |
| 模块4 | 画布引擎与节点 | `05-模块4-画布引擎与节点.md` |
| 交叉流 | 4 条端到端流缝合 | `06-交叉流审计.md` |
| 纠错 | 给文档维护者的错锚点回填清单（不动权威文件） | `07-文档纠错建议.md` |
| 校验 | 门3 机器校验报告 | `校验报告-AI11.md` |

## 四、边界契约（每个模块必含子节）

记录本模块**接收/发出**的接缝：HTTP 路由、`window` CustomEvent、`config` 开关。均带 `file:line`，作为阶段2.5 的缝合点。

## 五、本次不触碰

`dist/`、`reference/`、`src/v2/`、`scripts/`（除只读参考）、任何运行代码；不回写 `func-mapping.txt`/`var-mapping.txt` 原表。
