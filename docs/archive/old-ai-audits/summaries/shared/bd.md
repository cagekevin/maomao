## bd — 🔗 共享函数

| 属性 | 值 |
|------|----|
| 行号 | L20297 |
| 类型 | fn |
| 入度 | 4 |
| 依赖 | `Y` (React), `md`, `X` (JSX(createElement)) |
| 被依赖 | `Id`(usePropertyPanel), `Rd`, `Bd`(ModelInspectorPanel), `Zd` |
| 参数 | `label`→`e`, `rangeAriaLabel`→`t`, `numberAriaLabel`→`n`, `value`→`r`, `onValueChange`→`i`, `onRangeChange`→`a`, `onNumberChange`→`o`, `onNumberBlur`→`s`, `min`→`c`, `max`→`l`, `step`→`u` |

- **数据**: value
- **hooks**: useRef, useEffect
