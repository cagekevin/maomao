## vi — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L1191 |
| 类型 | var |
| 入度 | 1 |
| 依赖 | `Y` (React), `Un` (createPortal), `X` (JSX(createElement)) |
| 被依赖 | `Nv` |
| 参数 | `open`→`e`, `title`, `onClose`→`n`, `children`→`r` |

- **数据**: createPortal, body
- **hooks**: useState, useRef, useEffect, useCallback
