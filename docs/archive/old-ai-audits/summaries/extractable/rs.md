## rs — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L8823 |
| 类型 | fn |
| 入度 | 0 |
| 依赖 | `Y` (React), `Un` (createPortal), `X` (JSX(createElement)) |
| 参数 | `open`→`e`, `modelNames`→`t`, `specsByName`→`n`, `selectedModel`→`r`, `onClose`→`i`, `onConfirm`→`a` |

- **数据**: map, createPortal, body
- **hooks**: useMemo
