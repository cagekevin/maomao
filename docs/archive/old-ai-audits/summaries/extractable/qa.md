## qa — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L1389 |
| 类型 | fn |
| 入度 | 1 |
| 依赖 | `Y` (React), `Un` (createPortal), `X` (JSX(createElement)) |
| 被依赖 | `Ja`(filterAndSortByType) |
| 参数 | `open`→`e`, `onClose`→`t`, `onUse`→`n`, `onToast`→`r`, `defaultCategory`, `presetPrompts` |

- **数据**: slice, map, get, toLowerCase, delete, filter
- **hooks**: useState, useEffect, useMemo
