## zg — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L35226 |
| 类型 | fn |
| 入度 | 1 |
| 依赖 | `Y` (React) |
| 被依赖 | `Nv` |
| 参数 | `isLoaded`→`e`, `showTaskList`→`t`, `globalTasks`→`n`, `handleUpdateGlobalTasks`→`r`, `showToastMessage`→`i`, `localPort`→`a`, `localToolConnected`→`o`, `sd2VideoApiUrl`→`s`, `sd2VideoApiKey`→`c`, `videoApiUrl`→`l`, `videoApiKey`→`u`, `discountVideoApiUrl`→`d`, `discountVideoApiKey`→`f`, `aiAppApiUrl`→`p`, `aiAppApiKey`→`m` |

- **数据**: status, data, fail_reason, progress, video_url, result_url
- **hooks**: useRef, useCallback, useEffect
