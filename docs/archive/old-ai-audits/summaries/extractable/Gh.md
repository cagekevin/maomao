## Gh — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L27323 |
| 类型 | fn |
| 入度 | 1 |
| 依赖 | `Y` (React), `Un` (createPortal), `X` (JSX(createElement)) |
| 被依赖 | `xg` |
| 参数 | `imageUrl`→`e`, `initialTool`→`t`, `onSave`→`n`, `onClose`→`r` |

- **数据**: drawImage, getImageData, tagName, scrollLeft, scrollTop, length
- **hooks**: useRef, useState, useEffect
