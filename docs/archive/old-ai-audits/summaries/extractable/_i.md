## _i — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L1191 |
| 类型 | var |
| 入度 | 1 |
| 依赖 | `Y` (React), `X` (JSX(createElement)) |
| 被依赖 | `Nv` |
| 参数 | `targetRef`→`e`, `onRequestFullscreen`→`t`, `onResizeEnd`→`n`, `minWidth`, `maxWidth`, `minHeight`, `maxHeight`, `className` |

- **数据**: preventDefault, stopPropagation, clientX, clientY
- **hooks**: useState, useCallback
