## hv — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L38595 |
| 类型 | fn |
| 入度 | 1 |
| 依赖 | `Y` (React), `X` (JSX(createElement)) |
| 被依赖 | `Nv` |
| 参数 | `open`→`e`, `projectId`→`t`, `projectName`→`n`, `existingAppId`→`r`, `onClose`→`i`, `onPublished`→`a` |

- **数据**: appName, description, visibility, map, value, target
- **hooks**: useState, useEffect, useMemo
