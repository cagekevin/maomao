# 123 · 视频剪辑器 —— 架构定义（自下而上）与落地施工图

> **状态**：定义已冻结（L0 已裁定）· **日期**：2026-09-13 · **审计轮**：2026-09-13（按「架构师改码 7 步法」做过取证审计，修正见 §四）
> **上游**：`120`（数据流契约：行为规则）· `113`（移植与收口：拿什么/不拿什么 + Gate）
> **本文补的缺口**：120 定的是**行为契约**（"应该怎样"），113 定的是**移植清单**（"拿什么"）。两者都没有把**地基**单独定义出来，也**没有把定义锚到仓库里可核对的符号上**——本文补这两样。
> **外部参考**：`~/Downloads/AI-Canvas-tauri-video-editor`（**A**）· `~/Downloads/Nomi-main`（**N**）。**均只作参考实现读，不整包搬**。
> **冲突裁决**：与 `113` 冲突以本文为准；与 `120` 冲突以 `120` 为准（120 是契约真相源）。

---

## 〇、方法论与「可执行性契约」

### 0.1 三层依赖（方向单向，不可颠倒）

```
L0  定义（地基）   时间原语 / 数据模型 / 不变量 / 原语签名 / 结果契约 / 能力·持久化·边界契约
L1  落地（骨架）   目录归属 / 外部移植映射 / 施工任务卡 G0–G5 / 门禁 / 必须新增的守卫
L2  功能（最后）   工具带 / 轨道 / 交互 —— 只是 L0+L1 的投影，不定义新东西
```

### 0.2 唯一判据

> **每个 L2 功能必须能回指到 L0 的某条定义或原语。回指不上 → 回 §一 补定义，不许在 UI 里就近硬写。**

### 0.3 可执行性契约（**本次审计新增 · 全文最高优先**）

> 由母体「文档描述的符号与仓库真实符号之间没有校验」直接推出。**违反即视为未定义完。**

| 规则 | 内容 |
| --- | --- |
| **E1 三列制** | L0 每条定义 / 原语 / 契约必须带三列：**锚点** · **状态** · **验证**。 |
| **E2 锚点非空** | 锚点 = `文件:行`（已有）或 `待建 文件路径`（不存在）。**锚点为空 = 未定义完，不得进入施工**。 |
| **E3 禁未取证符号** | 禁止引用**未在仓库中取证过**的符号（本次审计已实证：`mediaType.ts`、`checkRegistered` 导出、`UPLOAD_DIRS.videoEditor`、`videoEditorOpen`、`video-editor-*` 键**都不存在**）。引用前必须 `grep` / 读文件确认。 |
| **E4 状态二分** | `状态` 只有两个值：`已有`（本文件已存在，锚点给行号）· `待建`（锚点给要创建的确切路径）。**没有"大概有"**。 |
| **E5 验证可跑** | `验证` 必须是**可执行命令**或**可断言的测试文件名**，不得写"应当正确"。 |
| **E6 拒绝不是结论** | 任何"某能力已有/某闸能拦"的说法，必须附取证行；拿不出 → 标 `待建` 并列为待补守卫（§二.6）。 |
| **E7 兜底数最少（最优解判据）** | **兜底/守卫的数量是"地基做没做对"的体温计**：每加一条兜底前，先回答"它在掩盖哪条地基缺陷"。答得出 → **改地基、删兜底**；答不出才是真需求。见 §二.9 全表。 |

### 0.4 执行顺序

0. **第 0 步（开工前置，见 §0.5）**：能力重叠普查 +「时间轴真源」裁决 —— **必须先于 G0**；
1. L0 已冻结（§一）；
2. 按 §二.3 的任务卡 G0→G5 顺序施工，每张卡的前置是上张卡的验收通过；
3. §三 只在实现某个 UI 时回查（回指）；§五 是取证实录，供任何人复核锚点。

### 0.5 第 0 步：能力重叠普查与「时间轴真源」裁决（**开工前置**）

> **为什么它在最前**：L0 回答"我们要什么"，但**没回答"仓库里已经有什么、该不该另起"**。不问就开工，剪辑器会自然长出第二套。

**取证（2026-09-13）—— "两套"已经存在一半：**

| 重叠项 | 现有实现（取证） | 风险 |
| --- | --- | --- |
| **时间轴数据模型** | `src/components/nodes/VideoProcessNode.tsx:312-313` `VTrack{id,name,kind,clips,muted}` / `VClip`；`:481-483` 已构造 `video-track-1`；`:1594-1649`「concat：多轨时间线」面板；`:1041+` trim/concat 导出 | 与 `Track/Clip` **高度同构** → 120 §四 只写"作 UI 参考、不重复逻辑"**不足以阻止第二套** |
| **抽帧 / 缩略图** | `src/components/base/ui/VideoThumbnail.tsx` · `VideoProcessNode.tsx:639-668` · `VideoExtractNode` · `Director3DNode`（全库 19 文件命中） | 已是**多份实现**；剪辑器胶片条（120 C11.10）若再写 = 又一份 |
| **引用登记（`I6` 的落点）** | **未见实现**（`src/` 中"引用感知 GC / isReferenced / collectRefs" **0 命中**；`13-文件生命周期` 是**方案文档**） | 谁先建谁定形态；剪辑器自建 = **第三套** |

**处置判据（沿用 Step 3「收口探测、保留判据」）：**

- **运算重复**（时间映射 / trim 语义 / 抽帧缩略图 / 拼接）→ **收口为共用原语**；以现有实现为准，不满足处**改造现有**（**"改造现有"优先于"新建"**）。
- **判据 / 宿主不同**（`VideoProcessNode` = "这个节点怎么处理"；剪辑器 = "这条片子怎么编"）→ **保留分层**，不合并。
- 若要**新建**唯一实现，必须写出"现有为什么承载不了"；否则违铁律 5。

**产出（已完成 2026-09-13）**：`docs/124-视频剪辑器-能力重叠普查与时间轴真源裁决-2026-09-13.md` —— 22 项能力的 `现有实现(file:line) → 处置` 全表 + 三条裁决。
**三条裁决摘要**：① 三套时间轴（`VideoProcessNode` / `director3d Timeline` / 剪辑器）**对象与判据不同 → 不合并**；② **「时间轴运算层」（时间↔像素 / 刻度 / 吸附 / 框选 / 播放钟）收口到 `base/utils/timeline/`**，现有 2 份为待迁存量；③ **原 §二.7 P3 的 `view/` 层作废**——视图换算的家是 `base/`，不是剪辑器内部。
方法沿用仓内先例：`docs/119-节点层收口机会普查-重复逻辑收敛清单-2026-09-11.md`。

---

## 一、L0 定义（地基 · 已冻结）

> 每张表的三列（锚点 / 状态 / 验证）按 §0.3 E1 强制。
> 全部落 `core/`（`src/components/videoEditor/core/`），**零 React、零存储、零网络**（120 §二 铁律）。

### 一.1 时间原语：唯一时间单位 = **秒（float）**

| 记号 | 定义 | 锚点 | 状态 |
| --- | --- | --- | --- |
| `timelineStart` | 片段在工程时间轴上的起点（秒） | `待建 src/components/videoEditor/core/types.ts` | 待建 |
| `sourceStart` / `sourceEnd` | 从源素材取用的入点 / 出点（秒） | 同上 | 待建 |
| `duration` `:= sourceEnd - sourceStart` | 派生量，**不落盘** | 同上 | 待建 |
| `clipEnd` `:= timelineStart + duration` | 派生量 | 同上 | 待建 |

> **更新(2026-09-13 · 取证修正 · 字段名)**：本表原写 `sourceIn` / `sourceOut`，与仓库事实冲突 → 已就地改为 **`sourceStart` / `sourceEnd`**。依据：
> ① 仓内既有持久化字段就是这两个名（`VideoProcessNode.tsx:204 TimelineClip{sourceStart,sourceEnd,timelineStart}`），而本文 §二.2 明确要求「片段持久化字段须与 `TimelineClip` 对齐」；
> ② 前置收口已把跨域映射原语下沉为 `base/utils/timeline/sourceTime.ts`，其入参 `ClipTimeWindow{timelineStart?,sourceStart?}` 用的就是 `sourceStart`，且 §一.4 已由审核改判「A 组跨域部分 → `base/utils/timeline/`，**直接复用、严禁重写**」；
> ③ 若沿用 `sourceIn`，共用原语无法被 `Clip` 直接喂入，每个调用点都要写适配 ⇒ 正是要消灭的「同一真相两份」。
> **实现落点**：`core/types.ts`（G1 已完成）。

**纪律**
- **T1（唯一单位）**：时间戳一律秒 / float；**帧不是存储单位**，只是显示与导出采样。验证：`npm run check:arch`（见 §二.6 待补规则）+ 评审。
- **T2（派生不落盘）**：`duration` / `clipEnd` 现算，不进持久化。验证：`tests/unit/videoEditor/normalize.test.ts`（待建）断言序列化产物无这两键。
- **T3（浮点纪律）**：比较走 `EPS = 1e-3`；吸附归到离散候选值，禁止就地 `±=`。验证：`snapTime` 单测（§一.4 D 组）。

**唯一映射原语（预览=导出的最小充分条件）**

```
sourceTimeAt(timelineTime, clip) := clip.sourceStart + (timelineTime - clip.timelineStart)
```

> **更新(2026-09-13)**：公式与**入参顺序**按已实现并已收口的共用原语对齐 ——
> `base/utils/timeline/sourceTime.ts`（`sourceTimeAt(timelineTime, clip)` + 反函数 `timelineTimeAt(sourceTime, clip)`，`clip` 为窄结构类型 `ClipTimeWindow`，零依赖）。
> 剪辑器**直接用**它（`Clip` 结构上即其入参），**不在此另写一份**（§一.4 的 A 组二分裁定）。

**为什么不是帧 —— 已由取证裁定，无需用户拍板**

| 证据 | 结论 |
| --- | --- |
| `src/components/base/utils/videoEngine.ts:57` 起：`start/end`、`startTime`、`metadata.duration` **全部是秒** | 仓库唯一的媒体引擎**已经用秒** |
| 若改帧：所有 `videoEngine` 调用点都要加"秒↔帧"转换层 | = 制造**第二套时间表示**，违反 120 §0.5「数据模型只有一套」 |
| 120 C12.2「帧率随时可改」+ C12.3「改了不移动片段」 | 帧进存储则必须重解释全部时间戳 → 两条无法同时成立 |
| （对照 N `workbench/timeline/timelineTypes.ts:71` 把 `fps` 写进 state；A `types/videoEditor.ts` 用秒） | 我们**与 A 同侧**，并把它变成全仓一致 |

### 一.2 数据模型：SSOT 三实体

**身份**

| 实体 | 身份 | 锚点 | 状态 |
| --- | --- | --- | --- |
| `Project` | `projectId`（工程身份即项目；**无锚点节点**） | 键模板 `待建`（§一.7） | 待建 |
| `Track` / `Clip` | `track.id` / `clip.id`（全局唯一） | `idGen.ts:11`（生成器已有） | 已有 |

**id 生成器（已取证）**：`src/components/base/core/idGen.ts:11` — `export function generateId(prefix = 'id'): string`，格式 `${prefix}_${Date.now().toString(36)}_${Math.random().slice(2,8)}`。
> **禁止照搬 A 的 id 生成**（A 用 `${clip.id}-b${Date.now().toString(36)}`，见 §二.5）。一律 `generateId`。

**引用三态（"片段指向的素材"）**

```
clip 素材来源 := sourceUrl（画布节点产物 / 本机落盘 /files/…）
             ｜ assetId（素材库资源，经素材库 resolve）
             ｜ nodeId （仅作溯源：回填产物 URL 用，不作寻址）
```

| 规则 | 内容 | 锚点 | 状态 |
| --- | --- | --- | --- |
| **R1** | `sourceUrl` 空且 `assetId` 解析不出 → **断链**（判定与处置只走 120 C13）。**"解析 URL"与"判是否断链"必须是同一个原语**——一处决定「能不能读」（§二.7 P4） | `待建 data/sourceResolver.ts` | 待建 |
| **R2** | `nodeId` **只读不写**——是溯源标记，不是寻址键 | 同 R1 | 待建 |
| **R3** | id 一律 `generateId`；**禁时间基后缀** | `idGen.ts:11` | 已有 |

**轨道：两类 × 三状态**

| 维度 | 定义 | 锚点 | 状态 |
| --- | --- | --- | --- |
| 两类 | **主视频轨**（磁吸：无空隙、压实）· **自由轨**（叠加 / 音频：可留空）——判据是**显式 `overlay` 标记**，不靠"第一条"位置推断 | `待建 core/types.ts` | 待建 |
| 三状态 | `locked` / `hidden` / `muted`——**只增状态字段，不增实体**（120 C7.1） | 同上 | 待建 |

> **M1 轨道集合固定**（视频轨 ×1 + 音频轨 ×1）。`createTrack`/`moveTrack`/`removeTrack` 属 M2 叠加轨能力，**只定义不暴露**。

### 一.3 不变量（每条给可测断言 + 验证）

| # | 不变量 | 可测断言 | 验证（可跑） |
| --- | --- | --- | --- |
| **I1** | 主轨**默认**磁吸：无空隙、首尾相接。**唯一允许产生空隙的显式动作 = 留洞删除**（120 C15）；此后 **「闭合空隙」是恢复磁吸的显式动作**（⚠️ 120 C15 引用过「闭合空隙」但从未定义入口/原语，与此处冲突——**本节裁决**，见 §一.4 `closeGaps`） | 磁吸态：`clips[i+1].timelineStart === clipEnd(clips[i])`（EPS 内）；留洞后允许空隙；`closeGaps` 后复归磁吸 | `tests/unit/videoEditor/timelineOps.test.ts`（待建） |
| **I2** | 顺序 = 入轨先后（120 C11.3） | 连续入轨 A→B→C，主轨顺序恒 A,B,C；库内**无**任何排序函数 | 同上（断言不存在排序导出） |
| **I3** | 单一时间单位（T1） | `types.ts` 时间字段仅 §一.1 四个 + 派生 | §二.6 待补 arch 规则 |
| **I4** | 无变化不落盘（120 C14.1） | 无变化操作返回值 `===` 入参（同引用），`revision` 不自增 | `timelineOps.test.ts`（同引用断言） |
| **I5** | 单一写者（120 C2.4） | `tracks` 写点只有 `projectRepository` | `npm run check:arch`（现有规则 6/9 同族） |
| **I6** | 引用被生命周期看见（120 C13.1） | 每个 `sourceUrl`/`assetId` 都在文件生命周期层"被引用"集合内 | ⚠️ **不是"复用"**：`src/` 中引用感知 GC **0 命中**，`13-文件生命周期` 是**方案**不是实现 → 须**与 `13`/`122` 合建**，剪辑器以"引用来源之一"接入（§0.5 / §四） |
| **I7** | UI 分档不下沉（120 §0.5） | `core/` 内查不到"基座/工作台"分支 | §二.6 待补 arch 规则 |

### 一.4 原语清单（只定义签名，不实现）

> 分组按依赖方向：E → A → B → C → D。
> **落点（定位审计 + §0.5 普查 + 前置收口三重修正）**：**E/B/C 三组 → `core/`**（领域真相，零 React / 零 IO）；**A 组按"跨域性"二分** —— **跨域共用**部分 → `base/utils/timeline/`（前置收口已下沉 `sourceTime.ts` 的 `sourceTimeAt` / `timelineTimeAt`，**G1 直接复用、严禁重写**），**剪辑器专有**部分 → `core/`（`relayoutSequential` / `closeGaps` 等）；**D 组 → `base/utils/timeline/`**（共用层，不落剪辑器内部）—— 普查出它跨 3 个域共用（`VideoProcessNode` / `director3d` / 剪辑器），**原拟的 `view/` 层作废**（见 §二.7 P3 与 `docs/124`）。
> **锚点列**：`已有 src/components/base/utils/timeline/<file>` 或 `待建 src/components/videoEditor/core/<file>`（本仓 videoEditor 代码为 0；`base/utils/timeline/` **已存在** `sourceTime.ts` → **G1 是续建、不是从零建**：开工前先 `ls` + 读该目录，防写第 2 份）。
> **来源列**：`A`/`N` = 可参考搬；`新` = 新增。
> **里程碑过滤（Step 1 反向证伪补做）**：120 §0.8 已把「**音量包络** / 转场 / 画中画(transform) / 叠加轨(增删轨)」判给 **M2**。原则：**数据模型留字段**（免迁移，120 C1），**但 M1 不实现其原语**。M1 不做的原语（标 `M2`）：`evaluateVolume` · `setVolumePoint` · `removeVolumePoint` · `evaluateTransitionAlpha` · `createTrack` · `removeTrack` · `moveTrack`。
>
> ★**更新(2026-09-13 · 实现修正)**：「留字段」指**序列化位置**保留，**不是 M1 替 M2 定型**。故 M2 四字段（`volumePoints` / `transform` / `transitionIn` / `transitionOut`）在 `core/types.ts` 里的类型是 **`unknown`** ——
> 若 M1 写死一个猜测形状（如 `{t,gain}[]`），加载时就会拿这个猜测去"校验"；M2 真实形状一旦不同 → 字段被**静默过滤** = 数据丢失且无提示（假成功）。
> M2 定稿时把 `unknown` 收窄为真实类型即可：**只改类型，无数据迁移成本**。

**E · 规范化（最底层，最先做）**

| 原语 | 签名 | 语义 / 边界 | 来源 | 锚点 |
| --- | --- | --- | --- | --- |
| `createEmptyProject` | `() => Project` | 空工程（fps 30 / playhead 0 / 固定双轨） | N `timelineMath.ts` | `core/normalize.ts` |
| `normalizeProject` | `(raw: unknown) => Project` | 加载期**唯一**入口：缺字段补全 + `schemaVersion` 守卫 + **重复 id 重铸** | N `timelineMath.ts:128` | `core/normalize.ts` |

> **`normalizeProject` 的语义必须一分为二**（否则是把"损坏"当"旧版"静默补全 = 假成功）：
> ① `schemaVersion` **低于当前** → 走**迁移**（缺字段有已知语义，补默认是对的）；
> ② 版本**缺失 / 高于当前 / 结构损坏** → **拒载并明示**（对齐 120 C1「读到更高版本按『无工程』处理，**不按缺字段渲染**」）。
> **id 去重重铸已删（§二.9 第 1 行）**：我们有 `generateId`（含 6 位随机段），N 的事故根因（**归零自增序列**）在本仓不存在 → 为不会发生的碰撞加防卫，违铁律 5「不虚构」。

**A · 时间/几何查询（只读）**

| 原语 | 签名 | 语义 / 边界 | 来源 |
| --- | --- | --- | --- |
| `clipDuration` | `(clip) => number` | `max(0, sourceOut - sourceIn)` | A `getClipDuration` |
| `clipEnd` | `(clip) => number` | `timelineStart + clipDuration` | A `getClipEnd` |
| `sourceTimeAt` | `(clip, timelineTime) => number` | §一.1 映射原语（预览=导出共用） | 新 |
| `findClipAt` | `(clips, t) => {clip,index} \| null` | 末片段**闭区间** | A `findClipAtTime` |
| `activeClipsAt` | `(track, t) => clip[]` | 该时刻在轨上显示/发声的片段 | A `getActiveClips` |
| `timelineDuration` | `(tracks) => number` | 全轨最大 `clipEnd` | A `computeTimelineDuration` |
| `clipEdges` | `(clips) => number[]` | 吸附候选（含 0），去重升序 | A `clipEdges` |
| `appendTime` | `(track) => number` | 该轨末尾（= 入轨追加位置） | N `findAppendFrame` |
| `hasOverlap` | `(track, clip) => boolean` | 自由轨碰撞判据 | N `hasClipOverlap` |

**B · 编辑（输入 → 新数组）**

| 原语 | 签名 | 语义 / 边界 | 来源 |
| --- | --- | --- | --- |
| `relayoutSequential` | `(clips) => clips` | 主轨压实（保相对顺序、只合空隙）= **I1/I2 的实现** | A |
| `moveClipTo` | `(clips, id, index) => clips` | 拖序（目标夹取；未变则**原样返回** = I4） | A `moveClipTo` |
| `duplicateClip` | `(clips, id) => clips` | 副本紧随其后（复用已修剪物料，120 C15.2） | A |
| `removeClips` | `(clips, ids, mode:'lift'\|'ripple') => clips` | **二态**：留洞 / 波纹前移。⚠️ **不引入"至少保留一个"** | A（改语义） |
| `splitAt` | `(clips, t) => clips \| null` | **严格内部**才可切（落在片段头/尾 → `null`，调用处置灰 + tooltip，**不静默**）。★更新(2026-09-13)：**不设 `MIN_SPLIT_MARGIN` 魔法容差** —— §二.9 第 4 行裁定「弱化」：「落在边界」已由播放头吸附到 `clipEdges` 变成**明确状态**，容差只剩浮点意义（`EPS`） | A `splitClipsAt`（改判据） |
| `trimLeftAt` | `(clips, t) => clips \| null` | 保留播放头**右**半 | 新（N `timelineEdit.ts` 可参考） |
| `trimRightAt` | `(clips, t) => clips \| null` | 保留播放头**左**半 | 新（同上） |
| `freezeFrameAt` | `(clips, t, dur) => {clips, frameAt} \| null` | 切开 + 插静止片段；产物是**图片片段**，不新增 clip 类型 | 新 |
| `updateClip` | `(tracks, id, fn) => tracks` | 按 id 更新所属轨（主轨顺排、自由轨保留位置） | A `updateClipInTracks` |
| `placeClipAt` | `(clips, id, t) => clips` | 自由轨定位（**不压实**） | A |
| ~~`closeGaps`~~ → **不另立原语**（更新 2026-09-13） | — | **恢复磁吸**：把留洞产生的空隙前移闭合。★本行自己写明「**语义入口 = `relayoutSequential`**」⇒ 两者同义；另立别名 = 两名指一物（7 步法 Step 6 明禁）。mockup E2 的「有 N 处空隙 · 闭合」按钮**直接调 `relayoutSequential`**；`timelineOps.test.ts` 已断言库内**不存在** `closeGaps` 导出 | 新（并入 `relayoutSequential`） |
| `setVolumePoint` / `removeVolumePoint` | `(clip, t, gain) => clip` | 音量包络控制点（按 t 排序保可插值）—— **M2，M1 不实现** | A |

**C · 判据（谓词，唯一真值源）**

| 原语 | 签名 | 语义 / 边界 | 来源 |
| --- | --- | --- | --- |
| `routeClipToTrack` | `(kind) => 'video' \| 'audio'` | **分轨唯一判据**（120 C11.1）；UI 禁内联 `kind==='audio'?…` | 新（N `getTrackTypeForClipType` 结构参考） |
| `needsCompositing` | `(project) => boolean` | **导出路径唯一判据**（120 C5.1）。⚠️ 必须含"片段尺寸 ≠ 工程尺寸"（A 版没有，须补）。**入参收成 `Project` 而非 `tracks + size`**：判据的真源是"工程基准"，两参并行必漂（§二.7 P6） | A（补判据 + 收参） |
| `hasMixedSources` | `(profiles) => boolean` | 异构素材（编码/分辨率不一致） | A |
| `isTrackLocked` / `isClipLocked` | `(tracks, id) => boolean` | **锁定收口一处**（120 C7.3）；未知按未锁定 | A |
| `isTrackHidden` / `isTrackMuted` | `(tracks, id) => boolean` | 只影响渲染与导出，**不动数据** | 新 |

**D · 视图换算（**共用层** · 落 `base/utils/timeline/`，不落剪辑器内部）**

| 原语 | 签名 | 语义 / 边界 | 来源 |
| --- | --- | --- | --- |
| `snapTime` | `(t, candidates, pps, tolPx) => number` | 容差以**像素**给 → 换算成秒。★更新(2026-09-13)：`tolPx` **必传**（容差多大是各域判据，共用层不预设默认值 —— 预设就是替宿主做决定） | A |
| `clampZoom` / `fitZoom` | `(pps) => number` / `(duration, w) => number` | 缩放夹取 / 铺满 | A |
| `dropIndexAt` | `(clips, t, draggedId) => number` | 落点 → 插入序号（按中点判定） | A |
| `rectsIntersect` | `(a, b) => boolean` | 框选相交（边缘接触也算） | A |
| `pickTickStep` / `formatTickLabel` / `buildTicks` | **`(pps) => number`** / `(t, step) => string` / `(duration, step) => Tick[]` | 刻度步长 / 标签 / 刻度点。★更新(2026-09-13)：`pickTickStep` 收敛为**只吃 `pps`** —— 原签名 `(duration, pps?)` 里的 `duration` 不参与决策，只会得到一个永不触发的分支或一个"总长兜底"（E7：为不存在的需求加防卫） | A `rulerTicks.ts` |
| `timeToX` / `xToTime` | `(t, pps, scroll) => number` / `(x, pps, scroll) => number` | **必须抽出**（A 内联在组件 → 否则"时间↔像素"出第二份实现） | 新 |

**常量（★更新 2026-09-13：改为**分两层**，由依赖方向强制 —— 原写「`core/constants.ts` 单一出处」与现实冲突）**

| 层 | 常量 | 为什么必须在**这一层** |
| --- | --- | --- |
| `base/utils/timeline/` | `MIN/MAX_PIXELS_PER_SECOND` | 唯一消费者 `clampZoom` 在这一层；而 G-1 禁 base→业务域、G-2 禁 `core/`→base ⇒ **两者不可能同处一案** |
| `videoEditor/core/constants.ts` | `EPS` · fps · 画布尺寸 · `DEFAULT_IMAGE_CLIP_DURATION` · 轨道默认名 · `VIDEO_EDITOR_SCHEMA_VERSION` | 只被 `core/` 消费 |

- **`MIN_SPLIT_MARGIN` 不建**（§二.9 第 4 行「弱化」；见 B 组 `splitAt` 行）。
- **`SNAP_TOLERANCE_PX` 不预置**：它是 UI 调用点的默认值 → 按「常量在**首次被消费处**落地」（7 步法 A8 + `check:dead-code`）在 G3/G5 到来时补入 `core/constants.ts`。

> **D 组已实现（2026-09-13 · G1）**：`base/utils/timeline/timeScale.ts`（`timeToX` / `xToTime` / `pxDeltaToTime` / `timeDeltaToPx` / `clampZoom` / `fitZoom` / `snapTime` / `dropIndexAt` / `rectsIntersect`）+ `rulerTicks.ts`（`pickTickStep` / `formatTickLabel` / `buildTicks`）。
> **存量迁入**：`VideoProcessNode`（6 处内联换算 + 手写吸附 → 共用原语，语义等价，先红后绿已跑）；**`director3d` 不迁**（见下方 §二.1 更新条）。

### 一.5 结果契约：诚实三态（判别联合）

```
type OpResult<T> =
  | { status: 'ok';       value: T }
  | { status: 'degraded'; value: T; reason: string }           // 成了但有损（如音轨被丢）
  | { status: 'reject';   reason: string; expected?: string }  // 没做，且说清为什么
```

| 规则 | 内容 | 锚点 | 状态 |
| --- | --- | --- | --- |
| **O1** | **`reject` 只准用于语义冲突**（类型不符）；**位置冲突一律 `ok` + 自动落最近合法位**（120 C14.3） | `待建 core/result.ts` | 待建 |
| **O2** | 长任务（导出）带**阶段**（混音/画面/写音频）+ `AbortSignal`（120 C5） | `待建 export/pipeline.ts` | 待建 |
| **O3** | **无"静默 null"**：`splitAt` 的 `null` 必须在调用处转成"置灰 + tooltip"（120 C15） | `待建 panels/dock/*` | 待建 |
| **O4（定位红线）** | **`OpResult` 只用于「跨系统边界」的操作**（入轨 / 导出 / 落盘）；**纯函数返回纯值**（数组 / `null` / 原样返回）——不得给所有原语套 `OpResult`（宽接口 + 薄实现 = 泄漏；见 §二.7 P7） | `待建 core/result.ts` | 待建 |

> 参考契约：N `addAssetToTimeline.ts:12` 的 `AssetDropResolution = {status:'accept'} | {status:'reject'; expectedTrack}` —— **结构同源**，可直接对齐。

**失败可见性给对读者（Step 4 第 3 问 · 补做）**

| 失败 | 读者 | 载体 |
| --- | --- | --- |
| 导出中断 / 失败（带阶段信息） | 用户 | 基座内进度区 + 可复制错误文本（120 C9.1） |
| 音轨被丢 / 入点吸附（有损但成功） | 用户 | 导出完成后一次性 toast + 工程内标记（120 C4.3） |
| 断链（资源真读不到） | 用户 | 片段红标 = **持续状态**（不是 toast） |
| 能力探针缺编码器 | 用户 | 导出前**红字明错**（P2） |
| 纯函数输入不合法（`splitAt` 越界等） | 用户 | **置灰 + tooltip**——它是「不可用状态」，不是「刚发生的动作」 |
| 存储写失败 / 对账异常 | **开发者** | `logger`（不打扰用户） |

> 判据：**瞬时动作反馈 → toast；持续状态 → UI 标记；开发者可查 → logger**。三者不得混用（拿 logger 当用户可见性 = 给错读者）。

### 一.6 能力契约：环境能力是**被测出来的**

```
type EncoderCapability = { ok: true } | { ok: false; missing: 'audio'|'video'|'both'; detail: string }
```

| 规则 | 内容 | 锚点 | 状态 |
| --- | --- | --- | --- |
| **P1** | 启动期探针跑一次，结果按 UA/版本 memo；**不每次导出重跑**（120 C9/D10） | `待建 export/capability.ts`（它调 mediabunny = 环境/IO，**不进 `core/`**，§二.7 P5） | 待建 |
| **P2** | 能力缺失 → **直接报错并明示用户**，**绝不退化产出未压缩 PCM**（120 C5/D1/D2） | 同上 | 待建 |

> 可达性：`mediabunny@1.54.0` 已在 `package.json:69`（**已取证：版本已精确钉死，无 `^`**），探针用它自带能力查询，不自造。

### 一.7 持久化契约：一条键、一次守门、一个写者

| 定义项 | 内容 | 锚点 | 状态 |
| --- | --- | --- | --- |
| 键 | `STORAGE_KEYS['video-editor-project-{projectId}']`，`backend:'kv'` | `待建`（登记进 `contracts.ts`，模板照 `canvas-state-v1-`） | 待建 |
| 键模板参照 | `CANVAS_STATE_PREFIX='canvas-state-v1-'`；`canvas-state-v1-{projectId}` 已登记 `backend:'kv'` | `src/components/base/core/contracts.ts:157` / `:200` | 已有 |
| 同类先例 | `director3d-project-{nodeId}`（`backend:'kv'`，`pattern:true`） | `contracts.ts:431` | 已有 |
| 读入口 | `normalizeProject` 是唯一读入口；`schemaVersion` 高于当前 → 按"无工程"处理 | `待建 data/projectRepository.ts` | 待建 |
| 读原语 | `contentGetAsync(key)` / `contentSetAsync(key, v)` | `src/components/base/core/contentStore.ts:578` / `:588` | 已有 |

> ★**更新(2026-09-13 · 实现修正 · 写路径取严格族)**：**读**用 `contentGetAsync`（本行指定）+ `contentKvGetVersion`（取 CAS 基线）；**写**改用 **KV 严格族 CAS**（`contentKvSetCas`，`contentStore.ts:734`）。
> 理由：本表下方「并发」行自己写的是「复用 `118` 的 CAS + 冲突可见，**不静默覆盖**」，而 `contentSetAsync` 属**尽力而为族**（可能降级写本地副本）→ 用它写工程记录会让「别人刚改过」这一事实**消失**，正是 `docs/120` C2.7 要禁的静默覆盖。两行冲突时取更严的那条。
> 实现：`data/projectRepository.ts`（G2 已完成）；409 → 返回 `{status:'conflict'}`，**一个字节不写**，绝不静默重试覆盖。
| 写者 | `projectRepository` 是**唯一写者**（120 C2.4） | 同读入口 | 待建 |
| 体积 | 只存元数据与 URL，**禁内联 base64**（120 C2.5） | 契约测试 | 待建 |
| 并发 | 复用 `118` 的 CAS + 冲突可见（120 C2.7），**不静默覆盖** | 跨域，归 `118` | 已有（复用） |

> ⚠️ **不可调 `contentStore.checkRegistered`** —— 取证：它是 `contentStore.ts:232` 的**内部函数，未导出**。红线靠 `npm run check:keys`（编译期）+ 写入路径内部守卫。

### 一.8 边界契约：与画布只有**一个读方向**

| 规则 | 内容 | 锚点 | 状态 |
| --- | --- | --- | --- |
| **B1（读）** | 画布节点 → 素材提取走 `getNodeMedia`（判 image/video/audio 并取**本体 URL**）；**不另写一份** | `src/components/base/canvas/nodeMedia.ts:49`（`(node) => {type,url}`） | 已有 |
| **B2（写）** | 导出产物回画布走 `spawnAndCommit` / `buildSpawnNodes`；产物须是**既有可被下游消费**的节点类型（120 C4.8） | `src/components/base/canvas/deriveNodes.ts:152` / `:63` | 已有 |
| **B3（方向）** | 画布 → 工程**单向**；画布节点不持有、不回写 tracks（120 C2.4） | 评审 + §二.6 待补规则 | 待建 |
| **B4（挂载）** | 基座挂 React Flow provider 子树内，缺失即 `throw`（120 C4.5） | `待建 panels/dock/VideoEditorDock.tsx` | 待建 |

### 一.9 裁定结果（原「待签」→ **已裁定**）

| # | 项 | 裁定 | 依据（取证/契约） |
| --- | --- | --- | --- |
| Q1 | 时间单位 | **秒（float）** | `videoEngine.ts:57+` 已是秒；改帧=第二套表示，违 120 §0.5 |
| Q2 | 改帧率语义 | **只改渲染与导出采样，不动片段时间** | 120 C12.3 |
| Q3 | M1 轨道 | **固定双轨**（视频×1 + 音频×1） | 120 §0.2 / §0.8 |
| Q4 | 定格默认时长 | `DEFAULT_IMAGE_CLIP_DURATION`（3s） | 与图片素材同源，避免第二个常量 |
| Q5 | 断链判定触发点 | `sourceUrl` 空 **或** 探测/读取失败；节点消失**只提示不判红** | 120 C13 / C13.3 |

> 以上 5 项**无需用户拍板**：全部由"既有代码事实"或"120 已定契约"推出，属架构一致性判断（7 步法 Step 5 的"架构师直接定"）。

---

## 二、L1 落地骨架

### 二.1 目录 ↔ L0 归属

```
src/components/base/utils/timeline/   # ← 【共用层 · 3 域共用】D 组（时间↔像素 / 刻度 / 吸附 / 框选 / rAF 播放钟）+ A 组跨域部分（已建 sourceTime.ts → G1 续建）
src/components/videoEditor/
├── core/            # ← 领域真相：E/B/C 三组 + A 组"剪辑器专有"部分 + types + constants + result。零 React、零 IO
├── data/            # ← §一.7 持久化 + 素材 URL 唯一出口（解析 + 断链判定同一处）
├── export/          # ← 导出管线 + 能力探针（IO/环境 → 不进 core）
├── panels/dock/     # ← L2：只读 core/view，只经原语改数据
└── hooks/           # ← 历史栈绑定（pointer 侧的 begin/commit/undo/redo）
tests/unit/videoEditor/*.test.ts   # ← 扁平（与现有 tests/unit 惯例一致；用例名见 §一.3 验证列）
```

> ★**更新(2026-09-13 · `director3d` 改判：不迁，保留分层)**：本节原把 `director3d` 列为共用层的「待迁存量」。取证后改判 ——
> 它是**帧基**（`contentWidth = totalFrames * pxPerFrame`、`clientX / pxPerFrame`、标尺以帧为单位），且 `fps` 是**刻意未用**的参数（组件签名 `fps: _fps`）；其「框选」是**一维邻近**（`Math.abs(x - item.frame * pxPerFrame) < radius`），**不是矩形相交** ⇒ 与共用层之间**只有单位换算，没有可漂移的同一算法**。
> 迁移需把 `fps` 穿透进一个刻意不用它的组件、6+ 处加换算层 ⇒ **改动半径变大、复杂度上升、收益 0**（Step 3 判据：半径没变小就只是搬家），且该组件**无测试覆盖**可保证行为不变。
> 故按「判据/宿主不同 → 保留分层」**不迁**；共用层仍服务 `nodes/` + `videoEditor/` **两域**（非假接缝）。**存量迁入只做 `VideoProcessNode`（已完成）**。

### 二.2 外部移植映射

| 本仓目标 | 参考来源（已取证存在） | 处置 |
| --- | --- | --- |
| `core/types.ts` | A `src/types/videoEditor.ts` | **改搬**：删 Tauri/`filePath`；时间字段收敛为 §一.1 四个 |
| `core/timelineOps.ts`（E/A/B 组） | A `src/components/videoEditor/timelineOps.ts` + A `types/videoEditor.ts` 内纯函数 | **直搬 ~70%**（`relayoutSequential`/`findClipAtTime`/`moveClipTo`/`duplicateClip`/`snapTime`/`clampZoom`/`fitZoom`/`dropIndexAt`/`clipEdges`/`rectsIntersect`/`evaluateVolume`/`evaluateTransitionAlpha`/`hasMixedSources`） |
| `core/rulerTicks.ts`（D 组） | A `src/components/videoEditor/rulerTicks.ts` | **直搬** |
| `core/normalize.ts`（E 组） | N `src/workbench/timeline/timelineMath.ts` 的 `normalizeTimeline` / `createDefaultTimeline` | **借结构**：缺字段补全 + 重复 id 重铸；`fps` 不进时间戳 |
| `core/routeClip.ts`（C 组） | N `timelineTypes.ts` 的 `getTrackTypeForClipType` | **借结构**（不搬其"三轨一一对应"；我们 video\|audio 二分） |
| 入轨（点选即追加轨尾） | N `addAssetToTimeline.ts` 的 `addAssetToTimelineEnd` + `findAppendFrame` | **借结构**：探测时长 → 追加到匹配轨尾 → 展开面板 + toast |
| 落位/拒绝契约 | N `addAssetToTimeline.ts` 的 `resolveAssetDrop` | **借契约** → §一.5 的 `OpResult.reject` |
| 编辑对账 / 节点对账 | N `timelineEditReconcile.ts` / `timelineNodeReconcile.ts` | **按需借**：对应 120 C13.4/C13.6（**N 最值钱的两块之一**） |
| 播放钟 / 播放 URL | N `useTimelinePlaybackClock.ts` / `timelinePlaybackUrl.ts` | **按需借**（M1 走带真出声，120 C11.7） |
| 编辑纯逻辑（裁左/裁右/定格） | N `workbench/timeline/timelineEdit.ts` | **按需借** |
| 撤销栈 | A `useTimelineHistory.ts` | **借结构**（120 C6：只快照轨道、连续交互只落一张） |
| 探测/缩略图/波形 | A `useVideoEditorSources.ts` / `VideoEditorWaveform.tsx` | **借结构**；上限按 120 C11.10（禁每片段挂 `<video>`） |
| 拖拽载荷 / 落点反馈 | N `timelineDragPayload.ts` / `timelineDropFeedback.ts`（带 test） | **按需借**（M1 仅"轨内拖序"用得上） |
| UI 层全部 | A `VideoEditorTimeline/MediaPanel/Inspector/Preview/Ruler/TextPanel/TransitionPanel/CodecPanel/AiTransitionPanel/Window` | **只借结构与交互口径，代码重写** |
| 测试靶子 | A `tests/`（components/services）+ N `workbench/timeline/*.test.ts` | **借用例清单**：把 N 的测试当行为契约抄成我们的断言 |

> ⚠️ **仓内来源优先于外部参考（§0.5 普查修正）**：以下能力**仓库里已有实现，不抄外部** ——
> `sourceTimeAt` ← `VideoProcessNode.tsx:1356/1370/1552`（**下沉**）· 抽帧 ← `VideoProcessNode.tsx:142 captureFrame`（**下沉**，`124` §二 优先 1）· rAF 播放钟 ← `director3d/App.tsx:1069` · 编码器探针 ← `director3d/App.tsx:1919`（`getFirstEncodableVideoCodec`）· 历史栈 ← `base/canvas/historyStack.ts:15 HistoryStack<T>`（**泛型，直接复用**，不新写第 6 套）· 片段持久化字段 ← `VideoProcessNode.tsx:266 TimelineClip`（**字段须对齐**）。

### 二.3 施工任务卡 G0–G5

> 每张卡：**前置**（上张卡验收通过 + L0 条款）· **动作** · **产出** · **验收命令**。

**G0 · 契约登记（先登记再写代码）**
- 前置：§一.7
- **硬边界（红线）**：G0 = **纯登记、零行为变化** —— 这 4 处加完后**没有任何代码消费它们**（读键 / 读设置是 G2/G3 的活）。若在 G0 里顺手接线（某处读 `videoEditorOpen`、某处用 `UPLOAD_DIRS.videoEditor`）→ **停手**：那会让 G0 从"零风险增量"变成"引入行为变化"，且与 G2/G3 混线。
- 动作：
  1. `contracts.ts` 的 `STORAGE_KEYS` 加 `video-editor-project-{projectId}`（`domain:'videoEditor'` · `backend:'kv'` · `pattern:true`，照 `:200`/`:431` 模板）；`store` 填**待建约定路径** `videoEditor/data/projectRepository.ts`（照实填，**不要为"指向已存在文件"填假值**）；`note` 写明"**tracks 唯一真相源**"（120 C2.4）。**不复用** `director3d-project-{nodeId}`（对象 / 写者不同）。
  2. `src/components/base/utils/uploadDirs.ts:12` 的 `UPLOAD_DIRS` 加 `videoEditor: 'canvas/video-editor'`（**不是** `'video-editor'`，与 `videoProcess:'canvas/video-process'` 同构）；**不复用** `videoProcess` —— 产物语义不同（**帧处理** vs **影片编辑**），混目录后**无法按目录清理 / 迁移**，理由写进该行注释。
  3. `src/components/base/store/settingRegistry.ts:58` 的 `SETTING_DEFS` 加 `videoEditorOpen`（`type:'boolean'` · `default:false` · `ui:false`，照 `agentOpen`）；`group`/`icon`/`title`/`desc` 按仓内惯例选（**优先不新增 `group`**），选择理由写进回报。
  4. **同步** `contracts.ts:220` 的 `app_settings` 键 `note` —— ⚠️ 取证发现该 note 列 **6 键**，而 `SETTING_DEFS` 实际 **7 键**（多 `autoSyncEnabled`）→ **note 已漂**。本次须与 `SETTING_DEFS` **全集对齐**（补齐 + 加 `videoEditorOpen`）。**只改 note 文本，不换其机制**（"手写 note → 生成式"是另一个任务，**不在 G0**）。
- 产出：以上 4 处增量（无新文件）
- 验收：`npm run check:keys && npm run check:upload-dirs && npm run type-check` **+ 探针（先红后绿，必做）**：
  - `contentGetAsync('video-editor-project-abc')` → **通过**（证明 `pattern:true` 登记生效）；
  - `contentGetAsync('video-editor-foo')` → `check:keys` **变红**（证明闸真的在看登记表）。
- ⚠️ **无需动后端白名单**：`check-upload-dirs.mjs` 只对账**顶层根**，`canvas` 已在白名单（120 D8 已证伪，见 §四）。

**G1 · `core/` 全量原语 + 单测（先做）**
- 前置：§一.1–一.5 全部 **+ 以下 D 组硬约束（2026-09-13「前置收口」裁定）**：
  - D 组（视图换算）**先建共用层再写剪辑器**，**禁止"先内联再抽"**；
  - **建层与存量迁入同批做**（`VideoProcessNode` 先、`director3d` 最后）——否则共用层零消费方 = 假接缝（铁律 5）；
  - 可收口的只有**运算 / 探测**（时间↔像素 / 刻度抽稀 / `rectsIntersect` / rAF 计时）；"吸附到什么 / 框选什么 / 是否出声"是**各域判据**，保留在各域（Step 3）；
  - 每处迁移**先红后绿**：改坏新原语 → 对应宿主用例应变红；不红即"没测到东西"，须重写测试后才算迁完；迁移中若既有测试变红，**先假定"收口越界"**，不得改测试就范（7 步法 7.1 / `126` §八）。
- 动作：按 §一.4 五组实现（E→A→B→C→D）；同步写 `tests/unit/videoEditor/*.test.ts`
- 产出：`core/{types,constants,normalize,timelineOps,rulerTicks,routeClip,result,capability,refs}.ts`
- 验收：`npx vitest run tests/unit/videoEditor && npm run type-check`
- ⚠️ 实现前**先做 §二.5 的拒绝清单核对**（id 生成 / removeClips 语义 / needsCompositing 判据）

**G2 · `data/` 数据层**
- 前置：§一.2 / 一.7
- 动作：`normalizeProject`(E 组) + `projectRepository`（`contentGetAsync`/`contentSetAsync`，键用 G0 常量）+ `sourceResolver`（URL 三源归一）
- 产出：三个文件
- 验收：`npx vitest run tests/unit/videoEditor && npm run test:smoke`

**G3 · `panels/dock/` 基座形态**
- 前置：§一.8 B4 · §三 回指表
- 动作：常驻底部层（**非 portal、不覆盖画布**）+ 工带 + 双轨；`appSettings.videoEditorOpen` 控开合；`key={activeProjectId}`；键盘归属按激活位（只让位 `Delete`/`⌘Z`/`⌘⇧Z`）
- 锚点参照：`settingRegistry.ts:58`(agentOpen 先例) · `src/hooks/useCanvasShortcuts.ts:80`(查询式让位) · `App.tsx:1400`(`deleteKeyCode`)
- 产出：`VideoEditorDock.tsx` + `panels/dock/*`
- 验收：`npm run build && npm run test:smoke`；人工：基座展开时画布仍可点选/拖节点（证明没登记 modalLayer）

**G4 · `export/` 导出**
- 前置：§一.5 O2 · §一.6 P1-P2
- 动作：`exportPipeline` 单一入口（`needsCompositing` 分流）+ 探针 + `AbortController`；缺口补在 `videoEngine.ts` 之上，**不新建平行 services**
- 产出：`export/{pipeline,composite,audio}.ts`
- 验收：三种工程（纯直通 / 混合需合成 / 含叠加）各导一次；取消真中断无残留
- ⚠️ 产物回画布（`spawnAndCommit`）若新增节点类型 → 跑 `npm run check:node-types && npm run check:node-data`

**G5 · 粗排体验项**
- 前置：§三 回指表
- 动作：缩略图胶片条 + 真实波形（120 C11.10/C11.7b）· 吸附 · 走带真出声 · 撤销 · 三状态 · 断链变红 · 导出前常驻信息（120 C5.6）
- 产出：交互补丁
- 验收：`npm run test:unit` + 120 §七 F0–F22 逐条对 `mockup/video-editor-dock-mockup.html`

### 二.4 门禁（真实存在的闸 · 各自能拦什么）

| 命令 | 脚本 | 对本剪辑器管什么 |
| --- | --- | --- |
| `npm run check:arch` | `scripts/check-arch.mjs` | 循环依赖 · base 反向依赖（**见 §二.6**）· 存储唯一入口 · KV 键禁同步读 · storage 深路径 |
| `npm run check:keys` | `check-storage-keys.mjs` | 裸存储键字面量（G0 登记后才可写） |
| `npm run check:upload-dirs` | `check-upload-dirs.mjs` | 前端 `UPLOAD_DIRS` ↔ 后端白名单顶层根一致 |
| `npm run check:events` | `check-events.mjs` | 裸事件名（本项目剪辑器**不需要**跨窗口事件，若引入须先登记） |
| `npm run check:catch` | `check-silent-catch.mjs` | 静默吞异常（导出/探测链必跑） |
| `npm run check:any` | `check-any-honesty.mjs` | 类型诚实性 |
| `npm run check:dead-code` | `check-dead-code.mjs` | 死代码（照搬 A 后清理残余时用） |
| `npm run check:strict-src` | `check-strict-src.mjs` | 严格化白名单 |
| `npm run type-check` / `test:unit` / `test:smoke` / `build` | — | 基线四件套 |

**每 Gate 收尾**：`npm run type-check && npm run test:smoke && npx vitest run tests/unit/videoEditor && npm run build`

### 二.5 ⚠️ 从参考实现**必须拒绝**的东西

| 参考做法 | 出处 | 拒绝理由 | 我们的做法 |
| --- | --- | --- | --- |
| 时间基后缀生成 id：`` `${clip.id}-b${Date.now().toString(36)}` `` / `-copy${Date.now()}` | A `timelineOps.ts` `splitClipsAt`/`duplicateClip` | 违 120 C14.2（可碰撞 id；N 为此出过"编辑一条字幕串改另一条"事故） | 一律 `generateId`（`idGen.ts:11`）；`normalizeProject` 加载时重铸 |
| `removeClips` 强制 **"至少保留一个片段"** | A `timelineOps.ts` | 违 120 C11.9（**不设最少片段数**） | 删除就是删除；空工程合法 |
| 删除只有**一种语义**（顺排压实） | A `timelineOps.ts` | 120 C15 要**二态**（留洞 / 波纹前移） | `removeClips(..., mode)` |
| 时间戳用**帧**、`fps` 进 state | N `timelineTypes.ts:71` | 与 120 C12.2/C12.3 冲突（§一.1 对照） | 秒；fps 是工程参数 |
| 独立窗口 / 自绘标题栏 / `postMessage` | A `VideoEditorWindow.tsx` / `videoEditorWindowService.ts` | 本仓是**常驻底部层 + 直接回调**（120 §1.2 / C4） | 常驻层 + `onExport` 回调 |
| IndexedDB / `asset://` / `filePath` / `getConvertFileSrc` | A `services/indexedDb/*` | 第二落盘通道（120 C2/C3 红线） | `contentStore` + `localTool /files/` |
| `i18n`（中文 key）/ `@iconify/react` / 5 个自建 CSS | A `i18n/` `styles/` | 第二套 i18n / 图标 / token 体系 | 中文直写 + `lucide-react`（已在 `package.json:68`）+ `tailwind.config.ts` |
| `needsCompositing` **不看**工程尺寸差异 | A `types/videoEditor.ts:307` | 120 C12.1 红线：漏了会出现"尺寸不一致却走直通" | 判据补"片段尺寸 ≠ 工程尺寸" |

### 二.6 需要补的机器守卫（**三条，且都不许"手写清单"**）

> 按 §0.3 E6：这些是"以为有、实际没有"的，必须显式补。
> 按 §0.3 E7 与 §二.8 的母体：补的时候**不许再制造"手写清单 + 靠人记得同步"**。

| # | 缺口（取证） | 补法（**结构判据，非手写名单**） | 验证 |
| --- | --- | --- | --- |
| **G-1** | `scripts/check-arch.mjs:183` 的 `BUSINESS_RE = /^src\/components\/(nodes\|scriptbox\|agent\|panels)\//` **不含 `videoEditor`** → base/ 反向依赖剪辑器域**拦不住**（120 §1.1 的"check:arch 拦截"对剪辑器域不成立） | **不要"加一个域名"**——每来一个业务域就要记得加一次，是母体本身。改为**反向判据**：base/ 不得 import `src/components/**` 下任何**非 `base/`** 的目录（仅保留现有 2 处 allowlist）。一次改完，自动覆盖未来所有业务域 | `npm run check:arch` + 反向注入 1 行 `base/* → videoEditor` 验证变红（探针） |
| **G-2** | "`core/` 零 React / 零 IO"（120 §二 铁律）**无任何规则** | **不要写三条禁令**（禁 React / 禁 IO / 禁 fetch 必漏）。改为**一条导入白名单**：`videoEditor/core/**` 只准 import `videoEditor/core/**` 与 `base/core/idGen.ts`——一条规则覆盖全部外来依赖 | 同上注入探针验证变红 |
| **G-3** | ~~激活位判据被抄成 2 份，需收成 `isCanvasSuppressed()`~~ → **改判：需求已不存在（更新 2026-09-13 取证）** | **前置收口卡 9 已完成**：`modalLayer.ts:169 isCanvasSuppressed()` 是唯一真源；查询式 `useCanvasShortcuts.ts:80`、订阅式 `App.tsx:1131 useSyncExternalStore(subscribeModalLayer, isCanvasSuppressed)`（→`:1401 deleteKeyCode`）、第三消费方 `AssistantTablePanel.tsx:441` 均已消费它；`grep hasModalLayer` 在 `modalLayer.ts` 外**零消费点**；`tests/unit/modalLayer.test.ts:188-202` 已有「让位消费方认的是同一判据」用例 ⇒ **Step 0「已有 1 份且是真源 → 一行不写」** | **只补防回潮**（已加，`check-arch.mjs` 规则 5）：`modalLayer.ts` 外**禁直调底层 `hasModalLayer()`**（否则等于自建第二处判据）。将来剪辑器接入激活位 → **只在 `isCanvasSuppressed()` 里 `\|\|` 一项**，规则保证没有第二个落点 |

> **三条的共性：把"手写清单"换成"结构判据"。** 仓内已有正确先例——`check-upload-dirs.mjs` 不是"两处手写同步"，而是**两端集合对账**。
> ⚠️ 120 §1.1 原文"base/ 不得反向依赖业务域（`npm run check:arch` 拦截）"——**对已有业务域成立，对 videoEditor 目前不成立**，须先补 G-1（数据驱动版）才成立。

---

### 二.7 复杂度定位审计（7 步法 Step 3 · 五问）

> 判据用的是 Step 3 五问：真源在哪 / 变化速率 / 有几种实现 / 改动半径 / 接口窄不窄。

| # | 对象 | 五问结论 | 动作 |
| --- | --- | --- | --- |
| **P1** | `export/mediaEngine.ts`（120 §1.3 曾列） | **只有 1 种实现**（转发 `videoEngine`）→ 假接缝（铁律 5） | **删**；缺口（分组无损搬运 / 合成渲染）直接补进 `base/utils/videoEngine.ts`（`videoEngine.ts:176/206/312/519/597` 已有 trim/concat/gif/落盘） |
| **P2** | `core/refs.ts`（本文初稿） | 与 `data/sourceResolver.ts` **同语义**（都在答"这个 clip 能不能读"）→ 同一真相两份 | **删**，合并进 `sourceResolver`（120 C3） |
| **P3** | D 组（视图换算）放进 `core/` | ① 它依赖 `pps`/`scroll`（**视口快变量**），而 `core/` 是**领域真相（慢变量）**；② **普查进一步发现它跨 3 域共用**（`124` §一 第 3/4/5/12 项） | **移出** `core/` → **`base/utils/timeline/`**（共用层；原拟 `view/` 作废） |
| **P4** | "解析 URL" vs "判断链" 两个判点 | 同一真相两份判点 → 必漂（判 A 处真、B 处假） | **收成 1 个原语**：返回 `{url}` 或 `{broken, reason}` |
| **P5** | 能力探针放 `core/` | 它要调 mediabunny（环境/IO），而 `core/` 的不变量是"零 IO" | **移出** → `export/capability.ts`；`core/` 只保留其**返回类型** |
| **P6** | `needsCompositing(tracks, projectSize)` | 判据真源是**工程基准（Project）**，拆成两参 → 两参不一致时判据说谎 | **收成一个参数** `(project)` |
| **P7** | `OpResult` 铺给所有原语 | 纯函数的"失败"是**输入不合法**（可判定），不是**系统错**；全铺 = 宽接口 + 薄实现 = 泄漏 | **限定**：只用于跨系统边界（O4） |
| **P8** | 键盘让位判据（120 C10） | **激活位判据被抄成 2 份**（查询式一处、声明式一处）→ 必漂 | **收成一个谓词** `isCanvasSuppressed()`（G-3） |

**度量（Step 3 要求的「改前 / 改后」可比口径）**

| 口径 | 120/113（改前） | 123（改后） |
| --- | --- | --- |
| 冗余层（假接缝） | 1（`export/mediaEngine.ts`：只转发） | **0** |
| 同一真相的判点 | URL 解析 2 处 · 激活位判据 2 处 · 断链判定 2 处 | 各 **1** 处 |
| `core/` 认知单元 | 5 组同居（领域慢变量 + 视图快变量） | 4 组（领域）；视图独立 → 各自单元更小 |
| 跨层跳跃 | `core/` 内出现 IO 需求（探针） | 探针归 `export/`；`core/` 零 IO 可被一条规则守住 |
| M1 需实现的原语 | 含 M2 专属项（音量包络等） | **已剔除 M2 项** |
| 兜底/守卫 | 13 条 | **8 条**（删/降级 5，见 §二.9） |

### 二.8 正确性审计（7 步法 Step 4 · 三问）

| 检查项 | 现状 | 判定 |
| --- | --- | --- |
| 成功信号是否类型层可判别 | `OpResult` 三态（`ok`/`degraded`/`reject`） | ✅ 诚实；但须限定边界（O4） |
| **`normalizeProject`「缺字段一律补默认」** | 把"**旧版本**"与"**数据损坏**"混为一谈 → 可能把损坏工程补成半截工程静默上屏 | ❌ **不诚实** → 已改二分支（§一.4） |
| **`BoundedConcurrencySource`「失败退避重试一次」** | 其根因自述是"**WebKit** 并发压力"（120 C5.5），而 120 C5 自己论证本仓运行时=**Blink/Chromium** | ❌ **根因不适用于目标平台** → 见 §二.9 第 3 行 + §二.10 待决 |
| **断链三件套**（标红 + 导出预检弹窗 + 跳过继续） | 真根因只有一个：素材**被 GC 清掉**（= `I6` 缺位） | ⚠️ **兜底过厚** → 见 §二.9 第 2 行 |
| 错误在哪炸开 | 导出失败带**阶段**（混音/画面/写音频）+ `AbortSignal`（O2） | ✅ 在根因发生地炸，不在 UI 层兜底 |
| 能力缺失的诚实度 | 明错、**不退化产出未压缩 PCM**（P2） | ✅ 已把 PCM 兜底删掉（120 C5），是**正确形态的样板** |
| 无"静默 null" | `splitAt` 返回 `null` → 调用处置灰 + tooltip（O3） | ✅ 诚实 |
| 有损/精度损失是否如实告知 | `audioDropReason`、`actualStart` 回填 | ✅ 诚实，保留 |

### 二.9 兜底 → 地基（**本审计的核心：验证 §0.3 E7**）

> 口径：**"掩盖地基缺陷"的 → 改地基、删兜底**；**"表达语义 / 诚实失败"的 → 保留**。

| # | 兜底 / 守卫 | 它在掩盖什么（根因） | 地基解 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | `normalizeProject` 里的 **id 去重重铸** | 旧 id 生成器会碰撞 | 本仓 `generateId`（`idGen.ts:11`）**含 6 位随机段**；N 的事故根因（**归零自增序列**）在本仓不存在 | **删**（未来若支持"导入外部工程"，再作为**导入校验**单列，不混进加载路径） |
| 2 | 断链三件套（标红 + 预检弹窗 + 跳过继续） | 素材被 GC 清掉 | **`I6`**：把工程引用登记进文件生命周期（120 C13.1）。⚠️ **该引用层尚未实现**（§0.5 取证）→ 它是**与 `13`/`122` 合建**的前置，不是"复用" | **降级**：`I6` 落地后，C13 从"整节机制"降为"极端情况一行提示"；预检从"弹窗"降回 C5.6 的"常驻一行" |
| 3 | `BoundedConcurrencySource` + 退避重试 | WebKit 并发压力 | 本仓运行时 = **Blink/Chromium**（120 C5 自证）→ 根因不适用 | **不做**（先实测；真复现再加）—— ⚠️ **见 §二.10，需你点头** |
| 4 | `MIN_SPLIT_MARGIN`（0.05s 魔法容差） | 播放头可能落在片段边界 | 播放头**吸附到离散候选**（`clipEdges`）→"落在边界"成为**明确状态**，UI 置灰；`splitAt` 只判"严格内部" | **弱化**：容差从"魔法秒数"改为"严格内部判据"（不必两段各留 0.05s） |
| 5 | 音频 **PCM 兜底** | 没有编码器 | 已改判：明错不退化（120 C5） | **已删** ✅（120 自己删的；**本例即正确形态的样板**） |
| 6 | `removeClips`"至少保留一个片段"（A 的写法） | —— | 空工程是合法状态（120 C11.9） | **已删** ✅ |
| 7 | `schemaVersion` 守门 + 缺字段补全（**两套**机制） | 工程结构演进 | 一个机制：**版本化迁移链**（低版本→迁移；缺失/更高/损坏→拒载） | **收成一个**（§一.4 已改） |
| 8 | CAS + 冲突可见 | 多窗口并发写 | **真正确性问题**，不是兜底 | **保留** |
| 9 | 激活门（折叠态不接收素材） | 静默副作用 | 语义定义 | **保留** |
| 10 | 键盘让位 3 键 | 画布误删 / 误撤销 | 语义（激活位派生） | **保留**（判据须单点 → G-3） |
| 11 | C4.5 挂载硬断言 `throw` | 挂错位置静默失败 | 挂载点由 App 结构固定；断言是**启动期诚实失败**，成本≈0 | **保留**（它是"诚实"，不是"掩盖"） |
| 12 | 能力探针 P1/P2 | 编码器缺失 | 目标平台保证存在；探针是"**确认**"，不是"兜底" | **保留**（一次 + memo） |
| 13 | G-1/G-2/G-3 机器守卫 | 位置判据会漂 | **位置正确性的守卫**，不掩盖任何错误 | **保留**（且改数据驱动 → §二.6） |

**结论**：13 条里 **4 条可删/降级（1/2/3/4）+ 1 条收成一个（7）**，其余 8 条都是**语义表达或诚实失败**——**这正是"地基做够了就不需要多兜底"的量化证明**。

### 二.10 需要你点头的一项（唯一带用户可见后果）

> 其余定位修正（P1–P8）与删减（§二.9 的 1/4/7）都是**结构判断**，改完用户能做的事不变，按 7 步法 Step 5 由架构直接定。
> **只有第 3 行（删导出读取的稳健性兜底）会改变"导出失败 vs 可能成功"的用户体验**，属业务边界，交你拍板：

```
方案 A（推荐）：不做 BoundedConcurrencySource —— 按 Blink 原生并发直接读；实测若真复现 fetch 失败，
                再从「提高并发上限 / 流式 / 临时落盘」三种根因解里选（120 C5.5 自己也说正确解不是内存兜底）。
                成本 0；收益 = 少一个自定义 Source + 少一条退避逻辑；风险 = 若某环境确有此问题，导出会「明确报错」而非「重试后可能成功」（本就该报错，不静默）。
方案 B：照 120 C5.5 现状实现（限并发 ≤4 + 单段失败退避一次）——成本 ≈ 半天 + 一条常驻维护面；收益 = 多一次重试机会。
方案 C：不做兜底 + 把失败提示做扎实（阶段 + 可复制 + 建议重试）——成本极小；与 A 的差别只是「不排期」。
请决策。
```

---

## 三、L2 功能（最后 · 只做回指，不定义新东西）

| 功能（M1） | 回指到哪条 L0 | 参考图 |
| --- | --- | --- |
| 在画布上点选素材 → 追加到对应轨尾 | §一.2 R1/R2 · §一.4 `routeClipToTrack`/`appendTime` · §一.5 | mockup 舞台 3 |
| 工具带（走带 / 时间码 / 编辑动作 / 全局开关） | §一.1 · §一.4 B/C/D 组 · §一.5 O3 | mockup 舞台 2 |
| 轨道区（双轨 + 轨道头图标网格 + 三状态） | §一.2 · §一.4 `isTrackLocked/Hidden/Muted` | mockup 舞台 2 / 4 |
| 分割 / 裁左 / 裁右 / 定格 | §一.4 `splitAt`/`trimLeftAt`/`trimRightAt`/`freezeFrameAt` · §一.5 O3 | mockup 舞台 5 |
| 删除二态（留洞 / 波纹） | §一.4 `removeClips(mode)` · §一.3 I1 | mockup 舞台 5 |
| 吸附 / 缩放 | §一.4 `snapTime`/`clampZoom`/`fitZoom`/`clipEdges` | mockup 舞台 2 |
| 缩略图胶片条 / 真实波形 | 120 C11.10 / C11.7b（数据来自 `useEditorSources` 探测，属 L1） | mockup 舞台 2 |
| 走带真出声 | §一.4 `sourceTimeAt` · 120 C8.3（编辑先自动暂停） | mockup 舞台 4 |
| 导出（直通 / 合成 + 进度 + 可中断） | §一.4 `needsCompositing` · §一.5 O2 · §一.6 P1/P2 · §一.7 | mockup 舞台 4 |
| 断链变红（仍在轨上） | §一.2 R1 · 120 C13 | mockup 舞台 4 |
| 撤销 / 重做（工程级） | 120 C6（只快照轨道数组） | mockup 舞台 2 |
| 键盘归属（只让位 `Delete`/`⌘Z`/`⌘⇧Z`） | 120 C10（按激活位派生） | mockup 舞台 4 |

> **入轨入口以 120（2026-09-13 修订版）为准：在画布上点选素材节点 → 追加到对应轨尾；无节点按钮、无拖拽。**（113 §四 那句"节点按钮 + 拖进轨道"为旧表述。）

---

## 四、审计修正表（2026-09-13 取证推翻的描述）

> 按 7 步法 Step 1"任何描述都是线索，不是结论"。以下均为**取证后推翻**，供后续 AI 不再沿用旧表述。

| 原描述（120 / 113 / 本文初稿） | 取证真相 | 处置 |
| --- | --- | --- |
| "时间单位需拍板（秒/帧）" | `videoEngine.ts:57+` **已是秒** | 改判：**直接定秒**，非待决项（§一.1 / §一.9 Q1） |
| 120 §1.1「base 不得反向依赖业务域 → `check:arch` 拦截」 | `check-arch.mjs:183` `BUSINESS_RE` **不含 `videoEditor`** | 记为缺口 **G-1**（§二.6），补后才成立 |
| 120 D8「UPLOAD_DIRS 白名单锁步债（后端未同步→静默 404）」 | `check-upload-dirs.mjs` 只对账**顶层根**；`canvas` 已在白名单 | **改判：非债**。加 `canvas/video-editor` 无需后端改动 |
| 120 §1.3「mediabunny 须钉死精确版本 1.54.0（去掉 `^`）」 | `package.json:69` **已是 `"1.54.0"`** | 该待办**已完成**，非待办 |
| **`CLAUDE.md` §5.4.9**（本文初稿误记为"120 §5.4.9"，此处自纠）「媒体判型唯一真值源 `components/base/utils/mediaType.ts`」 | 该文件**不存在**；真源是 `components/base/utils/assetType.ts`（`classifyAssetUrlKind:54`/`detectAssetType:71`/`isAudio:132`） | 引用须改指 `assetType.ts` |
| 120 多处「`contentStore.checkRegistered` 在 dev 下 throw」 | 它是 `contentStore.ts:232` 的**内部函数，未导出** | 外部**不可调用**；红线靠 `check:keys` + 写入路径内部守卫 |
| 113 §2「`d3dPersistence.ts` L260-269 有 local 兜底」 | 该区间是 `hydrateProject` 调 `contentGetKvWithFallback` + 拒收处理；**兜底实现在 contentStore** | 表述须改为"委托 `contentStore.contentGetKvWithFallback`" |
| 本文初稿 §二.6「G-1 = 在 `BUSINESS_RE` 里加 `videoEditor`」 | **该补法本身就是母体**（手写清单 + 靠人记得加；下一个业务域会重演同一坑） | 改为反向判据「base 不得 import 任何非 `base/` 目录」（§二.6） |
| 本文初稿 §一.4「重复 id 重铸 / D 组放 `core/` / 探针放 `core/`」 | ① id 重铸**无根因**（§二.9-1）；② D 组是**快变量**、探针有 **IO**，与 `core/` 不变量冲突 | 已删 / 已移（§二.7 P3 / P5） |
| 120 C5.5「`BoundedConcurrencySource`（治 WebKit 并发压力）」 | 该根因适用于 **WebKit**，而 120 C5 **自证**本仓运行时是 Blink/Chromium | 已选方案 A（不做） |
| 本文初稿 §一.3「I6 = 引用被生命周期看见（归 `13-文件生命周期`）」 | `src/` 中"引用感知 GC / isReferenced / collectRefs" **0 命中**；13 号是**方案文档**，实现未见 | I6 改为「**与 `13`/`122` 合建**，剪辑器作为引用来源接入」（§0.5） |
| 120 §四「`VideoProcessNode`（concat 模式）作 UI 参考，不重复逻辑」 | 该节点**已有** `VTrack/VClip` 时间轴模型 + 缩略图 + 播放 + 拼接导出（`VideoProcessNode.tsx:312-313/481-483/639-668/1594-1649`） | 升级为**第 0 步的"时间轴真源"裁决**（§0.5） |

**G0–G2 施工期（2026-09-13）新增的取证推翻（本节续）**

| 原描述（本文/120） | 取证真相 | 处置 |
| --- | --- | --- |
| 本文 §一.1「`sourceIn` / `sourceOut`」 | 仓内持久化字段是 `sourceStart` / `sourceEnd`（`VideoProcessNode.tsx:204`），已收口的共用原语 `ClipTimeWindow` 亦用 `sourceStart`；§二.2 自己要求「与 `TimelineClip` 对齐」 | 就地改为 `sourceStart`/`sourceEnd`（§一.1 已回改）；实现见 `core/types.ts` |
| 本文 §一.4「常量 = `core/constants.ts` 单一出处」 | 视图常量（`MIN/MAX_PIXELS_PER_SECOND`）的唯一消费者在 `base/`，而 G-1 禁 base→业务域、G-2 禁 core→base ⇒ **物理上不可能同处一案** | 改「**分两层**」（§一.4 已回改）；`SNAP_TOLERANCE_PX` 等按 A8 在首次被消费处落地 |
| 本文 §一.4 `closeGaps` 原语 | 同表自述「语义入口 = `relayoutSequential`」⇒ 同义，另立即两名指一物 | 不另立（§一.4 已回改）；`timelineOps.test.ts` 断言其不存在 |
| 本文 §一.4 `splitAt` 用 `MIN_SPLIT_MARGIN` | §二.9 第 4 行已裁定「弱化」；「落在边界」由播放头吸附 `clipEdges` 变成明确状态 | 不建该常量，只判**严格内部**（§一.4 已回改） |
| 本文 §一.4 `pickTickStep(duration, pps?)` | `duration` 不参与决策 → 只会得到永不触发的分支或"总长兜底"（E7） | 收敛为 `pickTickStep(pps)`（§一.4 已回改） |
| 本文 §一.4「M2 字段留字段」 | 「留字段」是**序列化位置**，不是 M1 替 M2 定型；M1 写死猜测形状会在加载时静默过滤掉 M2 真实字段（假成功） | M2 四字段类型 = **`unknown`**（§一.4 已回改） |
| 本文 §一.7 读原语 `contentSetAsync` | 与本表「并发行」自述的 CAS/不静默覆盖冲突；`contentSetAsync` 属尽力而为族（可降级写本地副本） | **写路径取严格族 CAS**（§一.7 已回改）；实现见 `data/projectRepository.ts` |
| 本文 §二.1 / `124`：`director3d` 为共用层「待迁存量」 | 它是**帧基**且 `fps` 刻意未用（`fps: _fps`）；框选是一维邻近而非矩形相交 ⇒ 与共用层只有单位换算 | **不迁，保留分层**（§二.1 已回改） |
| 本文 §二.6 G-3「判据被抄成 2 份，需收成 `isCanvasSuppressed()`」 | **前置收口卡 9 已完成**（唯一真源 + 两机制消费 + 单测在位 + 模块外零消费） | **改判为已存在**，只补防回潮规则（§二.6 已回改） |
| 120 C1 表「`VideoEditorProjectRecord.projectId` **必须有**」 | 与本文 §一.2 及 120 C2 的键设计冲突；`createEmptyProject()`/`normalizeProject(raw)` 均无 projectId 入参 | **身份在键上**，记录体内不存（120 C1 已就地回改） |
| 120 C2.1「未登记键在 dev 下 `checkRegistered` throw」 | `checkRegistered` 是 `contentStore.ts:232` 的**内部函数、未导出** → 外部不可调用 | 红线实为**编译期 `check:keys` + 写入路径内部守卫**（120 C2.1 已就地回改） |
| 120 C2.2「写盘用 `createDebouncedPersist` 节流」 | 节流会让 `saveProject` 变成发射后不管 ⇒ **CAS 的 409 没有返回路径**，C2.7「不静默覆盖」当场失效 | 节流归**调用方**且必须消费 conflict（120 C2.2 已就地回改） |
| 120 C3 让解析走 `resolveAssetUrl` | `assetUrl.ts:179` 的 render 出口把**任何**本地文件 URL 改写成 `/files/thumbnail?…`（Jimp 只服务图片）→ 视频经它**静默拿错结果** | 取 `toAbsoluteFileUrl`（原文件语义）（120 C3 已就地回改） |
| 120 C5.5「实现 `BoundedConcurrencySource`」 | 其自述根因是 **WebKit**，而 120 C5 自证本仓跑 **Blink** ⇒ 根因不适用 | **不做**（§二.10 方案 A）（120 C5.5 / D5 已就地回改） |
| 120 D8「UPLOAD_DIRS 白名单锁步债」 | `check-upload-dirs.mjs` 只对账**顶层根**，`canvas/video-editor` 的顶层根 `canvas` 已在白名单（G0 实测两端 5 根一致） | **非债**（120 D8 已就地回改） |
| 120 §1.3 / C5.2 `export/mediaEngine.ts` 为「唯一封装层」 | 它只有 1 种实现（纯转发 `videoEngine`）= 假接缝（本文 §二.7 P1） | **不建**；缺口直接补 `base/utils/videoEngine.ts`（120 §1.3 / C5.2 已就地回改） |

---

## 五、锚点核对表（本次取证实录，供复核）

| 符号 / 事实 | 路径:行 | 关键证据 |
| --- | --- | --- |
| `generateId` | `src/components/base/core/idGen.ts:11` | `(prefix='id') => \`${prefix}_${Date.now().toString(36)}_${random}\`` |
| `getNodeMedia` | `src/components/base/canvas/nodeMedia.ts:49` | `(node) => {type, url}`，`MediaType='image'\|'video'\|'audio'\|''`（:47） |
| `buildSpawnNodes` / `spawnAndCommit` | `src/components/base/canvas/deriveNodes.ts:63` / `:152` | `(parentNode, childSpecs, edgeOpts) => SpawnResult` / `(spawned, handles) => Node[]` |
| `contentGetAsync` / `contentSetAsync` | `src/components/base/core/contentStore.ts:578` / `:588` | `(key) => Promise<unknown>` / `(key, value) => Promise<void>` |
| `checkRegistered` | `contentStore.ts:232` | **`function`（无 export）**，内部守卫 |
| `CANVAS_STATE_PREFIX` / 画布键 / d3d 键 | `contracts.ts:157` / `:200` / `:431` | `'canvas-state-v1-'`；两键均 `backend:'kv'` |
| `EVENTS` / `apiRegistry` | `contracts.ts:73` / `:680` | `Record<string, EventRegistryEntry{from,to,payload,note}>` / `{fn,method,path,envelope,status}` |
| `app_settings` 键 note | `contracts.ts:218-220` | 手工 note 列 setting key，注释明示"改动 settingRegistry 须同步" |
| `videoEngine` 时间单位=秒 | `src/components/base/utils/videoEngine.ts:57+` | `start/end`、`startTime`、`metadata.duration` 均秒 |
| `readVideoMetadata`/`processVideo`/`concatVideos`/`videoToGif`/`uploadResult` | 同文件 `:176`/`:206`/`:312`/`:519`/`:597` | — |
| `VideoSampleSink` | 同文件 `:29`（import）/`:398`（使用） | 来自 `mediabunny` |
| `UPLOAD_DIRS` | `src/components/base/utils/uploadDirs.ts:12` | 7 键：`tasks/web/canvas/canvasDrop/videoProcess/migrated/director3d`；**无 videoEditor** |
| `FullscreenShell` | `src/components/base/panels/FullscreenShell.tsx:64` | 内部 `createPortal(…, document.body)`(:99) |
| `hasModalLayer`/`subscribeModalLayer`/`useFullscreenEditorKeys` | `src/components/base/core/modalLayer.ts:153`/`:78`/`:223` | `registerLayer`(:111) **内部未导出** |
| `useCanvasShortcuts` + 查询式让位 | `src/hooks/useCanvasShortcuts.ts:47` / `:80` | `if (hasModalLayer()) return;` |
| `SETTING_DEFS`（无 `videoEditorOpen`） | `src/components/base/store/settingRegistry.ts:58` | keys：`thumbnailOn/minimapOn/agentOpen/performanceMode/debugOn/pinnedTools/autoSyncEnabled` |
| `HistoryStack` | `src/components/base/canvas/historyStack.ts:15` | `class HistoryStack<T>`（push/undo/redo/canClear…） |
| `useAppSettings` / `setSetting` | `src/components/base/store/appSettings.ts:111` / `:66` | — |
| `ImageEditor` 全屏挂载先例 | `src/components/base/editors/ImageEditor.tsx:960-979` | 用 `<FullscreenShell>`（:978 含 `fixed inset-0 z-ceiling`） |
| `check-arch` 业务域名单 | `scripts/check-arch.mjs:183` | `BUSINESS_RE = /^src\/components\/(nodes\|scriptbox\|agent\|panels)\//`（**无 videoEditor**） |
| `check-upload-dirs` 口径 | `scripts/check-upload-dirs.mjs:33-47` | 只取**顶层根**（`canvas/drop`→`canvas`）做集合对账 |
| `mediabunny` 版本 | `package.json:69` | `"mediabunny": "1.54.0"`（**已精确，无 `^`**）；`lucide-react` :68 |
| videoEditor 存量 | 全仓 | `src/` **0 匹配**；仅存在于 docs / mockup |

---

## 六、下一步

**卡点已解**：§二.10 已选 **方案 A**（不做 `BoundedConcurrencySource`，实测复现再按根因解处理）。

开工顺序（**前置在最前**）：
-1. **前置收口（`docs/125-视频剪辑器前置收口任务书-2026-09-13.md`）**：10 张收口卡 + 5 个批次（A→E）；**全部完成后**才进 G0 —— **此期间不碰 `videoEditor/` 任何代码**；
0. **第 0 步（§0.5）**：能力重叠普查 +「时间轴真源」裁决 → **已完成 2026-09-13**（产出 `docs/124`）；
1. **G0**：改 4 处（`STORAGE_KEYS` / `UPLOAD_DIRS` / `SETTING_DEFS` / `app_settings` note），跑 `npm run check:keys && npm run check:upload-dirs && npm run type-check`；
2. **G0b（守卫先行）**：§二.6 的 G-1/G-2/G-3 三条**改在写 `core/` 之前**——守卫先于代码，才能"防回潮"而不是"事后补"；
3. **G1**：`core/`（E/A/B/C，剔除 M2 项）+ `view/`（D）+ 单测；开工前先核 §二.5 拒绝清单。

---

## 七、落账清单（7 步法 Step 7.4 · **不做=白干**）

| 落点 | 写什么 | 状态 |
| --- | --- | --- |
| `docs/120`（契约真相源） | **就地修正事实错误**（`checkRegistered` 未导出 / D8 实为非债 / `mediabunny` 已钉死 / `check:arch` 对 videoEditor 原不成立 / C1 表 projectId / C2.2 节流归属 / C3 出图端点陷阱 / C5.5 不做 `BoundedConcurrencySource` / `mediaEngine.ts` 不建） | ✅ **已完成 2026-09-13**（就地改事实 + **保留原推理链** + 加「更新(2026-09-13)」注记；未删旧行） |
| `docs/113`（移植清单） | ① **就地修正**（§四 目录注释「节点按钮 + 拖进轨道」→ **点选即入轨**；§2 的 `d3dPersistence` 兜底表述）；② **然后并入本文件后归档**（独有价值只剩工时 / 风险 → 并入 §二.3 任务卡）。此后施工只认 **120（契约）+ 123（施工图）** 两份 | **待做** |
| `daily/架构日志/债务.md` | 登记改判（**不删旧行**）：D8 **非债** · `BoundedConcurrencySource` **不做** · id 重铸 **删** | **待做** |
| 区域日志 `<NN>-视频编辑器-<日期>.md` | 改动 / 度量 / 探针结果 / **债描述被修正处** | ✅ **已随 Gate 写**：`daily/架构日志/21-视频剪辑器前置收口-2026-09-13.md` §九–§十二（G0 / G0b / G1 / G2） |
| `index.md` | 区域状态 | 待做 |
| `spec/DATAFLOW.md` | 链路有变才刷（尚未落码 → 暂不刷） | N/A |

> ⚠️ 第一条是**必须项**：把修正只记在 123 §四 而不回改 120，就等于"同一真相两份"——**正是 §二.8 认定的母体本身**。
> 做的方式不是删旧行，而是**就地改事实 + 保留原推理链**（`CLAUDE.md` §5.2 留痕铁律：只追加，不抹决策）。
