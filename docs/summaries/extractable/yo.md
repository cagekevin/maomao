## yo — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L4767 |
| 类型 | fn |
| 入度 | 0 |
| 依赖 | `Y` (React), `X` (JSX(createElement)), `Un` (createPortal) |
| 参数 | `state`→`e`, `onChange`→`t`, `upstreamUrls`→`n` |

- **数据**: zIndex, filter, naturalWidth, naturalHeight, join, locked
- **hooks**: useState, useRef, useEffect, useCallback, useMemo
