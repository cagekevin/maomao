## Oo — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L7322 |
| 类型 | fn |
| 入度 | 0 |
| 依赖 | `Y` (React) |
| 参数 | `nodeId`→`e`, `initialUploadedAssets`→`t`, `updateNodeData`→`n`, `onUploadAsset`→`r`, `onShowToast`→`i` |

- **数据**: url
- **hooks**: useState, useRef, useEffect, useCallback
