## c_ — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L36121 |
| 类型 | fn |
| 入度 | 2 |
| 依赖 | `Y` (React), `X` (JSX(createElement)) |
| 被依赖 | `l_`, `Nv` |
| 参数 | `open`→`e`, `onClose`→`t`, `defaultAppId`→`n` |

- **数据**: createElement, value, body, select, execCommand, token
- **hooks**: useState, useCallback, useEffect
