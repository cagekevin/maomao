## av — ✅ 可提取

| 属性 | 值 |
|------|----|
| 行号 | L38176 |
| 类型 | fn |
| 入度 | 1 |
| 依赖 | `Y` (React), `X` (JSX(createElement)) |
| 被依赖 | `Nv` |
| 参数 | `open`→`e`, `globalTasks`→`t`, `useThumbnail`→`n`, `onClose`→`r`, `onRefreshTask`→`i`, `onRerunTask`→`a`, `onFullscreen`→`o`, `setGlobalTasks`→`s`, `showToastMessage`→`c` |

- **数据**: toLowerCase, total, page, items, map, filter
- **hooks**: useState, useRef, useCallback, useEffect, useMemo
