## id — 🔗 共享函数

| 属性 | 值 |
|------|----|
| 行号 | L19711 |
| 类型 | fn |
| 入度 | 23 |
| 依赖 | `Y` (React), `$`, `nd`, `X` (JSX(createElement)) |
| 被依赖 | `eo`, `ss`, `qc`, `Du`(createDefaultDirectorProject), `Iu`(createCharacterPreset), `Bu`(createSceneObject) ...共23 |

- **数据**: assets, objects, sourceType, kind, crowdId, crowdLabel
- **hooks**: useState, useMemo, useEffect
