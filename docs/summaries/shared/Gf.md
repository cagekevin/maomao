## Gf — 🔗 共享函数

| 属性 | 值 |
|------|----|
| 行号 | L22433 |
| 类型 | fn |
| 入度 | 4 |
| 依赖 | `Y` (React), `$`, `X` (JSX(createElement)), `If` |
| 被依赖 | `pp`, `mp`, `hp`, `_p` |
| 参数 | `mode`→`e`, `object`→`t`, `onObjectChange`→`n`, `onTransformEnd`→`r`, `onTransformStart`→`i`, `translationSnap`→`a` |

- **hooks**: useRef, useCallback
