# TASK-026 · 域主：`agent`

> **先读 `TASK-022-总纲-域籍判定与交叉验证规程（全批必读）.md`** —— 判据、四件证据、成因代号、输出格式、交叉验证协议全在那里，本文件只给**你的领地**和**本域特有事项**。
> **你只能写这个文件**：`docs/agent 批量任务/TASK-026-域主-agent.md`。**碰任何其他文件视为失败。**
> **本轮只登记，不搬。**（不许 `git mv`、不许改 `src/**`）

## 1. 领地（逐件，不许抽样）

```
src/components/agent/**        # 约 61 件，递归全部
```

实测子目录：`assistantTable/` · `canvas/` · `conversation/` · `panels/` · `runtime/`，+ 域根散件（若有）· 门面 `index.ts`（若有）。
**不含**（别越界）：`base/**`（TASK-028）· `src/hooks/**`（TASK-029）· `message`/`chat` 若实测住在别域，那是别域的活（别域会送给你，你第二波复核）。

## 2. 本域计划内结构（判「域内错位」的比对基准）

来自 `docs/plan/域归位-最终执行计划.md` §3：

```
agent/   （已有；**stores/ 并入 runtime/**）
```

计划对本域只写了**一条硬约束**：**`agent/stores/`（若存在）应并入 `runtime/`** —— 即：状态件与运行时件在**同一子域**，不另立 `stores/`。
其余子域按现状，但你必须按 D2/D3 复核它们**是否达标**：

| 子域 | 职责应当是什么 | 达标要件 |
| --- | --- | --- |
| `runtime/` | 智能体运行时（编排 · 工具调用 · 会话驱动）+ **状态** | ≥3 件、职责单一 |
| `conversation/` | 对话状态与消息流 | 同上 |
| `panels/` | 智能体面板/消息 UI | 同上 |
| `assistantTable/` | 助手表格子能力 | 同上 |
| `canvas/` | 智能体对画布的操作（工具实现） | 同上 · **注意与 `canvas/` 域区分** |

## 3. 本域特有事项（必须逐条给结论）

1. **`stores/` 是否已并入 `runtime/`**（计划硬约束）：实测若仍有 `agent/stores/` ⇒ 登记为「域内错位（组）」，给出逐件去处。若已并入，写明证据。
2. **`runtime/` 与 `conversation/` 的边界（D3）**：两者都可能装 store / 状态机。请逐件判：**状态件到底该在 `runtime/` 还是 `conversation/`**（按 D4 分层 + 职责单一），混装 ⇒ 假子域。
3. **`agent/canvas/` 是不是"域中域"（成因 H · 必答）**：这批件是"智能体驱动的画布操作"。判它的界面/数据落点，给结论：① 属 agent 的机制层 ② 属 `canvas/` 域 ③ 它其实是第三个东西。**只登记判断，不搬。**
4. **`assistantTable/` 达标性**：件数、职责纯度（是否混了非表格件）。
5. **域根散件（D1）**：域根除 `index.ts` 外出现任何件 ⇒ 逐件给应入子目录。
6. **域内分层方向（D4）**：`panels/`（视图）→ 允许依赖 `runtime/`；**`runtime/` 不许 import `panels/`**。抽 3 件跑 `refs` 验证。
7. **域内成环**：`runtime/` ↔ `conversation/` 互为依赖是常见形态，有环就登记（别自行重构）。

## 4. 线索（只是线索 · 不是结论 · 可能已过期）

> **不要照抄**。每条用 `refs`/`ls`/`grep` 现场复核；对不上按实测记录，并标进「描述过期」。

- `TD-25-3` / `TD-25-4`：原始债描述写 `base/ui/attachmentCover.tsx` · `base/utils/volumePolicy.ts`，计划 A4 的裁定是 **`attachmentCover` → `agent/panels/`**、**`volumePolicy` → `agent/runtime/`**。但这两笔债**已被裁为「误判撤回」**，而实测这两个文件**早已搬进 agent 域** ⇒ 典型**成因 G（描述过期）**。请现场核实它们**现在在哪个子目录**，并判该落点对不对。
- `TD-25-2`：`agentKeys` 已裁**非债**（键构造器 = 契约原语）。若你发现类似"键/常量构造器"类件散在 agent 域，按 D3 判它是契约层还是能力层（登记，别硬搬）。
- `TD-11-15`（母债）：摘要 4 件里的 `attachmentCover` 属本域；**摘要已过期**，别当清单用。
- `TD-18-38`：前端 AI 中继（约 919 行）平铺在 `base/api` —— 若它其实服务 agent，由 TASK-028 报送，你第二波复核。
- 计划 §3 对 `agent/` 只给了 `stores/ 并入 runtime/` 一条，**其余没定** ⇒ 你的「判据缺口」小节对本批特别重要：哪些子域边界**没有判据可依**，请写明。

## 5. 输出

按 **TASK-022 §7** 的格式（主表 + 8 个末尾小节）。**域籍判定列只许四种**。

## 6. 验收补充（除 TASK-022 §9 外）

- [ ] `find src/components/agent -type f | sort` 的**每一件**都在主表出现。
- [ ] 「`stores/` 并入 `runtime/`」给了**实测结论**（有/无 + 证据）。
- [ ] 「`runtime/` vs `conversation/` 边界」「`agent/canvas/` 是否域中域」「域根散件」三问**各有明确结论**。
- [ ] 对 `runtime/` 跑了 `refs` 验证它没反向 import `panels/`。
