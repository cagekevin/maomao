# TASK-020 · 核对：计划的 A1~A9 与 §5 验收表，逐条"做到了没"

> **你只能写这个文件**：`docs/agent 批量任务/TASK-020-核对-计划完成度总核对.md`。**碰任何其他文件视为失败。**
> **任务类型：A 类（核对）—— 清单来自计划本身，但"做到了没"只能你自己实测。**

## ⚠️ 铁律（违反重做）

1. **只登记，不搬不改**：不许 `git mv`、不许编辑 `src/**` 或 `tests/**`。你只产出本文件里的表格。
2. **既有文档只能当「目标」，不能当「结论」**：`docs/plan/域归位-最终执行计划.md` **必须读**（它是基准）；但**现状判断只能来自代码 + `refs` 实测**。禁止读 `daily/架构日志/债务*.md` 与其它 `TASK-*` 的产出文件。
3. **不许按名字 / 目录名判归属**（ADR-0040 L2）。每条结论必须附实测命令与输出。
4. **工具随便用（只禁"新建脚本文件"）**：
   - ✅ 允许：`node scripts/mv-sync-refs.mjs refs <file>` · `git` · `ls` / `find` / `grep` / `rg` · `for` 循环与管道 · 读任意文件。
   - ❌ 唯一禁止：在仓里**新建脚本文件**（`.mjs`/`.cjs`/`.sh`/`.js`）—— 你的产出只许是本文件。
5. **"大概范围"不等于"只有这些"**。

## 审计基准（唯一来源 · 不许自造判据）

**`docs/plan/域归位-最终执行计划.md`** —— §3「最终嵌套目录树」· §4「执行批次 A1~A9」· §5「验收总表」· §0.5「决定性事实」· §7「附录：原地件清单」。

## 你要核什么

**逐条**核对计划 §4 的 **A1 ~ A9 九个批次**，以及 §5 的**六项验收指标**。

产出「计划条款 → 实测 → 判定 → 证据 → 具体差距」。

🔴 **纪律**：计划里标了 `✅ 已完成` 的批次（A1 / A2 / A5）**也要核** —— 标注 ≠ 做完。
**已知值得怀疑的方向（不是清单，只是提示，你要自己查）**：
- §3 末尾那句「**旧目录必须消失**」列的每一项，今天还在不在？
- §2「审计裁决」表里每一条改判，代码里落地了没？
- §4 各批「验收」段写的数字（如「`base/store/` 剩 9 件」「`canvas/nodes/` 只剩 4 项」）今天对不对？
- §7 附录标「+ 迁入 xxx」的每一处，迁入了没？

## 输出格式（唯一）

| # | 计划条款（§几） | 计划要求（原话摘录 ≤40 字） | 实测 | 判定 | 证据（命令 + 关键输出） | 未完成的具体差距 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | §4 A6 表第 2 行 | `canvas/canvasSyncBus.ts` **回** `base/core/` | 仍在 `src/components/canvas/` | **未开始** | `ls src/components/canvas/` → `canvasSyncBus.ts` | 需 move + 同步引用 |

**判定列只许填**：`已完成` / `部分完成（缺什么）` / `未开始` / `判据过期（计划说的已不成立）`。

文末加：

```
## 本面结论计数
- A1~A9 各批：已完成 N / 部分完成 N / 未开始 N
- §5 六项指标：达标 N / 未达标 N
- 计划自身过期或自相矛盾处（附证据）：
```

## 验收标准（自测 · 缺一即不合格）

- [ ] A1~A9 **九个批次每个都出现过**（含标 ✅ 的），§5 **六项指标逐项**有实测数字。
- [ ] 每条"已完成"都附**可复现命令 + 关键输出**；禁止写"我看过了 / 应该没问题"。
- [ ] 每条"判定过期"都给出为什么过期（哪次搬迁/改判导致）。
- [ ] 除本文件外未改动任何文件（`git status` 自检）。
