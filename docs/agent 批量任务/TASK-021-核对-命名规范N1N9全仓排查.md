# TASK-021 · 核对：命名规范 N1~N9（ADR-0039）的全仓违反项

> **你只能写这个文件**：`docs/agent 批量任务/TASK-021-核对-命名规范N1N9全仓排查.md`。**碰任何其他文件视为失败。**
> **任务类型：A 类（核对）—— 规则来自 ADR，清单你自己找。**

## ⚠️ 铁律（违反重做）

1. **只登记，不搬不改**：不许 `git mv`、不许编辑 `src/**` 或 `tests/**`。你只产出本文件里的表格。
2. **既有文档只能当「目标」，不能当「结论」**：`docs/adr/ADR-0039-*.md`（命名规则）与 `docs/adr/ADR-0038-*.md`（字符串边界）**必须读**；但**现状只能靠 `grep`/`ls`/`refs` 实测**。禁止读 `daily/架构日志/债务*.md` 与其它 `TASK-*` 产出。
3. **不许按名字 / 目录名判归属**（ADR-0040 L2）—— 本任务查的就是"名字"，所以**每条违反都必须附实测**（同名导出 ≥2 处、文件≠主导出名……），不许凭感觉。
4. **工具随便用（只禁"新建脚本文件"）**：
   - ✅ 允许：`grep` / `rg` / `ls` / `find` / `node scripts/mv-sync-refs.mjs refs` · `for` 循环与管道 · 读任意文件（含读 `docs/adr/`）。
   - ❌ 唯一禁止：在仓里**新建脚本文件**（`.mjs`/`.cjs`/`.sh`/`.js`）—— 你的产出只许是本文件。
5. **"大概范围"不等于"只有这些"**。

## 审计基准（唯一来源）

`docs/adr/ADR-0039-*.md` 的 **N1 ~ N9**（逐条读原文，含每条的「判据 / 边界 / 例外」）+ `docs/adr/ADR-0038-*.md`（N8 的边界）。

## 你要核什么（每条规则都要落到"证据"）

| 规则 | 查什么 |
| --- | --- |
| **N1** | 域专用导出带**域前缀**（camelCase，域名原词不缩写）。**判据 = 撞名**（同名导出 ≥2 处且分属不同域）—— 不是"所有导出都加前缀" |
| **N2** | 文件名 = **主导出名**；纯类型文件叫 `<域>Types.ts` |
| **N3** | `*Store.ts` **仅状态真源**（非真源不许叫 Store；是真源却不叫 Store 也要报） |
| **N4** | `Contract` 一词**专属** `core/contracts.ts`（别处出现即违反） |
| **N5** | 域目录**必须有 `index.ts` 门面**；跨域**只准**走门面（直连内部件 = 违反；例外须在门面头写理由） |
| **N6** | 横切层**不放域专用物**；通用物**不装进域** |
| **N7** | 前缀用**域名原词**（`videoEditor` ✅ / `ve` ❌） |
| **N8** | 改名**只保证符号名 + 文件路径**；运行时字符串（键名/事件名/节点类型串/uploads 子目录/URL）**默认不改** → 边界见 ADR-0038 |
| **N9** | *(ADR 里若有第 9 条，一并核；以 ADR 原文为准)* |

🔴 **N5 注意**：门面"存在"不等于"被走"。你要测的是 **跨域消费点实际走没走门面**，不是目录里有没有 `index.ts`。

## 输出格式（唯一）

| # | 规则 | 违反项（`文件:行` 或 `文件`） | 实测证据（命令 + 关键输出） | 该域/该件现在是否已有例外登记 | 建议 |
| --- | --- | --- | --- | --- | --- |
| 1 | N4 | `src/components/x/y.ts:12` 导出了 `FooContract` | `grep -rn "Contract" src \| grep -v core/contracts` | 无 | 改名 / 移入 contracts |

文末加：

```
## 本面结论计数
- N1~N9 各条命中违反数：
- 无违反的规则（也要列，证明你查过）：
```

## 验收标准（自测 · 缺一即不合格）

- [ ] **N1~N9 每条**都有一段结论（含"无违反"的也要写，并附你跑过的命令）。
- [ ] 每条违反都附**可复现命令 + 关键输出**；不许只写规则名。
- [ ] N1 的结论必须基于"**是否撞名**"，不是"是否所有导出都带前缀"。
- [ ] N5 的结论必须区分「门面是否存在」与「门面是否被走」两个数。
- [ ] 除本文件外未改动任何文件（`git status` 自检）。
