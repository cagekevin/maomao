# 云存储配置机制（待补全）

> 本文件为占位骨架。请按 `docs/模块专题/` 现有文档风格补全内容。
> 核心要求：**所有 `文件:行号` 与函数名必须先在 `src/` 里 grep 验证，再写入**，不要凭空编造。

## 补全指引（请据此 grep 后展开正文）

- 存储键：`Z.CLOUD_STORAGE_CONFIG = 'cloud_storage_config'`（`src/config/storageKeys.js:12`）。
- App.js 中的状态与消费点：
  - 状态 `cloudStorageConfig: ce`（`App.js:29201`）。
  - 启动加载：`Q.getObject(Z.CLOUD_STORAGE_CONFIG)`（`App.js:40582`），注入 `ce`。
  - 落盘：`Q.setObject(Z.CLOUD_STORAGE_CONFIG, o)`（`App.js:40989`）。
  - 同步给节点实例：grep `cloudStorageConfig` 在 `fileToUrlNode` 等节点里的使用（`App.js:29319` 附近、`:33723` 附近、`:42012` 等）。
- 说明该配置是「用户把资源上传到自有云存储（如图床/对象存储）的凭据」，与《中转资源本地落盘机制》的本地 :18080 中转是两条不同路径；与《GAS 云同步机制》的 `gasUrl` 也不同。
- 导出导入里的处理：`:41972` / `:42012` 相关分支。

## 文档规范

- 中文、分节、用 grep 验证过的 `文件:行号` 引用 + 少量 verbatim 代码片段。
- 压缩变量名（如 `ce`、`o`）请标注可读语义；行号注明可能漂移。
- 不改动任何源码，只产出本 `.md`。
- 写完后把本文件顶部「待补全」字样删除，并说明已补全。

（完成后请通知主 AI 审计，并同步更新 `README.md` 与 `审计与覆盖清单.md`。）
