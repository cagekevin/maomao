# 模块专题（按需查阅）

> 本目录是按模块深挖的剖析文档，**不是改码前必看**，而是在你确实要动某块功能、想先搞清它全貌时才查阅。
> 所有内容均 grep 源码坐实（行号随构建漂移，以函数语义名 + 真实路径为准）。动手前仍须回源码复核。

## 目录

- 画布模板生命周期
- 自定义节点模板生命周期
- 资源上传机制
- GAS 云同步机制
- 本地 JSON 存储机制

> 画布模板 / 自定义节点模板是两套独立体系，别混写。资源上传 / GAS 同步 / 本地 JSON 存储三篇各自独立、互不关联。
