# 模块专题 · 审计与覆盖清单（查漏补缺）

> 审计日期：2026-07-23
> 审计范围：`docs/模块专题/` 现有 5 篇 + 对照 `src/App.js`、`src/utils/storage/index.js`、`src/config/storageKeys.js` 实际代码
> 方法：grep 复核实函数名 / 存储键 / 行号；枚举 `storageKeys.js` 全部键 + 功能入口，找缺失模块
> 结论：现有 5 篇整体可信，发现 1 处事实错误（已就地修正）；应用真正高频的核心模块——画布自动保存、项目多画布、AI 任务轮询、导出导入、本地中转落盘——全部缺文档，本次补写 P0 五篇。动手前仍须回源码复核行号。

---

## 一、现有 5 篇准确性复核

### 1.1 已核对准确的要点

| 文档 | 关键事实 | 复核结果 |
|------|----------|----------|
| 本地JSON存储机制 | `Q.setObject` 空值拒写（{} / [] / 空串）@ `storage/index.js:239-248` | ✅ 行号吻合 |
| GAS云同步机制 | `CloudSyncEngine`@41312、`syncToCloud`@41365、push 键集合 | ✅ 行号吻合 |
| 自定义节点模板 | `CUSTOM_NODE_TEMPLATES`@`storageKeys.js:9`、`Si`/`Ci`/`Tn` | ✅ 吻合 |
| 画布模板 | `LOCAL_TEMPLATES_KEY`（文档写 29043，实 28626，漂移但同函数）、`Qh`/`$h`/`Jr` | ✅ 同函数，行号漂移符合预期 |
| 资源上传机制 | `Zh`@28612、`qh`/`Jh()` | ✅ 吻合 |

### 1.2 已修正的错误

- **`本地JSON存储机制.md` §二「写入优先级 wr→Mr→Nr→Pr」描述错误**：实际 `Q.setObject`/`Q.setConfig` **只写 `wr`（localTool KV）**，localTool 不可用直接返回 false，不存在写降级链。该顺序仅存在于**读取/迁移**路径（`getObject`→`jr` 缓存→`wr`；`syncToLocalTool`≈305 按 `Mr→Nr→Pr` 把数据拉回 `wr`）。已就地更正。

---

## 二、模块覆盖矩阵（真实模块 vs 已文档）

枚举 `storageKeys.js` 全部键 + 关键功能入口，标注覆盖状态：

| 模块 | 存储键 / 入口 | 状态 | 优先级 |
|------|--------------|------|--------|
| 画布状态自动保存 | `canvas-state-v1-<projectId>` | ❌ 缺 → **已补** | P0 |
| 项目与多画布 | `projects` / `lastOpenedProject` | ❌ 缺 → **已补** | P0 |
| 导出 / 导入 | `exportData`/`importData`（`Ri`/`Bi`） | ❌ 缺 → **已补** | P0 |
| 中转资源本地落盘 | `transitResources` + `Zr`（18080 FILE api） | ❌ 缺 → **已补** | P0 |
| 全局任务与 AI 轮询 | `globalTasks` + 9004 轮询 | ❌ 缺 → **已补** | P0 |
| 节点体系与 AI 生成流水线 | `*Node` + 网关 `:9004` | ❌ 缺 → **已补**（2026-07-23，已审计） | P1 |
| 会员 / 登录 | `membership` / `auth_token` | ❌ 缺 → **已补**（2026-07-23，已审计） | P1 |
| API 配置 | `api_configs` / `app_settings` | ❌ 缺 → **已补**（2026-07-23，已审计） | P1 |
| 提示词管理（预设提示词 + 提示词库/广场/最近使用 + 节点下拉） | `presetPrompts` | ❌ 缺 → **已补**（2026-07-23，已审计） | P2 |
| 模型调度 | `modelSchedules` + `src/services/modelSchedules.js` | ❌ 缺 → **占位已建**（待 AI 补全） | P2 |
| 云存储配置 | `cloud_storage_config` + `App.js:40582/40989` | ❌ 缺 → **占位已建**（待 AI 补全） | P2 |
| 背景 Service Worker | `src/background.ts` | ❌ 缺 → **占位已建**（待 AI 补全） | P2 |
| 画布模板 | `local_templates` | ✅ 已有 | — |
| 自定义节点模板 | `customNodeTemplates` | ✅ 已有 | — |
| 资源上传（云端） | `upload/asset` | ✅ 已有 | — |
| GAS 云同步 | `CloudSyncEngine` | ✅ 已有 | — |
| 本地 JSON 存储 | `StorageManager Q` | ✅ 已有（已修正 1 错） | — |

> 注：小配置键（`device_id`/`current_user_id`/`video_size`/`video_seconds`/`video_model`/`sync_version`/`last_sync_time`）与 `app_settings` 散字段（默认模型、`*ApiConfigId`、`transitGridCols`、`globalPolling*`）多依附于上方模块，未单列。

---

## 三、补缺计划与执行

- **P0（本次已补写 5 篇）**：画布状态自动保存机制 / 项目与多画布机制 / 导出导入机制 / 中转资源本地落盘机制 / 全局任务与AI生成轮询机制。
- **P1（待补）**：无。（节点体系与 AI 生成流水线、会员登录机制、API 配置机制均已于 2026-07-23 补写并审计通过）
- **P2（占位已建，待 AI 补全）**：模型调度、云存储配置、背景 Service Worker 三篇已建占位骨架（含 grep 锚点指引），待另一 AI 读 `src/` 补全正文并交主 AI 审计。（提示词管理已于 2026-07-23 补写并审计通过）
- **已修正**：`本地JSON存储机制.md` §二 写入顺序描述。

---

## 四、一句话结论

模块专题原 5 篇集中在「配置 / 模板 / 同步 / 存储」；应用每日真正跑的高频核心——画布自动保存、项目多画布、AI 任务轮询、导出导入、本地中转落盘——此前全无文档。本次修正 1 处错误并补齐 P0 五篇，P1/P2 视需要续写。
