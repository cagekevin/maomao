# 模型调度机制（待补全）

> 本文件为占位骨架。请按 `docs/模块专题/` 现有文档风格补全内容。
> 核心要求：**所有 `文件:行号` 与函数名必须先在 `src/` 里 grep 验证，再写入**，不要凭空编造。

## 补全指引（请据此 grep 后展开正文）

- 存储键：`Z.MODEL_SCHEDULES = 'modelSchedules'`（`src/config/storageKeys.js:11`）。
- 调度逻辑实现：`src/services/modelSchedules.js`（请读全文，定位模型→网关/配置的分发函数、默认模型回退逻辑）。
- 权限/可用模型：`src/services/modelEntitlements.js`（已审计过，`LOCAL_MODE_ALLOW_ALL`、`allowedModels` 等；注意与会员篇衔接）。
- App.js 中的模型选择状态：`textModel` / `drawingModel` / `videoModel` / `sd2VideoModel` / `audioModel` 等 setter（grep `textModel`、`drawingModel` 等定位），以及启动加载 `@40770` 附近、落盘 `@40989` 附近。
- 说明「调度」与「API 配置」的关系：节点实际用哪个 `apiConfigId`、模型不可用时如何回退。

## 文档规范

- 中文、分节、用 grep 验证过的 `文件:行号` 引用 + 少量 verbatim 代码片段。
- 压缩变量名（如 `ce`、`s`、`vt`）请标注可读语义；行号注明可能漂移。
- 不改动任何源码，只产出本 `.md`。
- 写完后把本文件顶部「待补全」字样删除，并说明已补全。

（完成后请通知主 AI 审计，并同步更新 `README.md` 与 `审计与覆盖清单.md`。）
