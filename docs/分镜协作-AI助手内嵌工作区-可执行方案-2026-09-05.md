# 分镜协作 · AI 助手内嵌工作区 —— 可执行技术方案（4 步法施工稿）

> **上游权威**：`docs/分镜协作-AI助手内嵌工作区-2026-09-05.md`（方案定稿，需求/决策以它为准）。
> **本文档目标**：把定稿需求翻译成**可落地的工程方案**——按「定契约 → 定数据流 → 构思测试 → 施工骨架」四步走，
> 重点锁死 **文件归属 / 数据流 / 契约登记**，消灭返工。施工时以本稿为准，读完即可动手。

> **架构纪律总纲**（本工程横切 5 类单一入口，改动必须走它们，禁自建旁路）：
> ① 通信层 `eventBus`（EVENTS 登记）② 表现层 `toastStore` ③ 观测层 `logger` ④ 持久化层 `storageAdapter + *Store` ⑤ 能力层单一函数。
> 契约常量一律查 `src/components/base/core/contracts.ts`（`STORAGE_KEYS` / `EVENTS` / `NODE_TYPES`），禁裸字面量。
> 会话数据真相源 = `conversation/`（底座 + 聚合 `conversationStore.ts`），外部从 `agent/index.ts` barrel import，勿绕深层路径。

---

## State 1：定契约（输入 / 输出 / 边界 / 文件归属）

### 1.1 一句话需求复述

在 AI 助手面板 `AgentPanel` 左侧开一个**分镜表格工作区**，与右侧 AI 对话并排协作：
- 表格数据两个来源：① 外部粘贴纯文字（首行为表头，其余为数据行）；② **在对话区发「需求 + Skill」让 AI 生成整表**（顶层 `globalStyle` + 行数组，AI 按 Skill 设计列/填行）。
- 选中某一行（可加选画布图作参考）→ 作为上下文随对话发给 AI；AI 精简结构化返回 → **对话框确认后写回该行**，可反复打磨。
- 某行定稿后可"发送到画布"落成节点去生图/视频（**可选出口**）。
- **表格完全可定义**：AI 生成/粘贴只是起点，落表后点格改字、加删行、调序、改列都可自由编辑（见 §1.6）。
- **表格归属 = 当前 AI 对话**：一个对话一份表，新建对话=新空表，回看旧对话=表还在。不做多项目管理。

### 1.2 输入源 → 输出目标 → 副作用（本步只定边界，链路归属 State 2）

| 能力 | 输入源 | 输出目标 | 副作用(Side Effects) |
|---|---|---|---|
| A. 粘贴成表 | 剪贴板 `clipboardData`（TSV 文本 / HTML `<table>`），**表格入口显式拦截** | 内存态分镜表（表头 `columns[]` + 行 `rows[]`），经会话持久化 | 写会话记忆字段；走 `contentStore` 落盘（会触发 `persist:failed` 可观测） |
| B. AI 生成初始表 / 改单行 | 需求文本 / 选中行文字 + globalStyle +（可选）参考图 | AI 精简结构化 JSON → **渲染成可视化预览 → 用户确认 → 写回表** | 发一次 LLM 调用；确认后才写表，预览不改数据 |
| C. 选行(+画布图)作上下文发 AI | 表内选中行、画布选中节点图 | 对话框新增一轮 user 消息（携带选中行 + globalStyle + 参考图） | 调 `useAgentChat.send`；会话历史 + 落盘 |
| D. 单行落画布节点（出口） | 定稿行文字 | 画布上一个文本节点（承载行内容） | 走 `canvasHost` 唯一写入口建节点（禁裸 `setNodes`） |

### 1.3 收口检索：复用 vs 新增

**已有可复用（标记"复用/扩展既有件"，勿新造）**：
- **会话隔离/持久化**：`conversationState.ts` 按 agentKey 隔离 + KV 落盘 + 水化时序守卫 → **分镜表挂在当前会话记忆字段**（与 `global_contract`/`artifacts` 并列），顺着现有隔离走，**不另建存储体系**。
- **AI 结构化输出契约**：剧本盒 `scriptBoxPrompts.ts` 的 `SCRIPT_WRITER_FORMAT`（"只返回纯 JSON"输出契约）+ 顶层 `globalStyle` + shots/行数组形态 → **照抄为工作区生成/改行的 schema**。
- **globalStyle 通道**：会话 `memory.global_contract.unified_style_prompt` 是既有"统一风格→自动注入 LLM"通道 → 工作区 globalStyle 映射到它（见 State 2），不造新字段名。
- **选中画布图→参考图**：`App.tsx` 传 `selectedImageNodes` + `useCanvasAgentTools.getNodeImageUrl` → AgentPanel 现有 `pendingImageNodes/confirmPendingImages` 附件链路。**去掉"附件卡片/防误触" UI**，保留"选中行/图作上下文随对话发"的底层带图能力。
- **确认交互范式**：`AgentConfirmCard` + `conversationStore` 的 `setAwaitingConfirm/getAwaitingConfirm` 是现成"AI 返回后需用户确认"的卡片 → 改单行/生成初始表的"确认写回"借鉴此范式（非复制整套 runMode 门禁）。
- **「发送到画布」= 既有工具 + 既有按钮**：`useCanvasAgentTools.ts` 的 `create_node` 工具（L382，`text` 参数把内容落 textNode 的 `data.text`）就是 AI 建文本节点的现成工具，走 `canvasHost` 唯一入口；`useAgentChat.sendContentToCanvas`（L946）已是"把一段文本建成 textNode"的**现成按钮函数**，AgentPanel 已持有。表格"某行发到画布"**直接复用 `sendContentToCanvas`**（入参 = `storyboardModel.rowToText` 拼出的行文字），不新造落画布函数。

**确属新增（无同类件，需新建，归属见 §1.4）**：
- 分镜**表格数据结构 + 归一化**（`columns`/`rows`/`selectedRowId`/`preview`）——项目无现成可编辑表格数据。
- 表格**渲染 + 单元格编辑**（点格改字、加删行、上下移、复制）——`clipboard.sanitizePastedText` **会压平 `\t` 不能用于 TSV 解析**（已核对 `clipboard.ts:37-56`），需在表格入口内**自行解析 TSV(`\t`) / HTML(`<table>`)**。
- 表格内**显式粘贴入口**（与画布级 `useAssetDropPaste` 分流）。
- **行↔JSON 双向映射**（row ↔ `{列名:值}` 对象、`rows[]↔顶层 globalStyle 结构`）。

### 1.4 文件归属（先定，防返工）

> 原则：优先并入既有文件 / 复用既有职责点；确需新文件才新建，且与目录语汇一致、引用单向。
> 任何移动/改名文件一律走 `node scripts/mv-sync-refs.mjs`，禁手写 mv 或漏改 import。

**本次改动按"宿主 + 新增模块"划分，尽量少落新文件**：

| 归类 | 文件 | 动作 | 说明 |
|---|---|---|---|
| **并入既有** | `src/components/panels/AgentPanel.tsx` | 改造 | 左栏分镜表格 UI 壳（表格面板 + 右侧对话）。**UI 壳留 panels/**，逻辑不堆这里。 |
| **并入既有** | `src/components/agent/conversation/conversationSnapshot.ts`（D 类快照）/ `conversationAiState.ts`（F 类：contract/artifact 读写） | 扩展 | 新增「分镜表」作为会话记忆的一个字段读写（对齐 `setCurrentGlobalContract` 范式，见 State 2 归属裁决）。 |
| **并入既有** | `src/components/agent/index.ts`（barrel） | 追加 re-export | 新增对外符号一律在此透出，外部不绕深层 import。 |
| **并入既有** | `src/components/base/core/contracts.ts` | 登记 | 新增存储字段名/事件（若有）在此登记。 |
| **新增（能力层纯函数）** | `src/components/agent/storyboard/storyboardModel.ts`（建议新目录 `agent/storyboard/`） | 新建 | 表格数据结构 + 归一化 + TSV/HTML 解析 + 行↔JSON 映射 + `rows[]↔globalStyle` 序列化/反序列化。**100% 纯函数**，无 React/state（对齐 `scriptBoxPrompts.ts` 的"纯函数层"铁律，便于单测）。 |
| **新增（UI + 逻辑薄壳）** | `src/components/agent/storyboard/StoryboardPanel.tsx` + `storyboardPrompt.ts` | 新建 | 左栏表格组件（渲染/单元格编辑/粘贴/预览/确认写回按钮）+ 注入 AI 的 system/user 拼装。 |
| **并入既有** | `src/components/agent/runtime/useAgentChat.ts` | 改造（仅透出） | `send` 调用点接住"选中行上下文"；落画布**直接复用既有 `sendContentToCanvas`**（已是 textNode 单一按钮入口，勿新增）。 |

> ⚠️ **不新建**：不新建独立 store / 存储键 / 事件——分镜表**随会话记忆走既有会话隔离链路**（见 State 2），避免冒出第二套存储与第二份渲染数据源。

### 1.5 额外检查（边界矛盾点）

- **确认才写回是数据真相**：改单行/生成初始表都是"AI 出 JSON → 预览 → 确认 → 才写表"。**预览与正式表是两份不同状态**，确认动作是唯一写回闸口，需防"预览误当正式表"。
- **表格归属切换**：切对话/新建对话时，左侧表格必须跟随 `activeConversationId` 变（空表/旧表），不能串话。
- **图只读、不带回**：AI 参考图 URL 只在发出时带上，AI 返回不含图字段；回填只覆盖文字。
- **globalStyle 空值**：随内容自然发 AI（空则 AI 补、有则守），**不加"空则填/非空则守"这类刻意义务逻辑**（需求定稿 §1.4 决策 14）。

### 1.6 表格承载批量：AI 生成整表（需求 + Skill，走对话区）

> **需求补充（2026-09-05）**：表格是**完全可定义**的结构化容器，是将来**批量的承载者**。
> 用户原话："我需要这个表格是完全可定义的。一开始我发一个需求加一个 skill（如'如何把一句话变成一个完整的分镜脚本'），AI 就帮我生成了整个表格。"

**① 生成整表 = 需求 + Skill，走对话区**：
- 入口在 **AI 对话区**（不是表格工具条单独造 Skill 选择 UI）：你在对话框里（已选 Skill / 发一段 Skill 内容 + 一句话需求）→ AI 理解 → **返回整个表格结构 JSON**（列名由 AI 按 Skill 设计 + N 行内容，顶层可带 globalStyle）。
- 表格只承接：AI 返回的 JSON → 渲染成**可视化预览表格** → 你确认 → 落表成可编辑数据。
- **Skill 在这里的角色** = "如何把 X 变成完整 Y"的**方法论指令**，让 AI 知道按什么结构去填表（列/行/内容组织）。它不再是"AI 直接批量跑 N 个 generation 图"的依据。
- **「AI 生成整表」与「改单行」共用同一套 JSON schema**（顶层 globalStyle + 行数组），仅行数/范围不同（整表 vs 单行）。

**② 高度自定义（表生成后可任意改）**：
- 生成只是起点。落表后**完全可编辑**：点格改字、加行/删行/上移/下移/复制、改列名、（后续可按列类型化支持图片列）。
- **选中单行 → 交给 AI 优化/补齐该行**（走对话区，AI 只改该行文字，图只读参考）→ 预览 → 确认写回，可反复打磨。
- 不预设列模板——列/行全由 Skill+需求或粘贴决定，AI 生成后你仍可自由调整结构。

**③ 承载批量（与 AI 助手"批量编排删除"的衔接）**：
- AI 助手现有一套"Skill 三阶段 / AI 规划多 generation 批量执行"将删除（见 `docs/AI助手-执行模型精简方案-2026-09-05.md`）。
- 批量由**本表格承接**：Skill + 需求 → AI 产出**结构化内容行**（非直接生成图）→ 落表 → 你编辑确认 → **逐行/整表触发生成**。
- 表格的"逐行触发生成"走"单行内容 → 建节点 → 生成"的底层（execute_plan 单 gen 能力保留给交互式生成），不需要 AI 现场造 generation 数组。
- **确认 UI**：AI 生成整表 / 改单行的"确认写回"沿用现有 `AgentConfirmCard` 类确认交互；该确认卡将来也服务表格的"整表/批量确认后生成"（`docs/AI助手-执行模型精简方案` §保留清单里确认卡 UI 先保留、注明改造即为此）。

**数据流一句话**：对话区发「需求 + Skill」→ LLM 返回表结构 JSON → `storyboardModel.jsonToSb` → 渲染预览 → 确认 → `setCurrentStoryboard` 落表(可编辑) → 之后逐行打磨/逐行触发生成。

### 1.7 表格模式：展开表格时给 AI 追加"表格专注上下文"（决策，2026-09-05）

> **需求**：展开表格即进入"表格模式"，用**同一个 AI**（不是新对话/新 agent），但通过**动态追加系统提示词**让 AI 聚焦表格，减少它"猜你是要表格还是要画布"的摇摆。
> 用户原话："展开表格就变成表格模式，系统给 AI 一个提示词让它专注这个表格，防止 AI 猜我意图。"、"还是同一个 AI，展开后系统提示词就变成表格模式。表格模式我希望 AI 能生成某行。"

**决策要点**：
- **同一 AI、同一会话**：展开/收起表格不换对话、不换 agentKey，只**动态改变注入给该 AI 的 system 上下文**。
- **追加而非替换**：展开表格时在普通系统提示词之上**追加**一段"表格专注上下文"，不替换整条系统提示词（保留 AI 完整能力，见下）。收起表格 → 追加块移除，回到普通。
- **工具保留（AI 能生成某行）**：表格模式**不砍画布工具**——因为用户确认"AI 要能直接生成某行"。AI 在表格模式下仍需建节点/触发 `execute_plan` 单 gen 去生成该行图/视频，credit 闸照拦烧钱。
- **AI 不用猜意图**：追加块明确告知"当前工作对象是左侧表格（列结构 + 当前选中行）"，用户说"改第3行/生成这行"时 AI 直接对应表格行，不必在"改表格 vs 去画布"间摇摆。

**注入块内容（展开表格时追加的 system 文本，含当前表格列结构）**：
```
【表格协作模式】当前展开左侧分镜表格，是当前对话的结构化内容（与对话隔离，随对话持久化）。
列结构：<列名1>, <列名2>, ...（由当前表格 columns 实时拼接）
你的任务：配合用户生成/填充/优化表格内容——整表生成走 JSON（顶层 globalStyle + 行数组），
改单行返回该行 JSON（带列名）。用户说"生成某行/这行去生成"时，把该行内容落到画布建节点并生成。
AI 返回表格内容前，先给用户看可读预览（渲染成表格）等确认，不直接改正式表。
```
- **实现归属**：追加块由 `useAgentChat` 在 send 时**动态拼入 systemPrompt**（或作为额外 system 消息 push），数据源 = 当前表格的列结构（从会话记忆 `storyboard` 读，经纯函数 `storyboardModel` 序列化列名）。
- **触发态**：AgentPanel 维护 `tableOpen` 态（左栏表格是否展开）；展开时注入，收起时移除。
- **与 §1.6 的关系**：§1.6 的"需求 + Skill 生成整表 / 改单行"正是在此表格模式上下文下进行；此注入块让 AI 知道"现在在处理表格"。

### 1.8 AI 获取表格上下文：自动注入 + 预留只读工具（决策，2026-09-05）

> **需求**：AI 默认聚焦用户给的"当前选中行"，但需要完整表格内容时也能自己取，不靠用户每次把整表塞进对话。
> 结论：**双通道**——① send 时自动注入"列结构 + globalStyle + 当前选中行"（轻量，AI 开箱聚焦）；② 预留一个**只读工具** `read_storyboard`，AI 需要看选中行以外的完整表/某行时可自己调。

**① 自动注入（每次 send，轻量）**：
- 注入内容：当前表格 **列结构（列名列表）+ globalStyle + 当前选中行**（带列名）。让 AI 知道表长啥样、要处理哪行。
- 不给全表（几十行塞进去浪费 token 且让 AI 分心）；AI 需要全貌走下面工具。

**② 预留只读工具 `read_storyboard`（AI 按需自取）**：
- 语义：**只读**当前对话的分镜表格上下文，无副作用、不改表、不落盘。
- 参数：
  - 无参 → 返回当前表格**完整上下文**（列名 + 各行内容带 id + globalStyle）。
  - `rowId` → 只返回该行（带列名），供 AI 精确看某行。
- 实现归属：加在 `useCanvasAgentTools` 的工具注册表（AI 可调）；执行时读 `getCurrentStoryboard()`（当前 agentKey/当前会话的表格，勿读错对话），经 `storyboardModel` 序列化成文本返回。
- **防滥用**：工具 description 写明"仅当需要查看当前选中行以外的表格内容（其它行/完整表结构）时才调用；一般场景上下文已含当前选中行，无需调用"——**预留但引导 AI 不滥用**（避免 AI 每次都读全表多此一举）。
- **为什么用工具而非只注入全表**：注入给 AI"聚焦单行"，工具给 AI"需要时自取全貌"，两者互补；AI 既有上下文又不必被几十行拖慢。

**数据流一句话（AI 拿表格上下文）**：`StoryboardPanel` 选中行 → send 时前端把"列结构+globalStyle+选中行"拼进上下文 → AI 开箱有当前行；若需全貌 → AI 调 `read_storyboard`（无参全表 / rowId 单行）→ 只读返回。

---

## State 2：定数据流（链路 / 唯一入口 / 契约登记）

### 2.1 数据从哪来、经哪些模块、落哪

核心链路（单向，无环，对齐 `agent/index.ts` 顶部全景）：

```
[分镜表状态] StoryboardPanel(左栏)
   │ 读写
   ▼
session-memory 字段 storyboard(per-conversation)   ←── 唯一数据真相源
   │ setCurrentStoryboard(…)  →  conversationStore.commit → contentStore 落盘(自动)
   │
   ├── [粘贴] StoryboardPanel.onPaste ──► storyboardModel.parsePasted(clipboardData) 纯函数
   │                                              │ 产出 { columns, rows }
   │                                              ▼  (State1 A)
   ├── [生成初始表 / 改单行] StoryboardPanel ─► send(带 storyboardPrompt.user) 
   │                                        → useAgentChat.send(text) → LLM
   │                                              │ 返回 精简 JSON
   │                                              ▼
   │                               StoryboardPanel: renderPreview(json) → 确认 → setCurrentStoryboard(写回)
   │
   ├── [选中行作上下文] 点选行(+画布图) → send(拼入选中行文字 + globalStyle + refImage)
   │
   └── [单行落画布] StoryboardPanel.onSendRowToCanvas(row)
        → storyboardModel.rowToText(row) 拼成文字
        → 复用 useAgentChat.sendContentToCanvas(text)   ← 既有「发到画布」按钮同一函数
        → callTool('create_node', { type:'textNode', text }) → canvasHost 建 textNode
```

**每条链路的输入 / 输出 / 失败拦截点**：

| 链路 | 输入 | 模块 | 输出/副作用 | 失败在哪拦截 |
|---|---|---|---|---|
| 会话记忆读写 | patch `{ storyboard }` | `conversationSnapshot/aiState` + `conversationStore.commit` | 落盘 KV（防抖 300ms + pagehide flush） | `commit` 内 `reportDegrade` 可见；异步由 `withTimeout` 兜 |
| 粘贴解析 | `clipboardData` | `storyboardModel.parsePasted`（纯函数） | `{ columns, rows }` 或 `null`(非法) | 解析失败返回空/显式错误，UI toast，不落半成品 |
| AI 生成/改行 | user prompt | `useAgentChat.send` → LLM | AI 返回 JSON → 预览 | `send` 已有 error/sending 态；JSON 解析失败停在预览（透传原因） |
| 确认写回 | 预览确认 | StoryboardPanel → `setCurrentStoryboard` | 表行更新 | 写回即 commit，失败经 logger/toast 可见 |
| 单行落画布 | 定稿行 | `storyboardModel.rowToText` + 复用 `sendContentToCanvas` | 画布 textNode | `sendContentToCanvas` 内部 `callTool('create_node')` → canvasHost，失败返回 `{ok:false,error}`，不静默 |

### 2.1b 表格端到端数据流（阶段 A-D 汇总，全貌串联）

> 假设 AI 助手批量编排已删（auto+credit 闸，execute_plan 只做单 gen，见 `docs/AI助手-执行模型精简方案`）。
> 本小节把 §1.6/§1.7/§1.8 与既有的表格契约串成**一条可执行的完整数据流**，施工照此闭环。

```
【阶段 A · 建表】数据进表格（两条路）
  粘贴 ──► StoryboardPanel.onPaste ─► storyboardModel.parsePasted(TSV/HTML) ─► {columns, rows}
  AI 生成整表（对话区）──► user: 需求 + Skill → LLM 返回表 JSON(globalStyle+rows)
       ─► storyboardModel.jsonToSb ─► 渲染预览表格 ─► 确认 ─► 落表（可编辑）
       └ 前置：表格模式上下文注入（§1.7）让 AI 知道返回表结构

【阶段 B · 编辑/打磨】表格 = 可编辑真相源（会话记忆 storyboard）
  人工：点格改字/加删行/调序/改列名/globalStyle → setStoryboard(不可变) → commit → KV 落盘
  AI 改单行：点选行 → send 注入(列结构+globalStyle+选中行，§1.8) → AI 返回该行 JSON
       ─► 渲染 diff 预览 ─► 确认 ─► 替换该行文字（图/列结构不动）
  AI 取全貌（需要时）→ 调 read_storyboard（只读，无参=全表/rowId=单行，§1.8）

【阶段 C · 行→画布生成（出口）】AI 能生成某行
  选中行 → "让 AI 生成这行"（对话）→ AI 把该行内容 → execute_plan(单 gen)
       → 建 promptNode + data.images(参考图) + 触发 → credit 闸拦烧钱 → 生成
  或表格行按钮直接复用 sendContentToCanvas / 单 gen 建节点触发

【阶段 D · 结果】生成图/视频在画布节点（或预览可见）。表格**不回填结果**——生成完即见，表格只管内容组织，不做结果存储。
```

**阶段衔接要点（防断点）**：
- A 的"AI 生成整表"与 B 的"AI 改单行"共用同一套 JSON schema（顶层 globalStyle + 行数组），仅范围不同。
- B 里 AI 每次 send 只被注入"列结构+globalStyle+选中行"（§1.8 自动注入），需要看选中行以外才调 `read_storyboard`（预留只读工具）。
- C 里"生成某行"走 execute_plan **单 gen**（复用交互式生成的落地），credit 闸照拦；表格不驱动 AI 造多 gen 数组。
- 表格归属当前对话：A/B/C 读写的都是当前 `activeConversationId` 的 `storyboard`；切对话即切表。
- **结果不回填（决策，2026-09-05）**：某行生成后结果在画布节点/预览，**不存回表格该行**。表格是"内容组织/编辑"层，不是"结果存储"层；要回填须额外加"结果引用"字段，属非必要复杂度，不做。

### 2.2 统一解析/序列化：强制复用既有统一器，禁就地 parse

- **JSON 精简契约**：生成初始表 / 改单行，都走**同一套"顶层 `globalStyle` + 行数组"输出格式**，形态**照抄** `scriptBoxPrompts.ts` 的 `SCRIPT_WRITER_FORMAT`（"只返回纯 JSON 不解释不 Markdown"）——**不新造一套**。可把该输出契约抽成工作区自己的 `storyboardPrompt.ts`，但**形态/措辞对齐剧本盒**，禁止另起风格。
- **TSV/HTML 解析**：项目**没有**能保留 `\t` 的现成表格解析统一器（`clipboard.sanitizePastedText` 会压 Tab）。这是**新增能力层唯一入口**：`storyboardModel.parsePasted`，只此一处解析表格粘贴，禁止在组件里 `split('\t')` 各写各的。
- **信封剥离/存储路由**：分镜表不新增 KV 键（走会话记忆字段），故**不新增中央解析器**；行↔JSON 映射是**领域内纯函数**（`rowToObj`/`objToRow`），属 `storyboardModel`，不污染全局信封解析。
- **会话字段落盘**：沿用 `normalizeMemory`/`normalizeConversation` 体系——新增 `storyboard` 字段必须在 `conversationState.ts` 的 `emptyMemory`/`normalizeMemory`/`RawMemory` **成对登记**，反序列化入口统一归一，防历史字段/脏数据。

### 2.3 契约常量先查登记（禁裸字面量）

| 用途 | 契约登记处 | 动作 |
|---|---|---|
| 会话存储键 `agent_conversations_{agentKey}` / `agent_active_conversation_id_{agentKey}` | `contracts.ts` `STORAGE_KEYS`（已登记，backend:kv） | **复用，不改** |
| 记忆字段 `global_contract`/`artifacts`/新增 `storyboard` | `conversationState.ts` `ConversationMemory`/`RawMemory`/`normalizeMemory` | **登记**：把 `storyboard` 字段类型 + 归一加进去 |
| globalStyle 通道 | `memory.global_contract.unified_style_prompt`（`conversationAiState` 已有 `set/getCurrentGlobalContract`） | **复用**：工作区 globalStyle 映射该字段，不造第二个全局风格字段 |
| （若需）面板/子模块事件 | `contracts.ts` `EVENTS` | 仅当确有发布↔订阅才登记；**默认不新增事件**（面板内部 state 自洽即可，避免死事件） |

> 结论：本功能**几乎不新增全局契约**——存储键复用会话 key、globalStyle 复用既有字段、不新增事件。新增的仅是 `storyboard` 这一**会话记忆内聚字段**（单一数据源，顺着 `global_contract`/`artifacts` 的既有一致写清）。

### 2.4 幂等 / 去重 / 时序

- **行 id**：新增行/粘贴行必须带**稳定唯一 id**（复用 `uid('row')` 即 `conversationState.uid` / `idGen.generateId`），禁 `Date.now()` 当键、禁数组 index 当 key（增删行会串）。对齐 `normalizeConversation` 对消息补 id 的幂等写法。
- **会话跟随**：表格读当前 `activeConversationId`；切对话/新建对话时 `useEffect` 重新从该会话记忆读 `storyboard`，并**清本地预览/选中态**（防跨对话串）。
- **防抖**：点格编辑只改本地编辑态；落表走 commit（既有 `persistDebounced` 已防抖落盘）。大量 `columns/rows` 变更**不裸奔**逐字落盘。

### 2.5 避交叉 / 避黑盒

- 单向：StoryboardPanel → `storyboardModel`(纯函数) / `conversationStore`(读写会话) / `useAgentChat`(发 AI)。不反向 import 大模块，无环。
- 不静默吞错：`send` 走 async + 原样透传 error（对齐现有 `handleSend` 的 `Promise.resolve(send()).catch(logger.error)` 模式）；JSON 解析失败停在预览并透传原始原因，**禁止 `catch(()=>{})` 或抛泛化"出错了"**。
- 全异步总超时：AI 调用与任何网络都在 `useAgentChat`/`roundTrip` 侧有总超时（复用既有 `withTimeout`），表格自身不新增裸 fetch。

---

## State 3：构思测试（细化契约 + 数据流 → 可断言）

> 判定红线：断言须"实现一变必红"，禁自证式（仅验不崩 / mock 被调用无效）。

### 3.1 `storyboardModel` 纯函数单测（核心，重仓）

1. **parsePasted(TSV 文本)**：给 `"列A\t列B\n中景\t人物走入"` → 得 `{ columns:['列A','列B'], rows:[{…首个数据行}] }`；**首行为表头，其余为数据行**；空剪贴板/无表头返回 `null`/空（契约：粘贴空数据不落表）。
2. **parsePasted(HTML `<table>`) 保留 Tab 不被压平**：断言多格多行还原正确（区别于 `sanitizePastedText` 会压 `\t`——本实现必须自解析）。
3. **rowToObj / objToRow 带列名**：行转 `{列名:值}` 后，改单行发给 AI 时**每值带列名**（`列A:…`），可还原不丢字段。
4. **rowsToJson / jsonToRows（顶层 globalStyle + 行数组）**：`{ globalStyle, rows:[…] }` → AI 生成/改行共用同一 schema，roundtrip 数据不丢；`globalStyle` 空时照常序列化（不进内容、不加约束）。
5. **增删/上下移/复制行**：返回**新数组、原行引用不动**（不可变），行 id 稳定不重生成；删行后行序正确。

### 3.2 会话记忆字段（conversationState/aiState）测试

6. **normalizeMemory 含 storyboard**：给定宽松 `RawMemory`（缺 storyboard / 旧字段）→ 归一结果 `storyboard` 字段形状齐全；空则默认空表（对齐 `emptyMemory`）。
7. **per-conversation 隔离**：`setCurrentStoryboard(表A)` → `switchConversation(另一会话)` → 读回为空/旧会话的表；切回 A → 表 A 仍在。断言不跨对话串表。
8. **setCurrentStoryboard 幂等**：重复写同值不产生多余消息对象/行 id 漂移。

### 3.3 AgentPanel / 交互（组件关键路径 + 防崩）

9. **选行上下文**：点某行 + （选画布图）→ 断言传给 `send` 的 user 文本**含该行（带列名）与 globalStyle**；画布图 URL 进入参考图附件链路。
10. **确认才写回**：AI 返回 JSON → 先渲染预览（**不改正式表**）→ 未点确认时 `storyboard` 不变；点确认后才写回该行。断言"预览改数据"这一违规路径被拦。
11. **切对话重置**：`activeConversationId` 变化 → 表格/预览/选中态重置为对应会话，不残留上一会话。

### 3.4 写不出断言的即退回

凡某条链路写不出「给定输入→期望经链路→期望输出」断言，说明契约或数据流没定清 → **退回 State 1/2 补边界/补链路**，不硬施工。

---

## State 4：施工骨架（模块骨架 / 签名 / 空函数骨架 / 可变性纪律）

### 4.1 模块骨架与核心数据结构

```text
agent/storyboard/
├── storyboardModel.ts        # 100% 纯函数：数据结构 + 归一 + 解析 + 行↔JSON
├── storyboardPrompt.ts       # AI 生成/改行的 system(输出契约) + user 拼装（对齐剧本盒形态）
├── StoryboardPanel.tsx       # 左栏表格组件（渲染/编辑/粘贴/预览/确认）
```

**核心数据结构（放 `storyboardModel.ts`，一次性定对，防可变性污染）**：

```ts
/** 表格单元格（数据层是字符串；编辑态由 UI 层持有） */
export type CellValue = string

/** 一列：列名 = 粘贴首行 / AI 设计，顺序即显示顺序 */
export interface StoryColumn { id: string; label: string }

/** 一行：稳定 id + 各列值；值以「列 id → 文本」映射（列增删不毁行） */
export interface StoryRow {
  id: string                      // uid('row')，稳定唯一，禁 index/Date.now()
  values: Record<string, CellValue>  // [columnId]: text
}

/** 分镜表模型（会话记忆内聚字段） */
export interface Storyboard {
  columns: StoryColumn[]
  rows: StoryRow[]
  // 全局风格映射到会话 memory.global_contract.unified_style_prompt，不在本表重复存
}

/** 宽松/反序列化形态（供 normalizeStoryboard 归一，兼容历史/脏数据） */
export interface RawStoryboard {
  columns?: unknown[]
  rows?: unknown[]
  [key: string]: unknown
}

/** AI 生成初始表/改行的统一精简 JSON（对齐剧本盒「顶层 globalStyle + 行数组」） */
export interface StoryboardJson {
  globalStyle?: string
  rows: Array<Record<string, unknown>>  // 每行：{ 列名: 值 }
}
```

> **可变性纪律**：`Storyboard` 数据层**不可变**——任何增删改行/列都返回**新对象/新数组**（沿用 `scriptBoxPrompts.patchShots` 的不可变更新范式），UI 侧一律 `setStoryboard(next)`，禁原地 `mutate`。共享表不跨组件原地改。

### 4.2 函数签名清单

**storyboardModel.ts（纯函数，全部 `export`，可单测）**
```ts
export function emptyStoryboard(): Storyboard
export function normalizeStoryboard(raw: unknown): Storyboard
export function parsePasted(rawText: string, htmlText?: string): Storyboard | null   // TSV/HTML → 首行表头
export function addRow(sb: Storyboard): Storyboard                                   // 增行
export function deleteRow(sb: Storyboard, rowId: string): Storyboard                 // 删行
export function moveRow(sb: Storyboard, rowId: string, dir: 'up'|'down'): Storyboard // 上下移
export function duplicateRow(sb: Storyboard, rowId: string): Storyboard              // 复制
export function setCell(sb: Storyboard, rowId: string, colId: string, text: string): Storyboard
export function rowToObj(sb: Storyboard, row: StoryRow): Record<string, string>      // 行→{列名:值}
export function rowToText(sb: Storyboard, row: StoryRow): string                     // 行→文本（落画布/发 AI 拼接用）
export function sbToJson(sb: Storyboard, globalStyle: string): StoryboardJson        // →顶层globalStyle+行数组
export function jsonToSb(json: StoryboardJson): { globalStyle: string; sb: Storyboard }  // JSON→可编辑表
```

**storyboardPrompt.ts（纯函数，对齐剧本盒形态，勿新造风格）**
```ts
export const STORYBOARD_FORMAT: string       // 输出契约：只返回纯 JSON、顶层 globalStyle+rows（照抄 SCRIPT_WRITER_FORMAT 措辞/形态）
export const STORYBOARD_SYSTEM: string       // 与输出结构分两段的"整体风格"系统提示词（globalStyle 注入参考）
export function buildGenerateUser(idea: string): string                        // 描述需求 → 生成初始表
export function buildRefineRowUser(rowText: string, globalStyle: string, instruction: string): string // 改单行
```

**StoryboardPanel.tsx（UI 薄壳 + 关键 props/回调）**
```tsx
export default function StoryboardPanel(props: {
  // 会话快照（含本对话 storyboard）经 AgentPanel 传入；发 AI / 落画布回调也由上层注入，面板不碰 store
  storyboard: Storyboard
  activeConversationId: string
  onCommit: (patch: { storyboard: Storyboard }) => void      // → setCurrentStoryboard(等价 setCurrentSnapshot({…}))
  onRequestAi: (opts: { action: 'generate'|'refine'; text: string; globalStyle: string; selectedRowId?: string; refImages?: Array<{url:string}> }) => void
  onSendRowToCanvas: (row: StoryRow) => void   // 内部调 rowToText(row) 后复用 sendContentToCanvas
})
```

**会话记忆新增读写（并入 conversationStore 层，对齐 `setCurrentGlobalContract` 范式）**
```ts
// conversationSnapshot.ts / conversationAiState.ts（新增）
export function getCurrentStoryboard(): Storyboard      // 读当前会话记忆 storyboard（归一兜底空表）
export function setCurrentStoryboard(sb: Storyboard): void // 写当前会话记忆 storyboard + commit(自动落盘)
```
> 归属裁决（State1 复用的落地）：`globalStyle` **不再另存**，复用既有 `setCurrentGlobalContract`/`getCurrentGlobalContract`（写 `global_contract.unified_style_prompt`）；仅 `storyboard`（columns/rows）作为新记忆字段，`emptyMemory`/`normalizeMemory`/`RawMemory` 成对登记。

**useAgentChat / AgentPanel 改造点**
```tsx
// useAgentChat.ts：无需新增——落画布复用既有 sendContentToCanvas（L946）：
//   sendContentToCanvas = (content) => callTool('create_node', { type: 'textNode', text })  // 已是 textNode 唯一入口
// AgentPanel.tsx：左侧插入 <StoryboardPanel>（与对话栏并排/左表右对话）
//   onSendRowToCanvas={row => sendContentToCanvas(rowToText(storyboard, row))}
```

### 4.2b StoryboardPanel UI 布局规格（2026-09-05 定稿）

> **UI 布局**：表格嵌 AI 助手面板，样式复用 AgentPanel 既有 Tailwind 体系（`bg-canvas/border-edge-faint/text-primary/text-secondary/surface-hover` 等），视觉与 AI 助手一致，不新造主题。

**① 左右分栏 + 可拖拽分隔条（宽度）**
- AgentPanel 内容区横向分：`[表格区 StoryboardPanel] | [拖拽分隔条] | [对话区 AgentMessage 列表]`。
- 分隔条可拖拽调左右宽度（照抄 AgentPanel 面板宽度拖拽的 `startDrag/onMove/onUp` 现成模式）。
- 表格区宽度存 localStorage（对齐 `agent_panel_width`：新键如 `agent_split_width`），记住偏好，刷新恢复。
- 表格区宽 **min/max 约束**（如 min 260px、max 不把对话挤没，预留对话可视宽度）。对话区 flex 撑剩余。

**② 高度 + 滚动（表格长时能滑）**
- 表格区与对话区**各自独立垂直滚动**（都在面板内容区高度内，互不影响）。
- 表格区内部**表头固定 + 表体滚动**：表头行常驻顶部，表体 `overflow-y:auto`，滚动时列名不跟着滚走（长表格标准体验）。
- 单元格长内容允许**换行**（多行撑高），不强制单行截断。
- 复用 AgentPanel 内容区高度约束（扣除标题/工具栏后，表格区占满剩余高度）。

**③ 表格内部结构（骨架）**
> 表格在 AI 助手面板内，**无独立标题栏**（面板自身有标题栏）。表格区最顶 = globalStyle 全局条。
```
StoryboardPanel（表格区）
├── 【全局条 globalStyle】(表格最顶一行、宽与表格一致)  ← 表外独立行，非 rows 内容行
│    全局风格显示 + 点开编辑；数据存 table.globalStyle（独立属性，不进 rows）
├── 工具栏（粘贴表格 / 用 AI 生成表入口）
├── 表头行（列名，固定不滚）
└── 表体（overflow-y:auto）
    └── 每行：单元格(可点格编辑) + 行操作(增/删/上移/下移/复制) + 选中该行交给 AI + 该行落画布
```
> **globalStyle 归属（决策 2026-09-05）**：存 `table.globalStyle`（**独立属性**，与 columns/rows 平级），**不占 rows、不进内容行**。UI 呈现在**标题栏下方一行"全局条"**（宽与表格一致、表外独立、非表内行），避免"全局整行"破坏内容行可增删/排序的语义；也避免塞标题栏显得挤。AI 生成/改行时随上下文带上它（§1.8）。

**④ 交互状态（承接 §1.7/§1.8）**
- 展开表格 = `tableOpen` 态；选中行 = `selectedRowId`；预览 = `preview`（AI 返回 JSON 渲染的可视化表，确认才落正式表）。
- 选中行变化 → 同步给 AgentPanel 供 AI 上下文注入（§1.8 自动注入"列结构+globalStyle+选中行"）。

### 4.3 空函数骨架（带"内部即将实现步骤"注释，不写具体逻辑）

> 此处仅给签名 + 注释内步骤清单。**填完 State1-3 且骨架画通后**，才严格按签名/步骤填充（见 §4.4）。

```ts
// storyboardModel.ts 骨架
export function emptyStoryboard(): Storyboard {
  // 步骤：返回 { columns:[], rows:[] }
  throw new Error('TODO: 依 §4.2 签名与 §State1 契约填充')
}
export function normalizeStoryboard(raw: unknown): Storyboard {
  // 步骤：宽松形态逐字段校验；columns 归一 [{id,label}]（补 uid）；rows 归一（无 id 补 uid、values 收敛为 string map）；缺省返回 emptyStoryboard
  throw new Error('TODO')
}
export function parsePasted(rawText: string, htmlText?: string): Storyboard | null {
  // 步骤：① html 有 <table> 优先解析 <tr>/<td>；② 否则按行 \n 再按 \t 切分；③ 首行为表头→columns；④ 其余行为数据行→rows（逐格字符串）
  // ⚠️ 禁止复用 clipboard.sanitizePastedText（会压 \t）；失败/无表头返回 null
  throw new Error('TODO')
}
// … addRow / deleteRow / moveRow / duplicateRow / setCell：均「读旧 → 返回新对象/数组」，绝不 mutate 原 sb
export function rowToObj(sb: Storyboard, row: StoryRow): Record<string, string> {
  // 步骤：columns 顺序遍历 → { [col.label]: row.values[col.id] ?? '' }（发给 AI 带列名）
  throw new Error('TODO')
}
export function rowToText(sb: Storyboard, row: StoryRow): string {
  // 步骤：行 → 一段可读文本（如每列一行「列名：值」）供「发送到画布」复用 sendContentToCanvas
  throw new Error('TODO')
}
export function sbToJson(sb: Storyboard, globalStyle: string): StoryboardJson {
  // 步骤：顶层 { globalStyle, rows: rows.map(rowToObj) }（对齐剧本盒 globalStyle+shots 形态）
  throw new Error('TODO')
}
export function jsonToSb(json: StoryboardJson): { globalStyle: string; sb: Storyboard } {
  // 步骤：读 rows[0] 的键当列名（AI 设计的列）；逐行值写回；globalStyle 单独返回
  throw new Error('TODO')
}
```

```ts
// storyboardPrompt.ts 骨架
export const STORYBOARD_FORMAT = `
// 步骤：输出契约文本——严格顶层 { globalStyle, rows:[{列名:值}…] }，只返回纯 JSON 不解释不 Markdown
//   （形态/措辞照抄 scriptBoxPrompts.SCRIPT_WRITER_FORMAT，勿新造风格）
`.trim()
export const STORYBOARD_SYSTEM = ''
export function buildGenerateUser(idea: string): string {
  // 步骤：包一层需求描述，说明要 AI 设计列 + 填初始内容（表头/每行/全局要求都由 AI 按描述构思）
  throw new Error('TODO')
}
export function buildRefineRowUser(rowText: string, globalStyle: string, instruction: string): string {
  // 步骤：给「当前该行(带列名) + globalStyle + 用户修改意见」；globalStyle 空就自然传"未设置"，不加约束
  throw new Error('TODO')
}
```

```tsx
// StoryboardPanel.tsx 骨架（节选逻辑回调）
// - 粘贴：onPaste → e.preventDefault → parsePasted(e.clipboardData) → onCommit({ storyboard: sb })（空则 toast，不落）
// - 选行：点行设本地 selectedRowId → 与画布选中图一并经 onRequestAi({ action:'refine', … })
// - AI 生成：输入想法 → onRequestAi({ action:'generate' }) → 拿到 JSON → renderPreview(json)（本地 preview state，不改正式表）
// - 确认写回：预览确认 → sb=jsonToSb(json)；jsonToSb 产出正式 sb → onCommit({ storyboard: sb }) → 清 preview
// - 单元格编辑：本地编辑缓冲 + blur/enter 才 setCell → onCommit（防抖，不逐键落盘）
// - 发送到画布：onSendRowToCanvas(row) → rowToText(sb,row) → 复用 sendContentToCanvas(text)（已是 create_node→canvasHost 唯一入口）
```

### 4.4 防返工自检（每步过一遍）

- 代码是否偷偷引入 State2 未声明的依赖/状态？——分镜表数据只从当前会话记忆来，不新造本地 store。
- 是否偏离已定链路？——发 AI 只经 `useAgentChat.send`；落画布只经 `canvasHost`；落盘只经 `commit`/`setCurrentStoryboard`。
- 若再加一个"类似表格工作区"功能：本架构是**在 `storyboardModel` 加几个纯函数 + 复用会话记忆字段**，而非拆掉重写——防返工锚点达标。

### 4.5 若填充发现兜不住

发现模块拆分要变 / 函数签名兜不住需求 → **立即退回 State 1/2/3 重定**，不得硬填。

---

## 施工顺序建议（照此推进，每步可验证）

1. `storyboardModel.ts` 纯函数 + 其单测（State3 §3.1 全绿）——先定数据结构与解析/映射。
2. `conversationState.ts` 记忆字段登记（`storyboard`） + `get/setCurrentStoryboard` + State3 §3.2 测试。
3. `storyboardPrompt.ts`（生成/改行输出契约 + user 拼装）。
4. `StoryboardPanel.tsx` + `AgentPanel.tsx` 左栏插入 + 接线（选行上下文/确认写回/切对话重置），补 State3 §3.3。
5. 落画布按钮接线：`StoryboardPanel` 用 `rowToText` + 复用既有 `sendContentToCanvas`（无需新增出口）。

> **注意**：State1-3 若需再确认某文件行号/符号，按 `docs/分镜协作-AI助手内嵌工作区-2026-09-05.md` §四「实现参考」清单读码定位（行号会漂移，按语义找）。
