# 多 Provider 接入 · 可审计完整规格

> **用途**：交给他人审计 / 评审。所有字段、枚举、逻辑均以 Infinite-Canvas 真实源码为准，**标注权威行号**，可逐条核对。
> **范围**：逻辑照抄 `/Users/kevin/Documents/画布/Infinite-Canvas`（main.py / static/js/api-settings.js / static/api-settings.html / data/api_providers.json），UI 配色用我们原型，第一个 provider = 我们 9004（Lovart）。
> **不碰**：1mao 官方混淆代码、`src/bundle/`、官方 `dist/`。
>
> **核对基准（审计必读）——权威源文件完整绝对路径，请逐一 `read_file` 核实**：
> 1. 后端主文件（17582 行，绝大部分断言在此）：`/Users/kevin/Documents/画布/Infinite-Canvas/main.py`
> 2. 后端字段契约类 `ApiProviderPayload`（main.py:2595-2623）、`TestConnectionPayload`（main.py:12441）
> 3. 前端配置逻辑（200KB）：`/Users/kevin/Documents/画布/Infinite-Canvas/static/js/api-settings.js`
> 4. 前端配置 HTML 结构：`/Users/kevin/Documents/画布/Infinite-Canvas/static/api-settings.html`
> 5. 真实注册表数据（3311 行）：`/Users/kevin/Documents/画布/Infinite-Canvas/data/api_providers.json`
> 6. 顶层说明：`/Users/kevin/Documents/画布/Infinite-Canvas/README.md`、`/Users/kevin/Documents/画布/Infinite-Canvas/运行说明.txt`
>
> **本文所有行号以该 main.py 当前版本为准。** 若审计方拿到不同版本，行号可能漂移，请以函数名/常量为准核对（本文每个条目都给了函数名或常量名，行号是辅助定位）。
> 所有断言均可直接 `read_file` / `grep` 上述 1-5 号文件核实，不依赖任何二手转述。

---

## A. 数据契约（字段定稿，可审计）

### A.1 provider 对象字段（主表）
来源：`ApiProviderPayload`(main.py:2595-2623) + `normalize_provider` 输出(main.py:1290-1311) + `api_providers.json`。

| # | 字段 | 类型 | 默认 | 含义 | 存放 | 出处 |
|---|---|---|---|---|---|---|
| 1 | `id` | str | "" | 唯一标识，路由键，`^[a-zA-Z0-9_-]{2,40}$` | json | 1255-1257 |
| 2 | `name` | str | id | 展示名（空白归一≤60） | json | 1258 |
| 3 | `base_url` | str | "" | API 基地址，必须 http(s) 开头；CLI 留空 | json | 1259-1261 |
| 4 | `protocol` | str | "openai" | 协议身份（8 选 1），非法回退 openai | json | 1262-1264 |
| 5 | `image_request_mode` | str | "openai" | 图像请求形态（4 选 1），非法回退 openai | json | 1265 |
| 6 | `image_generation_endpoint` | str | "" | 覆盖文生图端口 | json | 1266 |
| 7 | `image_edit_endpoint` | str | "" | 覆盖图生图端口 | json | 1267 |
| 8 | `enabled` | bool | true | 启用开关 | json | 1298 |
| 9 | `primary` | bool | false | 首选标记（**全表最多 1 个 true**） | json | 1299 |
| 10 | `image_models` | List[str] | [] | 文生图模型 | json | 1300 |
| 11 | `chat_models` | List[str] | [] | 聊天模型 | json | 1301 |
| 12 | `video_models` | List[str] | [] | 视频模型 | json | 1302 |
| 13 | `model_names` | Dict[str,str] | {} | 单模型显示名覆盖 | json | 1303 |
| 14 | `model_protocols` | Dict[str,str] | {} | 单模型协议覆盖（仅 openai/gemini） | json | 1304 |
| 15 | `ms_loras` | List[Dict] | [] | ModelScope Lora（专属） | json | 1305 |
| 16 | `ms_defaults_version` | int | 0 | ModelScope 默认版本（专属） | json | 1306 |
| 17 | `rh_apps` | List[Dict] | [] | RunningHub AI 应用（专属） | json | 1307 |
| 18 | `rh_workflows` | List[Dict] | [] | RunningHub 工作流（专属） | json | 1308 |
| 19 | `volcengine_project_name` | str | "default" | 火山项目名（专属） | json | 1309 |
| 20 | `volcengine_region` | str | "cn-beijing" | 火山区域（专属） | json | 1310 |

### A.2 key 类（只进 env，绝不落 json）
| 字段 | 含义 | env 名 | 出处 |
|---|---|---|---|
| `api_key` | 主 key | `provider_key_env(id)`：comfly→`COMFLY_API_KEY`；modelscope→`MODELSCOPE_API_KEY`；runninghub→`RUNNINGHUB_API_KEY`；volcengine→`ARK_API_KEY`；自定义→`API_PROVIDER_{ID大写}_KEY` | 702-711 |
| `wallet_api_key` | RH 钱包 key | `RUNNINGHUB_WALLET_API_KEY` | 713-714 |
| `volcengine_access_key_id` | 火山 AK | `VOLCENGINE_ACCESS_KEY_ID` | 716-717 |
| `volcengine_secret_access_key` | 火山 SK | `VOLCENGINE_SECRET_ACCESS_KEY` | 719-720 |

key 读取优先级（`provider_env_key_value`）：`os.getenv` → 回退 `read_api_env_value`(722，读 `API_ENV_FILE` 文本) → modelscope 专属全局。(739-747)
掩码（`mask_secret`）：尾 4 位 + `••••••••` 前缀。(767-771)

### A.3 脱敏只读字段（GET 返回，key 不回明文，`public_provider`）
来源：`public_provider`(1332-1365)：
`has_key` / `key_preview` / `key_env` + （runninghub 专属）`has_wallet_key`/`wallet_key_preview`/`wallet_key_env` + （volcengine 专属）`has_volcengine_access_key`/`has_volcengine_secret_key` 及各自 preview/env。

### A.4 清 key 标志位（PUT payload，防回显）
来源：`ApiProviderPayload`(2618-2623)：`clear_key` / `clear_wallet_key` / `clear_volcengine_access_key_id` / `clear_volcengine_secret_access_key`（bool，true 时清对应 env 不重设）。保存逻辑：12476-12496。

### A.5 两个正交枚举（不可改）
- `SUPPORTED_PROVIDER_PROTOCOLS`(317) = `{openai, apimart, gemini, gemini-cli, volcengine, runninghub, jimeng, codex}`
- `SUPPORTED_IMAGE_REQUEST_MODES`(318) = `{openai, openai-json, openai-video-proxy, openai-responses}`
- `PER_MODEL_PROTOCOL_OPTIONS`(4290) = `{openai, gemini}`
- `FIXED_PROTOCOL_PROVIDER_IDS`(4292) = `{modelscope, volcengine, jimeng, runninghub}`

### A.6 协议锁死规则（normalize 时强制）
来源：`normalize_provider`(1270-1286) + `locked_recommended_provider_rule`(1197-1209)：
- `volcengine` id → protocol=volcengine，base 补 `ark.cn-beijing.volces.com/api/v3`，region/project 补默认
- `jimeng` → protocol=jimeng，base 置空
- `codex`/`gemini-cli` → base 置空
- `runninghub` id → protocol=runninghub，base 补 `www.runninghub.cn`
- `LOCKED_RECOMMENDED_PROVIDER_RULES`(1180-1195)：按 id/name/base_url 域名匹配已知站：
  - exellome → `apimart` + `openai-video-proxy`
  - fhl → `openai` + `openai-responses`

### A.7 我们的默认 seed（替换 IC 的 modelscope/runninghub/volcengine）
```json
[{"id":"lovart","name":"Lovart(自托管)","base_url":"http://127.0.0.1:9004","protocol":"apimart",
  "image_request_mode":"openai","image_generation_endpoint":"","image_edit_endpoint":"",
  "enabled":true,"primary":true,
  "image_models":["gpt-image-2-low","gpt-image-2-medium","gpt-image-2","nano-bn-pro","nano-bn-2"],
  "chat_models":["lovart-chat"],"video_models":["seedance-2.0-fast","seedance-2","kling-v3-omni"],
  "model_names":{},"model_protocols":{},"ms_loras":[],"ms_defaults_version":0,
  "rh_apps":[],"rh_workflows":[],"volcengine_project_name":"","volcengine_region":""}]
```

---

## B. 注册表读写（可审计）

| 函数 | 行号 | 行为 |
|---|---|---|
| `normalize_provider(item)` | 1254 | 清洗+校验+锁协议+归一专属字段，返回 A.1 标准结构 |
| `load_api_providers()` | 1313 | 文件不存在→默认；存在→逐项 normalize→merge；解析失败→回退默认 |
| `save_api_providers(providers)` | 1326 | `GLOBAL_CONFIG_LOCK` 锁 + 写 json（ensure_ascii=False, indent=2） |
| `default_api_providers()` | 784 | IC 默认 3 平台（modelscope/runninghub/volcengine）→ **我们改成 9004 Lovart** |
| `merge_default_api_providers(providers, inject_missing=True)` | 841 | 合并默认 + 用户；ms 灰度/rh 合并/volc 迁移/jimeng·codex 规范化 |
| `get_primary_provider_id(providers=None)` | 1370 | 优先 primary=True→次非 modelscope→再第一个 |
| `get_api_provider(provider_id="comfly")` | 1381 | 空/不存在回退 primary；禁用→400 |
| `get_api_provider_exact(provider_id)` | 1394 | 严格取，未保存→400 |
| `public_provider(provider)` | 1332 | 脱敏 key 的只读视图 |
| `public_api_providers()` | 1367 | 全列表脱敏视图 |

---

## C. 协议分派（可审计）

| 函数 | 行号 | 行为 |
|---|---|---|
| `provider_protocol(provider)` | 4286 | `provider.protocol or "openai"` |
| `effective_protocol(provider, model)` | 4316 | 基础→FIXED 直接返→`model_protocols[model]`（仅 openai/gemini） |
| `detect_image_request_mode(base_url, models)` | 4342 | agnes→openai-json（base_url 或模型名前缀） |
| `effective_image_request_mode(provider, model)` | 4351 | 先 detect 再 normalize |
| `is_apimart_provider` | 4329 | protocol==apimart 或 base 含 `apimart.ai` |
| `is_fhl_provider` | 4333 | 域名 fhl.mom 或 name fhl |
| `is_gemini/volcengine/runninghub/codex/gemini_cli/jimeng_provider` | 4357-4373 | 按 protocol/id 判定 |
| `generate_ai_image(prompt,size,quality,model,reference_images,provider_id)` | 10482 | 总入口：get_api_provider → 按 protocol 分流到各 generate_* |

`generate_ai_image` 分派结构（10482-10515）：
```
provider = get_api_provider(provider_id)
if id==modelscope → generate_modelscope_provider_image
if is_codex → generate_codex_provider_image          （CLI）
if is_gemini_cli → generate_gemini_cli_provider_image（CLI）
if is_jimeng → generate_jimeng_provider_image        （CLI）
if is_runninghub → generate_runninghub_provider_image（工作流）
if effective_protocol=="gemini" → generate_gemini_provider_image
if is_volcengine → generate_volcengine_provider_image
否则 → openai 兼容 HTTP 分支（按 effective_image_request_mode 分 6 支）
```

---

## D. 各协议 client 真实请求构造（可审计）

### D.1 openai 兼容站 · 6 种请求分支（generate_ai_image 内 10528-10693）

| 分支 | 触发 | 请求构造 | 参考图 | 出处 |
|---|---|---|---|---|
| openai-video-proxy | request_mode=openai-video-proxy | `POST {base}/videos`，body `{model,prompt,aspect_ratio}` | 本地 multipart `images`，网络 `body.images[]` | 10528-10574 |
| openai-responses | request_mode=openai-responses | `POST {endpoint}/v1/responses`，body `{model, input:[{role:user,content:[input_text,input_image]}], tools:[{type:image_generation,action,size,quality}], tool_choice:{type:image_generation}}` | `input_image.image_url` | 10575-10597 |
| openai-json | request_mode=openai-json | `POST /images/generations`，body `{model,prompt,size,extra_body:{response_format:url}}`，**绝不走 edits** | `extra_body.image:[base64≤1536]` | 10598-10606 |
| apimart | is_apimart_provider | `POST /images/generations`，body `{model,prompt,n:1,size,resolution,official_fallback:false}` | `image_urls:[base64≤1536]` | 10607-10621 |
| gpt-image-2 文生图 | is_gpt_image_2_model 且无 refs | `POST /images/generations`，body `{model,prompt,size,quality}`；失败+unsupported→回退 edits | — | 10622-10628 |
| 标准图生图/编辑 | 有 image_refs | `POST /images/edits` multipart（image+mask）；失败→非 GPT-2 回退 generations+`image:[]` | multipart | 10629-10684 |

**通用收尾**(10696-10739)：`raise_for_status`→`raw=response.json()`→`extract_image` 同步出；无图→`extract_task_id`→`wait_for_image_task` 轮询。
**特例**：fhl keepalive 明确报错(10710)；GPT-2 编辑失败不自动重试(10665)；responses 无图→`responses_output_text_image` 兜底(10722)。

### D.2 gemini（generate_gemini_provider_image，9268-9287）
```
endpoint = {base}/v1beta/models/{model}:generateContent
body = {contents:[{role:user,parts:[{text}] + inlineData}], generationConfig:{responseModalities:["TEXT","IMAGE"], imageConfig}}
headers = x-goog-api-key
→ extract_image(raw)   # candidates[].content.parts[].inlineData
```
辅助函数：
- `gemini_model_name(model)`(9236)：去 `models/` 前缀，默认 `gemini-3-pro-image-preview`
- `gemini_endpoint_url(provider, model)`(9240)：`{base}/v1beta/models/{urlquote(model)}:generateContent`
- `gemini_image_config(size)`(9244)：像素→`parse_size_pair`→`apimart_size_resolution`→`{aspectRatio,imageSize}`；`1K/2K/4K`→`{aspectRatio:"1:1",imageSize}`；`比例`→`{aspectRatio,imageSize:"1K"}`；默认 `{1:1,2K}`
- `gemini_reference_part(ref)`(9256)：base64→`inlineData`；http→`fileData`

### D.3 volcengine（generate_volcengine_provider_image，9298-9315）
```
endpoint = {base}/api/v3/images/generations
body = {model, prompt, size, response_format:"url"}; 参考图 → body["image"]=[base64≤1536]
→ extract_image(raw)
```

### D.4 runninghub（generate_runninghub_provider_image，10329）
```
异步工作流：POST /openapi/v2/workflows/{id}/run 或 /ai-app/{appId} → taskId
→ wait_for_runninghub_openapi_task 轮询
→ runninghub_store_remote_output 落本地资产库
鉴权：Bearer + Host: www.runninghub.cn（free_key 或 wallet_key，9330-9339）
```

### D.5 本地 CLI（run_codex_cli，4402-4456 / generate_codex_provider_image，4938-4950）
```
run_codex_cli: codex exec --cd BASE_DIR --sandbox workspace-write --skip-git-repo-check [--model] [--image path]* [--output-last-message tmp] -   （stdin 喂 prompt）
  超时→504；非0退出→502；结果从 --output-last-message 落盘文件读回
generate_codex_provider_image: 先 codex_reference_paths 物化参考图 → generate_codex_provider_image_via_gpt_image_2_skill → 找不到 skill→400"禁 $imagegen 回退"
```
CLI 状态探测：`codex_status`(12159) / `gemini_cli_status`(12228) / `jimeng_status`(12295) —— spawn `--version` 等检测安装 + 登录态。

---

## E. 产物归一 + 素材吞入（可审计）

### E.1 产物归一
- `extract_image(data)`(3882)：`extract_images` 取第一张；否则 Gemini inlineData；都无→502"无法识别生图接口返回格式"
- `extract_images(data)`(3777)：兼容 Gemini candidates/inlineData、`data.result.images[].url[]`（apimart）、`image_generation_call`，去重
- `extract_image_flexible(value)`(3745)：递归找图，三类 key hints（base64/output/container），depth≤8
- `image_payload_from_string`(3727)：data:image/ 前缀→b64；URL→url；base64→b64
- **key hints 键名**（3684-3691）：
  - `IMAGE_OUTPUT_KEY_HINTS` = `("url","image_url","imageUrl","image","output_url","outputUrl","result_url","resultUrl","download_url","downloadUrl","asset_url","assetUrl")`
  - `IMAGE_CONTAINER_KEY_HINTS` = `("images","image","output","outputs","result","results","data","items","files")`
  - `IMAGE_BASE64_KEY_HINTS` = `("b64_json","base64","image_base64","imageBase64")`
- **responses 兜底** `responses_output_text_image(raw)`(4071)：部分 RS 中转把图以 `output_text` markdown `![...](url)` 或裸图 URL 返回，递归捞 `output/content/text/output_text/message/response` 再正则提取

### E.2 素材吞入
- `reference_to_data_url(ref, max_size=1536)`(7528)：本地文件→base64（超长边 thumbnail），URL 原样
- `output_file_from_url(url)`(6246)：`/assets/`/`/output/`/`/api/storage-files/` 解析回本地路径
- `upload_image_for_apimart(client, provider, ref_url)`(8300)：本地/data URL→上传 `/v1/uploads/images`→公网 URL；失败返回 `ERR:` 前缀
- `responses_input_image_url(ref)`(4004)：本地优先传图床→回退内联 base64；本机/内网 URL 不直接透传
- `openai_video_proxy_public_reference_url`(8064) / `openai_video_proxy_local_image_path`(8100)：video-proxy 分支的参考图处理
- `bearer_auth_value`(780)：`Bearer {token}`（去 scheme）

---

## F. 异步轮询 + 状态词（可审计）

### F.1 状态词表（6113-6114）
```
SUCCESS = {SUCCESS, SUCCESSFUL, SUCCEED, SUCCEEDED, COMPLETED, COMPLETE, DONE, FINISHED, OK, READY}
FAILED  = {FAILURE, FAILED, FAIL, ERROR, ERRORED, CANCELED, CANCELLED, TIMEOUT, REJECTED, EXPIRED}
```

### F.2 task 相关
- `extract_task_id(data)`(3940)：task_id/taskId/submit_id/video_id/videoId/id(以task开头)→钻 data 信封
- `image_task_url_for_provider(provider, task_id)`(6116)：openai-video-proxy→`/videos/{id}`；apimart→`/tasks/{id}`；否则→`/images/tasks/{id}`
- `image_task_status(payload)`(6131)：`data.status or task_status` → UPPER
- `image_task_fail_reason(payload)`(6135)：fail_reason/message/error.message 取失败原因
- `fetch_image_task_payload(client, task_id, provider)`(6161)：GET task_url，transient 重试 3 次
- `wait_for_image_task(client, task_id, provider)`(6173)：循环到 deadline；状态词命中 SUCCESS 返回 / FAILED 抛 502 / 无状态但有图也返回 / 超时 504
- `httpx_request_with_transient_retries`(6140)：502/503/504/520/522/524 + 连接类重试

---

## G. 管理 API（可审计，改前缀 /api→/v1 到我们网关）

| 路由 | 行号 | 行为 |
|---|---|---|
| `GET /api/providers` | 12457 | `{providers: public_api_providers()}` |
| `PUT /api/providers` | 12461 | `List[ApiProviderPayload]`：逐项 normalize→id 重复 400→至少保留 1→primary 唯一化(取最后)→写 key env→`update_env_values`+`reload_env_globals`→返回脱敏 |
| `POST /api/providers/test-connection` | 12768 | 协议推断→CLI 直接返状态→runninghub 工作流→其余 GET `/v1/models`（301/HTML/≥400 处理 + 方舟自愈）→分类模型 |
| `POST /api/providers/probe-async` | 12881 | 假 task_id 探 `/v1/tasks/*`：400+invalid task id=端点存在；401/403=key 无效；404=非 apimart；方舟双路探测 |
| `POST /api/providers/fetch-models` | 13157 | 拉上游模型分类 |
| `GET /api/providers/{id}/fetch-models` | 13164 | 按已存 id 拉模型 |

### G.1 primary 唯一化（PUT 内，12511-12515）
```
primary_indices = [i for i,flag in enumerate(raw_primary_flags) if flag]
if primary_indices:
    winner = primary_indices[-1]
    for i,p in enumerate(providers): p["primary"] = (i==winner)
```

### G.2 key 保存 + 热更新（PUT 内，12475-12519）
- `provider_key_env` → `update_env_values({key_env: api_key})` 写 env 文件 → `reload_env_globals()` 同步模块全局（免重启）
- `update_env_values`(1430)：读 `API_ENV_FILE` 文本，命中 key 更新，未命中追加，`env_quote`(1424) 处理空白/引号
- runninghub 走 `wallet_env`；volcengine 走 `ak_env`/`sk_env`

### G.3 test-connection 完整链路（12768-12879）
```
protocol = protocol_from_payload(payload)   # 12548：provider_id/base_url 嗅探
codex/gemini-cli/jimeng → 本机 CLI 状态（不 HTTP）
runninghub → runninghub_models_payload
其余：
  url = upstream_models_url(base_url, protocol)  # gemini:/v1beta/models 方舟:/api/v3/models openai:/v1/models
  GET url
    301/302/307/308 → "请填 API Base URL，勿填网页地址"
    HTML → "返回网页 HTML"
    ≥400 → 方舟→probe_volcengine_auto_detect；openai→probe_volcengine_auto_detect（自动切方舟）
    <300 → parse_upstream_models 分类 + agnes defaults + locked rules
  返回 {ok,status,model_count,image_models,chat_models,video_models,all,image_request_mode}
httpx.HTTPError → 方舟再 probe 自愈；否则 ok:false
```

### G.4 probe_volcengine_auto_detect(12698) 双路自愈
```
probe_volcengine_task_endpoint(12618): 探 {base}/api/v3/.../tasks/healthcheck_probe_do_not_submit → <500 判"方舟任务协议"
否则 probe_openai_compat_bearer_endpoint(12643): POST {root}/v1/chat/completions 空 messages → <500 判"Bearer 兼容"
否则 → 报详细 raw
```

### G.5 模型拉取分类
- `fetch_models_from_upstream`(13027)：CLI 返本机状态；runninghub 工作流；其余 GET models 端点 + 缺失探测自愈
- `classify_upstream_model(mid)`(12719)：video 关键词（veo/sora/wan2/wanx/doubao-seedance/kling/hailuo/video/t2v-/i2v-/s2v）→video；image 关键词（banana/image/dalle/imagen/flux/stable/sdxl/midjourney/nano-banana/seedream/...）→image；否则 chat
- `parse_upstream_models(raw, protocol)`(12729)：兼容 data/models/list 三字段，gemini 去 models/ 前缀，去重排序分类
- `upstream_models_url`(12580) / `upstream_model_headers`(12589)

---

## H. 产物落盘（不丢图，可审计）

- `output_url_for(filename, category)`(6206)：本地→`/assets/{rel}` 或 `/api/storage-files/{kind}/{rel}`
- `save_remote_video_to_output(url, prefix, category)`(8839)：下载远程视频落盘，校验 content-type 非 html/json，按 ext/ct 定扩展名，失败删临时文件降级返回原 url（不抛 500 阻断）
- `output_path_for`(6218) / `output_storage`(6203)
- > 我们 localTool 已有 `saveRemoteUrl`（files.ts，`[download]` 日志 + 降级）可复用/对齐。

---

## I. chat 多 provider 分派（可审计）

`resolve_chat_provider(provider, model, ms_model)`(3522-3551)：
```
provider==modelscope → 专用 base/key/model
否则 get_api_provider(provider)：
  codex/gemini-cli → 400"用本机登录态"
  protocol=gemini → base 补 /v1beta
  protocol=volcengine → base 补 /api/v3
  protocol=runninghub → base=RUNNINGHUB_LLM_BASE_URL
  否则 base 补 /v1
  hdrs = api_headers(provider, model)
```

---

## J. 前端（照抄成 ProviderSettings.jsx，配色用原型）

### J.1 api-settings.js 结构（static/js/api-settings.js，200KB）
- 全局 DOM 引用：providerList/editorTitle/nameInput/idInput/baseInput/protocolInput/imageRequestModeInput/keyInput + CLI 面板 + 方舟 + RH
- `RECOMMENDED_APIS`(155)：预置推荐站（id/name/category/base_url/protocol/image_request_mode/register_url/tagKeys/advantages/models）
- `CLI_PROTOCOLS`(95)=`{jimeng,codex,gemini-cli}`；`API_PROTOCOLS`(96)
- `applyCliProtocolDefaults`(130)：选 CLI 协议自动填默认模型
- `ONBOARDING_GUIDES`(102)：引导注册链接

### J.2 api-settings.html 表单结构（static/api-settings.html）
- providerList(40)：左侧卡片列表
- settingsContent(54)：右侧编辑器
  - 基本信息块：nameInput(78)/idInput(hidden)/baseInput(84)
  - API Key 块：keyInput(104)+saveKeyOnly/clearKeyOnly；RH 双 key（rhFreeKeyInput/rhWalletKeyInput）；方舟（volcAkInput/volcSkInput/volcProjectInput/volcRegionInput）
  - CLI 面板：jimengCliPanel(211)/codexCliPanel(228)/geminiCliPanel(242)（各自状态+动作）
  - 验证行(258)：testUrlBtn→testConnection()；probeAsyncBtn→probeAsync()；protocolInput(266) 8 选项；imageRequestModeInput(278) 4 选项
  - runninghubConfigBlock(290)：rhPasteInput 粘贴路径创建卡片 + rhAppsList/rhWorkflowsList
  - 模型区(319)：fetchModelsBtn→fetchModels()；imageModelList(345)/chatModelList(356)/videoModelList(367)/msLoraList(382)
- recommendContent(386)：推荐平台面板

---

## K. 落地到我们项目（映射结论）

| 层 | 抄 Infinite-Canvas | 放我们哪 |
|---|---|---|
| 后端注册表 | A/B/C/G + api_providers.json | `apimart-gateway/providers.py` + `providers.json` |
| 后端分派 | C/D generate_ai_image 结构 | `apimart-gateway/main.py` 各生成路由 |
| 后端管理 API | G（前缀 /api→/v1） | 网关 `/v1/providers*` + localTool `/api/providers` 透传 |
| 产物归一/素材吞入 | E/F/H | `openai_client.py` 等各 client + 复用现有 `saveRemoteUrl` |
| chat 分派 | I | `main.py` chat 路由 |
| 前端面板 | J（api-settings.js + html） | `ProviderSettings.jsx`（配色用原型） |
| 默认 seed | 替换为 9004 Lovart | `providers.json` |

**执行顺序**（见 `00-执行计划.md`）：providers.py → 管理 API → 分派骨架(Lovart 零回归) → openai client → localTool 透传 → 前端面板 → 逐个扩展。

---

## L. 审计核对表

- [ ] 字段名与 A.1 一致，无自创名（避免加翻译层）
- [ ] 枚举与 A.5 一致，未增改
- [ ] 协议锁死规则与 A.6 一致
- [ ] key 只进 env，列表脱敏（A.2/A.3），primary 唯一化（G.1）
- [ ] 各协议请求构造与 D 一致（openai 6 支/gemini/volcengine/runninghub/CLI）
- [ ] 产物归一/素材吞入与 E 一致，落盘与 H 一致（不丢图）
- [ ] 异步轮询/状态词与 F 一致，端点拼接前缀吸收（1221）
- [ ] 连接测试/异步探测/方舟自愈与 G.3/G.4 一致
- [ ] chat 分派与 I 一致
- [ ] 前端表单字段与 J 一致，配色用我们原型
- [ ] 默认 seed = 9004 Lovart，Lovart 零回归，端口/入口铁律不破
