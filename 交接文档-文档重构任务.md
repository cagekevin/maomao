# 交接文档 · 重构 docs/ 理解文档层

> 生成时间：2026-07-21
> 适用：把本任务交接给新窗口 / 新 AI 会话。读完即可接手，无需重读全部历史对话。

---

## 0 · 任务目标

为个人项目 `maomao`（一毛AI画布，Chrome 扩展/本地应用）建立**标准理解文档层**，收敛到 `docs/` 目录，采用数字前缀（宏观→微观）。

核心原则（用户明确）：
- **防遗忘 + 支撑后续 agent 开发**，不是企业级合规文档。
- **代码即文档**：只写代码表达不了的"是什么"（接口、数据流、黑话），**不编 Why / 设计理由**。
- 用户原话："把所有接口，所有能找到的数据流集中写清楚，别瞎编乱造为什么这样设计了。"
- 后续步骤："先把 12 份拼起来，然后再搜啊" → 即**先以 AI13 收敛产物为素材源，缺口再搜源码**。

---

## 1 · 已锁定的关键结论（不要再纠结）

1. **AI13 不是又一份审计，而是"12 份拼完 + 去冲突 + 源码实锤"的权威收敛版**。它已经是可用的素材源，不要再去重读 AI01~AI12 或重啃 4 万行源码。
   - AI13 三份权威产物：`AI05断言清单.md`（18 节 M1~M7）、`裁决表.md`（每条 ❌ 附 file:line 实锤）、`交叉验证最终报告.md`（总览 + 7 冲突 + 缺口）。
   - `草稿/` 下 52 份核对 + 12 份回执是过程证据，已闭环。

2. **七个冲突点已全部裁决（AI05 基准正确）**，标准文档必须采用这些结论：
   - `canvas-state-change` 事件 **不存在**（grep 0 命中）；画布持久化走 `Q.saveCanvasState`(L1642) 直接写 localforage。
   - `mutiwindow-sync-local` **不存在**（实为 `handleSyncLocal`@L44426 函数）。
   - `Zr` 双绑定：L1827 下载函数 / L43893 logout。
   - `toAbsoluteFileUrl` 真身 = `localTool/src/routes/resources.ts#L31`（App.js 内 0 命中）。
   - `Jn` 两绑定：L89 LogoIcon / L32490 生图主回调（真身）。
   - `Th` = Director3DNode（注册 L31141），**非失效**；`$d` = DirectorShell(L24391)。
   - `Hr` = `localEngineBase()`(L1732) 动态随开关；`vv`(L42808) 硬编码 18080。

3. **同名遮蔽全集（必须进 glossary）**：ei/ti 四义（ei@L1853 Map / L36784 打组 / L43950 push / ti@L36822 清缓存 / L43974 pull）、Jn、Zr、we、R、Th。引用必须带行号/语义名。

4. **AI01~AI13 全部保留不动**（过程证据）；旧 `ARCHITECTURE.md`/`TASKS.md` 用户要求"先留着不动"（曾尝试改写被取消）。

---

## 2 · 已实锤、可直接写入标准文档的素材（来自 AI13，无需再搜）

### 接口全集
- **localTool (:18080)**：files（upload/read/thumbnail/mkdir/move/open/open-dir/list）、kv（get/set）、system（status/proxy/jianying-send 占位）、resources（GET/save/batch-save/delete/clear/rescan）、tasks（GET/save/batch-save/delete/batch-delete/clear）。函数真身见 `localTool/src/routes/*.ts`。已知债务：wv 只删 DB 不删盘、tasks clear 无删盘、rescan 孤儿清理只清 `source='local-tool'`。
- **网关 (:9004)**：`POST /v1/images|videos/edits/generations`、`GET /v1/tasks/{id}`（轮询）、`POST /v1/tasks/{id}/confirm`、`/v1/chat/completions`（SSE：abort→400 L502/L521、超时→504 L541、heartbeat L549、CHAT_SYNC_TIMEOUT 300s）、`/v1/balance`、`/music|/audio/*` → **501**（main.py L655/L659/L663，README 矛盾）。
- **跨进程**：`resourceAdded`（chrome.runtime，background→App.js L43527 落盘）。

### 数据流全集（端到端，全部 ✅ 多 AI grep 佐证）
1. **AI 生图流**：`Jn`(L32490) → `POST /v1/images|videos/generations` → 轮询 `GET /v1/tasks/{id}`(L33005) → completed 取 `images[].url[0]`(L33031) → `ii`(L33049) 落盘 18080 → `mutiwindow-task-completed`(L38481) → `Ev`(L42883) rescan → 资源面板刷新。
2. **资源采集流**：background 不下载(L80) 只存 `source:'extension'` → `resourceAdded` 跨进程 → 前端 `Zr`(L1827) 下载 → `Sv`(L42838) 入库。
3. **画布拖入流**：`onDrop`(Lr) → `ii({subfolder:'canvas/drop'})` → `POST /api/files/upload`。
4. **统一同步流**：遍历 `globalTasks` → 未本地化则 `uploadFile` → `mutiwindow-task-completed` → `Ev` rescan（已本地化跳过，防死循环）。
5. **rescan 流**：`Ev` → `POST /api/resources/rescan` → 扫盘排除 `.thumbnails` → `toAbsoluteFileUrl` 补全 → 孤儿清理只清 local-tool。
6. **画布持久化流**：`Q.saveCanvasState`(L1642) 直接写 localforage，**无 canvas-state-change**。
7. **GAS 云同步流**：`ei`(push, L43950) 收集 9 类配置（`app_settings`/`api_configs`/`users`/`membership`/`projects`/`presetPrompts`/`customNodeTemplates`/`modelSchedules`/`cloud_storage_config`）→ `CloudSyncEngine.push`(L43896)；`ti`(pull, L43974) 拉取后 1s `window.location.reload()`。GAS 须部署为"所有人访问"否则 `<html` 拦截(L43914)。
8. **3D 封装**：`$d`=DirectorShell(L24391) 布局壳 / `Th`=Director3DNode(L187/L31141) 节点壳。
9. **启动播种流**：幂等锁 kv/Ov(L42894/L42895)；`Zr`(L43104)+`Sv`(L43107) 迁移过滤 `source!=='local-tool'`；`Y_` batch-save(L43132)。
10. **撤销重做**：store `r` 统一接管（ARCH 旧表述不精确）；`mutiwindow-rerun-task`(L44343)→监听 L36618。
11. **task-meta 流**：`mutiwindow-update-task-meta`(L41032 发 / L43734 监听，1s 防抖) → `J_`(L41611) `POST /api/tasks/save`。

---

## 3 · 当前文件状态（磁盘真实情况）

- 我新建的六层文件：`01-concept.md` / `02-architecture.md` / `03-database.md` / `05-runbook.md` 曾被清空为 **0 字节空文件**；`glossary.md` 有内容（较早写的版本，需按新标准重写）；`04-api/api-reference.md` **从未成功写入**（多次被取消）。
- `README.md`（根目录）不确定是否被用户删除。
- **AI01~AI13、ARCHITECTURE.md、TASKS.md 均未动。**

> 接手方应视为：六层文件基本从零重写，glossary 可参考旧版但要按新标准（只写接口/数据流/黑话，不编 Why）重写。

---

## 4 · 待补缺口（AI13 标 ➖，需搜源码）

以下 AI13 明确未覆盖（非冲突，建议补进标准文档时标"待补"，或由用户指定后搜源码补齐）：
- 网关 chat 内部机制细节（main.py L434-574，AI05 13 节已写较细，可直接用）。
- GAS 9 类配置具体内容（AI05 15 节已坐实，可直接用）。
- 启动播种幂等锁细节（L42894/L42895）、撤销栈内部（undoStack L21219 / r L21580 / Su L21178）。
- 跨窗机制（17-5，纯 CustomEvent 不能跨扩展 window，待查）。

> 注：AI05 的 13 节（网关chat）、15 节（云同步）已经写得很细，**缺口其实比 AI13 最终报告说的更小**——很多"➖"在 AI05 深一度文档里已有详细内容，可直接吸收，不必重搜。

---

## 5 · 下一步动作（接手方按此执行）

1. 以 **AI13 三份权威产物 + AI05 深一度文档（08~18 节）** 为唯一素材源。
2. 重写六层文件（02/03/04/05 写全接口+数据流；01 极简；glossary 写同名遮蔽+已证伪+跨包真身）：
   - `04-api/api-reference.md`：**接口全集**（localTool + 网关 + 跨进程），表格化，含方法/参数/返回/状态码。
   - `02-architecture.md`：**数据流全集**（用 Mermaid 时序图 + 文字，覆盖第 2 节 11 条流）。
   - `03-database.md`：resources/tasks 表字段 + DDL（来自 `localTool/src/db/database.ts`，已实锤）+ 已知债务。
   - `05-runbook.md`：启动三步（localTool→网关→前端，来自 `启动项目.ps1`）+ 配置指向 `src/_engine/config.js`。
   - `01-concept.md`：产品定位 + 边界。
   - `glossary.md`：同名遮蔽 + 已证伪项 + 跨包真身。
   - `README.md`（根目录）：索引链接六层。
3. **不编 Why**，不碰 AI01~AI13、不碰旧 ARCHITECTURE.md/TASKS.md、不碰源码。
4. 缺口部分：吸收 AI05 深一度已写内容；其余如实标"待补"。

---

## 6 · 关键文件速查

| 文件 | 用途 |
|------|------|
| `docs/AI13/AI05断言清单.md` | 18 节断言目录（M1~M7），交叉验证基准 |
| `docs/AI13/裁决表.md` | 每条冲突的源码实锤 |
| `docs/AI13/交叉验证最终报告.md` | 总览 + 7 冲突 + 缺口 |
| `docs/AI05/04~18-*.md` | 基准原文（含数据流细节，13/15 节最细） |
| `localTool/src/routes/*.ts` | localTool 接口真身（已读 files/kv/system/resources/tasks） |
| `localTool/src/db/database.ts` | DDL 真身（已读） |
| `apimart-gateway/main.py` | 网关端点（已读 501/abort/timeout/heartbeat/轮询） |
| `src/_engine/config.js` | 配置开关（已读） |
| `启动项目.ps1` | 启动步骤（已读） |
