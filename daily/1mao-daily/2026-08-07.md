# 修复日志 — 2026-08-07

> 今日工作：① 排查 localTool 是否还有"不支持中文"同类低级错误（结论：核心链路已全通，无崩溃级问题）；② 系统性排查 localTool 潜在隐患，按**奥卡姆剃刀**决策——均属"目前未暴露"的低概率风险，**不做代码改动，仅登记留痕**。

---

## 一、localTool 潜在风险排查（奥卡姆剃刀决策：暂不处理）

按用户要求，对 localTool 全部核心文件（index 路由、admin/files/kv/official/passthrough/platform/resources/system/tasks 路由、database、helpers、netProxy、fileStore）做了隐患扫描，发现若干"目前未暴露"的潜在风险点。

**决策原则（奥卡姆剃刀）**：这些都不算当前暴露的大问题，若为它们引入鉴权机制/复杂加固等新实体，反而增加复杂度与回归面。**故不写代码，仅登记留痕**，后续若实际触发再针对性处理。

### 潜在风险清单（已识别，暂不修复）

| # | 位置 | 风险描述 | 严重度 | 处理 |
|---|---|---|---|---|
| 1 | `resources.ts` `resourceToRow`/`upsertResource` | 无列名白名单（tasks.ts 有 `ALLOWED_TASK_COLUMNS`，resources 漏了），前端 body 键拼 SQL 列名 | 🔴 中（仅本机+固定字段，触发面窄） | 暂不修 |
| 2 | `helpers.ts` `buildPaginatedQuery` L195 | `filters` key 拼列名无白名单（值已参数化） | 🔴 中 | 暂不修 |
| 3 | `admin.ts` `handleAdminImport` L167-179 | 导入数据键拼列名无白名单 | 🔴 中 | 暂不修 |
| 4 | 管理接口无鉴权 + CORS `*` | 本机恶意网页可 CSRF 触发 export/import/cleanup/clear-cache（服务仅绑 127.0.0.1） | 🟡 中 | 暂不修（加鉴权=新增实体，违反剃刀） |
| 5 | `admin.ts` `cleanup` / `database.ts` `deleteLocalFile` | URL 编码一致性依赖"DB 存原始中文 URL"；若存编码 URL 会误删/删不掉 | 🟡 中 | 暂不修（当前假设成立，实测正常） |
| 6 | `helpers.ts` `parseMultipart` L86/L97 | 硬编码 CRLF，非标准客户端可能损坏文件 | 🟢 低 | 暂不修（浏览器恒 CRLF） |
| 7 | `admin.ts` import 无规模/类型校验 | 可导入畸形数据清空业务库（需 confirm:true） | 🟡 中 | 暂不修 |
| 8 | `netProxy.ts` `proxyResponse.clone` | clone 后 body 一次性空流 | 🟢 低 | 暂不修 |

> 注：若后续某项从"潜在"变为"实际暴露"，按优先级处理——先 1/2/3（列名白名单，改动最小、照抄 tasks.ts 现成模式），再考虑管理接口鉴权。

---

## 二、复盘：剧本盒子中文路径修复（昨日延续）

昨日已修复剧本盒子「生成资产」两个叠加 bug 并推送（`20dd0d2`/`f3c23f6`/`bb340b3`）：
1. `Mr` 不解析 SSE → 兼容 SSE 流解析（前端）。
2. localTool 静态服务 `handleStaticFile` 不解码中文 subfolder → 加 `decodeURIComponent`。

**今日补充排查**：确认 localTool 其余文件/路由对中文均正常（`handleThumbnail`/`handleRead`/`handleList`/`handleMkdir`/`handleOpen`/`handleOpenDir` 走 `searchParams.get` 自动解码或 JSON body 原样，直接 `path.join` 落盘，实测 thumbnail/read 均 200）。唯一用未解码 `pathname` 拼磁盘路径的 `handleStaticFile` 已修，是根上的断裂点，已覆盖所有 `/files/` 静态读取。

---

## 三、测试残留清理

排查实测时产生的 `~/.maomao-localtool/uploads/测试目录/` 已清理（rm -rf），无残留。

---

## 四、画布右键导入·素材 tab 为空 —— 修复（方案 A：后端 save 统一 dataURL 落盘）

> 详见 `docs/34-素材系统与落盘rescan专题.md`（第 9 节）。核心：剪贴板粘贴只把 dataURL 塞进内存/SQLite、从不写真实文件 → 本地文件夹必空、刷新易丢。

### 根因确认（源码逐节点核对）

- 粘贴：`Vr.jsx:2050-2079` `readAsDataURL` → dataURL → `Ia(t,'image')`（`Vr.jsx:2085`）→ `url: e` 存 dataURL → `Ar()` 调 `POST /api/resources/save`。
- 后端 `handleResourcesSave`（`resources.ts`）此前只 `upsertResource`，**从不解码写盘** → 磁盘 `upload/migrated/` 永远空。
- 刷新后 `L` 靠 `xt()` 重建（`Vr.jsx:147`，有端口守卫），rescan 只扫磁盘、dataURL 记录无文件不接管 → 端口离线/清库时该素材永久丢失。

### 代码改动（仅后端 `localTool/src/routes/resources.ts`，约 +20 行）

1. import `writeUploadBuffer`（fileStore）
2. 新增 `decodeDataUrl`：解 dataURL base64 + MIME 推导扩展名
3. `handleResourcesSave` 开头：`url.startsWith('data:')` → `writeUploadBuffer(folder,'clip-<ts>.<ext>')` 落盘 → `body.url` 改写为 `http://127.0.0.1:18080/files/...` → 再 upsert。落盘失败不阻断。

### 现场验证（rescan / 数据核对）

- 服务 18080 在跑；`rescan` → `{scanned:303, added:0, skipped:303, orphanDeleted:0}`（磁盘文件全在库，库健康）。
- 库 311 条：`tasks:299`、`migrated:8`、`canvas:4`。
- 磁盘 `upload/`：`canvas:273`、`migrated:1`、`tasks:556`；`migrated/` 只有 1 个缩略图，无真实图。
- **直接证据**：库 `migrated` 中 1 条记录 url 是 `data:image/png;base64,...`，磁盘无对应文件 → 粘贴未落盘，正是空 tab 根因。

### 待办 / 边界

- 改后需**重启 localTool** 生效；改后**新粘贴**素材才落盘。
- 已存在的存量 dataURL 记录**不自动迁移**，如需恢复需一次性解码落盘（可选）。
- 远程 http(s) URL / AI 资产（`ci`/`hi`）不在本次范围（详见 docs 9.2）。
- 用户待测试：重启后重新粘贴图片 → 看 `upload/migrated/` 是否有文件 + 素材 tab 刷新不丢。

---

## 五、网关：垫图部分失败静默丢图 → 改为阻断（方案 A）

> 现象排查：用户 15:32:28 发出的任务，垫图 3 张（2 base64 + 1 本机 `http://127.0.0.1:18080/f...`），网关日志 `raw_urls=3 attachments=1` —— 2 张垫图上传 Lovart CDN 失败被静默丢弃（`resolve:error`：本机回环URL + 裸base64 均"上传连接失败(直连及代理均不可达)"），仅 1 张成功仍照常发出 → 用户感觉"垫图没成功"。

### 根因（代码确认）

- `resolve_attachments`（`apimart-gateway/main.py`）兜底条件原为 `if raw_urls and not out and last_upload_err`：**只有"全部失败"才抛错**。部分成功（out 非空）→ 不抛错，继续发，prompt 却写 `Reference image attached` → Lovart 按不完整的垫图生成，静默误导。

### 改动（方案 A，仅 `apimart-gateway/main.py`）

1. `resolve_attachments` 返回签名 `list → Tuple[list, int]`，新增 `failed_count`（统计真实上传/下载失败数；主动 drop 的 blob/未知格式**不计入**）。
2. 兜底条件 `not out` → **`failed_count > 0`**：只要存在任一参考素材真实失败即抛 `LovartError`（502，信息带失败张数），**垫图不齐就不发出**。
3. 各失败分支（本机回环 URL、data:base64、裸 base64）加 `failed_count += 1`。
4. 适配两处调用：`_do_submit`（解包 `attachments, _failed`）、chat `run_and_get`（取 `[0]`）。

### 效果对照

| 场景 | 改前 | 改后 |
|---|---|---|
| 3 张垫图，1 张成功 | 静默发出（只带 1 张）| 阻断，报"有 N 个参考素材上传失败" |
| 单张垫图失败 | 抛错 | 抛错（不变） |
| 垫图全成功 | 正常发出 | 正常发出（不变） |

### 注意

- **需重启网关 9004 生效**（Python 无热加载）。
- `smoke_test.cjs` 只查 `src/bundle/`，与本改动无关；已用 `py_compile main.py` 验证语法（EXIT=0）。
- 行为变更：网络/VPN 波动导致垫图上传失败时，任务被明确阻断并提示重试，不再静默丢图生成。
