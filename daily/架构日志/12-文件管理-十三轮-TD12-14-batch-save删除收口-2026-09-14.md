# 12 文件管理 · 十三轮 · TD-12-14 batch-save 删除收口（2026-09-14）

## 一、证伪（Step 1）：这条债真实存在吗？

- **现象**：`/api/resources/batch-save` 是第二处可写 context 的旁路（`upsertResource` 直刷 `name/folder/project_id`，不过 context-only 三不变式）；前端 `apiRegistry` 标 `RESERVED`、零消费 → 一旦接线即绕过不变式。
- **取证（grep 实证，非读描述）**：
  - 后端 handler `handleResourcesBatchSave`（`localTool/src/routes/resources.ts:557`）仅被 `router.ts:46/188` 注册、`index.ts:454` 的 help 文案引用。
  - 前端 `contracts.ts:1111` `resourcesBatchSave` 状态 `RESERVED`、`fn:'(前端零消费)'`；`localToolApi.ts` 仅引用 `/api/tasks/batch-save`（tasks，非 resources）。
  - `localTool/test/localtool.test.js` 只测 `handleTasksBatchSave`，**无 resources batch-save 用例**。
  - 结论：确为第二写入口（真债），但**零消费方** → 按 Step 3 删（死代码零消费 = 收口为单一写路径），而非"接线后补不变式"（那会保留第二份，与 SSOT 相悖）。

## 二、处置（Step 3：删）

删除整条旁路，写路径收口为 1 份（唯一真源 = `recordUploadedFileRow` + `applyResourceContextChange`/`Move` + `rescan`）：

| 文件 | 改动 |
| --- | --- |
| `localTool/src/routes/resources.ts` | 删 `handleResourcesBatchSave`（557–576） |
| `localTool/src/router.ts` | 删 import + 路由 `/api/resources/batch-save` |
| `src/components/base/core/contracts.ts` | 删 `apiRegistry.resourcesBatchSave`（RESERVED 登记）—— `check:api` 对 "RESERVED 无路由" 判 **warn + exit 1**，故真源两侧须同步删 |
| `localTool/src/index.ts` | help 文案移除 `/api/resources/batch-save` |

## 三、度量（改前 / 改后，可比）

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| 资源 context 写入口数 | 2（recordUploadedFileRow + batch-save） | **1**（仅 recordUploadedFileRow 系） |
| 绕过三不变式的旁路 | 1 | **0** |
| 改动文件 | — | 4 |
| `check:api` | — | error 0 / warn 0 / info 5（exit 0） |
| localTool 测试 | — | **297 / 297 绿** |

## 四、验证（删除类无行为探针，改以"消费方计数 0"为证据）

- grep 全仓：`handleResourcesBatchSave` 仅在删除点之前存在；删除后 0 生产消费方（含测试）。
- `node scripts/check-api-contract.cjs` → `API 契约校验通过 ✔（error 0 / warn 0 / info 5）`。
- `cd localTool && npm test` → 297 pass / 0 fail。
- `git status` 仅 5 文件改动（`resources.ts`/`router.ts`/`index.ts`/`contracts.ts` + 闸），无意外扩散。

## 五、债描述修正

- 债务.md 中 TD-12-14「当前 RESERVED、前端零消费 → 无实害，但一旦接线即绕过不变式」**属实**，处置从"登记不动手"改为"删除以收口"（用户本轮要求清偿非视频债）。
