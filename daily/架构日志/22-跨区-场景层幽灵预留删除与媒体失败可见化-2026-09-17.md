# 22-跨区 · 场景层幽灵预留删除与媒体失败可见化（TD-16-29② 收口 ／ TD-22-63 全面清账）· 2026-09-17

> **日期**：2026-09-17 ｜ **交接来源**：`docs/138-彻底施工规范与交接-错误透传与成功判据-2026-09-17.md` §五 T1/T2
> **裁定记录**：场景层（Scene）功能用户裁定「**删**」（原 T2 指令"补场景重命名/删除 UI"在概念澄清后改判）。

## 〇 · 一句话

> T1 的 8 处裸媒体元素**逐个**「改 5 + 证伪 1」（+ 2 处前轮已修核对）；T2 的命令层/manager 层静默失败**全部定性**（改 6 + 证伪 7 + 删 1 个零消费方法）；顺带按用户裁定把**从未接线的多场景半成品层整层删除**（5 方法 + 3 命令 + 3 纯函数 + 1 条锁空集的测试用例）。

## 一 · 概念澄清（先于动手 · 用户质问后补的取证）

| 层 | 是什么 | 证据 |
| --- | --- | --- |
| **项目**（`canvasProjectId`） | 宿主画布的工程（maomao 是画布应用，剪辑器嵌在画布里，三把存储键由它组出） | `editor-provider.tsx` `setEditorContext` |
| **作品**（`TProject`/`editorId`） | 剪辑器里的"一部片子"；UI 的新建/切换/重命名/删除**全在此层且已存在** | `editor-header.tsx`「我的作品」 |
| **场景**（`TScene`） | 作品内部的时间轴"分幕"（一组轨道+书签）；时间轴显示的轨道 = 活跃场景的轨道 | `timeline-manager.getTracks = scenes.getActiveScene()?.tracks` |

**回答"是不是有两个新建"**：是——`createNewProject`（作品，UI 已接）与 `scenes.createScene`（场景，**零消费**）。

## 二 · T1（TD-16-29②）：8 处裸媒体逐个结论

| 站点 | 结论 | 做法 |
| --- | --- | --- |
| `preview/index.tsx`（AssetPreviewPlayer img/video/audio） | **改** | 共用新原语 `useMediaLoadFailed`；失败 → `MissingMediaIndicator`（不再渲染坏元素与占位同屏） |
| `VideoThumbnail.tsx:98`（`<video>`） | **改** | 同原语 + 失败占位（本组件是全站视频缩略图唯一出口，一处修 5 个调用方） |
| `timeline-element.tsx:412`（贴纸 `<img>`） | **改** | 抽 `StickerContent` 子组件（onError 是 hook，不能进条件分支——hooks 规则）；失败 → 告警图标 |
| `timeline-element.tsx:495`（CSS backgroundImage） | **改** | 改 `<img>`（`object-cover` ≡ `background-size:cover`，`pointer-events-none` 保持不挡拖拽），从而可挂 onError |
| `video-thumbnail-strip.tsx:113`（CSS backgroundImage） | **证伪** | `thumbnailUrl` 是 **dataURL**（不经过网络，无 4xx/超时可重试失败；损坏属生成侧问题）；canvas 才是真实内容；抽帧失败已有 `logger.warn`；已在代码内留 4 点证伪注释 |
| `AssetNode.tsx:468`（audio）/`media.tsx`（×2） | 前轮已修 | 核对无误，不再动 |

**新原语**：`base/utils/useMediaLoadFailed.ts`（失败置位粘住 + `logger.warn` 留痕 + `resetKey` 复位；`detail` 收 ref 保引用稳定）。
**行为断言**：`useMediaLoadFailed.test.tsx`（4 条：置位+留痕 / detail 归因 / 粘住 / 复位）＋ `VideoThumbnail.test.tsx`（3 条：正常态 / 失败占位+留痕 / 换源复位）。

## 三 · T2（TD-22-63）：命令层/manager 层全量定性

### 3.1 改（仅 logger → reportDegrade，"开发者留痕 + 用户 toast"一次搞定）

| 站点 | 失败的用户后果（改前零提示） |
| --- | --- |
| `media-manager.clearProjectMedia` | 面板已清空、持久层还在 → 刷新后**全部复活** |
| `project-manager.loadAllProjects` | 「我的作品」静默空白 → 新增 `projectsLoadError` 持续态 + `editor-header` 渲染错误行（范式同 `media.loadError`；**不 rethrow**——列表失败不该阻断剪辑器加载） |
| `add-media-asset.ts`（保存路径走 **redo**） | redo 落盘失败 → 素材刷新后消失 |
| `remove-media-asset.ts` undo | 撤销删除失败 → UI 已恢复、刷新后素材消失 |
| `delete-elements.ts` ×2（清理/恢复 ephemeral） | 同"复活/消失"两形态（乐观 UI 已生效） |

### 3.2 证伪（保留 logger，注释防重报）

| 站点 | 证伪理由 |
| --- | --- |
| `move-elements.ts` ×3 | drop 目标经 `computeDropTarget` 的 `isCompatible` 过滤 + hook 先行存在性检查；守卫是**内部不变量防御**（拖拽中状态漂移），非用户可达；拖拽高频，toast 即噪音 |
| `audio-manager.ts:305/:208` | 解码失败有 warn；null 是文档化契约；播放循环内 toast = 刷屏；持续态读者 = 波形错误态 |
| `project-manager:121/:155/:166` | 三处均有对读者：SaveManager toast / `media.loadError` 面板态 / rethrow 给调用方 |
| `save-manager:151` | 协议外异常 → 诚实 outcome → `reportSaveFailure` toast（唯一读者确立） |
| `renderer-manager:152` | catch → outcome `{ok:false}` 契约给导出 UI |

### 3.3 删（场景层幽灵预留 · 用户裁定）

- `ScenesManager`：`loadProjectScenes` / `createScene` / `deleteScene` / `renameScene` / `switchToScene` —— 5 方法**全仓 0 消费者**
- `commands/scene/`：删 `create-scene.ts` / `delete-scene.ts` / `rename-scene.ts`；命令桶只余书签两命令
- `lib/scenes.ts`：删 `canDeleteScene` / `getFallbackSceneAfterDelete` / `findCurrentScene`（消费者只剩上述死链）；`updateSceneInArray` 有活消费者（书签命令）保留
- `veProjectContextRelease.test.ts`：删「切场景 = 切编辑上下文」用例——**有测试 ≠ 有消费者**：它锁的是"如果切场景必须清上下文"的条件不变式，触发条件运行期不存在 = 锁空集。TD-22-32 的知识移入 `scenes-manager` 头注释
- **弯路留痕（必读）**：`switchToScene` 有行为锁 + `deleteScene`/`renameScene` 第一反应是"对齐 loadError 范式补错误态"——理由都通顺，但都是**给死代码续命**。判据：先证运行时消费链，再看测试。多场景的产品语义本身不完整（预览/导出只渲染活跃场景、作品时长只算主场景），将来要做**整层重做**，不复活单个方法

## 四 · 验证（收尾四件套）

- `tsc --noEmit`：本会话改动 **0 错**（剩余 3 错在并行会话的在途文件：`link-media-refs.ts` ×2 + `creditGateModes.test.ts` ×1，**非本会话所改，未触碰**）
- 测试：`veProjectContextRelease` / `useMediaLoadFailed` / `VideoThumbnail` / `LazyImage` / `AssetNode` / `timelineShared` / `trackHeightScale` / `ImageBoxNode` / `VideoExtractNode` / `VideoGenerate` / `TaskCenter` / `GeneratedView.refresh` **全绿**（48+，未跑全量——用户明令）
- `check:arch`：✅ ｜ `arch-index`：✅
- 临时探针 `.probe/detect-catch-default.mjs`（catch→return 默认值复查：58 处 / 其中多数有留痕）**跑完即删**

## 五 · 并行会话边界（重要）

本轮发现**另一 AI 会话正在同一工作区做「可引用媒体源/导入弹窗」链**（未提交改动：`App.tsx`、`media/*`、`ImportMediaModal*`、`projectStore`、`link-media-refs.ts`、`views/media.tsx` 等）。其消费侧与生产侧契约当前自相矛盾（`link-media-refs` 期望判别联合、`mediaRefFactsOf` 裁定 throw），属其**在途中间态**。本会话与其文件集**零交集**，未触碰其任何文件；其 tsc 红不归本会话修。

## 六 · 落账

- `TD-16-29`：①②③ 全部完成 → **resolve**
- `TD-22-63`：iconify（前轮）+ 本轮命令/manager 全量定性 → **resolve**
