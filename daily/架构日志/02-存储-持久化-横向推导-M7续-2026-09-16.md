# 02 · 存储 / 持久化 · 横向推导（M7 续）· 2026-09-16

> **性质**：横向推导轮（用户 2026-09-16 要求「根据现有债务从底层横向推导还有没有别的债」）。
> 触发母体 = M7（工程数据无统一契约／绕过唯一入口 `contentStore`）。起点 = 最底层 `contracts.ts`(STORAGE_KEYS 登记表) + `contentStore.ts`(唯一入口)，沿 fan-in 与全局裸存储访问向下取证。
> **结论落点**：本文件（唯一写全处）；债 ID 绑区 02（与 M7 同区）。

---

## 一 · 取证方法

1. `refs contentStore.ts` → 46 个正经消费者（均经唯一入口）。
2. 全局搜 `localStorage|sessionStorage|indexedDB` → 40 文件命中，逐文件排查绕过点。
3. 比对 `STORAGE_KEYS` 登记表（contracts.ts:208+）确认键是否登记、写路径是否经 `contentStore`。

## 二 · 探债段

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **TD-02-34** | `director3d-custom-poses` 已登记 STORAGE_KEYS 却经 `storage.ts:43` 裸 `localStorage.setItem` 绕过 `contentStore`；`backupStore.exportAll` 硬编码清单漏收 | 增债 | 结构债 | 低 | 母体 M7（绕过唯一入口／第二存储通道）— 与 TD-02-33 同源 | poses 也经 `contentStore` 落盘，或显式排除并加 backup/monitor 白名单；随 M7 方案 A 一并处理 | 待还 |

### 已裁定非债／有意设计（留痕，防下个 AI 重审）

| 现场 | 裁定 | 证据 |
| --- | --- | --- |
| `director3d-project*` / `d3dPersistence.writeProject` | 非债 | 经 `contentSetKvWithFallback`（contentStore.ts）落盘；键已登记 contracts.ts:445/453；双通道降级副本由 contentStore 内部托管 |
| `accountsStore` 28 处 `localStorage` 匹配 | 非债 | 自身数据走 `contentGetAsync/contentSetAsync`（accountsStore.ts:13）；匹配项均为扩展抓「页面」登录态功能（readTabLocalStorage/writeTabLocalStorage），非应用自身存储绕过 |
| `captions.tsx`（editor-caption-language/model-id/template-id） | 已被 TD-02-33 覆盖 | captions.tsx:12,36-46 即用 `use-local-storage.ts`，TD-02-33 描述已含「caption 语言等」 |
| `volumePolicy.ts` / `useScriptBoxEngine.ts` / `App.tsx` / `PromptPresetView.tsx` / `promptHubStore.ts` / `nodePrefs.ts` | 非债 | `localStorage` 仅出现在注释或经 contentStore 的已登记 store（nodePrefs 正是把官方散落 `mutiwindow_*` 键收敛为统一 `yimao_node_prefs` 的方案） |

## 三 · 覆盖度表（本轮纳入）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `base/core/contracts.ts`(STORAGE_KEYS) | L0 契约 | 深审 | 🟢 | 09-16 | 全表比对 | 无债 |
| `base/core/contentStore.ts` | L1 唯一入口 | 深审 | 🟢 | 09-16 | refs 46 消费者 | 无债（已有 check:arch 规则 6/7） |
| `director3d/storage.ts` | L1 存储层 | 深审 | 🔴 | 09-16 | storage.ts:43 裸 setItem | TD-02-34 |
| `director3d/d3dPersistence.ts` | L1 存储层 | 深审 | 🟢 | 09-16 | d3dPersistence.ts:26,230 经 contentStore | 非债 |
| `base/store/accountsStore.ts` | L2 store | 深审 | 🟢 | 09-16 | accountsStore.ts:13 | 非债 |
| `base/canvas/nodePrefs.ts` | L2 store | 深审 | 🟢 | 09-16 | 收敛键 yimao_node_prefs | 非债 |
| `videoEditor/hooks-cutia/storage/use-local-storage.ts` | L1 存储层 | 深审 | 🔴 | 09-16 | 09-15 已立 | TD-02-33（兄弟，同源） |
| 其余 6 文件（volumePolicy/useScriptBoxEngine/App/PromptPresetView/promptHubStore） | 各层 | 抽审→深审 | 🟢 | 09-16 | 仅注释/经 contentStore | 非债 |

## 四 · 结论

存储地基（M7 母体）的兄弟绕过点**基本已被现有债覆盖**：3D 工程键已正确登记并路由 contentStore、accountsStore 为合法扩展功能、captions 已入 TD-02-33。唯一真实残留 = `director3d-custom-poses`（登记却裸写、备份漏收），登记为 TD-02-34（低息，随 M7 方案 A 一并处理）。更高价值的横向推导预计在「手抄不变量／交互模式」母体（TD-04-28 三写不变量、TD-22-20 十五处两段提交）所在的 UI/hook 层，非本底层扫描范围。
