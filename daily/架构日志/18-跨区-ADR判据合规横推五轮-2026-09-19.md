# 18 · 跨区 · ADR 判据合规横推 · 五轮（2026-09-19）

> 触发：用户「根据我们的 ADR，你去查一查还有哪些是违反 ADR 的，我们再去找找。」
> 接续：前四轮（横推 / 二轮 / 三轮 / 四轮）已落 TD-18-10~26，多数已解决；本轮补三件事 ——
> ① 一轮遗留的 `projectMemoryStore.ts` **抽审**项定性；② `localTool/**`（四轮明列未纳入）的 ai-relay 静默吞与 5xx 重试语义；
> ③ 一轮 `§六 未审` 清单里 ADR-0013/0014/0015/0028 的正式裁定（前数轮均未覆盖）。
> 方法：取「📌 违反时的判据」→ 可复现检索 → 命中处回读真实代码；静默吞/重试语义交叉比对 `genErrors.ts`（TD-16-16）。

## 一 · 探债

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话) | 状态 |
| -- | ---------------------- | -------- | ---------- | -------- | ---- | ---------------- | ---------------- | ---- |
| TD-18-27 | `src/components/agent/runtime/projectMemoryStore.ts:66-82`（`withLock` 内 `next.catch(() => {})` :71 + `catch { }` :77-80，均标 `// catch-ok: LOCK_CHAIN`） | ADR-0011 判据3 + ADR-0019 形态①：两处零留痕 catch，锁链错误被静默吞（无 log / 无 throw / 无标记） | 结构 / 18 core（agent 记忆并发写锁） | 记忆并发写锁失败时错误不可见 ⇒ 锁失效无人知，并发覆盖丢数据风险不可观测 | 中 | **母体**（与 TD-18-14 同族：catch-ok 豁免语义未收口为原语；本文件是一轮 `§三` 标「抽审·待下轮定性」的漏网项） | 锁链错误收口为原语（自带 logger.warn 且仍向上抛/标记），删手写 catch-ok | 待还 |
| TD-18-28 | `localTool/src/ai-relay/assistantStream.ts:124-141` 与 `:272-287`（`if (!response.ok)` 分支内 `catch { /* ignore */ }` :130 · :276，解析错误体失败静默吞） | ADR-0011 判据3 + ADR-0019 形态①：两处 `catch { /* ignore */ }` 零留痕（localTool ai-relay 包，四轮明列未纳入） | 结构 / 18 core（localTool 中继流） | AI 中继报错时错误体解析失败被吞；HTTP 错误本身仍 `onEvent`+`throw`，危害低，但解析失败不可见 | 低 | 孤例（本包内 2 处同语义，未收口） | 解析失败补 `logger.warn`（仍用默认 message） | 待还 |
| TD-18-29 | `localTool/src/ai-relay/assistantStream.ts:137` 与 `:283`（`retryable: response.status >= 500`） | ADR-0018 违反：5xx 标 `retryable: true`，但 ADR-0018 规定 5xx＝服务端错误不得重试；且违反本仓已立口径 `genErrors.ts`（TD-16-16）`http(含 5xx) → retryable: false` | 结构 / 18 core（localTool 中继错误分类） | `retryable` 字段虽仅观测（全库无 `if (retryable)` 分支，见 genErrors.ts:10），但写错语义误导日志/遥测，并为日后「5xx 真重试」回潮留口子 | 中 | 孤例（口径错位：与 genErrors.ts 第二份 retryable 语义） | 改为 `retryable: false`（5xx 非可重试），与 genErrors 口径对齐 | 待还 |
| TD-18-30 | `src/components/agent/conversation/conversationState.ts:387-390`（`catch { // catch-ok: MIGRATION }` 迁移失败静默吞） | ADR-0011 判据3 + ADR-0019 形态①：存量会话迁移（旧键→KV）失败零留痕（注释称「同款兜底语义」但无 log / 无标记） | 结构 / 18 core（agent 会话迁移） | 旧键会话迁 KV 超时/失败不可见 ⇒ 迁移缺失静默，用户会话可能无声丢失 | 中 | 孤例 | 迁移失败 `logger.warn` 留痕（含 key） | 待还 |
| TD-18-31 | `src/components/agent/canvas/canvasPlanExecutor.ts:758`（`id: __auto_fusion_${Date.now()}`） | ADR-0026 违反（TD-18-18 漏网）：手写 `Date.now()` 当唯一 id，TD-18-18 收口「7 站全部收口 idGen」未覆盖此生成步 id | 结构 / 18 core（画布规划 fusion 步） | 同毫秒生成两 fusion 步理论撞 id（实际极低，单次规划内唯一）；属 TD-18-18 收口漏网＝回潮 | 低 | **母体**（TD-18-18 收口未净，同族 Date.now 键） | 改用 `idGen` 唯一入口生成 step id | 待还 |

## 二 · 已裁定非债（留痕，禁再当 bug 补）

| 对象 | 判据 | 结论与证据 |
| ---- | ---- | ---------- |
| ADR-0013 纯查询禁止有副作用 | 判据 | **合规**：`librarySource.ts:172` 只读收口（TD-02-59）；`mergeResourcesFromBackend` 仅由所有方自有 `refreshFromBackend`（`resourceStore.ts:433`）与 picker/面板显式调用，无 `*.list()` 查询内写他人 store |
| ADR-0014 命令栈重置 | 判据 | **合规**：切换/加载作品全路径（`project-manager.ts:89/138/270` · `editor-header.tsx:67/86` · `editor-provider.tsx:110` · `EditorShell.tsx:59`）均经 `releaseProjectContext()`（`engine/core/index.ts:78-82` 含 `command.clear()`＋`audio.reset()`＋`playback.reset()`） |
| ADR-0015 批量入栈 | 判据 | **合规**：`executeBatch`（`commands.ts:33`）唯一原语；批量操作均委托（`use-editor-actions.ts:492` · `timeline-manager.ts:241/361`），无 `for (…) { cmdManager.execute(…) }` 循环单条 |
| ADR-0028 拉取反流 | 判据 | **合规**：`categories(items)` 仅见于 `ImportMediaModal.tsx` 注释（旧反模式文档）；现行默认分类取 `provider.categories()`（无参），意图状态存对象含 query，无回边 |
| `logger.ts:78-82` RECURSION_GUARD | ADR-0019 形态①? | **非债**：logger 自身无法上报自己的失败（会递归），零留痕是结构性必然，非隐藏缺陷（注释自证「本文件内禁止调 logger 自身」） |
| `AssistantTablePanel.tsx:449` 第二份可编辑判据 | ADR-0029? | **非债（死代码·零行为影响）**：判据重复面已被 TD-18-10~13 用户裁定非债覆盖；且 `:450` 无条件 `return` 使其不可达，属死代码，归后续死代码清扫，不立债 |

## 三 · 覆盖度表（增量 · 本轮新纳入 / 状态变化）

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| ------ | ---------- | --------- | --------------- | ------- |
| `src/components/agent/runtime/projectMemoryStore.ts` | 有债待还(TD-18-27) | 本轮 | `:66-82`（`:71`·`:77-80`） | 一轮抽审→定性：两处零留痕 catch |
| `src/components/agent/conversation/conversationState.ts` | 有债待还(TD-18-30) | 本轮 | `:387-390` | 迁移失败零留痕 |
| `src/components/agent/canvas/canvasPlanExecutor.ts` | 有债待还(TD-18-31) | 本轮 | `:758` | Date.now 漏网（TD-18-18 收口未净） |
| `localTool/src/ai-relay/assistantStream.ts` | 有债待还(TD-18-28/29) | 本轮 | `:130`·`:276`·`:137`·`:283` | 静默吞 2 处＋5xx 标可重试 |
| `src/components/base/media/providers/librarySource.ts` | 已裁定非债 | 本轮 | `:172` | ADR-0013 合规 |
| `src/components/videoEditor/engine/core/index.ts` | 已裁定非债 | 本轮 | `:78-82` | ADR-0014 合规 |
| `src/components/videoEditor/engine/core/managers/commands.ts` | 已裁定非债 | 本轮 | `:33` | ADR-0015 合规 |
| `src/components/base/panels/ImportMediaModal.tsx` | 已裁定非债 | 本轮 | `:67-69`·`:166`·`:224`·`:300`（均注释） | ADR-0028 合规 |
| `src/components/base/core/logger.ts` | 已裁定非债 | 本轮 | `:78-82` | RECURSION_GUARD 结构性必然 |
| `src/components/agent/assistantTable/AssistantTablePanel.tsx` | 已裁定非债 | 本轮 | `:449`（`:450` 不可达） | 死代码·TD-18-10~13 非债覆盖 |

## 四 · DATAFLOW 对账

- 无链路漂移（未改动 `spec/DATAFLOW.md`）。本轮内容属判据层与中继错误分类，按 ADR-0025 归 ADR / 代码注释。

## 五 · 覆盖账

- 本轮审 **10** 个文件（含 1 个 localTool 文件）；新增有债待还 **5**（TD-18-27~31，其中 2 条为母体漏网：TD-18-14 静默吞家族新增 3 处 src/localTool 漏网、TD-18-18 Date.now 收口漏网 1 处）；已裁定非债 **5**；抽审 **0**。
- **闭合**：一轮 `§三` 抽审项 `projectMemoryStore.ts`（待下轮定性）→ 本轮定性 TD-18-27；一轮 `§六 未审` 清单 ADR-0013/0014/0015/0028 → 本轮全部裁定合规。
- 未审（逐个列名 + 理由）：
  1. **ADR-0019 判据 2/4**（兜底总数净减度量、测试是否新增适配分支）—— 需改前/改后 diff 度量，静态审不可得（四轮亦未覆盖）；
  2. **ADR-0016 判据 2**（每个闸是否答得出 6 问 / 有负例探针）—— 归 17 区更合适。

## 六 · 状态行

- **状态**：已验证(有债待还) · 🔴
- **母体**：TD-18-14（catch-ok 豁免语义未收口为原语，本轮新增 `projectMemoryStore`/`conversationState`/`assistantStream` 三处 src/localTool 漏网）／TD-18-18（Date.now 收口未净，本轮漏网 `canvasPlanExecutor:758`）
- **待用户拍板**：无（结构债，架构师可自定）
- **本轮零代码改动**（只读审计）· **测试执行次数 0**
