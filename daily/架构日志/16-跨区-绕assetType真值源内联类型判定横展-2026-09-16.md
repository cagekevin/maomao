# 16 跨区 · 绕 assetType 真值源内联类型判定横展（2026-09-16）

> 横向推导起点：TD-16-4/5/6/7（母体 = 绕过 `assetType` 唯一真值源的内联扩展名/类型判定）+ TD-22-9（兜底式静默归位母体）。
> 本次只审计「mime/ext → kind 分类」是否还有散落内联实现绕过 `assetType.detectFileType` 唯一真值源。只读，不动代码。

## §一 链路快照（refs 实证）

- `assetType.ts:5` 自述：【唯一真值源】全库「扩展名 / mime → 媒体类别」判定只有 `EXT_KIND` 一张表 + `data:` 前缀；`detectFileType`(88-93) 与 `classifyAssetUrlKind` 是唯二入口。
- `EXT_KIND` 全仓仅 `assetType.ts` 引用（grep `EXT_KIND` → 4 处全在 `assetType.ts`）→ 任何别处做 mime/ext→kind 都是第二份。
- `media-utils.ts` refs：fan-in 3（`audio.ts:14`、`processing.ts:4`、`timeline-element.tsx:36`）→ `getMediaTypeFromFile` 是活代码。
- `processing.ts` refs：被 `use-editor-actions.ts:22`、`use-timeline-drag-drop.ts:4`、`media.tsx:39` 等消费 → 活代码。

## §二 溯源（从历史债横向推导）

- TD-16 母体：内联扩展名/类型判定绕过 `assetType.EXT_KIND`，已登记 `creativeCatalog.ts:83`、`scriptBoxEngine.ts:1165`、`director3d/project.ts:2020`、`markdownImages.ts:12` 四例（均待还）。
- 横向假设：同一母体（绕过 `detectFileType` 唯一真值源 + 二进制兜底静默）大概率不止这 4 处。重新全库 grep `startsWith('(image|video|audio|text)/`、内联 `? :` 分类，找出未被登记的同类。
- 已排除：`director3d/project.ts:2021` 即 TD-16-6 已覆盖；`providerModels.ts:57` 经读 `ModelDomain` 枚举（`'image'|'chat'|'video'`）确认三元穷举、`: 'video_models'` 仅类型兜底，**非债**。

## §三 定海（唯一真值源）

- 媒体类型判定真源 = `src/components/base/utils/assetType.ts` 的 `detectFileType` / `classifyAssetUrlKind` / `EXT_KIND`。任何文件不得另立 mime→kind 或 ext→kind 映射。
- 兜底式分类须穷举 `Record<K,V>`（TD-22-9 裁定），不得用 `x === 'a' ? A : B` 二进制静默归位。

## §四 切割（本次命中的内联副本）

| 文件:行 | 内联实现 | 与真源关系 |
| --- | --- | --- |
| `videoEditor/engine/lib/media/media-utils.ts:10` | `getMediaTypeFromFile` 完整复制 detectFileType 的 `image/→image / video/→video / audio/→audio / 否则 null` | 第二份（漏 `text/` 分支、与真源漂移） |
| `videoEditor/engine/lib/media/processing.ts:285` | `file.type.startsWith('video/') ? 'video' : 'audio'` | 二进制兜底静默归位（非音视频静默建 audio 元素） |
| `scriptBoxEngine.ts:1181` | `(file.name.split('.').pop() || 'png')` 扩展名提取，未知扩展静默兜底 `png` | TD-16-5 同母不同行（:1165 已登，本行未登） |

## §五 探债（债候选 + 归因 + 归属）

| TD-ID | 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-16-9 | `media-utils.ts:10 getMediaTypeFromFile` 完整内联复制 detectFileType 的 mime→kind 映射，绕过 assetType 唯一真值源 | 增债 | 结构债 | 3 处 fan-in；真源改 mime 映射时本条不跟 | 中 | 母体 M16（绕过 assetType 真值源） | 删 `getMediaTypeFromFile`，改调 `assetType.detectFileType`（对齐 `MediaType`/`AssetType` 口径，缺类补 `null` 分支） | 待还 |
| TD-16-10 | `processing.ts:285 getMediaDuration` 用 `video/ ? 'video' : 'audio'` 二进制兜底静默归位，非音视频文件静默建 `<audio>` 元素，失败可见性缺失 | 增债 | 结构债 | 上传非音视频文件时 duration 探测静默失败 | 中 | 母体 M22-9（兜底式静默归位）+ M16 | 改用 `detectFileType` 取 kind；非 image/video/audio 显式 early-return 而非静默建 audio | 待还 |
| TD-16-11 | `scriptBoxEngine.ts:1181` 内联 `split('.').pop() 或 'png'` 扩展名提取，未知扩展静默兜底 `png`，与 assetType 真值源口径错位 | 增债 | 结构债 | 无扩展名文件落盘名被静默当 png | 低 | 母体 M16（TD-16-5 同母不同行） | 复用 assetType 的扩展名真值源或抽 `getExt(name)` helper | 待还 |

观察（非债，留痕防重审）：
- `useAssetDropPaste.ts:448-450` / `AssetNode.tsx:235` / `AgentPanel.tsx:1052` / `ImageBoxNode.tsx:296,329` 的 `startsWith('image/')` 仅作拖入过滤，未做 kind 分类、未静默兜底，属可接受用法；记「已裁定 2026-09-16·非债」。
- `providerModels.ts:57` 经枚举取证为非债（见 §二）。

## §六 结论

TD-16 母体（绕过 assetType 唯一真值源的内联类型判定）横向推导发现 3 处未登记实例：两处完整/部分复制 mime→kind 映射（video 引擎），一处内联扩展名提取兜底 `png`。均属结构债、绑定根因区 16（TD-16-9/10/11），偿还方向统一为「收口到 `assetType.detectFileType` / 抽 helper + 穷举替代二进制兜底」。本次不改代码。

## 覆盖度表（增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `src/components/base/utils/assetType.ts` | 地基 | 深审（真源，无债） | 🟢 | 16-跨区-…-2026-09-16 | `assetType.ts:5` 自述唯一真值源 | 已裁定非债（根因，无债） |
| `src/components/videoEditor/engine/lib/media/media-utils.ts` | 上层 | 有债待还(TD-16-9) | 🔴 | 同上 | refs 3 fan-in；`media-utils.ts:10` | TD-16-9 |
| `src/components/videoEditor/engine/lib/media/processing.ts` | 上层 | 有债待还(TD-16-10) | 🔴 | 同上 | refs 活代码；`processing.ts:285` | TD-16-10（@见 16，文件属 22） |
| `src/components/scriptbox/scriptBoxEngine.ts` | 上层 | 有债待还(TD-16-11) | 🔴 | 同上 | `scriptBoxEngine.ts:1181` | TD-16-11（TD-16-5 同母不同行） |
| `src/hooks/useAssetDropPaste.ts` | 上层 | 已裁定非债 | 🟢 | 同上 | `useAssetDropPaste.ts:448-450` | 已裁定 2026-09-16·非债 |
| `src/components/director3d/project.ts` | 上层 | 已裁定非债 | 🟢 | 同上 | `project.ts:2021` = TD-16-6 已覆盖 | 已裁定（旧债覆盖） |

## §七 状态行

- 区域 16（utils 工具层）：`已验证(有债待还)` —— 本轮新登 TD-16-9/10/11（均结构债·待还），母体 M16 横向推导续扫。
- 区域 22（视频）：`已验证(有债待还)` —— `processing.ts` 症状 @见 16，债 ID 绑根因区 16。
- DATAFLOW：不涉及链路漂移，未改 `spec/DATAFLOW.md`（本轮为代码层 SSOT 债）。
- 覆盖账：本轮审 6 文件（含 1 真源深审 + 3 待还 + 2 非债裁定）；本区 16 共 N 文件未全审，余下未审清单见各轮次覆盖度表累计；本次不写「审完」。
