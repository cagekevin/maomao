# 00 · 跨区 · 前端 ext→mime 第二份横展（从历史 SSOT 收口债推导未登记盲区 · 2026-09-16）

> **触发**：用户「从历史已有债务深入推导，未登记 没有被发现的债务」。
> **种子母体**：`TD-02-41` 链引爆的 `M-SSOT第二份` —— `localTool/src/utils/mime.ts`（2026-09-11 收口，自陈「媒体 MIME ↔ 扩展名映射**唯一实现**」，原散落 6 处后端副本已并表）。
> **推导假设**：mime.ts 收口只覆盖了**后端**（node 运行时可互 import）；前端（浏览器运行时）需要 ext→mime 映射却**无法 import 后端模块**，于是各前端 util 手抄一份 → 必漂。验证：前端是否真有未被收口的手抄副本、且与 mime.ts 漂移。
> **上游（不重述）**：`16-跨区-静默吞母体横向推导复查`（TD-16-3）· `02-跨区-SSOT前后端常量横向推导` · `02-跨区-缩略图格式白名单收口`（TD-02-41）· `localTool/src/utils/mime.ts` 头注释（收口边界）。

---

## 一 · 链路快照（取证路径）

```
种子：mime.ts EXT_TO_MIME（后端 SSOT，含 tiff/svg/avif）
  └→ 全仓 grep `TO_MIME|MIME_BY_EXT|EXT_TO_MIME` 找第二份
       ├─ localTool/mime.ts:23            EXT_TO_MIME  （SSOT，合法）
       ├─ localTool/fileStore.ts:281      JIMP_MIME_BY_EXT（同运行时，但语义=能力表，L272-276 自证非描述表→非债）
       └─ src/.../imageCompress.ts:33     EXT_TO_MIME  （前端私有副本，与 SSOT 漂移←本轮命中）
  └→ 前端其它 ext→mime 手抄？ grep 仅 imageCompress 一处（assetType.EXT_MAP 是 ext→kind，异真相）
```

## 二 · 溯源（逐点实证，证据 = 文件:行 / grep / refs）

### 2.1 命中：imageCompress.ts 私有 EXT_TO_MIME 漂移（新债）

两表逐键比对（`imageCompress.ts:33-41` vs `localTool/src/utils/mime.ts:23-55`）：

| 扩展名 | imageCompress.ts | mime.ts（SSOT） | 判定 |
| --- | --- | --- | --- |
| `.tiff` | **缺** | `image/tiff` | 漂移：前端不识 tiff→`inferMime` 回 null→`keepOriginalFormat` 兜底 png |
| `.svg` | **缺** | `image/svg+xml` | 漂移：svg（矢量）被当未知→强转 png |
| `.avif` | **缺** | `image/avif` | 漂移：avif 被当未知→强转 png（丢现代格式收益） |
| `.jpe` | `image/jpeg` | **缺**（仅 `.jpg/.jpeg`） | 漂移：前端认 `.jpe`、后端 `extToMime('.jpe')` 回 `application/octet-stream` 前后不一致 |

- `imageCompress.ts:33` 为 `const`（未导出），仅供本文件 `inferMime`（:44-56）用；`keepOriginalFormat` 路径（:88-92）依赖它。
- debt.mjs search `EXT_TO_MIME` = **0 命中** → 全档（主表+归档）从未登记 → 确为**未登记盲区**。
- mime.ts 头注释（:11-13）明列收口豁免仅 `FRONTEND_MIME(woff2/ico)` / `handleDepthResource`（推理运行时），**未豁免 imageCompress** → 该副本不在收口边界内，属漏网。

### 2.2 排查：同源第二份是否还有其它（非债留痕）

| 候选 | 实证 | 判定 |
| --- | --- | --- |
| `localTool/fileStore.ts:281 JIMP_MIME_BY_EXT` | 同运行时（node）但 `:272-276` 自证：本表是「Jimp 能编码哪些」**能力表**（webp/avif/svg 故意不登记，因无编码器），与 mime.ts「描述性映射」语义不同；混用会产出「.webp 文件名+原字节」假图（TD-03-7 旧坑） | **非债**（有意设计＋注释固化，收口边界内） |
| `src/.../assetType.ts:37 EXT_MAP` | 是 ext→**AssetType(kind)** 真源，与 ext→mime 异真相；`detectFileType` 为唯一入口 | **非债** |
| `src/.../accountsStore.ts:397/439/444` 裸 localStorage | `chrome.scripting` 注入**目标站点** MAIN-world localStorage（跨站登录态切换），注释 `storage-raw-ok`：contentStore 物理不可达，非本仓存储 | **非债**（意图豁免） |
| `src/.../promptHubStore.ts:240-250 readCache` | L242 注释述「原 `as Record` 假收窄」已为 `isSourceCache`（:228-237）运行期校验取代；TD-05-10 已解决 | **非债**（已收口） |
| `src/.../export-button.tsx:72` `includeAudio` | L69-71 注释 `TD-22-38① 收口`：原 `|| true` 恒真已改直接引用常量 | **非债**（已收口） |
| `src/.../sounds-store.ts:13-18` 幽灵预留 | L13-17 注释「TD-22-47 清理」：22 个第三方搜索成员已整体删，仅留诚实本地库 | **非债**（已收口） |
| `src/hooks/useEdgeData.ts:16-21` 对称 API | L16-21 注释「2026-09-14 删掉三个零引用 setEdges 包装层」；属已删死代码说明 | **非债**（已删） |
| `src/components/base/utils/imageCompress.ts:24 MIME_TO_FORMAT` | mime→canvas 格式（jpeg/png/webp/gif/bmp），前端 canvas 专用，mime.ts 无对应反向段；属域专用 | **非债**（收口边界内，类比 FRONTEND_MIME） |

## 三 · 定海（唯一真源 / 契约）

- 通用媒体 ext↔mime 真源 = `localTool/src/utils/mime.ts`（后端）；前端因运行时隔离无法 import，须有自己的前端真源常量，而非各 util 私抄漂移副本。
- 能力表（Jimp 可编码）与描述表职责分离已成立（fileStore 注释固化），非债。

## 四 · 切割（本轮不审）

- 后端 mime.ts / fileStore 已健康（SSOT + 能力表分离）。
- 前端 `assetType.ts`(kind) / `MIME_TO_FORMAT`(canvas 格式) 各司其职，不归本推导。

## 五 · 探债（明细真源）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-16-12 | `src/components/base/utils/imageCompress.ts:33` 私有 `EXT_TO_MIME` 与 SSOT `localTool/src/utils/mime.ts:23` 漂移：缺 `.tiff/.svg/.avif`、多 `.jpe`；`inferMime`(:55)→`keepOriginalFormat`(:91) 路径对 svg/avif 强转 png、`.jpe` 前后端不一致 | 增债 | 结构债 | 1 文件（压缩保格式路径，边缘格式） | 低 | 母体 M-SSOT第二份（mime.ts 收口 2026-09-11 仅覆盖后端 6 处，前端手抄副本漏收口致漂；同源 TD-02-41） | 前端建一份 ext→mime 真源常量（如 `src/utils/extMime.ts` 或并入 `assetType.ts` 派生），收口 imageCompress 私有表；并挂闸（比对 localTool/mime.ts 键集）防漂。因浏览器无法 import 后端模块，偿还＝建前端真源而非 import | 待还 |

**非债留痕**（禁再当 bug 补）：§二 2.2 表 8 处（JIMP 能力表 / assetType / accountsStore 豁免 / promptHub 已收口 / export-button 已收口 / sounds-store 已收口 / useEdgeData 已删 / MIME_TO_FORMAT 域专用）。

## 六 · 结论 + 覆盖账

1. **新债 1 笔**（TD-16-12，归 16 区）：前端 `imageCompress.ts` 私抄 ext→mime 表漂移出 SSOT，是 `mime.ts` 收口（TD-02-41 同源）**漏掉前端副本**的复发实例。
2. **盲区价值**：SSOT 收口若只并表「可互 import 的同运行时」副本，会留下「跨运行时必须手抄」的副本持续漂移 —— 收口边界须显式列出前端真源，否则等于没收口。
3. **覆盖账**：本轮深审 9 文件（1 债 + 8 非债留痕）。未深审：mime.ts/fileStore 原已审（沿用）；`assetType.ts` 仅查 EXT_MAP 角色（未全量，下轮按需）。
4. DATAFLOW：无链路漂移，不改。

---

## 覆盖度表（本轮增量，不重抄旧行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `src/components/base/utils/imageCompress.ts` | L0 地基(utils) | 有债待还(TD-16-12) | 🔴 | 09-16 跨区 | :33-41 表 vs mime.ts:23-55 漂移；debt search EXT_TO_MIME=0 | TD-16-12 |
| `localTool/src/utils/mime.ts` | 后端 地基 | 已裁定非债 | 🟢 | 09-16 跨区 | :2-19 自陈 SSOT；:11-13 收口边界 | 非债 |
| `localTool/src/utils/fileStore.ts` | 后端 | 已裁定非债 | 🟢 | 09-16 跨区 | :272-276 能力表语义异于描述表 | 非债 |
| `src/components/base/utils/assetType.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 跨区 | :37 EXT_MAP 是 ext→kind 异真相 | 非债 |
| `src/components/base/store/accountsStore.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 跨区 | :438/444 storage-raw-ok 跨站注入豁免 | 非债 |
| `src/components/base/prompt/promptHubStore.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 跨区 | :228-237 isSourceCache 取代假收窄；TD-05-10 | 非债 |
| `src/components/videoEditor/ui/editor/export-button.tsx` | L3 UI | 已裁定非债 | 🟢 | 09-16 跨区 | :69-72 TD-22-38① 收口 | 非债 |
| `src/components/videoEditor/stores/sounds-store.ts` | L2 存储 | 已裁定非债 | 🟢 | 09-16 跨区 | :13-17 TD-22-47 清理 | 非债 |
| `src/hooks/useEdgeData.ts` | L1 hook | 已裁定非债 | 🟢 | 09-16 跨区 | :16-21 零引用 setEdges 已删 09-14 | 非债 |

---

## 七、结论与下一步

- 本区域状态：`已验证(有债待还)`（16 区，TD-16-12 新增待还）。
- 待还债项已流入 `债务.md`：是（TD-16-12，本轮 `debt.mjs add`）。
- DATAFLOW.md：已对账一致（无链路漂移，未改）。
- 灯：本区覆盖度表已更新（🔴 TD-16-12；其余 🟢 非债留痕）；区域级灯由 index.md 自动派生。
