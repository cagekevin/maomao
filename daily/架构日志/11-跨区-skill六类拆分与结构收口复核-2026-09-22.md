# 11-跨区 · skill「六类拆分 + 结构收口」复核（2026-09-22）

> **触发**：用户「他又几乎快做完了，根据现在的进度重新去审计，还是按一开始的方法。一定要全面」。
> **对象**：`11-跨区-skill六类混居拆分-2026-09-22.md` · `11-跨区-skill复核八笔结构收口-2026-09-22.md`（他对我
> `11-跨区-skill整改批复核-2026-09-22.md` 的处置）+ 模块 24 文件在 01:48–01:55 的改动。
> **判据源**：六条试金石 + 选落点三问 + **对方新写进 SOP 的第 8 条「判据适用面」** · `ADR-0044 §8`（门面按用例）·
> `ADR-0056 Q2`（磁盘是真相源）· `ADR-0053`（幽灵预留）· 本仓「一归 / 一凭证 / 一诚实」。
> **只读轮**：`src/**` / `localTool/**` 一行未改。**未跑测试**（用户裁定）；重跑的是 3 条静态闸。
> ⚠️ **读数时间戳**：本轮全部代码读数取自 **2026-09-22 02:0x–06:5x**。目标在动，结论请照此时间戳用。

## 一 · 覆盖账（先报读了什么）

| 范围 | 覆盖 |
| --- | --- |
| 本轮新增/改出的 7 个文件 | **全读**：`skillGroup`(76) · `skillPickerView`(73) · `skillInjectText`(140) · `skillBudget`(59) · `skillResourcePath`(77) · `skillDirName`(64) · `skillLibraryView`(441，全读) |
| 组合层与门面 | **全读**：`index.ts`(76) · `skillApi.ts`(165) · `skillInject.ts`(109) · `skillRegistry.ts`(92) |
| 资料读取与安全判据 | **全读**：`skillResource.ts`(108) · `skillResourcePath.ts`(77) · `skillDirName.ts`(64) |
| 消费方 | `SkillSettings`(toggleIds / kind 段) · `AgentPanel`(picker 段 :590-597) |
| 文档 | 模块 `README.md` §1–§5 全读 · `docs/plan/140` §2.1/§2.2/§3.2/P0~P1 施工单 · `docs/plan/141` |
| 账本 | `debt.mjs list --area 11 --all`（71 条）· `debt.mjs audit` · `债务.md` 主表 |
| SOP | `.codebuddy/commands/架构师改码7步法.md` Step 3（含新增第 8 条）· 我建的 skill `architect-7-steps/SKILL.md` |

**未覆盖（如实记）**：`localTool/**` 本轮只读过 `files.ts` 的越根守卫改动（上一轮已核），未重扫全部后端；
探针（`scripts/probe.mjs`）**未重跑**（他会注入再还原，与「不跑测试」相邻，且我的职责是复核结论不是复跑他的凭证）。

## 二 · 声明 vs 实况（逐条复核）

| 他的声明 | 复核 | 证据 |
| --- | --- | --- |
| 旧 `skillCatalog.ts` **真删了**（不是并列新增） | ✅ 真 | `ls` 无该文件；`src/**` 内仅剩 5 处**历史注释**提到它（`skillBudget:8` / `skillInjectText:10` / `skillResourcePath:8` / `skillDirName:7` / `skillGroup:4`），全部是"历史上它与另外四类一起住在…"的留痕，非引用 |
| 6 个新文件是**忠实搬家**（逻辑 0 新增） | ✅ 真 | 逐文件读过：`skillBudget`(59) / `skillInjectText`(140) / `skillResourcePath`(77) / `skillDirName`(64) / `skillGroup`(76) / `skillPickerView`(73) 的函数体与搬家前形态一致；`skillLibraryView` 507 → 441（减的就是被搬走那两类） |
| `skillGroup.ts` 是**两个用例共用**的家 | ✅ 真 | `skillLibraryView.ts:27-36` 与 `skillPickerView.ts:17` 同向 import；`SkillSettings:940/1280` 只比 `kind`，`AgentPanel:590` 明写"本面板不认识任何哨兵" |
| 闸 `SENTINEL_OWNER` 已指向新定义处 | ✅ 真 | `check-arch.mjs:2308` = `src/components/agent/skill/skillGroup.ts`；实跑该闸 ✅ |
| 结清 TD-11-64（入口名闸漏检） | ✅ **两半都做了** | ① 闸改大小写不敏感：`check-arch.mjs:2225` 的正则带 `/i`；② 两处改**派生**：`skillImport.ts:122` `const entryLower = SKILL_ENTRY_FILE.toLowerCase()`，`:125` `:148` 都用它比 ⇒ `src/**` 内已无 `'SKILL.md'` 代码字面量 |
| 结清 TD-11-65（云写回拉全量） | ✅ 真 | `skillCloudSync.ts:62` 改调 `readDiskSkillPackages()`（→ `listSkillPackages({withContent:true, onlySkillMd:true})`，`skillApi.ts:107`） |
| 结清 TD-11-67（判据两份） | ✅ 真 | 视图层 `toggleIds`（`skillLibraryView:388` 与 `toggleable` 同源）；`SkillSettings:447` 只转发 `group.toggleIds`，不再 `filter(state==='ok')` |
| 结清 TD-11-70（导出不对称） | ✅ 真 | `skillPersist.ts:385` `skillMarkdownForExport` 改读磁盘整包（`:392` `readSkillPackage(...'content')`），读不到 ⇒ `null` |
| 结清 TD-11-66（`listAllSkills` 吞失败） | ✅ 真（上一轮已核，本轮 mtime 未变） | 返回 `{ok,list,error}`；面板横幅 + 读失败不剔已选 id |
| 结清 TD-11-43（`handleOpenDir` 越根） | ✅ 真（上一轮已核） | 走 `resolveUploadFile` 原语，越根 400 |
| **TD-11-47 的另一半**（`skillInjectText` 那段注释是否仍与新口径矛盾） | ✅ **已改，非债** | `skillInjectText.ts:106-109` 明写"口径已于 TD-11-26 改…`room<=0` 现在是一点都不给"，与 `skillBudget.ts:56` 的 `limit<=0 ⇒ {content:'', truncated:true}` **一致** ⇒ 结案说法覆盖到了两处 |
| 机器凭证 `tsc 0` / `lint 0` / `check:arch` 全绿 | ✅ **这次为真** | 本轮重跑：`check:arch` ✅（跨栈对账 3 项 + 入口名闸 + 禁 exec + 哨兵闸全绿）· `npm run lint` **无输出（0 问题）** · `type-check` 见 §六 |

## 三 · 新登记债（探债 · 线索全）

> 本轮 5 笔**全是"描述/声明层"的债，没有一笔是功能错** —— 这不是巧合：他这一批把**代码**层面的判据
> 都落成了结构，而"声明面"没有跟着一起收口。这正是他自己刚写进 SOP 的第 8 条（**判据适用面**）的镜像：
> **改了实例（代码），没改规则（声明同一条判据的所有地方）**。

| ID | 位置（文件:行号） | 债的形态 | 归属 | 爆炸半径 | 利息 | 根因（母体?/孤例） | 大致解法 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **TD-11-72** | `skill/index.ts:5` vs `:6-19` vs `:21-22` | **门面头注释漂移**：写"当前承载**五类**用例"却列了 ①–⑦（**7** 类）；且"其余用例（云同步后重写磁盘 / 模型读资料）在实现它们的那一期加入本文件" —— 这两个的导出就在本文件 `:41`（`readSkillResource`/`setSkillTurnBindings`）与 `:42`（`applyCloudSkillsToDisk`） | agent/skill | 门面是域外**唯一入口**、也是新读者第一个读的文件；头注释与实际导出面不符 ⇒ 按注释判断"这个用例有没有出口"会判断错 | 低 | 孤例（与 TD-11-69 **完全同族**：改了实现没回改自称） | 就地回改：① 五类 → 七类；② 删掉"其余用例…加入本文件"这句（或改写为"用例随实现它们的那一期加入，本文件不留空壳"，去掉具体点名） | 待还 |
| **TD-11-73** | `docs/plan/140 §2.2:245` · 模块 `README §2`（`skillInject`=① · `skillResource`=⑤ · `skillUsage`=⑧ · `skillLibraryView`=①（新））· `index.ts:5-19`（①=渲染库 / ③=注入 / ⑥=读资料） | **用例编号三处互不一致**：README §2 内部**自相冲突**（`skillInject` 标 ①、`skillLibraryView` 也标 ①）；README 用的是 plan §2.2 的老编号，门面用的是另一套新编号 | agent/skill + 文档 | 用例编号是**跨文件引用的坐标**（轮次文件、ADR、README §4 都靠它定位）；三份手抄 ⇒ 任一处改了另两处静默不一致，引用失效 | 低 | 孤例（同一判据的第二份，M3/M4） | 定一套唯一（建议以门面 `index.ts` 为准，因为它就是导出面本身），README §2 与 plan §2.2 改成**指针式**引用（"编号见门面"）而不是各抄一份 | 待还 |
| **TD-11-74** | `skillGroup.ts:2`（头声明"…**排序**…"）vs `skillLibraryView.ts:401-405`（官方恒排最前）· `:411-415`（仅索引恒排最后） | **声明面 > 实现面**：`skillGroup.ts` 自称"分组的全部语义（唯一家）：… 排序 …"，但 `compareSkillGroups` **只认 `_未分类`**；"官方恒排最前"与"仅索引恒排最后"两条规则住在视图文件里 | agent/skill | 想改"官方组排第几"的人按头注释进 `skillGroup.ts` **找不到**，会以为没有这条规则而另写一份 ⇒ 判据两个出生地（正是本轮拆分要消灭的形态） | 低 | 孤例（本轮新代码里的边界不符，与 TD-11-72 同族） | 把排序位次收进 `skillGroup.ts`（如 `orderOfGroup(name)` 返回位次，`compareSkillGroups` 用它），视图只剩"按位次排 + 追加仅索引段" | 待还 |
| **TD-11-75** | `skillLibraryView.ts:244`（`enabledMap` **必填**）· `skillPickerView.ts:62`（传 `{}`）· `skillLibraryView.ts:203`（`state: SkillRowState = 'ok'`） | **视图入参允许"编值"，且缺省 = 危险值**：① 选用入口根本拿不到启用态，`enabledMap` 却必填 ⇒ 只能传 `{}` 表达"本层不判"；② `rowFromIndex` 的 `state` 有默认值 `'ok'` ⇒ 漏传即静默得到 `usable:true` + `editable:true` | agent/skill | 与本文件 `:195-197` **自己的注释直接矛盾**（那里明写"先写死 `ok` 再覆盖 = 先造错再覆盖，TD-11-56"），而默认值正好保留了那个 affordance；TD-11-71 只删了消费端的 `r.enabled` 过滤，**没动根因所在的签名** | 低 | 孤例（**缺省即危险值**，试金石 ③⑤：约束落在注释而非结构） | ① `enabledMap` 改可选（选用入口直接不传）；② `state` 去掉默认值、两处调用点显式传（`:261` 传 `'ok'`）⇒ 类型层不再允许"编一个值进来" | 待还 |
| **TD-11-76** | `docs/plan/140:287-302`（§3.2 纯函数规格块） | **§3.2 仍把 5 个函数挂在已删除的 `skillCatalog.ts` 名下，且无对账指针**：`buildSkillIndexText` / `truncateSkillContent` / `assertSafeSkillRelativePath` / `extractResourcePaths` / `SKILL_CONTENT_LIMITS` 实际已分住 `skillInjectText` / `skillBudget` / `skillResourcePath` | docs/plan | 同一次拆分，**§2.1 补了对账指针**（`:229-233` 明写"以 README §2 为准 + 拆到哪了"），**§3.2 没补** ⇒ 判据适用面只铺了一半：读者按 §3.2 去找 `skillCatalog.ts`，文件不存在 | 低 | 孤例（M4 描述层无对账） | 照 §2.1 的写法在 §3.2 补一句指针（"以上 5 个函数 2026-09-22 起分住 XXX，本条只留痕"），或删掉代码块只留指针 | 待还 |
| **TD-11-77** | `skillResourcePath.ts:71`（`if (!bare || !bare.includes('/')) return;`）vs `assertSafeSkillRelativePath`（`:41-55`，**允许** `style.md` 这类裸文件名）vs `README §3` 不变量 4（"只有正文里显式写出的路径才可读"） | **两个函数对"什么可读"的口径不一致**：提取侧要求路径含 `/`，安全侧允许裸文件名 ⇒ 写在包根的 `style.md` **正文里明明引用了却提取不到**；模型来读时 `skillResource.ts:85` 回「「X」正文里没有引用这个文件」—— **报错给了一个不成立的原因**（M5 近亲：失败信息说谎） | agent/skill | 模型据此**无法自纠**（它会以为自己路径写错，实际是被提取口径拒了）；且 README 不变量 4 的声明**比实现宽** | 低 | 孤例（与 74/76 同族：判据面／声明面没收口） | 二选一：① 去掉"必须含 `/`"这条（改用扩展名白名单控精度）；② 保留它但把报错改成"只收带目录的引用（如 `references/x.md`）"，并把 README 不变量 4 补成同口径 | 待还 |

> ⚠️ **六笔的共同点（给他的话，也给我自己）**：他刚把"**判据适用面 —— 这条判据还有哪些地方该一起改**"
> 写进 SOP Step 3 第 8 条，并配了自检"**我改的是实例，还是规则？**"。这六笔全部是**那条自检的命中项**，
> 只是方向分两路：
> - **前五笔（72–76）落在"声明面"** —— 他修的是**代码实例**，没改的是**描述同一条判据的所有地方**
>   （门面头注释 / 用例编号 / 组语义头声明 / 方案文件规格块 / 参数签名）⇒ 第 8 条目前只在"代码"这一个面上生效。
> - **第 6 笔（77）落在"代码面"但同根** —— 两个函数各自定义了一条"什么可读"的口径，彼此**没对账**，
>   而 README 不变量 4 又把较宽的那条当成了对外承诺 ⇒ 还是"同一条判据有几个地方写着它"这个问题，
>   只是这次那几个地方**都在代码里**。

## 四 · 非债裁定（判过但不算 · 留痕）

| 判据点 | 裁定 | 理由 |
| --- | --- | --- |
| `skillInjectText.ts:106-109` 注释 vs `truncateSkillContent` 新口径 | **非债** | 注释已随 TD-11-26 回改（明写"口径已于 TD-11-26 改"），且 `room<=0` 的解释与 `skillBudget:56` 逐字对得上 |
| `asText`（通用 `unknown→string`）住在 `skillLibraryView.ts:189` 却被 `skillPickerView.ts:15` import | **非债** | 它虽然是通用强制转换，但承担的是"`IndexRow` 逐字段取值口径只留一份"这件事（`:186-187` 注释明写，且"门面不转发"）⇒ 落点与它的**实际职责**一致；跨文件 import 是因为两个用例读同一个形状 |
| `skillCloudSync.ts:16` 头注释仍写"一次 `listSkillPackages`" | **非债** | 底层确实就是它（`readDiskSkillPackages → listSkillPackages`），只是没点出 `onlySkillMd`；语义未被证伪 |
| `skillApi.ts:2` 头写"不进 `index.ts` 门面"，而 `index.ts:47-52` 从它导了 4 个符号 | **非债** | 那句指的是**模块路径**不对外（域外禁引 `skill/skillApi.ts`），不是"本文件的符号不进门面"；与门面纪律一致 |
| `skillPickerView` 传 `enabledMap:{}` 算不算"编造输入" | **并入 TD-11-75**（不单列） | 行为正确且注释诚实（`:60-61` 明写"本函数不读它"），问题在**签名逼人编值**而不是这一处传值；单列会把根因挤掉 |
| `skillRegistry.ts:32` 还留着 `const str = (…)`，与 `skillLibraryView.ts:189` 导出的 `asText` **逐字同形** | **非债**（消它比留着更贵） | 他那轮"批量改名 `str(`→`asText(` 只改调用点没改定义行"的弯路（见 `六类混居拆分` §六 弯路 2）**没收干净** —— `skillRegistry` 这份从未参与改名。但要消它，得让 `skillRegistry` import `skillLibraryView` ⇒ **组合层依赖视图层**，比留着这 1 行更糟（真要收，应把它挪去两者都该认的中立处，属"为整洁做扰动"，现在不做） |
| `skillResource.ts:71` 的歧义报错在 `ref` 为空时输出「本轮有多个 Skill 匹配「」」 | **非债**（文案退化，信息仍完整） | 「」是空的，读着别扭；但它同时给出了**候选列表**（`describe(pool)`）与**怎么做**（请用 id 指明）⇒ 模型能自纠，不构成"失败不可见"。与 TD-11-77 不同族：那条是**原因说谎**，这条只是**修辞退化** |
| 账本 `debt.mjs audit` 报 3 条锚点断链（TD-01-27 / TD-02-67 / TD-04-55） | **非本轮、非本区**（只报不修） | 三处锚点文件（`01-跨区-前端persist…` / `02-跨区-孤儿回收…` / `04-跨区-node.data…`）内**一个 TD-xx 号都没有** ⇒ 是历史遗留，与 2026-09-22 的 skill 施工无关；归属区 01/02/04 |
| `docs/plan/141`（列表即磁盘）与 141 的"未收口"标注 | **上一轮已核（✅）** | 本轮 mtime 未变，不重复取证 |

## 五 · 覆盖度表

| 点位 | 覆盖 | 灯 | 债/裁定 |
| --- | --- | --- | --- |
| `index.ts`（门面） | 深审（全读 76 行） | 🔴 | **TD-11-72**（头注释漂移） |
| `skillGroup.ts` | 深审（全读 76） | 🟡 | **TD-11-74**（头自称"排序"但只认 `_未分类`）· 62/68 已修 ✅ |
| `skillLibraryView.ts` | 深审（全读 441） | 🔴 | **TD-11-75** · 44/56/67 已修 ✅ |
| `skillPickerView.ts` | 深审（全读 73） | 🟡 | TD-11-71 已修 ✅（只按 `usable`）· 55 已修 ✅（真传 `diskSnapshot`） |
| `skillInjectText` · `skillBudget` · `skillDirName` | 深审（全读） | 🟢 | 26/59/60 已修 ✅ · 47 另一半非债 |
| `skillResourcePath` · `skillResource` | 深审（全读） | 🔴 | **TD-11-77**（提取口径 vs 安全口径不一致 ⇒ 假报错） |
| `skillInject` · `skillRegistry` | 深审（全读） | 🟢 | 66/51 已修 ✅（`listAllSkills` 真返 `{ok,list,error}`、冻结只吃 id） |
| `skillApi.ts` · `skillCloudSync.ts` | 深审 | 🟢 | 39/65 已修 ✅ |
| `skillPersist.ts` | 深审（导出段） | 🟢 | 70 已修 ✅ |
| `SkillSettings` · `AgentPanel`（skill 段） | 抽查（toggleIds / kind / picker） | 🟢 | 67 已修 ✅ · 面板不认哨兵 ✅ |
| `docs/plan/140` | 深审（§2.1/§2.2/§3.2/P0~P1） | 🔴 | **TD-11-73**（编号）· **TD-11-76**（§3.2 无指针） |
| 模块 `README.md` | 深审（§1–§5） | 🟡 | **TD-11-73**（§2 内部编号冲突）· §5 状态行属实 ✅ |
| 账本 | 深审（71 条 + audit） | 🟢 | 11 区只剩 TD-11-63 待还；3 条历史断链非本区 |

## 六 · 收尾自检

| # | 项 | 结论 |
| --- | --- | --- |
| 1 | `src/**` / `localTool/**` 一行未改？ | ✅ 只写本文件 + 账本 + 我自己的 skill |
| 2 | 每笔债挂证据？ | ✅ 全带 `文件:行号`；5 笔均可复算 |
| 3 | 判了母体还是孤例？ | ✅ 6 笔全孤例（**未编造母体**）；但都注明"与 XX 同族"（72↔69、73↔M3/M4、74↔72、75↔71、76↔64/65/68、77↔74/76） |
| 4 | 全程没跑测试？ | ✅ 测试 0 次；重跑 3 条**静态闸**：`check:arch` ✅ · `lint` 0 问题 · `type-check`（见下） |
| 5 | 主动纠正自己的错判？ | ✅ 上一轮我把 `skillInjectText:106-109` 列为"待核的 TD-11-47 另一半"，本轮读完判定**已改 ⇒ 非债**（见 §四） |
| 6 | 判非债也留痕？ | ✅ 7 条（§四） |
| 7 | 目标在动时的自保动作 | ✅ 开场读到的模块 mtime 是 **01:48–01:55**（比我上一轮读数新）⇒ 全部按当前磁盘重读一遍再下结论；结论均标时间戳 |
| 8 | 我自己的产物有没有同样的病？ | ⚠️ **有，且已修**（见 §七）：我建的 skill `architect-7-steps/SKILL.md` 缺 SOP 新增的第 8 条「判据适用面」 |

**机器凭证（本轮重跑）**：

- `npm run check:arch` → **✅ 架构校验通过**（跨栈对账 3 项全等 · 入口名闸 · 禁 exec/execSync · 组哨兵闸）
- `npm run lint` → **无输出（0 问题）**
- `npm run type-check` → **退出 0、无输出（0 错）**
- **未跑任何测试**（用户裁定）；探针 `scripts/probe.mjs` 亦未重跑 —— 复核的是他的结论，不是复跑他的凭证

## 七 · 结论与下一步

- **总判定：这一批（六类拆分 + 八笔结构收口）是真的做完了，且做对了。** 我逐条复核的 12 项声明**全部为真**
  （旧文件真删 · 搬家忠实 · 组语义真成两个用例共用的家 · 闸 owner 真跟着定义走 · 64/65/67/70 两头都改了 ·
  47 另一半已收口）。**没有发现任何功能错或逻辑错。**
- **剩下的 6 笔（全低息）**：**5 笔是"声明面没收口"** —— 门面头注释（72）· 用例编号三份（73）·
  组语义头声明（74）· 参数签名允许编值（75）· 方案文件 §3.2 无对账指针（76）；
  **1 笔是"代码面两个函数没对账"**（77：`extractResourcePaths` 要求含 `/` vs `assertSafeSkillRelativePath` 允许裸文件名
  ⇒ 包根资料读不到、且模型收到**原因说谎**的报错）。**77 是本轮唯一可能影响使用体验的一笔**（其余 5 笔都不影响功能）。
  ⇒ **这正好是他自己刚写进 SOP 的第 8 条的命中项**：第 8 条目前只在**代码面**生效，还没覆盖**声明面**。
  建议第 8 条补一句："这条判据的**声明**（头注释 / 编号 / 规格块 / 类型签名）在哪些地方也写着同一件事？"
- **他这轮唯一"自己挖自己"的动作值得记一笔**：§三 与 §二 各有一条"探针没红 ⇒ 先去查测试"的弯路
  （自证式断言 / 桩不忠实），两条都归因到"判据落在哪一层，断言就必须打到那一层"。这是方法级进步。
- **我自己的产物也有同一条病，已修**：SOP `架构师改码7步法.md` 在 01:35 加了 Step 3 第 8 条「判据适用面」，
  而我 01:08 建的 skill `architect-7-steps/SKILL.md` **只有 1–7 条** ⇒ 同一套判据两份且不一致。
  本轮已把第 8 条同步进 skill（含自检"我改的是实例，还是规则？"）。
  ⇒ 这条**不进账本**（当场修完），只为留痕：它证明"声明面没收口"这条对**我也成立**。
- **账目**：本轮新登记 **6 笔**（TD-11-72 ~ 77，全孤例、全低息）；11 区账本状态不变 ——
  已解决 70 条，**只剩 TD-11-63**（面板 `.md` 导入路径不可达，`[待还]`，等用户一句"接不接"）。
- **本区域状态**：**已验证(有债待还)**。
- **优先处置序**：`77`（**唯一影响使用体验**：改报错口径或放宽提取，二选一）→ `72`（门面头注释，一行）→
  `73`（编号，定一套 + 两处改指针）→ `74`（排序位次归位）→ `75`（签名，可选化 + 去掉默认值）→
  `76`（方案文件补指针）。除 77 外都不影响功能，可随轮次清。
