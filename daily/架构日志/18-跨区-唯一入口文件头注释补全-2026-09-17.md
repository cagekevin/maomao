# 18 跨区 · 唯一入口文件头注释补全（高价值 SSOT 体检）— 2026-09-17

> **触发**：@command://债务登记5步法 子任务——「检查高价值的，找出实际唯一但代码顶部无注释者，直接补注释（加注释非改码）」。
> **结论唯一落点 = 本文件**；本任务为注释体检，非新债登记。

---

## §一 · 范围（Step 0/1）

聚焦高价值债形中的 **唯一入口 / SSOT 第二份** 这一条：逐一对 `CLAUDE.md §5.4(9)`「已收口先例」列明的唯一入口做"是否已在文件头声明唯一性"体检。不审逻辑、不跑测试，只核实声明完整性。

## §二 · 取证（Step 2 · refs + grep 实据）

| 检查对象 | 取证方式 | 结果 |
| --- | --- | --- |
| `core/utils.ts` 是否唯一真源 | `refs` → **39 处 fan-in**；`grep "export function clamp\|export function deepClone"` → 全仓**各仅 1 处定义**（base/core/utils.ts:160 / :96） | 是。其余 `clamp*`（clampPlaybackRate/clampKeyframeFrames/clampZoom/clampTrackHeightScale/clampInt）为域专用、签名/语义不同，非第二份（与 TD-18-6 结论一致 → **判非债**） |
| `assetType / idGen / contentStore / config / filesApi / nodeImage` | 读真实代码文件头 | 均有【唯一真值源/唯一入口/横切权威入口/单一来源/全站文件域单点/主图唯一写入口】横幅 ✅ 已注 |
| `contracts.ts` | 读文件头（1-28 行） | 有「唯一性声明 / 单一事实来源」横幅 ✅ 已注 |
| `useNodeRename / useNodeExpanded` | 读 function 级 JSDoc | 「节点 data 写回唯一实现」/「单一实现」✅ 已注 |
| `useNodeData`（`patchNodeDataById`） | 读 function 级 JSDoc | 「节点 data 写回唯一入口」✅ 已注 |
| `useNodeField` | 读 function 级 JSDoc | 仅写「统一实现」，未显式"唯一/禁止旁路" ⚠️ 措辞偏弱（见 §三） |

## §三 · 探债（Step 3/4 · 明细）

| ID | 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| @见 TD-18-6 等 | `core/utils.ts` 缺文件头【唯一入口】横幅（39 fan-in，clamp/deepClone 全仓唯一定义却顶部无声明） | 持平（注释缺失） | 结构 | 全仓通用工具调用方 | 中 | 收口时只补了 function 级注释，漏补文件头 | 补文件头横幅 | 已解决 |
| @见 | `useNodeField` 顶部注释写"统一实现"，未显式"唯一/禁止旁路"，与 CLAUDE.md §5.4(9) 及兄弟 hook 表述不一致 | 持平（注释偏弱） | 结构 | node 字段写回链路 | 低 | 历史措辞遗留 | 强化为"唯一实现/禁止旁路" | 已解决 |

> 证据闸：两处结论均挂 `refs`/真实代码行号（core/utils.ts:160/:96；useNodeField.ts:5）。本任务为注释补全，**未改任何逻辑**、**未跑测试**（审计禁令）。

## §四 · 覆盖度表（Step 0/4.3 增量）

| 文件层 | 状态灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- |
| `core/utils.ts` | 深审(无债) | 2026-09-17 | refs 39 fan-in + grep 单定义 | 已裁定非债（补注释，非逻辑债） |
| `useNodeField.ts` | 深审(无债) | 2026-09-17 | 读真实代码 | 已裁定非债（措辞强化，非逻辑债） |
| `assetType/idGen/contentStore/config/filesApi/nodeImage` | 深审(无债) | 历史轮次 | 文件头横幅 | 已裁定非债（已注） |
| `contracts.ts` | 深审(无债) | 历史轮次 | 文件头横幅 | 已裁定非债（已注） |
| `useNodeRename/useNodeExpanded/useNodeData` | 深审(无债) | 历史轮次 | function 级 JSDoc | 已裁定非债（已注） |

## §五 · 结论（Step 3.4）

本轮为"唯一入口注释体检"，**非新债登记**。高价值 SSOT 唯一入口绝大多数已在文件头/函数头声明唯一性；仅 `core/utils.ts` 缺文件头横幅（已补），`useNodeField` 措辞偏弱（已强化）。**无新增待还债**。

## 七 · 本区域状态

- 本轮状态：**已验证(无债)**（仅注释补全，未引入逻辑变更；src 逻辑 0 改动，仅增注释）。
- 覆盖账：审 9 个唯一入口文件 / 共 9 / 未审 0。本轮为注释体检，不新增区域覆盖。
- 全程未跑测试（审计禁令，测试执行次数 = 0）。
- 未发现新债；`core/utils.ts`、`useNodeField.ts` 注释已补全。
