# 18 · 跨区 · ADR 判据合规横推 · 三轮（2026-09-18）

> 触发：用户「根据 ADR 再查还有哪些违反 ADR 的」。
> 范围：一轮/二轮遗留的 6 条未审 ADR —— **0012 / 0016 / 0019 / 0021 / 0027 / 0030** × `src/**` + `localTool/**` 实况。
> 方法：逐条取「📌 违反时的判据」→ 转成可复现检索 / `refs` 取证 → 命中处回读真实代码定性；ADR-0030 追加 `refs` 查 fan-in。

## 一 · 探债

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话) | 状态 |
| -- | ---------------------- | -------- | ---------- | -------- | ---- | ---------------- | ---------------- | ---- |
| TD-18-21 | `agent/runtime/agentRuntime.ts:294`（`const rawText = await res.text().catch(() => '')`）；下游 `:307-337` `content = msg?.content ?? json?.data?.text ?? rawText ?? ''` | **ADR-0019 形态①②**：`res.text()` 读体失败被 `.catch(() => '')` 静默吞 → `rawText=''` → `safeParseNonStreamJSON('')` 返 null → `content=''` → 用户收到**空回复、零日志、零报错** | 结构 / 11 AI 助手 | 非流式整条回复静默消失，排障只能看到"收不到回复" | 中 | 孤例（残留静默 catch；同文件 `:290-292` 注释自承「绝不静默丢内容」却只修了 `res.json()` 那条，`res.text()` 这条未覆盖） | 读体失败改 `logger.error` + 显式抛错/标记，勿归并成 '' | 待还 |
| TD-18-22 | `base/api/httpClient.ts:224`（`const data = res.ok ? await res.json().catch(() => ({})) : await readErrorBody(res)`） | **ADR-0019 形态② + ADR-0002（压平无留痕）**：2xx 响应体非法 JSON（空体/代理 HTML/崩溃页）被静默成 `{}` 下发作成功；`catch` 零日志 | 结构 / 18 core（api 原语） | 全仓 fetch 2xx 解析失败被当成成功，下游拿到空 data | 中 | 孤例（中央 HTTP 原语的历史静默 catch 残留；`:220-223` 注释只护了非 2xx 的 `readErrorBody`，这条 2xx catch 漏了） | 2xx 解析失败改 `logger.warn` + 抛 `HttpError`，与 `:225` 非 2xx 同口径 | 待还 |
| TD-18-23 | `director3d/App.tsx:635`（`referenceCanvasForExport(...).catch(() => { if (active) setMonitorReferenceBackground(null); })`） | **ADR-0011 判据3 + ADR-0019 形态①**：导出参考图画布渲染失败被静默吞，无日志、无报错，用户只见背景消失 | 结构 / 07 3D | 监控参考背景渲染失败不可见，无法归因 | 低 | 孤例（无 `catch-ok` 标记、无注释说明为何可吞） | 失败处补 `logger.warn`；背景清空保留但须留痕 | 待还 |
| TD-18-24 | `localTool/src/routes/files.ts:160`（`handleUploadFormData` `await recordUploadedFileRow(...)` 无 try/catch）· `:246`（`handleUploadJson` dataUri 分支同） | **ADR-0012 判据3（部分违反）**：「登记行失败」未与「下载/落盘失败」分开归因、缺「登记失败」专用留痕；对照 `:295-304`（remote 路径）已正确分因并 `uploadLog(500,…)` + 返回 500 | 结构 / 08 后端（files 路由） | form-data / dataUri 两条落盘路径：登记 resource 行抛错时被外层兜底成泛化上传失败，归因错、无专用迹 | 低 | 孤例（三处落盘路径口径不一致；remote 路径已正确，另两处漏了） | 两处 `recordUploadedFileRow` 包 try/catch，复用 `:301-303` 的「登记失败 500 + 留痕」写法 | **已结清 2026-09-19**（描述半真，见下） |

> ⚠ **【已修正 2026-09-19 · 回改原文（A7）】TD-18-24 的现象描述只写了「无分离归因与留痕」，**漏了同处更硬的一半**：
> multipart 分支**另手写了一份相对路径解析**（`startsWith('/files/')` + `replace(/^\/files\//,'')`），
> 是 `relativePathFromFileUrl` 的**第二份实现**。故该债不只是"补 try/catch"，而是**收口为唯一实现**。
> 已落 `registerUploadedRowOrFail`（解析 + 登记 + 分离归因 + 专用留痕），3 处调用点全部委托。
> 结论落点：`18-跨区-ADR0019静默吞母体五笔清偿-2026-09-18.md` §二。
| TD-18-25 | `videoEditor/engine/lib/tts/service.ts`（整文件）· 证据：`refs` → **模块引用 0 处**；`:37` `throw '语音生成（TTS）待接入…'`；`:39` `eslint-disable-next-line no-unreachable -- 保留原实现骨架，待接入时启用` + `:40-54` 不可达骨架 | **ADR-0030 违反**：fan-in=0 的生产实现＝假接缝（幽灵预留），且「为将来铺路」预留骨架；`check:dead-code` 基线未报（已在基线内） | 结构 / 22 视频（localTool 出站未接） | 死代码基线被占一格；将来接 TTS 时有人会误以为这份骨架可用 | 中 | 孤例（诚实占位 throw 本身合规，违规的是其下不可达「预留骨架」） | 删 `:39-54` 不可达骨架，仅留诚实 throw 占位（或接 localTool `/api/tts` 后整体启用） | **已结清 2026-09-19**（前提部分失真，见下） |

> ⚠ **【已修正 2026-09-19 · 回改原文（A7）】**本行证据栏「`refs` → **模块引用 0 处**」是**假象**：
> `mv-sync-refs.mjs` 的别名基址算错一层（`<root>/src` + `src/` = `<root>/src/src`）⇒ 全库 `@/…` import
> 被静默丢弃、fan-in **系统性少报**（见 **TD-17-19**，轮次文件 `17-跨区-refs工具别名基址与默认后缀-2026-09-19.md`）。
> 修复后真实 fan-in：`generateAndInsertSpeech` = **1**（`text-speech-panel.tsx:20`），它在 `:103` 再调
> `generateSpeechFromText` ⇒ **后者是活的内被调函数，不是"没人接的假接缝"**。
> 但**违规点依然成立**：其下那段**永不可达**的"预留骨架"是死代码，且带着 `asyncGuard` 原语之外
> **最后一处**手写 `PARSE_FALLBACK`（一枚永不执行的标记，污染普查口径）。已删骨架 + 其专属 helper
> `base64ToArrayBuffer`，`src` 手写 `PARSE_FALLBACK` **1 → 0**；诚实 `throw` 保留并补 2 条防回潮锁。
> 明细见 `18-跨区-TD18-25TTS幽灵预留证伪与清偿-2026-09-19.md`。

## 二 · 已裁定非债（留痕，禁再当 bug 补）

| 对象 | 判据 | 结论与证据 |
| ---- | ---- | ---------- |
| ADR-0021 禁 `as`/! 绕判别联合 | 判据 1/2/3（grep `as unknown as` 全仓 42 处） | **合规**：38 处 `src` + 4 处 `localTool` 全部落在 three.js / `useThree` controls / xyflow `Node`·`Edge` / React `RefObject` 三方库**类型缝隙**（如 `Viewport.tsx:94,310` · `App.tsx:257,688` · `useScriptBoxEngine.ts:113`），与 agent 模块边界单点收窄（`agentRuntime.ts:232,236` `ApiChatMessage`/`parseAgentError` 双同名类型 seam，`useAgentChat.ts:768`→`Record`，均带注释「单点收窄、不扩散 cast」）；**零处**落在 boolean 判别联合 `{ok:true;value}\|{ok:false;error}` 消费点（`degrade.ts` `tryParseOr` 契约形状已按 ADR-0021 形状①收口）。故判非债，了结一轮「未审」项 |
| ADR-0016 闸不许凌驾正确性 | 判据 1/2/3 | **合规**：`scripts/gates.manifest.json`（2026-09-18 已专项审计）14 道在册闸全部带 `★闸的申诉口` 三问；删 `check-token-mirror`/`check-css-globals`（从未跑 + 假防线）/ 删 `check-gate-vitals` 残留登记；`catch` 闸已按 ADR-0011 删除。无「为变绿抄 N 份」「改声索不补覆盖」形态。判非债 |
| ADR-0027 uploads 分域 | 判据 1/2/3/4/5 | **合规**：前后端分域表一致——前端 `uploadDirs.ts::UPLOAD_ROOT_KIND`/`KNOWN_SUB_DIRS`（`canvas/drop`·`canvas/video-process`·`canvas/video-editor`·`canvas/face_mosaic`）＝ 后端 `fileStore.ts::SUB_DIR_ALLOW`（`:96-101`）；`normalizeSubfolder`（`:119-135`）系统产物根未登记子目录 ⇒ `null` → `resolveUploadTarget` `throw 400`（不再静默回退 `:149`）；代码字面量扫描（`'canvas/[a-zA-Z_]+'`/`'tasks/'`/`'web/'`/`'director3d/'`）未现表外 subfolder；闸 `check-upload-dirs.mjs` 覆盖「值域⊆登记表」。判非债 |
| ADR-0012 写盘判据（dedup/subfolder/rescan 三项） | 判据 | **合规则（三项已治）**：`writeUploadDedupSync`（`fileStore.ts:304-316`）命中去重补磁盘 `existsSync` 校验、不在则 `console.warn` 重落盘；`normalizeSubfolder` 非法子目录 `throw`（`:149`）；`rescan` 静默 return 项已随孤儿目录治理消失（files.ts 无 `rescan` 残留）；仅 `recordUploadedFileRow` 归因缺口见 TD-18-24 |
| `projectMemoryStore.ts:71` `next.catch(() => {})` LOCK_CHAIN（一轮抽审项） | ADR-0011 判据3 | **合规（保护性 catch，非吞错）**：`withLock` 先 `return next`（原始可能 reject 的 promise 回传调用方，失败对调用方可见），`locks.set(key, next.catch(() => {}))` 仅保护**锁链 bookkeeping promise** 不产生 `unhandledRejection` 崩溃；catch 吞的是"锁链维护"而非"操作结果"。了结一轮抽审。**【2026-09-19 补充 · 回改原文（A7）】**TD-18-27 曾把本处**与 `:77-80` 一并**登记为债；复核后**本处裁定维持（非债）**——真债在 `:77-80`：那里是 `try { next.finally(…) } catch {}`，**`try/catch` 包不住 promise rejection** ⇒ `next` 失败时 `finally` 的**派生** promise 无人处理 ⇒ **真的会产生 unhandledRejection**（即本行担心的那件事，恰恰发生在**另一处**）。另同根因：`locks.set(key, next.catch(…))` 存的是派生 promise，而 finally 里比较 `locks.get(key) === next` **恒假** ⇒ 清理分支是死代码。已一并修，见 `18-跨区-ADR0019静默吞二轮-锁链假护栏与零留痕-2026-09-19.md` |
| `agentCore.ts:652` `res.text().catch(() => '')` | ADR-0019 形态① | **边界·判非债**：`text=''` 后 `tryParse(text)` → `r.ok=false` → `logger.warn('错误响应体非 JSON', …)`（`:657`），失败**有迹**（虽被误标为"非 JSON"而非"读体失败"）；可见即不算债，仅标记轻微误标，不单列债 |
| `tts/service.ts:47` `response.json().catch(() => null)` | ADR-0019 形态①② | **不可达死代码（随 TD-18-25 一并处置）**：位于 `:39` `eslint-disable no-unreachable` 之后的不可达骨架内，永不执行；`:48` 已 `throw` 带 `error?.error` 兜底文案，失败可见。不单列债。**【已处置 2026-09-19】**整段不可达骨架连同本行标记**已删**（含其专属 helper `base64ToArrayBuffer`）—— 正解是**删**而不是本表下方 §四第 7 行写的"改调 `tryParse` 原语"（那会救活一段死代码）。见 `18-跨区-TD18-25TTS幽灵预留证伪与清偿-2026-09-19.md` |

## 三 · 覆盖度表（增量 · 本轮新纳入 / 状态变化）

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| ------ | ---------- | --------- | --------------- | ------- |
| `src/components/agent/runtime/agentRuntime.ts` | 有债待还(TD-18-21) | 本轮 | `:294` `:307-337` | 读体失败静默吞 |
| `src/components/base/api/httpClient.ts` | 有债待还(TD-18-22) | 本轮 | `:224` | 2xx 解析失败静默成 {} |
| `src/components/director3d/App.tsx` | 有债待还(TD-18-23) | 本轮 | `:635` | 参考图渲染静默吞 |
| `localTool/src/routes/files.ts` | 有债待还(TD-18-24) | 本轮 | `:160` `:246`（对照 `:295-304`） | 登记行归因缺口 |
| `src/components/videoEditor/engine/lib/tts/service.ts` | **已结清(TD-18-25, 2026-09-19)** · 新增 TD-18-26(`ephemeral:true` 待还) | 本轮 | `refs`→0 引用（**工具 bug 假象**，见 TD-17-19；真实 fan-in=1） `:37` `:39` | 幽灵预留（仅不可达骨架成立） |
| `src/components/agent/runtime/agentCore.ts` | 已裁定非债 | 本轮 | `:652` `:657` | 误标但有迹 |
| `src/components/agent/runtime/projectMemoryStore.ts` | 已裁定非债 | 本轮（了结一轮抽审） | `:66-82` | LOCK_CHAIN 保护性 |
| `src/components/base/utils/uploadDirs.ts` | 已裁定非债 | 本轮 | 全表 + `isKnownUploadDir` | ADR-0027 合规 |
| `localTool/src/utils/fileStore.ts` | 已裁定非债 | 本轮 | `:96-135` `:304-321` | ADR-0012/0027 合规（dedup/subfolder） |
| `scripts/gates.manifest.json` | 已裁定非债 | 本轮 | 全表 | ADR-0016 合规 |

## 四 · DATAFLOW 对账

- 无链路漂移（未改动 `spec/DATAFLOW.md`）。TD-18-21/22/23/24/25 均属**判据层 / 错误透传层**内容，按 ADR-0025 归 ADR 与代码注释，不落 DATAFLOW。

## 五 · 覆盖账

- 本轮审 **10** 个文件/清单：有债待还 **5**（TD-18-21~25）；已裁定非债 **5**（含 2 处了结一轮抽审：projectMemoryStore LOCK_CHAIN / agentCore 误标）。
- 本轮**闭合**一轮/二轮全部「未审」ADR：**0012**（仅余 TD-18-24 一项归因缺口）／**0016**／**0019**（新扫出 3 处静默 catch）／**0021**／**0027**／**0030**（新扫出 tts 幽灵预留）。
- 仍未审（**0 条**）：上一轮遗留 6 条未审 ADR 本轮全部定性，无剩余未审。
- 说明：ADR-0023/0024/0025（判据/内容/机制落点）由 TD-18-20（CONTEXT 判据未归位）覆盖，本轮不重复。

## 六 · 状态行

- **状态**：已验证(有债待还) · 🔴
- **母体**：无新增母体（5 笔均为孤例；TD-18-20 的 CONTEXT 判据未归位仍为既有母体，跨轮持续）
- **待用户拍板**：无（5 笔均结构债，架构师可自定）
- **本轮零代码改动**（只读审计）· **测试执行次数 0**
