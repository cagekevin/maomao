# 架构日志 · 05 创作库 · 预设悬停显示提示词（用户裁定 + 施工）· 2026-09-19

> **触发**：承接 `05-创作库-预设悬停显示提示词-2026-09-18.md`（TD-05-19 登记待拍板）。
> 用户裁定：**「MJ 码图不用显示，其他都要显示」**。
> **性质**：**业务债施工轮**（Step 5 已拍板 → Step 6 施工）。
> **结论落点**：本文件（唯一写全处）。
> **一句话**：卡片悬停标题由「只给名字」改为「名字 + 提示词正文」；**MJ 天然不在本组件**，
> 故**不需要任何 kind 判断**（一行改动，无分支）。

- **本区域状态**：`已验证(无债)` —— **TD-05-19 结清**。

---

## 一 · Step 0~1 取证（复核上一轮描述，两条修正）

| 上一轮描述 | 复核结果 |
| --- | --- |
| `:41 title={p.name}` + `:82-84` 显示 `p.name` | ✅ **属实** |
| `p.prompt` 数据已存在 | ✅ 属实（`creativePresets.ts:58` **必填** `prompt: string`；`creativeCatalog.ts:92/106` 归一化）|
| 「**部分很长**，原生 title 会被截断」 | ❌ **证伪** —— 实测 **285/285 全有值**，长度 **min 21 / 中位 54 / 均值 60 / max 97**，**>200 字占 0%** ⇒ 原生 tooltip **完全放得下** |
| 「4 类全部预设卡片（778 项）经此组件渲染」 | ❌ **证伪** —— `CreativeLibrary.tsx:34` 明写「主网格 **3 分区**；**mj / prompt 各有专属视图，不走 PresetGridView**」|

> **为什么这两条修正决定了实现方案**：
> ① 「不截断」⇒ **不需要自定义 tooltip 组件**（它是为长文本设计的，此处用不上）；
> ② 「MJ 不走本组件」⇒ 用户要求的「MJ 不显示」**由结构天然满足**，**不必写 `kind` 判断**
> （写判据 = 在错误的位置解决问题；结构已保证的，不要再用条件分支重复表达）。

## 二 · Step 4~6 定契与施工

**契约**：卡片 `<article title>` = **名字 + 换行 + 提示词正文**；名条（`.cl-name`）**仍只显示名字**。

| # | 位点 | 改动 |
| --- | --- | --- |
| ① | `PresetGridView.tsx:41` | `title={p.name}` → `` title={`${p.name}\n${p.prompt}`} `` + 6 行注释（含取证结论）|
| ② | `tests/unit/presetGridHover.test.tsx` | **新增**（2 条锁）：① title 含 prompt；② 名条**不含** prompt |

**为什么名条不改**（用户未指定，架构师判）：名字用于**识别**、prompt 是**内容**，分工不同；
用户要的是「悬停可见」（title 已满足），不是「一眼可见」（那要改布局，属另一需求）。

改动半径：**1 生产文件（1 行 + 注释） + 1 新测试文件**。**未动** `CreativeLibrary` 结构、未引入新组件、未改 CSS。

### ⚠️ 防重复取证（补做 —— 首轮只查了一半，用户指出）

> **首轮的取证漏洞**：我只查到「MJ 不走 `PresetGridView`」（`CreativeLibrary.tsx:34`），就断言
> 「MJ 天然满足」→ **没继续查 MJ 视图本身有没有 prompt 显示能力**。结论碰巧对，**推理是坏的**。

**补做全景盘点：4 类的 prompt 显示能力**（这才是"想清楚最优解 + 防重复"）：

| 类 | 视图 | prompt 显示能力 | 判定 |
| --- | --- | --- | --- |
| **style / filter / motion** | `PresetGridView` | 改前**只有名字** → 改后**名字 + prompt** | ✅ **唯一真缺口**（本轮修）|
| **mj** | `MjStyleBrowser` | **点卡片进详情态** → 4 模式切换（风格码 / 风格＋参数 / **完整提示词** / 色调描述）`MjStyleBrowser.tsx:93-98/150-186` | ✅ **已有，且更强** |
| **prompt（我的提示词）** | `PromptPresetView` | **卡片常驻显示** `c.content`（`:181` `<p class="cl-pdesc">{c.content}</p>`）| ✅ **已有，且更强（不需 hover）** |

**⇒ 防重复结论**：
1. **MJ 绝不能再加 tooltip** —— 它已有**详情态 + 4 模式**（能看 `codes`/`parameters`/`prompt`/`vibe`），
   卡片上再加一份 tooltip = **造第二份更弱的实现**；
2. **prompt 类也不能加** —— 它已**常驻显示**正文，比 hover 更强；
3. **只有 style/filter/motion 缺**（改前连 hover 都看不到）—— 本轮的 1 行改动落在**唯一正确的位置**。

> **留痕理由**：这个"看似不用查"的判断，恰恰是**最容易造成重复实现**的地方
> （"给每个类都补一份显示"听起来很一致，实际是 N 份真相）。**用户 2026-09-19 判据：
> 「一定要想清楚最优解再解决，还要防止重复」**。

## 三 · Step 7 验证（探针先红后绿 + 反向探针）

```
🔬 探针结论｜TD-05-19 悬停显示提示词 先红后绿
   文件   : src/components/base/creative/views/PresetGridView.tsx（命中 1 处，已还原 sha=d7b2a533d99d）
   命令   : bash -c 'npx vitest run tests/unit/presetGridHover.test.tsx > /tmp/p0519.log 2>&1; echo EXIT=$?; grep -E "Tests |×" /tmp/p0519.log | head -5'
   观测   : exit=0｜输出 4 行
   ✅ 退出码 == 0 — 实际 0
   ✅ 输出含「EXIT=1」 — 命中
```

**注入语义**：`` title={`${p.name}\n${p.prompt}`} `` → `title={p.name}`（= 还原旧实现）。**旧实现下测试变红**。

**后绿**：`npx vitest run tests/unit/presetGridHover.test.tsx tests/unit/creative/presets.test.ts`
→ **Test Files 2 passed ｜ Tests 26 passed**（含新增 2 条）。
**反向探针**：同域既有 `creative/presets.test.ts`（24 条）**改后仍全绿** ⇒ 未破坏既有语义。
`npx tsc --noEmit` → **0 错**。

**门禁**：`check:any` **0 违规** · `check:dead-code` **76 = 76 无新增** · `check:doc-refs` **0 违规**。

> **未跑全量**（用户 2026-09-19 指示「非必要不要跑全量」）：本轮只动 1 个展示组件（纯 props 驱动、
> 无 effect），改动半径与相关测试已覆盖；全量在此属非必要。

## 四 · 六闸自查

- **一归**：不适用（未收口判据）—— 但**未新增第二份真相**（prompt 真源仍只有 `creativeCatalog`）✅
- **减一**：**未新增任何兜底 / 分支** —— 特意**不写** `kind === 'mj'` 判断（结构已保证）✅
- **减二**：**1 行改动**；无新组件、无新 CSS、无新分支（环节数不变）✅
- **一诚实**：数据齐备即消费，不再"藏着不用" ✅
- **零炸**：见三问 ✅
- **一凭证**：探针结论块 + 相关测试计数 + tsc + 3 道闸 ✅

**零炸三问**：

1. **哪些行为被改了？** 仅**悬停 tooltip 文案**：由「名字」→「名字 + 提示词正文」（style/filter/motion 三类）。
   **未改**：卡片视觉、点击行为、名条文案、MJ / prompt 类视图（它们不走本组件）。
2. **改前能走的路径，改后仍能走吗？** → 是。`creative/presets.test.ts` 24 条同域测试全绿。
3. **改前会失败的，改后仍失败吗？** → 是。未顺手改任何无关行为（名条仍只显示名字，已由新锁固定）。

**用户可感知的行为变化（显式声明）**：**有** —— 悬停风格/滤镜/运镜卡片时，提示词正文可见（此为需求本身）。

## 五 · 结论与下一步

- **TD-05-19 结清**（业务债 → 用户已拍板 → 施工完成）。
- **A7 回改**：`05-创作库-预设悬停显示提示词-2026-09-18.md` 的两条失真描述（"部分很长会被截断"、
  "4 类全部经此组件"）已在本文件 §一 更正；该文件 §七 补指向本文件的最终结论。
- **触发本轮的判死复盘**：16 条待办中**仅此 1 条真该问用户**（其余 15 条该架构师自判或用户已裁）——
  见 `18-跨区-类型与入口纪律四笔-2026-09-19.md` §一。
