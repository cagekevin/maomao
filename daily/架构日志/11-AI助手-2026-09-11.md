# 架构日志 · AI 助手（含表格）· 2026-09-11

> **文件名**：`11-AI助手-2026-09-11.md`。区域 11「AI 助手」**自主新增**（按 `index.md`「区域治理规则」第 1 条）。
> 审计视角：数据流（用户输入 → runtime 组装 → LLM → 工具执行 → 会话状态落盘 → UI 渲染）。
> 审计依据：架构师心法 Phase 1–4 + 静默自检清单；实证工具 `node scripts/mv-sync-refs.mjs refs <file>` + `grep` + `npm run type-check` + `vitest run`（2026-09-11 实跑）。

---

## 〇、为何自主加区（漏列说明）

`index.md` 原 10 区把 AI 助手全部吸进「01 生成链路」的附属脚注（`agent/runtime/agentRuntime.ts → chatStream` 一行），**而 AI 助手实为独立子系统**：自有会话状态层（`conversation/*` 8 文件）、自有运行时（`runtime/*` 12 文件）、自有工具层（`canvas/*` 3 文件）、自有 UI（`panels/AgentPanel.tsx` 1899 行）与表格子系统（`assistantTable/*` 23 文件），且 `AgentPanel.tsx` 是 TD-5 认定的 ≥1490 行主力大模块。

按规则第 1 条，自主追加为**区域 11**，编号顺延 `max(10)+1`。

> **不含**：区块 03 的 `resultUrlExtractor` 死代码与 `AgentPanel.test.tsx` config mock 债（已分别归 TD-15/TD-14）；本区仅登记该测试文件的**残留类型缺陷**（TD-11-1）。

---

## 一、链路快照（refs 实证，非凭文件名猜）

### 1.1 模块地图与 fan-in（`refs` 实跑）

| 层 | 文件 | refs 实证生产 fan-in | 判定 |
| - | - | - | - |
| L0 类型地基 | `agent/agentTypes.ts`（`Violation`） | 2（`conversationInvariants`、`tableInvariants`） | ✅ 活跃（中立类型，刻意零环） |
| L0 类型地基 | `conversation/conversationTypes.ts` | 经 `conversationState` re-export；`conversationInvariants` type-only 依赖 | ✅ 活跃（会话形状真源，注释明确「各子域只从此处 import 类型」） |
| L1 状态底座 | `conversation/conversationState.ts` | 13 模块（aiState/skillState/snapshot/imageMap/useAgentChat/useActiveAssistantTable/TableWorkspacePanel + 5 测试） | ✅ 活跃（唯一可写源 + 落盘 + 归一） |
| L1 校验 | `conversation/conversationInvariants.ts` | 2（`conversationState` 写前校验、`conversationState.invariants.test.ts`） | ✅ 活跃（批1/L4 已落地） |
| L2 聚合 | `conversation/conversationStore.ts` | 18 模块（assistantTable 4 + canvas 2 + index + runtime 3 + 8 测试） | ✅ 活跃（对外唯一入口，re-export 聚合） |
| L2 运行时 | `runtime/useAgentChat.ts` | 8（index + useCanvasAgentTools + 6 测试） | ✅ 活跃（唯一发送入口） |
| L2 运行时 | `runtime/agentCore.ts`（纯函数+常量） | 6（agentRuntime/useAgentChat/AgentMessage + 3 测试） | ✅ 活跃 |
| L2 运行时 | `runtime/agentRuntime.ts`（roundTrip/runToolCalls，依赖注入） | 2（useAgentChat + 测试） | ✅ 活跃 |
| L2 运行时 | `runtime/projectMemoryStore.ts` | 5（useCanvasAgentTools 仅取枚举+脱敏 / memoryRetrieval / useAgentChat / 2 测试） | ✅ 活跃 |
| L3 工具 | `canvas/useCanvasAgentTools.ts` | 8（index/useAgentChat/TableWorkspacePanel + 5 测试） | ⚠️ 活跃但 **1817 行**（TD-5 红线持有） |
| L3 工具 | `canvas/canvasHost.ts` | 3（canvasPlanExecutor/useCanvasAgentTools + 测试） | ✅ 活跃（画布写操作唯一入口） |
| L3 表格 | `assistantTable/tableWorkspaceState.ts` | 经 AssistantTablePanel/TableWorkspacePanel | ✅ 活跃（运行态枢纽，不落盘） |
| L4 UI | `panels/AgentPanel.tsx` | **0 模块 import**（App.tsx 内部引用） | ⚠️ 叶节点，**1899 行**（TD-5 红线持有） |

### 1.2 外部化死壳（整模块删除，非死代码）

`runtime/runModeRegistry.ts` —— **文件已不存在**（`ls` 实测 `No such file or directory`），全仓 `runMode|workMode|WorkMode` 仅剩 **6 条注释**（`contracts.ts:34` 更新说明、`conversationStore.ts:297` 删除留痕、`useAgentChat.ts:194` 执行模型说明、`agentCore.ts:137/350/353-354` 常量提升说明）。`spec/AI-ASSISTANT-REDUNDANCY-CUT.md` 的砍除提案**已 100% 落地**。

### 1.3 DATAFLOW.md 漂移（已就地刷新）

`spec/DATAFLOW.md` 对 AI 助手仅有 **2 行**（line 40/41：「其它直接消费 generate 门面」），**缺整个会话状态层 / 工具层 / 表格子系统的数据流**。按心法 §七.2 就地追加独立段（禁静默删旧行，见文末「五」）。

---

## 二、溯源（Phase 1：症状 vs 病根）

- **本区域结构总体能承载业务**：会话状态层已完成「上帝文件 → 6 文件单向无环拆分」（2026-08-21）；表格域 16 条债已清（A/B/C 级全结）；执行模型三态冗余已砍净；`useAgentChat` 是唯一发送入口，无第二条 LLM 出站路。
- **病根级观察（非症状）**：AI 助手的复杂度**不在链路，在「横切切面」**——`useAgentChat` 同时承担发送编排、上下文预算压缩、记忆落库、对话 CRUD、表格桥接、确认门禁 6 类职责（1103 行），并通过 `UseAgentChatReturn` 回传 5 个 store 原子能力（`setCurrentSnapshot`/`setAwaitingConfirm`/`setCurrentAssistantTable`/`setCurrentGlobalContract`/`markMessageTableResolved`）供 UI 直调。
  - 这**不是末端 `if-else` 补丁**（无漏斗），而是「hook 作为传递门面」的耦合——架构上属**结构债**而非错误债：当前单实例、无 bug、测试网覆盖到位，故不等价于「现在不能动的债」。
  - **建议动作**：转「架构5步法」评估（`UseAgentChatReturn` 收窄为只读 + action），**不进 TECH-DEBT**（无运行时风险，属翻新范畴）。
- **喂症状清单（已核，均已落地，无需再动）**：`getCachedProjectMemories(agentKey, projectId)` 的 `projectId` 形参、`loadProjectMemories` / `removeProjectMemories` / `deleteProjectMemories` 的 `_projectId` 形参、`buildProjectMemoryContextFromStore(agentKey, '', text)` 的 `''` 实参——「按 agentKey 全局、不分项目」的**决策注释**已写在各文件头 + 工具描述（`useAgentChat.ts:690` 传空串）。
  - **判定**：这是**已收敛的语义**（决策留痕清晰、实参唯一、无第二个消费方），不是悬空冗余。唯一残留是**签名噪声**（TS 允许缺省，三处形参可删）；若删须同步文件头 JSDoc 与 `projectMemoryStore.test.ts`（有 2 处显式传 `projectId` 断言「被忽略」）→ **成本 > 收益，判为「中性 · 不动」**。

---

## 三、定海（Phase 2：不变式 / 唯一真相源）

| 数据 | SSOT | 实证 | 判定 |
| - | - | - | - |
| 会话数据（对话/消息/memory/workflow） | `conversationState.states[agentKey]` | 模块私有 `states`，`commit` 唯一写漏斗（`conversationState.ts:427`） | ✅ 唯一 |
| 当前对话快照 | `getCurrentSnapshot()` 派生 | `conversationSnapshot.ts:53` 读时计算，无缓存副本 | ✅ 无第二真相 |
| 表格数据 | per-conversation `memory.assistantTables` | `get/setCurrentAssistantTabs` 读写；`materializeAssistantTabs` 落稳定基线（幂等） | ✅ 唯一（SSOT-7 已根治） |
| 表格运行态（选中/预览/选区） | `tableWorkspaceState` 模块级（不落盘） | 显式声明「不落盘、不进 conversationState」；宽度单独走 `agent_split_width` | ✅ 唯一 |
| 长期记忆 | `projectMemoryStore`（`agent_project_memory_v1_<agentKey>`） | 与对话 `memory` 物理隔离；跨域经 `pendingMemorySuggest` 暂存桥接 | ✅ 唯一 |
| 输入态 | `InputStateMachine` 实例（per-conversation，`load(id)`） | 与 store `sending` 分工明确（状态机=UI 按钮语义，sending=发送锁） | ✅ 无冲突 |

**边界校验（探口）**：三条运行期 Guard 实测均存在且有效——
1. `commit` 前 → `validateConversationState`（dev 下 error 级 `logger.warn`，`conversationState.ts:115-128`）；
2. `setCurrentAssistantTabs` 前 → `validateTabs`（`conversationAiState.ts:111-119`）；
3. `creditGate` 读侧 → `isCreditGate` 运行时守护（`conversationSkillState.ts:92`，替代裸 `as`）。

**静默 no-op 已出声**：`requireActiveConv(op)` 统一守卫（`conversationState.ts:471`，5s 限频 warn）；`ensureActiveConversation` / `applyConversation` 的两处静默回退均已补 `logger.warn`。**无「把 catch 兜底埋到三公里外」形态。**

---

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- **深模块判定 — `conversation/*`（✅）**：窄接口（`get/setCurrent*` 原子函数）+ 厚实现（归一/落盘/降级/校验）。`conversationStore` 作为聚合 re-export 单一入口，18 个消费方 import 路径稳定 → 符合标准深模块。
- **深模块判定 — `assistantTable/*`（✅）**：模型/纯函数（`buildPreviewResult` 单源）/运行态/UI 分层清晰，16 条历史债清理后「预览=确认」单源成立。
- **过度抽象判定 — `conversation/*` 6 文件拆分（✅ 合格，非过度）**：判据是**是否已成环或反复漂移**——实测**单向无环**（`state ← {snapshot, aiState, skillState} ← {imageMap, store}`），且拆分前是 674 行上帝文件。属「真缝隙」（变化速率不同：底座/快照/AI 态/Skill 态/图记忆），符合心法 Phase 3 探界。
- **上帝类判定 — `useAgentChat`（⚠️ 唯一候补）**：1103 行、6 类职责、回传 5 个 store 原子能力。**但**：其内部已把纯函数下沉 `agentCore`、运行时依赖注入下沉 `agentRuntime`、消息构造下沉 `agentMessages`、附件下沉 `agentAttachments`、状态机下沉 `inputStateMachine`、工作流迁移下沉 `workflowState`——**hook 本体的「厚」主要是编排顺序，不是脏逻辑**。单独再切会造出「编排碎片」（跨 6 文件追一条 send 流），**当前切分点已接近最优**。
- **收敛口判定**：AI 助手出站 LLM 唯一走 `base/api/generate.ts`（`chatStream`，见 DATAFLOW line 40）；画布写操作唯一走 `canvasHost`；消息写唯一走 `agentMessages`。✅ 三处收敛正确，无第二实现。

---

## 五、探债（Phase 4：技术债账本）

> **第 2 轮补审（2026-09-11，用户要求「再研究一下那个是不是债」）** —— 逐条量化后**改判**：第 1 轮的「`useAgentChat` 6 职责」候选**不是债**（见 §五.1 裁定），真债是**草稿的「双写 + 双 SSOT」**（TD-11-2，见下）。

| TD-ID | 决策点 / 问题点 | 归类 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - |
| TD-11-1 | `AgentPanel.test.tsx` 的 `importOriginal` 部分 mock 未收窄类型（`await importOriginal()` 返回 `unknown`）→ `{...actual}` 报 `TS2698`，`type-check:tests` 长期 1 错 | 还债 | 1 处（tests/tsconfig 编译门） | 中 | TD-14 改 mock 时漏照抄既有范式（`useAssetMoveToFolder.test.tsx:22` 已注明「importOriginal 返回 unknown，直接 spread 报 TS2698」），且只跑 `vitest run` 未跑 `type-check` | **已修**：补 `as Record<string, unknown>` + 注释留痕；`npm run type-check` src+tests 双向 0 错 | ✅ 已解决 2026-09-11 |
| TD-11-2 | `AgentPanel` 输入草稿 7 处裸 `contentSet(contentGet)(AGENT_DRAFT_KEY, …)` —— 既**双写**（`input` state + `agent_draft` 键，无订阅桥接），又**绕过 SSOT**（会话侧 `conv.draft` 为唯一真源，仅发送/切对话时被覆盖修正），且绕过 CLAUDE §5.4.5「存储键禁止裸字符串」红线形式 | 增债 | 7 处同文件 + 1 契约漂移点 | 中 | 草稿是 UI 高频输入（逐字 `onChange`），未纳入会话 store 的 per-conversation 快照，改以「独立键 + 双写」图省事 | **已修（2026-09-11 第 3 轮）**：删 `AGENT_DRAFT_KEY` 全部 7 处，草稿唯一真源 = `conv.draft`；`input` 初值读 `getCurrentSnapshot().draft`，逐字/清空/发送/确认后一律走 `saveDraft`；`handleConversationChange` 删除「也写一次键」 | ✅ 已解决 2026-09-11 |
| TD-11-3 | `useAgentChat` 回传 5 个 **写操作**（`setCurrentSnapshot`/`setAwaitingConfirm`/`setCurrentAssistantTable`/`setCurrentGlobalContract`/`markMessageTableResolved`）给 UI 直调 —— 展示层持写权 | 增债 | 1 处（`AgentPanel.tsx` 实收 2 个；另 3 个本面板未接） | 低 | 收口 `store 穿透` 时的「薄适配」半成品：`UseAgentChatReturn` 混入 store 原子能力 | **已修（2026-09-11 第 3 轮）**：`UseAgentChatReturn` 收窄为 4 个语义化 action（`saveSkills`/`saveDraft`/`saveAttachments`/`closeAwaitingConfirm`），5 个 store 写操作全部移除透传；测试加「防回潮」断言（5 个 store 函数必须 `undefined`） | ✅ 已解决 2026-09-11 |
| **TD-11-9** | 三项对账：① `MUTATING_TOOLS` **零测试覆盖**（集合若漂移无人发现）② 单飞锁超时自动释放**静默无日志**（锁泄漏不可见）③ 工具去重/模块级单例待判 | 还债 | 1 处（测试缺口）+ 1 处（日志） | 中（① 撤销栈是「AI 可撤回」唯一保障，漂移即静默失效） | ① `mutating` 隐式契约未固化为断言；② 超时解锁路径只解锁不告警 | **已修（第 9 轮，2026-09-11）**：① 加 `canvasAgentTools.test.ts` 对账用例——20 工具逐个断言「该进/不该进 undo 栈」，**含负例验证**（注入 `focus_node` → 精确报错）② 超时解锁补 `logger.warn` 留痕 ③ 三项对账结论见 §六.7 | ✅ 已解决 2026-09-11 |
| **TD-11-8** | 工具层**裸调画布写操作**绕过唯一入口：`connect_nodes:781` / `batch_connect_nodes:825` / `delete_edge:849,855` 共 **4 处** `ctx.setEdges` 直改画布 —— 违反 `agent/index.ts:52` 红线「画布写操作只经 canvasHost」 | 还债 | 4 处（3 工具）+ 全部连线路径 | 中（唯一入口红线；连线是高频操作，绕过入口后审计/撤销/并发防护都失效） | **canvasHost 缺 `removeEdges` 原语**，被逼裸调（同区域 03「缺通道 → 绕路」形态）；裸调本身属末端规避 | **已修（第 8 轮，2026-09-11）**：① `canvasHost` 补 `removeEdges(ids)` 原语（补齐缺口，非打补丁）；② 4 处改走 `appendEdges`/`removeEdges`；③ `check-arch.mjs` 加**AST 级规则**「工具层禁裸调画布写操作」（canvasHost 本体豁免）——负例实测报错、还原通过 | ✅ 已解决 2026-09-11 |
| **TD-11-7** | `AgentPanel.creditGatePreview` 是**本地 `useState` 副本**（仅挂载时读 `getCreditGate()` 一次 + 靠 `CREDIT_GATE_EVENT` 刷新）；切对话**既不重读也不广播事件** → 在 A 命中积分闸（卡亮）后切到 B，**卡片残留在 B** | 还债 | 1 处（UI 副本）+ 全部切换路径 | **高**（与 TD-11-4 同源：本地副本 + 切对话不重置 → 跨对话状态泄漏） | 门禁真源是 per-conversation `conv.creditGate`（真源层已正确隔离，实测通过），错在 UI 侧持有副本且**未随真源切换重算** | **已修（第 6 轮，2026-09-11）**：新增 `[activeConversationId]` effect，按当前对话真源重读 `creditGatePreview` + 重置 `creditGateDismissed`；补测试见 §六.4 | ✅ 已解决 2026-09-11 |
| **TD-11-6** | `src/components/base/canvas/workflowRuntime.ts` 是**生产级死代码**（0 生产 import，仅 2 个自证测试）；其 `awaitingConfirm`/`aiUndoStack`/`steerQueue`/`status` 与 `agent/conversation/*` 的 per-conversation 实现**完全同构** → 「第二份真相」 | 还债 | 1 模块 + 1 自证测试 + 3 处注释引用 | 低（但含第二真相源风险） | 历史遗留：全图工作流运行时曾规划独立 PRD，后由 `conversationSkillState`/`workflowState`/`conversationSnapshot` 承担实际职责，旧模块未清理 | **已删（第 6 轮，2026-09-11）**：删模块 + `workflowRuntime.test.ts` + `canvasHooks.test.ts` 第 3 节；同步 `base/README.md` 文件清单、`upstreamLink.ts`/`inputStateMachine.ts` 注释漂移；禁据注释恢复 | ✅ 已解决 2026-09-11 |
| **TD-11-5** | `setCurrentSnapshot` 是**部分 patch**（`Partial & Record<string, unknown>`）→ 「漏列举字段」在**类型层面不可防**（多传/少传都合法）；`clear()` 就地手写 11 个字段 = 复制 `emptyMemory()` 定义 | 还债（**第 5 轮已还**） | 8 个调用点 + 1 处语义动作 | 中 | 部分 patch API 把「要改哪些字段」的判断推给每个调用点，漏一处即静默不同步（TD-11-2/11-4 同源病灶的共同温床） | **已还（第 5 轮，2026-09-11）**：见 §六.3 | ✅ 已解决 2026-09-11 |
| **TD-11-4** | **第 3 轮「结清」是假的**：`conv.draft` 收敛后，`switchChat`/`newChat`/`deleteChat` 三处仍带 `draft: ''` 切换前暂存（独立键时代的遗留防御）→ **清掉即将离开会话的草稿**，切走再切回**永久丢字**；`send` 的 `finally` 同样带 `draft: ''` → **清掉 AI 回复期间用户的新输入**（长任务下高发）；`updateMessageByContent` 无理由顺带清草稿；`contracts.ts` 的 `agent_draft` 登记与 `cloudSync.SYNC_EXCLUDE` 成**指向不存在代码的死登记** | 增债（**第 3 轮增债**：修了「双写」却引入「静默丢数据」） | 4 处（3 切换入口 + 1 收尾）+ 2 处死登记 | **高**（核心链路：用户输入内容丢失，且全程静默无提示） | 把「草稿是独立键」时代的防御逻辑留在原处没复核；`setCurrentSnapshot` 部分 patch 语义未显式化 → 「要不要带 `draft`」全靠调用点自觉 | **已修（第 4 轮，2026-09-11）**：① 三切换入口改 `flushCurrentConversation()`（语义化、**不列举任何字段**）；② `send` finally 删 `draft: ''`（只落 messages/skills）；③ `updateMessageByContent` 只传 messages；④ `setCurrentSnapshot` 部分 patch 语义写成显式契约（`undefined` = 不动，禁顺手列举）；⑤ 删 `agent_draft` 的 STORAGE_KEYS 登记；`cloudSync` 排除项保留但标注「已退役·仅旧机器残留双保险」；⑥ 新增 `agentDraftTd17.test.ts` 3 条（切走切回保留 / 发送期间新输入不被清 / per-conversation 隔离），**先红后绿**留证 | ✅ 已解决 2026-09-11 |
| **TD-11-10** | 表格域模型层审计三缺陷：① `validateWorkspace`（W/P 类不变量）**全库零生产调用**（仅测试）→ "写了不跑 = 形同没写"（spec §七 原话）；② 校验清单 **W5/W6 声称实现却无**：W5 选区锚点失效无校验；W6 基于错误假设——`TableClipboard` 实为 `{rows|range|cell}` **无 `colIds` 字段**（spec §七.L4 与实现漂移）；③ 列合并逻辑 **两处逐字同构**（`buildPreviewResult` 与 `copyRowsToTab`，I4「复制/重映射脱节」同源病灶） | 还债 | 2 文件（tableInvariants/tableWorkspaceState）+ 列合并收口 | 中（不变量从不实际执行 → 引用脱节长期静默；重复实现改一边忘改另一边即漂移） | spec §七 把"W/P 不变量必须运行期校验"列为硬要求，但 `validateWorkspace` 从未被生产代码调用；两份同构列合并是 I4 类缺陷温床 | **已修（第 10 轮，2026-09-11）**：① `tableWorkspaceState.setState` 内接线 `validateWorkspace`（dev 下 `logger.warn` 告警 + 签名缓存防高频刷屏，生产零开销）；② `tableInvariants` 补 **W5**（选区锚点失效→warn），**W6 不谎报**（查实 `TableClipboard` 无 `colIds`，诚实标注"基于错误假设、未实现"）；③ 抽 `mergeColumnsByLabel` 单一真相源，两处重复实现收口；④ 补 2 条防回潮测试（W4 自检触发 + 列合并去重保 id） | ✅ 已解决 2026-09-11 |

### 五.1 裁定：「`useAgentChat` 6 职责 + 5 回传」——**不是债**（本轮改判，须留痕）

第 1 轮把它列为「待翻新候选」，量化后**改判为非债**，四条证据：

1. **薄检查（实质行数）**：`useAgentChat` 1102 行但**注释占 348 行（31.6%）**，代码 754 行；其中 `send` 主循环约 250 行是**不可约的编排时序**（流式占位 → 预算决策 → 压缩 → roundTrip → 解析 generations → 工具循环 → 门禁停点 → finally 收尾），六类职责是**一条线上的先后步骤**，不是并列的六坨。
2. **已充分下沉**：纯函数→`agentCore`、依赖注入运行时→`agentRuntime`、消息写→`agentMessages`、附件归一→`agentAttachments`、状态机→`inputStateMachine`、工作流迁移→`workflowState`。hook 内**再无已可独立的脏逻辑**，只剩编排顺序。
3. **再切是造碎片**：hook 直接 import `conversationState` 的 `getState/subscribe`，读 `getCurrentSnapshot/…` 共 **28 处**；任一切法都要么让新模块反向 import 该 hook（环），要么新造一层 store 读入口（新的重复真相源）。**切下去的收益 < 认知负荷**。
4. **探界判据不满足**：深模块要求「窄接口厚实现」。本 hook 接口窄（`UseAgentChatReturn` 一个对象），实现厚，**是合格深模块**；「厚 = 债」的直觉在此不成立（厚的是编排必要性，不是复杂度堆积）。

> **结论**：`useAgentChat` 保留现状，**不进 TECH-DEBT**。TD-5 对 `AgentPanel.tsx`(1899)/`useCanvasAgentTools.ts`(1817) 的红线继续有效（新子面板走 `panels/agent/*`、新工具走 `agent/tools/*` 注册），`useAgentChat` 同理（新能力下沉 `runtime/*`，勿在 hook 内加并列职责）。

### 五.2 判定「非债」的其余候选（记录理由，防后续误翻新）

1. `projectMemoryStore` 的 `_projectId` 形参噪声 → **中性 · 不动**（注释/决策在案、实参唯一，删签名成本 > 收益，理由见 §二）。
2. `conversation/*` 6 文件拆分 → **合格深模块**（单向无环，非过度抽象）。
3. `conversationTypes.ts` 的 `assistantTables: unknown` 宽松类型 → **刻意**（真源形状归 `assistantTable.ts`，消费方一律经 `normalizeAssistantTabs`），非假收窄。
4. `republish.ts` 侧「侧信道」：AI 助手实际用 `contentGet + contentSubscribe` 消费设置（§三 已核）→ **正常观察者模式**，非第二真相源，不动。

---

## 六、结论与下一步

- 本区域状态：**已验证(无债)** —— 10 轮审计共 10 债（TD-11-1~11-10）**全部已还清**（2026-09-11）；「`useAgentChat` 6 职责」**改判非债**（§五.1 四条证据）。
- 待还债项是否已流入 `spec/TECH-DEBT.md`：**是，且已结清**（TD-16 = TD-11-1；TD-17 = TD-11-2/TD-11-3，均已标 `[已解决 2026-09-11]`）。
- DATAFLOW.md 是否需要刷新：**是，已就地追加**「AI 助手（Agent）链路」独立段（A0 会话 / A1 发送工具循环 / A2 表格 / A3 长期记忆 / 出站约束），旧 line 40/41 保留不删。

### 六.1 TD-17 偿还实施记录（2026-09-11 第 3 轮，「已还债」）

- **改了什么**：
  1. **草稿收敛到唯一真源**（TD-11-2）：删 `AgentPanel` 常量 `AGENT_DRAFT_KEY` 与全部 7 处裸键读写。`input` 初值改读 `getCurrentSnapshot().draft`（只读入口）；逐字 `onChange` / 发送清空 / 确认后清空 → 一律 `saveDraft`；`handleConversationChange` 删掉「也写一次存储键」的双写分支。草稿真源 = 当前对话 `conv.draft`（per-conversation，随会话 300ms 节流落盘，切对话自动跟随）。
  2. **展示层去写权**（TD-11-3）：`UseAgentChatReturn` 移除 5 个 store 写操作透传，改为 4 个语义化 action（`saveSkills`/`saveDraft`/`saveAttachments`/`closeAwaitingConfirm`）；`AgentPanel` 的 skills/attachments 同步 effect、确认门禁翻转全部改走 action。
- **收益**：消除「双写 + 双 SSOT」一份副本；草稿天然 per-conversation（旧实现切对话靠 `handleConversationChange` 手动搬运）；UI 不再能任意 patch 会话字段；AgentPanel 内 `contentSet/contentGet` 仅剩自有 UI 偏好 `agent_panel_width`（未跨界）。
- **验证（实跑）**：`npm run type-check` src + tests **双向 0 错**；`CI=1 npx vitest run` **189 文件 / 2408 用例全绿**（较上轮 2405 +3：新增 3 条 TD-17 用例）；`npm run build` ✅；`npm run check:health` ✅ **0 错**；`npm run test:smoke` ALL PASS。
- **新增门禁测试**：`AgentPanel.test.tsx` 2 条（逐字 `saveDraft('草稿')` / 发送后 `saveDraft('')`）；`useAgentChat.hook.test.ts` 2 条（4 个 action 存在 + **5 个 store 函数必须 `undefined`** 的防回潮断言 / `saveDraft` 落 `conv.draft` 往返）。
- **下一步**：区域 11 结清。按 `index.md`「层（底→顶）」推进，剩余「待审计」区优先级最高者为 **04 画布 / 节点**（`canvas/NodePalette` · `nodePrefs` · `useGenerateNode`）。

### 六.2 第 4 轮自我推翻记录（2026-09-11，「我掩盖了什么」）

> 用户质问「真的结清了吗？你现在是掩盖了错误还是暴露了错误？」→ 复核后**承认第 3 轮的「结清」不成立**，本记录如实留痕（不美化）。

**第 3 轮的错**：我把「删掉双写」当成了「修好草稿」。实际上只做了**语义迁移**（键 → `conv.draft`），
**没有复核迁移后原逻辑是否仍然成立**——而那些逻辑全是独立键时代的产物：

| 我漏掉的 | 后果 | 性质 |
| - | - | - |
| `switchChat`/`newChat`/`deleteChat` 切前 `draft: ''` | 清**即将离开会话**的草稿 → 切走再切回丢字 | **数据破坏**（静默） |
| `send` 的 `finally` 带 `draft: ''` | 清 AI 回复期间用户的**新输入** | **数据破坏**（静默，长任务高发） |
| `updateMessageByContent` 顺带 `draft: ''` | 改消息却清草稿（无理由副作用） | 副作用污染 |
| `agent_draft` 登记仍在 contracts/cloudSync | 指向不存在代码的死登记（假入口） | 契约漂移 |

**为何没被测出来**：第 3 轮我加的是「`saveDraft` 被调用了吗」这类**实现断言**——它只验证"我写的代码执行了"，
不验证"用户的数据还在吗"。**自证式断言无效**（心法 §五 探口：脏数据要在边界炸，而非靠断言堆绿）。

**方法论修正（已落到本轮做法）**：
1. 改语义迁移类改动，**必须复核原有防御逻辑在新语义下是否仍成立**，不能只做替换。
2. 测试要写**行为断言 + 先红后绿**：`agentDraftTd17.test.ts` 先在未修代码上跑出 `expected '' to be 'A 的草稿'`，再改代码转绿——**先证伪、再证明**。
3. `setCurrentSnapshot` 这类「部分 patch」API 是**缺陷温床**：调用点必须自列举字段 → 漏一处即静默不同步。已写进函数契约，且禁止在切换路径列举字段。

**仍未做（诚实列出，不假装完美）**：
- `setCurrentSnapshot` 的「部分 patch」形态本身**未重构**（只显式化了契约 + 加了注释）。彻底做法是拆成 `patchConversation({...})` 窄接口或按字段提供 `setDraft/setMessages` 原子 setter，让「漏列举」在类型层面不可能发生。**属翻新范畴，需单独评估**（涉及 9 个调用点 + 测试网）。
- 本轮只审到**草稿**这条链路；`Conversation` 里 `draft`/`pendingGenerations`/`awaitingConfirm`/`referenceImages` 等**同为「运行态还是数据」边界模糊**的字段，尚未逐个做生命周期对账（TD-11-2 只覆盖 draft 一个）。

### 六.3 第 5 轮：TD-11-5 窄接口收窄（2026-09-11，「让漏列举不可能」）

> 用户追问「第一层的改动现在还做好了没有？深化再深化」。第一层（草稿链路）已还清；**第一层的地基（TD-11-5）本轮补齐**。

**做了什么（接缝画在「变化速率不同」处）**：
- `conversationSnapshot.ts` 新增 **5 个窄接口**（一次只改一件语义事，签名即契约）：
  - `setCurrentDraft(draft: string)` / `setCurrentSkills(unknown[])` / `setCurrentAttachments(unknown[])` / `setCurrentMessages(ConversationMessage[]|unknown[])`——单字段原子写，**参数类型即该字段真类型**（不再是宽松 `Record<string, unknown>`）；
  - `resetCurrentConversationToEmpty(skills?)`——把 `clear()` 就地手写的 **11 个字段**收敛为「重置」这一**语义动作**（原先等于复制 `emptyMemory()` 定义；`Conversation` 每加需清字段就要回来改，必漏）。
- `setCurrentSnapshot` 保留为唯一写漏斗 + 迁移期出口，但契约注释写明「新代码禁止用其列举字段」。
- **8 个调用点全部改走窄接口**：`agentMessages` 3 处、`useAgentChat` 5 处。
- **顺带删掉 2 处 no-op**（用测试证明，非推理）：
  - `send` 入口的 `messages: getCurrentSnapshot().messages` 是**读回原值再写回**（部分 patch 下不传即等价）；`skills` 亦有独立写点 `saveSkills`；
  - `send` 的 `finally` 整段 `setCurrentSnapshot({messages:<读回>, skills})` 同属 no-op（消息已在循环内逐步写入 store），删除后仅少一次多余 commit+落盘。
  - 为此新增用例：断言收尾**不再产生「只带 skills 的 no-op 调用」**。

**验证**：`type-check` src+tests **0 错**；AI 助手全域 **11 文件 / 125 用例全绿**；`agentDraftTd17.test.ts` 扩至 **5 条**（新增「窄接口只动目标字段，其余原样」+「reset 清全态且 skills 按入参保留」两条守卫——窄接口若退化成列举，这两条必红）。

**仍未做**：`Conversation` 其余字段（`pendingGenerations`/`awaitingConfirm`/`referenceImages`/`aiUndoStack`）的**「运行态 vs 数据」生命周期对账**尚未做；`setCurrentSnapshot` 本体仍在（未物理删除，因有 `workflow`/`pending`/`memory` 等一次性多字段场景）。

### 六.4 第 6 轮：运行态字段生命周期对账（2026-09-11）

> 承接 §六.3「仍未做」项。对 `Conversation` 的 5 个「运行态还是数据」边界字段逐个对账（方法同 TD-11-2：问「切对话该留还是该清」）。

**步骤 1 · 真源层对账（全 5 字段 ✅ 正确）**

| 字段 | 真源 | 切对话语义 | 实测 |
| - | - | - | - |
| `pendingGenerations` | `conv.pendingGenerations`（唯一读写点 `conversationSkillState`） | per-conversation，随会话走 | ✅ |
| `awaitingConfirm` | `conv.awaitingConfirm`（同上） | per-conversation | ✅ |
| `pendingMemorySuggest` | `conv.pendingMemorySuggest`（同上） | per-conversation | ✅ |
| `referenceImages` | `conv.referenceImages`（`conversationAiState`） | per-conversation | ✅ |
| `aiUndoStack` | `conv.aiUndoStack`（同上） | per-conversation | ✅ |

**步骤 2 · UI 副本层对账（抓出 TD-11-7）**

5 个字段在 `AgentPanel`/`useAgentChat`/`useCanvasAgentTools`/`AgentConfirmCard` 中**均无 `useState` 本地副本**（全部实时读 store 或由消息字段 `message.awaiting_confirm` 驱动）→ 本层干净。

但同层的 `creditGatePreview`（积分闸卡片）**持有 `useState` 副本**：仅挂载时读一次 + 靠事件刷新，切对话既不重读也不广播 → **跨对话残留**。已修（`[activeConversationId]` effect 按真源重算）。

**步骤 3 · 死代码清扫（TD-11-6）**

对账中撞见 `base/canvas/workflowRuntime.ts`：0 生产 import，其 `awaitingConfirm`/`aiUndoStack`/`steerQueue`/`status` 与 `conversation/*` **完全同构** → 「第二份真相」+ 生产级死代码（同区域 03 `resultUrlExtractor` 形态）。已整模块删除 + 同步 3 处注释漂移。

**步骤 4 · 补测试缺口（子代理实证：切对话重置「无任何覆盖」）**

新增 `agentDraftTd17.test.ts` 2 条：⑥ 门禁 per-conversation 隔离、⑦ **5 字段切对话互不串 + 切回原样**（此前 `useAgentChat.hook.test.ts` 的切对话用例只断言 `activeConversationId` 与 `messages`）。

**验证**：`type-check` 0 错；AI 助手全域 + 死代码影响面 **12 文件 / 243 用例全绿**；`agentDraftTd17.test.ts` 扩至 **7 条**。

**`pendingImageNodes` 裁定（2026-09-11 用户确认）**：**非债，不改**。画布选中待引用图（`AgentPanel.pendingImageNodes`）**有意不随对话切换清空**——选中图是**画布全局概念**（"我现在在画布上选了这些图"），不属 per-conversation 数据。切对话后仍保留待引用，**符合设计**。
> ⚠️ 后续 AI 注意：这与 TD-11-4/11-7 的「跨对话残留」**形态相似但性质相反**——那两处是「per-conversation 真源被 UI 副本污染」，本条是「非 per-conversation 的全局态本就不该随对话走」。**禁止**把它当残留 bug 清掉。

### 六.7 第 9 轮：工具层三项遗留对账（2026-09-11）

> 补齐 §六.6 末尾「未审完」三项（子代理输出被截断，本轮亲自取全证据）。

**① `MUTATING_TOOLS` 一致性对账（结论：集合本身无缺陷，但零测试覆盖 → TD-11-9 已补）**

判定标准取自源码决策注释（`:296-299`）：`MUTATING_TOOLS` = 「会改画布 `nodes/edges` 的写工具」，用途 = 执行前 push `{nodes,edges}` 快照进 `aiUndoStack`。按此标准逐个核对 20 工具：

| 类别 | 工具 | 在集合 | 判定 |
| - | - | - | - |
| 只读 | read_canvas/list_nodes/list_edges/get_node_details/read_table | ❌ | ✅ 正确 |
| 节点增删改 | create_node/batch_create_nodes/delete_node/batch_delete_nodes/update_node/update_node_any_field | ✅ | ✅ 正确 |
| 连线 | connect_nodes/batch_connect_nodes/delete_edge | ✅ | ✅ 正确 |
| 编排 | execute_plan | ✅ | ✅ 正确（一次编排 push 一次，整体撤回） |
| **生成类** | generate_node / run_existing_plan | ❌ | ✅ **正确地不进**：写 `node.data` 但非「画布结构编辑」；且撤销生成无意义（已烧积分、已进任务中心，撤回 data 反造「节点在但图没了」假状态） |
| 视口/其它 | focus_node（只改视口）/ undo_ai（自身是撤销）/ memory_suggest（不碰画布） | ❌ | ✅ 正确 |

**结论：10 进 / 10 不进，恰好覆盖 20 工具，无遗漏无错登。** 但此前**零测试覆盖**——集合若漂移（新增工具漏登/误登）无人发现。已补对账用例 + **负例验证**（临时把 `focus_node` 塞进集合 → 报错 `focus_node 不应 push: expected 1 to be +0`；还原通过）。

**② 单飞锁（`executingPlan`）—— 逻辑正确，补 1 处日志**

- `finally` 释放 + `EXECUTING_PLAN_TIMEOUT`(120s) 超时兜底，**无死锁风险** ✅
- 全局单飞锁**跨对话共享是正确设计**（同一时刻只该有一套批量生成在跑，防重复烧积分），非串台缺陷 ✅
- **唯一瑕疵**：超时自动释放**静默无日志** → 若真发生锁泄漏，排障时看不到。已补 `logger.warn`（不阻断，只留痕）。

**③ 工具去重（`update_node` vs `update_node_any_field`）—— 判定：非重复，保留**

- `update_node`：**白名单 8 字段**（`UPDATE_NODE_WHITELIST`，含 `resolution`→`imageSize` 映射），防 LLM 乱改 data。
- `update_node_any_field`：**高级逃生舱**（任意 `patch` 合并进 data），注释标明「对齐官方 update_node_raw」。
- 判定 = **「窄接口 + 逃生舱」的合理分层**（与心法「深模块窄接口 + 受控逃生」一致），**非「两套规则打架」**，不适用单一规则原则。保留。

**④ 模块级单例扫描（4 处，均无新增串台风险）**：`genParams`（面板生图参数，全局设计且已文档化）、`FIG_NUM_RE_CACHE`（纯正则缓存无状态）、`executingPlan`/`executingPlanSince`（全局单飞锁，跨对话共享正确）。

**验证**：`type-check` 0 错 · `check:arch` 通过 · 工具层 3 文件 / 67 用例全绿。

**本区工具层至此审完**（`useCanvasAgentTools` 1817 行 + `canvasPlanExecutor` 938 行 + `canvasHost`）。**仍未审**：`AssistantTablePanel.tsx`(778) 等表格 UI 组件层、`AgentMessage.tsx`/`AgentConfirmCard.tsx` 渲染层。

### 六.6 第 8 轮：工具执行链路审计（2026-09-11）

> 承接用户「深化再深化」。前 7 轮集中于会话状态层，本轮转向**本区最大未审面**：`useCanvasAgentTools.ts`(1817) + `canvasPlanExecutor.ts`(938) —— AI 真正改画布的链路。仍用 `code-explorer` 子代理独立调查 + 我逐条复核。

**抓出 TD-11-8（唯一入口红线违规，已修）**：4 处 `ctx.setEdges` 裸调绕过 `canvasHost`。根因是 **canvasHost 缺 `removeEdges` 原语**——按心法「把难题逼到它该在的位置」，**补原语**而非在工具层打补丁。

**子代理确认无问题的项**（交叉验证，留证防重复审计）：
- **工具信封**：`buildCanvasAgentTools` 在注册层对每个 `execute` 统一 try/catch 兜成 `{ok:false,error}`（`:1742-1749`），20 个工具无一异常冒泡、无非法信封 ✅
- **`canvasPlanExecutor`**：全程只用 `host.appendNode/appendEdges/updateNodeData`，**零裸调** ✅
- **`canvasHost` 本体**：内部 `ctx.setNodes/setEdges` 是设计内的唯一实现 ✅

**新增编译期守卫（防回潮）**：`scripts/check-arch.mjs` 追加第 3 条规则——AST 级检测「agent/canvas 工具层裸调 `setNodes/setEdges/addNodes/addEdges`」（`canvasHost.ts` 本体豁免）。**负例验证**：注入 `ctx.setEdges([])` → 报错定位到行；还原 → 通过。至此「唯一入口」红线从**注释约定**升级为**机器强制**。

**验证**：`type-check` 0 错；`check:arch` 通过（含新规则）；工具层 4 文件 / 90 用例全绿。

**未审完（下一轮）**：`useCanvasAgentTools` 的**工具去重**（子代理报「存在功能重叠工具，如 `update_node` vs `update_node_any_field`」）、`MUTATING_TOOLS` 与「实际改画布工具」的一致性对账、模块级单例（`genParams`/`currentTaskId`）的串台风险。子代理输出被截断，这三项**未取到完整证据**，需下一轮补。

### 六.5 第 7 轮：子代理独立复核（2026-09-11，防「自证」）

> 用 `code-explorer` 子代理对 `Conversation` 5 字段做**独立**对账（不由我出结论，避免自证式审计）。

- **子代理结论**：5 字段真源层与 UI 副本层**均无残留**（与本轮 §六.4 一致，交叉验证成立）。
- **子代理新发现**（我未预判）：① `creditGatePreview` 本地副本 + 切对话不重置（→ TD-11-7，**已修**）；② 切对话状态重置**完全无测试覆盖**（→ 已补 2 条）。
- **子代理存疑项**：`pendingImageNodes` 跨对话保留（画布选中态语义）→ **已裁定：设计如此、非债**（见 §六.4 末「裁定」段），未改。
- **方法论**：审计结论应由**独立路径**复核，不能只信自证断言（呼应 §六.2 教训）。

### 六.8 第 10 轮：表格域模型层审计（2026-09-11）

> 承接 §六.7「工具层审完」后，转向本区第二大未审面：**表格域模型层**（`assistantTable.ts` 1184 行 + `tableInvariants`/`tableWorkspaceState`/`tableWorkspaceTypes`/`tableHistory`）。仍用 `code-explorer` 子代理独立调查 + 我逐条复核（§六.5 方法论：不自证）。

**撞出 TD-11-10（三项，均实锤，非症状）**：

1. **`validateWorkspace` 生产侧零接线（"写了不跑 = 形同没写"）**。全库 `grep "validateWorkspace"` 仅命中 `tableInvariants.ts` 自身定义与测试——**生产代码零调用**。spec §七 把"W/P 类不变量必须在运行期校验"列为硬要求，但写了校验函数却从不跑，等于没写（spec §七 原话）。这是"暴露缺陷而非掩盖"的直接反面教材。
2. **校验清单 W5/W6 声称实现却无**。逐条核 `validateWorkspace` 实现：W1/W2/W4/P1/P3 在；W5（选区锚点失效）、W6（剪贴板 colIds ⊆ 当前列）**声明在覆盖清单却无对应分支**。
   - W5 → **补实现**（选区锚点 `r0/c0/r1/c1` 任一指向不存在行/列 → `warn`，对齐 `assistantTable.rangeToCells`"锚点失效返回 []"的语义，把"会失效"在写侧就报出）。
   - W6 → **查实是 spec 与实现的漂移**：`TableClipboard`（`tableWorkspaceTypes.ts:41-44`）实为 `{kind:'rows',rows}|{kind:'range',cells}|{kind:'cell',text}`，**无 `colIds` 字段**（2026-09-08 重构删 colIds 时 spec §七.L4 没同步）。故 W6 **不谎报覆盖**——在覆盖清单诚实标注"基于错误假设、未实现"，留痕给后续 spec 修订，而非伪造一个永远为真的分支。
3. **列合并逻辑两处逐字同构**（I4「复制/重映射脱节」同源病灶）：`buildPreviewResult`（AI 键名合并）与 `copyRowsToTab`（跨表列对齐）各写一份 `labelToCol` + 遍历追加新列，逻辑完全一致。改一边忘改另一边即漂移。→ **抽 `mergeColumnsByLabel` 单一真相源**，两处收口（禁再裸写）。

**偿还（已落代码）**：

- `tableWorkspaceState.setState` 内接线 `validateWorkspace`：dev 下（`import.meta.env?.DEV` 安全形式，生产零开销）对每次运行态变更做不变量自检，把"会失效的引用"在写侧就 `logger.warn` 暴露；**签名缓存**避免持久违规在拖拽宽度等高频 setState 下刷屏。
- `tableInvariants` 补 W5 实现 + 覆盖清单诚实化（W6 标注未实现）。
- `assistantTable` 抽 `mergeColumnsByLabel`，两处重复实现删除。

**补防回潮测试**（`tableWorkspaceState.test.ts` 2 条）：① 构造幽灵 `focusedCell` → 断言 setState 触发 `运行态不变量告警 W4`（验证"写了不跑"已修，属行为断言而非自证）；② `mergeColumnsByLabel` 保现有列 id、按归一去重、新列追加新 id。

**验证**：`type-check` src+tests 双向 0 错；表格域 4 文件 / **92 用例全绿**（`assistantTable` 66 + `tableWorkspaceState` 21 + memory 5 + property 2）。

**本区表格域模型层至此审完**。`validateWorkspace` 从"纸面契约"变为"运行期实际执行"。**仍未审**：表格 **UI 组件层**（`AssistantTablePanel.tsx` 778 / `AgentMessage.tsx` / `AgentConfirmCard.tsx`）—— 下一区优先项。

> ⚠️ 与历史债关系：TD-11-4/11-7 是"per-conversation 真源被 UI 副本污染"；TD-11-10 是"写了校验却不跑 + 重复实现"。三者共性 = 心法"**暴露缺陷而非掩盖**"的具体落点；本轮做法（接线自检 + 不谎报 W6 + 抽单一真相源）正是该心法的执行。
