# 架构日志 · 11 区（agent/skill 目录分组） · 2026-09-22

> 与同日《11-跨区-skill数据层收口》同轮工作的第二部分：**结构分组**（零行为变化）。

## 一、链路快照

`agent/skill/` 由 23 个文件**平铺** → 4 个子目录 + 顶层 2（门面 `index.ts` · 类型真源 `skillTypes.ts`）。
**域外零改动**：门面路径 `@/components/agent/skill` 不变 ⇒ 20 个消费方 import 一字未改。

```
skill/
├─ index.ts · skillTypes.ts    顶层：门面 + 类型真源（被所有层引 ⇒ 放子目录会逼人人跨目录引）
├─ rules/   纯规则（无 IO）      skillManifest · skillEntry · skillDirName · skillGroup · skillResourcePath · skillText
├─ store/   后端 / 存储 IO      skillRepository · skillApi · skillHydrate · skillMigration
├─ view/    界面判定纯函数       skillLibraryView · skillPickerView
├─ write/   副作用（落盘/网络）   skillPersist · skillImport · skillCloudSync
└─ model/   给模型              skillBuiltins · skillRegistry · skillBudget · skillInject · skillInjectText · skillResource
```

`spec/DATAFLOW.md` 已同步（`skillRepository` 新路径）。

## 二、溯源（Phase 1：症状 vs 病根）

症状：23 个文件平铺（8 个 < 80 行），找一个功能要在一屏里挑。
**先否掉了"合并减件数"**：按 `ADR-0053` 目的关，三处合并都答不出"复杂度降在哪一项"（只是"少几个文件"），
且各自会撞既有拆分理由（TD-11-60 六类混居的解药 / TD-11-52 / TD-11-59）⇒ 改走**分组**：不减文件，只减"一屏"。

## 三、定海（Phase 2）

本轮**不改任何 SSOT**（不动存储键、不动判据、不动门面导出面）。搬移只改文件位置与 import 路径。

## 四、切割（Phase 3：分组判据）

判据 =「**变更理由 + IO 边界**」——改哪一组，"该跑什么验证"是确定的：

| 组 | 改了要跑 |
| --- | --- |
| `rules/` | 纯函数单测 |
| `store/` | 存储契约 + `check:keys` |
| `view/` | 行 / 组判定用例 |
| `write/` | 落盘 / 冲突链路用例 |
| `model/` | 注入文本用例 |

**不是"看着像"**：门面与类型真源留顶层，判据是"被所有层引"。

## 五、探债（Phase 4）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| — | 本轮无新债 | 持平 | — | — | — | — | — | — |

## 六、覆盖度表（只登记本轮变化行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `agent/skill/**`（21 件移动） | 模块内 | 深审 | 🟢 | 11 / 2026-09-22 | 全量单测 + 四闸全绿 | 非债 |

**覆盖账**：本区 23 个源文件 / 本轮审 23（全部随搬移复核）/ 未审 0。

## 七、结论与下一步

- 本区域状态：`已验证(有债待还)`（TD-11-72 仍在，归 UI 批）。
- 待还债项已流入 `daily/架构日志/债务.md`：否（同前一轮，待随 UI 批登记）。
- DATAFLOW.md：已就地改正。
- 灯：本区覆盖度表已更新。

## 八、交付闸自查

| 闸 | 本轮结论（≤1 行） | 可复现凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | **无判据变化**（纯搬移；`check-arch.mjs` 改的是**路径字面量**，不是判据） | `npm run check:arch` → ✅ 架构校验通过 |
| 减一 · 兜底净减 | 无（本轮不减逻辑、不减分支） | — |
| 减二 · 复杂度实减 | **一屏可读**：23 平铺 → 5 组（每组 3–6 件）；模块文件数与总行数**不变** | 目录表见 §一 |
| 一诚实 · 失败诚实可见 | 未触碰任何失败路径 | — |
| 零炸 · 防回归防副作用 | 搬移零行为变化；全量单测 + 构建全绿 | `Test Files 272 passed` · `Tests 3286 passed` · `✓ built in 25.05s` |
| 一凭证 · 证据可复现 | `type-check` 0 · `check:arch` ✅ · `check:keys` ✅ · `check:dead-code` 67/67 | 上述命令输出 |

**零炸三问**：

1. **哪些行为被改了？** **无** —— 只改文件位置与 import 路径（用户与运行时都不可感知）。
2. **改前能走的路径，改后仍能走吗？** 是（全量单测 + `build`）。
3. **改前会失败的，改后仍失败吗？** 是（未触碰失败路径）。

**用户可感知的行为变化**：**无**。

## 九、决策留痕

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度（→ §八 减二） | SSOT（→ §八 一归） | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **不做**合并减件数（`skillText`→`skillImport` 等 3 处） | 答不出"复杂度降在哪一项"（`ADR-0053` 目的关）；且撞 TD-11-60/52/59 的拆分理由 | — | 件数不变、一屏照样乱 | — | **不做** |
| 2 | 改走**目录分组**（判据 = 变更理由 + IO 边界） | 每组对应确定的验证方式（可数） | 门面 + 类型留顶层 ⇒ 域外零改动 | 23 平铺 → 5 组 | 一归（导出面不变） | 做 |
| 3 | 移动 3 个受闸文件后**同步 `check-arch.mjs` 5 处路径** | 改路径不改判据 ⇒ 不走探针先红后绿 | `check-arch.mjs` 是脚本字面路径的唯一维护点（`CLAUDE §5.4.8`） | — | — | 做 |

**存在性凭证**：

```bash
grep -n "## 九、决策留痕" daily/架构日志/11-跨区-skill目录分组-2026-09-22.md
```
