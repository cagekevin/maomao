# 架构日志 · 文件管理（资源素材库 / 画布资产引用层收口） · 二轮 · 2026-09-13

> **文件名**：`12-文件管理-二轮-实施后复核-2026-09-13.md`（同区多轮各写新日期文件，不覆盖 [首轮设计态](./12-文件管理-资源素材库与画布资产引用层收口-2026-09-12.md)）。
> 区域 12（自主新增，跨 02/03/04/08 横切）。本轮状态：**已验证（有债待还）**。
> 审计依据：`债务登记5步法.md`（自底向上 + 覆盖度 + 反偷懒闸）+ 架构师心法 Phase 1–4；数据流实证统一 `node scripts/mv-sync-refs.mjs refs <file>`。
> 证据：本轮全部结论基于 2026-09-13 重新取证（**未抄首轮结论**）。

## 〇、本轮为什么开（区域状态回退的触发）

首轮（2026-09-12）把本区登记为「**审计中（设计态，待实施）**」——即 docs/122 的改动 #1–#7 尚未落码。但**本轮第一步取证即推翻该描述**：

- `git log` 实测存在落地提交 `3eddd67 feat(localTool): 文件管理收口——资源素材库与画布资产引用层对齐`；
- 代码实证 docs/122 的高杠杆项**已部分落地**：`database.ts:307` 建表已含 `project_id`、`:323-324` 有 `ALTER TABLE ... ADD COLUMN project_id`；`resources.ts:376-379` 的 `handleResourcesGet` 已接 `?projectId`；`fileStore.ts:133` 已有 `contentHashName` 内容寻址命名；`files.ts:108-116` 的 multipart 落盘已走 `writeUploadDedup` 按 `contentId` 去重；
- 首轮债前提的两函数 `applyResourceIdentityChange` / `rewriteUrlReferences` **在全仓已无定义**（`grep 'export (async )?function (applyResourceIdentityChange|rewriteUrlReferences)'` = 0 命中；仅 4 处**注释**残留）——改名/移动已改由 context-only 的 `applyResourceContextChange`(`resources.ts:512`) / `applyResourceContextMove`(`:544`) 承担。

→ 状态按 §区域治理规则 6 回退：**从「设计态」进入「实施后复核」**；首轮 TD-12-1 的前提已变、TD-02-13/14 的前提已消失（见 §五 改判）。

## 一、链路快照（refs 实证，非凭文件名猜）

| 主文件 | refs / grep 实证 | 判定 |
| - | - | - |
| `src/components/base/store/resourceStore.ts` | `refs` 模块引用 **11** 处（LeftPanel/ResourceLibrary/AssetNode/ImageGenerate/ScriptBoxAssetPicker/StepAssets/scriptBoxEngine + 4 测试） | 现实枢纽 = 前端素材库 SSOT |
| `src/components/base/api/filesApi.ts` | `refs` 模块引用 **32** 处（resourceStore/ResourceLibrary/GeneratedView/OverlayEditor/DepthVideoModal/videoEngine/d3dPersistence/ImageBoxNode/useImageHoverActions + api barrel + 21 测试） | 落盘唯一桥，契约已收口（@见 03 五轮） |
| `localTool/src/utils/fileStore.ts` | 模块引用 0（**import 用 `.js` 后缀，refs 以 `.ts` 路径查不到**）；`grep 'utils/fileStore.js' localTool/src` = **3**：`routes/files.ts`、`routes/localPatch.ts`、`routes/resources.ts` | 后端落盘真源（低层、无 DB 依赖） |
| `localTool/src/routes/resources.ts` | 模块引用 0；`runtime` 挂载在 `router.ts:188-192`；`applyResourceContextMove` 被 `routes/files.ts:34` import | 后端资源表 / 上下文变更真源 |
| `localTool/src/routes/files.ts` | 上传/读/缩略图/mkdir/move/open/list；`writeUploadDedup` 唯一调用方（`:108`） | 后端文件路由 |
| `src/components/nodes/AssetNode.tsx` | `resourceStore.ts:36` 导入 `sendToResourceLibrary / getResources` | 引用方（@见 04） |

**DATAFLOW 对账**：`spec/DATAFLOW.md` 资产/素材段（175–192）已写 `move[context-only]`，与实测一致 → **该段已对账一致**（但旧「改名/移动唯一入口 applyResourceIdentityChange」表述在**注释**里残留，见 TD-12-6；另补灯，见 §七）。

## 二、溯源（Phase 1：症状 vs 病根）

- **首轮三条根因的处置已分化**：根因 A（重复文件）→ 已由 `contentHashName`(`fileStore.ts:133`) + `writeUploadDedup`(`:216`) + `upsertResource` 同内容同行(`resources.ts:303-340`) 在**落盘边界**收口（源头收敛，非末端补丁 ✅）；根因 B（文件不跟 project）→ **表/端点/前端传参三处都在，但写入链断了一节**（§五 TD-12-5）；根因 C（双注册表）→ **未动**（`resourceStore.ts:263` 仍 `genId()` 随机 id vs 后端 `resourceIdOf` 路径 id，`TD-12-2`）。
- 没有发现「为兼容乱象而在末端写 if-else」的新补丁——本轮的缺陷属于**实施不完整**，不是策略模式兜底。

## 三、定海（Phase 2：不变式 / 唯一真相源）

三条不变量（docs/122 §1）本轮实测状态：

1. **SSOT 内容（字节唯一真源 = `contentId`）**：✅ 成立。`writeUploadBuffer`(`fileStore.ts:151-152`) 以 `sha1(bytes)` 命名；`doSaveRemoteUrl`(`files.ts:291-303`) 下载后按**解码字节**算 `contentId` 查重复用；`upsertResource` 落实「同内容→同行」。
2. **project 跟随（`resource.projectId` 绑定当前项目）**：❌ **不成立**——见 TD-12-5（登记归一化静默丢弃 `projectId`）。
3. **context 与 content 正交（改名/移动只动 context，永不破图）**：✅ 成立。`handleResourcesRename`(`resources.ts:582`) → `applyResourceContextChange`(`:512`) 只 `UPDATE ... SET name`；`handleMove`(`files.ts:466`) → `applyResourceContextMove`(`:544`) 只 `UPDATE ... SET folder`；两处都**不碰磁盘、不改 `url`/`contentId`**，且 `contracts.ts:106-107` 明确 `resource:renamed` 事件已删（四态 url 改写广播随 context-only 消失）。

> 类型诚实（铁律 2）：`ResourceItem`（`localToolApi.ts:42`）已声明 `projectId`，但前端域模型 `Resource`（`resourceStore.ts:37-49`）**未声明** `projectId`，靠 `[key: string]: unknown`(`:48`) 兜住 → 编译器对「登记时丢字段 / 字段名拼错」完全无感（正是 TD-12-5 能被写出来还过 `type-check` 的原因）。

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- **收敛口（choke-point）已立但未唯一**：`writeUploadBuffer` 名义是「内容寻址落盘」单点，实测后端仍有**第二个绕过入口**——`handleResourcesSave`(`resources.ts:426`) 直调 `writeUploadBuffer` 且用 `clip-${Date.now()}${ext}` **时间戳命名**，绕过 `writeUploadDedup`/`contentHashName`（→ TD-12-7），也违反 docs/122 §7 验收「grep `writeUploadBuffer` 直调仅剩 fileStore 内部」。
- **无需新抽象**：本轮无「上帝类 / 过度抽象」证据；`buildResourceRecord` 作为登记归一化纯函数本身就是**正确的收口点**，缺陷是它**漏抄了一个字段**（不是接缝设计错）。

## 五、探债（Phase 4：技术债账本）

> ⚠️ 登记阶段**只读不改码**（见 `债务登记5步法.md` 第一条禁令）：`偿还计划` 写的是"将来怎么改"，本流程不执行。

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| **TD-12-5** | **项目隔离写入链断裂（母体）**：`buildResourceRecord`(`resourceStore.ts:261-272`) 返回对象**不含 `projectId`**，而两处调用方都传了它——`sendToResourceLibrary`(`:353`)、`localizeAndStoreToResourceLibrary`(`:524`) 均传 `projectId: currentProjectId() || undefined` → **静默丢弃**；`resourcesOfProject`(`:223` `r.projectId == null \|\| r.projectId === pid`) 因此恒判 legacy → 新素材**跨项目可见**。次生：后端落盘链（`filesApi.uploadFileToLocal`/`saveInlineToLocal`）**不带 projectId** → 后端行 `project_id` 恒 NULL（`database.ts:312` rescan 行亦保持 NULL）。违反 docs/122 §7 验收「素材库切换项目 → 素材集随 `currentProjectId` 变化」 | 增债 | 结构债 | `resourceStore.ts` + `ResourceLibrary.tsx` + `AssetNode`/`ImageGenerate`/`scriptBoxEngine`（均经 send/localize 登记）+ 后端 `resources` 落盘 | 中 | **母体 M-12-1**：声明式字段未纳入「登记归一化」；域模型用 `[key:string]: unknown` 兜底（`:48`），编译器无法发现漏字段 | ① `Resource` 显式声明 `projectId?: string \| null`（去索引签名兜底）；② `buildResourceRecord` 透传 `item.projectId`；③ 落盘请求携 `projectId`（后端 `handleResourcesSave`/upload 写 `project_id`）；④ 补 2 测试：登记后 `projectId` 保留 + `useResources` 按项目过滤生效（现测试用手写 `as never` 字面量，`tests/unit/resourceStore.test.ts:113`，测不到登记链） | 待还 |
| **TD-12-6** | **注释漂移指向已删除函数**：`files.ts:465`、`fileStore.ts:12`、`helpers.ts:30` 三处文档仍称「真·身份变更由 `applyResourceIdentityChange` 承担 / 改名后命名失效 / HttpStatusError 为它而设」，但 `applyResourceIdentityChange` 与 `rewriteUrlReferences` **全仓已无定义**（仅这 3 处 + `resources.ts:509` 注释残留）→ 误导「物理改名 + 引用改写机制仍在」 | 增债 | 结构债 | 3 个注释（`files.ts` / `fileStore.ts` / `helpers.ts`） | 低 | 孤例：一次 context-only 重构删了函数却未清引用注释（描述漂移，五问第 5 问） | 三处注释改为事实：「改名/移动已收口 context-only（`applyResourceContextChange`/`Move`）；原物理移动 + 引用改写机制已删」；`resources.ts:509` 同批 | 待还 |
| **TD-12-7** | **第二落盘入口绕过内容寻址/去重**：`handleResourcesSave`(`resources.ts:426`) 对 dataURL 素材直调 `writeUploadBuffer(folder, clip-${Date.now()}${ext}, buf)`——时间戳命名、**未经** `writeUploadDedup`/`contentHashName` → 同字节可落两份物理文件（DB 侧仅靠 `rescan` 的 contentId 查重(`:210-218`) 跳过建行，遗留无 row 的孤儿文件待 GC）；违反 docs/122 §7「`writeUploadBuffer` 直调仅剩 fileStore 内部」 | 增债 | 结构债 | `resources.ts` + 后端 `uploads/` 磁盘 | 低 | 孤例：第二落盘入口未随 #1/#6 收口（`files.ts` 已收口，`resources.ts` 漏）。**注**：该端点 src 侧 0 生产调用方（`saveResource` 仅 `contracts.ts:761` 登记 + `tests/unit/resourcesApi.test.ts:105`），故无现网实害 | A：该端点改走 `writeUploadDedup`（与 multipart 同口径）；或 B：若确认零消费 → 退役端点并摘 `apiRegistry` 登记（`/api/resources/batch-save` 已是 `前端零消费/RESERVED`，`contracts.ts:1097`） | 待还 |

**本轮改判（留痕，禁默默略过）**：

| TD-ID | 原判 | 现判 | 证据 |
| - | - | - | - |
| **TD-12-1** | `resources` 加 `project_id`（改 DB 契约 + `GET ?projectId`），增债·不可逆·待还 | **[已解决 2026-09-13]**（schema+端点+前端传参三处均落地） | `database.ts:307` 建表含 `project_id`、`:323-324` `ALTER TABLE ... ADD COLUMN`；`resources.ts:376-379` `?projectId` 过滤（`helpers.ts:227` nullOrEqCols）；`ResourceLibrary.tsx:143/193/238` 透传 `projectId`。**功能有效性**残留 → @见 **TD-12-5**（写入链丢字段，非本债范围） |
| **TD-02-13** | `rewriteUrlReferences` 直改 `kv.value` 不推进 `_version` → 其他窗口 CAS 覆盖 | **[已解决 2026-09-13·由区域 12 context-only 设计取代]** | `rewriteUrlReferences` 全仓 **0 定义**（仅 4 处注释残留）；`contracts.ts:106-107` 明确 `resource:renamed` 已删、改名/移动改由 contentId 模型处理；改名/移动不再改 `url` → 「同 key 内容变更」路径消失（3s 轮询/CAS 前提不成立）。@见 `12-文件管理-二轮` |
| **TD-02-14** | 改名/移动的引用改写失败不可见（返回值无信号） | **[已解决 2026-09-13·同上]** | 同上：`applyResourceIdentityChange`/`rewriteUrlReferences` 已不存在，无「改写失败」可暴露；context-only 下 `url`/`contentId` 不变故无 404 破图面。@见 `12-文件管理-二轮` |

**仍待还（首轮已登记、本轮复核前提仍成立）**：

- **TD-12-2**（持平→待还，中）：双 id 空间未合并——`resourceStore.ts:263` `genId()`（`resource-xxx`）vs 后端 `resourceIdOf`（`local-${folder}-${name}`）；`mergeResourcesFromBackend`(`:300-301`) 保留本地随机 id 项、再并入后端项 → **同一素材两行**（本地上传项 + 后端 rescan 项）。首轮判定「改动 #7 投影化时结清」仍成立。
- **TD-12-3**（待还，低）：`clip-*`/旧时间戳文件残留（`uploads/` 磁盘），命名空间不同不冲突；由 docs/13 引用感知 GC 自然回收。本轮无法在只读流程内核实磁盘，维持。

## 六、覆盖度表（查过什么 / 还剩什么 —— 防重复审、防假装审完）

> 状态只允许：`未审` / `抽审` / `深审` / `已裁定非债` / `有债待还(TD-xx)`；灯：🟢 无债 / 🟡 持平待裁定 / 🔴 有债待还 / ⚪ 未审。

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| - | - | - | - | - | - | - |
| `src/components/base/core/contracts.ts` | L0 契约 | 深审（@见 02/13/09） | 🟢 | 02 十三轮 09-13 | `:106-107` resource:renamed 已删；`:761` saveResource 登记 | 无债 |
| `localTool/src/utils/fileStore.ts` | L1 落盘真源 | 有债待还(TD-12-6) | 🔴 | 二轮 09-13 | `:133` contentHashName / `:216` writeUploadDedup / `:53` UPLOAD_ROOT_ALLOW / `:12` 注释漂移 | TD-12-6 |
| `src/components/base/utils/uploadDirs.ts` | L1 白名单 | 抽审（闸实证） | 🟢 | 二轮 09-13（闸 09-13 立） | `gates.manifest.json:46-50` upload-dirs 闸在 build 阶段 | TD-12-4 已解决 |
| `localTool/src/utils/helpers.ts` | L1 工具 | 有债待还(TD-12-6) | 🔴 | 二轮 09-13 | `:30` 注释漂移；`:227` nullOrEqCols 真实现 | TD-12-6 |
| `localTool/src/db/database.ts` | L2 DB 契约 | 抽审（仅 project_id 段） | 🟢 | 二轮 09-13 | `:307`/`:323-324` project_id 列 + 迁移；`:312` rescan 行恒 NULL | TD-12-1 已解决 |
| `src/components/base/store/resourceStore.ts` | L2 store（核心） | 有债待还(TD-12-5 · TD-12-2) | 🔴 | 二轮 09-13 | `refs` 模块 11 处；`:261-272` 丢 projectId；`:300-301` 双注册表 | TD-12-5 · TD-12-2 |
| `src/components/base/api/localToolApi.ts` | L2 api 薄壳 | 抽审 | 🟢 | 二轮 09-13 | `:42` ResourceItem 已声明 projectId；`:193` fetchResources 收 projectId；`:214` saveResource | 无债 |
| `src/components/base/api/filesApi.ts` | L2 落盘桥 | 深审（@见 03 五轮） | 🟢 | 03 五轮 09-13 | `refs` 模块 32 处；契约微漂移 4 处已清 | 无债 |
| `localTool/src/routes/resources.ts` | L2 路由 | 有债待还(TD-12-7) | 🟡 | 二轮 09-13 | `:426` 直调 writeUploadBuffer + 时间戳命名；`:512/:544` context-only | TD-12-7 |
| `localTool/src/routes/files.ts` | L2 路由 | 有债待还(TD-12-6)（08 首轮已审 → 本轮改动后回查） | 🔴 | 二轮 09-13 | `:108-116` writeUploadDedup；`:465` 注释漂移；`:466` handleMove context-only | TD-12-6 |
| `src/components/base/panels/ResourceLibrary.tsx` | L3 UI | 抽审 | ⚪ | 二轮 09-13 | `:143/189-198/234-238` projectId 透传 + mergeResourcesFromBackend | 下轮深审（列表/分页/拖拽/预览） |
| `src/components/nodes/AssetNode.tsx` | L3 节点 | 深审（@见 04） | 🟢 | 04 区 | refs → `sendToResourceLibrary/getResources`（`:36`） | 无债（登记缺 projectId @见 TD-12-5） |
| `src/components/nodes/ImageGenerate.tsx` | L3 节点 | 深审（@见 04） | 🟢 | 04 区 | `:571` sendToResourceLibrary（不传 projectId，由 store 内部注入） | 无债（@见 TD-12-5） |
| `src/hooks/useResourceMoveToFolder.ts` | L3 hook | 未审 | ⚪ | — | 拖拽移动归类 → `/api/files/move`（context-only） | 下轮 |
| `src/hooks/useAssetDragToCanvas.ts` | L3 hook | 未审 | ⚪ | — | 消费 useResourceMoveToFolder | 下轮 |
| `src/components/scriptbox/ScriptBoxAssetPicker.tsx` | L3 UI | 未审 | ⚪ | — | `:52` mergeResourcesFromBackend | 下轮 |
| `src/components/scriptbox/StepAssets.tsx` | L3 UI | 未审 | ⚪ | — | `:16` resourceFolderOf | 下轮 |
| `src/components/base/panels/LeftPanel.tsx` | L3 UI | 未审 | ⚪ | — | `:41` `useResources()` | 下轮（仅 1 处消费） |

**覆盖账**：本区共 **18** 个文件 / 本轮审 **13** 个 / 未审 **5** 个（逐个列名 + 理由）：

- `src/hooks/useResourceMoveToFolder.ts` — 理由：拖拽移动归类链路（双 MIME 协商 + context-only 移动），本轮未读，下轮深审（首轮 §5 曾列为 8 机制之一）。
- `src/hooks/useAssetDragToCanvas.ts` — 理由：同上，仅是 `useResourceMoveToFolder` 的宿主。
- `src/components/scriptbox/ScriptBoxAssetPicker.tsx` — 理由：只做后端列表并入，价值低，下轮。
- `src/components/scriptbox/StepAssets.tsx` — 理由：只读 `resourceFolderOf` 映射，下轮。
- `src/components/base/panels/LeftPanel.tsx` — 理由：仅 1 处 `useResources()`，下轮。

> 另注：`localTool/src/routes/localPatch.ts`（`writeUploadBufferAt` 落盘）**不属本区**（属本地补丁/合并特性），未纳入本区覆盖表，交其归属区。

## 七、结论与下一步

- 本区域状态：**已验证（有债待还）**（首轮「设计态待实施」已被代码推翻；实施已落地大半，问题转为「实施不完整」）。
  - ⚠️ `已还清` ≠ 覆盖完备：本表仍有 **5 行 `未审`**，故**不许**写「该区审完」。
- 待还债已流入 `daily/架构日志/债务.md`：**是** —— 新增 `TD-12-5`/`TD-12-6`/`TD-12-7`；改判 `TD-12-1`（→已解决）、`TD-02-13`/`TD-02-14`（→已解决·由 context-only 取代）；维持 `TD-12-2`/`TD-12-3` 待还。
- **DATAFLOW.md**：
  - 资产/素材段与实测**已对账一致**（`move[context-only]` 已写）；本轮**就地追加**两条 `更新(2026-09-13, refs实证)`（① 实施已落地、② 项目隔离写入链断裂 + `applyResourceIdentityChange`/`rewriteUrlReferences` 已删）；**禁静默删旧链路**。
  - 灯已按本覆盖度表就地改：`resourceStore.ts 🔴`、`fileStore.ts 🔴`、`resources.ts 🟡`、`files.ts 🔴`、`uploadDirs.ts 🟢`、`filesApi.ts 🟢`、`localToolApi.ts 🟢`、`AssetNode.tsx 🟢`（**只改灯，未塞明细**）。
- 下一步（本流程不施工，交给统一决策架构师）：
  1. **TD-12-5** 是唯一「利息率 中 + 有功能实害」的项，建议优先（改动小：1 处归一化 + 1 处类型声明 + 落盘携参 + 2 测试）。
  2. TD-12-6/12-7 可并入同批（均为低息、同属「实施未收尾」）。
  3. 收尾后按首轮 §六 节奏续做 #7（投影化，结清 TD-12-2）。
  4. 覆盖表 5 行 `未审` 留待下一轮（拖拽移动 hook 优先）。
