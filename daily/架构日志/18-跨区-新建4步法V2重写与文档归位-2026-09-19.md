# 架构日志 · 18 跨区 · 新建 4 步法 V2 重写与文档归位 · 2026-09-19

> **本轮中心**：`.codebuddy/commands/架构师写新业务代码.md`（V1）的**审计与重写**。
> 用户指定："任务已经变成修复这个文档了，这个 MD 文档才是你现在的中心"；随后拍板"V1 留作历史，写 V2，
> 只管这一份，总纲由我（架构师）定"；并明确"缺的东西登记债务即可，不要顺手写代码，重点还是 V2"。
> **结论落点**：本文件（唯一写全处）。

---

## 一 · V1 审计结果（6 处问题，均已修）

| # | 问题 | 处置 |
|---|---|---|
| 1 | **悬空引用**：审计第 10 条要求执行「TS+React 异常／容错代码禁止项」—— 全仓无此文档；其机器形态 `scripts/check-silent-catch.mjs` **已于 2026-09-17 按用户裁定删除**（`gates.manifest.json`「注（2026-09-17）」：贴 `// catch-ok:` 标记的合法通过成本低于真正改造 ⇒ 反而在生产它本要禁止的行为） | 改为逐条校验 ADR-0011 / 0002 / 0003 / 0019 / 0036 |
| 2 | 「禁止裸 throw 给调用方」**过宽**（本仓 throw 合法，如 ADR-0005 要求含函数/Symbol/DOM 的克隆根部抛 `DataCloneError`） | 补 throw 边界：只用于"不该发生" |
| 3 | 自证式断言**自指**（未指向 7步法 §7.2 + A15 真源） | 指向真源 |
| 4 | Step 2 的 `{ok:false, error, msg}` 与 §2 模板 `message` **自相矛盾** | 统一 `code` + `message`（实测 `message` 9 / `error` 5 / **`msg` 0**） |
| 5 | ADR-0037 **引错**（它管"工具不得替调用方决定 import 后缀"，不是"必须写显式后缀"） | 补说明，去张冠李戴 |
| 6 | 笔误「直 point」 | 修正 |

## 二 · V2 重写（`.codebuddy/commands/架构师写新业务代码-V2.md`）

**总纲（架构师定）**：本文边界（只管"从零构建"）· 判据真源分工表（防两份）· 红线 vs 结构偏好 · 判据写法硬约束。

**核心增补**：

- **§0.4 判据写法硬约束**：每条标准必须给「红线 + 验收 + 🔍 违反时怎么发现」，**答不出 ⇒ 不许写**。
  ⇒ 首轮自审发现 **§2 自己没遵守**（Step 2/3 是口号），已补齐：现共 **18 处** 🔍，覆盖 §1.1–1.4 + Step 1–4 + 审计 prompt。
- **§0.5 不变量下沉**（本文第一原则）：**L0 文字（无强制力）→ L1 类型层（编译期）→ L2 唯一入口收口 → L3 闸（默认不建）**；
  与心法对应：Phase 2「筑基与定海（不变式与唯一真相）」=L2 · 三.2「捍卫类型与边界的绝对诚实」=L1 · 零.4.1「闸不是红线 + 成本守恒」=L3。
- **§0.6 本文自身维护约定**（V1 死因）：给了自检命令 + "谁改了被引用者谁更新本文" + "描述与签名冲突以签名为准"。
- **举例全部本仓化**：删虚构 `user.role==='admin'`（本仓无角色系统，0 命中）⇒ 换真实 `ResourceLibrary.tsx:104 / :151-154`。
- **store 两种模式**：`useSyncExternalStore` 22 处（默认）vs `zustand` 7 处且全在 `videoEditor/stores/`；判据=**按域一致**。
- **§4.7 测试**：补桩约定（`importOriginal` 展开，否则加导出即崩）+ jsdom 环境规则。
- **§4.8 新画布节点**：最常见问题形态的必备接线（NodeShell / nodeDefaults / NODE_TYPES / NODE_HANDLE_CONTRACT / nodeDataSchema / generateId / patchNodeDataById / persistUrlToUpload）；⚠️ 节点域闸在 **build** 阶段不在 push。
- **§4.9 渲染异常**：`ErrorBoundary`（`NodeShell.tsx:324` 已自动包节点级）+ `main.tsx` 全局兜底（P0-5，非渲染期异常走 `logger.error` → `/api/logs`）。
- **§5 原语表**：补回漏掉的横切出口（toast / eventBus / downloadUrl / contracts 五张登记表 / 文案直写中文）与落盘域分层。

## 三 · 顺带查出并处置的问题

| 项 | 取证 | 处置 |
|---|---|---|
| **CLAUDE.md 引用的 6 个 docs 全部找不到** | 实际都在 `docs/plan/`（路径前缀不对） | 5 个已 `git mv` 到 `docs/`（ARCHITECTURE / BASE-CAPABILITIES / CANVAS_PERFORMANCE / NODE-DESIGN-SPEC / README）；全仓旧路径引用 = 0 |
| `node-types-map.md` | **被有意删除**（`docs/README.md:87`：随 `src/bundle/` 移除；映射真源 = `src/components/` 实际组件 + `contracts.NODE_TYPES`） | 回改 3 处过时引用：`ARCHITECTURE.md:5`、`CODING-STANDARD.md:16`、`:39`（后者原标"✅ 有效"是错的） |
| 落盘域"深模块" | `persistUrlToUploads` **本就是 facade**（`filesApi.ts:655-682` 委托 saveInlineToLocal / uploadFileToLocal / uploadRemoteUrl）；`uploadFileToLocal` 的 11 个调用方传的全是 File/Blob ⇒ 用底层正确，非平行实现 | **TD-02-66 证伪改判非债** |
| 落盘返回契约描述漂移 | 文件头「失败一律返回 null 不抛」、`uploadFileToLocal` 的 `@returns` 写 `Promise<string\|null>`，实际是 `UploadOutcome` 判别联合（`:315`） | 就地更正（纯注释，`filesApi.test.ts` 53 条绿，`tsc` 0 错） |

## 四 · 本轮登记的债务（**只登记，不施工** —— 用户明确）

| 债号 | 内容 | 状态 |
| --- | --- | --- |
| **TD-18-32** | `import.meta.env` 裸读越唯一入口 4 处（`models.tsx:81` BASE_URL；`project.ts:517`／`modalLayer.ts:129`／`tableWorkspaceState.ts:120` 的 `DEV`）。`config.ts` **已备** `IS_DEV` / `BASE_URL` 出口 ⇒ 收敛可行 | 待还 |
| **TD-18-33** | 缺遥测：`logger` 只出文本日志流（`POST /api/logs` + `GET /api/logs/stream`），**无结构化事件/聚合/历史查询**（`base/core/logger.ts` 无 `track`/`metric`）。**已定：纯本机、不出网**。设计要点：唯一出口 `trackEvent` + 事件登记 `contracts.TELEMETRY_EVENTS` + 查询端点 | 待还（①③ 目的与最小范围未定，**未经定权不得开工**） |
| **TD-18-34** | **画布域分裂（大级别）**：`base/canvas/`（18 文件：nodeDataSchema/nodeDefaults/deriveNodes/historyStack/nodePrefs/toolRegistry…）与 `agent/canvas/`（3 文件，含**写唯一入口 `canvasHost`**，DATAFLOW:132"禁裸 useReactFlow"）并存；**两处均无 `index.ts` 门面**（fan-in：`canvasHost` 5 个文件）⇒ 宽接口、非深模块。**与 filesApi 收口前"域劈两半"同构** | 待还 |
| **TD-18-35** | **图片/媒体处理域分裂**：9 个文件散在 `base/utils/`（约 1400 行：`imageCompress` 180 / `imageUpscale` 164 / `imagePixel` 41 / `captureFrame` 263 / `faceMosaic` 412 / `encoderProbe` 108 / `externalizeInline` 95 / `useImageFallbackSrc` / `useMediaLoadFailed`）⇒ 收敛为 media 域深模块（注意现有 `base/media/` 是"媒体引用/素材库"域，非同一关注点） | 待还 |
| **TD-18-36** | `storage/kvStore.ts`（26 行）仅 re-export 兼容常量 `CANVAS_STATE_PREFIX`，是**浅壳** ⇒ 常量归真源后删壳 | 待还（低） |
| **TD-18-37** | **打码域分裂（与 canvas 同型）**：逻辑 `utils/faceMosaic.ts`（**412 行**）与 UI `editors/FaceMosaicEditor.tsx` 分家两处 | 待还（中，需 Step1 证伪） |
| **TD-18-38** | **前端 AI 中继子域平铺**：`api/generate.ts`(334) + `api/pollTask.ts`(164) + `api/relayProxy.ts`(421) = **919 行**平铺在 `api/`；**后端已有 `ai-relay/` 域模块可作对照** ⇒ 前端收为 `api/relay/` 深模块 | 待还（中） |
| **TD-18-39** | **提示词域疑似分裂**：`creative/promptManager.ts` 与 `prompt/*`（`promptChips` / `promptMention` / `promptHubStore` / `promptLayout`）各占一处 | **已裁定非债（证伪）**：三者是**不同关注点** —— `promptManager` = 提示词**预设**数据层（localStorage）· `promptChips` = **DOM 芯片**转换层（依赖 `document`，非纯逻辑，TD-05-5 已诚实化口径）· `promptHubStore` = 提示词**社区库**数据层。职责不重叠，不合并 |
| **TD-18-40** | `store/generationContract.ts` **名不副实**：实为"生成编排原语**唯一实现**"（TD-01-8 收口），却叫 `Contract` 且住在 `store/` ⇒ 易被误判为第二份契约（真源 `core/contracts.ts`） | 待还（低，命名/落点误导） |
| **TD-18-42** | `videoEditor/lib/logger.ts`（40 行适配壳 → `base/core/logger`，category 固定 `videoEditor`）**导出符号也叫 `logger`**，与统一 logger 撞名 ⇒ 改名 `videoEditorLogger` | 待还（低） |
| **TD-18-43** | `videoEditor/lib/toast.ts`（44 行适配壳 → `base/core/toastStore`）导出也叫 `toast`，与统一提示出口撞名 ⇒ 改名 `videoEditorToast` | 待还（低） |
| **TD-18-44** | **videoEditor 工具层散在 3 处**：`utils/`(4：string/platform/browser/ui) + `lib/`(3：toast/mediaDisplayUrl/logger) + `engine/lib/`(6：i18n/scenes/export/time…) 共 13 文件、三种命名 ⇒ 收敛命名与落点 | 待还（中） |
| **TD-18-45** | `src/types/asset.ts` 与 `videoEditor/types/assets.ts` **只差一个 s**（前者全局 `AssetType`/`AssetLoadOptions`，后者 `MediaAsset`/`MediaAssetData`；类型名不撞但文件名易混）⇒ 文件名需可区分 | 待还（低） |
| **TD-18-46** | **通用 UI 组件层两套**：`base/ui/`(24，含 Select/ContextMenu/Toggle/NodeShell/ErrorBoundary) 与 `videoEditor/ui/ui/`(31，含 select/context-menu/button/dialog/tooltip/menu/tabs/slider) ⇒ 同类基础组件做了两套，收敛为一套 | 待还（中） |
| **TD-18-41** | **命名与落点约定缺失**（本轮排查顺手登记）：`store/` 15 个文件中 **6 个**非 `*Store.ts`（`appSettings` / `autoSync` / `cloudSync` / `generationContract` / `settingRegistry` / `taskCompletionBus`）⇒ 目录语义被稀释；`Contract` 一词应专属 `core/contracts.ts`；**同域的 UI 与逻辑应同目录**（反例 `utils/faceMosaic.ts` vs `editors/FaceMosaicEditor.tsx`）。**需立前缀与目录规范**（含门面 `index.ts` 规则） | 待还（中） |
| **TD-18-42** | `videoEditor/lib/logger.ts`（40 行，转发到 `base/core/logger`，category 固定 `videoEditor`）与统一 `logger` **撞名** —— 二者 import 后都叫 `logger`，在 videoEditor 域内极易误引 ⇒ **改名为 `videoEditorLogger`**（文件同名），保留适配转发 | 待还（低） |

**排查中确认"不是问题"的（留档，防后人重查）**：
- `videoEditor/lib/logger.ts` **不是**第二份实现（40 行纯转发适配，为 ~85 处原 `console.` 调用点兼容）—— 但符号撞名是真问题（→ TD-18-42）。
- `store/generationContract.ts` **不是**第二份契约（实为生成编排原语唯一实现，TD-01-8 收口）—— 但命名误导（→ TD-18-40）。
- `toAbsoluteFileUrl` **无重复**（`assetUrl.ts:34` import 后 re-export，TD-06-7 已收口）。
- `base/media/` 与 `utils/` 图片处理**不是同一关注点**（前者=媒体引用/素材库，后者=图像处理原语）。
- `depthVideo/` 是**域内 UI 与逻辑同目录**的正确写法（`DepthVideoModal.tsx` + `depthUrls/engine/loader/spawn`）。
- `panels/` 无游离逻辑 `.ts`（只有 `panel-kit.css` + `sections/`），干净。
- 钩子：`src/hooks/` 之外有 **45 个** `use*` 文件，但绝大多数属各自域内（`videoEditor/hooks-cutia/*`、`nodes/*`、`director3d/*`）⇒ 域内 hook 就近合理，**不列为债**。

## 五 · 度量

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| V1 悬空/错引 | 3 处（catch 闸 / ADR-0037 / 自指） | 0 |
| V1↔V2 内部矛盾 | 1 处（契约字段） | 0 |
| V2 带「🔍 违反时怎么发现」的条目 | 4（仅 §1） | **18**（§1 + Step1–4 + 审计） |
| V1 体积 → V2 | 43.5 KB | 40.3 KB（结构重写，非删减） |
| V2 引用路径终检 | — | **0 MISS** |

## 六 · 交付六闸自查

- **一归**：判据真源分工表建立；自证式断言定义段唯一；V2 自称持有的判据与 7步法/心法/ADR 无重叠 ✅
- **减一**：未新增兜底 ✅
- **减二**：V1 的重复/矛盾消除；V2 结构比 V1 更可直接执行 ✅
- **一诚实**：TD-02-66 **登记后经取证改判非债**（附反证）；"未验证故未写"的三项已明确标注 ✅
- **零炸**：本轮**未改任何业务代码**（仅 5 个文件 `git mv` + 3 处注释回改 + 文档） ⇒ 无回归面 ✅
- **一凭证**：每条结论附落点行号/命令；V2 全文路径 0 MISS；债账 `audit` 已复核 ✅

## 七 · 遗留（诚实边界）

1. V2 **未写性能阈值**（无证据：`React.memo` 42 / `useMemo` 63 / `useCallback` 109 有实践但无成文阈值）⇒ 只指 `docs/CANVAS_PERFORMANCE.md`。
2. TD-18-33 的 ①目的 ③最小范围 **未定** ⇒ 不得开工。
3. 本仓 `director3d` 域多处自成一套（ErrorBoundary / log.ts），属已登记例外，未纳入收口。
