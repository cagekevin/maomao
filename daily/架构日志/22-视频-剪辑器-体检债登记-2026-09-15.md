# 22 · 视频（剪辑器）· 初级 bug 体检与债登记 · 2026-09-15

> 触发：用户要求「全面检查 video editor 的初级 bug；简单的修、麻烦的登记」。
> 本轮前段（用户逐项授权）已修 3 笔；后半段用户改口**只读登记**（2026-09-15 口谕：禁止修改一行代码）。
> 本文件 = 本轮结论唯一落点；`债务.md` 摘要经 `debt.mjs add` 写入（禁手写）。

## 一、链路快照（本轮取证过的面）

- 面板控件链：`properties/*` → `editor.timeline.updateElements`（`pushHistory` 两段式提交）
- 渲染链：`renderer/nodes/*` → `visual-node.isInRange` → `getVisualSourceTime`（`element-utils.ts:125`）
- 拆分链：`commands/timeline/element/split-elements.ts` → `timeline.updateTracks`
- 导出解码：`engine/lib/media/mediabunny.ts:133`（自算 trim 上界）
- 键盘链：`hooks-cutia/use-keybindings.ts`（编辑器内，防护齐全）＋ 宿主 6 处 window/document 级监听（**无编辑器门**）

## 二、本轮已修（前段，用户逐项授权「简单的立刻修」）

| # | 修复 | 证据 |
| --- | --- | --- |
| 1 | 字体下拉「根本没显示」：编辑器根 `z-ceiling(2147483647)` 盖住 body 上 `z-50` 的 Portal 弹层 → 编辑器根归位 `z-modal`、7 类弹层归位 `z-modal-raise`；FontPicker 改面板流内展开（`font-picker.tsx` 重写，不再 Portal） | `App.tsx:1570` · `ui/ui/{select,dropdown-menu,popover,tooltip,dialog,context-menu,color-picker}.tsx` |
| 2 | sticker 颜色写回丢 `#`（同字段两种格式，靠 `normalizeHex` 容错掩盖）→ 写回补 `#` | `sticker-properties.tsx:527` |
| 3 | 背景默认值字面量 12 份散落 → 收口 `DEFAULT_BG_{OPACITY,PADDING_X,PADDING_Y,BORDER_RADIUS}` | `text-constants.ts` · `text-properties.tsx`（16 处接入） |
| 4 | split 的 trim 换算：变速后偏移未乘 `playbackRate`；`trimEnd` 违反不变式 `trimEnd = trimStart + duration×rate` | `split-elements.ts:45-107`（与 `getVisualSourceTime`、`mediabunny.ts:133` 同源对齐） |
| 5 | 字体清单含 5 个未加载的"Google 字体"（画布静默回落 = 选了无效）→ 清单收敛为真实系统字体 + 回落链 `buildFontFamilyStack` | `font-constants.ts` 重写 · `renderer/font-stack.ts` 新建 · `text-node.ts:108` |

**关联旧债**：#4 与 **TD-22-14**（变速漏 2 落点：mixClip / PlaybackSink）同属母体「**变速感知散落各路径、无单点原语**」——split 是第 3 个落点，本轮已修；TD-22-14 的两个落点仍待还。

## 三、探债（本轮新登记）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-19 | 编辑器全屏时宿主 6 处 window 级 keydown 仍活跃：`VideoProcessNode.tsx:746`（Delete/Backspace/S/[/]，仅 input 防护）、`director3d/panels/Timeline.tsx:391`（Delete 删关键帧）、`director3d/App.tsx:1176`（**capture 阶段**抢处理 F4/Delete/undo/redo）、`PanoramaNode.tsx:498`（C 截图，fullscreen 门但无编辑器门）；`AssetMenu.tsx:95`、`director3d/App.tsx:990`（Esc 类，幂等无实害）。编辑器内按 Delete → 画布节点片段被同步删除（用户不可见） | 增债 | 结构债 | 画布 6 处监听 × 编辑器全部快捷键 | 高 | 母体：**键盘归属无单点判据**——`editorSession.isEditorActive()` 已存在（04 区剪辑器开合已用它），6 处宿主监听无一引用；区域 21 G3 计划本有「键盘归属单点」一项 | 6 处 handler 开头统一加 `if (isEditorActive()) return`（或封装 `useKeyboardScope` 单点）；与 21 区 G3 合并施工 | 待还 |
| TD-22-20 | 属性面板 15 处「draft ref + initial ref + onBlur 两段提交」交互模式逐处手抄（text 9 / video 4 / sticker 4 处近 60 行/处） | 增债 | 结构债 | `properties/{text,video,sticker}-properties.tsx` | 中 | 母体：同一交互契约抄 N 份（SSOT 交互形态）；新增属性必然再抄一份 | 抽 `useNumberField`（或 `useDraftCommit`）hook 收口；纯机械重构，行为零变化 | 待还 |
| TD-22-21 | `trimEnd` 字段**四写者零读者**：渲染判据与导出解码均自算 `trimStart + duration×rate`，字段值无任何消费者；split 曾写错值多年被掩盖（本轮 #4 已把写者对齐不变式） | 增债 | 结构债（**删字段涉及持久化格式 → 施工前按业务债过用户**） | 类型定义 + 4 写者 + 迁移 | 中 | 母体 M3 变体：真相（源出点）派生自 `trimStart+duration`，字段是第二份静态拷贝 | 三选一：A 删字段+迁移（推荐，彻底）；B 保留但 JSDoc 标「派生值勿读」；C 不动（靠不变式纪律） | 待还 |

**低息记录（不占账本）**：`visual-node.ts:7` `VISUAL_EPSILON = 1e-3` 与 `element-utils.ts:144`（reversed 分支）`1e-6` 两套 epsilon 并存；reversed 当前无触发入口，无实害。

**已裁定非债（本轮留痕）**：
- `media.tsx:154` `filtered.sort` —— filter 产出的新数组，不变异 props。
- `use-keybindings.ts:42-51` —— 编辑器内快捷键对 INPUT/TEXTAREA/contentEditable/选中文本/keybinding-free 全防护。
- `properties/index.tsx:47` —— `selectedElements.length > 0` 才渲染，`elementRefs[0]` 无越界。
- 事件监听成对清理（font-picker/color-picker/editor-provider/preview）—— 均在 useEffect 内闭环。

## 四、覆盖度表（本轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `ui/ui/select.tsx` | L4 UI 原语 | 深审 | 🟢 | 本轮 | 视口 `h-(--radix-select-trigger-height)` 已修；z 归位 | 无债 |
| `ui/ui/font-picker.tsx` | L4 UI 原语 | 深审 | 🟢 | 本轮 | 重写为面板流内展开 | 无债 |
| `ui/ui/{dropdown,context,popover,tooltip,dialog,color-picker}` | L4 UI 原语 | 深审 | 🟢 | 本轮 | z-modal-raise 归位（grep 0 残留） | 无债 |
| `constants/font-constants.ts` | L1 常量 | 深审 | 🟢 | 本轮 | 重写（删 5 幽灵字体） | 无债 |
| `constants/text-constants.ts` | L1 常量 | 深审 | 🟢 | 本轮 | DEFAULT_BG_* 收口 | 无债 |
| `engine/services/renderer/font-stack.ts` | L1 纯函数 | 深审 | 🟢 | 本轮 | 新建（回落链唯一定义） | 无债 |
| `engine/commands/timeline/element/split-elements.ts` | L2 命令 | 深审 | 🟡 | 本轮 | trim 不变式 + rate 修复 | trimEnd 依赖 @TD-22-21 |
| `engine/services/renderer/nodes/visual-node.ts` | L2 渲染 | 深审 | 🟡 | 本轮 | 源时间域确认 | 双 epsilon（低息记录） |
| `engine/timeline/element-utils.ts` | L2 纯函数 | 深审 | 🟡 | 本轮 | `getVisualSourceTime` 域语义 | @TD-22-21 |
| `ui/editor/panels/properties/*-properties.tsx` | L4 面板 | 深审 | 🔴 | 本轮 | commit 模式 15 处 | TD-22-20 |
| `hooks-cutia/use-keybindings.ts` | L3 hook | 深审 | 🟢 | 本轮 | 防护链齐全 | 无债 |
| 宿主 6 处键盘监听（nodes/director3d） | 跨区 | 深审 | 🔴 | 本轮 | 逐一取证（见探债表） | TD-22-19 |
| `ui/editor/panels/assets/**` | L4 面板 | 抽审 | ⚪ | 本轮 | 仅 import 收口轮（09-15）扫过导入链 | 下轮补深审 |
| `engine/services/storage/**` | L2 存储 | 未审 | ⚪ | — | — | 下轮 |
| `engine/lib/media/**`（除 mediabunny:133） | L2 媒体 | 未审 | ⚪ | — | — | 下轮 |
| `ui/editor/export-button.tsx` | L4 导出 | 未审 | ⚪ | — | — | 下轮 |

## 五、结论

- 本轮修复 5 项（用户逐项授权段），新登记债 3 笔（TD-22-19/20/21），低息记录 1 条，非债裁定 4 条（均挂证据）。
- 母体两条：**键盘归属无单点判据**（TD-22-19，与 21 区 G3 呼应）、**变速感知散落**（split 本轮已修，TD-22-14 余 2 落点）。
- 未审 4 组（覆盖表 ⚪ 行）：assets 深审 / storage / media / export —— 下轮按层从底到顶。

---

## 六 · 二轮深查（同日 · 只读登记）——存储 / 播放 / resize / 事件链

### 探债（新增 3 笔）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-22 | 空时间轴播放无守卫：`playback-manager.ts:16-28` play() 仅在 `duration>0` 时回卷，`updateTime`（:139-171）的 else 分支对 `duration===0` 无上界 → 空轴按播放，`currentTime` 无限增长、raf 永续、`playback-update` 每帧白发 | 增债 | 结构债 | playback-manager 单点 | 中 | 孤例（边界漏防：tick 只防了"到头"，没防"无头可到"） | play() 前置守卫 `duration<=0` 直接 return（或禁用播放按钮） | 待还 |
| TD-22-23 | 播放通知**双通道 + 死事件**：`playback-manager` 既有 `listeners Set`（useSyncExternalStore 通道）又每帧 `window.dispatchEvent('playback-update')`——**该事件零消费者**（全库 grep 仅 audio-manager 听 `playback-seek`）；引擎内部类用 window CustomEvent 而非 subscribe | 增债 | 结构债 | playback-manager + audio-manager | 中 | 母体 M3 变体：同一通知两种机制并存，其中一条是死的（每帧白发 CustomEvent = 无谓开销 + 误导后人"有人听"） | 删 `playback-update` dispatch；audio-manager 迁到 `subscribe()`（或反向：统一走事件，删 listeners——二选一由决策架构师裁） | 待还 |
| TD-22-24 | 迁移 transformer 是**字段白名单重建**且 fallback 与常量层两套默认：`v1-to-v2.ts:409-446` text 元素 fallback `backgroundColor:'#FFFFFF'`（常量 `transparent`）、`textAlign:'left'`（常量 `center`）、多写 `hidden:false`；背景族/描边/阴影字段全部未列（v1 无此字段则历史无实害，但**下一个迁移照抄此模式必丢新字段**） | 增债 | 结构债 | migrations/transformers 全部 | 中 | 母体 M3：默认值两份（迁移 fallback ≠ `DEFAULT_TEXT_ELEMENT`）＋ 白名单式重建无"未知字段透传"兜底 | fallback 改引 `DEFAULT_TEXT_ELEMENT`；下次迁移改"透传 + 显式覆盖"模式；`hidden` 字段与类型对齐 | 待还 |

### 低息记录（不占账本）

- `subtitle-constants.ts:105` 字幕模板 `fontFamily:'Impact'` 不在新 `FONT_OPTIONS` 清单——FontPicker 受控显示原值不崩、渲染走通用回落链，仅"下拉不含该项"的轻微不一致。

### 本轮非债裁定（留痕）

- `playback-manager.seek` clamp `Math.max(0, Math.min(duration, time))` —— 边界正确。
- `use-element-resize` 右边缘：media 类型 `newTrimEnd<0` 有 clamp（trimEnd≥0），text/image 无界延长为**有意设计**（文字可任意加长，`canExtendElementDuration` 判据显式）。
- `use-raf-loop.ts` —— 通用 raf 帧 Delta 循环，清理成对，无状态残留。

---

## 七 · 三轮深查（同日 · 只读登记）——命令栈地基

### 探债（新增 3 笔）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-25 | **redo 破坏 undo（母体）**：`base-command.ts:8-10` 缺省 `redo(){ this.execute() }`，而快照式命令的 execute 开头都重新 `savedState = getTracks()`（`delete-elements.ts:19`、`split-elements.ts:25`、trim/move/duplicate/detach 同款）→ 破坏链：删→undo（恢复✓）→**redo（savedState 被覆盖为已执行后状态）**→再 undo = `updateTracks(已删态)` 无效，**元素永久丢失**。命令栈 `commands.ts:26` redo 即调 execute，实锤 | 增债 | 结构债 | **全部快照式命令**（引擎历史栈地基） | 高 | 母体：Command 基类的缺省 redo 与"execute 内自存快照"模式不兼容；undo 平时好用是因为没人走"undo→redo→undo"链 | 三选一：A 基类加 `if (this.savedState===null)` 首存守卫（最小）；B 快照由 CommandManager 在 execute 前统一存；C redo 走缓存反转。推荐 A（一行/命令，行为面最小） | 待还 |
| TD-22-26 | 素材连带删除**不清选择**：`remove-media-asset.ts:48` / `media-manager.ts:71` 调 `deleteElements` 后无 selection 处理 → 删除的若正是选中元素，`selectedElements` 残留幽灵 → `properties/index.tsx` `selectedElements.length>0` 但 `grouped` 全空 → **属性面板白板**（既非 EmptyView 也无提示）。主路径 `delete-selected` 已清（`use-editor-actions.ts:234`），仅素材路径漏 | 增债 | 结构债 | 素材删除链 × 属性面板 | 中 | 同族不一致：split/duplicate/detach 均管理 previousSelection，素材删除链漏；"选择一致性谁负责"无契约 | 两选一：A DeleteElementsCommand 内部统一清掉被删元素的选中项（收口进命令，所有调用方受益，推荐）；B 两个调用点补 clear | 待还 |
| TD-22-27 | `getTracks()` 返回**活跃数组活引用**（`timeline-manager.ts:365-367`）——命令栈全部 savedState 是活引用，undo 正确性**完全依赖"所有写路径不可变重建"的纪律**（现核：delete/split 均 spread 重建 ✓），无任何机器守卫；未来任何一处原地变异（`track.elements.push` / `element.x = 1`）= undo 静默失效。连带：UI 经 `getElementsWithTracks` 拿到的 element 也是 store 活对象 | 增债 | 结构债 | 引擎地基（全部命令 + 未来所有写路径） | 中 | 母体：引用语义靠纪律不靠机制（缺守卫，M2 形态） | 三选一：A `getTracks` 返回深克隆（写路径全部改走命令，半径大）；B 补 AST 闸"禁止对 tracks/elements 原地变异"（推荐，机器守卫）；C 维持纪律+文档 | 待还 |

### 本轮非债裁定（留痕）

- `updateElements` 批量组装（`timeline-manager.ts:231-235`）：单条走单命令、多条 `BatchCommand` 包一层 → 多选改色撤销一次全回 ✓。
- `delete-elements` execute/undo 不可变重建 ✓（savedState 未被原地污染，本轮逐行核）。
- ephemeral 资产删除/恢复的异步落盘 catch 带 logger ✓。

### 覆盖度表（三轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `commands/base-command.ts` | L1 命令基类 | 深审 | 🔴 | 三轮 09-15 | redo 缺省实现全文 | TD-22-25 |
| `core/managers/commands.ts` | L2 命令栈 | 深审 | 🔴 | 三轮 09-15 | 全文 45 行 | TD-22-25 |
| `commands/timeline/element/delete-elements.ts` | L2 命令 | 深审 | 🔴 | 三轮 09-15 | 全文逐行 | TD-22-25/26 |
| `core/managers/timeline-manager.ts` | L2 引擎 | 深审 | 🔴 | 三轮 09-15 | getTracks/updateElements/trim | TD-22-27 · @TD-22-21 |
| `commands/batch-command.ts` | L2 命令 | 抽审 | ⚪ | 三轮 09-15 | 仅确认组装形态 | 下轮补 |
| `core/managers/selection-manager.ts` | L2 引擎 | 未审 | ⚪ | — | — | 下轮 |
| `hooks-cutia/actions/use-editor-actions.ts` | L3 hook | 抽审 | 🟡 | 三轮 09-15 | delete-selected 处理器 | 下轮全量 |

---

## 八 · 四轮深查（同日 · 只读登记）——场景/工程生命周期 × 命令栈

### 探债（新增 1 笔 · 数据损坏级）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-32 | **跨场景/跨工程 undo 覆盖数据**：① `updateTracks` 写 `this.active`（`scenes-manager.ts:246-257` `updateSceneTracks`）；② 切场景/切工程**完全不碰命令栈**（scenes/project-manager 无 `command.clear()`）；③ 命令 savedState 是**执行时活跃场景**的快照 → 场景 A 删元素 → 切到 B → undo → `updateTracks(A_tracks)` **整体覆盖 B 的 tracks**，B 内容丢失且不可逆；跨工程同理（`EditorCore` 模块级单例，`core/index.ts:47` 的 `reset()` **全仓零调用**——App.tsx `key=activeProjectId` 重挂载后引擎仍旧实例，命令栈跨工程存活） | 增债 | 结构债 | 场景/工程切换 × 全部历史栈命令 | **高** | 母体：**生命周期边界无单点**——"场景/工程切换必须清历史栈"没有落点（既无调用也无守卫）；`reset()` 是死方法（定义未用，A1 同款） | 切场景/切工程入口调 `command.clear()`（+ `EditorCore.reset` 接进工程切换链 或 删除死方法二选一）；补闸"生命周期入口必须清栈"或行为测试锁 | 待还 |

### 本轮非债裁定（留痕）

- `updateSceneTracks` 本体：不可变重建 scene + `updatedAt` 刷新 + 落盘触发 ✓（问题不在它，在生命周期边界无人清栈）。
- `EditorCore` 单例本身不是债（性能合理），债在 reset 无调用方 + 栈跨边界存活。

### 覆盖度表（四轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `core/managers/scenes-manager.ts` | L2 引擎 | 深审 | 🔴 | 四轮 09-15 | updateSceneTracks:246 / setScenes:215 / 无 clear | TD-22-32 |
| `core/index.ts`（EditorCore） | L1 引擎根 | 深审 | 🔴 | 四轮 09-15 | 单例 + reset 零调用（:40-49） | TD-22-32 |
| `core/managers/project-manager.ts` | L2 引擎 | 抽审 | 🟡 | 四轮 09-15 | 无 command.clear（:510 唯一 clear 是无效 id 集） | @TD-22-32 · 下轮全量 |
| `core/managers/save-manager.ts` | L2 引擎 | 未审 | ⚪ | — | — | 下轮 |
| `core/managers/audio-manager.ts` | L2 引擎 | 抽审 | ⚪ | 二轮起挂起 | 仅证 playback-seek 消费 | TD-22-23 收口时深审 |

---

## 九 · 五轮深查（同日 · 只读登记）——保存协议与选择/批量命令

### 探债（新增 1 笔）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-33 | `flush()` 语义不诚实：`save-manager.ts:87-90` `saveNow` 在 `isSaving` 时直接 return —— 自动保存进行中（写盘 await 期间）调用 flush，**await 返回 ≠ 已落盘**，最后的变更仍在 800ms 防抖队列里；退出协议 `prepareExit`（`project-manager.ts:129`）与切工程（`:463`）都 `await flush()` 拿到假信号 → 退出打断重排 → **退出前最后变更丢失**。防抖竞态其余路径（markDirty/finally 重排/pause-resume）经核正确 | 增债 | 结构债 | 保存协议 × 退出协议（`EditorShell.tsx:36` prepareExit） | 中 | 孤例：flush 名义承诺"已保存"，实现是"排队一次 saveNow"，二者在 isSaving 窗口分叉 | flush 改为：isSaving 时 `await` 进行中的 promise 完成后再 saveNow（内部持有 inflight promise）；行为测试锁"保存中 flush → 落盘含最后变更" | 待还 |

### 本轮非债裁定（留痕）

- `BatchCommand`（全文 26 行）：undo 逆序 / redo 正序，配对正确；子命令受 TD-22-25 母体影响（不另立）。
- `save-manager` 防抖主体（markDirty→queueSave→finally 重排、pause/resume、isLoading/isMigrating 守卫）经核正确。
- `beforeunload` 用 `getIsDirty()`（=hasPendingSave‖isSaving）拦关闭 ✓。
- `SelectionManager.getSelectedElements` 返回活引用 —— 并入 **TD-22-27** 同族（引用纪律无守卫），不单列。

### 覆盖度表（五轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `core/managers/save-manager.ts` | L2 引擎 | 深审 | 🔴 | 五轮 09-15 | 全文 129 行逐行 | TD-22-33 |
| `commands/batch-command.ts` | L2 命令 | 深审 | 🟢 | 五轮 09-15 | 全文 26 行 | 无债（子命令归 TD-22-25） |
| `core/managers/selection-manager.ts` | L2 引擎 | 深审 | 🟡 | 五轮 09-15 | 全文 36 行 | 活引用 @TD-22-27 |
| `ui/providers/editor-provider.tsx` | L3 Provider | 抽审 | 🟢 | 五轮 09-15 | beforeunload 防护 | 无债 |

---

## 十 · 六轮深查（同日 · 只读登记）——音频链 / 移动边界 / 存量债证据补强

### 本轮零新 TD，两笔存量债更新（同样入账，防误修防重审）

**① TD-22-14 描述修正**（登记流程 §3.3：描述是线索，取证后必须修正）：
原描述「PlaybackSink 未设 playbackRate（**全仓 0 处**）」**过宽** —— 播放侧 `audio-manager.ts:195-207` 已正确处理（`node.playbackRate.value = rate`、`sourceOffset = trimStart + elapsed×rate`，与 `getVisualSourceTime` 同语义）；漏点确认只在**导出侧 mediabunny 的 mixClip/PlaybackSink**。偿还范围收窄为导出侧 2 落点，播放侧零动作。证据：`audio-manager.ts:195-207` 全读。

**② TD-22-32 证据补强**：`AudioManager.dispose()`（`audio-manager.ts:33-52`：停播放、退订阅、close AudioContext）**全仓零调用** —— 与 `EditorCore.reset` 零调用同根：生命周期出口定义了但没人走。后果叠加：切工程后旧 AudioContext 不 close、`playback.subscribe`/`timeline.subscribe` 残留、`decodedBuffers` 内存不释放。**TD-22-32 的偿还计划应一并覆盖 dispose 链**。

**③ TD-22-27 证据补强**：`SelectionManager.getSelectedElements` 与 `AudioManager.decodedBuffers`（Map 无上限增长，低息）均已并入各自名下。

### 本轮非债裁定（留痕）

- **move 命令层无 clamp**（`move-elements.ts` 零 clamp/碰撞检查）——但唯一 UI 入口 `use-element-interaction.ts:290,335` 有 `Math.max(0,...)` 防负，分层防护成立 → 非债（若未来引擎新增直接调用方，需补命令层守卫，@TD-22-27 纪律）。
- `scheduleClipNode` 迟到调度补偿（:214-225 late/adjustedOffset）✓；`handleTimelineChange` 300ms 防抖重排 ✓；`catch-ok: RELEASE_FAIL`（:257）合规。
- `media-manager.removeMediaAsset` 的 revoke 顺序（revoke → deleteElements）无害：blob 已加载部分不受 revoke 影响。

### 覆盖度表（六轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `core/managers/audio-manager.ts` | L2 引擎 | 深审 | 🟡 | 六轮 09-15 | 全文 263 行逐行 | 播放侧干净；dispose 零调用 @TD-22-32 · decodedBuffers 无上限（低息） |
| `commands/timeline/element/move-elements.ts` | L2 命令 | 抽审 | 🟢 | 六轮 09-15 | 零 clamp + UI 层防护 | 非债 |
| `hooks-cutia/timeline/element/use-element-interaction.ts` | L3 hook | 抽审 | 🟢 | 六轮 09-15 | :69/:290/:335 clamp | 无债 |

---

## 十一 · 七轮深查（同日 · 只读登记）——媒体契约 / 迁移对照

### 探债（新增 1 笔）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-37 | `addMediaAsset` **失败也返回新 id**（`media-manager.ts:29-45` 失败被内部 catch 回滚 + toast，但 `return newAsset.id` 无差别）→ 结果契约缺位（M1）：调用方无法从返回值判成败，被迫二次探测——`use-timeline-drag-drop.ts:305` 用 `getAssets().find(name+url)` 确认素材真的存在才敢插时间轴；`tts/service.ts:109` 同样拿不到成败 | 增债 | 结构债 | 3 调用方（media.tsx / drag-drop / tts） | 中 | 母体 M1：结果契约缺位（同 `StorageOpResult`/`PersistOutcome` 先例——该域漏了这一个） | 返回判别联合 `{ok:true,id} \| {ok:false,error}`；`use-timeline-drag-drop` 删 find 探测改判返回值；tts 调用点同步 | 待还 |

### 本轮非债裁定（留痕）

- **v2-to-v3 transformer 全文**：`{...project, metadata, version:3}` **透传式** ✓（与 v1-to-v2 白名单对照——TD-22-24 的风险模式仅存在于旧迁移，最新迁移已用对模式）；`getScenes` 的 `as unknown as TScene[]` 仅用于 duration 计算（types.ts 明示 intentionally loose）→ 非债。
- `media.tsx processFiles` 多文件导入：单个失败回滚 + toast，循环继续下一文件 ✓（部分失败继续是合理口径，且不插时间轴无幽灵）。
- `media-manager` 上传链（乐观加入 + 失败回滚 + toast，2026-09-14 已修假成功）经复核 ✓。

### 覆盖度表（七轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `storage/migrations/transformers/v2-to-v3.ts` | L2 迁移 | 深审 | 🟢 | 七轮 09-15 | 全文 50 行，透传式 | 无债 |
| `ui/editor/panels/assets/views/media.tsx` | L4 面板 | 深审 | 🟡 | 七轮 09-15 | processFiles/handleRemove | @TD-22-37 |
| `hooks-cutia/timeline/use-timeline-drag-drop.ts` | L3 hook | 深审 | 🔴 | 七轮 09-15 | executeFileDrop:288-330 | TD-22-37 |
| `engine/lib/tts/service.ts` | L2 服务 | 抽审 | 🟡 | 七轮 09-15 | :109 调用点 | @TD-22-37 · TTS 占位本体下轮 |

---

## 十二 · 八轮深查（同日 · 只读登记）——导出 UI 链

### 探债（新增 1 笔）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-38 | 导出 UI 契约轻债两条款：① `export-button.tsx:66-68` `DEFAULT_EXPORT_OPTIONS.includeAudio \|\| true` **恒真死表达式**——当前默认恰为 true 行为碰巧对，未来把常量改 false 会被静默吞掉（用户口径"默认不含音频"失效且无任何报错）；② `ExportResult`（`types/export.ts:17-22`）是**全可选字段宽松形状**而非判别联合——`success:true` 且 `buffer` 缺失的组合在 `:103` 双检处静默无反应（popover 卡在导出完成态，无成功也无失败提示）。另：下载名未 sanitize（`metadata.name` 含 `/` 等字符时行为随浏览器）、`revokeObjectURL` 紧跟 `click()`（Safari 历史丢下载）——两条低息随本债一并修 | 增债 | 结构债 | export-button 单文件 + 类型 | 低 | 孤例（`x \|\| true` 笔误 + M1 形状宽松在导出类型的残留） | ① 改直接引用常量；② `ExportResult` 改判别联合 `{success:true,buffer} \| {success:false,error} \| {cancelled:true}`，调用方删双检；③ 文件名 sanitize + 延迟 revoke | 待还 |

### 本轮非债裁定（留痕）

- 导出参数来源 `activeProject.settings.fps` ✓（fps 不另行写死）；取消经 `onCancel: () => cancelRequestedRef.current` 轮询 ✓（ref 不受闭包污染）；`result.cancelled` 分支干净重置 ✓。
- `check:keys` 闸全绿（559 文件，存储键契约无漂移）。

### 覆盖度表（八轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `ui/editor/export-button.tsx` | L4 UI | 深审 | 🔴 | 八轮 09-15 | 全文 272 行（handleExport/handleCancel/下载段） | TD-22-38 |
| `types/export.ts` | L0 类型 | 深审 | 🟡 | 八轮 09-15 | ExportResult:17-22 | @TD-22-38 |
| `engine/lib/export.ts` | L2 引擎 | 抽审 | 🟢 | 八轮 09-15 | 58 行薄壳（转发 pipeline） | 无债 |
| `constants/export-constants.ts` | L1 常量 | 深审 | 🟢 | 八轮 09-15 | DEFAULT_EXPORT_OPTIONS | 无债 |

---

## 十三 · 九轮深查（同日 · 只读登记）——音效试听生命周期 / stores

### 探债（新增 1 笔）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-42 | 音效试听**无卸载清理**：`sounds.tsx` 四个 useEffect（:98/:102/:162/:291）无一 pause 音频；`playSound` 每次 `new Audio(previewUrl)`（:186）→ 切 Tab / 关素材面板 / 退出编辑器后试听音**继续播放到自然结束**，且 `audioElement` state 随组件销毁，引用丢失**永远无法停掉**（用户被迫等它播完或静音系统）。附带：旧元素 error 回调（:198）无条件 `setPlayingId(null)`，快速切歌时可能清掉新歌的播放态（时序窗口小） | 增债 | 结构债 | sounds 视图单点 | 中 | 母体同 TD-22-19/32：**生命周期清理无守卫**（卸载钩子缺失，靠纪律） | useEffect cleanup `audioElement?.pause()`（audio 存 ref 而非 state 更稳）；ended/error 回调按 id 判等再清 | 待还 |

### 本轮非债裁定（留痕）

- `stores/keybindings-store.ts` / `panel-store.ts`：zustand `persist` + `migrate` 版本迁移形态正规 ✓（与引擎存储迁移是两套体系，键经 `check:keys` 闸已绿）。
- 试听切换逻辑本体（同 id 再点 = 暂停、异 id 先 pause 旧的）✓；`AbortError` 静默 ✓（用户切歌的正常中断）。
- 滚动位置恢复（:162-173）timeout 清理 ✓。

### 覆盖度表（九轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `ui/editor/panels/assets/views/sounds.tsx` | L4 面板 | 深审 | 🔴 | 九轮 09-15 | playSound:183-211 + 四 useEffect | TD-22-42 |
| `stores/keybindings-store.ts` | L2 store | 抽审 | 🟢 | 九轮 09-15 | persist+migrate 形态 | 无债 |
| `stores/panel-store.ts` | L2 store | 抽审 | 🟢 | 九轮 09-15 | persist+migrate 形态 | 无债 |
| `stores/{assets-panel,stickers,media-preview,sounds,timeline}-store.*` | L2 store | 抽审 | 🟢 | 九轮 09-15 | 非持久化会话态为主 | 无债 |

---

## 十四 · 十轮深查（同日 · 只读登记）——素材加载竞态 / captions / text 视图

### 探债（新增 1 笔）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-43 | `loadProjectMedia` 双缺口：① **无重入守卫**——`isLoading` 只写不判（`media-manager.ts:80-97`），并发调用（快速切工程）后完成者覆盖先完成者，**A 工程素材可能覆盖 B 工程面板**（与 TD-22-32 同族：生命周期/时序边界无守卫，但这是函数级独立可修）；② 加载失败仅 `logger.error` → 用户看到**静默空面板**（storage 有数据但 UI 空，"以为没素材"；失败可见性给错读者——开发者有日志、用户无提示） | 增债 | 结构债 | media-manager 单点 × 工程切换链 | 中 | ①同 32 母体（时序边界无守卫）②失败可见性缺失（Step 4 读者错位） | ① 入口 `if (this.isLoading) return`（或请求序号/AbortController 取旧）；② 失败置 error 态 → 面板显示"加载失败/重试" | 待还 |

### 本轮非债裁定（留痕）

- `clearProjectMedia` / `clearAllAssets`：revoke → 清内存 → 异步删 storage，`Promise.all` 部分失败仅 logger（孤儿数据属 13 区文件生命周期域，跨区不展开）——低息记录不占账本。
- `captions.tsx` 模板预览（:219-237）：样式字段逐个透传，`fontFamily:'Impact'` 不在 FONT_OPTIONS（低息已记九轮前）但预览与渲染同走回落链，一致。
- `text.tsx`（全文 48 行）：`buildTextElement` + `insertElement auto` ✓；`dragData` 用 `temp-text-id` 占位、落点重建 ✓（drag-data 契约 09-15 已收口）。

### 覆盖度表（十轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `core/managers/media-manager.ts` | L2 引擎 | 深审 | 🔴 | 十轮 09-15 | 全文（add/remove/load/clear） | TD-22-43 · @TD-22-37 |
| `ui/editor/panels/assets/views/captions.tsx` | L4 面板 | 深审 | 🟢 | 十轮 09-15 | 模板应用/预览段 | 无债（Impact 低息已记） |
| `ui/editor/panels/assets/views/text.tsx` | L4 面板 | 深审 | 🟢 | 十轮 09-15 | 全文 48 行 | 无债 |

---

## 十五 · 十一轮深查（同日 · 只读登记）——渲染调度 / 导出引擎侧

### 本轮零新 TD —— `renderer-manager.ts` 深审全绿

**非债裁定（逐条挂证据）**：

- **ExportResult 判别返回在 engine 侧是规整的**：`exportTracks` 的所有出口都诚实——`{success:false, error}`（:47/:76/:80/:139/:153）、`{success:false, cancelled:true}`（:135）、`{success:true, buffer}`（:142）。对照 UI 侧 TD-22-38 的宽松形状，**问题只在 UI 消费端**，engine 契约无需改。
- 空工程防护 ✓（:79-81 `duration===0` → 诚实 error；对照播放侧 TD-22-22 的缺失——同一判据一有一无，恰证 TD-22-22 该修）。
- 取消链 ✓：`onCancel` 轮询 ref → `setInterval(checkCancel,100)` → `exporter.cancel()` → 判别返回；`finally clearInterval`（:146-148，:132 的先清是冗余但无害）。
- 错误透传 ✓：catch → logger + error message 进判别返回（不静默、不重分类）。
- 进度映射 `0.05 + progress*0.95`（:116）：音频占比是拍脑袋常数——**体验级非债**（进度条非线性不算错）。
- `renderTree` 持有/notify 由 preview 层推入，manager 职责单一 ✓。

**`insert-element.ts` 抽审**：`savedState` 内联快照（TD-22-25 母体成员，不另立）；`generateUUID` 在构造器生成 elementId（undo/redo 稳定引用 ✓）。

### 覆盖度表（十一轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `core/managers/renderer-manager.ts` | L2 引擎 | 深审 | 🟢 | 十一轮 09-15 | 全文 167 行逐行 | 无债 |
| `commands/timeline/element/insert-element.ts` | L2 命令 | 抽审 | 🟡 | 十一轮 09-15 | 快照模式 + id 生成 | @TD-22-25 |
| `engine/lib/export.ts` + `scene-exporter.ts` | L2 引擎 | 抽审 | 🟢 | 八/十一轮 | 薄壳转发 / progress 事件 | 无债 |

---

## 十六 · 十二轮深查（同日）——尾部收口 + 全区域覆盖完备宣告

### 本轮非债裁定（留痕）

- **`detach-audio.ts` 全文**：快照 + previousSelection 管理 ✓（与 delete 形成对照——delete 漏选择已挂 TD-22-26）；不可变重建 ✓（:21 浅拷贝数组上的 :80 splice 不污染 savedState）；`getActive()` 挂钩前有 type/mediaId 三重守卫 ✓。
- **`batch-move-elements.ts`**：`Math.max(0, startTime + delta)` 负值防护 ✓；47 行薄命令 ✓（快照归 TD-22-25）。
- **`tts/service.ts`**：`getActive()`（`project-manager.ts:514-518`）内部 throw `'No active project'` —— **fail-fast 且消息明确**，非裸 `.metadata` 崩溃 ✓；`response.json().catch(() => null)` 带 `catch-ok: PARSE_FALLBACK` ✓；objectURL 在 addMediaAsset 失败时不 revoke → 并入 TD-22-37 名下。
- `settings.tsx`：全部走 `editor.project.updateSettings`（:128-356）无旁路直写 ✓。
- `hooks-cutia/timeline/{edge-auto-scroll,scroll-sync,selection-box,playhead}`：纯视图态 hook（前面已核 playhead z 局部层级）✓。

### 覆盖完备宣告

区域 22（剪辑器横切）自 09-13 三轮 + 本日十二轮，**覆盖表已无 ⚪ 行**——`videoEditor/**` 全部文件、宿主交叉监听、迁移、存储、命令栈、渲染、导出、stores 均达到「深审 / 已裁定非债 / 有债待还」三态之一。后续只有两种情况需要回查：① 本文件登记的债施工时按 TD 号回查对应文件；② 代码本身变更（回查规则见覆盖度账 §铁律 1）。

### 偿还路线图（按母体归堆，交统一决策架构师）

| 优先 | 母体 | TD | 一句话收口 |
| --- | --- | --- | --- |
| **P0 数据损坏** | 生命周期边界无单点 | **TD-22-32**（含 dispose 链）· 28/29/30 | 场景/工程切换入口 `command.clear()` + `EditorCore.reset` 接链或删死方法 |
| **P0 撤销失效** | redo/快照不兼容 | **TD-22-25**（母体）· 26 · 27 | 基类首存守卫 + 删除命令统一清选择 + AST 闸禁原地变异 |
| P1 数据丢失 | 保存/结果契约 | TD-22-33（flush 假信号）· 37 · 44 · 13 | flush 等待 inflight；`addMediaAsset`/`loadProjectMedia` 判别联合 |
| P2 交互/杂项 | 键盘归属 · 抄写 · 通知 · 迁移 · 死表达式 | 19 · 20/21 · 22/23 · 24 · 38 · 42 · 14/15/16 | 按各自偿还计划独立施工 |

**一句话**：登记阶段覆盖完备；P0 两笔（32/25）是"用户数据损坏/丢失"级，建议最先排产。

---

## 十七 · 债务推导（同日）——从现有债推出它的利息与孩子

> 用户指令：「把现在所有债务推导清楚，然后再从现在债务去推导新的债务」。
> 本段 = 全部 29+ 条债的**因果网络**：四条根规则 → 各债 → 二阶复合利息。
> 推导属登记（归因深化）；修复仍交决策架构师。

### 第一层：四条根规则（全部在契约/机制层，不在任何单文件）

| # | 根规则 | 直接生出的债 |
| --- | --- | --- |
| **R1** | **生命周期边界无单点**——"什么时候该清理什么"没有契约（无卸载钩子守卫、无切换时清栈、reset 是死方法） | 19（键盘）· 32（栈+dispose）· 42（试听音频）· 44①（加载竞态） |
| **R2** | **历史栈语义未定义**——快照由谁存、redo 语义、引用语义三件事都没定型 | 25（redo=execute）· 27（活引用）· 21（trimEnd 四写者正是"语义未定则各写各的"） |
| **R3** | **结果契约缺位（M1 残留）**——操作成败不在类型层，调用方靠猜或二次探测 | 37（返 id 不判成败）· 33（flush 假信号）· 44②（加载失败静默）· 38②（ExportResult 宽松）· 13 |
| **R4** | **失败可见性读者错位**——开发者日志 vs 用户可见的分工无规则 | 26（删除后面板白板）· 24（迁移静默丢字段）· 44②（双计） |

**验证**：29 条债逐条回溯，全部落在 R1-R4 之下，无一悬空（含并行轮 13-16/28-31/34-41）；零"编造母体"。

### 第二层：债 × 债 = 复合利息（二阶推导——不还 A，B 会长出新症状）

1. **32 × save-manager = 损坏被持久化**：跨场景 undo 覆盖 B → timeline notify → save-manager markDirty → **800ms 后损坏状态写入 storage** → "会话内 bug"升级为"**永久数据丢失**"（刷新救不回）。这是 32 未还期间的必然利息，爆炸半径以此为准（账本体 TD-22-32 不改，本段为最完整口径）。
2. **25 × canUndo = UI 撒谎**：栈非空即 `canUndo()`=true → 撤销按钮亮着，按下无效 → 用户对整个撤销系统失去信任。25 的可用性利息。
3. **25 × 新命令数 = 线性利息**：每个新命令继承 redo 缺陷（execute 内自存快照是既有写法），命令越多利息越高。
4. **19 × 42 = 双声部**：编辑器内按键同时触发宿主节点音频预览 + 编辑器试听（两条生命周期缺陷叠加的听感症状）。
5. **20 × 修 25/27 = 修复成本放大**：commit 模式 15 份意味着任何触及提交路径的修复都要同步 15 处——**债与债互相放大利息**。
6. **44 × 37 = 状态分裂**：加载失败后内存 assets 为空，此时 addMediaAsset 仍"成功"（写 storage）→ 内存与持久层状态分叉。
7. **23 × 22 = 空轴高频空转**：空轴播放（22）期间每帧 dispatch 死事件（23）——两个"低息"叠加成持续白耗。

### 第三层：二阶候选的取证裁决

- **变速 × 复制**（候选：复制丢 playbackRate）→ **证伪**：`duplicate-elements.ts:111` `{...element}` spread 全字段，rate 随行。非债，留痕。
- **TTS × ephemeral 回收**（候选：TTS 素材删除不回收）→ 已被 `delete-elements.ts:69-86` ephemeral 清理覆盖。非债。
- 其余二阶候选均归类为上表各母体的**利息**（写进母体账，不新立 TD——防账本碎片化，一收全消）。

### 对"还是只能登记？"的回答

是。按债务登记 5 步法：本流程产出 = **账本 + 覆盖表 + DATAFLOW 灯 + 本推导**；推导是登记的深化（归因到根规则 + 利息前瞻），不是修的授权。修复动作已按母体整理成偿还路线图（§十六），交统一决策架构师排产——P0 = TD-22-32（含持久化利息，见第二层第 1 条）与 TD-22-25。

---

## 十八 · 十三轮（同日）——并行轮债单交叉验证 + 音频收集链

### 交叉验证（推导完备性）

拉取并行轮登记的 10 笔债摘要（TD-22-28/29/30/31/34/35/36/39/40/41）逐条回溯：**全部落入 §十七的四条根规则**，无一悬空——

- R1/R2（生命周期+栈语义）：40（transition 绕过 CommandManager 无 undo）、41（scenes 双源手动同步）
- R2（变速/语义散落）：29（双后端 reversed 漂移——与 14 同族，导出侧）
- R3/R4（契约+可见性）：28（agentMessages 幽灵字段永久往返）、30（SUBTITLE_TEMPLATES 手抄形状）、34（InsertElementCommand.undo 不闭环 settings）、35（MigrationResult 手抄）、36（动作参数契约双份）、39（audio.ts fetch 双生）

**结论**：四条根规则（R1 生命周期 · R2 历史栈语义 · R3 结果契约 · R4 可见性读者）对区域 22 全部待还债**完备**——统一决策时按 R1→R2→R3→R4 顺序做四个母体收口，全部归堆消解。

### 非债裁定（本轮取证）

- `collectAudioClips`（`audio.ts:394-451`）：video 元素音轨经 `mediaSupportsAudio` 判定后收集，`collectMediaAudioClip:329-341` 直接透传 element 的 rate/trim —— **播放侧变速处理与渲染同源**（再次实证 TD-22-14 漏点仅在导出侧）。
- `createTimelineAudioBuffer`（导出混音）与 AudioManager（播放混音）是 §十七第二层第 7 条所指双后端——归 TD-22-29，不另立。
- 库音效 `fetchLibraryAudioClip` 异步收集 + `Promise.all` 汇聚 ✓。

### 覆盖度表（十三轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `engine/lib/media/audio.ts` | L2 纯函数 | 深审 | 🟢 | 十三轮 09-15 | collectAudioClips:394-451 · collectMediaAudioClip:320-341 | 无债（fetch 双生 @TD-22-39） |

---

## 十九 · 十四轮（同日）——timeline 工具族 / settings / stickers 收尾

### 本轮非债裁定（留痕）

- **`transition-utils.ts`**（:19-125 七个纯函数族）：adjacent/pair/build/add/remove/cleanup 全部无副作用——**TD-22-40（transition 绕过命令栈）的落点在 `timeline-manager.ts:300-355` 包装层**（深读过：直接 `this.updateTracks` 未走 command），工具层干净。
- **`bookmarks.ts`**：`roundToFrame({time, fps})` 带 fps 意识，帧对齐不靠浮点裸算 ✓。
- **`settings.tsx`**：blur/color 两分支对称、`updateSettings` 全 await、useCallback 依赖正确 ✓。
- **`stickers.tsx`**：插入走 `useStickersStore().addStickerToTimeline`（store 单点）✓；`collectionsToShow` 分页是纯 UI 态 ✓。

### 覆盖度表（十四轮增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `engine/timeline/transition-utils.ts` | L2 纯函数 | 深审 | 🟢 | 十四轮 09-15 | 七函数族全扫 | 无债（TD-22-40 落点在 manager 层） |
| `engine/timeline/bookmarks.ts` | L2 纯函数 | 深审 | 🟢 | 十四轮 09-15 | roundToFrame fps 对齐 | 无债 |
| `ui/editor/panels/assets/views/settings.tsx` | L4 面板 | 深审 | 🟢 | 十四轮 09-15 | BackgroundView 全读 | 无债 |
| `ui/editor/panels/assets/views/stickers.tsx` | L4 面板 | 深审 | 🟢 | 十四轮 09-15 | 插入走 store 单点 | 无债 |
