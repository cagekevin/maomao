# 架构日志 · 生成链路（跨区） · 2026-09-26

**任务**：登记一债 —— 文本节点（同步能力）经**异步编排原语**生成，落到了任务中心。

## 一、链路快照（refs 实证）

```
runGenerationOrchestration 消费方（refs 模块引用 3）：
  ├ scriptbox/scriptBoxEngine.ts        （资产图 :556 / 尾帧图 :1634 —— 异步，正确）
  ├ hooks/useNodeGeneration.ts:269      （全部生成节点，含文本）
  └ tests/unit/generationContract.test.ts

useGenerateNode 消费方（refs 模块引用 5）：TemplateNode · ImageGenerate · TextGenerate · VideoGenerate（+测试）

resultKey 声明面（grep）：
  ImageGenerate:'assetUrl' · VideoGenerate:'videoUrl' · TemplateNode:'assetUrl' · TextGenerate:【无】（:184 自述「不接 resultKey」）
```

## 五、探债

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-01-32 | **文本节点（同步能力）走异步编排原语**：`runGenerationOrchestration` 给文本做的四件事全无意义或副作用 —— ① `reportGenerate` 建「文本」任务行（`taskStore.ts:229` TASK_TYPE_META.text）并 `openTaskCenter` 弹窗（`:344`）；② `progress(5/10/30)`（`:127`）；③ `saveResultToTasks('')` 恒 `skipped`（`filesApi.ts:726`）；④ `done('')` 不广播（`generationOrchestration.ts:32-33` R7 / `:189`）。违反「**文本全同步**」不变量；该原语 R7 自述「非异步任务不要走本原语」 | 增债 | 结构债 | 文本节点 **1 个**（唯一不声明 resultKey 的生成节点，`TextGenerate.tsx:184`）；用户每次文本生成被弹任务中心；任务中心出现无结果「文本」卡 | 中 | **母体 M**：① 同步节点**没有自己的编排原语** —— `runGenerationOrchestration` 是唯一原语且只服务异步（假定有 resultUrl），借用即带出「建行／落盘／done」三个无意义步骤；② 「文本不进任务中心」**无机器闸**（仅文档化，无守卫） | 按 `resultKey` 分派：**有 → 异步原语；无 → 同步轻路径**（去掉 report／persist／done，**保留** claim／validate／loading／abort／`registerTaskRetry`(`useNodeGeneration.ts:302`)，否则 Agent `generate_node`／`canvasPlanExecutor`／`upstreamLink` 驱动不了文本节点） | 待还 |

**对照（同为正文本消费者，均不走该原语）**：剧本盒 5 处 `chatCompletions`（`scriptBoxEngine.ts:377/760/920/1030/1456`）· AI 助手 `chatStream`（`agentRuntime.ts:227`）· 会话摘要 `chatCompletions`（`contextCompression.ts:149`）。

---

> §二 溯源 / §三 定海 / §四 切割 / §八 交付闸 / §九 决策留痕：本轮为**纯登记轮**（无代码改动），留空；母体判断已并入上表「根因」列。

## 六、覆盖度表

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| - | - | - | - | - | - | - |
| `hooks/useNodeGeneration.ts` | 生成契约 | 有债待还(TD-01-32) | 🔴 | 本轮 · 2026-09-26 | refs + `:143-144`（文本不声明 resultKey）· `:269` | TD-01-32 |
| `generate/lib/generationOrchestration.ts` | 生成编排 | 有债待还(TD-01-32) | 🔴 | 本轮 · 2026-09-26 | R7 `:32-33`；`:126/:172/:189` | TD-01-32 |
| `text/TextGenerate.tsx` | 节点 | 有债待还(TD-01-32) | 🔴 | 本轮 · 2026-09-26 | `:184`（自述不接 resultKey）· `:206` | TD-01-32 |
| `base/store/taskStore.ts` | store | 深审 | 🟢 | 本轮 · 2026-09-26 | `:229`（TASK_TYPE_META）· `:337/:344`（reportGenerate→openTaskCenter） | 无（承载方，非债） |
| `scriptbox/scriptBoxEngine.ts` | 剧本盒 | 深审 | 🟢 | 本轮 · 2026-09-26 | `:556/:1634` 异步用原语**正确** | 无 |

**覆盖账**：本轮为**单债登记轮**（非全域审计轮），只覆盖该债链路上表 5 个文件；未审：区域 01 其余文件（不属本债链路 / 下轮）。

## 七、结论与下一步

- 本区域状态：`已验证(有债待还)` —— 区域 01 既有 **TD-01-30**（persist 无重试）· **TD-01-31**（initTasks 弱于 resourceStore）**待还**，本轮**新增 TD-01-32**。
- 待还债项已流入 `daily/架构日志/债务.md`（轻量索引）：是（TD-01-32）。
- DATAFLOW.md：**已对账一致**（本债是实现偏离，链路拓扑未变 —— DATAFLOW §一 如实记录了「文本节点走主范式」的现状）。
- 灯：本区**覆盖度表**已更新（🔴）；区域级灯由 `index.md` 自动派生。
