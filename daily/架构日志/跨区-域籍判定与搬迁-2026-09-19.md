# 跨区 · 域籍判定与搬迁 · 2026-09-19

> **一句话结论**：派 7 本域主任务书覆盖 **311 件**（311 件全量台账），实测「非本域」仅有 **9 件**（canvas 2 + base 7）⇒ **域籍基本是对的，问题集中在"域内子目录落点"**；据此搬迁 **21 件**（域内 6 · 跨域 15 · 清理 3）+ 同步 **5 类闸/白名单/桶/测试**；判据落 `ADR-0040`（L1-bis/L3-bis/L4-bis/L4-ter）与 `ADR-0044`。
> **本文件是过程与证据**；**判据不在本文件**（判据真源 = `docs/adr/`）。

## 一 · 派单与覆盖（域主台账）

| 任务书 | 域主 | 领地 | 件数 | 合规 | 域内错位 | 非本域 | 待核 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TASK-022 | 总纲（判据 · 四件证据 · 成因代号） | — | — | — | — | — | — |
| TASK-023 | image | `image/**` | 27 | 24 | 3 | 0 | 0 |
| TASK-024 | video | `video/**` | 13 | 13 | 0 | 0 | 0 |
| TASK-025 | canvas | `canvas/**` | 50 | — | — | **2** | — |
| TASK-026 | agent | `agent/**` | 62 | 58 | 2 | 0 | 2 |
| TASK-027 | scriptbox+settings+creative | 三域 | 43 | — | — | 0 | 2 |
| TASK-028 | **base（横切层·非域）** | `base/**` | 96 | 50 真横切 + 9 宿主层 | — | **7** | 18 |
| TASK-029 | 小域 + hooks + types | 9 小域 + `src/hooks` + `src/types` | 33 | 25 | 0 | 0 | **8** |

- **跳过**（用户裁定 · 属其域内事务）：`videoEditor/**`(259) · `director3d/**`(30) ⇒ 这两域内部错位本轮**无人覆盖**（已知缺口）。
- 域主原始台账在该任务书**文件末尾**（附录形）。
- 关键实测：**`refs` 的 ① 段只给直连** ⇒ 域主若只看它会把"经桶消费"漏掉（见 §四-3）。

## 二 · 裁定（裁判直判 · 未派第二波）

| 件 | 送出方 | 裁定 | 依据（实测） |
| --- | --- | --- | --- |
| `canvas/backupStore.ts` | canvas | **认** ⇒ `base/store/` | 数据落点是**全量 localStorage**（`getLocalKeys()`）⇒ 不认识任何单域；与 `base/store/cloudSync.ts` 同族；界面 = `App.tsx:72` 工具栏 |
| `canvas/parts/JianyingIcon.tsx` | canvas | **并案** ⇒ `base/ui/` | image/video 各 1 处渲染 · 零数据落点 · canvas 0 处 ⇒ 零业务语义件 2 域即归横切（L3-bis） |
| `agent/canvas/*`（3 件） | agent 请求复核 | **不认**（维持 agent） | canvas 域**零消费**；agent 3 处消费（含门面 `index.ts:64`）；且它们**写 agent 会话数据**（`conversationStore`）⇒ 驱动方 + 唯一消费域 |
| `settings/sections/SkillSettings` ↔ `agent/runtime/skillStore` | agent 请求并案 | **不认并案**（维持 agent） | 数据真源在 agent（`contracts.ts` 登记 `domain:'agent'`），settings 仅管理 UI |
| `src/hooks/` 8 件待核 | base/小域 | **一件都不迁**（维持 `src/hooks/`） | 消费方只有 `App.tsx`（根装配）或横切管线条；且 `useNodeData`/`useEdgeData` 是**闸指定的写回唯一入口** |
| `base/**` 7 件 | base | **全认**，落点按裁定表（见 §三） | 各附四件证据（界面位置 · 数据落点 · `refs` 原文 · 反证检查） |
| `base/store/{nodeRuntimeStore,taskCompletionBus}` | base | **改判** ⇒ `task/`（非 base 原判的 `task`/`generate` 混合） | 计划 §3 明定 `task/ = {taskStore, taskCompletionBus, TaskCenter}` |
| `base/media/{canvasNodesBridge,libraryBrowse}` · `base/utils/media/nodeMedia` · `base/api/{generate,pollTask,relayProxy}` · `base/utils/imagePixel` | base | **认（按成片整体搬）** | 成片①「AI 中继」4 件 960 行 = TD-18-38 所指；成片③「画布节点-媒体/同步」3 件达 D2 门槛 |

### 二-bis · 域内落点：4 处无据可依的实证（→ `ADR-0044`）

| 现象 | 实测（`文件:行` / 命令） | 为什么无据可依 |
| --- | --- | --- |
| `image/` 域根两件 hook 无出口 | `image/useCopyNode.ts` · `image/useFitNodeRatio.ts`；消费方全在域内（`nodes/ImageBoxNode.tsx:30,118` · `nodes/ImageGenerate.tsx:37`） | D1 只许留"门面 + 计划指定例外"（`useImageHoverActions.tsx` 有豁免，这两件没有）；D3 的职责名表**没有 `hooks/`** ⇒ 既不能留根也无处可去 |
| D2 与 D3 给出两个落点 | `image/editors/cameraStudio.ts`（189 行，头注自述"纯逻辑层，无 UI 依赖"），消费者 `editors/CameraStudioPanel.tsx:24` | 按 D3（职责单一）属 `lib/`；按 D2（≥3 件门槛 ⇒ 并进职责最近的现成子目录）属 `editors/cameraParams/` |
| 子域门面被判"应删" | `image/editors/index.ts`：6 个消费方**全在域内**（`grep -rn "image/editors" src` 域外命中 0）；`image/index.ts:16-17` 明写"不重复导出 editors 符号" | "只留一份门面"未区分**域门面**与**子域门面**；删它要么改 6 处深路径、要么做宽门面（违 ADR-0039） |
| 类型件落点无判据 | `src/types/*`（5 件，含 `index.ts` barrel）与各域 `<域>/types.ts` 并存 | 无条款界定"什么时候随域、什么时候集中" |

### 本轮**推翻**的既有结论（回改留痕）

| 被推翻 | 原文 | 实测 | 处置 |
| --- | --- | --- | --- |
| `TD-25-21` 的处置 | "`base/store/backupStore.ts` 仅被 canvas 消费 ⇒ 迁 canvas" | 按**直接 fan-in 只数到一个件**判定；实际数据落点是全量 localStorage | 已回迁 `base/store/`；撤回该债 |
| 计划 §A5 行 | "`image/editors/index.ts` → **删**" | 其 6 个消费方**全在域内**、域外 0 处 ⇒ 是**子域门面**不是第二份域门面 | 已标推翻（ADR-0044 §3） |
| 计划 §A6 行 | "`captureFrame` → `video/lib/`" | 跨 scriptbox+video 真横切（`video/index.ts:11` 已裁"不得重审"） | 已标过期：留 `base/utils/` |
| 域主口径 | "`src/hooks/` 搬出会触发**规则 2**" | 规则 2 只管 `base/{core,utils,ui,api,storage}`（`check-arch.mjs:403`），hooks **不在管辖内** | 判据改为 L4-bis（已入 ADR-0040） |

## 三 · 执行（21 件）

| 批 | 内容 | 件数 |
| --- | --- | --- |
| 域内归位 | `image/hooks/`（新建，落实 D3 增 `hooks/`）· `editors/cameraParams/` · `agent/contract/`（新建） | 6 |
| 跨域·成片③ | `base/{core,media,utils}` → `canvas/lib/` | 3 |
| 跨域·成片① | `base/{api,utils}` → `generate/lib/`（AI 中继 4 件） | 5 |
| 跨域·其余 | `backupStore`→`base/store/` · `JianyingIcon`→`base/ui/` · `agentKeys`→`agent/runtime/` · `libraryBrowse`→`resource/` · `creative-library.css`→`creative/` · `nodeRuntimeStore`+`taskCompletionBus`→`task/` | 7 |
| 清理 | 3 个 `.DS_Store` | 3 |

工具：`node scripts/mv-sync-refs.mjs move`（**逐件**，未用 `move-dir`）。

## 四 · 闸 / 白名单 / 桶 / 测试 同步（不同步就假红 —— 成因 B）

| # | 位置 | 动作 | 不同步的后果（实测） |
| --- | --- | --- | --- |
| 1 | `check-arch.mjs` 规则 11 白名单 | `canvas/backupStore.ts` → `base/store/backupStore.ts` | 该件是新落点下合法 `getLocalKeys()` 消费者 ⇒ **假红 1 处** |
| 2 | `strict-src-whitelist.json` | 补 `canvas/lib/` `generate/lib/` `task/` | 三目录收了原受白名单覆盖的件 ⇒ **保护范围静默缩水** |
| 3 | `base/api/index.ts` 桶 | **摘掉 3 行** `export *`（generate/pollTask） | 桶继续 re-export 迁出件 ⇒ 横切层反向依赖业务域 ⇒ **规则 2 报红 2 处**；消费者改指 `generate/lib/*` |
| 4 | 测试写死路径（4 处） | `mediaRef` · `nodeImageWrite` · `logger` · `mockPartialSpread` | 全红。`mockPartialSpread.WATCHED` 原要求 `\.tsx?$`，而同步工具产出**无后缀**路径（仓内主导写法 1291 vs 56）⇒ 扫描基数假性掉到 <195（**假绿**） |
| 5 | `tests/unit/generate.test.ts` barrel 契约 | 改为「桶不再导出生成门面 + 门面由 `generate/lib/generate` 提供」 | 契约**因裁定而变**，断言须随更新（不是回退） |
| 6 | `scripts/node-file-resolver.cjs`（新建）· `check-node-handles` · `check-node-data` · `_smoke_checks.cjs` | 节点组件**落点唯一真源**收口（见 §五-1） | 三脚本各自拼路径 ⇒ A2 搬迁后豁免失效（build 硬红）＋规则 3 覆盖 15→4 件 |
| 7 | `videoEditor` 面板契约测试 | 判据两处过期（见 §五-2） | 5 个合规文件被误判 |

## 五 · 探针（改判据必须"先红后绿"）

1. **`check-node-handles` 规则 3 覆盖**：注入 `targetHandleId="probe-na"`（`video/nodes`）与 `sourceHandleId="probe-nb"`（`scriptbox/`）⇒ **两处都精确红** ⇒ 证明这两个目录此前是**盲区**（A2 后规则 3 只扫 `canvas/nodes`，15→4 件）。还原后绿。
2. **`vePanelLayoutContract` ③（占位撑满）**：原判据两处过期 —— ① 组件按 **TD-18-41（域前缀强制）** 已改名 `PanelState`→`VideoEditorPanelState`，正则没跟 ⇒ 5 文件被误判"0 占位"；② 原"数量**相等**"口径过窄（`grow` 分区可装内容，非占位独享）。改为**鸽笼判据**（占位 > grow 分区数 ⇒ 必违规）后：注入 1 个裸占位 ⇒ **精确红在 `properties/empty-view.tsx (PanelState 2 / grow 分区 1)`**；还原后 4/4 绿。

## 六 · 验证（全量实测）

| 检查 | 结果 |
| --- | --- |
| `tsc --noEmit` | **0 错** |
| `check-arch` | ✅ 通过（含"未发现会 TDZ 的循环依赖"·"横切层无反向依赖业务域"） |
| build 闸组（6 道） | ✅ 全过 |
| `check-strict-src` | ✅ 白名单 0 隐式 any |
| 单测 | **246 文件 / 3044 例全绿** |
| 死代码闸（knip） | 基线 **71 = 当前 71** · ✅ 无新增死代码（改名/搬迁 churn 已按"件随搬迁走"同步基线键；`videoEditor/index.ts` 的 `EditorShellProps` 零消费 re-export 按"判死→删"清掉，接口定义保留） |
| `adr audit` | 0 问题（ADR-0040 压回一页；记录型 ADR-0043 已删号，内容并入 0040 + 本文件） |

## 七 · 未处理 / 已知缺口

- **`videoEditor`(259) · `director3d`(30)**：按用户裁定跳过 ⇒ 其内部错位与测试归其域内自理（本轮唯一失败测试 `vePanelLayoutContract` 已因判据过期修好，见 §五-2）。
- **债账本回改未做**：`TD-18-35/36/37/38`（实测已偿/已过期/本轮已搬）· `TD-25-20`（锁死留原地）· `TD-18-34`（描述过期）· `TD-11-15`（母债摘要过期）—— 待下一轮统一 resolve。
- **`base/store/cloudSync`/`autoSync`** 实测 0 业务域消费（仅 App 装配）⇒ 本轮按"不触发 L4"留原地，未动。

## 八 · 覆盖度表（本轮登记）

| 区域 / 域 | 覆盖状态 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- |
| image | 深审（27/27） | 域籍判定第一波-2026-09-19 | `TASK-023` 主表 | 域内错位 3 件（已执行 3） |
| video | 深审（13/13） | 同上 | `TASK-024` | 无债（送出 0） |
| canvas | 深审（50/50） | 同上 | `TASK-025` | 送出 2 件（已裁） |
| agent | 深审（62/62） | 同上 | `TASK-026` | 待核 2（口径） |
| scriptbox+settings+creative | 深审（43/43） | 同上 | `TASK-027` | 送出 0 |
| **base（横切层）** | 深审（96/96） | 同上 | `TASK-028` | 送出 7（已裁）+ 待核 18（16 被横切件锁死） |
| 小域+hooks+types | 深审（33/33） | 同上 | `TASK-029` | 待核 8（已裁定全留） |
| videoEditor / director3d | **未审**（用户裁定跳过） | — | — | — |
