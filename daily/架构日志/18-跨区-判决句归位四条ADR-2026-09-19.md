# 架构日志 · 18 跨区 · CONTEXT 判决句归位（4 条 ADR）· 2026-09-19

> **触发债**：TD-18-20（ADR-0025 违反 · `spec/CONTEXT.md` 判决句未归位）。
> **性质**：**判据归位轮**（新建 ADR + CONTEXT 删正文留指针）—— 走《架构师改码 7 步法》。
> **分条粒度**：**用户拍板 A**（按能力域 4 条，与 ADR-0005 / ADR-0006 的 house style 一致）。
> **结论落点**：本文件（唯一写全处）。
> **一句话**：4 条判决句在 CONTEXT 里躺了很久，`adr.mjs search` **0 命中** ⇒ 没有违反判据、没有责任人、
> 等于没写。其中「配置集中」一条更是**写着却被违反 4 处** —— 这就是"判据不归位"的真实代价。

- **本区域状态**：`已验证(有债待还)` —— 本轮 **TD-18-20 结清**；**新登记 1 条**（`import.meta.env` 裸读 4 处）。

---

## 一 · Step 0 普查 + Step 1 证伪

`adr.mjs search` 逐条实测（关键词 → 命中数）：

| CONTEXT 行 | 判决句 | 关键词 | 改前命中 | 判定 |
| --- | --- | --- | --- | --- |
| `:53` | 禁止新增平铺顶层组件 | 平铺 / 目录 | **0**（命中 5 条的"目录"全是关键词巧合） | 属**组织约定** ⇒ 被用户 TD-23-2/3/4「组织约定组暂缓」裁定覆盖 |
| `:57` | 禁止裸读 env / 裸写数字 | 裸读 / 配置 | **0** | 真判据 |
| `:103` | 禁止手写外壳（NodeShell） | NodeShell / 外壳 | **0** | 真判据 |
| `:113` | 禁自写网络错误判定 / 禁无超时 | 网络错误 / 错误分类 | 3（**全是 ADR-0018「重试」**） / 0 | 真判据（与 0018 **互补不重叠**） |
| `:156` | 禁止越层 | 越层 / 分层 | **0** | 真判据 |

⇒ **债描述"5 处"里只有 4 处是真判据**（`:53` 是组织约定），且"违反"属实。

## 二 · Step 2~5 归因 / 定位 / 定契 / 定权

- **根因**：判据住在 CONTEXT（按 ADR-0025，CONTEXT 只装路由与例外）⇒ **没有违反判据、没有检索入口**。
- **可测后果**：「配置集中」写着，却仍有 **4 处** `import.meta.env` 裸读（见 §五新登记债）——
  规则不是没有，是**没有防线**。
- **定权**：口径由 ADR-0025 已定（用户裁定）；**分条粒度**属治理判断 ⇒ **摆 A/B/C/D 由用户拍板**（已选 **A**）。

## 三 · Step 6 改动

| # | 产出 | 内容 |
| --- | --- | --- |
| ① | **ADR-0033** 配置与常量的单一真源 | 唯一入口 `core/config.ts`（叶模块、零 import）；禁裸读 env / 裸写数字；`DEV` 判定的边界 |
| ② | **ADR-0034** 节点外壳唯一实现 NodeShell | 唯一实现 `base/ui/NodeShell.tsx`；配套 `useState(data.xxx)` / `setNodes` 不可变 / `useConnectedInputs` |
| ③ | **ADR-0035** 错误分类与异步超时唯一入口 | `genErrors.classifyError` + `asyncGuard`；禁关键词式判定、禁无超时 Promise；与 ADR-0018 的分工 |
| ④ | **ADR-0036** 分层职责不越层 | 六类意图各归其位；禁越层代劳；登记子项**已由 `check:events`/`check:keys` 毕业为闸** |
| ⑤ | `spec/CONTEXT.md` 4 处 | **删判据正文、留一行指针**（ADR-0025 要求）；`:53` 保留原文并就地标注「组织约定暂缓」的**例外**说明 |

**未改动任何代码** ⇒ 运行期零影响。

## 四 · Step 7 验证

**违反判据的可测复验**（改前 0 命中 → 改后各 1 命中）：

| 关键词 | 改前 | 改后 |
| --- | --- | --- |
| 裸读 | 0 | **1**（ADR-0033） |
| NodeShell | 0 | **1**（ADR-0034） |
| 错误分类 | 0 | **1**（ADR-0035） |
| 越层 | 0 | **1**（ADR-0036） |
| 平铺 | 0 | **0**（**刻意**：组织约定暂缓，已在 CONTEXT 标注例外） |

`node scripts/adr.mjs audit` → **35 条（现行 30）· 问题 0 项**（无模板占位符残留 ⇒ 不是空壳）。

**为何无需跑单测**：本轮**零代码改动**（仅新增 4 份 ADR 文档 + 改 `spec/CONTEXT.md` 文案）⇒ 不影响任何运行时行为。

## 五 · 新登记债（本轮取证中挖出）

**TD-18-32（新登记）**：`import.meta.env` 裸读 4 处越过唯一入口 —— `models.tsx:81`(`BASE_URL`)、
`project.ts:517` / `modalLayer.ts:129` / `tableWorkspaceState.ts:120`（均为 `DEV`）。
`core/config.ts:4` 早已明文「所有 `import.meta.env` 读取必须集中在此文件」⇒ **规则存在却被违反 4 处**，
正是本轮要治的病。判据见 **ADR-0033**。

> 本轮**不清理**这 4 处：它们属代码改动（需逐一判断 `DEV` 是否该走 `config.ts` 导出的 `isDev`），
> 超出"归位"半径，且 `DEV` 判定是否算违规本身需要先定边界 ⇒ 入账待还。

## 六 · 交付六闸自查

- **一归**：4 条判据各归其 ADR；CONTEXT 由"抄判据"改为"指判据" ✅
- **减一**：未新增任何代码兜底 ✅
- **减二**：CONTEXT 判据正文 −4 段（留指针）；4 条判据从"无检索入口"变为可点名检索 ✅
- **一诚实**：如实标注 `:53` 仍 0 命中（属暂缓，非遗漏）；如实写出"规则写着却被违反 4 处" ✅
- **零炸**：**零代码改动** ⇒ 无回归面 ✅
- **一凭证**：`adr.mjs search` 改前/改后对照 + `adr audit` 0 问题 + 逐条取证（文件:行） ✅

## 七 · 结论与下一步

- TD-18-20 **结清**（4 条判据已归位 + CONTEXT 留指针）。
- **A7 回改**：`spec/CONTEXT.md` 4 处已就地改为指针；`:53` 保留并标注例外理由（避免后人以为是遗漏）。
- **未纳入本轮**：
  1. `:53` 的归位 —— 组织约定，用户已裁定暂缓；
  2. 4 处 `import.meta.env` 裸读的清理 —— 已入账（见 §五）；
  3. ADR-0025 目标「CONTEXT 337 → 约 120 行」的其余段落 —— 本轮只动了被 TD-18-20 点名的 5 处。
- **可复用教训**：**判据不住在 ADR，就等于没有判据** —— 它没有违反判据、没有责任人、也进不了检索。
  判断某条规则是否"真存在"，最省事的一问就是：`adr.mjs search <关键词>` 命中吗？
