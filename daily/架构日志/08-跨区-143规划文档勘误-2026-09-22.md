# 08-跨区 · `143` 规划文档勘误 · 2026-09-22

> 审计对象：`docs/plan/143-生成链路超时与常量收口-规划-2026-09-22.md`（当前版含三轮扩写 + 配套任务书）
> 依据：债务登记 5 步法 · **只读**（未改任何 `src/**`、`localTool/**`）· **未跑任何测试**
> 定性：本轮登记的是**规划文档自身**的债（其内容是后续施工的判据 ⇒ 文档错会级联成施工错）

## 一、探债表

> 债号对照（2026-09-22 施工复核轮补，修锚点断链）：①=TD-08-47 · ②=TD-08-48 · ③=TD-08-49 · ④=TD-08-50。
> **均已由他轮处置并归档**；本轮逐条回查修复实况：①（§7 撤销 G 已补，`:340-346`）✓ · ③（S0 改「用符号定位」，`:246`）✓ · ④（读数除量级外全改指针，`:392-399`）✓ · ②（S11′ 三处中 `:8`/`:249`/`:371` 已改，但 `:385` 仍写「待拍板（仅 1 项）：决议⑥」）**只修一半 ⇒ 回潮，见 TD-08-58**。

| ID | 位置(文件:行号) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因 | 大致解法(一句话) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ① | `143-….md:8`、`:36` | **悬空引用**：两处写「见 §7 撤销 G」，而 §7 只到 **撤销 F**（`:322-332`），**无 G** | 规划文档（localTool 超时域） | 读者无法核证"决议⑥/S11′ 已撤销"的依据；下一位会去找不存在的章节 | 低 | 孤例（写作时引用了未落地的编号） | 补 §7 撤销 G 或把引用改指实际章节 | 待还 |
| ② | `143-….md:7`、`:249`、`:371` | **半态残留**：S11′ 三处状态互斥 —— `:7` 标「已撤销」/ `:249` **仍在施工表**且依赖「拍板①」/ `:371` 写「仅 S11′ 等拍板」；且「拍板①」本身已在 `:261` 撤回 ⇒ 依赖项不存在 | 同上 | 执行方按 `:249` 会去等一个不存在的拍板项；按 `:371` 会误判"有阻塞" | 中 | **改一处未改另一处**（撤销只改了正文与顶部，漏施工表） | 定死 S11′ 存废并三处同步 | 待还 |
| ③ | `143-….md:239`（S0 验收）、`:241`（S2′ 落点） | **行号错误 + 自相矛盾**：S0 称 `relay-poll.ts:598` 是「全文件唯一读 `poll.*` 的点，只读 `intervalMs`」；实测 `:594` 是**注释行**、`:598` 是 `handle.upstreamStartedAt = ackAt;` —— 二者均落在 **`runDirectSubmit` 尾部**，不是 `poll.*` 消费点；S2′ 又写 `:594` | 规划文档 → 直接影响 S0 验收 | S0 的**锁事实断言指向错行** ⇒ 该验收即使"绿"也不锁任何东西（A15 装饰风险） | 中 | 行号取自改动前的旧文件，未随正文编辑重算 | 重新定位 `registerHandle` 内 `handle.poll?.intervalMs` 的真实行号并统一 | 待还 |
| ④ | `143-….md:373` vs `:6/:35/:261/:317/:361/:375` | **SSOT 第二份（可 grep 计数）**：`:373` 声明「**读数唯一真源在任务书**」「本文件不再复述读数」，但同文件复述读数 ≥4 处（`482.3s`、`295.8s`、`n=478`、`250s`、`0.84%`） | 规划文档 | 两处读数必然漂移；且与它自己声明的"唯一真源"冲突 | 中 | 声明与实操不一致（声明后未回删已抄的读数） | 读数只留任务书，本文件改指针 | 待还 |

> 证据口径：①② 由 `grep 撤销 G / S11′ / 拍板①` 直接命中；③ 由 `read_file relay-poll.ts offset=590 limit=14` 实读（`:590-603` 全部为 `runDirectSubmit` 尾部 + catch）；④ 由 `grep 482.3s / 295.8s / n=478` 计数。

## 二、判非债（留痕，防重审）

| 项 | 裁定 | 证据 |
| --- | --- | --- |
| §2.3 的「chat 保持前端声明」 | **非债**（2026-09-22 用户裁定） | `:104-117` 分层判据「谁等，谁就是生产者」自洽；chat 同步链无句柄、响应即终态 |
| §4 #6a 改判「修」不「删」 | **非债** | `:187-199` 四关逐关给了答案，ADR-0053 判据引用正确 |
| §4.1 第二例（`MAX_CONSECUTIVE_POLL_ERRORS` 前端 5 / 后端 30） | **非债** | `:177-185` 载体不同、值不同，判"不得按名合并"正确 |
| §7 撤销 D/E/F 的留痕体例 | **非债** | 符合「弯路的思路比结论更该留」；E 的"一手观察也要能对账"是有效教训 |

## 三、覆盖度表

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- |
| `docs/plan/143-…规划-2026-09-22.md` | 🔴 | 08-跨区-143规划文档勘误-2026-09-22 | 见 §一 | ①②③④ 待还 |
| `docs/plan/143-…取证与施工任务书-2026-09-22.md` | ⚪ 未审 | — | — | **本轮未审**（未读，故不构成"审完"） |
| `localTool/src/relay-poll.ts` | 🟡 抽审 | 同上 | 仅实读 `:590-603` 一处 | 由 ③ 涉及，未做全文件重审 |
| 其余 `localTool/src/**` | ⚪ 未审 | — | — | 超出本轮范围（本轮对象 = 规划文档） |

**覆盖账**：本轮对象 1 份（`143` 规划）→ 审 1 / 已登记债 4。**另 1 份配套任务书未审**（未读）⇒ 本区**不可写"审完"**。

## 四、状态行

- 本轮**只读**：`src/**`、`localTool/**` 一行未改；**未跑任何测试**（执行次数 = 0）。
- 命中**工具债**：无（`scripts/**`、`.codebuddy/commands/**`、`package.json scripts` 均未涉及）。
- 跨区发现只开本一份文件。
- 产出：本文件 + `debt.mjs` 4 行摘要。
- 下一步：读配套任务书 → 补审（其中"已核实前提 §1.5"清单是重点，因其自称不必再查）。
