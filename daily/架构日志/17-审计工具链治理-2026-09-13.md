# 架构日志 · 审计工具链治理 · 2026-09-13

> **文件名**：`17-审计工具链治理-2026-09-13.md`。同区多次审计各写新日期文件、不覆盖旧档。
> **触发**：用户发起「挖掘债务表没登记的债，可能是其他领域」（2026-09-13）。巡检 01–16 区债表后，发现**审计/腐化检测工具链自身**（`audit/`）是债表完全零覆盖的"其他领域"。
> **审计依据**：`债务登记5步法.md`（自底向上 + 覆盖度 + 反偷懒闸）。实证用 `node scripts/mv-sync-refs.mjs` + `package.json`/`ci.yml` grep + `audit/download/out/*.json` 产物 mtime 交叉验证。
> ⚠️ **只读不改码**：以下债只登记；修复（挂 gate / CI）交统一决策架构师。
>
> **更新（二轮 · 2026-09-13）**：TD-17-1 已按用户裁定**收口** —— `audit/` 沙盒整体退役，唯一主工程未覆盖的能力「死代码 knip」已并进主工程闸体系（devDependency + 根 `knip.json` + `scripts/check-dead-code.mjs` 基线永不复涨 + `gates.manifest` push 层 + CI 步）。**下文对 `audit/` 路径与产物的描述均为历史快照，已不再对应现网**。详见 [二轮·TD17-1并进主工程](./17-审计工具链治理-二轮-TD17-1并进主工程-2026-09-13.md)。

## 一、链路快照（refs 实证，非凭文件名猜）

本区是"审计/治理基建"这条**元链路**，不是业务数据流：

```
工具链契约层（audit/，不进主工程 package.json）：
  audit/README.md（定位：给 AI 的静态分析工具箱，§五明示"不挂 pre-commit，只手动/PR 级跑"）
  audit/knip.json / .dependency-cruiser.cjs / .dependency-cruiser.localtool.cjs（配置）
  audit/download/（依赖沙盒 + 产物 out/，gitignore 不入库）
    └→ out/knip.json(09-09) / out/jscpd-report.json(09-13刚跑) / out/deps.json(09-01)
       / out/madge.json(09-06) / out/oxlint.json(09-09) / out/oxlint-localtool.json(09-02)
       证据：out/ 各产物 mtime 与代码改动时间错位（09-11~09-13 有 localTool 重构/节点收口等大量改动，未重跑）

主工程 gate 执行层（scripts/，已被 02 区十四轮收口为 gates.manifest，非本区债）：
  scripts/gates-run.mjs + gates.manifest.json（13 道闸分 build/commit/push/health，MD-14-1 已收口）

CI 接线层（.github/workflows/ci.yml）：
  仅 type-check + test:coverage + localTool(type-check/build/test) —— 不接 audit/ 任何工具
```

**DATAFLOW.md 校对**：此前 16 个区域段**完全未覆盖** audit/ 工具链 → 本区是 DATAFLOW 进度地图的**新盲点**（已在本文件收尾补 17 段打灯）。

## 二、溯源（Phase 1：症状 vs 病根）

- 病根不在业务模块，而在**治理基建自身**：有 knip/jscpd/oxlint/depcruise/madge 五套静态分析能力，却从未接入任何自动化 gate/CI，且产物快照停留在 09-01~09-09，与当下代码（09-13）严重脱节。
- 这是"审计师不审计自己"的元结构债——与已登记的 **MD-14-1**（主工程 scripts/ gate 立了没挂）同源，但**独立领域**：MD-14-1 是主工程 gate 漏挂 prebuild/pretest；本区是**另一套完全游离在 CI/gate 之外的工具箱**，连"漏挂"都谈不上（README 明示不挂）。

## 三、定海（Phase 2：不变式 / 唯一真相源）

- **应有不变式**：「任何静态分析能力，要么挂进 gate/CI 自动跑，要么明确标记为'手动工具、结论不保证时效'」。现状两者皆非：audit/ 既未挂自动化，又未声明"产物可能过期"，导致 `out/*.json` 被误当"已审计"证据。
- SSOT 问题：无（工具链本身结构清晰，问题在"接线"不在"实现"）。

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- **第二份真相**：jscpd 报告 137 处重复代码块。⚠️ **抽样核实 1 处已证非债**——`tableInvariants.ts:25-30` 与 `conversationInvariants.ts:43-48` 的 `err()/warn()` 工厂函数逐字雷同，但两文件头注释（L7/L8-9）明写"经中立 `agentTypes.ts` 共享 `Violation[]` 范式"，且因依赖方向约束（避免与 conversationState 成环）**刻意不复用**。删除重复会引入环。→ **jscpd 137 处不能批量登记为债**，须逐块取证（见 §五 待核实清单）。
- 无上帝类问题；audit/ 工具箱结构清晰（README + 5 配置 + download 沙盒）。

## 五、探债（Phase 4：技术债账本）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-17-1 | 静态分析工具链（audit/ 的 knip/jscpd/oxlint/depcruise/madge）完全游离于 gate/CI：package.json 零引用、ci.yml 仅 type-check+test、README 自承不挂 pre-commit、`download/out/*.json` 产物 mtime 09-01~09-09 未随 09-11~09-13 重构重跑 | 增债 | 结构债 | 全仓腐化可见性（死代码/重复/环依赖持续累积无人知） | 高 | 母体 M-17-1（审计基建自身未被治理，"闸自身无闸"） | 将 knip(jscpd 可选) 挂入 `scripts/gates.manifest.json` 的 health 阶段 / 或 ci.yml 增一步 `cd audit && npm run dead:code && npm run dup:code` 并 fail 阈值化；产物随 PR 重跑 | 待还 |

**jscpd 137 处重复——待逐块取证清单（非债、不登记）**：
- 已取证 1 处：`tableInvariants.ts` vs `conversationInvariants.ts` 的 `err()/warn()` 重复 → **已裁定非债**（刻意零环设计，见 §四）。
- 其余 136 处（含 `canvasHost.ts:39-52` vs `toolRegistry.ts:29-35` 工具注册样板、`canvasPlanExecutor.ts` 内部多段自重复、`assistant-table.css` 多段样式重复）**未取证**，严禁直接抄成账本。下一步：抽 ≥5 处逐块读代码判"真重复债 vs 有意近似 vs 样式对称"，取证后才登记。本流程不代执行。

**死类型疑点——已复核撤销（证据不足，不登记）**：
- 本轮回查曾疑 `src/types/refToken.ts`（`RefImageNode`/`RefTextNode`）与 `gifenc.d.ts` 为死类型，但全仓 grep 复核：`refToken.ts` 运行时编解码函数（encodeRefToken/parseRefTokensFromText）在既有 16 区首轮已裁定非债，真实消费方为 agentRuntime/useCanvasAgentTools；其类型导出经 `@/types` barrel 别名消费时 `refs`/`knip` 均因别名盲区漏报，证据不充分 → **撤销死类型债登记**（证据闸：挂不出可靠证据=无效结论）。`gifenc.d.ts` 为外部库类型声明，find-dead 误报常见，亦不登记。

## 六、覆盖度表

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| audit/README.md | L0 契约 | 深审 | 🟡 | 一轮 09-13 | README §五 自述"不挂 pre-commit" | 非债(有意设计)·但产物过期=TD-17-1 |
| audit/knip.json | L0 契约 | 深审 | 🔴 | 一轮 09-13 | package.json grep 零引用 knip | TD-17-1 |
| audit/.dependency-cruiser.cjs | L0 契约 | 抽审 | ⚪ | 一轮 09-13 | 同 TD-17-1 母体 | 待还(随 TD-17-1) |
| audit/download/out/*.json | L1 产物 | 深审 | 🔴 | 一轮 09-13 | mtime 09-01~09-09 vs 代码 09-13 | TD-17-1(过期) |
| .github/workflows/ci.yml | 横切 CI | 深审 | 🔴 | 一轮 09-13 | ci.yml 仅 type-check+test:coverage+localTool | TD-17-1(未接工具链) |
| scripts/gates-run.mjs + manifest | L2 gate | 已裁定非债 | 🟢 | 一轮 09-13 | MD-14-1 已收口(02 区十四轮) | 非债(非本区债) |
| jscpd-report 137 重复块 | 横切 | 抽审 | ⚪ | 一轮 09-13 | 抽样 1 处证非债，余 136 未取证 | 待逐块取证(不登记) |
| src/types/refToken.ts | L0 类型 | 已裁定非债 | 🟢 | 一轮 09-13 | 全仓 grep：编解码函数有消费方(16 区已裁定) + barrel 别名盲区 | 非债(证据不足·撤销登记) |
| src/types/gifenc.d.ts | L0 类型 | 已裁定非债 | 🟢 | 一轮 09-13 | 外部库 .d.ts，find-dead 误报常见 | 非债(不登记) |

**覆盖账**：本区共 9 个文件/资产 / 本轮审 9 个 / 未审 0 个。
- 未审：0（本区资产已全部过一遍）。
- 注：`audit/download/out/` 下的 jscpd 137 重复块作为"清单"列为抽审待取证，不计入"未审文件"（其宿主文件已深审）。

---

## 七、结论与下一步

- 本区域状态：**已验证(有债待还)**（TD-17-1 高息母体）。
- 待还债项已流入 `daily/架构日志/债务.md`：是（TD-17-1）。
- DATAFLOW.md：已就地补「审计工具链治理」段并打灯（🟡/🔴），未塞明细。
- 下一步建议（交统一决策架构师）：① TD-17-1 挂 gate/CI；② 对 jscpd 136 处未取证重复抽样 ≥5 块判真债（本流程不代执行）。
