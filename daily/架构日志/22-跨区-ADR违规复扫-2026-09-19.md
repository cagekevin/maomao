# 22-跨区-ADR违规复扫-2026-09-19

- **区域**：22 视频（全量重审）· 跨区 ADR 合规复扫
- **触发**：用户 `债务登记5步法` —— "根据我们的 ADR，去查还有哪些违反 ADR 的"
- **层**：横跨 地基/中间/上层（以 ADR 为透镜，不按单区）
- **日期**：2026-09-19
- **方法**：读 `docs/adr/README.md` 现行 32 条 → 取各 ADR「📌 违反时的判据」→ 对 `src`/`localTool` 跑定向 grep + 读真实代码取证；`tests`/`docs`/`.regression-entry.cjs` 已排除。

---

## 一 · 探债（线索全 · 解法从简）

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-66 | `src/components/base/utils/videoEngine.ts:203` · `:315` · `:381` | ADR-0019 形式4（失败归一 null）· 无痕兜底：`computePacketStats(120).catch((): null => null)` 把 fps 探活异常静默吞，零日志/零告警 | base/utils · videoEngine | fps 静默变 0（readVideoMetadata:210）/ 30（concatVideos:392）；导出帧率失真无人知 | 低 | 孤例（非母体；全仓同类已用 `releaseQuietly`/`logger` 留痕，唯此处漏） | 改为 `.catch((e)=>{ logger.debug('videoEngine','fps 探活失败(不阻断)',e); return null; })` | 待还 |
| TD-07-9 | `director3d/Viewport.tsx:94/310/505/667/692/753/809/814/1270`(×9) · `SceneGizmo.tsx:97` · `models.tsx:269/273`(R3F `useThree` 集群≈12) · `timeline-element.tsx:689/702`(KeyboardEvent 当 MouseEvent 强转) · `useScriptBoxEngine.ts:113`(setEdges 签名强转) | 双重断言 `as unknown as` 可避免子集：R3F `useThree()` 把 loose `state` 断言成具体形（可抽 `useThreeControls()` 根除）；事件/setEdges 为真类型谎言（掩盖形参/契约错配） | director3d/视频编辑器/脚本盒 | R3F 集群：controls 为 null 即读数 TypeError；事件强转：KeyboardEvent 缺 MouseEvent 字段运行时可能 undefined | 中 | 母体=缺 typed wrapper / 上游 loose 类型未收敛（非判别联合绕墙，但违反用户「TS+React 禁止项」#4） | 抽 `useThreeControls()` 等 typed 出口收敛断言到一处；`toggleMirror/toggleReverse` 形参放宽为 `React.SyntheticEvent` | 待还 |
| @观察 | `scripts/1mao-scripts/scriptbox-split-snapshot/shared.js:8169` | `JSON.parse(JSON.stringify(e))` 深拷贝（`hd` 函数）；ADR-0005 禁手写 JSON 往返 | 工具链 · scriptbox 快照 | 工具内深拷贝，非运行时业务；语义等价但无 structuredClone 守卫 | 低 | 工具链范围灰色（ADR-0005 措辞「业务代码禁止」）；疑为构建期辅助 | 工具内改用 `structuredClone`；或确认属生成物不修 | 观察 |

---

## 二 · 已裁定非债（取证留痕 · 防重审）

> 下列 ADR 经本轮 grep + 读真实代码取证，确认已收口 / 非债。**再审计时直接采信，不再重查。**

| ADR | 结论 | 证据（文件:行） |
| --- | --- | --- |
| ADR-0002 压平必须留痕 | 非债：3 处 `.ok ?` 均把 `message`（err）透传，未丢 | `backupStore.ts:94` · `tableWorkspaceState.ts:199` · `director3d/storage.ts:59`（`outcome.ok ? undefined : outcome.message`） |
| ADR-0004 禁止从显示串反解析 | 非债：formatBytes 已改走结构化出口 | `StorageMonitor.tsx:661`（注释明示走生产者结构化出口）· `core/utils.ts:407` |
| ADR-0005 深拷贝=结构化克隆 | 非债（src 内）：仅实现层 `core/utils.ts:114` 用 structuredClone；业务代码 0 手写 | `core/utils.ts:8/114`（实现+注释）· `director3d/project.ts:25`（注释，已收口） |
| ADR-0006 canvas 产出唯一出口 | 非债：生产代码仅 `core/utils.ts` 内 `canvasToImageDataUrl`/`canvasToBlob` 为唯一出口；调用方全走 wrapper | `useVideoPoster.ts:56`(canvasToImageDataUrl) · `captureFrame.ts:261` · `clipboard.ts:84/175` · `imageUpscale.ts:105` · `processing.ts:147` |
| ADR-0007 接管全局 API 须闭包捕获原引用 | 非债：未发现接管 console/fetch/Date 后查全局私有属性的写法；`logger.ts` 为日志 sink 本身 | `logger.ts:116-118`（sink）· `backendLogStream.ts:27-29`（映射，非接管） |
| ADR-0008 定序真源=git时间（缺失值不比较） | 非债：`lastModified` 均为媒体资产元数据属性，非「系统入库顺序」定序判据 | `videoEditor/.../assets.ts:20` · `service.ts:441/654`（资产属性，非排序键） |
| ADR-0009 resultKey 唯一写回路径 | 非债（TD-01-21 已收口）：`useNodeGeneration` 为唯一写回点，`onSuccess/onRecover` 只做非 data 副作用 | `useNodeGeneration.ts:176-180` · `useGenerateNode.ts:153-165` · `ImageGenerate.tsx:298/336` · `VideoGenerate.tsx:221/252` |
| ADR-0011/0019 禁静默吞（catch-ok 已删） | 非债（主体已收口）：残留 `catch-ok:` 注释均为「文档化结构性豁免」（RECURSION_GUARD / LOCK_CHAIN / NON_BLOCKING / RELEASE_FAIL），且对应实现已走 `releaseQuietly`/`logger` 留痕 | `logger.ts:78`(RECURSION_GUARD) · `projectMemoryStore.ts:71`(LOCK_CHAIN) · `asyncGuard.ts:209/283`(NON_BLOCKING/RELEASE_FAIL) · `ImageBoxNode.tsx:354`(已改 logger.warn) |
| ADR-0018 仅网络错误重试 | 非债：`httpClient.ts` 显式 4xx/HttpError 不重试；`retryable` 字段已删去误导 | `httpClient.ts:754-756` · `contracts.ts:752-756` |
| ADR-0030 零生产消费不许预留 | 非债：注释中的「别提前预留」均为**删除幽灵预留后的告诫**，非新增预留 | `mediaRefTypes.ts:22` · `mediaRefRegistry.ts:17` · `canvasNodesBridge.ts:33` · `timeScale.ts:11`（均 TD-02-50 清偿后留痕） |
| ADR-0021 判别联合禁 as/! 绕墙 | 非债（字面 scope）：全仓 38 处 `as unknown as` 经逐点核验，**无一在绕判别联合**——均为「从 unknown/第三方 loose 类型取形」或「事件/函数签名错配」。严格按 ADR-0021 判据不命中；其中可避免子集已立 TD-07-9 | `config.ts:45`/`appSettings.ts:93`(DebugWindow 全局) · `projectStore.ts:443`/`deriveNodes.ts:139`/`structuralSnapshot.ts:154`/`App.tsx:257/688`(反序列化→Node) · `videoEngine.ts:294`/`transcription/worker.ts:154/186/189`/`faceMosaic.ts:63`(第三方库 chrome/transformers/mediabunny) · `editor-provider.tsx:153`(beforeunload returnValue) |
| 用户「TS+React 禁止项」#4 双重断言 | 观察：该规则绝对禁止 `as unknown as`，但上述「必要的 unknown/第三方 loose 取形」类（≈23 处）若强行改会引入 `any` 或断类型链；**是否按 #4 严格推行属待用户拍板**：若推行，须先补 typed wrapper / 全局 augmentation，而非裸删断言 | 同上 38 处全量清单见本轮探债 TD-07-9 行 + 本表非债行 |

> 注：本轮未做语义级深审的 ADR（ADR-0013 纯查询禁副作用 · ADR-0028 拉取只由视图意图驱动 · ADR-0029 交互单一所有者）属「抽审」状态，下轮如需再查。

---

## 三 · 覆盖度表

| 文件层 | 文件 / 系统 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- |
| 地基 utils | `src/components/base/utils/videoEngine.ts` | 🔴 有债待还 | 本轮 2026-09-19 | `:203/:315/:381` | TD-22-NEW |
| 地基 utils | `src/components/base/utils/asyncGuard.ts` | 🟢 已裁定非债 | 本轮 | `:119`(catch 重抛) | 非债 |
| 地基 utils | `src/components/base/utils/faceMosaic.ts` | 🟢 已裁定非债 | 本轮 | `:82`(catch 重抛) | 非债 |
| 上层 hook | `src/hooks/useVideoPoster.ts` | 🟢 已裁定非债 | 本轮 | `:56/:59-61`(wrapper+logger.debug) | 非债 |
| 上层 UI | `src/components/base/prompt/PromptHub.tsx` | 🟢 已裁定非债 | 本轮 | `:231-238`(status='error' 可见态) | 非债 |
| 上层 节点 | `src/components/base/store/useNodeGeneration.ts` 等(resultKey) | 🟢 已裁定非债 | 本轮 | `:176-180` | 非债(ADR-0009) |
| 中间 API | `src/components/base/api/httpClient.ts` | 🟢 已裁定非债 | 本轮 | `:754-756` | 非债(ADR-0018) |
| 上层 3D | `src/components/director3d/*.tsx` + 跨区事件/setEdges 强转 | 🔴 有债待还 | 本轮 | `Viewport.tsx:94/310/505/667/692/753/809/814/1270` · `SceneGizmo:97` · `models:269/273` · `timeline-element:689/702` · `useScriptBoxEngine:113` | TD-07-9（可避免子集）；余 23 处判非债 |
| 元层 工具 | `scripts/1mao-scripts/scriptbox-split-snapshot/shared.js` | ⚪ 抽审 | 本轮 | `:8169` | @观察 |
| 后端 | `localTool/**` | 🟢 深审无债 | 本轮 | grep 0 命中(src 级) | 非债 |

---

## 七 · 状态行

- **本轮结论**：跨区 ADR 合规复扫（续）。新增：director3d `as unknown as` 集群全量 38 处逐点核验 → 可避免子集立 **TD-07-9**（增债/中/结构）；其余按「从 unknown/第三方 loose 取形」判非债（ADR-0021 字面不命中）。工具链 JSON 往返（shared.js:8169）维持观察。全仓 `as unknown as` 是否按用户「#4」严格推行属**待用户拍板**。
- **覆盖账**：本轮审 12 类（M=12）/ 全仓 ADR 现行 32 条（N=32，按可 grep 取证项计）；未做语义深审 3 条（ADR-0013/0028/0029，理由：需逐文件语义比对，超出本轮 grep+读码范围，标抽审）；未审 0。
- **未审清单**：ADR-0013（纯查询禁副作用）、ADR-0028（拉取只由视图意图驱动）、ADR-0029（交互单一所有者）—— 均因需语义级比对，本轮未纳入，下轮若查须从契约/事件层起手。
- **动作**：已 `debt.mjs add` TD-22-66（ADR-0019 无痕兜底）+ TD-07-9（as unknown as 可避免子集）；工具链 JSON 往返（shared.js:8169）维持观察，待用户拍板是否计入。
- **下一区**：待用户就「#4 是否严格推行」及「工具链 JSON 往返」两项拍板；或挑 ADR-0013/0028/0029 做语义深审。
