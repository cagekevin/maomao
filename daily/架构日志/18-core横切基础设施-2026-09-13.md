# 架构日志 · core 横切基础设施（base/core 地基层） · 2026-09-13

> **文件名**：`18-core横切基础设施-2026-09-13.md`。本区为**其他领域**（区域 01–17 仅把 base/core 的个别文件当作某业务段的脚注——`contentStore` 属 02、`contracts`/`eventBus` 属 13、`logger` 在 07-1/02-16 顺带提及——从未把 `base/core/` 作为**地基域**整体审计）。自主追加为第 18 区（编号顺延 `max(NN)+1`，依据 `index.md` 区域治理规则第 1 条；注：区域 17 已被审计工具链治理占用，故本域顺延为 18）。
> **为何漏列**：`base/core/` 是全局最底层地基（日志/确认/通知/模态层/id/配置/降级/工具），几乎所有模块都 import，却因"被各业务域顺手引用"而从未作为主区域审视。其健康项多，但**地基层一处重复/死代码会被全仓放大**，故必须独立取证留痕。
> 审计视角：数据流（地基层无 fan-in 数据流，按「定义地基 → 消费方」取证）。实证统一用 `node scripts/mv-sync-refs.mjs refs <file>` + 读真实代码（**全程只读不改码**）。

## 一、链路快照（refs 实证，非凭文件名猜）

> 本域为横切地基，无跨模块 fan-in 数据流；取证重点是「是否仍是单源 / 有无死代码 / 注释与实现是否一致」。
> `node scripts/mv-sync-refs.mjs refs logger.ts`（2026-09-13）：**87 处 import**（全仓几乎所有 store/节点/agent/api 均消费），是全局日志唯一入口，健康。
> 其余文件均为模块级 store / 纯函数，fan-in 见覆盖度表。

## 二、溯源（Phase 1：症状 vs 病根）

- 本域是「横切基础设施」，健康项占绝对多数（logger 单入口 + 去重、confirmStore/toastStore 范式对齐、modalLayer 防误登记告警、idGen/config 单源、degrade 两层降级收口、utils 大量收口纯函数）。
- 真缝隙集中在「**注释自承的约定 vs 实际实现**」与「**兼容性壳零调用**」两处（见 §五）。

## 三、定海（Phase 2：不变式 / 唯一真相源）

- **日志单源**：`logger.ts` 唯一出口 + `backendLogStream.ts` 镜像 + `reportToBackend` 200ms 同 key 去重（设计意图合理，区域 16 已判非债）。**非债**。
- **ID 单源**：`idGen.generateId` 唯一入口，禁散落 `Math.random()`。**非债**。
- **配置单源**：`config.ts` 集中 env 读取。**有债**（TD-18-1：该文件自陈约定被自身违反）。
- **降级单源**：`degrade.ts` 两层降级收口。**有债**（TD-18-2：兼容分支零调用）。

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- 唯一真缝隙：① `config.ts:68` 顶层缓存的 `DEBUG_ASSET` 与同文件 line 49-51「调试开关不缓存」约定自相矛盾（TD-18-1）；② `degrade.ts:43` 字符串两参兼容分支无调用方（TD-18-2）。
- 其余文件无需切割。

## 五、探债（Phase 4：技术债账本）

> 本轮**只读**，2 笔债只登记；偿还计划写"将来怎么改"，不执行。

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-18-1 | `config.ts:68` `export const DEBUG_ASSET = isDebugModuleOn('asset')` **顶层缓存**了调试开关，与同文件 line 49-51 自陈约定「总开关不在模块顶层缓存（否则运行时设 `window.__DEBUG_ALL` 不生效），改由 `isDebugModuleOn` 每次实时读」**直接矛盾**；且 `DEBUG_ASSET` 在 src 业务侧**零消费方**（grep 实证仅 `config.test.ts` 引用），「旧代码引用 DEBUG_ASSET 处无需改动」注释（line 66-67）失实 → 死别名 + 注释漂移 | 增债 | 结构债 | config 调试开关契约（误导后续改动信任该别名实时性） | 低 | 孤例（注释漂移 + 死别名，五问⑤命中；非母体） | 删 `DEBUG_ASSET` 死导出（同步删 line 66-67 失实注释；若确要保留 alias 语义，改为 `export function debugAssetOn() { return isDebugModuleOn('asset'); }` 函数形态，遵守"实时读"约定）；属纯删/改名，零行为变化 | 待还 |
| TD-18-2 | `degrade.ts:43` `reportDegrade('layer', err)` 两参字符串兼容分支内 `arguments[1]` 取第二参；grep 实证全仓 11 处 `reportDegrade(` 调用**全用对象形态** `reportDegrade({layer,key,e})`（含 toast 文案），旧式字符串两参形态 **0 调用方** → 该兼容分支（含 `eslint-disable-next-line prefer-rest-params` 掩盖）是死代码 | 持平 | 结构债 | degrade 公共 API 契约面 | 低 | 孤例（死分支，五问④「死代码」命中；非母体） | 评估：① 若保留为公开 API 兼容性面 → 补 1 个该形态的调用/测试锁死，免被误当无用删掉；② 若确认不再需要 → 删字符串两参分支 + `eslint-disable` 注释，仅留对象形态。决策待定（不阻塞主链路） | 待裁定 |

> 健康项（已裁定非债，留痕防重审）：
> - `logger.ts`（87 fan-in 唯一日志出口，去重设计合理）🟢非债
> - `backendLogStream.ts`（SSE 镜像后端日志，幂等订阅，无回写不递归）🟢非债
> - `canvasSyncBus.ts`（BroadcastChannel 单源 `CANVAS_SYNC_CHANNEL`，与 `useCanvasSync` 3s 轮询互补，非第二份真相）🟢非债
> - `confirmStore.ts` / `toastStore.ts`（模块级 store 范式对齐，订阅/结算/重入保护完备）🟢非债
> - `modalLayer.ts`（登记表 + 常驻误登记告警 + `coversViewport` 可见性判据，根治快捷键静默失效事故）🟢非债
> - `idGen.ts`（generateId 唯一入口）🟢非债
> - `uiHooks.ts`（isEditableTarget/useOutsideClick/useSizeSync/useNodeResize/useContentHeightSync/useNodePosition 大量收口，纯函数可单测）🟢非债
> - `utils.ts`（toAbsoluteFileUrl/deepClone/stableStringify/contentFingerprint/formatBytes/debounce/throttle/createRafBatch 等统一收口，无第二份）🟢非债
> - `contentStore.ts` / `contracts.ts` / `eventBus.ts`（已在区域 02 / 13 深审，本区不重审，状态沿用他区覆盖度）🟢非债

## 六、覆盖度表（查过什么 / 还剩什么 —— 防重复审、防假装审完）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| src/components/base/core/config.ts | L0 配置地基 | 有债待还 | 🔴 | 首轮 09-13 | :68 `DEBUG_ASSET` 顶层缓存 + :49-51 自陈「不缓存」矛盾 + grep 零 src 消费方 | TD-18-1 |
| src/components/base/core/degrade.ts | L0 降级地基 | 有债待还 | 🔴 | 首轮 09-13 | :43 `arguments[1]` 两参分支 + grep 11 处调用全对象形态、0 字符串两参 | TD-18-2 |
| src/components/base/core/logger.ts | L0 日志地基 | 已裁定非债 | 🟢 | 首轮 09-13 | refs 87 fan-in 唯一出口；去重设计合理（区域 16 已判非债） | 非债 |
| src/components/base/core/backendLogStream.ts | L0 日志镜像 | 已裁定非债 | 🟢 | 首轮 09-13 | SSE 幂等订阅，无回写 | 非债 |
| src/components/base/core/canvasSyncBus.ts | L0 跨窗同步 | 已裁定非债 | 🟢 | 首轮 09-13 | `CANVAS_SYNC_CHANNEL` 单源，与 useCanvasSync 互补 | 非债 |
| src/components/base/core/confirmStore.ts | L0 反馈层 | 已裁定非债 | 🟢 | 首轮 09-13 | 模块级 store 范式对齐 | 非债 |
| src/components/base/core/toastStore.ts | L0 反馈层 | 已裁定非债 | 🟢 | 首轮 09-13 | 同上 | 非债 |
| src/components/base/core/modalLayer.ts | L0 模态层 | 已裁定非债 | 🟢 | 首轮 09-13 | 常驻误登记告警 + coversViewport 判据 | 非债 |
| src/components/base/core/idGen.ts | L0 ID 地基 | 已裁定非债 | 🟢 | 首轮 09-13 | generateId 唯一入口 | 非债 |
| src/components/base/core/uiHooks.ts | L0 UI 钩子 | 已裁定非债 | 🟢 | 首轮 09-13 | 多 hook 收口，纯函数可单测 | 非债 |
| src/components/base/core/utils.ts | L0 工具地基 | 已裁定非债 | 🟢 | 首轮 09-13 | 多纯函数统一收口 | 非债 |
| src/components/base/core/contentStore.ts | L0 存储地基 | 已裁定非债 | 🟢 | 首轮 09-13 | 区域 02 已深审（15 轮） | 非债(他区) |
| src/components/base/core/contracts.ts | L0 契约地基 | 已裁定非债 | 🟢 | 首轮 09-13 | 区域 13 已深审 | 非债(他区) |
| src/components/base/core/eventBus.ts | L0 事件总线 | 已裁定非债 | 🟢 | 首轮 09-13 | 区域 13 已深审 | 非债(他区) |

**覆盖账**：本区共 14 个文件 / 本轮审 14 个 / 未审 0 个（14 全列名：config 🔴 · degrade 🔴 · logger 🟢非债 · backendLogStream 🟢非债 · canvasSyncBus 🟢非债 · confirmStore 🟢非债 · toastStore 🟢非债 · modalLayer 🟢非债 · idGen 🟢非债 · uiHooks 🟢非债 · utils 🟢非债 · contentStore/contracts/eventBus 🟢非债(他区深审)）。无未审行，本区可称审完（2 债待还）。

---

## 七、结论与下一步

- 本区域状态：**已验证(有债待还)**（2 债：TD-18-1 `DEBUG_ASSET` 死别名+注释矛盾、TD-18-2 `degrade` 死兼容分支）。
- 待还债项已流入 `daily/架构日志/债务.md`：是（TD-18-1 / TD-18-2）。
- DATAFLOW.md：本域此前无进度节点，已**就地新增「core 横切基础设施」段**（含灯，只放灯未塞明细）。
- 其他候选未登记领域（仍待续）：base/ui 基础组件层（见 19 区）。
