# 11-跨区 · Skill 系统（docs/plan/140）落地审计（2026-09-21）

> **触发**：用户指令「对照 `docs/plan/140-skill系统-可执行最终方案-2026-09-21.md` 审计其实现是否真按方案完成；重点核架构/UI/功能落地 + 代码严谨性（含过度复杂）+ 逻辑错误 + 用户视角」。
> **判据源**：`docs/plan/140`（唯一施工入口）· `ADR-0056`（四问）· `ADR-0044 §7/§8`（门面按用例）· `ADR-0053`（幽灵预留）· 本仓「诚实优先/禁静默兜底」铁律。
> **范围**：`src/components/agent/skill/**` · 域外消费者（AgentPanel / useAgentChat / useCanvasAgentTools / SkillSettings / cloudSync）· `localTool/src/routes/skills.ts` · `db/database.ts` · `contracts.ts` · 模块 `README.md`。**只读轮**：`src/**` / `localTool/**` 一行未改。

## 一 · 探债（线索全 · 解法从简）

| ID | 位置（文件:行号 / refs） | 债的形态 | 归属（模块） | 爆炸半径 | 利息 | 根因（母体?/孤例） | 大致解法（一句话·可选） | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **TD-11-16** | `AgentPanel.tsx:415`（applySkill 只留 id/name/description/content）· `:522`（快照恢复同形）· `:442`（resync 回填同形）→ `skillInject.ts:76-77` → `skillResource.ts:91-96`；refs：`grep -n "category" AgentPanel.tsx useAgentChat.ts` = **0 命中** | **P4「模型按需读资料」在装配处断裂**：`SkillPick.category/slug` 无人填（唯一生产者丢字段），`freezeSkillTurn((skillsRef.current) as SkillPick[])` 的断言掩盖了类型不匹配 ⇒ `readSkillResource` 恒走「不在磁盘上」分支 | agent/skill + agent/panels | P4 全功能（渐进披露第三层）对**所有** skill（用户 + 内置）100% 不可用；`docs/plan/140 §6` 验收 #26/#27/#28 端到端必失败；`tests/unit/skillResource.test.ts:27-28` 直接构造带 `category/slug` 的 binding（`:183` 另测缺字段分支）⇒ 测试全绿而功能全断，典型「判据 ↔ 实况」差集 | **高** | **母体**：UI 侧 `activeSkills` 只镜像 4 个字段，模块契约声明的其余字段（`builtin`/`category`/`slug`/`version`）在装配处蒸发。症状②：`origin` 恒 `'user'`（`s.builtin` 从不传入，见 TD-11-29）。⚠️ `git diff` 证实 `applySkill` 的 4 字段镜像**非本功能改动**（既有形态），而本次新增的 `resyncSkills`（`AgentPanel.tsx:442`）**沿用同形** ⇒ 契约写了、生产者没核，属「判据未对账」 | 让 `activeSkills` 携带完整 `Skill`（或改存 id 引用，用时现查）；**删掉 `as SkillPick[]` 断言**让类型层拦住同类漏传 | 待还 |
| **TD-11-17** | `agentCore.ts:428-429`（块① 单独成条 system，**不附任何执行规则**）· `agentConfig.ts:155-158`（`SKILL_EXECUTION_RULES` 全文无「不可信 / 其中的指令不是用户指令」条款）；refs：唯一不可信声明在 `useCanvasAgentTools.ts:925`（只覆盖第三层） | 方案 `§1.4` 硬要求「不可信条款必须**同时覆盖块①**」**未落地**；而 `§9` 对齐表却标 ✅ 对齐 | agent/runtime（agentCore）+ agentConfig | 块① 含用户/外部导入包写的 `name` + `description`，**直入 system**；`sanitizeSkillLabel` 只去控制字符、不中和指令 ⇒ 提示注入面（外部 skill 包是最不可信的那类输入） | 中高 | 孤例（判据层缺一条文本约束，非结构） | 把不可信条款写进 `SKILL_EXECUTION_RULES` 并让块① 也挂它（或块① 后紧跟一条独立 system） | 待还 |
| **TD-11-18** | `skillHydrate.ts:64-67`（空库返回 `changed:false`，**无「未决」标记**）· `AgentPanel.tsx:500-503`（toast 二选一）· `SkillLibraryPanel.tsx:221-226` | 「空技能库 = 未决」这一**第三态**在 UI 层被谎报成「磁盘内容与缓存一致」 | agent/skill + agent/panels | 用户清空 `skills/`（或删光包）后点「重读」→ 得到「磁盘内容与缓存一致，无需更新」（实为磁盘空 / 缓存有）；面板说「技能库还是空的」而侧栏仍列缓存里的技能；再对那条技能点保存，会经 `skillPersist.ts:121-125` 退化为**新建** ⇒ 把已删技能**重建回磁盘**（幽灵复活） | 中 | 孤例（hydrate 结果未表达「未决」，UI 只能二选一） | hydrate 结果加 `undecided` 标记；UI 如实说明「磁盘为空 ⇒ 缓存未改（要清空请在设置页逐个删除）」 | 待还 |
| **TD-11-19** | `runtime/skillStore.ts:15-21`（深引 `skillRepository` 5 个函数）+ `:52` `:231`（re-export 2 个键）；`SkillSettings.tsx:18`（域外 import `skillStore` 的 `setSkillEnabled`）vs 门面 `index.ts:41`（`setSkillsEnabled`） | 门面唯一出口被域外绕过（`§2.3` 道1）；同一语义（写启用态）**两个 API** | agent/runtime + settings | 门面纪律只靠 grep 维持（`§2.3` 道3 未建闸）；启用态写入面分裂 ⇒ 后人接线二选一，口径易分叉 | 中 | **母体**：存储收口期留的转发层从未退役（模块 README §4 自认「待 P3 收拾」，而 P3 已落地） | skillStore 的读/运行态改走门面（或把 skillStore 并进 skill 模块）；启用态写只留门面一条 | 待还 |
| **TD-11-20** | `skill/README.md:15`（仍写 `skillTypes` 导出 `RuntimeSkill`）· `:63-64`（「已知例外（待 P3 收拾）」）· `:74`（「云同步 pull 后回写磁盘仍未做」）；refs：`skillCloudSync.ts` + `cloudSync.ts:54,879` | 模块 README 三处与实现漂移，其中两处**同文件自相矛盾**（`:71` 已写「已按 ADR-0053 删除」、`:68` 已写「旧写路径已删除」） | agent/skill | README 是「后续 AI 就地读懂」的唯一入口（`§2.3` 守卫第 2 道）⇒ 漂移直接误导后来者按错误事实动手 | 中 | 孤例（文档未随 P3/P4 收尾回改） | 三处就地回改：删 `RuntimeSkill` 表述 · 消「已知例外」 · 云同步回写改标「已做（`applyCloudSkillsToDisk`）」 | 待还 |
| **TD-11-21** | 判据：`docs/plan/140 §7` 表（references 引用断裂 → 「**P4 落地时**静态扫正文反引号引用，缺失即 hydrate 报警」）；实况：`skillHydrate.ts` 全文件**无** `extractResourcePaths` 调用 | 方案 §7 承诺的偿还动作**未落地**（而 P4 已标 ✅ 落地） | agent/skill | 正文引用 `references/x.md` 但文件不在包内时，**装载期静默通过**，只在模型运行期调 `skill_read_file` 才暴露（`skillResource.ts:101`）⇒ 可提前发现的错被推给运行期 | 中 | 孤例（P4 只做了读侧准入，未做装载期对账） | hydrate 逐包对账「正文引用 vs 包内实有」，差集写进 `failures` / 面板 notice | 待还 |
| **TD-11-22** | `SkillSettings.tsx:923-928`（`onClick={() => showToast('已切换到该技能')}`，零状态变更） | **假动作**：按钮声称已切换，实际什么都没发生 | settings | 用户点「去使用」以为已切到该技能；真正的选中态在 AgentPanel 的 Skill 按钮（两处语义不同），用户无从察觉 | 中 | 孤例 | 要么真接切换（写 `activeSkills` / 跳转），要么删掉该按钮 | 待还 |
| **TD-11-23** | `SkillSettings.tsx:377`（`category: form.category.trim() \|\| undefined`）→ `skillPersist.ts:97-102,131`（`undefined` ⇒ `wantGroup=''` ⇒ `targetCategory` 回落旧 `category` ⇒ 不移动） | **逻辑错误**：清空「分组」输入框**不能**把技能移回未分组 | settings + agent/skill | 已在命名组里的技能无法经 UI 回到未分组（只能删了重建或去 Finder）；与同页 placeholder「留空 = 未分组」及说明「改名即把技能移到该分组」**直接矛盾** | 中 | 孤例（`undefined` 同时表示「未提供」与「清空」，两种语义被折叠成一个值） | 传显式空串（或加 `categoryChanged` 标记），让 `''` 明确表示「落未分组」 | 待还 |
| **TD-11-24** | `skillCloudSync.ts:84-85`（`if (!id \|\| !name \|\| !content) continue;`） | 形状不全条目**静默跳过**：既不进 `failed`、也不进 `protected`，且无日志 | agent/skill | 云端拉回 N 条、实际写回 M 条，**差额不可见**（用户以为已全部收敛）；与同文件 `protected` / `failed` 的留痕纪律自相矛盾 | 低 | 孤例（写侧留痕纪律漏了一个 `continue`） | 把跳过项收进 `failed`（或新增 `skipped`）并 `logger.warn` | 待还 |
| **TD-11-25** | 前端 `skillCatalog.ts:87-99`（slugify 把 `*?"<>\|` 换成空格）vs 后端 `routes/skills.ts:66-75`（`isSafeSegment` **放行** `*?"<>\|`）；`routes/skills.ts:369`（`execSync(\`${cmd} "${dir}"\`)` 模板串拼 shell）；refs：同款 `execSync` 已在 `files.ts:688,720` 两处 | 目录名合法性**两份判据** + 命令注入面 | agent/skill + localTool 后端 | ① Finder 建的含 `"` / `*` / `?` 的组，前端寻址时被改写成另一个名字 ⇒ 落成**新目录**（组分裂，用户看到两个组）② 分段校验不拒 `"` `$` 反引号 `;` ⇒ 目录名带引号可注入命令（**利用需能直调本地 API 或手工建含引号目录**；导入链路经 slugify 已剥引号）⇒ 低息 | 低 | 母体：目录名合法字符集**无单一真源**（前端 slugify / 后端 isSafeSegment 各一套）；`execSync` 拼串属**复发**（files.ts 同款） | 后端分段校验对齐前端口径（或反向收口成一份判据）；`execSync` 改 `execFile(cmd, [dir])` | 待还 |
| **TD-11-26** | `skillCatalog.ts:34-41`（`limit <= 0 ⇒ 原样返回` = 「不限」）；调用点被迫补判 `skillCatalog.ts:209-213`（含 5 行说明）+ README §7 专门声明 | 反直觉契约：`0` 表示「不限」而非「不注入」 | agent/skill | 已在 P1 收尾片**咬过一次**（预算耗尽反而整段注入，靠调用点加判补救）；契约留着 ⇒ 下个调用点还会咬 | 低 | 孤例 | 改 `limit <= 0 ⇒ 返回空`；「不限」改显式常量（如 `Infinity`）传 | 待还 |
| **TD-11-27** | `SkillSettings.tsx:53`（`MAX_IMPORT_FILE_BYTES`）vs `localTool/src/routes/skills.ts:41`（`MAX_FILE_BYTES`），同为 `2*1024*1024` | **SSOT 第二份**：同一数值两处定义（注释自辩「两种用途、不是重复实现」，但**数值**是同一事实） | settings + localTool 后端 | 后端放宽上限后，前端仍静默跳过（提示「跳过 N 个超大文件」，而其后端本可接受）⇒ 两侧行为不可预测地分叉 | 低 | 孤例 | 数值收口（后端回传上限 / 落契约常量），或加一条「两边必须同改」的机器对账 | 待还 |
| **TD-11-28** | `SkillSettings.tsx:568-569`（`skillMarkdownForExport(id)` 返回 `null` 时回落 `target.content`） | 导出**静默降级**为「仅正文」，把 P1 已修的「导出丢声明」缺陷悄悄放回 | settings | 缓存损坏 / 条目缺失时导出的 `.md` 丢 `name`/`description`/`version`，而 toast 只说「已导出」 | 低 | 孤例（回落无痕） | 回落时如实提示「仅导出正文（未能读到完整条目）」 | 待还 |
| **TD-11-29** | `skillTypes.ts:73`（声明）· `skillInject.ts:75`（写入）；refs：skill 语境 `.origin` **读点 0** | `SkillBinding.origin` **只写不读**（零消费者字段） | agent/skill | 即便修好 TD-11-16 的装配，该字段仍无消费者；当前恒 `'user'`（`s.builtin` 恒 `undefined` ⇒ 恒假分支） | 低 | 孤例（ADR-0053 口径的「零消费者字段」） | 与第一个真实消费者同批保留，否则删 | 待还 |
| **TD-11-30** | `SkillSettings.tsx:255-269`（`groups` 从**缓存成员**派生）vs `docs/plan/140 §1.6`（组清单 = 官方 ∪ **磁盘分类目录**） | 组清单取材偏离方案：**空分组在侧栏不可见** | settings | 新建空组后侧栏看不见（只靠一条 toast 说明），用户无法把技能「移进」该组（只能手输名字）；磁盘真值只在 `SkillLibraryPanel` 里可见 | 低 | 孤例（实现选了「无第二真相源」的派生法，但取材面比方案窄） | 侧栏组清单 = 磁盘 `groups` ∪ 缓存派生组（空组显式标「空」） | 待还 |

## 二 · 已裁定非债（留痕，防反复重审）

| ID | 判据点 | 位置 | 裁定 | 证据 / 理由 |
| --- | --- | --- | --- | --- |
| **TD-11-31** | 「空技能库 = 未决，不写空」闸门**本身** | `skillHydrate.ts:64-67` · `:120-123` | **已裁定非债** | 有意设计（防「失败伪装成空」）。后端不可达已由 `!r.ok` **单独早退**；`next.length > 0` 闸门只挡「磁盘有包但全不可纳入」（全缺 id / 全读失败）⇒ 判据与注释**一致**。UI 呈现问题另计（= TD-11-18） |
| **TD-11-32** | 门面未导出 `openSkillFolder`（方案 `§2.2` 用例② 列了它） | `index.ts:35` 附近 · `skillApi.ts:110` | **已裁定非债** | ADR-0053「零消费者不导出」；`SkillLibraryPanel` 在**模块内**消费，域外无需求（`check:dead-code` 无新增）⇒ 属方案文档滞后，与 TD-11-20 同源 |
| — | 块② 结束标记带「（N 项）」，与方案 `§1.4` 样例字面不同 | `skillCatalog.ts:223-226` | 已裁定非债 | 方案样例**自身首尾不一致**；实现首尾一致且把计数交给文本，语义等价且更利于模型判断 |
| — | `splitFrontmatter` 为导出符号 | `skillManifest.ts:50` | 已裁定非债 | 模块内（`parseSkillMarkdown`）+ 单测消费，无新增死代码 |

## 三 · 覆盖度表

| 文件 / 判据点 | 层 | 覆盖状态 | 灯 | 证据（refs / 行号） | 债 / 裁定 |
| --- | --- | --- | --- | --- | --- |
| `src/components/agent/skill/index.ts`（门面） | 上层 | 深审 | 🔴 | `refs` ① 段 6 处**全为域外消费者** | 用例面符合 §2.2（TD-11-19 是域外绕过门面，非门面自身） |
| `skillTypes.ts` · `skillManifest.ts` | 地基 | 深审 | 🔴 | `:73` `:106-134` `:145-164` | TD-11-29（origin 只写不读） |
| `skillCatalog.ts` | 地基 | 深审 | 🔴 | `:34-41` `:87-99` `:150-169` `:189-239` | TD-11-25 · TD-11-26 |
| `skillRepository.ts` | 地基 | 深审 | 🟢 | 四键唯一读写口（`grep KEY_AGENT_SKILL src` 只余 contracts + 本文件） | 无债（写后绕缓存回读比对，口径正确） |
| `skillApi.ts` | 中间 | 深审 | 🟢 | 6 端点 · `retries:0` 于写侧 | 无债 |
| `skillHydrate.ts` | 中间 | 深审 | 🔴 | `:64-67` `:120-141` `:150-190` `:209-239` | TD-11-18 · TD-11-21 |
| `skillMigration.ts` · `skillImport.ts` | 中间 | 深审 | 🟢 | 幂等 / 旧 id 进 aliases / 按「直接含 SKILL.md」切包 | 无债（`root === ''` 边界见 §四 观察） |
| `skillPersist.ts` | 中间 | 深审 | 🔴 | `:88-203` `:209-234` `:241-256` `:263-280` | TD-11-23（与 UI 折叠值合谋）· TD-11-28 |
| `skillInject.ts` | 中间 | 深审 | 🔴 | `:62-87` | TD-11-16（装配）· TD-11-29 |
| `skillResource.ts` | 中间 | 深审 | 🔴 | `:56-111` | TD-11-16（恒失败点 `:91-96`） |
| `skillCloudSync.ts` | 中间 | 深审 | 🔴 | `:84-85` `:90-107` | TD-11-24 |
| `SkillLibraryPanel.tsx` | 上层 | 深审 | 🟡 | `:84-110` `:113-132` `:221-226` | 随 TD-11-18（空库呈现） |
| `SkillSettings.tsx`（域外） | 上层 | 深审 | 🔴 | `:53` `:255-269` `:290-296` `:358-396` `:447-558` `:561-573` `:923-928` | TD-11-22 · TD-11-23 · TD-11-27 · TD-11-28 · TD-11-30 · TD-11-19 |
| `AgentPanel.tsx`（域外） | 上层 | 深审 | 🔴 | `:384-459` `:465-518` `:1528-1545` | TD-11-16（母体）· TD-11-18 |
| `useAgentChat.ts`（域外） | 中间 | 深审 | 🟢 | `:652-660` `:748-759` `:880-882`（冻结 → 全工具轮次共用 `turn.docs` → `finally` 清绑定） | 无债（P2 冻结链路正确） |
| `useCanvasAgentTools.ts`（域外） | 上层 | 深审 | 🟡 | `:922-951`（工具定义）· `:1773`（只读组末位，共 21 工具，不在 `MUTATING_TOOLS`） | 无债（薄适配正确）；不可信声明只在此处（= TD-11-17 的另一面） |
| `base/store/cloudSync.ts`（域外） | 中间 | 深审 | 🟢 | `:54` `:790-791` `:866-888`（拉取前采漂移 → `protectIds` → 写回磁盘） | 无债（D6 已接线，含「本地未回灌改动不覆盖」） |
| `agentCore.ts`（域外） | 中间 | 深审 | 🔴 | `:397-435`（第 10 参 = 块①，第 4 参改预拼串） | TD-11-17 |
| `agentConfig.ts`（域外） | 中间 | 深审 | 🔴 | `:153-158` | TD-11-17 |
| `runtime/skillStore.ts`（域外） | 中间 | 深审 | 🔴 | `:15-21` `:52` `:202-208` `:231` | TD-11-19 |
| `localTool/src/routes/skills.ts` | 后端 | 深审 | 🔴 | 6 端点 · `:66-75` `:78-91` `:142-201` `:343-377` | TD-11-25 |
| `localTool/src/db/database.ts` | 后端 | 深审 | 🟢 | `:38-40` `getSkillsDir()`（与 uploads 并列兄弟目录，§1.1.1 落地） | 无债 |
| `contracts.ts` | 地基 | 深审 | 🟡 | `:310-313`（4 键）· `:410-441`（`store` 指真实写入者）· `:1229-1278`（**6** 条端点） | 无债（`:1230` 注释写「5 条」= 低息文案漂移，已并入 TD-11-20 同源） |
| `localTool/src/router.ts` | 后端 | 深审 | 🟢 | `:191-202`（6 路；动态段在前的顺序有注释说明） | 无债 |
| `skill/README.md` | 元层 | 深审 | 🔴 | `:15` `:63-64` `:74` | TD-11-20 |
| `docs/plan/140`（判据源） | 元层 | 深审 | 🟡 | `§1.4` `§1.6` `§2.2` `§3.4` `§4` `§6` `§7` `§9` | 与实现差集：`§1.4`（TD-11-17）· `§7`（TD-11-21）· `§2.2`（非债）· `§1.6`（TD-11-30） |
| `src/**` 域外深引扫描 | — | 深审 | 🟢 | `grep -rn "agent/skill/" src` ⇒ 非 import 命中 0（仅 `contracts.ts` 的 `store` 字符串 4 处 + `useAgentChat.ts:48` 注释） | 门面纪律成立（唯一绕过点 = TD-11-19 的 skillStore） |

**覆盖账**：一轮审 **26** 个点位 / 审 **26** / 未审 **0**（P0–P4 五期施工单 + §6 验收 28 条逐条对账完毕）；二轮换判据面复核 6 个已变文件（§5.3）+ 新纳入 4 个点位（`skillManifest` 幂等面 · `skillImport` 准入面 · `.trash` 生命周期 · 二进制判据）⇒ **累计审 30 点位 / 未审 0**。

## 四 · 观察（不立债，仅备查）

- `skillImport.ts:104-106`：`root === ''`（无 `webkitRelativePath` 时回落 `f.name`）会让该"包"吞掉其余包的文件。当前路径下 `webkitdirectory` 必带相对路径 ⇒ 不可达，只登记为边界观察。
- `localTool/test/skills.test.js` 与 `tests/unit/skill*.test.ts`（9 个文件）覆盖了**单元**边界；TD-11-16 属**装配**层，任何单测都测不到（除非有「面板 → freeze → read」的集成用例）⇒ 建议 TD-11-16 的偿还凭证必须含一条端到端（或面板级）用例。

## 五 · 深审二轮（2026-09-21 23:11）

> ⚠️ **事实基线已漂移（必须先说）**：本轮二轮取证时发现**另一个会话正在同一模块上施工** —— mtime 从 22:09 扫到 23:09（按文件顺序逐件写：`skillMigration` 22:09 → `skillManifest` 22:11 → … → `README` 23:01 → `skillCatalog`/`skillApi`/`index` 23:08 → `SkillSettings` 23:08:50 → `AgentPanel` 23:09:08 → `tests/unit/skillCatalog.test.ts` 23:09:54）。**一轮的 6 个文件读数因此过期**，故二轮第一件事是**逐条复核**一轮结论（下表），再往下挖新面。

### 5.1 一轮债复核（对 23:11 快照）

| 债 | 复核 | 依据（当前行号） |
| --- | --- | --- |
| TD-11-16 | ✅ **仍成立** | `AgentPanel.tsx:397-399`（类型仍 4 字段）· `:419-425`（`applySkill` 仍只镜像 4 字段） |
| TD-11-17 | ✅ 仍成立 | 两个文件 mtime（`agentCore` 22:14 / `agentConfig` 19:46）早于读数，未被改 |
| TD-11-18 | ✅ 仍成立 | `AgentPanel.tsx:511` 仍是 `changed ? … : '磁盘内容与缓存一致，无需更新'` 二选一 |
| TD-11-19 | ✅ 仍成立 | `skillStore.ts` mtime 22:37 早于读数；深引仍在 |
| TD-11-20 | ⚠️ **部分修复** | 23:01 改动把「云同步回写仍未做」改正确（新增 `skillCloudSync.ts` 行）；但 `:15` 仍写 `RuntimeSkill`、`:73` 仍写「已知例外待 P3 收拾」，且 `:85-86` 与 `:81-83` 成了同义重复 ⇒ 账本摘要已就地更正 |
| TD-11-21 | ✅ 仍成立 | `skillHydrate.ts` mtime 22:27 早于读数 |
| TD-11-22 | ✅ 仍成立 | `SkillSettings.tsx:969-974`（「去使用」仍只 `showToast`） |
| TD-11-23 | ✅ 仍成立 | `SkillSettings.tsx:412` 仍是 `category: form.category.trim() \|\| undefined` |
| TD-11-24 | ✅ 仍成立 | `skillCloudSync.ts` mtime 22:56 早于读数 |
| TD-11-25 | ✅ 仍成立 | `skillCatalog.ts:145-157`（slugify）与后端 `skills.ts:66-75` 均未变 |
| TD-11-26 | ✅ 仍成立 | `skillCatalog.ts:39` 仍是 `limit <= 0 ⇒ 原样返回` |
| TD-11-27 | ✅ 仍成立 | `SkillSettings.tsx:59` 仍是 `2 * 1024 * 1024` |
| TD-11-28 | ✅ 仍成立 | `SkillSettings.tsx:603-604` 仍是 `full ?? target.content` |
| TD-11-29 | ✅ 仍成立 | `skillTypes.ts` 22:39 / `skillInject.ts` 22:27，读点仍为 0 |
| TD-11-30 | ✅ **已解决**（账本已 resolve） | 23:08 改动新增 `listSkillGroups` + `buildSkillGroupTree(skills, diskGroups)`（磁盘目录 ∪ 缓存成员、`_未分类` 例外）+ README 不变量 15；**残留洞另计 TD-11-40** |

### 5.2 二轮新债（换判据面，非重查）

| ID | 位置（文件:行号 / refs） | 债的形态 | 归属（模块） | 爆炸半径 | 利息 | 根因（母体?/孤例） | 大致解法（一句话·可选） | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **TD-11-33** | 判据：`docs/plan/140 §1.2` 字段表（`description` **必填 ✅**）；实况：`SkillSettings.tsx:393-403` 只校验 name / content；`skillCatalog.ts:210` 把空 description 的项滤掉 | 方案声明 `description` 必填，**实现不校验** ⇒ 空描述的技能在块① 里**静默消失** | settings + agent/skill | 用户打开「默认把全部 Skill 清单发给 AI」后，那个技能**不在清单里**，且界面没有任何提示（用户只会觉得"开关没生效"）；而块① 的取材处**只认 description** | 中 | 孤例（必填判据只在方案里，没落到校验点） | 要么保存时校验（并说明"不填描述 = 不会出现在清单里"），要么在块① 里对空描述给一句兜底文案 | 待还 |
| **TD-11-34** | `skillImport.ts:42`（`/\.(md\|markdown\|txt\|json\|ya?ml\|csv)$/i`）vs `skillCatalog.ts:44-55`（另含 `tsv`/`html`/`htm`） | **同一事实（哪些扩展名算文本资料）两份判据，取值不一致** | agent/skill | `references/page.html` 或 `data.tsv`：**读侧允许、导入侧被跳过**（提示"跳过 N 个非文本文件"）⇒ 用户被拦在一个本可用的文件外，而模型那边其实读得到 | 低 | 孤例（SSOT 第二份 + 口径错位） | 两份收口成一份（`skillImport` 直接复用 `assertSafeSkillRelativePath` 的判据） | 待还 |
| **TD-11-35** | `localTool/src/routes/skills.ts:166-170`（覆盖保存时旧包 rename 进 `.trash/`）· `:197-199`（删除时同理）；refs：`grep trash localTool/src` 无任何读取/清理点 | `.trash/` **只写不清**：每次覆盖保存 + 每次删除各留一份整包 | localTool 后端 | **每次保存技能都在 `.trash` 留一份历史包**（改 N 次 = N 份），无 GC / 无上限 / 无文档 / 无 UI 入口；用户无从知道它存在（面板只提示"`scripts/` 不会被读到"） | 低 | 孤例 | 给 `.trash` 加保留策略（按时间/条数）或至少在面板如实告知"删除/覆盖会留一份在 `.trash`" | 待还 |
| **TD-11-36** | `localTool/src/routes/skills.ts:130-138`（`const binary = buf.includes(0)`）→ `:150-163`（写回用 `Buffer.from(content, encoding)`） | 二进制判据 = 「含 NUL 字节」；**无 NUL 的二进制**会被当 utf8 读出（有损），整包写回即**静默损坏** | localTool 后端 + agent/skill | 走 `scope=package` 往返的两条路都中招：`saveSkillToDisk` 的「改这一份」与 `backfillMissingIds`。小文件（<1KB）无 NUL 概率 ≈ 2%，故**可达**；且损坏**无任何提示** | 低 | 孤例（判据不严；诚实标注：需包里真的放了无 NUL 的二进制才触发） | 判据换成「非 UTF-8 可解码即 base64」或按扩展名白名单；`scope=content` 不返回二进制已是一道，但 `package` 这条没护住 | 待还 |
| **TD-11-37** | `skillManifest.ts:147-149`（`push` 的 `v !== ''` 守卫）vs `:162`（`unknown` 无守卫）；判据声明在同文件 `:139-143`（"语义幂等 … 两者都在单测里锁死"），单测 `tests/unit/skillManifest.test.ts:91-115` **只覆盖全非空样例** | 文档声明的幂等口径**强于实现**：空值**已知**字段往返丢行（`when-to-use:` / `version:` / `id:` 由 `''` 变 `undefined`），而 `unknown` 无守卫 ⇒ **同一文件里两类字段的往返语义不一致** | agent/skill | 影响 `saveSkillToDisk` 的「整文件重建」（空值行在保存后消失）；更贵的是**判据被当成已锁死**，后续重构会依赖一个不成立的不变量 | 低 | 孤例（注释/单测与实现三者不一致） | 统一口径：要么 `unknown` 也加守卫、要么 `push` 去掉守卫；并补一条空值样例单测 | 待还 |
| **TD-11-38** | `SkillSettings.tsx:128-152`（`NumberField` 只 `min={1}`，无 `max`）· `skillRepository.ts:180-181`（`num()` 只校验 `>0` 且有限） | 预算旋钮**无上界** | settings + agent/skill | 预算层的存在意义是"防上下文被顶爆"，而它的旋钮可以调到形同关闭（`singleSkillChars = 99999999`）⇒ 一选就整段注入、直接顶爆，**无任何提示**（方案 §6 验收 #18 要防的正是这个） | 低 | 孤例（校验只挡下界不挡上界） | 给三项预算加上界（或至少超阈值时在 UI 上显式警告"已超出建议范围"） | 待还 |
| **TD-11-39** | `skillHydrate.ts:219`（`listSkillPackages({ withContent: true })`，`detectSkillDrift` 内）· 触发点 `AgentPanel.tsx:477-489`（focus / visibilitychange，5s 节流） | 漂移侦测的**判据很轻（比 5 字段签名）但取数极重（把全库每个包的 `SKILL.md` + `references/**` 正文全拉下来）** | agent/skill | 库大（或 `references` 大）时，每次切窗口都全量拉一遍正文，只为回答"改没改"；与"只读比对"的初衷（轻量）不匹配 | 低 | 孤例（缺一个"只取指纹"的读法） | 后端加 `?hashes=1`（只回 `id + 正文指纹`），或改用 `mtime`/`size` 做粗判 | 待还 |
| **TD-11-40** | `SkillSettings.tsx:240-252`（`diskGroups` 只在 `libRefresh` 变化时重读）· `SkillLibraryPanel.tsx:66-71`（面板只有入参 `refreshSignal`，**无子→父回调**） | 二轮新修的组树有**残留洞**：面板驱动的「重读」不通知父级 ⇒ 磁盘组清单不刷新 | settings + agent/skill | 方案 §1.6 明写「Finder 里建目录 + 重读**同样可行**」；但 Finder 里建**空**目录后点面板「重读」，侧栏**看不到**该组（要再做一次写操作才出现）。有包的新目录不受影响（走成员派生那半边） | 低 | 孤例（新修只接了"写盘动作"这条触发，漏了"面板重读"） | `SkillLibraryPanel` 加一个 `onReloaded` 回调，父级收到即 `refreshDiskGroups()` | 待还 |

### 5.3 二轮增量覆盖（只记状态变化行）

| 文件 | 变化（mtime） | 复核结论 |
| --- | --- | --- |
| `src/components/agent/skill/skillCatalog.ts` | 23:08:09 | 新增 `buildSkillGroupTree` / `skillGroupLabel` / `compareSkillGroups`（组树真源）；**`TEXT_EXT` 与 `truncateSkillContent` 未变** ⇒ TD-11-25/26 仍成立；TD-11-30 由此修复 |
| `src/components/agent/skill/skillApi.ts` · `index.ts` | 23:08:10 / 23:08:19 | 新增 `listSkillGroups` 并入门面用例②（与 TD-11-32 的非债裁定一致：**有真实消费者才导出**） |
| `src/components/settings/sections/SkillSettings.tsx` | 23:08:50 | 新增 `diskGroups` + `contentSubscribe(SKILLS_KEY)`（修掉"面板重读后侧栏不刷新"）；TD-11-22/23/27/28 **仍在**；TD-11-30 修复、TD-11-40 残留 |
| `src/components/agent/panels/AgentPanel.tsx` | 23:09:08 | 改动未触及 `applySkill`（`:419-425`）与重载 toast（`:511`）⇒ TD-11-16/18 **仍成立** |
| `src/components/agent/skill/README.md` | 23:01:32 | 新增 `skillCloudSync.ts` 行与不变量 13/14/15；**`:15` `RuntimeSkill` 与 `:73` 已知例外仍在**，并新增 `:85-86` 重复段 ⇒ TD-11-20 部分修复 |
| `tests/unit/skillCatalog.test.ts` | 23:09:54 | 并行会话正在补组树单测（本轮**未跑**，只记事实） |

## 六 · 收尾自检（对 5 步法 §五 清单）

| # | 自检项 | 结论 |
| --- | --- | --- |
| 1 | `src/**` / `localTool/**` 一行都没改？ | ✅ 本轮只写 3 个文件（本文件 · `债务.md` · `index.md`）；**唯一例外** = 按用户指令就地修正 `.codebuddy/commands/债务登记5步法.md` §4.2（SOP §零.3 例外②「当场修工具债」允许） |
| 2 | 命中的工具债当场修了？ | ✅ 一条（SOP §4.2 字段约束缺 `≤120 字`/三档利息）当场补齐，未留成待还债 |
| 3 | 结论只写在轮次文件？§七 有状态行？ | ✅ 探债/复核/覆盖全在本文件；§七 首行即「本区域状态」 |
| 4 | 跨区发现只开了一份文件？ | ✅ 后端（08）/ 云同步（10）/ 设置（上层）的问题全部挂在本份，未另开 |
| 5 | 从类型 / 契约地基起手？ | ✅ 起点 = `skillTypes.ts` / `skillManifest.ts` / `skillCatalog.ts`，再沿 fan-in 向上 |
| 6 | 覆盖账报全？ | ✅ 一轮 26 + 二轮新纳 4 = **30 点位 / 未审 0**（§三 + §5.3） |
| 7 | 每笔债 / 每个非债都挂了证据？ | ✅ 全表带 `文件:行号`；非债 4 条各有判据（§二） |
| 8 | 判定了孤例还是母体？ | ✅ 母体 2 个（TD-11-16 装配丢字段 · TD-11-19 转发层未退役），其余标孤例，未编造 |
| 9 | 标了 结构债 / 业务债？ | ✅ **全部 24 行均为结构债**（无需用户拍板选项）—— 本模块未出现改对外契约/改业务流程的债 |
| 10 | 没把假习惯当红线？没重审深审件？ | ✅ 红线均回到 `CLAUDE.md`/`ADR` 取值；二轮只对**已被改过**的 6 个文件做「回查」，未重查未变件 |
| 11 | 全程没跑任何测试？ | ✅ **测试执行次数 = 0**（用户明确禁止；且通过测试不构成验收门槛）。另：**静态闸也一并停跑**（`check:*` 只作过一轮旁证，结论不依赖它） |
| 12 | 每个捕获点 / 默认值点都问了"失败有留痕吗"？ | ✅ 查出 3 处无痕：`skillCloudSync.ts:84`（静默跳过，TD-11-24）· `SkillSettings.tsx:603`（导出降级，TD-11-28）· `skills.ts:132`（二进制误判致静默损坏，TD-11-36） |

## 七 · 结论与下一步

- **总判定：方案 P0–P3 基本按方案落地（后端 6 端点 · 磁盘真相源 · 冻结 · 漂移侦测 · 整目录导入 · 预算配额 · 写路径收口均经取证为真）；P4 的 `skill_read_file` 属「代码全对、链路全断」——功能等于零落地**（TD-11-16）。架构形态（模块落点 / 门面按用例 / 唯一读写口 / 域外零深引）**符合方案描述**，缺陷集中在**装配层与文档层**。
- **二轮补充判定**：深审换判据面后又出 8 笔（TD-11-33 ~ TD-11-40），其中 **TD-11-33（description 必填未校验 ⇒ 技能在块① 静默消失）** 与 **TD-11-39（漂移侦测拿全量正文比签名）** 最值得先看；TD-11-36（无 NUL 二进制被当文本往返）是**潜在静默数据损坏**，但需特定输入才触发（已如实标注前提）。
- **⚠️ 审计基线在动**：二轮取证时确认**另一会话正按文件顺序施工同一模块**（22:09 → 23:09+，含单测）。⇒ 一轮的 `TD-11-30` 在复核时**已被其修复**（账本已 `resolve` 并留凭证）、`TD-11-20` 部分修复（摘要已就地更正）；**本文件全部结论以 2026-09-21 23:11 快照为准**，后续若该模块再被改动，相关行需标「回查」重核。
- **本区域状态**：**已验证(有债待还)**（累计新登记待还 **22** 笔：TD-11-16 ~ TD-11-29 · TD-11-33 ~ TD-11-40；非债裁定 2 条入账 TD-11-31 / TD-11-32，2 条仅本文件留痕；TD-11-30 已解决）。
- **优先处置序（供偿还者）**：TD-11-16（高，一行装配 + 去断言）→ TD-11-17（安全判据缺条）→ TD-11-33（用户可见的功能缺失）→ TD-11-18/19/20/21（诚实与纪律）→ 其余低息随轮次清。
- **下一步**：交「架构师改码7步法」按上述顺序施工；本轮为**只读审计轮**，`src/**` / `localTool/**` 一行未改（唯一例外 = 按用户指令就地修正 `.codebuddy/commands/债务登记5步法.md` 的 §4.2 字段约束，属 SOP §零.3 例外② 允许的「当场修工具债」）。
