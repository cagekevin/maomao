# 13-跨区 · 底层 SSOT 横向推导 · 2026-09-16

> **触发**：用户「根据现有债务横向推导还有没有其他的债务？从底层开始找」。
> **方法**：方向铁律自底向上（L0 类型/契约 → L1 utils/core → L2 store），把现有债的三大母体投射到未专项深审的底层文件，找同类未登记债。
> **结论**：底层 L0/L1/L2 对三大母体**已全部收口，本轮未新登记债**。指纹扫描命中的"手抄不变量"候选经逐条读实代码，全部为已修复留痕 / 用户裁定有意设计 / 已不存在 → 一律**已裁定非债**（留痕防重审）。

---

## 一 · 母体来源（现有债 → 待投射母体）

| 母体（来源债） | 形态 | 投射目标层 |
| --- | --- | --- |
| SSOT 第二份 / 同真相两份 | TD-02-23 · TD-13-1 · TD-15-1/2 · TD-17-4 · TD-04-28 · TD-12-8 · TD-22-28/30/35/36/39 | L0 契约登记表 / L1 常量 / L2 store 写点 |
| 静默吞错 / 假成功 / 恒返 ok:true | TD-15-3 · TD-12-10 · TD-22-33/37/38/43 · TD-02 成本层 | L0/L1 utils/core 结果返回 |
| 幽灵消费方预留 / 死代码 / 死参 | TD-13-2 · TD-16-1 · TD-18-1/2 · TD-17-5 · 20 跨区清偿轮 | L0/L1 死导入 / 死参 / 冗余清单 |

## 二 · 链路快照（底层结构，refs 实证）

```
L0 契约地基：  base/core/contracts.ts（EVENTS/STORAGE_KEYS/GEN_ERRORS/NODE_TYPES/
              NODE_HANDLE_CONTRACT/API_ENDPOINTS/apiRegistry 七表）· agentKeys.ts
L0 类型地基：  src/types/{errors,asset,provider,refToken}.ts（+ index.ts barrel）
L1 utils：     base/utils/{asyncGuard,assetUrl,...}（region 16）
L1 观测/core： base/core/{degrade,logger,canvasSyncBus,eventBus}.ts（region 18）
L2 store：     base/store/{nodeRuntimeStore,cloudSync,projectStore}.ts
```

## 三 · 探债（候选 → 取证 → 裁定）

### 候选 A：EVENTS 第二套广播（母体 1）
- 实证：`grep publish/subscribe('` 全仓 23 命中，全部命中 `contracts.EVENTS` 已登记名；`window.dispatchEvent` 仅剩注释（playback-manager.ts:196-204）。
- 裁定：**已收口（TD-22-23）**，非债。

### 候选 B：STORAGE_KEYS / 常量第二字面量（母体 1）
- 实证：裸 `localStorage.*`/`contentSet('…')`/`contentGet('…')` 全仓仅 1 处（projectStore:542 注释提醒"勿再读 contentGet('projects')"）。
- 常量登记表逐一核验：
  - `SHOT_HANDLE_PREFIX`（contracts:599）↔ useConnectedInputs 仅注释引用 → 单源 ✅
  - `VIDEO_EDITOR_*_PREFIX`（contracts:204-206）↔ videoEditorKeys.ts:11-28 import → 单源 ✅
  - `CANVAS_STATE_PREFIX`（contracts:166）↔ kvStore/api 全 import → 单源 ✅
  - `CREDIT_SWITCH_KEY`（contracts:38）↔ useCanvasAgentTools:46/91 import → 单源 ✅
  - `AGENT_KEY_PREFIX`（agentKeys:17，TD-15-1 收口）单源 ✅
  - `CANVAS_SYNC_CHANNEL`（canvasSyncBus:18）单源 ✅
- 裁定：**已收口**，非债。

### 候选 C：GEN_ERRORS 绕过（母体 1/2）
- 实证：`classifyError` 为唯一入口（genErrors.ts:24），全仓消费方均 import；无裸 `if(/网络错误/)`（仅注释）✅。
- 裁定：**已收口**，非债。

### 候选 D：静默吞错 / 假成功（母体 2）
- 实证：`base/` 内 `.catch(()=>` 仅 2 处——upstreamLink:45（注释记已修，失败可见）+ logger:76（`catch-ok: RECURSION_GUARD` 故意递归守卫，合法）。`base/` 的 `ok:true/false` 返回全为诚实用判别联合（PersistOutcome / NodeGenerationRunResult 等），无恒返 ok:true。
- 裁定：**已收口**，非债（logger 守卫为有意设计）。

### 候选 E：cloudSync SYNC_EXCLUDE 双真相（母体 1/3）
- 实证：SYNC_EXCLUDE（cloudSync.ts:907-930）是 `getLocalKeys()` 之外的**显式 deny-list**（隐私双保险）。其中 `agent_draft`/`agent_conversations`/`agent_active_conversation_id` 为"已退役/已迁移"键的冗余排除——但 `getLocalKeys()` 已用 `!v.migration` 过滤（contracts:507），这些键本就不在同步集；保留系"旧机残键双保险"的刻意隐私防御（注释已固化，[TD-14]）。
- 裁定：**已裁定非债**（有意设计 + 注释固化，防后续 AI 当 bug 补）。

### 候选 F：uploadDirs 两端必须一致（母体 1）
- 实证：UPLOAD_DIRS（前端）↔ localTool `UPLOAD_ROOT_ALLOW`（后端）须同步；原闸 `check-upload-dirs.mjs` 已**按用户裁定 2026-09-15 主动删除**（实测常绿、性价比低），改为两处注释提醒（uploadDirs.ts:17-20）。
- 裁定：**已裁定非债**（用户裁定有意设计）。

### 候选 G：指纹扫描"手抄不变量"（母体 1）—— 全为已修复留痕
对 `保持一致|必须同时|手抄|三写` 全仓扫描 29 命中，逐条读实代码：
- `director3d/rig.ts:279-296` + `project.ts:1988-1990`：注释描述"models.tsx 与 project.ts 各手抄 bodyHeight 表"——**已收口**：rig.ts 现唯一定义 `MIXAMO_BODY_SCALES`（:290），project.ts 经 `bodyScaleHeight` 引用（:5/:1990）；`models.tsx` 无第二份（全仓 grep 仅 rig.ts + project.ts）。→ 非债。
- `videoEditor/.../definitions.ts:9-13`：注释明写"`args` 已删（TD-22-36）"——平行手抄副本**已删除**。→ 非债。
- `subtitle-constants.ts:7-15`：注释描述旧"8 模板手抄全字段"——**已收口（TD-22-30）**：现模板只写覆盖差、走 `buildTextElement` 单源。→ 非债。
- `canvasSnapshotSchema.ts:13`：注释提及"测试里手抄白名单副本"——`*.test.ts` grep `NODE_KEEP/EDGE_KEEP` **0 命中**，副本已随收口清除。→ 非债。

## 四 · 覆盖度表（本轮底层推导）

| 文件 | 层 | 覆盖状态 | 灯 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- |
| `src/types/*` | L0 类型 | 深审 | 🟢 | index.ts barrel 干净；errors.ts 与 GEN_ERRORS 对齐 | 非债 |
| `base/core/contracts.ts` | L0 契约 | 深审 | 🟢 | 七登记表 SSOT；SHOT/VIDEO/CANVAS/CREDIT/AGENT 常量全 import 无第二字面量 | 非债 |
| `base/core/agentKeys.ts` | L0 契约 | 深审 | 🟢 | TD-15-1 收口，AGENT_KEY_PREFIX 单源 | 非债 |
| `base/utils/asyncGuard.ts` | L1 utils | 深审 | 🟢 | 静默释放/解析/非阻塞原语 SSOT（TD-02-26 收口） | 非债 |
| `base/core/degrade.ts` | L1 观测 | 深审 | 🟢 | reportDegrade SSOT，2 消费方 | 非债 |
| `base/core/logger.ts` | L1 观测 | 深审 | 🟢 | 仅 catch-ok RECURSION_GUARD（故意） | 非债 |
| `base/store/nodeRuntimeStore.ts` | L2 store | 抽审→深审 | 🟢 | 瞬态 SSOT，无吞错路径 | 非债 |
| `base/core/canvasSyncBus.ts` | L2 core | 抽审→深审 | 🟢 | CANVAS_SYNC_CHANNEL 单字面量；广播失败 logger.warn 可见 | 非债 |
| `base/store/cloudSync.ts`(SYNC_EXCLUDE) | L2 store | 深审 | 🟢 | deny-list 有意设计（隐私双保险） | 已裁定非债 |
| `base/utils/uploadDirs.ts` | L1 utils | 深审 | 🟢 | 同步闸用户裁定删除，注释提醒 | 已裁定非债 |
| `director3d/rig.ts`+`project.ts`(bodyHeight) | L2 | 深审 | 🟢 | MIXAMO_BODY_SCALES 单源，project 引用 | 非债 |
| `videoEditor/.../definitions.ts`(args) | L3 | 深审 | 🟢 | TD-22-36 已删 | 非债 |
| `videoEditor/constants/subtitle-constants.ts` | L3 | 深审 | 🟢 | TD-22-30 已收口 | 非债 |
| `canvasSnapshotSchema.ts`(测试副本) | L2 | 深审 | 🟢 | *.test.ts 0 命中 | 非债 |

## 五 · 横向推导总账

- **底层（L0/L1/L2）对三大母体：零新债**。现有债的母体在底层已被前序审计（区域 02/13/16/17/18 + 20 跨区清偿轮）收口。
- **指纹扫描命中 = 全部已修复留痕或用户裁定有意设计**，无真债遗漏。
- **全仓横向推导的唯一新债仍为上一轮 TD-04-28**（三写不变量三处手抄，位于 L3 `useArrangeCanvas`/`uiHooks`/`groupNodes`）——母体同属 SSOT 第二份，但发生在**上层 hooks/写点**，不在底层。
- 覆盖账：本轮审 14 个底层/L2-L3 文件，全判非债（0 新债）。无未审项（底层区域 13/16/18 此前已深审，本轮为复探留痕）。
