# 16-跨区·静默吞母体横向推导复查 · 2026-09-16

> 触发：用户「债务登记 5 步法 · 横向推导：根据现有债，从底层开始找有没有其他债务」。
> 起点：区域 16（utils 工具层 / 原语层，最底层纯函数与契约基础设施）。
> 跨区只开一份（@见各根因区）。本轮是 2026-09-16 第二轮的底层横向推导（首轮见 `02-跨区-母体横向推导复查-2026-09-16.md`，聚焦存储键 / 事件表 / 第二套广播 / 失败可见性 / 绕过入口）。

## 一 · 链路快照（自下而上）

```
base/utils（纯函数原语层）
  ├─ asyncGuard.ts        → releaseQuietly / releaseQuietlyAsync（RELEASE_FAIL 唯一真源）
  ├─ clipboard.ts / imageCompress.ts / faceMosaic.ts / captureFrame.ts（catch-ok 豁免点）
base/core（横切基础设施）
  ├─ catchOk.ts           → CATCH_OK 登记表（SSOT，TD-02-26 收口）
  ├─ videoEditorKeys.ts / agentKeys.ts（键值构造器，从 contracts.ts import 前缀）
director3d/storage.ts    → 工程键委托 d3dPersistence.writeProject（KV 收口）；非工程键裸 localStorage
videoEditor/hooks-cutia/storage/use-local-storage.ts（captions 三键裸写）
```

## 二 · 溯源（母体）

既有债里反复出现的 4 大母体（首轮已收口实证）：
- M-SSOT 第二份（双写 / 双源 / 手抄形状）
- M-静默吞（catch 空吞 / 失败伪装成功 / 假豁免）
- M-幽灵预留 / 假护栏（恒真守卫 / 空白名单 / 死字段）
- M-绕过唯一入口（裸写底层 / 双落盘）

本轮自底层再扫这些母体的**遗漏实例**，重点：成本层收口后的回潮信号 + 盲区裸写。

## 三 · 定海（唯一真源 / 契约）

- `catchOk.ts` 头注释明写："再出现手写 RELEASE_FAIL 豁免标记 = 回潮信号，应改调 `releaseQuietly` 原语，别复制粘贴"（L52-56 附近）。
- `releaseQuietly` / `releaseQuietlyAsync` 在 `asyncGuard.ts:164/179` 注释标明为 RELEASE_FAIL 的**唯一实现**。

## 四 · 切割（实证结果）

| # | 发现 | 实证 | 判定 |
| --- | --- | --- | --- |
| A | `audio-waveform.tsx:75,103`、`use-editor-actions.ts:510`、`./audio-manager.ts:313` **手写 `catch-ok: RELEASE_FAIL`** 而非调 `releaseQuietly`；三文件均无 `releaseQuietly` import | grep `catch-ok: RELEASE_FAIL` + 逐文件 import 复核（全空） | **新债**（回潮） |
| B | `use-local-storage.ts` 仍裸写 `localStorage.setItem`（L35），`captions.tsx` 用 `editor-caption-language/-model/-template` 三键，均**不在 STORAGE_KEYS 表** | refs 确认 captions.tsx 活 import（经 `assets/index.tsx`）；grep `contracts.ts` 无这三键 | 已立债 **TD-02-33 未收口**（本次实证核实，不重立） |
| C | `videoEditorKeys.ts` / `agentKeys.ts` 是键值构造器，从 `contracts.ts` import 前缀常量 | 读全文：确 import `VIDEO_EDITOR_PROJECTS_PREFIX` 等；注释"清单 vs 构造器同源" | **非债**（有意设计，已裁定） |
| D | `director3d/storage.ts` 的 `removeKey` 只清 localStorage、KV 无删除；但唯一调用方 `project.ts:1285` 仅清 `LEGACY_PROJECT_STORAGE_KEY`（KV 无此键） | grep `removeKey` 调用方仅 1 处 legacy 键 | **非债**（设计留痕，无活实例） |
| E | `base/utils` 其余 `catch-ok` 点（clipboard/imageCompress/faceMosaic）均有 `logger.warn` 或 `catch-ok` 原语豁免，非裸吞 | 读上下文：clipboard 返回 `{ok:false,msg}`；imageCompress/faceMosaic 为浏览器 API 探测 | **非债**（TD-02-26/27 已收口） |

## 五 · 探债（明细）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-16-3 | `audio-waveform.tsx`(×2) / `use-editor-actions.ts` / `audio-manager.ts` 手写 `catch-ok: RELEASE_FAIL` 绕过 `releaseQuietly` 原语（catchOk.ts 注释明示回潮信号） | 增债 | 结构债 | 3 文件释放路径 | 低 | 母体 M-静默吞（成本层收口回潮：TD-02-26/27 建原语后未接回） | 三处改调 `releaseQuietly`/`releaseQuietlyAsync`；补 `check-silent-catch` 探针识别"手写 RELEASE_FAIL 而非调原语" | 待还（@见 16 区） |

> TD-02-33（非本轮新立）：`use-local-storage.ts` 裸写 localStorage 未登记 STORAGE_KEYS，本轮实证**仍活未收口**——`captions.tsx` 三键 `editor-caption-*` 不在 `contracts.ts` STORAGE_KEYS 表。归区域 02，状态维持待还，本次只核实不重立。

## 六 · 结论 + 覆盖账

- **新债 1 笔**（TD-16-2，归 16 区）：RELEASE_FAIL 回潮，M-静默吞母体在成本层收口后的遗漏实例。
- **已立债核实 1 笔**（TD-02-33）：仍活未收口，跨区 @见 02 区。
- **非债裁定 3 笔**（C/D/E）：videoEditorKeys/agentKeys 合法构造器；director3d removeKey 无活实例；base/utils 其余 catch-ok 已收口。

**覆盖度表（本轮增量）**：

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `base/utils/asyncGuard.ts` | L0 原语 | 已裁定非债（原语本身健康） | 🟢 | 本轮 09-16 | 读 L164/179 注释 | 非债 |
| `base/core/catchOk.ts` | L0 契约 | 深审 | 🟢 | 本轮 09-16 | 读头注释 SSOT | 非债 |
| `videoEditor/ui/editor/panels/timeline/audio-waveform.tsx` | L3 UI | 有债待还(TD-16-2) | 🔴 | 本轮 09-16 | L75/103 手写 RELEASE_FAIL | TD-16-2 |
| `videoEditor/hooks-cutia/actions/use-editor-actions.ts` | L3 hook | 有债待还(TD-16-2) | 🔴 | 本轮 09-16 | L510 手写 RELEASE_FAIL | TD-16-2 |
| `videoEditor/engine/core/managers/audio-manager.ts` | L2 引擎 | 有债待还(TD-16-2) | 🔴 | 本轮 09-16 | L313 手写 RELEASE_FAIL | TD-16-2 |
| `videoEditor/hooks-cutia/storage/use-local-storage.ts` | L2 存储封装 | 有债待还(TD-02-33) | 🔴 | 本轮 09-16（核实） | refs 0 模块引用误解（@videoEditor 别名盲区）+ captions.tsx 活 import | TD-02-33 未收口 |
| `base/core/videoEditorKeys.ts` / `agentKeys.ts` | L0 构造器 | 已裁定非债 | 🟢 | 本轮 09-16 | 读全文 import 前缀常量 | 非债 |
| `director3d/storage.ts` | L0 存储封装 | 已裁定非债 | 🟢 | 本轮 09-16 | removeKey 仅清 legacy 键 | 非债 |

**本轮审了 8 个文件（底层地基 + 盲区），未审 0（本轮聚焦清单已全核）。DATAFLOW 灯与实证一致，未漂，已对账一致。**
