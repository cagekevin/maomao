# 01-跨区-前端persist泄漏执行态列-2026-09-20

## 探债

| ID | 位置 | 债的形态 | 归属 | 爆炸半径 | 利息 | 根因 | 大致解法 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| @见 债务.md | `src/components/base/store/taskStore.ts` persist():134-142 → `localToolApi.saveTask` → POST `/api/tasks/save`；后端 `localTool/src/routes/tasks.ts`:207-221 `[upsertTask:client-blocked]` | 越权写/绕过唯一入口：前端对 backend-owned 任务（行内 `request_data._relayPoll` 句柄）仍把整份 Task 快照（含 `status`/`progress`/`errorMsg`/`resultUrl`）POST 给后端，后端逐次剥离执行态列并打 warn | 前端 base/store/taskStore | 所有 backend-owned 任务（图/视频/3D）每次生成产生 N 条告警 + 无效 payload 写 | 中 | 前端 persist 未区分"归属展示列"与"执行态列"，统一发整份快照；TD-08-38 守门生效但**源头泄漏未治** | 前端 persist 对 backend-owned 任务剥离执行态列再发（判据复用 `_relayPoll` 句柄） | 待还 |

> ▶ **2026-09-21 复检：改判非债**（`01-跨区-前端persist泄漏执行态列复检-2026-09-21.md`）。两处**描述失准**就地更正：
> ① **「打 warn」不成立** —— HEAD（`aa0121f1`「任务状态收敛」）已按 `blocksRealDamage` 把**冗余写降为 `debug`**（`[upsertTask:client-redundant]`），`warn` 只留给"晚到快照差点抹掉终态"的真竞态；后端注释明写「此形态每天上百条，占 warn 会把真信号淹掉」。
> ② **「源头可治」不成立** —— 实测前端**无法观测归属**：`src/` 对 `requestData` 的引用数 = **0**；`reportGenerate()` 本地造任务（不含句柄）后**立即** `persist`，此后 `persist` 都作用于同一对象 ⇒ 前端在 persist 时**拿不到** `_relayPoll`。
> 且后端判据是**逐行事实**（`existing && owner==='client' && hasRelayPollHandle(existing)`）：**行不存在 / 无句柄 ⇒ 不剥离**（文本·chat 链路前端本就是执行方，一刀切会造出"文本任务永远 running"的回归 —— 后端注释已明写）。⇒ **正确性已由拥有真相的那一层保证**，前端发整份快照是**结构必然**，不是缺陷。

## 覆盖度表

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- |
| `localTool/src/routes/tasks.ts` | 深审(有债待还) | 01-跨区-前端persist泄漏执行态列-2026-09-20 | :207-221 | TD-08-38 守门已解决；本次为 guard 持续命中暴露的**前端侧源头泄漏**（新债） |
| `src/components/base/store/taskStore.ts` | 深审(有债待还) | 同上 | persist():134-142 | 越权写执行态列 |

## 七、状态行

本轮审 2 文件 / 共 2 / 未审 0。
- 新债：前端 `taskStore.persist` 对 backend-owned 任务仍发执行态列 → 后端 `[upsertTask:client-blocked]` 告警（@见 债务.md）。
- TD-08-38（guard）已解决，本次为 guard 持续命中暴露的源头泄漏，非 recurrence。
- 待 §3.3 对账：查 `task_mu9mp6di_dhf736` 后端是否真写了终态（`result_url`/`status`），以判定是"缺执行方真 bug（任务卡进行中）"还是"纯前端越权写噪音"。
