# 13 跨区 · 底层 KV 键构造器收口遗漏（2026-09-16）

> 触发：用户指令「仔细看看，还有哪些应收口到底层却落在消费层／抽象层」。
> 方向：从最底层契约出发（`contracts.ts` STORAGE_KEYS + `base/core/*Keys.ts` 构造器），向上核对消费层是否仍裸拼键字面量。
> 范围：仅查 KV/存储键「键名构造器」这一条真源线（不重审已结清的裸 localStorage 直写 / SSOT 第二份其它形态）。

## 一 · 链路快照（真实现状）

```
contracts.ts STORAGE_KEYS（键名清单唯一真源）
  ├─ agent_conversations_{agentKey}            → base/core/agentKeys.ts::agentConversationsKey（构造器唯一真源）✅ 已收口
  ├─ agent_active_conversation_id_{agentKey}   → base/core/agentKeys.ts::agentActiveConversationKey            ✅ 已收口
  ├─ agent_project_memory_v1_{agentKey}        → 无构造器；projectMemoryStore.ts:58 本地裸拼 ❌ 债1
  ├─ director3d-project / director3d-project-{nodeId}  → 无 base/core 构造器；
  │      project.ts:216 PROJECT_STORAGE_KEY + d3dPersistence.ts:29 PROJECT_KEY_DEFAULT 两份裸写 ❌ 债2
  ├─ video_editor_*    → base/core/videoEditorKeys.ts（构造器唯一真源）✅ 已收口
  ├─ canvas-state-v1-* → contracts.ts CANVAS_STATE_PREFIX 常量 ✅ 已收口
  ├─ agent_chat_model / agent_history_turns / agent_skills / agent_skill_enabled / agent_split_width
  │      → contracts STORAGE_KEYS 已登记（L305/311/319/325/331/429）；但 agentModelStore/skillStore/
  │        tableWorkspaceState 仍本地裸写常量、未 import 真源 ❌ 债3（清单型第二份，见 TD-13-11/9/10）
  └─ SYNC_ALLOW 云同步白名单（cloudSync.ts:902）裸列 agent_* 键字面量（与 KEY_YIMAO_PRESET_PROMPTS 混写）❌ 债3 第三处
```

收口对照（对称先例已存在）：videoEditor / agent 两类 KV 键都已抽到 `base/core/*Keys.ts` 抽常量+构造器、消费层 `import` 引用；`agent_project_memory_v1_` 与 `director3d-project` 漏收口。第二轮深入扩到 `agent_` 域键 + 云同步白名单，发现同母体（M7 第二份）在"常量/清单"层系统性漏收口——且同批 `agent_credit_switch` 已正确收口到 `CREDIT_SWITCH_KEY`（`useCanvasAgentTools` import 真源），证明其余是"半收口漏网"而非"不知规范"。

## 二 · 溯源（谁产生→谁消费）

- `agent_project_memory_v1_*`：真源登记在 `contracts.ts:400`；唯一定义键名的消费方 `projectMemoryStore.ts:58` 手写 `memoryKey`，无 import contracts 常量、无 import agentKeys 构造器。
- `director3d-project`：真源登记在 `contracts.ts:490/506`；两处本地常量 `project.ts:216 PROJECT_STORAGE_KEY`、`d3dPersistence.ts:29 PROJECT_KEY_DEFAULT` 各自常量、各自裸字面量，互不引用。

## 三 · 定海（锚定根因区）

根因在底层契约层（区 13：contracts.ts 是 STORAGE_KEYS 唯一真源；base/core 构造器是键构造唯一真源）。消费层 `projectMemoryStore`(区 11) / `director3d/*`(区 07/09) 漏接。按跨区归属：债 ID 绑根因区 13。

## 四 · 切割（不重审边界）

本轮只处理「KV 键键名构造器漏收口」。以下已结清、不动：
- 裸 localStorage 直写（TD-02-36/38/39/42 已收口 → 走 contentStore）
- 一般 SSOT 第二份其它形态（已并 TD-02-21/23/30）
- conversationState 的 convKey/activeKey（实测 import agentKeys.ts，合法，非债）

## 五 · 探债

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-13-5 | `projectMemoryStore.ts:58` 本地裸拼 `agent_project_memory_v1_${agentKey}`，绕开 `contracts.ts` 真源 + `agentKeys.ts` 构造器模式 | 增债 | 结构债 | 备份/云同步清单 + 迁移读；键改名即漂 | 中 | M7 第二份（构造器层漏收口） | 在 `agentKeys.ts` 加 `agentProjectMemoryKey(agentKey)` 构造器（引 `contracts` 模板常量），`projectMemoryStore` 改 `import` 引用 | 待还 |
| TD-13-6 | `director3d-project` 键在 `project.ts:216`(`PROJECT_STORAGE_KEY`) 与 `d3dPersistence.ts:29`(`PROJECT_KEY_DEFAULT`) 两份裸写、无 `base/core` 同源构造器；且同模块 `d3dPersistence.ts:39`(`SAVE_CHANNEL='yimao_director3d_kv'`) BroadcastChannel 频道名也裸写（对称 `canvasSyncBus.ts` 已收口到 `base/core`） | 增债 | 结构债 | 3D 工程键/广播频道改名即漂；双真不一致风险 | 中 | M7 第二份（构造器层漏收口） | 抽 `base/core/director3dKeys.ts`（`DIRECTOR3D_PROJECT_PREFIX` 常量 + `director3dProjectKey(nodeId?)` 构造器引 contracts 真源；`DIRECTOR3D_SYNC_CHANNEL` 频道名常量），`project.ts`/`d3dPersistence.ts` 改 import、删本地 `PROJECT_STORAGE_KEY`/`PROJECT_KEY_DEFAULT`/`SAVE_CHANNEL` 常量（注：`LEGACY_PROJECT_STORAGE_KEY`/`CUSTOM_POSE_STORAGE_KEY` 合法——前者是 migration 读、后者已正确 re-export 真源，非债） | 待还 |
| TD-13-11 | `agentModelStore.ts:12/60` 本地裸写 `AGENT_CHAT_MODEL_KEY='agent_chat_model'`/`AGENT_HISTORY_TURNS_KEY='agent_history_turns'`；`skillStore.ts:32/287/324` 本地裸写 `SKILLS_KEY='agent_skills'`/`USAGE_KEY='agent_skill_usage'`/`ENABLED_KEY='agent_skill_enabled'`；均未 import `contracts` 已登记真源（对称 `agent_credit_switch`→`CREDIT_SWITCH_KEY` 已收口） | 增债 | 结构债 | 同批 agent_ 域键改名即漂；备份/云同步/迁移读全漂 | 中 | M7 第二份（常量层漏收口） | 在 `contracts.ts` 导出 `KEY_AGENT_CHAT_MODEL`/`KEY_AGENT_HISTORY_TURNS`/`KEY_AGENT_SKILLS`/`KEY_AGENT_SKILL_USAGE`/`KEY_AGENT_SKILL_ENABLED` 常量（或并 `agentKeys.ts`），两 store 改 import 引用、删本地常量 | 待还 |
| TD-13-9 | `cloudSync.ts:902-910` 的 `SYNC_ALLOW` 白名单裸列 `agent_chat_model`/`agent_history_turns`/`agent_skills`/`agent_skill_enabled`/`agent_credit_switch` 这 5 个 contracts 已登记键字面量，与同 Set 内 `KEY_YIMAO_PRESET_PROMPTS`（已 import 真源）混写 → 半收口 | 增债 | 结构债 | 白名单与真源键名失配即漏同步/误同步 | 中 | M7 第二份（清单型漏收口） | `SYNC_ALLOW` 内 5 个裸字面量改为 import 的 `KEY_*` 常量引用（与 `KEY_YIMAO_PRESET_PROMPTS` 写法统一） | 待还 |
| TD-13-10 | `tableWorkspaceState.ts:59` 本地裸写 `WIDTH_KEY='agent_split_width'`，未 import `contracts` 已登记真源（L429）；注释 L12 谎称"STORAGE_KEYS 已登记"实为未引用真源常量 | 增债 | 结构债 | 该键改名即漂；双真不一致 | 低 | M7 第二份（常量层漏收口） | `tableWorkspaceState.ts` 改 import `contracts` 的 `KEY_AGENT_SPLIT_WIDTH`（需在 contracts 导出），删本地 `WIDTH_KEY` | 待还 |

## 六 · 覆盖度表（增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| src/components/base/core/contracts.ts | 地基 | 深审 | 🟢 | 历史 + 本轮 | STORAGE_KEYS 登记 | 非债（真源本身正确） |
| src/components/base/core/agentKeys.ts | 地基 | 深审 | 🟢 | 历史 + 本轮 | 构造器模式已收口 | 非债（模式正确，仅漏项） |
| src/components/base/core/videoEditorKeys.ts | 地基 | 深审 | 🟢 | 历史 + 本轮 | 对称先例 | 非债（模式正确） |
| src/components/agent/runtime/projectMemoryStore.ts | 上层(消费) | 有债待还(TD-13-5) | 🔴 | 本轮 | `projectMemoryStore.ts:58` 裸拼 | TD-13-5 |
| src/components/director3d/project.ts | 上层(消费) | 有债待还(TD-13-6) | 🔴 | 本轮 | `project.ts:216 PROJECT_STORAGE_KEY` 裸写 | TD-13-6 |
| src/components/director3d/d3dPersistence.ts | 上层(消费) | 有债待还(TD-13-6) | 🔴 | 本轮 | `d3dPersistence.ts:29 PROJECT_KEY_DEFAULT` 裸写 | TD-13-6 |
| src/components/agent/conversation/conversationState.ts | 中间 | 已裁定非债 | 🟢 | 本轮 | `conversationState.ts:55-56` 实测 import agentKeys | 非债（合法引用） |
| src/components/base/core/canvasSyncBus.ts | 地基 | 已裁定非债 | 🟢 | 本轮 | `canvasSyncBus.ts:18` 频道名常量在 base/core、广播侧监听侧共用、3 import | 非债（收口榜样，对称 director3d 应照此） |
| src/components/director3d/project.ts | 上层(消费) | 已裁定非债 | 🟢 | 本轮 | `project.ts:223 CUSTOM_POSE_STORAGE_KEY` 已正确 re-export contracts 真源；`project.ts:217 LEGACY_PROJECT_STORAGE_KEY` 为 migration 读（合法） | 非债（仅 L216 PROJECT_STORAGE_KEY 属 TD-13-6） |
| src/components/director3d/storage.ts | 上层(消费) | 已裁定非债 | 🟢 | 本轮 | `storage.ts:41/53/76/95` 均用传入 key 经 contentStore，未裸拼键名 | 非债（已委托，无第二份） |
| src/components/base/store/agentModelStore.ts | 上层(消费) | 有债待还(TD-13-11) | 🔴 | 本轮 | `agentModelStore.ts:12/60` 裸写 AGENT_CHAT_MODEL_KEY/AGENT_HISTORY_TURNS_KEY 未 import 真源 | TD-13-11 |
| src/components/base/store/skillStore.ts | 上层(消费) | 有债待还(TD-13-11) | 🔴 | 本轮 | `skillStore.ts:32/287/324` 裸写 SKILLS_KEY/USAGE_KEY/ENABLED_KEY 未 import 真源 | TD-13-11 |
| src/components/base/store/cloudSync.ts | 上层(消费) | 有债待还(TD-13-9) | 🔴 | 本轮 | `cloudSync.ts:902` SYNC_ALLOW 裸列 agent_* 键字面量，与 KEY_YIMAO_PRESET_PROMPTS 混写 | TD-13-9 |
| src/components/agent/assistantTable/tableWorkspaceState.ts | 上层(消费) | 有债待还(TD-13-10) | 🔴 | 本轮 | `tableWorkspaceState.ts:59` WIDTH_KEY='agent_split_width' 未 import 真源（注释谎称已登记） | TD-13-10 |
| src/components/agent/canvas/useCanvasAgentTools.ts | 中间 | 已裁定非债 | 🟢 | 本轮 | `useCanvasAgentTools.ts:46` 正确 import CREDIT_SWITCH_KEY/CREDIT_GATE_EVENT（收口榜样） | 非债（对称 agent_* 应照此） |

## 七 · 结论（状态行）

```
区域 13 跨区：有债待还（TD-13-5/6/11/9/10，结构债，M7 第二份「键构造器+常量+清单」层漏收口）
本轮两深入审 11 文件 / 底层 contracts/agentKeys/videoEditorKeys 深审不重审
  - 第二批（本轮深入）新增：agentModelStore / skillStore / cloudSync / tableWorkspaceState 4 文件判有债；useCanvasAgentTools 判榜样
  - 未审：director3d/storage.ts 是否还散拼 director3d 键（refs 显示仅 project.ts/d3dPersistence.ts 两处裸写，storage.ts 经委托未裸拼，标深审）
覆盖账：本轮累计纳入 7 文件全判有债（projectMemoryStore/project.ts/d3dPersistence/agentModelStore/skillStore/cloudSync/tableWorkspaceState）；5 文件判非债留痕（conversationState/canvasSyncBus/director3d-storage/project.ts部分/useCanvasAgentTools）。
母体收敛：所有债同归 M7「SSOT 第二份」——只漏 director3d 模块（键+频道名）与 agent_ 域键群（agentModelStore/skillStore/cloudSync/tableWorkspaceState）+ agent_project_memory 构造器漏抽；videoEditor/agent_conversations/agent_credit_switch 已正确收口作对称先例。
下一步：取下一个待审计区（按 index 灯：19 / 22 为 🔴 上层，底层 09/10/14/16/18 已 🟢）；或就本轮 5 债当场施工收口。
```
