# 16 跨区 · 扩展名真值源横向推导 · 2026-09-16

> 触发：用户「从历史已有债务横向推导，未登记/未被发现的债务」。
> 方法：以 `债务登记5步法 §3.4` 高频形态为母题，全库 grep 横向扫兄弟实例，逐条重新取证（不抄历史结论）。
> 分工：本文件 = 审（只读）。施工交 `架构师改码7步法.md`。

## 一 · 链路快照（取证，非结论）

横向推导对象 = 已登记债的**形态**，不是具体文件。本轮把 8 类形态逐一全库扫：

| 形态（母题） | 检出手段 | 全库命中 | 新债？ |
| --- | --- | --- | --- |
| 绕过唯一入口 | `node scripts/check-arch.mjs` | 全绿（105 engine / 78 ui / 579 文件 0 红点） | 否（闸收口） |
| 内联媒体扩展名正则（EXT_KIND 旁路） | `\.\(` 字面量 grep（src/） | 仅 3 处正则 + 1 处自定义函数 | 否（= TD-16-4/5/6/7） |
| 静默吞·空 catch | `catch (\w*) {\s*}` grep | 0 命中 | 否（饱和） |
| 假护栏/恒真死表达式 | `\| true` / 假收窄注释 grep | 2 处命中，均**已登记** | 否（TD-22-38 / TD-05-10） |
| 幽灵预留/将来/占位 | `预留\|暂不\|将来\|备用` grep（179） | 179 命中，绝大多数为已固化有意设计注释 | 否（非债，见 §四） |
| SSOT 第二份·文档扩展名 | `md\|markdown\|txt` grep | **2 处逐字重复**，无统一 helper | **是（TD-16-8）** |

## 二 · 溯源（从最底层起手）

扩展名真值源地基 = `src/components/base/utils/assetType.ts`（`EXT_KIND` 一张表，测试注释「此前 5 处就地正则已全部委托本模块」）。
上层消费方本应统一走 `detectAssetType / classifyUrl / isAudio / isAssetUrl / resolveAssetType`。
本轮验证：媒体类内联正则**已全部收敛**（仅余 TD-16-4/5/6/7 待还），但**文档/技能类扩展名**从未纳入该真值源家族 → 在 UI 层自行内联。

## 三 · 定海（唯一入口 / 真值源清单）

- 媒体扩展名真值源：`assetType.ts` `EXT_KIND`（已立闸 + 测试对账）。
- 文档/技能扩展名真值源：**不存在**（本轮新债根因）。两处 UI 各自内联 `/\.(md|markdown|txt)$/i`。

## 四 · 切割（探债明细）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-16-8 | `AgentPanel.tsx:1034/1037` 与 `SkillSettings.tsx:226/230` 各内联 `/\.(md|markdown|txt)$/i`（逐字相同），无统一 `isSkillFileExt()` helper；新增扩展名需双改，漏改即漂移 | 增债 | 结构债 | 2 文件 | 低 | 文档/技能扩展名真值源缺失（SSOT 第二份，非母体，仅 2 份） | 抽 `isSkillFileExt(name)` 到 `base/utils`，两处改调；未来扩展只改一处 | 待还 |
| — | `export-button.tsx:70` `|| true` 恒真死表达式 | — | — | — | — | — | 已登记 **TD-22-38（已解决）**，非新债 | 已裁定非债 |
| — | `promptHubStore.ts:242` `typeof==='object'` 假收窄 | — | — | — | — | — | 已登记 **TD-05-10（已解决）**，非新债 | 已裁定非债 |
| — | `src` 空 `catch {}` | — | — | — | — | — | 0 命中，静默吞·空 catch 形态饱和 | 已裁定非债 |
| — | `App.tsx:1471` `glb\|gltf` 名剥离 | — | — | — | — | — | 单份、本地名剥离、无真源可重复 | 已裁定非债 |
| — | `预留/暂不/将来/占位` 179 注释 | — | — | — | — | — | 多为已固化有意设计（占位 UI / 文档化扩展点），非空预留 | 已裁定非债 |

## 五 · 覆盖度表（增量，本轮新纳入 / 状态变化）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `src/components/panels/AgentPanel.tsx` | 上层 | 有债待还(TD-16-8) | 🔴 | 16-跨区-扩展名真源横向推导-2026-09-16 | L1034/1037 `/\.(md\|markdown\|txt)$/i` | TD-16-8 |
| `src/components/base/panels/sections/SkillSettings.tsx` | 上层 | 有债待还(TD-16-8) | 🔴 | 同上 | L226/230 同正则逐字重复 | TD-16-8 |
| `scripts/check-arch.mjs`（绕过唯一入口形态） | 元层 | 已裁定非债 | 🟢 | 同上 | 全绿 0 红点 | 形态饱和 |
| `src` 全量空 catch | 上层 | 已裁定非债 | 🟢 | 同上 | grep 0 命中 | 形态饱和 |
| `export-button.tsx` `|| true` | 上层 | 已裁定非债 | 🟢 | 同上 | L70 = TD-22-38(已解决) | 非新债 |
| `promptHubStore.ts` 假收窄 | 地基 | 已裁定非债 | 🟢 | 同上 | L242 = TD-05-10(已解决) | 非新债 |
| `src/components/director3d/App.tsx` `glb\|gltf` | 上层 | 已裁定非债 | 🟢 | 同上 | L1471 单份名剥离 | 非债 |

## 六 · 结论

- 顶层高频形态（绕过唯一入口 / 内联媒体扩展名 / 空 catch 静默吞 / 假护栏）**已被闸与跨区轮(13/16/17/22/24)饱和**，横向推导仅命中间已登记实例，无新债。
- 唯一新债 = **TD-16-8**：文档/技能扩展名真值源缺位，UI 层两份逐字内联 `/\.(md|markdown|txt)$/i`，结构上与 EXT_KIND 同源（扩展名真值源），建议未来把「技能文件扩展名」也并入统一判定家族，杜绝双改漂移。
- 覆盖账：本区（16 utils）已审饱和 + 新登记 1；其余形态 0 新债，均标 `已裁定非债`，下次不再重导。

## 七 · 状态

已验证(有债待还)（TD-16-8 新登 · 顶层形态饱和核定：绕过入口／空catch／假护栏 已裁定非债）
