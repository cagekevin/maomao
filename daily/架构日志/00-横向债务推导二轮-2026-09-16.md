# 00 · 横向债务推导二轮（M7 衍生文件 §五 遗留三面 · 2026-09-16）

> **触发**：用户「根据现有债务横向推导还有没有其他的债务？要从底层开始找」第二轮。
> **上游**（本轮不重复其结论）：[00-横向债务推导-2026-09-16](./00-横向债务推导-2026-09-16.md)（5 候选全证伪）· [02-跨区-母体横向推导复查](./02-跨区-母体横向推导复查-2026-09-16.md)（地基 4 母体，仅 TD-22-54）· [02-存储-持久化-跨区-M7横向衍生](./02-存储-持久化-跨区-M7横向衍生-2026-09-16.md)（TD-02-34/35）。
> **范围**：只扫 M7 衍生文件 §五 明言「未横向延伸」的三面——① TD-22-52 统一图片出口横向面 ② TD-22-20 属性面板手抄交互横向面 ③ TD-09-2 tests 隐式 any。
> **结果**：新增债 1 笔（**TD-19-3**）；其余候选全部证伪/非债留痕。

---

## 一 · 链路快照（取证路径，自底向上）

```
面① 统一图片出口契约（base/utils/assetUrl.ts 头注「唯一出口」）🟢
  └→ 全仓普查 `new Image(` / `<img`（46 命中）→ 逐处分流「显示绕过」vs「像素处理/原语实现」
面② TD-22-20 母体（draft ref + initial ref + onBlur 两段提交，15 处手抄）🟡
  └→ 全仓普查 `onBlur`（23 文件）→ 逐处读码判「同形手抄」vs「异形单实现」
面③ TD-09-2（tests 侧 1323 处隐式 any 聚合债）
  └→ 无兄弟债可言（见 §四面③）
```

## 二 · 溯源（逐面实证，证据 = 文件:行号）

### 面① 统一图片出口横向面 —— 全部证伪（非债）

上一轮横展（[22-跨区-统一图片出口横展](./22-跨区-统一图片出口横展-2026-09-16.md)）只查了显示侧 `<img>`，漏了 `new Image()` 一族。本轮补扫：

| 文件:行 | 现状 | 判定 |
| --- | --- | --- |
| `director3d/project.ts:2015` `referenceImageFromFile` | `new Image()` 读**用户上传 File** → canvas → dataURL | 文件导入工具，非显示 → 非债 |
| `director3d/project.ts:2042` `referenceCanvasForExport` | `new Image()` 读 `reference.image` 进**导出合成** canvas | 导出侧解码，非显示 → 非债 |
| `director3d/depth.tsx:60` `DepthMeshModel` | `new Image()` 读 url **采样亮度生成深度网格** | 3D 几何处理，非显示 → 非债 |
| `base/utils/asyncGuard.ts:102` `loadImageWithTimeout` | 统一图片加载**原语本身**（头注：已替代各模块私有实现） | 契约实现，非绕过 → 非债 |
| `base/utils/clipboard.ts:67` `copyImageToClipboard` | 复制图片到剪贴板的像素处理 | 非显示 → 非债 |

> 契约边界由此轮划清：统一图片出口约束**显示侧**（`<img src>` / `background-image`）；像素处理 / 导出解码 / 加载原语不属该契约域。此结论留痕，后续勿把 `new Image()` 一族再当绕过候选重查。

### 面② TD-22-20 手抄交互横向面 —— 命中 1 笔新债（异形：SSOT 逐字复制）

`onBlur` 全仓 23 文件。videoEditor 属性面板 4 文件（sticker 5 / video 6 / audio 2 / text 7）= TD-22-20 已登记 15 处，**不重审**。外区逐处判定：

| 文件:行 | 形态 | 判定 |
| --- | --- | --- |
| `base/panels/ResourceLibrary.tsx:431-502` + `base/panels/GeneratedView.tsx:372-441` | 创建文件夹卡片 + 重命名输入条 + 触发按钮 onClick **三块逐字相同**（连 onFocus 选中主体逻辑与注释、className、「回车确认」全一致） | **TD-19-3 新债** |
| `prompt/PromptInput.tsx:741` | 富文本编辑器 blur → emitDOM/mention 收口 | 异形单实现 → 非债 |
| `panels/sections/SkillSettings.tsx:310` | blur → IME searchIme cancel | IME 守卫机制 → 非债 |
| `base/ui/NodeTitle.tsx:56` | 全体节点共用的标题编辑器（唯一实现） | 单实现 → 非债 |
| `nodes/GridMergeNode.tsx:794` | 网格文本 blur → parseGrid 解析提交 | 解析型单实现 → 非债 |
| `nodes/TextGenerate.tsx:400` | blur → 退出编辑态 | 单实现 → 非债 |
| `director3d/panels/ShotsPanel.tsx:89` | onChange 直写 + onBlur 带确认旗标 | 直写型，非两段提交 → 非债 |
| `director3d/panels/GlobalSettingsPanel.tsx:160` | durationDraft + commitDuration（含钳制） | 单字段 bespoke → 非债 |
| `base/editors/ImageEditor.tsx:1261` | 画布浮层文本 blur → commitText | 画布浮层单实现 → 非债 |
| `agent/assistantTable/CellEditor.tsx:66` | onBlur=onCommit，头注明写「上层驱动编辑态」设计 | 有意设计已固化 → 非债 |
| `agent/assistantTable/TableGrid.tsx:159` · `TableTabsBar.tsx:103` · `AssistantTablePanel.tsx:599` | 重命名/风格草稿 blur 提交，**带 `!isComposing` 守卫** | 11 区已深审；此处作 TD-19-3 的**分叉对照物** |
| `videoEditor/ui/ui/input.tsx` · `tooltip.tsx` | 共享原语内部 onBlur | 原语实现 → 非债 |
| `videoEditor/ui/editor/panels/assets/views/settings.tsx:230-244` | 局部 BlurCommit 包装（onChange 直写 + onBlur commit，单实现） | 非 TD-22-20 漏计 → 非债 |
| `videoEditor/ui/editable-timecode.tsx:107-126` | 单一时间码编辑组件 | 非 TD-22-20 漏计 → 非债 |

> **TD-19-3 的利息实证（已分叉）**：表格三件的重命名 onBlur 带 `!e.nativeEvent.isComposing` 守卫，而 ResourceLibrary/GeneratedView 的逐字复制块**没有**——同型交互已在两处体系各自演化，修一处漏一处的风险是现实的，不是理论。

### 面③ TD-09-2 tests 隐式 any —— 不可横扫

TD-09-2 本身就是「tests 侧 1323 处隐式 any」的**聚合债**（登记为待翻新，不阻塞主链路），不存在「兄弟债」；对它做横向推导 = 把同一笔债重数一遍。**裁定：无横向面，不再扫。**

## 三 · 定海

- 统一图片出口唯一真源 = `base/utils/assetUrl.ts`（渲染 `normalizeAssetUrl` / 发送 `normalizeAssetUrlForSend`）+ `LazyImage`；本轮划清契约边界（显示侧 only）。
- 「内联重命名/建夹输入」无共享原语 = TD-19-3 偿还落点（base/ui 窄抽取，先例 TD-19-1 `DropdownPanel`）。

## 四 · 切割（本轮不审）

- videoEditor 属性面板 15 处（TD-22-20 已登记）；表格三件细节（11 区已深审）；contracts/eventBus/contentStore/playback-manager（09-16 复查已裁非债/已收口）。

## 五 · 探债（明细真源）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-19-3 | ResourceLibrary 与 GeneratedView 面板孪生：创建文件夹卡片＋重命名输入条＋按钮 onClick 三块逐字复制（RL:431-502／GV:372-441），且复制块缺 `isComposing` 守卫已与表格实现分叉 | 增债 | 结构债 | 2 面板 3 块 | 低 | 孤例（面板孪生复制，SSOT 第二份实例） | 抽共享「内联重命名/建夹输入」原语（base/ui 窄抽取，先例 TD-19-1），行为零变化；触发条件＝任一面板改此交互时 | 待还 |

**非债留痕**（禁再当 bug 补）：§二 面① 表 5 处 + 面② 表 13 处（除 TD-19-3 行外全部）。

## 六 · 结论 + 覆盖账

1. 三面收口：面① 证伪全清（`new Image()` 一族全部非显示用途）；面② 命中 **TD-19-3**（本轮唯一新债）；面③ 无横向面。
2. **覆盖账**：本轮普查 `onBlur` 23 文件 + `new Image` 7 处；逐处深读判定 18 处；非债 17 处（§二 两表）；新债 1 笔。未深读仅 `videoEditor/ui/ui/input.tsx`/`tooltip.tsx`（共享原语内部实现，低价值，原语本体已由 22 区 radix 轮覆盖）。
3. **DATAFLOW**：链路无漂移；`panels/ResourceLibrary` / `panels/GeneratedView` 两灯 🟢→🔴（有债待还 TD-19-3），已就地改灯。
4. TD-19-3 归属说明：复制两方分属素材库（12 语义）与生成结果（01 语义）面板，共享原语的根因落点 = UI 基础层（19 区，先例 TD-19-2 同区），故债 ID 绑 19。

---

## 覆盖度表（本轮增量，不重抄旧行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `base/panels/ResourceLibrary.tsx` | L3 面板 | 有债待还(TD-19-3) | 🔴 | 09-16 二轮 | :431-502 与 GV 逐字对照 | TD-19-3 |
| `base/panels/GeneratedView.tsx` | L3 面板 | 有债待还(TD-19-3) | 🔴 | 09-16 二轮 | :372-441 与 RL 逐字对照 | TD-19-3 |
| `director3d/project.ts`（2015/2042 两点） | L3 | 已裁定非债 | 🟢 | 09-16 二轮 | 读码：File 导入 / 导出合成 | 非债（像素处理） |
| `director3d/depth.tsx` | L3 | 已裁定非债 | 🟢 | 09-16 二轮 | :60 深度网格采样 | 非债 |
| `base/utils/asyncGuard.ts`（loadImageWithTimeout） | L1 原语 | 已裁定非债 | 🟢 | 09-16 二轮 | :91-118 头注+实现 | 契约原语本身 |
| `base/utils/clipboard.ts`（copyImageToClipboard） | L1 原语 | 已裁定非债 | 🟢 | 09-16 二轮 | :62-83 | 非债 |
| 其余 §二 面② 表 11 文件 | L3 | 已裁定非债 | 🟢 | 09-16 二轮 | 见 §二 面② 表逐行 | 异形单实现 |
