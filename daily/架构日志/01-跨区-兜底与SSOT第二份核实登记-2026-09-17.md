# 01 跨区 · 兜底与 SSOT 第二份核实登记（生成链路调查延伸轮）—— 2026-09-17

> **触发区**：01（生成链路）。本轮为**只读审计轮**（债务登记 5 步法）：上一轮「任务结果媒体形态判据收口」调查延伸出的
> 12 个候选，逐个取证定性后登记。**本轮未改任何 src/localTool 代码、未跑任何测试。**
> 债按根因区挂账（02 存储／08 localTool 后端／01 生成链路），结论唯一落点 = 本文件。

---

## §一 · 起点（Step 1）

- **触发**：上一轮施工时通读了生成链路前后端 ~20 个文件，发现多处「同一判据两份」「死兜底」「兼容死分支」候选。
- **底层起点**：localTool 后端（utils／路由／adapter）+ 前端 store／api（非 UI 钻取）。
- **历史债对照**：`debt.mjs area 01` 已核（TD-01-11「文本无 resultUrl」与本轮 #7 直接相关）；`search` 未发现本轮各主题已有在办债。

## §二 · 取证（Step 2，证据 = 文件:行 + refs）

| 证据 | 手段 | 结论 |
| --- | --- | --- |
| `filesApi.ts` fan-in | `mv-sync-refs.mjs refs` | **39 处**（12 个 src 消费方 + 17+ 测试）→ 落盘收口必须"内部收敛、签名不变" |
| lovart 阻塞版消费方 | 全库 grep | `chatLovartText` 被 `generateEngine.ts:189` 消费；`generateImageLovart`／`generateVideoLovart`／`streamChatLovart` 被 `ai-relay/index.ts:203-218`（createRelay 分流）消费 → **非死代码**，改判（见探债表 #8） |
| `active_api_endpoint` 写入侧 | 全库 grep | 唯一生产者 `providerStore.ts:468` `contentSetAsync(KEY_ACTIVE_API_ENDPOINT, {providerId,name,base_url,protocol,…})` —— **只写 JSON 对象**，旧「纯 URL 字符串」格式无生产者 |
| 本地专属路径分派 | 读 `localTool/src/index.ts` | `:331` `/files/` · `:336` `/depth-video/` · `:363-364` 前端托管排除表 · `:372` 注释引用 passthrough 的拦截承诺；`passthrough.ts` 另持 `LOCAL_ONLY_PREFIXES` |
| 恢复轮询终态落地 | 读 `pollTask.ts` 全文 | `:83-90` `patchTask(completed)+publishTaskCompleted`，与 live 的 `done()`（taskStore.ts:287-302）平行 |
| 类型分类词表 | 读两文件 | `generatedSource.GENERATED_CATEGORIES`（无 text、有 audio）vs `GeneratedView.TYPE_FILTERS`（有 text、无 audio）—— **已实际漂移** |
| 预览出口 | 读两文件 | TaskCenter 与 GeneratedView **各自自写**全屏图片预览 overlay；video 判定口径分叉（精确等值 vs `startsWith('video')`） |

## §三 · 探债（Step 3+4，明细）

| 账本号 | 决策点／问题点 | 归类 | 归属区 | 爆炸半径 | 利息率 | 根因（母体） | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-02-58 | `filesApi.ts:511` `persistUrlToUploads` 自称"URL→uploads 唯一原语"，`:576` `saveResultToTasks` **又抄一份分流**（blob／data:／http／已是本机），且 **blob 语义分叉**（前者取 Blob 真上传，后者直接 `skipped`） | 增债 | 02 | fan-in 39 处（refs 实测） | 中 | M3 SSOT 第二份（注释宣称收口、实际留了第二份） | `saveResultToTasks` 内部委托原语，签名与三态语义不变 | 待还 |
| TD-08-32 | `resolveLocalImages.ts:81` 与 `:164` 两份深遍历 `walk`（约 30 行逐行相同，仅叶子变换不同：base64 vs 回环 URL） | 增债 | 08 | 该 utils 的 2 个消费方（generateEngine／relay-poll） | 中 | M3 SSOT 第二份（形态决策已收口 `refFormatOf`，遍历未收口） | 一个 walk + 两个叶子变换 | 待还 |
| TD-08-33 | **本地专属路径判据三处字面量**：`index.ts:331`（/files/）· `:336`（/depth-video/）· `:363-364`（前端托管排除表）＋ `passthrough.ts` 的 `LOCAL_ONLY_PREFIXES`。**已现缺口**：`/depth-video/` 是本地资源但不在 passthrough 清单，其 GET 未命中 `handleDepthResource` 时会落入 catch-all **被转发外网** | 增债 | 08 | 每新增一条本地路由都要改两处；漏一处 = 请求外泄 | **高** | M3 + 手写清单母体（清单必漏） | 清单由路由分派派生单一真源；`/depth-video/` 缺口随收口一并闭合 | 待还 |
| TD-08-34 | `lovart/index.ts` **提交序列（setMode→attachments→prompt→toolConfig→sendChatWithProject）内联 5 份**（`:111`／`:128`／`:160`／`:184`／`:226`）；轮询状态机 **2 套且口径分叉**：阻塞版 `pollLovartThread` done 后 5s 复核，生产单轮版 `pollLovartTaskOnce` **无复核** | 增债 | 08 | 改一处提交语义要改 5 处；done 复核语义两套行为 | 中 | M3 SSOT 第二份 ×2（提交序列 + 轮询状态机） | 抽"提交核心"一份；done 复核语义两套统一裁定（或都有或都无） | 待还 |
| TD-08-35 | `official.ts::extractBaseFromEndpointValue` 兼容「旧纯 URL 字符串」格式，写入侧唯一生产者只写 JSON → **死兼容分支** | 增债 | 08 | `readOfficialBase`（透传目标唯一决策点）的判定面变宽 | 低 | 为存量兼容（项目约定恒删） | 删旧格式分支 | 待还 |
| TD-08-36 | `logWriter.ts:50` 与 `:58` 各定义一份日志级别阈值词表 `order` | 增债 | 08 | 词表漂移时两处不同步 | 低 | M3 SSOT 第二份 | 提为模块常量一份 | 待还 |
| TD-08-37 | `logWriter.write():156` `_orig_${level} \|\| console[level]` 为**不可达死兜底**（`initLogWriter` 必先赋值 `_orig_` 才接管），且用"动态挂 console 属性"传原引用，掩盖接管时序契约 | 增债 | 08 | 无（死代码），但属"让人只能猜"的典型 | 低 | 死兜底（掩盖时序契约） | 删 `\|\|`，改为 init 时显式保存原引用（模块级 Map） | 待还 |
| TD-01-20 | **任务终态落地两套**：live=`done()`（taskStore.ts:287，含防字符串／埋点／`progressPersist.cancel`）；恢复=`patchTask(completed)+publishTaskCompleted`（pollTask.ts:83-90，**无 cancel 无防御**）。失败分支同理（`fail()` vs `patchTask(failed)+showToast`） | 增债 | 01 | live 与恢复两条路径行为分叉，排查时"为什么这条有防御那条没有" | 中 | M3 + 口径错位 | 抽 `completeTask`／`failTask` 原语，两路共用（`done()`/`fail()`/pollTask 全部委托） | 待还 |
| TD-01-21 | `useNodeGeneration` **写回路径 5 条并存**（`settle`／`onPersisted`／`resultKey`／`recoverable`／`onRecover`），文件头自述"并存时若都写了同一字段会幂等双写（无害）" | 增债 | 01 | 每新增节点要选走哪条路；双写=掩盖"谁是唯一写回" | 中 | 兜底形态（占位＋事后补／幂等双写当无害） | 定唯一写回路径（声明式或回调式二选一），删冗余 | 待还 |
| TD-01-22 | **生成类型分类词表两份且已漂移**：`generatedSource.GENERATED_CATEGORIES`（无 text、有 audio）vs `GeneratedView.TYPE_FILTERS`（有 text、无 audio），注释自称"同口径" | 增债 | 01 | 词表漂移 → 面板 pill 与可引用媒体分类不一致 | 中 | M3 SSOT 第二份（注释自承手抄） | 一处派生（生成面板与媒体源同读一词表） | 待还 |
| TD-01-23 | **图片全屏预览 overlay 两份**：TaskCenter（自写 overlay+像素角标）与 GeneratedView（:580-622 自写 overlay），video 判定口径分叉（`kind==='video'` vs `type==='video' \|\| startsWith('video')`） | 增债 | 01 | 两份 overlay 各自演进（像素角标只有一处有） | 中 | M3 SSOT 第二份 | 抽统一预览原语/组件，video 判定口径随 #媒体形态判据统一 | 待还 |

### 改判留痕（防后人再走）

- **候选「删除 lovart 阻塞版 API（原 #9，判幽灵预留）」→ 改判非债**：`chatLovartText` 有生产消费（`generateEngine.ts:189`），
  阻塞版三函数被 `ai-relay/index.ts` createRelay 消费；真正的债是其**内联复制**（TD-08-34）。grep 证据见 §二。
- **候选「删 pollTask 恢复轮询（与 in-flight 双轮询）」→ 维持既有裁定非债**：两套轮询生命周期不同（页面级 vs 数据级），
  `pollTask.ts:10-12` 头注释已论证；本轮只把"终态落地动作重复"单独立债（TD-01-20）。
- **`Task.type` 无类型域** → 不新开，挂既有计划（上一轮 01 区轮次文件 §八 第 2 条，留待 09 类型诚实性批次）。

## §四 · 覆盖度表（本轮触及文件）

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据（refs/行号） | 债/裁定 |
| --- | --- | --- | --- | --- |
| `localTool/src/routes/passthrough.ts` | 深审（上轮改过诊断 → 本轮回查） | 2026-09-17 | 全文通读 + 上轮施工 | TD-08-33 |
| `localTool/src/index.ts` | 抽审（只读分派段 :160-375） | 2026-09-17 | grep 行号 | TD-08-33 |
| `localTool/src/utils/resolveLocalImages.ts` | 深审 | 2026-09-17 | 全文 | TD-08-32 |
| `localTool/src/utils/logWriter.ts` | 深审 | 2026-09-17 | 全文 | TD-08-36 / TD-08-37 |
| `localTool/src/routes/official.ts` | 深审 | 2026-09-17 | 全文 | TD-08-35 |
| `localTool/src/relay-poll.ts` | 深审（上轮三处净减） | 2026-09-17 | 全文 + 上轮施工 | 无新债 |
| `localTool/src/ai-relay/providers/lovart/index.ts` | 深审 | 2026-09-17 | 全文 + grep 消费方 | TD-08-34 |
| `localTool/src/ai-relay/providers/lovart/lovart_task.ts` | 深审 | 2026-09-17 | 全文 | TD-08-34 |
| `localTool/src/ai-relay/providers/lovart/lovart_project.ts`／`lovart_attachments.ts`／`lovart_client.ts` | 抽审 | 2026-09-17 | 通读（未逐行核 mime 词表） | 无新债（TD-08-22 已收口 mime） |
| `localTool/src/providerConfigStore.ts` | 抽审（只读出站解析段） | 2026-09-17 | 通读 :140-210 | 无新债 |
| `src/components/base/api/filesApi.ts` | 深审 | 2026-09-17 | 全文 + refs（39） | TD-02-58 |
| `src/components/base/api/pollTask.ts` | 深审 | 2026-09-17 | 全文 | TD-01-20 |
| `src/components/base/api/relayProxy.ts`／`generate.ts` | 深审 | 2026-09-17 | 全文 | 无新债 |
| `src/components/base/store/taskStore.ts` | 深审（上轮施工） | 2026-09-17 | 大部分 + grep | TD-01-20 |
| `src/components/base/store/generationContract.ts` | 深审 | 2026-09-17 | 全文 | 无新债（随 TD-02-58 一并清） |
| `src/hooks/useNodeGeneration.ts` | 深审 | 2026-09-17 | 全文 | TD-01-21 |
| `src/components/base/panels/TaskCenter.tsx`／`GeneratedView.tsx` | 深审／抽审（GeneratedView 只读预览与词表段） | 2026-09-17 | 行号 | TD-01-22 / TD-01-23 |
| `src/components/base/utils/assetUrl.ts`／`useImageFallbackSrc.ts`／`ui/LazyImage.tsx` | 深审 | 2026-09-17 | 全文 | 无新债（08 区第二波已收口显示策略） |
| `src/components/agent/runtime/agentAttachments.ts`／`conversation/conversationImageMap.ts` | 抽审 | 2026-09-17 | 通读 | 无新债 |

## §五 · 覆盖账

本轮范围 = 上轮调查触及的 24 个文件（非 01 区全量）：**深审 17 · 抽审 7 · 未审 0**。
01 区全量覆盖仍以该区既有轮次文件的覆盖表为准，本轮不做全区结论。

## §六 · 收口动作（Step 5）

- 账本：`debt.mjs add` ×11（见 §三 账本号列），全部挂 anchor = 本文件。
- `arch-index.mjs --write` 重生成 index（机器维护）。
- DATAFLOW：无链路漂移（本轮只读登记），已对账一致。
- **禁令遵守**：本轮未改 `src/**`／`localTool/**` 一行；测试执行次数 = 0。

## §七 · 本区域状态

- 本区域状态：**已验证(有债待还)**
- 覆盖账：本轮为**只读审计轮**（新登记 11 债：02×1、08×6、01×4；改判非债 2 项留痕），未新增区域覆盖。

---

## §十 · 第二波（同日延伸）——「可能是债但还没查的」11 候选深挖

**触发**：用户问「还有哪些可能是债、你还没查的」。对第一波遗留的 11 条线索逐个取证（仍只读、测试 0 次），定性如下。

### 取证与定性

| 线索 | 取证（文件:行） | 定性 |
| --- | --- | --- |
| chat 超时口径 | `config.ts:112` `CHAT_TIMEOUT=120000` vs `lovart_config.ts:8` `LOVART_DEFAULT_TIMEOUT_MS=180_000`；`routes/generate.ts:9-11` chat「无句柄、不建任务行、不进 relay-poll」 | **债 TD-01-24**：前端必先超时，上游 thread 白跑到完成，无任何终态／恢复语义 |
| `upsertTask` merge | `routes/tasks.ts:136-150` `{...existing, ...row}` 整行浅覆盖 | **债 TD-08-38（高）**：后端终态写可被前端 running 快照覆盖（status 回退 + `result_url` 抹空）；竞态窗口需时序推演复核，但"后写者赢、无字段级写权限"是结构事实 |
| `debouncedSaveDb` | `db/database.ts:291-297`（500ms 防抖） | **债 TD-08-39**：TD-08-24 的「先落库再改内存」只保证内存 DB 顺序，落盘仍延迟 500ms —— 崩溃重启按旧文件重提 `pendingSubmit`，重复计费窗口未闭合 |
| `httpClient` 重试 | `httpClient.ts:174` `retries = 3` 默认、不分 method；`relaySubmit`／`filesApi UPLOAD_OPTS`／`relayChat`／`moveFile` 等各自 `retries:0` 自保 | **债 TD-08-41**：POST 幂等责任散落调用点，漏写一处即静默重试非幂等请求 |
| lovart 静默 drop | `lovart_attachments.ts:211-213`（drop 分支无日志、不计 failed_count） | **债 TD-08-40**：与 `resolveLocalImages.ts` 头注释引用的 docs/72 D-1 教训（绝不静默丢参考图）直接冲突 |
| 合成 SSE | `lovart_stream.ts` 全文（signal.aborted 处理正确；文件头自述 Lovart 无真流式） | **非债**（设计权衡，注释诚实）；"打字机"实为一次性吐出属体验事实，不为它造假延迟 |
| `genericOutput` 安全网 | `useConnectedInputs.ts:67-79` 三张表 + `:481` `uncoveredOutputNodeTypes()`（可测护栏，单测锁死）+ dev 告警；文本节点走特判不经安全网 | **非债**（护栏可测、范围明确 —— 第一波猜它与"文本被当 URL"有关，取证排除） |
| tests 手抄 mock 普查 | grep `statusLabel: (`／`typeLabel: (`／`statusDotClass: (` in tests → **0 命中** | **非债**（TaskCenter 那份是唯一实例，已随上轮改真源） |
| `x-official-base` | 全库 grep：`src/` **0 命中**，仅 `localTool/test/localtool.network.test.js:136` 注入 | **债 TD-08-42**：幽灵入口（M6），删或明示为调试后门 |
| 剧本盒 recover 补落库 | `useScriptBoxEngine.ts:161-204`（有 warn 留痕 + sha1 幂等 + ref 防重，但"自动归类"步骤本身无意图持久化） | **债 TD-01-25（低）**：补偿步骤形态，正确性靠幂等兜住而非状态 |
| `Task.type` 无类型域 | — | 维持既有计划（第一波 §八 第 2 条，留待 09 批次），不重复开账 |

### 新登记债（7 条）

| 账本号 | 一句话 | 区 | 利息 |
| --- | --- | --- | --- |
| TD-08-38 | upsertTask 整行浅覆盖，终态可被 running 快照覆盖 | 08 | **高** |
| TD-01-24 | chat 超时 120s vs 180s + 无句柄无恢复 | 01 | 中 |
| TD-08-39 | 防抖落盘 500ms，重复计费窗口未闭合 | 08 | 中 |
| TD-08-41 | httpClient 默认重试 POST，幂等责任散落 | 08 | 中 |
| TD-08-40 | lovart 静默 drop 参考素材 | 08 | 中 |
| TD-08-42 | x-official-base 幽灵入口 | 08 | 低 |
| TD-01-25 | 剧本盒归类步骤无意图持久化靠盲补 | 01 | 低 |

### 覆盖账（第二波增量）

新增触及：`lovart_config.ts`／`lovart_stream.ts`（深审）· `httpClient.ts`（深审）· `routes/tasks.ts`（抽审，只读 upsertTask／taskToRow 段）· `db/database.ts`（抽审，只读 debouncedSaveDb／initTables 段）· `useScriptBoxEngine.ts`（抽审，:160-214）· `useConnectedInputs.ts`（抽审，产出契约段）。
仍**未审**（本轮明确不做，待排期）：`ai-relay/protocol/` 全目录 · `httpTransport.ts` · `providerCatalog` 族 · `routes/files.ts` 全文 · `contentStore.ts` 实现 · `useGenerateNode` · `eventBus.ts` · `cloudSync.ts` · `resourceStore.ts`。

### 弯路留痕（第二波）

- **轮次文件债号回填漏了一处面**：第一波只回填了 §三 探债表与改判留痕，**漏了 §四 覆盖度表的债／裁定列**（8 处旧预设号）——
  靠追加第二波前重读全文才发现。教训：**回填编号要 grep 全文旧号清零，而不是凭记忆改"看得见的那几处"**。
- 一次 `read_file` 用 offset 越界误判"文件为空"——虚惊。教训：越界读取返回空 ≠ 文件空，先全文读再下结论。

## §十一 · 本区域状态（第二波后）

- 本区域状态：**已验证(有债待还)**
- 两波累计：**18 债**（02×1、08×12、01×5，其中 2 条高息）+ 改判非债 5 项留痕。
