# 架构日志 · 25 跨区 · ADR 合规横扫（0040 / 0038 / 0001）· 2026-09-19

> 触发：用户指令「审计 ADR-0040 / ADR-0038 / ADR-0001 三条，找出不符合的登记成债务」。
> 性质：跨区 ADR 合规横扫（元层）。area 25 此前只覆盖 0002–0032（见 `25-跨区-ADR合规横扫-2026-09-19.md`），本轮补齐这 3 条。
> 方法：只读审计（`refs` + 文件:行号 取证，未跑任何测试）。

## 探债

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-25-? | `src/components/base/utils/volumePolicy.ts` · refs: `agent/conversation/conversationSnapshot.ts:31` · `agent/conversation/conversationState.ts:46`（仅 2 处，全 agent/conversation） | ADR-0040 **L4**：只被一个域(agent/conversation)消费的件仍住 `base/utils` 横切层 | agent 域 | agent 会话体积预算逻辑找件困难，下一个人又会按目录名判错 | 中 | **母体**：S1-3 逆转(§7/§8)以 `check-arch` 规则2(import 方向)冒名 ADR-0040 L4(位置正确性)作判据且未自证(ADR-0001) | 收口：按 ADR-0040 L4 把 volumePolicy 迁 `agent/conversation/` | 待还 |
| TD-25-? | `src/components/base/ui/attachmentCover.tsx` · refs: `agent/panels/AgentMessage.tsx:4` · `agent/panels/AgentPanel.tsx:114`（仅 2 处，全 agent） | ADR-0040 **L4**：只被一个域(agent/panels)消费的件仍住 `base/ui` 横切层 | agent 域 | agent 消息附件封面件散在横切层，重复判错 | 中 | 同母体 | 迁 `agent/panels/` | 待还 |
| TD-25-? | `src/components/base/utils/faceMosaic.ts` · refs: `canvas/nodes/FaceMosaicNode.tsx` · `editors/FaceMosaicEditor.tsx`（仅 2 处，全图片能力） | ADR-0040 **L4**：只被一个域(图片能力)消费的件仍住 `base/utils` 横切层 | 图片能力域 | 图片打码件散在横切层 | 中 | 同母体 | 迁图片能力(与 `editors` 同域) | 待还 |
| TD-25-? | `src/components/base/panels/ResourceStrip.tsx` · refs: 6 处（PromptInput/FullscreenEditor/ImageGenerate/TextGenerate/VideoGenerate/TemplateNode 均画布域 · StepShots 属 scriptbox） | ADR-0040 **L1/L5**：归属三向冲突，同形态被拆——背景表说「跟 PromptInput 走(画布)」、`§3.1.3.4` 说「留通用面板层」、S2-9 计划说「归素材域」 | 素材?画布?**待裁定** | S2-9 执行会把 PromptInput 的配套件(输入区素材条)搬离主件(画布域)，违反 L5 同形态不拆 | 中 | 孤例·L1 两判据(界面位置 vs 数据落点)未裁定；ADR-0040 背景与执行计划自相矛盾 | 待用户拍板：界面位置(PromptInput 输入区=画布)优先于数据落点(resourceStore=素材) | 待裁定 |

> 母体债（TD-25-? 前 3 行）根因：域模块化 S1-3 收口在 2026-09-19 被逆转（DOMAIN-MODULES §7/§8），其理由「base 被业务域消费是正确单向、0 件可迁」引用了 `check-arch` 规则2（base 禁 import 非 base），但规则2 管的是 **import 方向**，与 ADR-0040 L4 管的 **位置正确性** 是两件事——引用既有规则作判据却未答「它凭什么成立」(ADR-0001)。结果 ADR-0040 背景表**亲笔点名**的 L4 实证件 `volumePolicy(agent 2)` · `attachmentCover(agent 2)` 不在被逆转覆盖的 11 件内，至今仍住 base 横切层；`faceMosaic` 同样单域消费、同样漏判。

## 覆盖度表（ADR 维度）

| ADR | 覆盖状态 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- |
| 0040 归属判据(界面位置+数据落点) | 有债待还 | 25-跨区-ADR合规横扫-0040-0038-0001-2026-09-19 | refs: volumePolicy/attachmentCover/faceMosaic 均单域消费；ResourceStrip 三向冲突 | 有债待还(TD-25-? ×4：L4 母体 1 + ResourceStrip 1) |
| 0038 改名字符串边界(只动符号+路径) | 已裁定非债 | 同上 | `contracts.ts:5-9` 键名冻结注释；`uploadDirs.ts:18`「一律不改名」；git 改名批仅移位未动键 | 合规 |
| 0001 既有约定不是判据来源 | 有债待还(母体推理瑕疵) | 同上 | S1-3 逆转 §7/§8 引规则2 作 L4 判据未自证 | 有债待还(TD-25-? 母体，含 ADR-0001 违反) |

## §七 本区域状态

- 本区域状态：审计中（ADR-0040 / ADR-0001 有债待还；ADR-0038 合规）。
- 覆盖账：本轮审 ADR 3（0040 / 0038 / 0001）/ 共关注 ADR 3 / 未审 0。
- 未做全量 base 横切层 sweep（仅审 ADR-0040 背景点名的 L4 实证件 + 图片能力 base 件 + ResourceStrip），余下 base/{core,utils,ui,api,storage} 全量 L4 sweep 建议另开一轮。
