# 16 · utils 工具层 · 跨区 · tryParse 判别联合「消费层压平 + 零留痕」· 2026-09-18

> **性质**：主题横推轮（主题 = **诚实性 · 生产者/消费者错位**），非区域全量轮。
> **触发**：用户指定主题「诚实性 · 消费者生产者错位」（区域 09「类型诚实性」为主题锚，根因落 16 utils 工具层）。
> **根因区**：16（`base/utils/asyncGuard.ts` 的 `tryParse` 原语所在）。
> **跨区影响**：18（`base/core/utils.ts`）· 11（agentCore / agentRuntime）· 09（类型诚实性）· 02（storageQuota）· 13（accountsStore）· 21（drag-data）。
> **结论落点**：本文件（唯一写全处）。
> **本轮结论一句话**：`tryParse` 这个**生产者已经诚实**（判别联合 `{ok,error}`），但**消费者把它压平回单值且不留痕** —— 全仓 14 个消费点里 **12 个零留痕**，其中 `parseSSEChunk` 更把失败翻译成 `true`（＝"已消费"），**谎报成功**导致 AI 回复静默截断。

- **本区状态**：`已验证(有债待还)`；本轮**新增 4 笔**（TD-16-49 / TD-16-50 / TD-18-8 / TD-18-9）。
- **覆盖账**：本轮审 12 / 涉及 12 / 未审 = `base/utils/**` 其余 23 文件（本轮为主题横推，不做区域全量，理由见 §四 范围声明）。

---

## 〇 · 一句话

> ① **母体**：`tryParse` 2026-09-17 契约收紧（TD-16-36）只改了**生产者**（`T|undefined` → 判别联合），**没同步改消费者** ⇒ **半截收口**。判别联合消除了"不可区分"，却**没有消除"静默"** —— 消费者拿到 `{ok:false}` 后 `r.ok ? r.value : <空值>` 一压，原始 `err` 就蒸发了。
> ② **判据不是我编的，是仓里自己写的**：`asyncGuard.ts:261` 明写「反模式：调用方拿到 `{ok:false}` 后**丢弃 err 且不留痕** = 静默吞（code review 必查）」。全仓 14 处，**12 处正在犯这个反模式**，且无机器守卫。
> ③ **口径错位已存在**：同一语义（"载荷 JSON 坏了"）有 **两套处理** —— `drag-data.ts:68-70` 与 `useResourceMoveToFolder.ts:67-69` 打 `logger.warn`；其余 12 处静默。同一真相两种口径 = SSOT 第二份。
> ④ **最重的一笔**：`agentCore.ts:210` 的 `if (!r.ok || !r.value) return true;` —— 把"解析失败"翻译给上层成 `true`（＝已消费/正常）⇒ `resolveBody:461` 因此跳过非流式兜底 ⇒ 坏 chunk 整块蒸发 ⇒ `content: acc.content || ''`（`:480`）空串当正常回复。而 `agentCore.ts:202-203` 的注释自称「防…被静默吞掉」—— **它自己就是那个吞的地方**，典型假护栏。

---

## 一 · 快照（本轮起点与灯）

| 项 | 值 |
| --- | --- |
| 主题 | 诚实性 · 生产者/消费者错位 |
| 主题锚区 | 09 类型诚实性（🟢 已验证无债 · 3 轮） |
| 根因区 | 16 utils 工具层（🔴 · 22 轮） |
| 起点文件 | `src/components/base/utils/asyncGuard.ts`（`tryParse` 原语 · 最底层契约） |
| 审查方向 | 类型/契约 → 纯函数 → store/core → hook → UI（自底向上） |

历史债防重（已查）：`TD-16-36`（tryParse 契约收紧 · 已解决）· `TD-08-28`（生产者给全 · 已解决）· `TD-12-10`（结果真相归属错位 · 已解决）· `TD-24-4`（全 src 错误兜底续审 · 待还）。
**复发判定**：TD-16-36 只覆盖生产者侧，**本轮 4 笔全是它的下游未收口面** ⇒ 属**回潮（半截收口）**，挂母体 TD-16-49。

---

## 二 · 溯源（取证）

### 2.1 生产者侧：确认 `tryParse` 已诚实（证毕）

`asyncGuard.ts:252-264`：

```ts
export function tryParse<T>(parser: () => T): ParseResult<T> {
  try { return { ok: true, value: parser() }; }
  catch (err) { return { ok: false, error: err }; }   // ← 失败携带原始 err
}
```

- 判别联合 `ParseResult<T> = {ok:true,value} | {ok:false,error}`（`:232`）—— 调用方**必须判 `ok`** 才能读 `value`，写错编译不过。✅ 生产者诚实。
- 生产者**故意不打日志**（`:256-260`，两条理由：① `logger → core/utils → asyncGuard` 会成环；② 判别联合已消除静默，留痕应由消费者按"读者"决定）。**这个职责划分本身成立**，故**不把"tryParse 不打日志"记为债**。
- ⇒ 债面**整个落在消费层**。

### 2.2 消费侧全量普查（`refs` + 逐条读上下文）

全仓 `tryParse(` 调用点 **14 处**，逐条判定：

| # | 位置 | 失败分支写法 | 留痕 | 判定 |
| - | --- | --- | --- | --- |
| 1 | `drag-data.ts:67-70` | `if (!r.ok) { logger.warn(...) ; return null }` | ✅ warn | **正面样板** |
| 2 | `useResourceMoveToFolder.ts:66-69` | `if (!r.ok) { logger.warn(...) ; return null }` | ✅ warn | **正面样板** |
| 3 | `core/utils.ts:54-59` `fileNameFromUrl` | `r.ok ? r.value : ''` | ❌ | 债（母体） |
| 4 | `core/utils.ts:77-83` `relativePathFromFileUrl` | `r.ok ? r.value : null` | ❌ | 债（母体） |
| 5 | `core/utils.ts:332-333` `formatTime` | `r.ok ? r.value : ''` | ❌ | 债（母体） |
| 6 | `accountsStore.ts:320-321` `hostOf` | `r.ok ? r.value : ''` | ❌ | 债（母体） |
| 7 | `storageQuota.ts:172-173` | `if (re.ok && re.value.test(key))` | ❌ | 债（母体） |
| 8 | `asyncGuard.ts:44-48` `sameOriginUrl` | `r.ok ? r.value : false` | ❌ | 债（母体） |
| 9 | `useAssetDropPaste.ts:148-149` `isCanvasGroupJson` | `if (!r.ok \|\| ...) return false` | ❌ | 债（母体） |
| 10 | `useAssetDropPaste.ts:302-312` | `assetR.ok ? assetR.value : null` → `if (asset?.url)` 整段跳过 | ❌ | 债（母体·用户可见） |
| 11 | `useAssetDropPaste.ts:369-370` | `rawR.ok ? rawR.value : undefined` | ❌ | 债（母体） |
| 12 | `useAssetDropPaste.ts:424-425` `extractImgFromHtml` | `docR.ok ? ... : null` | ❌ | 债（母体） |
| 13 | `agentCore.ts:209-210` `parseSSEChunk` | `if (!r.ok \|\| !r.value) return true` | ❌ | **债（高息·伪装成功）** |
| 14 | `agentCore.ts:611-613` `parseAgentError` | `if (r.ok) { ... }` 失败静默保留 fallback 文案 | ❌ | 债（母体·低） |

**口径错位量化**：留痕 2 / 静默 12。

> 注：#9 / #10 的"压平"本身在语义上**可以接受**（探测函数：坏 JSON 与"不是"对调用方答案相同）—— `drag-data.ts:64-66` 的注释自己写明了这点（"判别联合在这里零收益"）。**但同一注释紧接着写了判据**：「**但"坏 JSON"不能静默** —— 它意味着写入侧有 bug，必须留痕可查」。⇒ **本轮判据 = 不是"不许压平"，是"压平必须留痕"**。

> ⚠️ **2026-09-18 施工轮取证修正（详见 §8.1）**：该判据源出 `drag-data.ts` 的**时间轴拖拽数据**语境（那里坏 JSON 确＝写入侧 bug）；**横推到全部 12 处属推断、非取证**。实测 12 处**不同质**：3 处属探测语义（判**非债**，留痕只会把正常路径刷成噪音）· 4 处位于 logger 之下**物理不可留痕**（归位 TD-18-8）。

### 2.3 高息点链路（端到端 · 已逐跳取证）

```
agentCore.ts:209   const r = tryParse(() => JSON.parse(payload))      ← 生产者：{ok:false,error}
agentCore.ts:210   if (!r.ok || !r.value) return true;                ← 谎报：＝"已消费，正常"
   ↓
agentRuntime.ts:461  if (!parseSSEChunk(chunk, acc)) { tryParseNonStreamJsonFallback(chunk) }
                     ⇒ 因返回 true，兜底被跳过
   ↓
agentRuntime.ts:480  content: acc.content || ''                       ← 空串当正常回复
```

- 注释自证（`:202-203`）：「**防「流式模式下收到非流式 JSON」被静默吞掉**」—— 该守卫**只覆盖"非 `data:` 前缀"一种吞**，**没覆盖"`data:` 前缀但 JSON 坏了"这第二种吞** ⇒ **半截守卫**。
- 旁证（`:384-388`）：团队已识别"吞输出"并加 `tryParseNonStreamJsonFallback`，但入口判据取自 `parseSSEChunk` 的返回值，而该返回值**把失败与成功混为一谈** ⇒ 兜底**永不可能**在第二类吞时触发。
- 爆炸半径：AI 助手**全部流式对话**。用户看到的是"回复变短/变空"，日志无任何痕迹，无法归因。

### 2.4 类型诚实性（主题锚 09 · 病区归 18）

> **归档说明**：本笔主题属 09「类型诚实性」，但病灶文件 `base/core/utils.ts` 按本仓分区归 **18 core 横切基础设施**（前轮 TD-18-4 / TD-18-6 同文件、同为 18 区可证）。
> 故经 `debt.mjs move` 落 **TD-18-9** —— 若挂 09，index 会派生成 🟢 无债而账本实有债待还，**账实不符**（即"灯在撒谎"），与本轮主题自相矛盾。

`core/utils.ts:112-114`：

```ts
export function deepClone<T>(value: T): T {
  return (value === undefined ? undefined : JSON.parse(JSON.stringify(value))) as T;
}
```

- 签名承诺 `T`，实现是 **JSON 深拷贝**：丢 `undefined` 字段（`{a:undefined}` → `{}`）、函数丢弃、`Date`→`string`、`Map/Set`→`{}`、循环引用**抛 TypeError**。
- `as T` 是**显式假收窄**：`value === undefined` 分支返回 `undefined` 却断言成 `T`。
- 守卫形态是**注释**（`:111`「含函数/Date/循环引用者请勿用」）⇒ 无机器守卫 = 假护栏。
- 消费点：`director3d/project.ts:28`（`cloneProjectValue` re-export）· `clipboard.ts:322`。

### 2.5 格式化三兄弟（同型 · 一并归集）

| 位置 | 静默兜底 | 留痕 |
| --- | --- | --- |
| `core/utils.ts:322` `formatTime` | `Number.isNaN(d.getTime()) → ''` | ❌ |
| `core/utils.ts:346` `formatBytes` | `!isFinite(n) \|\| n <= 0 → '0 B'` | ❌ |
| `core/utils.ts:34` `toAbsoluteFileUrl` | `!url → ''`（null 与 '' 不可区分） | ❌ |

三处同一形态（非法输入 → 落空值 → 零留痕），一处修复可全消 ⇒ **按小母体登记一条**，不碎片化。

---

## 三 · 定海（本轮不审什么 · 及理由）

| 不审 | 理由 |
| --- | --- |
| `tryParse` 内部是否该打日志 | 已取证：`:256-260` 的"不打日志"是**有理由的职责划分**（循环依赖 + 留痕应由消费者定）。**判非债、留痕**，不再重审。 |
| 压平（`r.ok ? v : ''`）本身 | 探测函数压平在语义上成立（`drag-data.ts:64-66` 自证）。**判非债**；债面只限"压平 + 不留痕"。 |
| `localTool/**` 后端 | 本轮主题横推自底向上，前端消费层已命中母体；后端留作下一轮，不在此轮稀释。 |
| `base/utils/**` 其余 23 文件 | 本轮是**主题**（tryParse / 诚实性）横推，非区域全量；不假装"该区审完"。 |

---

## 四 · 切割（覆盖度表 · 增量账）

| 文件层 | 覆盖状态 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- |
| `base/utils/asyncGuard.ts` | 有债待还 | 本轮 2026-09-18 | `:232 :252-264 :44-48` | **TD-16-49**（母体 · `@见 TD-16-36`） |
| `base/core/utils.ts` | 有债待还 | 本轮 2026-09-18 | `:54-59 :77-83 :332-333 :112-114 :322 :346 :34` | **TD-16-49 / TD-18-8 / TD-18-9** |
| `agent/runtime/agentCore.ts` | 有债待还 | 本轮 2026-09-18 | `:209-210 :202-203 :610-613` | **TD-16-50**（高息）· TD-16-49 |
| `agent/runtime/agentRuntime.ts` | 抽审 | 本轮 2026-09-18 | `:461 :474 :480` | 已对账（消费侧受害点，非病灶） |
| `agent/runtime/useAgentChat.ts` | 抽审 | 本轮 2026-09-18 | `:29 :254 :520` | 已对账（纯 re-export 链） |
| `hooks/useAssetDropPaste.ts` | 有债待还 | 本轮 2026-09-18 | `:148 :302-312 :369-370 :424-425` | **TD-16-49** |
| `hooks/useResourceMoveToFolder.ts` | 已裁定非债 | 本轮 2026-09-18 | `:66-69`（`logger.warn` 留痕） | **正面样板** |
| `videoEditor/engine/lib/drag-data.ts` | 已裁定非债 | 本轮 2026-09-18 | `:67-70`（`logger.warn` 留痕） | **正面样板**（且为本轮判据出处 `:64-66`） |
| `base/store/accountsStore.ts` | 有债待还 | 本轮 2026-09-18 | `:320-321` | **TD-16-49** |
| `base/storage/storageQuota.ts` | 有债待还 | 本轮 2026-09-18 | `:172-173` | **TD-16-49** |
| `director3d/project.ts` | 抽审 | 本轮 2026-09-18 | `:28`（`cloneProjectValue = deepClone`） | TD-18-9 消费点 |
| `base/utils/clipboard.ts` | 抽审 | 本轮 2026-09-18 | `:322`（`deepClone`） | TD-18-9 消费点 |

**范围声明（防"假装审完"）**：本轮审 12 文件，全部由「`tryParse(` / `deepClone` 引用」这**一条主题线**牵引得出，**不构成 16 区全量覆盖**。`base/utils/**` 子树共 25 文件（前轮已声明 25/25 完备），本轮**仅复核其中 2 个**（`asyncGuard` 深审 / `clipboard` 抽审）—— 前轮结论**未被推翻**（前轮审的是"静默 catch"，本轮审的是"判别联合消费"，**不同面**），故 25/25 的覆盖状态**继续有效**，本轮不重设。

---

## 五 · 探债（明细）

| ID | 决策点/问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-16-49 | `tryParse` 判别联合在**消费层被压平且零留痕**：全仓 14 个消费点中 12 个 `r.ok ? r.value : <空值>` 丢弃 `error` 且无 warn/toast/上报 | 增债 | 结构 | 16/18/11/02/13/21 六区 · 12 处 | 中 | **TD-16-36 半截收口**：只改生产者（签名），未同步改消费者；`asyncGuard.ts:261` 自写反模式判据却**无机器守卫** | 见下方 A/B/C（**需用户拍板是否加闸**） | 待还 |
| TD-16-50 | `agentCore.ts:210` 把 `tryParse` 失败翻译为 `return true`（＝已消费/成功）⇒ `resolveBody:461` 跳过非流式兜底 ⇒ 坏 SSE chunk 静默蒸发 ⇒ `:480` 空串当正常回复 | 增债 | 结构（`@见 11`） | AI 助手**全部流式对话** | **高** | 「吞输出」守卫**只堵了"非 data: 前缀"一半**，未堵"data: 前缀但 JSON 坏"；且入口判据取自一个**把失败混为成功**的返回值 | ① `parseSSEChunk` 增第三态（区分 `notSSE` / `consumed` / `malformed`）；~~② `resolveBody` 在 `malformed` 时**也**触发 `tryParseNonStreamJsonFallback`~~ **〔2026-09-18 取证作废：该兜底首行是 `!t.startsWith('{')`，对 `data:` 开头的 malformed chunk 是空转〕**；③ 收尾零可读增量且有 malformed 时不可静默返回空串（**已裁定：抛错**，文案由生产者发布） | **[已解决]**（见 §八 施工轮） |
| TD-18-8 | `core/utils` 格式化三处非法输入静默落空值零留痕：`formatTime:322`（NaN→`''`）/`formatBytes:346`（NaN·负→`'0 B'`）/`toAbsoluteFileUrl:34`（null 与 `''` 不可区分） | 增债 | 结构 | 格式化输出全消费方 | 低 | 同一形态抄 3 份，各自就地兜底，无统一留痕口径 | 三处统一改经 `reportDegrade`（logger 留痕 + 节流 toast）或把入参收窄为**已校验类型**（非法值在入口抛，不在出口兜） | 待还 |
| TD-18-9 | `deepClone<T>(value:T):T` 签名承诺 `T`，实现是 JSON 深拷贝（丢 `undefined` 字段 / `Date`→string / `Map·Set`→`{}` / 循环引用抛错），`as T` 为显式假收窄；约束只写在注释（`:111`） | 增债 | 结构（`@见 18`） | `director3d/project.ts:28` · `clipboard.ts:322` | 低 | 类型层未表达"JSON 可序列化"边界 ⇒ 消费者按 `T` 预期使用 | ① 签名收窄为 `deepClone<T extends JsonValue>(v:T):T`（编译期拒绝不可 JSON 化的入参）；② 或改名 `deepCloneJson` 让语义自证；③ 二选一后删 `:111` 注释约束（类型即文档） | 待还 |

### TD-16-49 偿还计划 A/B/C（涉及"是否升机器守卫" —— 2026-09-17 用户曾裁定删除 `check:catch` 闸，故须拍板）

- **A（不加闸 · 对齐既有裁定）**：在 `asyncGuard` 增「压平即留痕」的配套原语（如 `tryParseOr(parser, fallback, 留痕)`），逐处改造 12 处；靠 code review + 本仓判据约束。**代价**：与 `asyncGuard.ts:261` 现状同款 —— 判据在注释里，无守卫 ⇒ 已知会回潮。
- **B（加闸 · 推荐）**：A 的改造 + 加一条 `check:parse-flatten` 闸：禁止 `r.ok ? r.value :` 裸三元出现在**未携带留痕调用**的分支（可先白名单 + 只增不减）。**代价**：新增一条机器规则（与该日"删闸"裁定相反，需用户明确许可）。
- **C（改签名 · 止血最快）**：取消判别联合，改回 `tryParse` **失败即抛**，让"压平"在**类型层不可能存在**。**代价**：探测类调用点（#3-#12 多数）本就要吞，会倒逼它们显式写 `try/catch`，改动面反而更大，且退回 TD-16-36 之前形态。

---

## 六 · 结论

1. **母体检出并坐实**：`tryParse` 是「生产者已诚实、消费者错位」的**样板**。收口做了一半（TD-16-36 只动签名），**诚实性在最后一跳被抹掉**。一个修复（消费层统一留痕口径）可全消 12 处 ⇒ 母体成立，不碎片化登记。
2. **判据沿用仓内自述**（`drag-data.ts:64-66`）：**压平可以，不留痕不行**。本轮全部 4 笔均以此为尺。
3. **最贵的一笔是"谎报"而非"静默"**：TD-16-50 不只是没留痕，是把失败**报成成功**，直接导致 AI 回复静默截断且不可归因 —— 利息**高**，建议优先偿还（可与母体并行，不必等母体收口）。
4. **两个正面样板已留痕免重审**：`drag-data.ts` / `useResourceMoveToFolder.ts`，后续同类改造**照抄这两处**即可。
5. **DATAFLOW 对账**：本轮未触及链路拓扑变更 ⇒ **已对账一致**，`spec/DATAFLOW.md` 不改。

---

## 七 · 状态

- **区域 16 状态**：`已验证(有债待还)`（新增 **TD-16-49**（母体）/ **TD-16-50**（高息））
  - ↳ **2026-09-18 施工轮（§八）**：**TD-16-49 / TD-16-50 已结清**；TD-18-8 / TD-18-9 仍待还（属 18 区）。
- **区域 18 状态**：`已验证(有债待还)`（新增 **TD-18-8** / **TD-18-9**；原登记为 TD-16-51 / TD-09-5，经 `debt.mjs move` 归位到病灶文件所属区）
- **区域 09（主题锚）**：本轮**无 09 区新债**，index 灯保持 🟢 为**真**（deepClone 一笔已归 18，理由见 §二.4）
- **覆盖账**：本轮审 12 / 涉及 12 / **未审 = `base/utils/**` 其余 23 文件**（主题横推，非区域全量；前轮 25/25 结论未被推翻）
- **证据闸**：每笔债均已挂 `文件:行号` ✅
- **测试执行次数**：0 ✅（本轮为只读审计）
- **改动**：`src/**` 零改动 ✅

---

## 八 · 施工轮（2026-09-18 同日）—— 收口落地

> **性质**：§五 探债的**施工轮**（同一主题 · 同一文件 = 结论唯一落点）。
> **裁定前置（Step 5）**：用户答「如果你想加闸，你能过 6 问么」→ 逐问作答，**第 2 问（有更根本手段：收口原语）与第 6 问（"压平是否携带留痕"不可机器验证 ⇒ 声索靠人解释 = 形态④）过不了** ⇒ **不加闸**（与 2026-09-17 删 `check:catch` 同向），改走 §五 **选项 A**。
> **失败呈现**（用户裁定）＝ **用户友好 + 可见**：取证确认 `useAgentChat.ts:833-841` 捕获异常后 `setError(e.message)` + 状态机 `failed`（可重试），故"抛一条带可读文案的错"正好落到用户可见且可行动。

### 8.1 取证修正（A7 回改原文）

| # | §二/§五 原文 | 取证结论（实测） | 处置 |
| - | --- | --- | --- |
| 1 | TD-16-50 偿还计划 **②**「`resolveBody` 在 malformed 时**也**触发 `tryParseNonStreamJsonFallback`」 | **无效**：该兜底首行 `if (!t.startsWith('{')) return false;`（`agentRuntime.ts:392`），而 malformed chunk 以 `data:` 开头 ⇒ 调用也是空转 | **删除该步骤**（已在 §五 就地标注） |
| 2 | 「12 处同一判据（压平必须留痕）」 | **推断非取证**：`drag-data.ts:64-66` 的判据是**针对时间轴拖拽数据**写的（那里坏 JSON 确＝写入侧 bug）。实测 12 处**不同质**，须逐处判 | 见 8.3 |
| 3 | §五 TD-16-49「逐处改造 12 处」 | 其中 **4 处物理不可留痕**（见 8.2） | 归位 **TD-18-8** |

### 8.2 硬约束取证：依赖环（决定原语落点）

实测 `logger.ts → core/utils.ts → asyncGuard.ts`（logger 依赖 core/utils 的 `formatTime`，`logger.ts:25`）：
- ⇒ 「压平即留痕」原语**不能**放 `asyncGuard.ts`（成环），落 **`degrade.ts`**（留痕层的家）；
- ⇒ `core/utils.ts`（3 处）与 `asyncGuard.ts`（1 处）**位于 logger 之下，物理上无法留痕** ⇒ 不属本原语适用范围，属 **TD-18-8 的契约收窄面**（非法值在入口抛 / 入参收窄为已校验类型），不靠"出口兜 + 留痕"。

### 8.3 逐处处置（12 处 → 4 改 / 3 判非债 / 4 归位 / 1 由 TD-16-50 收口）

| 站点 | 处置 | 依据 |
| --- | --- | --- |
| `drag-data.ts:67-70` · `useResourceMoveToFolder.ts:66-69` | 免动（正面样板） | 已 `logger.warn` |
| `core/utils.ts:54 / :77 / :332` · `asyncGuard.ts:44` | **归位 TD-18-8** | 物理不可留痕（8.2） |
| `useAssetDropPaste.ts:148` `isCanvasGroupJson` | **判非债** | 探测语义：粘贴到非 JSON 是**正常路径**，留痕＝把正常路径刷成噪音（§二.2 注自证「压平语义成立」） |
| `useAssetDropPaste.ts:369` `handleTextPaste` | **判非债** | 同上（剪贴板文本常常不是 JSON） |
| `useAssetDropPaste.ts:424` `extractImgFromHtml` | **判非债** | 同上，且其后有正则兜底 |
| `accountsStore.ts:320` `hostOf` | **改委托 `tryParseOr`** | 解析失败 ⇒ 该账号域匹配静默失效，是真信号 |
| `storageQuota.ts:172` `mapKeyToDomain` | **改委托 `tryParseOr`** | 自有登记表的 pattern 编译失败 = 写入侧 bug |
| `useAssetDropPaste.ts:302` 素材拖入 | **改委托 `tryParseOr` + toast** | 失败＝**用户无感**（拖了没反应），必须留痕 + 提示 |
| `agentCore.ts:640` `parseAgentError` | **就地 `logger.warn`**（不套原语） | 语义不同：保留的是状态码文案，不是"压平到某个值" |
| `agentCore.ts:209` `parseSSEChunk` | **由 TD-16-50 收口**（三态判别联合） | 见 8.4 |

### 8.4 TD-16-50 施工：SSE 解析失败诚实

- **生产者给全**（`agentCore.ts`）：`parseSSEChunk` 返回 `SSEChunkOutcome` 三态（`notSSE` / `consumed` / `malformed{message,error,payload}`）；文案由生产者导出（`SSE_MALFORMED_MESSAGE` / `SSE_STREAM_UNREADABLE_MESSAGE`），**消费者零自造**（Step 4 三铁律①②）。
- **消费者只转发**（`agentRuntime.resolveBody`）：`malformed` 聚合计数 + 首条证据 → `logger.error` **原样透传生产者的判词**（对齐非流式 `:359-364` 同口径判据 = **一归**）；聚合一次，不逐块刷屏。
- **不伪装成功**：整条流零可读增量（无 content / reasoning / 可用 tool_calls）却有坏 chunk ⇒ **抛错**（文案取生产者导出）→ 用户可见且可行动。
- **既有语义保留**：单条 malformed 且流有内容 → **不阻断**（既有测试「单条解析失败不影响整体流」逐字仍绿）。

### 8.5 先红后绿（`probe.mjs`，注入即自动还原）

```
🔬 探针结论｜TD-16-50 行为探针（还原旧 boolean 语义：malformed→已消费）
   文件   : src\components\agent\runtime\agentCore.ts（命中 1 处，已还原 sha=71f1182a61b9）
   命令   : npx vitest run tests/unit/agentLogic.test.ts tests/unit/agentRuntime.test.ts
   观测   : exit=1｜输出 55 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「expected 'consumed' to be 'malformed'」 — 命中
```

```
🔬 探针结论｜TD-16-49 行为探针（还原旧「压平不留痕」）
   文件   : src\components\base\core\degrade.ts（命中 1 处，已还原 sha=a22fbbab0c13）
   命令   : npx vitest run tests/unit/confirmPersist.test.ts
   观测   : exit=1｜输出 31 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「tryParseOr：压平即留痕」 — 命中
```

### 8.6 度量（改前 / 改后可比）

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| `parseSSEChunk` 返回值承载的状态数 | 2（boolean，把 malformed 混进 consumed） | **3**（判别联合，写错编译不过） |
| 「失败伪装成功」出口 | 1（malformed → `true` → 跳过兜底 + 零留痕） | **0** |
| 「压平即留痕」判据实现份数 | 样板 2 份手写 + 12 处裸三元（判据只在注释） | **1 份原语**（`tryParseOr`）+ 样板免动 |
| `tryParse` 消费点零留痕数 | 12 | **4**（且已归位 TD-18-8，非遗留） |
| 流式失败留痕判据份数 | 1（仅非流式有） | **1（两路同口径 · 一归）** |
| 新增兜底 | — | **0** |
| 改动半径 | — | 6 个 src 文件 + 3 个测试文件 |

### 8.7 验证（实跑）

`tsc --noEmit` ✅ 0 错 · `check:any` ✅ 0 违规 · `check:strict-src` ✅ 0 隐式 any · `check:arch` ✅ · `check:dead-code` ✅ 无新增 · `eslint`（9 个改动文件）✅ 0 错 · 单测 **122 passed**（confirmPersist 6 / accountsStore 31 / storageQuota 22 / useAssetDropPaste 29 / agentLogic 26 / agentRuntime 8）。

### 8.8 交付四闸自查

| 闸 | 证据 |
| --- | --- |
| **一归** | 流式/非流式「原文非空但解析不出内容」留痕判据 = 1 份同口径；「压平即留痕」= 1 份原语 |
| **减一** | 新增兜底 0；**删**了一处「失败伪装成功」（`return true`） |
| **减二** | 三态契约替代 boolean；12 处裸三元 → 4 处委托 1 份原语 |
| **一诚实** | 判别联合 + 根部炸开（生产者判 malformed / resolveBody 抛错）+ 读者正确（开发者 logger / 用户 setError 可重试） |

### 8.9 状态

- **区域 16**：`已验证(有债待还)` → 本轮 **TD-16-49 / TD-16-50 结清**（其余待还债不变）。
- **区域 18**：TD-18-8 **描述修正**（补第 4 处 `asyncGuard.sameOriginUrl` + 注明"物理不可留痕 ⇒ 须走契约收窄"）。
- **区域 09（主题锚）**：无新债，🟢 为真。
- **DATAFLOW**：链路**拓扑**未变（新增的是失败分支，不新增节点/边）⇒ 已对账，不改。
- **弯路留痕**：① 初版把留痕写在**消费层**（`resolveBody` 自拼 `logger.error` 文案）＝ **越权**（错误须由生产者发布）；用户裁定后改为「生产者导出文案 + 消费者只转发」。② 初版用 `Extract<SSEChunkOutcome,{kind:'malformed'}>` 承接闭包赋值，被 TS 窄化为 `never` ⇒ 改用 holder 对象。
- **改动**：`src/**` 6 文件（agentCore / agentRuntime / degrade / accountsStore / storageQuota / useAssetDropPaste）+ 测试 3 文件。
