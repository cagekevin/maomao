# 架构日志 · 04 画布/节点（跨区） · 2026-09-23

> 本轮：清偿 04 区全部 11 条待还债（9 条真做 + 2 条改判），并就地修正 `spec/判据登记表.md`。
> 判据来源：用户裁定「收口前数清中心数、收口后再数一次；必须对照 DATAFLOW + 判据登记表」。

## 一、链路快照（refs 实证，非凭文件名猜）

```
src/types/asset.ts（媒体类型目录真源）
  ← src/types/index.ts（barrel，模块引用 2 之一）
  ← tests/unit/mediaRef.test.ts
  本轮新增导出 isMediaRefType（唯一收窄守卫），消费 6 处：
    assetType.ts:111 · useConnectedInputs.ts:210／228 · nodeMedia.ts:64
    · canvasSource.ts:92 · useAssetDropPaste.ts:428

src/components/canvas/shell/promptChipFormat.ts（芯片格式契约真源，零依赖叶子）
  ← promptChips.ts（DOM 工具层，模块引用 2 之一）
  ← creativePresets.ts（纯逻辑）
  本轮新增写侧构造函数 buildChipText

canvas/contract（nodeDataSchema · nodeDefaults · nodePrefs）
  ← 各表键现受 contracts.NodeTypeKey 编译期约束
```

**DATAFLOW 对照**（§六 :374 明文「三张表别混」／§一 :82-83 判型／§七 :402 芯片唯一入口）：
本轮改动**未改任何链路的节点或边**，只把同一判据的 N 份载体收为 1 份 ⇒ DATAFLOW 现状图无需改（已核对）。

## 五、探债（本轮清偿明细 · 真源 = `scripts/debt.mjs`）

| TD-ID | 中心数（改前 → 改后） | 处置 |
| - | - | - |
| TD-04-60 | **7 → 1** | 收口 `isMediaRefType`（asset.ts:54）；登记表原只记 3 处，实测漏登 `assetType.ts:111`／`canvasSource.ts:92`／`useAssetDropPaste.ts:428` |
| TD-04-63 | **2 → 1** | `TASK_TYPE_META` 单目录（taskStore.ts:228）派生 `typeLabel` + `taskMediaKind` |
| TD-04-65 | **2 → 1** | `buildChipText` 唯一构造函数（promptChipFormat.ts:62），`encodeThumb` 一并下沉 |
| TD-04-66 | **6 → 1** | `NodeTypeKey` 编译期上界（contracts.ts:868）统一约束 6 处子集 |
| TD-04-68 | **2 → 0** | 注释改指 `lod.tsx:70`、不再复述阈值数值 |
| TD-04-70 | **2 → 1** | `taskResultExt`（taskStore.ts:256）派生下载扩展名 |
| TD-04-71 | **2 → 1** | `createNewShot` 派生自 `defaultShotFields`；5 处预填值差异**显式化** |
| TD-04-67 | 改判非债 | 理由 = DATAFLOW §六 :374 **明文分缝** + `check:node-data --strict` 对账（含 `parserBlind` 自检）；与判据登记表 §三 #4 同一判据 ⇒ §八 #6 改为指向 §三 #4 |
| TD-04-69 | 改判非债 | 理由 = 撤销栈活态（useCanvasHistory.ts:12）vs 持久化规范读取（projectStore.ts:48）**两个独立关注点**、不在同一链路数据流上 |
| TD-04-42 · TD-04-50 | 改判非债 | 2026-09-20 已裁「低息不修」，账本状态未收口 ⇒ 本轮补办 |

**债描述被修正处**：
- `TD-04-71` 原解法「新建默认与 schema 对齐」**判为错**：两者是「新建引导预填」vs「补齐旧数据基线」**两个角色**（同 §八 #5 的裁法）；正解 = 字段清单收口 + 值差异显式化，**不强行统一值**（统一任一方向都会让用户看到或旧数据补齐行为变化）。
- `TD-04-60` 原描述漏登 3 处（见上表）。
- `TD-04-67` 原标「无单一权威类型 ⇒ 未收口」，**结论对、理由错**：真正理由是 §六 明文分缝 + 有闸，**不是**「价值低／边缘」。

## 六、覆盖度表

| 板块 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- |
| §三 画布与节点（22 条） | 深审 | 🟢 | 2026-09-23 | 逐行核真源 file:line + N | TD-04-60／66／68 已清；#4／#17 裁定非债 |
| §八 文本与任务中心（12 条） | 深审 | 🟢 | 2026-09-23 | 同上 | TD-04-63／65／67／70／71 已清 |

**覆盖账**：本轮只动这两板块涉及的 9 条债；其余板块不变（上轮结果沿用）。

## 七、结论与下一步

- 本区域状态：`已验证(无债)`。
- 待还债项已流入 `daily/架构日志/债务.md`：否（04 区清零）。
- DATAFLOW.md：已对账一致（链路未变）。
- 灯：覆盖度表已更新（🟢）；区域级灯由 `index.md` 自动派生。

## 八、交付闸自查（本轮有改动）

| 闸 | 本轮结论 | 可复现凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | 成立：6 条债的中心数全部归 1（见 §五） | 见 §五表 + 各 `grep`（下方「收口实证」） |
| 减一 · 兜底净减 | 本轮无兜底增减（0 → 0），未新增静默 catch／默认值／假成功 | `grep -rn "catch"` 改动文件：零新增 |
| 减二 · 复杂度实减 | 成立：重复份数 7→1 · 2→1 · 2→1 · 6→1 · 2→1 · 2→1；`nodeMedia.getNodeMedia` 净减 4 行 | 见 §五表（改前/改后并列） |
| 一诚实 · 失败诚实可见 | 不适用（本轮无失败路径改动）；探针「先红」已真红 | 下方探针归档 ① ③ ⑤ |
| 零炸 · 防回归防副作用 | 成立：260 项相关测试 + 6 道闸全绿；未顺手改好任何无关行为 | 下方「反向探针」 |
| 一凭证 · 证据可复现 | 5 个探针结论块 + 反向探针块全部可复现 | 下方「探针归档」 |

**收口实证（改后仅剩 1 处）**：
```bash
$ grep -rn "=== 'image' ||" src/            # 同款三值判
（仅剩 videoEditor 独立域 / AssetNode 图标四分支 / pollTask 二类子集等**不同判据**）
$ grep -rn "isMediaRefType" src/ | grep -v types/asset.ts
assetType.ts:111 · useConnectedInputs.ts:210／228 · nodeMedia.ts:64
  · canvasSource.ts:92 · useAssetDropPaste.ts:428      # 6 处消费，全部委托
$ grep -rn "encodeThumb" src/                # 仅真源内
promptChipFormat.ts:49 · :63
```

**探针归档（SOP §7.1 三项：①结论块 ②注入语义一行 ③还原后绿）**：

① TD-04-60 注入：`isMediaRefType` 体 → `return false;`
```
🔬 探针结论｜TD-04-60 媒体形态真源失效
   文件   : src/types/asset.ts（命中 1 处，已还原 sha=c9d035b14bd2）
   命令   : npx vitest run tests/unit/mediaType.test.ts
   观测   : exit=1｜输出 21 行
   ✅ 退出码 == 1 — 实际 1
```
② TD-04-63 注入：`return taskTypeMeta(type)?.media || 'none';` → `return type === 'video' ? 'video' : 'none';`
```
🔬 探针结论｜TD-04-63 type 目录失效（丢别名）
   文件   : src/components/base/store/taskStore.ts（命中 1 处，已还原 sha=6cbb7c618025）
   命令   : npx vitest run tests/unit/taskStore.test.ts
   观测   : exit=1｜输出 23 行
   ✅ 退出码 == 1 — 实际 1
```
③ TD-04-65 注入：`buildChipText` 体 → `return \`@{${id}:${label}}\`;`
```
🔬 探针结论｜TD-04-65 芯片构造函数失效（丢 thumb 段）
   文件   : src/components/canvas/shell/promptChipFormat.ts（命中 1 处，已还原 sha=d0920cfae96c）
   命令   : npx vitest run tests/unit/promptChips.test.ts
   观测   : exit=1｜输出 135 行
   ✅ 退出码 == 1 — 实际 1
```
④ TD-04-66 注入：`textGenerateNode:` → `ghostNodeXyz:`（幽灵类型键）
```
🔬 探针结论｜TD-04-66 子集上界失效（注入幽灵类型键）
   文件   : src/components/canvas/contract/nodeDataSchema.ts（命中 1 处，已还原 sha=184587c98d88）
   命令   : npx tsc --noEmit
   观测   : exit=2｜输出 1 行
   ✅ 退出码 == 2 — 实际 2
   ✅ 输出含「error TS」 — 命中
```
⑤ TD-04-71 注入：`...defaultShotFields(),` → `// probe: 字段基线移除`
```
🔬 探针结论｜TD-04-71 字段基线失效
   文件   : src/components/scriptbox/scriptBoxPrompts.ts（命中 1 处，已还原 sha=6b0f3c83133d）
   命令   : npx vitest run tests/unit/scriptBoxPrompts.test.ts
   观测   : exit=1｜输出 31 行
   ✅ 退出码 == 1 — 实际 1
```
③ 还原后绿（全批一次跑完）：
```
$ npx vitest run <10 个相关测试文件>
 Test Files  10 passed (10) · Tests  260 passed (260)
```

**反向探针（零炸）**：
```
$ node scripts/check-arch.mjs        → ✅ 架构校验通过
$ node scripts/check-node-types.mjs  → 通过（613 文件）
$ node scripts/check-node-handles.mjs→ 通过（613 文件）
$ node scripts/check-dead-code.mjs   → 67 = 基线，无新增
$ node scripts/check-any-honesty.mjs → 0 违规（620 文件）
$ node scripts/check-node-data.mjs   → 字段缺口 0 处
```

**零炸三问**：
1. **哪些行为被改了？** 仅 `createNewShot` 补齐此前缺失的 `imgGenLoading:false`（原两工厂字段集分叉的修复）；其余 8 条**零行为变化**（集合等价 / 值等价）。
2. **改前能走的路径仍能走吗？** 是 —— 260 测试 + 6 闸全绿。
3. **改前会失败的仍失败吗？** 是 —— 未顺手修任何无关失败（如 videoEditor 域的同类判据**明确留着**，登记为跨板块另开轮次）。

## 九、决策留痕

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度（→ §八「减二」） | SSOT（→ §八「一归」） | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 媒体形态守卫落在 `src/types/asset.ts`（真源处） | 契约定义在拥有真相的层 | 消费方只委托、不自造 | 7 → 1 | 1 份 | 做 |
| 2 | `NodeTypeKey` 用**类型层**而非新增机器闸 | 编译期早于运行期 | 全集归 contracts 定 | 6 → 1 上界 | 1 份 | 做（建闸前置评审优先级 2） |
| 3 | `TD-04-71` **不**强行统一两套默认值 | 两个角色本就不同值 | 值归各场景、字段清单归 schema | 字段 2 → 1 | 1 份字段清单 | 做（值差异显式化） |
| 4 | `TD-04-67`／`TD-04-69` 改判非债 | 理由可复算（§六 明文分缝 / 两独立关注点） | — | 无改动 | 判据登记表去重 | 不做（改判） |
| 5 | 非债裁定须从固定角度择一给依据 | 「价值低」是**结论复述**非理由；非债无产物却是唯一能让线索永久离开主表的决策 | — | — | — | 已固化 → `ADR-0058`（五角度表：链路位置／判据性质／有机器守／代价倒挂／运行不可达） |
