# 18 · 跨区 · ADR 判据合规横推（2026-09-18）

> 触发：用户要求「按 ADR 查还有哪些违反 ADR 的」。
> 范围：`docs/adr/` 现行 24 条（0010/0017/0020/0023/0024 已被取代，不在本次范围）× `src/**` 代码实况。
> 方法：逐条取 ADR 的「📌 违反时的判据」→ 转成可复现检索 → 命中处回读真实代码定性。

## 一 · 探债

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话) | 状态 |
| -- | ---------------------- | -------- | ---------- | -------- | ---- | ---------------- | ---------------- | ---- |
| TD-18-10 | `videoEditor/utils/browser.ts:1-13`（`isTypableDOMElement`）；消费 `keybindings-store.ts:7,170,179` | **SSOT 第二份 + 口径错位**：与唯一判据 `base/core/uiHooks.ts:16 isEditableTarget` 各写一份，且口径互异 | 结构 / 22 视频 + 18 core | 键board 监听层：视频剪辑器内输入框焦点判定与全库不一致 | 高 | **母体**（可编辑元素判据未收口到单点） | 收口到 `isEditableTarget` 单点 | **已裁定非债**（ADR-0031） |
| TD-18-11 | `videoEditor/stores/media-preview-store.ts:24` | 手写 `closest("input, textarea, [contenteditable='true']")` = 第三份同语义判据 | 结构 / 22 视频 | 同上，判据份数 ≥3 | 中 | 同上母体（症状） | 同上 | **已裁定非债**（ADR-0031） |
| TD-18-12 | `director3d/panels/Timeline.tsx:382` | 手写 `target.tagName === 'INPUT'` = 第四份（只判 INPUT，漏 TEXTAREA/contenteditable） | 结构 / 07 3D | 3D 域快捷键在 textarea/contenteditable 内不归编辑器 | 中 | 同上母体（症状） | 同上 | **已裁定非债**（ADR-0031） |
| TD-18-13 | `useAssetDropPaste.ts:119` 注释自称「判据现在是 `uiHooks.ts::isEditableTarget` 的**唯一一份**」 | **注释漂移 + 假护栏**：声明唯一，实际 ≥4 份（TD-18-10/11/12 实证） | 结构 / 18 core | 误导后人以为已收口，不再复核 | 中 | 母体配套（声索与实况不符） | 随收口一并更正注释 | **已裁定非债**（ADR-0031） |

> ⚠ **TD-18-10 / 11 / 12 / 13 四笔已由用户裁定改判「非债」（2026-09-18 · ADR-0031）—— 我判错了。**
> 我原判「可编辑元素判据 4 份 = SSOT 第二份、应收口到 `isEditableTarget`」。ADR-0031 的二分推翻了它：
> **探测重复**（怎么读数据）必须收口；**判据重复**（要不要放行／动作）**严禁合并**。
> 四处差异均为域内必需：`browser.ts` 判 `disabled`（"能不能真打字"）· `Timeline.tsx:382` 多 `SELECT`
> （下拉框内 Delete 不该删关键帧）· `use-keybindings.ts:44` 判 `document.activeElement` 而非 `ev.target` + 自由区。
> 收口任一处都是**负收益**（引入真 bug／偷换语义／零收益非零改动半径）。
> **根因**：我顺着 ADR-0029 §决议6「域外副本待收口（不在本轮）」的尾巴照办 —— 而那条尾巴本身立错了。
> ADR-0031 §Lessons Learned 原文：「**『待收口』是一张会自己兑现的欠条**」。
| TD-18-14 | `useCanvasShortcuts.ts:65`（`catch { // catch-ok: BROWSER_API return false }`）；同类共 9 处：`accountsStore.ts:293,418,437` · `storageQuota.ts:155` · `storageAdapter.ts:48,67` · `VideoThumbnail.tsx:80,91` | **ADR-0011 判据 3**：同一 catch-ok 文案逐字重复 ≥3 处、未收口为原语；其中 `useCanvasShortcuts.ts:65` 与 `accountsStore.ts:296` 失败**零留痕**（前者伪装"无选区"，后者 `return null` 混同"抓不到"与"调用失败"） | 结构 / 横切 | 浏览器 API 失败全仓不可见 | 中 | **母体**（豁免语义未收口成原语，见 `asyncGuard.ts` 已有 RELEASE_FAIL/NON_BLOCKING 先例） | 收口为「浏览器 API 预期内失败」原语（自带留痕） | 待还 |
| TD-18-15 | `base/canvas/nodeDataSchema.ts:120` | **ADR-0005 判据 3**：裸 `structuredClone(defaults)` 拷贝业务对象，绕过唯一入口 `core/utils.ts:131 deepClone` | 结构 / 18 core | 第三份深拷贝实现；`deepClone` 未来加约束时此处不生效 | 低 | 孤例（唯一入口未贯通） | 改用 `deepClone` | 待还 |
| TD-18-16 | `core/utils.ts:59,83,400` + `asyncGuard.ts:48`（`r.ok ? r.value : ''/null/false`） | **判据张力待裁定**：ADR-0002 要求压平失败分支留痕；ADR-0003 要求 logger 之下失败只由返回值表达。二者在此处指向相反动作 | 结构 / 18 core | 全库底层压平点口径不统一 | 低 | 待裁定（两条现行 ADR 冲突，需用户裁定优先） | A/B/C 见下 | 待还 |

| TD-18-17 | `useAssetDragToCanvas.ts:44-53`（`fetchText`）；导出 `:151`，消费于 `ResourceLibrary.tsx:100` · `ResourcePreview.tsx:36` · `GeneratedView.tsx:90` · 本文件 `:87` | **ADR-0011 判据 2 + ADR-0002**：`.catch(() => '')` **裸吞**（连 `catch-ok` 标记都没有）＋ `r.ok ? r.text() : ''` 压平无留痕；且失败值 `''` 被 `textCache.set(url, '')` **写进缓存固化** | 结构 / 03 资产 + 18 core | 文字素材拖拽内容静默为空：4 处消费；失败永久不可见，**缓存被污染后重试也不恢复** | 高 | 孤例（裸吞 + 污染缓存，比 TD-18-14 更甚） | 失败值不写缓存 + 降级留痕（正面样板 `filesApi.ts:439`） | 待还 |

| TD-18-20 | `spec/CONTEXT.md:53,57,103,113,156`（同类约 10 处） | **ADR-0025 判据 1/2（母体）**：CONTEXT 出现判决句（禁止/必须/不得）而 `adr.mjs search <关键词>` **0 命中**（实测：越层 0 · NodeShell 0 · 平铺 0 · 裸读 0 · 网络错误判定 0） | 结构 / 元层（文档归位） | 判据埋在 47KB 的 CONTEXT 里：无 ADR 索引、无机器守卫 ⇒ 后人检索不到、审计也捞不着 | 中 | **母体**（判据归位未做全：对照「幂等」6 条 /「兜底」8 条**已**归位，且 CONTEXT:176 已有「幂等不许替代状态 → ADR-0010」的**正确路由表写法**） | 逐条迁判据进 ADR，CONTEXT 只留一行指针 | 待还 |

### TD-18-16 · 业务性待拍板（A/B/C 选项标题）

- **A · ADR-0002 优先**：所有 `r.ok ? … : …` 压平点补留痕调用（违反 ADR-0003「logger 之下不记日志」的字面）。
- **B · ADR-0003 优先**：底层原语维持返回值表达，视为合规；仅要求 JSDoc 写明「失败态与空态是否可区分」。
- **C · 收窄 ADR-0003 适用层**：仅 `core/utils.ts` 纯函数层豁免，其余层压平必须留痕。

## 二 · 已裁定非债（留痕，禁再当 bug 补）

| 对象 | 判据 | 结论与证据 |
| ---- | ---- | ---------- |
| ADR-0018 重试边界 | 判据 1/2/3/4 | **合规**：`httpClient.ts:261-264` 仅 `NetworkError`/超时/`isFetchNetworkTypeError` 可重试，`HttpError` 不重试；`relayProxy.ts:99` 建任务 POST 显式 `retries: 0`；`localToolApi.ts:116-157` 等写按 id 幂等（ADR-0026 唯一键=资源 id）。`isNetworkError` 无 TypeError 默认放行（`:274` 走 message 白名单） |
| ADR-0006 canvas 唯一出口 | 判据 1/2 | **合规**：`toDataURL(`/`toBlob(` 仅出现在 `core/utils.ts:184,218`（唯一出口）；`src/**` 内无 `startsWith('data:image/')` 第二份判据 |
| ADR-0007 原引用闭包捕获 | 判据 1/2/3 | **合规**：`_orig_` / `__orig` 在 `src/**` 代码里 **0 命中**（仅注释记述） |
| ADR-0009 写回唯一路径 | 判据 | **合规**：`recoverable` / `resultField` 在 `src/**` **0 命中**（仅 `useGenerateNode.ts:58` 注释记述 2026-09-18 已统一为 resultKey） |
| ADR-0011 判据 1（机制复活） | 判据 1 | **不命中**：`scripts/check-silent-catch.mjs` / `core/catchOk.ts` / `check:catch` 三者均确认不存在 |
| `asyncGuard.ts:181,209,224,283,291` catch-ok | ADR-0011 | **合规**：该处即 NON_BLOCKING / RELEASE_FAIL 的**唯一实现**（判据层），非逐处复制标记 |
| ADR-0003 格式化非法输入 | 判据 2 | **已被 TD-18-8 覆盖并清偿**：`formatBytes` 的 `n<=0` 合并问题已处置，不重复登记 |
| ADR-0015 批量入栈 | 判据 1/2/3 | **合规**：`commands.ts:33 executeBatch` 是全仓**唯一**批量原语（`:42` `commands.length === 1 ? commands[0] : new BatchCommand(...)`）；`batch-command.ts:9-22` 的循环是组合命令**单次入栈**，不是「循环单条入栈」；`timeline-manager.ts:241,361` 与 `use-editor-actions.ts:492` 均走同一入口，无第二份 |
| ADR-0008 定序 / 缺失值 | 判据 1/2 | **src 侧 0 命中**：`mtime` / `statSync` / `birthtime` 在 `src/**` 无一处；`|| -1` / `?? -1` 后接比较亦未命中 |
| ADR-0013 纯查询副作用 | 判据 1 | **合规（2 处候选均判误报）**：`d3dPersistence.ts:70 getChannel` 内 `set` 是单例缓存建立、非 store 写；`nodeRuntimeStore.ts:52 getNodeRuntime` 与 `:60 updateNodeRuntime` 是读写分离的两个函数 |
| ADR-0002 压平留痕 | 判据 | `filesApi.ts:430-441 persistInlineOrKeep` **合规**：`:439` 落盘失败调 `notifyLocalServiceDegrade()`（用户可见），属「真兜底 + 已留痕」——**它是 TD-18-17 的正面对照样板**。同族 `tableWorkspaceState.ts:198` 落 `landed:'failed'` 亦留痕 |
| ADR-0004 显示串反解析 | 判据 | **src 侧 0 命中**：`formatBytes(`/`formatDuration(`/`formatSize(`/`formatTime(` 后接 `.split(`/`.replace(` 无一处 |
| ADR-0014 跨上下文重置命令栈 | 判据 1/2/3 | **合规**：`releaseProjectContext()`（`engine/core/index.ts:78`）是唯一编排点、内含 `command.clear()`（`:82`）；调用方覆盖 `project-manager.ts:89,138,270` · `editor-header.tsx:67,86` · `EditorShell.tsx:59` · `editor-provider.tsx:110` ⇒ `command.clear` **非零调用**，无绕过路径 |
| ADR-0028 拉取反流 | 判据（人工项） | **抽审 · 机械探针 0 真命中**：`sounds.tsx:64-66` 依赖是 `[loadSavedSounds]`（非拉取结果）；`GridSplitNode.tsx:451-454` 依赖 `[data.rows]`（节点数据同步，非拉取）。语义项「意图状态是否需查表才能变成参数」须逐面板读码，留作下轮 |
| ADR-0001 既有约定当理由 | 判据 | **判非债**：`src/**` 仅 2 处命中 —— `uploadDirs.ts:19`「沿用现有值」**附了支撑证据**（避免存量 `/files/...` URL 与物理目录破链接），符合本 ADR；`agent-panel.css:854` 是配色收口注释，非判据依据 |
| ADR-0026 Date.now 作唯一键 | 判据 1/2 | **我判错了，改判为债 → @见 TD-18-18**（并行二轮已登记，本文件不重复记）。误判经过：我把 `projectStore.ts:326-327 genId()` 的 ``proj-${Date.now()}`` 归入"新建临时 id"，但它实际是**落库键**——`:333` 用它拼 `canvas-state-v1-<projectId>`；同毫秒建两个项目即**键碰撞**。教训：判"是不是键"要看**它下游被谁当 key 用**，不能只看生成处（`scriptBoxEngine.ts:1362,1499,1521`、`App.tsx:1271`、`StepAssets.tsx:216`、`canvasPlanExecutor.ts:758`），非去重键。`director3d/project.ts:359 uid()` 属 idGen 第二份，已由 **TD-18-7** 登记，不重复记 |

## 三 · 覆盖度表（增量 · 本轮新纳入 / 状态变化）

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| ------ | ---------- | --------- | --------------- | ------- |
| `src/components/base/core/uiHooks.ts` | 深审 | 本轮 | `:16-32` 读原码 | 无债（作为唯一判据基准） |
| `src/components/videoEditor/utils/browser.ts` | 有债待还(TD-18-10) | 本轮 | `:1-13` | 第二份可编辑判据 |
| `src/components/videoEditor/stores/keybindings-store.ts` | 有债待还(TD-18-10) | 本轮 | `:7,170,179` | 消费第二份 |
| `src/components/videoEditor/stores/media-preview-store.ts` | 有债待还(TD-18-11) | 本轮 | `:24` | 第三份 |
| `src/components/director3d/panels/Timeline.tsx` | 有债待还(TD-18-12) | 本轮 | `:382` | 第四份 |
| `src/hooks/useAssetDropPaste.ts` | 有债待还(TD-18-13) | 本轮 | `:119` | 注释漂移 |
| `src/hooks/useCanvasShortcuts.ts` | 有债待还(TD-18-14) | 本轮 | `:65` | catch-ok 零留痕 |
| `src/components/base/store/accountsStore.ts` | 有债待还(TD-18-14) | 本轮 | `:293,418,437`（`:296` return null） | catch-ok 复制 |
| `src/components/base/storage/storageAdapter.ts` | 抽审 | 本轮 | `:48,67` | 环境探测原语本体（判非债倾向，待下轮定） |
| `src/components/base/storage/storageQuota.ts` | 有债待还(TD-18-14) | 本轮 | `:155` | catch-ok 复制 |
| `src/components/base/ui/VideoThumbnail.tsx` | 有债待还(TD-18-14) | 本轮 | `:80,91` | catch-ok 复制 |
| `src/components/base/canvas/nodeDataSchema.ts` | 有债待还(TD-18-15) | 本轮 | `:120` | 裸 structuredClone |
| `src/components/base/core/utils.ts` | 抽审 | 本轮 | `:59,83,400` | 张力待裁定(TD-18-16) |
| `src/components/base/api/httpClient.ts` | 已裁定非债 | 本轮 | `:261-264,274` | ADR-0018 合规 |
| `src/components/base/api/relayProxy.ts` | 已裁定非债 | 本轮 | `:99` | retries:0 |
| `src/components/base/api/localToolApi.ts` | 已裁定非债 | 本轮 | `:116,128,147` | 按 id 幂等 |
| `src/components/base/utils/asyncGuard.ts` | 已裁定非债 | 本轮 | `:181,209,283` | 判据层唯一实现 |
| `src/components/agent/runtime/projectMemoryStore.ts` | 抽审 | 本轮 | `:71`（`.catch(() => {})` LOCK_CHAIN） | 待下轮定性 |
| `src/hooks/useAssetDragToCanvas.ts` | 有债待还(TD-18-17) | 本轮（第二轮） | `:44-53,151` | 裸吞 + 污染缓存 |
| `src/components/base/panels/ResourceLibrary.tsx` | 抽审 | 本轮（第二轮） | `:100` | 消费 TD-18-17 |
| `src/components/base/panels/ResourcePreview.tsx` | 抽审 | 本轮（第二轮） | `:36` | 消费 TD-18-17 |
| `src/components/base/panels/GeneratedView.tsx` | 抽审 | 本轮（第二轮） | `:90` | 消费 TD-18-17 |
| `src/components/base/api/filesApi.ts` | 已裁定非债 | 本轮（第二轮） | `:430-441` | 留痕兜底正面样板 |
| `src/components/base/agent/assistantTable/tableWorkspaceState.ts` | 已裁定非债 | 本轮（第二轮） | `:198` | `landed:'failed'` 留痕 |
| `src/components/videoEditor/engine/commands/batch-command.ts` | 已裁定非债 | 本轮（第二轮） | `:9-22` | 组合命令单次入栈 |
| `src/components/videoEditor/engine/core/managers/commands.ts` | 已裁定非债 | 本轮（第二轮） | `:33,42` | 唯一批量原语 |
| `src/components/videoEditor/engine/core/managers/timeline-manager.ts` | 已裁定非债 | 本轮（第二轮） | `:241,361` | 走 executeBatch |
| `src/components/director3d/d3dPersistence.ts` | 已裁定非债 | 本轮（第二轮） | `:70,78` | 单例缓存／`\|\| 0` 语义正确 |
| `src/components/base/store/nodeRuntimeStore.ts` | 已裁定非债 | 本轮（第二轮） | `:52,60` | 读写分离 |
| `spec/CONTEXT.md` | 有债待还(TD-18-20) | 本轮（第三轮） | `:53,57,103,113,156` | 判决句未归位 |
| `src/components/videoEditor/engine/core/index.ts` | 已裁定非债 | 本轮（第三轮） | `:78,82` | 重置唯一编排点 |
| `src/components/videoEditor/engine/core/managers/project-manager.ts` | 已裁定非债 | 本轮（第三轮） | `:89,138,270` | 走 releaseProjectContext |
| `src/components/videoEditor/ui/editor/editor-header.tsx` | 已裁定非债 | 本轮（第三轮） | `:67,86` | 走 releaseProjectContext |
| `src/components/videoEditor/ui/providers/editor-provider.tsx` | 已裁定非债 | 本轮（第三轮） | `:110` | 走 releaseProjectContext |
| `src/components/videoEditor/ui/editor/panels/assets/views/sounds.tsx` | 抽审 | 本轮（第三轮） | `:64-66,70-74` | ADR-0028 误报 |
| `src/components/nodes/GridSplitNode.tsx` | 抽审 | 本轮（第三轮） | `:451-454` | ADR-0028 误报 |
| `src/components/base/utils/uploadDirs.ts` | 已裁定非债 | 本轮（第三轮） | `:19` | 「沿用」附支撑证据 |

## 四 · 工具环境问题（非仓库债，仅提示）

- 本机 **未安装 `rg`**（`command not found`）。`docs/adr/ADR-0030` 的复核动作与多处流程文档均以 `rg` 为检索手段；
  在未装 `rg` 的机器上执行会得到**空结果而非报错** ⇒ 形同「扫 0 却绿灯」（与 `gate-vitals` 闸所治形态同族）。
  本轮已改用等效检索复核，结论不受影响。
- **已就地回改（唯一一处代码外改动）**：`docs/adr/ADR-0030:49` 的复核动作由 `rg` 改为 `grep -rn`，并补注
  「grep 不默认递归、不自动跳过 .gitignore，漏 `-r` 或不排除目录都会产出空结果而非报错」。
  ⚠ 该改动与 ADR README「accepted 后正文冻结」存在张力——改动的是**检测动作**而非判据本身，
  若坚持不可变性，应改为写 ADR-0031 取代 ADR-0030。**待用户确认**。`docs/plan/**` 属已完结记录，按用户指示**未改**。

## 五 · DATAFLOW 对账

- 本轮未发现链路漂移（未改动 `spec/DATAFLOW.md`）。「可编辑元素判据」与「catch-ok 豁免语义」均属**判据层**内容，
  按 ADR-0025 应归 ADR／代码注释，不在 DATAFLOW 落。

## 六 · 覆盖账

- 三轮合计审 **39** 个文件；已裁定非债 **17**；有债待还 **9**；抽审 **13**。
- **第一轮**：判据抽取 + 0002/0003/0004/0005/0006/0007/0009/0011/0018/0021/0027/0029 相关面。
- **第二轮**（原列未审的 5 条全部判非债）：ADR-0013 ／ 0015 ／ 0026 ／ 0008 ／ 0004；新增高息债 TD-18-17。
- **第三轮**：ADR-0014 合规 ／ 0028 机械探针 0 真命中（语义项留作下轮）／ 0001 判非债；新增母体债 **TD-18-20**（CONTEXT 判据未归位）。
- 仍未审（**3 条，均有明确理由**）：
  1. **ADR-0027** uploads 分域 —— 有 `upload-dirs` 闸覆盖，闸绿灯不构成证据，需专项前后端对账（`KNOWN_SUB_DIRS` vs `SUB_DIR_ALLOW`）；
  2. **ADR-0021** `as` 于判别联合消费处 —— 全仓 `as unknown as` 约 30 处，须逐处判是否落在判别联合消费点（多数疑为 three.js / React ref 三方库类型缝隙）；
  3. **ADR-0030** 零生产消费预留 —— 由 `dead-code`（knip）闸覆盖，同 ADR-0027，需专项取证。
  另 **ADR-0028 的语义项**（意图状态是否需查表才能变成参数）须逐面板读码，本轮仅完成机械探针。

## 七 · 状态行

- **状态**：已验证(有债待还) · 🔴
- **母体**：TD-18-14（catch-ok 豁免语义未收口）／TD-18-17（失败值写进缓存 · 孤例但高息）／**TD-18-20（CONTEXT 判据未归位 · 元层）**／@见 TD-18-18（ADR-0026 唯一键 · 并行二轮登记）
- **本轮教训（已写入 ADR-0031）**：~~TD-18-10~13~~ 是**假债（4 笔）**，病根是顺着 ADR-0029 §决议6 的「待收口」尾巴照办。
  登记前必须先分**探测重复／判据重复**（7 步法 Step 3）：前者收口，后者**严禁合并**。
- **本轮自我纠错 1 处**：ADR-0026 我原判非债，实为债（@见 TD-18-18）——`projectStore.ts:326` 的 `proj-${Date.now()}` 是**落库键**而非临时 id。
- **待用户拍板**：TD-18-16（ADR-0002 与 ADR-0003 张力 · A/B/C 见 §一）
- **本轮零代码改动**（只读审计）· **测试执行次数 0**
