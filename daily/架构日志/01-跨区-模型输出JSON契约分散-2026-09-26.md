# 架构日志 · 生成链路（跨区） · 2026-09-26

**任务**：登记三条债 —— ①剧本盒默认值/枚举无单一默认源；②「从模型回复正文读 JSON」无统一契约；③`response_format` 死参数（写侧假护栏）。

## 一、链路快照（refs / grep 实证）

```
从「模型回复正文」抠 JSON —— 提取器（**同判据，共 4 份**）：
  ① scriptBoxEngine.ts:123  parseJsonText              → {ok,data}（**无 reason**）；剧本盒消费 :399 / :786 / :930
  ② assistantTable.ts:469   tryParseAssistantTableJson → 判别联合 + reason + message ✅ 唯一达标
  ③ TextGenerate.tsx:248    自动拆分内联 try/catch      → items=[]（**静默降级**）
  ④ agentCore.ts:376        parseGenerationsFromReply  → {plan:null,generations:[]}（**静默压平三态**）

另判据（**不合入本债**）：
  ⑤ agentRuntime.ts:573  safeParseNonStreamJSON  —— 解析 **HTTP 响应体**（抗截断/非 JSON），非"回复正文"
  ⑥ assistantTable.ts:519 stripAssistantTableJson —— **反向**操作：剥掉 JSON、只留自然语言供界面显示

目标形态（已存在，只落一处）：assistantTable.ts:448-461 `ParseTableJsonResult` —— 判别联合 `{ok:true,json} | {ok:false,reason,message,error?}`（用户 2026-09-17 裁定「必须要透传」）

response_format 链（实测：**断在最后一步**）：
  scriptBoxEngine.ts:381/:764 →（generate.ts:174-175 归一）→ relayProxy.ts:366 → routes/generate.ts:85
  → generateEngine.relayGenerate（**:46 仅声明入参，全文无消费**）
  → 三个出站分支实参皆无它：chatWithTools / chatLovartText / chat（c）
  → ai-relay `ChatOptions` 亦无该字段 ⇒ **上游从未收到**
  失实注释：generateEngine.ts:45「chat：response_format…**TextNode JSON 依赖**」（TextNode 实际不传 responseFormat）
```

## 五、探债

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-01-33 | **剧本盒默认值/枚举无单一默认源**：同一默认答案散在 schema 结构默认 / 引擎回退字面量 / 纯函数新建模板 / UI 兜底**四处各写一份**（判据表 §七 #1/#4/#5/#7/#8/#14/#15 已登记载体 N>1）。原以 `CONTEXT §〇「不主动收口」` 豁免 —— 该豁免**不成立**（非用户裁定） | 增债 | 结构债 | 剧本盒 7 条判据载体：`'manga'`(§七#1) · `'keyframe'`(#4) · `'5s'`↔`'5'`(#5) · `'固定'`(#7) · `'original'`(#8) · `''`↔`'环境音'`(#14) · `'2K'`(#15) | 低 | **母体 M**：剧本盒无「默认值单一源」—— 默认答案分散在四处（schema／引擎／纯函数／UI） | 建剧本盒默认值单一源（schema 为默认权威），引擎/纯函数/UI 三处回退改为引用真源 | 待还 |
| TD-01-34 | **「从模型回复正文读 JSON」无统一契约**（同判据 **4 份**：①`scriptBoxEngine.ts:123` ②`assistantTable.ts:469` ③`TextGenerate.tsx:248` ④`agentCore.ts:376`）：只有 ② 用**用户已裁定的判别联合**；① **无 reason**（上层读不出失败原因）；③④ **静默压平**。**写侧实为 0 条硬约束** —— 唯一"硬"手段 `response_format` 是死参数（见 TD-01-35），真正生效的只有**提示词契约**这一招软约束。shape 不可统一（`{shots[],assets[]}` / `{rows[]}` / `{items[]}` / `{generations[]}` 各异），但**提取机制 + 读失败契约**可统一 | 增债 | 结构债 | 剧本盒 5 个文本出口 · AI 助手表格 · 文本节点自动拆分 · agent 回复 generations | 中 | **母体 M**：缺「模型输出读取」统一层 —— 目标形态（`ParseTableJsonResult`）**已存在且已被用户裁定**，但只落在表格一处，其余 3 份未推广 | 把 ② 的判别联合抽为共享原语（`{ok,raw,reason,message}`），①②③④ 共用；schema 校验由调用方注入。**不合入**：⑤（HTTP 体，另判据）· ⑥（剥离，反向） | 待还 |
| TD-01-35 | **`response_format` 是死参数（写侧假护栏）**：前端 `scriptBoxEngine.ts:381/:764` 写入 → `relayProxy.ts:366` → `routes/generate.ts:85` → `generateEngine.relayGenerate` **只声明入参 `:46`、全文从不消费**；三个出站分支实参皆无它、`ai-relay` 的 `ChatOptions` 亦无该字段 ⇒ **上游从未收到**。且 `:45` 注释称「TextNode JSON 依赖」**双错**（TextNode 实际不传）。"看起来在硬保证 JSON，实际零作用" | 增债 | 结构债 | 剧本盒 2 个 JSON 出口（生成剧本 `:381` / 分镜提示词 `:764`）· 任何以为"传了 responseFormat 就有 JSON 硬保证"的读者 | 中 | **孤例**：参数只接了半截（声明在、消费缺）；跨栈参数**无端到端对账**（前端发明→路由转发→引擎声明，无一处校验"是否到达上游"） | **ADR 判定（2026-09-26 核对）**：按 `ADR-0063` 六类落点 = **第④类「接线未完成」**（本该消费处 = 三个出站分支，那里"有调用点但漏接"）⇒ 默认处置 = **接上**；`ADR-0053` 消费关要求"四口径一致报零"，而本对象**有生产方（`scriptBoxEngine.ts:381/:764`）+ 路由转发（`routes/generate.ts:85`）⇒ 不报零 ⇒ 不许直接删，改为修**。二选一：**①接上**（三出站分支 + `ai-relay ChatOptions` 加字段，并处理上游能力差异）；**②删链**（= 砍掉一个**当前未生效**的能力，须走 `ADR-0053` 能力关「用户裁定 + 留痕」后执行）。`ADR-0063 ⑦` 强制：`generateEngine.ts:45` 失实注释**无论选哪条都须同批改** | **已还（2026-09-26）** |

### 五-b · TD-01-35 偿还留痕（本轮删链）

**裁定**：用户裁定**删链**（不走"接上"）。依据 = **消费端实证**：默认文本模型 `lovart::lovart-chat`（`providers.default.json:51-58` · `providerModels.ts:59-71` 取首位）命中 `generateEngine.ts:192` 分支 → `chatLovartText` 走 Lovart 私有 HMAC 协议（body 只有 `prompt/attachments/tool_config`，**无 messages 数组、无 `response_format` 位**）；且 `extractChatTextAndImages`（`providers/lovart/index.ts:74-116`）把 system/user **拍平成一个字符串**（`textParts.join('\n')`）⇒ 默认路径**结构上无法消费**。非 lovart 分支（`generateEngine.ts:222` → `ai-relay/generate.ts:43-55`）才是真"漏接"。⇒ 该参数**从未到达任何上游**。

**引入时间（git 考古）**：**`e96f6096` 初始提交**（2026-08-15 16:29:07 · cagekevin「初始提交：一毛AI画布原型主力工程（React原型 + localTool + apimart网关 + 1mao参考归档）」）。**非本仓设计，属原型移植**：同一提交的官方原型 `scripts/scriptbox-split-snapshot/H_.jsx:6295` 即写着
`response_format: i && !…includes('deepseek') && !…includes('claude') ? { type: 'json_object' } : undefined`
—— `useJsonObject` 那句"按模型名排除 deepseek/claude"的启发式**逐字来自该原型**（对应原型当时的模型阵容）。同提交落地为 `src/components/base/scriptBoxEngine.js:65/123/306`。

**完整时间线（git 考古 · 逐提交实证）**：

| 日期 | 提交 | 该提交下的状态 |
|---|---|---|
| 2026-08-15 | `e96f6096` 初始提交 | **进仓**（随原型移植）。前端 `src/components/base/chatApi.js:85` **直连上游**、形态**正确**：`body.response_format = { type: responseFormat }` ⇒ **当时是有效的** |
| 2026-09-03 | `1b0d72eb` 前端出口统一为单一 API | 出站改经 relay 代理，**并特意把它接上**：`relay.ts:162` 注入 `variables.response_format`，同时 `LOVART_CHAT_PROTOCOL` 的 body 加 `response_format: '{{response_format}}'`（`presets.ts`，注释写「temperature/response_format 用纯模板…前端 TextNode JSON（response_format）与温度控制依赖它们（2026-09-03 relay chat 透传）」）⇒ **真的到达上游**（`POST /v1/chat/completions`）。**但形态在同一提交同时退化**：前端已是 `relayProxy.ts:260` `if (opts.responseFormat) body.response_format = opts.responseFormat` —— **裸字符串** `'json_object'`，而非 OpenAI 要求的 `{ type: 'json_object' }` **对象** ⇒ 该字段虽到达上游，**形态非法，大概率被忽略** |
| 2026-09-04 | `4c0b755d` 目录搬迁 | `relay.ts` → `generateEngine.ts`，注入行随迁（`:166`）⇒ **仍通** |
| **2026-09-05** | **`fbe0a3d2`「lovart 直连收口」** | **功能失效点**：lovart chat 由「网关 `/v1/chat/completions` 模板」改为**原生 HMAC 协议**（`chatLovartText`）—— 该协议**没有 `response_format` 位**，messages 也被 `extractChatTextAndImages` 拍平成单条字符串 ⇒ 模板里的 `response_format: '{{response_format}}'` 随本提交**被移除**。**这条能力自此在（默认）lovart 路径上整根消失**。非"漏搬"，是**随平台协议切换一起退场** |
| 2026-09-20 | `54a8c77f`「localTool 引擎」 | **残留清理**：把已无消费者的 `variables.response_format` 注入行删掉，**唯入参声明 `:46` 未清** ⇒ 留下"看起来还在"的**假护栏**（09-20 → 09-26，共 6 天） |
| 2026-09-26 | 本轮 | **删净** |

⇒ 精确结论：**它是真能力、不是幽灵**。三段史：**满血 19 天**（08-15 → 09-03：前端直发 / 形态正确 `{type:'json_object'}` / 默认模型 `lovart-chat` 也被 `useJsonObject` 放行，上游为自托管 `http://127.0.0.1:9004`）+ **坏形态 2 天**（09-03 → 09-05：接线在，但值已被拍平成裸字符串 ⇒ 大概率无效）+ **字段消失 21 天**（09-05 起，`{{response_format}}` 全仓归零）。
⚠️ **诚实边界**：仓位内证据只能证明「**形态正确的字段确实进了出站请求体**」；「上游是否真的照做 `json_object`」属外部事实，**本仓无法证明**，且该 9004 自托管网关已换成 `lgw.lovart.ai` + 原生 HMAC，**已不可复现**。
⚠️ **定性更正**：本条**不宜**再套 `ADR-0063` 第④类「接线未完成（漏搬消费行）」—— 真相是**能力随平台协议切换而丧失**（09-05 起），六类落点均不完全贴合；若要重拾，等于**按当时的真实消费方重建**（`ADR-0053 ①`），而非"接上旧线"。

**删除清单（纯代码 8 文件）**：

| 文件 | 删除项 |
|---|---|
| `src/components/scriptbox/scriptBoxEngine.ts` | `useJsonObject()` + JSDoc（原 `:138-143`）· `:381` / `:764` 写入 |
| `src/components/generate/lib/generate.ts` | `GenerateRequest.responseFormat` · `ChatCompletionsOptions.responseFormat` · `json→json_object` 归一 · `chatCompletions` 透传 |
| `src/components/generate/lib/relayProxy.ts` | `relayChat` 的 `responseFormat` 入参 · `body.response_format` 赋值 |
| `localTool/src/generateEngine.ts` | `RelayGenerateInput.responseFormat` + 失实注释「TextNode JSON 依赖」 |
| `localTool/src/routes/generate.ts` | 路由转发 `responseFormat: body.response_format` |
| `localTool/src/ai-relay/protocol/variables.ts` | 协议模板变量白名单项 `'response_format'`（**第二批**：来历 = 本能力那次注入；注入器已无，`{{response_format}}` 模板消费者 grep 空 ⇒ 幽灵预留） |
| `tests/unit/generate.test.ts` | `responseFormat` 断言（**保留** `temperature` 断言） |
| `tests/unit/scriptBoxEngine.test.ts` | `useJsonObject` 的 `describe` 块 + 解构引用 + 头注释 |

**四关（`ADR-0053`）**：目的关 ✅（整条跨栈参数链消失）· 能力关 ✅（该参数从未到达上游 ⇒ 用户能做的事没变少；裁定人 = 用户）· 消费关 ✅（②-a 四口径报零 · ②-b 落点已定位）· 红线关 ✅（不承担任何一条红线）。

**白名单项已删的安全性取证**：`ALLOWED_VARIABLE_ROOTS` 的唯一消费点是 `validation.ts:72-90 validateTemplateVariables()`，它校验的是**模板变量根名**（从 `{{…}}` 里取的 `root`），**不校验 body 键** ⇒ 删 `'response_format'` **不影响** `presets.ts:47` 的 `extra_body`（那是 body 键）⇒ 安全。

**文档同步（第二批，消灭误导）**：
| 文档 | 类别 | 动作 |
|---|---|---|
| `docs/plan/74-剧本盒子提示词链路-交互图.html` | **现状类** | 删两处假事实：「引擎侧还有两道保险…模型支持时强制 response_format = json_object（useJsonObject，:238）」（改一道）· 步骤①「模型支持时才强制 json_object（useJsonObject）」→ 只留 `temperature 0.7` |
| `docs/plan/149-…甘特图与节点时序-…md` | **现状类** | ⑤ 步请求体列移除 `responseFormat`；文本节点「不传」补注；**新增 §八「能力退役」**（删了什么 / 消费端实证 / 历史三段史 / 另判据两项 / 不再有 JSON 硬保证）；版本 → 第三版 |
| `daily/架构日志/弯路汇编.md:551` | 活汇编 | 那条「一处未深究：未设 responseFormat」**结案**（机制已整条删除，线索关闭） |
| `daily/架构日志/01-生成链路-十四轮-TD01-8…md:54` | 历史轮次 | 同上加**结案标记**（正文不动） |
| `docs/plan/前端生成门面收口-L3-施工方案-待审计.md` · `…-L3-裁定与修订.md` · `交接文档-handoff-2026-09-03.md` | **历史留档** | 顶部加**勘误指针**（记录当时状态、禁止照此接线），**正文不动**（不篡改历史） |

**故意不动（另判据，连坐会出事）**：
- `ai-relay/protocol/presets.ts:47` `extra_body: { response_format: 'url' }` —— **图片**判据（Images API 真参数，值域 `url`/`b64_json`）。**新取证（未登记，另立一笔候选）**：`extra_body` 在协议引擎内**零消费**（`grep -rn extra_body localTool/src` 只命中 preset 自己）⇒ 它不会被引擎特殊处理，只会作为**字面键**随 body 发出，而 OpenAI 图片端点**没有 `extra_body` 字段**（`response_format` 才是）⇒ 该写法**大概率也不生效**。属**图片侧另一笔**。
- `routes/requestModes.ts:214/271` —— 语义不同（`body.text = {format:{type}}`）且**整文件疑似真幽灵**（见 §六 未取证线索）。

**闸**：`tsc --noEmit --noImplicitAny` ✅ · 单测 `generate.test` 27 + `scriptBoxEngine.test` 39 = **66 全过** ✅ · `check:arch` ✅ · `check:dead-code`（59→59，无新增）✅ · `check:api`（error 0 / warn 0）✅ · 第二批后复跑（`variables.ts` 改动）：见下「第二批复验」。


## 六、覆盖度表

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| - | - | - | - | - | - | - |
| `scriptbox/scriptBoxEngine.ts` | 剧本盒 | 有债待还(TD-01-33/34) | 🔴 | 本轮 · 2026-09-26 | `:123` 提取器（无 reason）· 原 `:138-143/:381/:764` 写 responseFormat **已删** | TD-01-33 · TD-01-34（TD-01-35 已还） |
| `agent/assistantTable/assistantTable.ts` | AI助手表格 | 有债待还(TD-01-34) | 🔴 | 本轮 · 2026-09-26 | `:448-461` 目标形态 · `:469` 唯一达标 · `:519` 剥离（另判据） | TD-01-34 |
| `text/TextGenerate.tsx` | 节点 | 有债待还(TD-01-34) | 🔴 | 本轮 · 2026-09-26 | `:248` 内联静默降级 | TD-01-34 |
| `agent/runtime/agentCore.ts` | AI助手核心 | 有债待还(TD-01-34) | 🔴 | 本轮 · 2026-09-26 | `:376` 静默压平三态 | TD-01-34 |
| `localTool/src/generateEngine.ts` | 后端出站 | 已还(TD-01-35) | 🟢 | 本轮 · 2026-09-26 | 原 `:45-46` 声明无消费 + 失实注释「TextNode JSON 依赖」**已删** | TD-01-35 已还 |
| `agent/runtime/agentRuntime.ts` | AI助手运行时 | 已裁定非债 | 🟢 | 本轮 · 2026-09-26 | `:573` safeParseNonStreamJSON —— **另判据**（HTTP 体抗截断） | 非债（另判据） |

**覆盖账**：本轮为**单债登记轮**（非全域审计轮），只覆盖上表 6 个文件；未审：区域 01 其余文件（不属本债链路 / 下轮）。

**未取证线索（下轮）**：`localTool/src/routes/requestModes.ts` —— **【2026-09-26 已处置 · 整文件删除】**。四口径全部报零：① 模块引用 **0**（`refs` + 全仓 `from '…requestModes'` 空）· ② 字符串裸引用 3 处**全是注释**（`agentRuntime.ts:27/275/492`）· ③ 桶/门面再导出**无**（`routes/` 无 `index.ts`；`router.ts` 声明式逐行显式 import handler，不含它）· ④ 运行时串**无**。外加：前端镜像 `src/components/base/requestModes.ts` **已不存在**；唯一引用者是其自带单测 ⇒ 按 `ADR-0063 ⑦`「测试不算生产消费者」。
⇒ 判为 `ADR-0063` **第①类真幽灵**，**已删** `localTool/src/routes/requestModes.ts`（403 行）+ `localTool/test/requestModes.test.js`（236 行），并同步 `agentRuntime.ts` 三处注释里的悬空文件名（改「responses 形态已退役（L3b 知识退场）」，去掉对已删文件的指认）。
⚠️ **闸盲区（重要）**：**`knip.json:63` 把 `localTool/**` 整体放进 ignore**（C 组：独立后端工程，不属前端构建图）⇒ **死代码闸从不扫描后端**，本文件永不会被报零 —— 后端死代码只能靠人工 `refs` + 四口径。
> 其 `responseFormat`（`:214/:271`）语义不同（`body.text={format:{type}}`，已退役的 responses 形态），随整文件一并删除；**与 TD-01-35 的 chat `json_object` 无关**（本案已单列处置，未计入 TD-01-35 的删除清单）。

**另判据·已标注豁免（不删）**：`ai-relay/protocol/presets.ts` 的 `OPENAI_IMAGE_PROTOCOL`（含 `extra_body: { response_format: 'url' }`）—— **不在生产链路**（生产 image/video 只走 `relay-poll.ts` 的 `resolveProviderAsyncProtocol()`，只认 `model_protocols[capability]` 的 `custom`，**无内置 preset 回退**；`generateEngine` 对 image/video 直接返错 ⇒ 生产上无 sync 生图路径）。其可达入口（`createRelay` / `generateImage()` / `getDefaultCustomProtocol('image')`）**仓内调用数均为 0** ⇒ 属 `ADR-0063` **第⑤类「跨边界」**（ai-relay kit 对外 API 面）⇒ **标注豁免**：已在 `presets.ts` 该常量上方加可达性注释留痕。

## 七、结论与下一步

- 本区域状态：`已验证(有债待还)` —— 区域 01 待还：TD-01-30 · TD-01-31 · TD-01-32 · **TD-01-33** · **TD-01-34**（**TD-01-35 已还**，见 §五-b）。
- 待还债项已流入 `daily/架构日志/债务.md`（轻量索引）：是（TD-01-33 · TD-01-34）；TD-01-35 → `[已解决 2026-09-26]`。
- DATAFLOW.md：**已对账一致**（三债均为实现层，链路拓扑未变）。
- 灯：本区**覆盖度表**已更新（🔴）；区域级灯由 `index.md` 自动派生。
