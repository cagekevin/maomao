# 架构日志 · localTool 后端 · 第四轮深扫（文档/脚本/测试/死角）· 2026-09-11

> **文件名**：`08-localTool-后端-四轮深扫-2026-09-11.md`（本区同日第四次审计，按 index.md 规则 8 拆分，不覆盖前三档）。
> 审计视角：前三轮已覆盖全部 src 文件 + 运行时系统面；本轮专查**文档/脚本/测试/残留死角**（不查清楚不留债）。
> 审计依据：架构师心法 Phase 1–4 + 静默自检清单（§五）。
> 本轮结论：发现 **2 处文档漂移债（均已当场修）**；测试覆盖完整、脚本无残留引用。

## 一、本轮审计焦点（死角扫描）

```
① 文档漂移：localTool/scripts/README.md 命令表漏登记 `--poll-status`
             （CLAUDE.md 已引用该命令，task-inspect.mjs:519/1120 已实现，README 未登记）
② 文档过时：ai-relay/protocol/PRESET_PROTOCOL.md §1/§3/§4/§5 引用已退役的
             `api.config.json` / `lovart-image/video/chat` preset / `presetMap`/`getPresetFor`/
             `presetNameFor`（均已随 lovart-old 9004 退役删除，presets.ts 现状只剩
             openai-chat/openai-image/agnes-video + custom）
③ 脚本残留：localTool/scripts/ 无任何对已删函数（persistThreadId/deleteLocalFile/
             handleReadProxy/API proxy/MIME_TO_EXT）的引用 ✓
④ 9004 残留：全库 9 处均为【正确历史注释】（说明退役语义），非过时引用 ✓
⑤ 测试覆盖：localTool/test 21 个文件，覆盖协议/传输/lovart adapter/KV-CAS/存储健康/
             MIME 补后缀/并发下载等；`npm test` 252 全绿 ✓
```

## 二、Phase 1–3（溯源/定海/切割）

- **文档漂移根因**：`--poll-status` 是 relay 重构（2026-09-05）后新增命令，CLAUDE.md 已同步但 scripts/README.md 未跟（README 维护滞后）。**PRESET_PROTOCOL.md** 是 lovart-old 9004 时代的范式文档，退役时只删了代码、未刷新文档——与 TD-08-4（退役未闭环）同源，属「退役文档残留」。
- **修复方式（追加不删）**：PRESET_PROTOCOL.md 头部追加 ⛔ 现状更新块（旧范式对照 presets.ts 现状逐条纠正），§4/§5 就地改写为现状链路——保留历史注记供理解，符合 CLAUDE §5.2「可删过时言论但要写明为何过时」。

## 三、探债（Phase 4）

| TD-ID | 决策点 / 问题点 | 归类 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - |
| TD-08-10 | scripts/README.md 漏登记 `--poll-status`（CLAUDE 已引用、命令已实现） | 还债 | 排障文档（丢图排查入口） | 中 | README 维护滞后于 relay 重构 | 2026-09-11 已补登记（含 poll_task_id/无需 HMAC 说明） | 已解决 2026-09-11 |
| TD-08-11 | PRESET_PROTOCOL.md 引用已退役 api.config.json / lovart-* preset / presetMap 等 | 还债 | 协议接入文档（后续 AI 接入新平台会被误导） | 高 | lovart-old 退役只删代码未刷新文档 | 2026-09-11 已加现状更新块 + 改写 §4/§5 | 已解决 2026-09-11 |

> 2 债当场收敛 + `tsc` 0 错 + `localTool npm test` 252 全绿验证。

**持平项（核实后不修）**：
- admin.ts export `version: '2.0.0'`：是「导出格式版本」，与 version.ts 产品版本正交（version.ts 头注释已说明内部格式版本各自独立），import 端不校验该字段属 admin 手动工具可接受。
- admin.ts `CATEGORY_BY_EXT`（中文报表分类，含 .csv/.xml/.log 全文件视角）vs resources.ts `RESCAN_FILE_TYPE`（入库 type，仅媒体/文本）：覆盖集合与用途均不同，属可辩护分工。
- `.env` 解析两处（loadDotEnv 启动注入 vs readEnvKey 热加载重读）：语义不同（一次性 vs 热加载），保留。
- resources.ts rescan 响应 `{ count: added, scanned, added, skipped, orphanDeleted }`：`added` 键重复出现（count 与 added 同值），无害冗余，不动（前端消费形状已定）。

## 四、结论与下一步

- **本区域状态**：`已还清`（四轮合计 11 债全清：TD-08-1~4 首轮、5~8 二轮、9 三轮、10~11 本轮）。
- **待还债项流入 TECH-DEBT.md**：否——全部当场可还。
- **DATAFLOW.md 刷新**：否。
- **下一步**：localTool 已四轮审透（文件层 + 运行时面 + 文档/脚本/测试死角）。可审 **01 生成链路**（前端侧主链路）或先清理已废弃的 `spec/TECH-DEBT.md`（历史债索引已迁 `daily/架构日志/债务.md`）。
