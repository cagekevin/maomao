# 08-跨区-后端无扩展名文件静默兜底 png 复发（2026-09-16）

> 轮次类型：横推补遗（母亲债「静默兜底」TD-16-11 的后端复发）
> 锚定：本文件 ｜ 债务号：TD-08-23 ｜ 归类：增债 ｜ 利息率：低

## 一、探债（找复发点）

本轮对全仓 6 个高频母亲债做机械 + grep 横推，结论是仓库已高度收敛：

- **注释引错债号**（TD-24-3 同形态）：写一次性机检脚本，抽取账本全部 311 实号，扫 `src`+`localTool/src` 代码注释里所有 `TD-xx-yy` 引用，比对后 **0 处引用不存在的债号** → 该母亲已饱和（TD-24-3 描述里 `TD-03-09` 现在账本已存在，数据自愈）。
- **media 类型 SSOT 绕过**：前端由 `check-arch.mjs` 机器闸覆盖（`\.\(png|jpe?g…\)` 列举 ≥2 分支 + 裸 `pathname.split('/').pop()` 反向判据），干净；后端 `localTool` 仅 `lovart_attachments.ts` 复发（**TD-08-22 本轮已登记**），其余 `mime.ts`/`sounds.ts`/`resources.ts` 均收敛真源。
- **URL 文件名提取漏 ?#**：前后端均为 0（后端走 `helpers.relativePathFromFilesUrl` 真源，TD-08-16/20 收口）。
- **静默吞 catch-return**（`.catch(()=>{})` / `catch{return null}`）：src + localTool 均 **0**，由 `catchOk` 模式控住。
- **假护栏 hardcoded**（`if(false)`/`process.env.NODE_ENV`/`invariant`/死 `disabled`）：仅 `contentStore.ts:237` 一处合法 dev-only 守卫，0 假护栏。
- **单扩展名 `endsWith('.mp4')` 漏判** / `|| 'image/video'` 静默错判 kind：前者 0；后者多为输出文件名默认值（refToken/DepthVideoModal/engine.ts），严重版（`resources.ts` rescan `|| 'image'`）已由 **TD-08-18** 修复并钉进 `mime.ts:113` 注释契约。

唯一漏网的真实复发见下。

## 二、命中（TD-08-23）

后端两处本地文件路径扩展名提取，无扩展名时**静默兜底 png**（与前端 `scriptBoxEngine.ts:1186` 同源的 TD-16-11 母亲，只是作用在真实磁盘文件路径而非 URL）：

1. `localTool/src/routes/files.ts:454`
   ```ts
   const srcExt = path.extname(filePath).toLowerCase().replace(/^\./, '') || 'png';
   ```
   缩略图端点：文件已 `existsSync` 确认存在，若磁盘文件**无扩展名**，`path.extname` 返空 → `|| 'png'` 把输出格式强行定成 png（真实无扩展 jpeg 会被重编码为 png，格式/mime 错标）。

2. `localTool/src/utils/resolveLocalImages.ts:50`
   ```ts
   const ext = path.extname(filePath).toLowerCase().replace('.', '') || 'png';
   ```
   `fileToInlineBase64`：同上，无扩展名文件静默当 png 交给 `jimpMimeForExt`。

**证据 / 双证**：
- 非死代码：`files.ts` 缩略图端点、`resolveLocalImages.ts` 内联 base64 均有活跃调用（前者被静态资源路由调用，后者被资源内联逻辑调用）。
- 未被现有债覆盖：TD-08-19 仅指 `resolveLocalImages.ts:32` 的 `MAX_SEND_DIM`；TD-08-21 仅指 `files.ts:364` 的 `imageExts` 白名单——两处 `|| 'png'` 行号均不在已登记范围内。

**利息率：低**。`path.extname` 提取本身正确（不像 URL 版会吃进 `?token`）；仅「无扩展名文件」这唯一边界触发，而本仓上传文件几乎都带扩展名 → 实际触发概率低。但它是同一母亲在后端的新一份，按「找复发即登记」原则入册，便于将来统一收口（建议：无扩展名时由文件头 `magic` 探测真实格式，或诚实回 `other`/失败而非静默 png）。

## 三、下一跳

- 解法入口：本文件 ｜ 母亲真源参照 `mime.ts` 的「禁 `|| 'image'` 静默兜底」契约（mime.ts:113）。
- 横推状态：本轮 6 个母亲中 5 个已饱和，本复发为唯一新增。

## 四、状态

- 本区域状态：**已验证(债已还清)**（TD-08-23 已于 2026-09-16 还清，见 §五）。
  注：08 区其余债（TD-08-16/17/18/19/20/21/22）已在同日另一轮
  [16-跨区-媒体类型判定与URL提取真值源收口-2026-09-16](16-跨区-媒体类型判定与URL提取真值源收口-2026-09-16.md) 结清。

---

## 五、还债轮次：TD-08-23（2026-09-16）

### 5.1 Step 0 普查（份数自己数）

`grep '\|\|\s*['\"]png['\"]'` localTool/src → **实测 2 处**（与债描述一致，本轮无低估）：
`routes/files.ts:454` · `utils/resolveLocalImages.ts:50`。
「无扩展名文件 → 真实格式」能力：**0 份**（`lovart_attachments.B64_MEDIA_MAGIC` 是 base64 域专用魔数表，
其自身注释（:55）已判「不可外借」，非通用实现）→ 处置 = 新建唯一原语，2 消费方委托。

### 5.2 Step 1 证伪（**证伪成立，但债的"建议解法"被推翻**）

债描述判「真债」正确；但它的建议解法「无扩展名时由**文件头 magic** 探测，或诚实回 `other`/失败」**是多余工作** ——
**Jimp 在 `Jimp.read` 时已按字节解出真实格式**（`img.getMIME()`），无需自写魔数表。

**只读实测（复现原始失败形态）**：造真 JPEG 字节 + 无扩展名文件名 → `extname` 返空 → 兜底 `'png'`
→ Jimp 按 png 重编码（字节头 `ffd8ffe0` → `89504e47`）= **格式丢失 + 体积膨胀**，且**零日志**（静默）。

### 5.3 Step 3/4 定位与定契

- **归属**：`fileStore.ts`（Jimp 能力表 `JIMP_MIME_BY_EXT` / `isJimpEncodableExt` 的既有宿主，变化速率同族）。
- **契约（判别式）**：扩展名能配 MIME → 用它（尊重声明名，历史行为零变化）；否则取 **`img.getMIME()`（字节真相）**；
  都拿不到 → **显式失败**（`files.ts` 返 415，前端 `<img onError>` 回退原图），**不再猜**。
- **改动半径**：新增 2 原语（host 1 文件）+ 2 消费点 = **3 文件**，消费方 import 面零扩大。

### 5.4 Step 6 施工

| 文件 | 动作 |
| - | - |
| `utils/fileStore.ts` | **新增** `jimpMimeForFile(ext, img)`（MIME 口径）· `jimpExtForFile(filePath)`（扩展名口径，仅在扩展名缺失时读盘 = 热路径零额外 I/O）· `DECODED_MIME_BY_EXT`（定向反转表，刻意**不含 webp/avif** —— 能读不能写）；**删** `jimpMimeForExt`（改后**零生产消费者**，仅剩自证单测 = M6 幽灵） |
| `routes/files.ts:453-468` | 无扩展名时 `jimpExtForFile` 定真格式；读不出 → **415 显式失败**（原为静默当 png） |
| `utils/resolveLocalImages.ts:47-65` | 顺序调整为「先 `Jimp.read` 取字节真相 → `jimpMimeForFile`」，去掉 `\|\| 'png'` |

### 5.5 Step 7 验证（探针先红后绿）

**① 修复探针**（注入 `jimpMimeForFile` 的旧"猜 png"行为）：
```
文件: localTool/src/utils/fileStore.ts（命中 1 处，已还原 sha=e7aaf0ebcf50）
命令: cd localTool && node --test --import tsx test/stage2-routes.test.js
观测: exit=1 ｜ ✅ 输出含「无扩展名 JPEG 不得被当成 png」 — 命中
```
**② 闸探针**（新增 `check:arch` 规则 13-c，注入 `extname \|\| 'png'`）：
```
文件: localTool/src/utils/resolveLocalImages.ts（命中 1 处，已还原 sha=4b2a04526b5b）
命令: npm run check:arch
观测: exit=1 ｜ ✅ 输出含「扩展名缺失静默猜格式」 — 命中
```
**③ 全量**：`localTool npm test` **320/320 绿** · `check:arch` ✅ · `check:dead-code` ✅ ·
前端 `type-check` ✅ · `test:smoke` ✅ · 残留 `\|\| 'png'` grep：仅剩 4 处**解释性注释**，零真实兜底。

**新增闸（防复发 · 附 B1「5 条复发债全部是改了但没加闸」）**：`check:arch` 规则 **13-c** ——
`localTool/src/**` 内「出现 `extname(` **且** 同行有 `\|\|`/`??` 媒体后缀兜底」→ 违规（豁免原语宿主 `fileStore.ts`）。
13-b 只拦 `includes('<mime词>')` 子串链，**拦不住本形态** —— 这正是 TD-08-23 能复发的结构原因。

### 5.6 弯路留痕（**必记**）

**弯路 ①：新增断言第一版就是红的 —— 夹具选错，差点被我自己当成"修复没生效"。**
我用 `new Jimp(20,20)` 当"已解码 JPEG"夹具 → 该对象**无源格式**，`getMIME()` 恒返 `image/png`，
断言 `jimpMimeForFile('', jpegImg) === MIME_JPEG` 必然失败（actual `image/png`）。
**错在「夹具不保真」**（对照 附录 C-D/E 的 E3/E5：mock 不保真 → 假红）。
正解 = 夹具必须是**真解码出来的 JPEG**（`await Jimp.read(await new Jimp(...).getBufferAsync(MIME_JPEG))`），
并加一行**前提断言** `jpegImg.getMIME() === MIME_JPEG` 把这个隐含前提钉死。
**它同时印证了规矩「新增断言先单独跑一次确认它是绿的」** —— 否则这条夹具错误会被探针的"红"掩盖。

### 5.7 覆盖账

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债 |
| --- | --- | --- | --- | --- | --- | --- |
| `localTool/src/utils/fileStore.ts` | L2 后端 | 已还清 | 🟢 | 还债 09-16 | 新增 2 原语，删 `jimpMimeForExt` | 无债 |
| `localTool/src/routes/files.ts` | L2 后端 | 已还清 | 🟢 | 还债 09-16 | :453-468 无扩展名读字节真相／415 显式失败 | 无债 |
| `localTool/src/utils/resolveLocalImages.ts` | L2 后端 | 已还清 | 🟢 | 还债 09-16 | :47-65 去 `\|\| 'png'` | 无债 |
| `scripts/check-arch.mjs` | 元层 | 已还清 | 🟢 | 还债 09-16 | 新增规则 13-c（含闸探针） | 无债 |
