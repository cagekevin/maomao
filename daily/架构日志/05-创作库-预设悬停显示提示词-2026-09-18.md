# 05 创作库 · 预设卡片悬停显示提示词（需求登记 2026-09-18）

> **触发**：用户需求「Creative 里那些预设（运镜 / 风格 / 滤镜 / MJ 码图）鼠标移上去现在只显示名字，希望能显示里面具体的提示词」。
> **性质**：业务债（改交互 / 对外可见行为），非审计债。本流程只登记、不写解法、不改码。

## 五、探债（1 笔待还）

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话·可选) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-05-19 | `src/components/base/creative/views/PresetGridView.tsx:41`（`title={p.name}`）+ `:82-84`（`.cl-name` 显示 `p.name`）｜数据真源 `p.prompt`＝`creativePresets.ts:58`，由 `creativeCatalog.ts:92` 归一化 `prompt: r.prompt`；4 类 catalog（style 217 / filter 17 / motion 51 / mj 493）统一经此组件渲染 | 业务债（UI 交互缺口：hover 未消费已存在的 prompt 字段） | 创作库 UI 层（creative/views/PresetGridView） | 4 类全部预设卡片（778 项）的 hover 体验；需处理 prompt 长度（部分很长，原生 title 会被截断） | 中 | 孤例（数据已备、UI 未接线，无同源母体债） | hover 时展示 `p.prompt`：建议自定义 tooltip 替换原生 `title`，避免长文本截断；可仅在有 prompt 的卡上启用 | 待用户拍板 |

**A/B/C（业务债必摆，仅选项标题）**：
- A：自定义 tooltip 组件替换原生 `title`，hover 展示 `p.prompt` 全文（样式可控、长文可换行/滚动）—— 推荐
- B：原生 `title` 直接塞 `p.prompt`（最快，但长文本被浏览器截断、无样式）
- C：卡片常驻 / 点击展开区显示 prompt（不依赖 hover）

## 七、状态行

- 本轮：登记 1 笔业务债（TD-05-19，待用户拍板）。
- 覆盖账：本轮非审计，仅登记单条需求，不报覆盖账。
