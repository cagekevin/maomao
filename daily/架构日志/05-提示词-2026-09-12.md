# 架构日志 · 区域 05 提示词 · 2026-09-12

> **文件名**：`05-提示词-2026-09-12.md`。区域 05 首轮审计（清单 `index.md` 原标「待审计」）。
> 审计视角：数据流（素材/预设 → 数据层 → 富文本输入 → 发送解析）。
> 审计依据：架构师心法 Phase 1–4 + 静默自检清单；数据流实证用 `node scripts/mv-sync-refs.mjs refs <file>`。
> 自底向上：纯函数（promptChips/promptMention）→ 数据层（promptManager/promptHubStore）→ UI（PromptInput/PromptLibrary/PromptHub/PromptLibraryButton）。
> 注：本区类型面（`noImplicitAny`）已于区域 09 批次清零（`base/prompt/` 在白名单内）；本轮查的是**链路/契约/类型诚实性的另一层**。

---

## 一、链路快照（refs 实证）

DATAFLOW「提示词链路」（§213-221）**准确**：

```
prompt/PromptInput · PromptLibrary · PromptLibraryButton · PromptHub（UI）
prompt/promptManager · prompt/promptHubStore（数据层）
prompt/promptChips · prompt/promptMention（纯函数）
```

| 文件 | 规模 | fan-in | 职责 |
| - | - | - | - |
| `promptChips.ts` | 474 | PromptInput（+测试） | `@{id:label\|thumb}` 芯片 ↔ DOM/纯文本 互转（**唯一入口**：禁散写 `/@\{/` 正则、禁就地解析） |
| `promptMention.ts` | 121 | PromptInput · promptChips（BREAK 集） | @提及 触发判定 + 弹层定位（纯函数） |
| `promptManager.ts` | 164 | PromptLibrary · PromptLibraryButton | 本地预设 CRUD（`yimao_preset_prompts`）+ 最近使用 + 卡片映射 |
| `promptHubStore.ts` | 353 | PromptHub（+测试） | 社区提示词源拉取/缓存/归一化（缓存 `yimao_prompt_hub_cache`） |
| `PromptInput.tsx` | 756 | 4 生成节点 + 全屏编辑器 + ResourceStrip | 富文本输入 + @候选 + 运行期自动转芯片 |
| `PromptLibrary.tsx` | 464 | PromptLibraryButton | 预设库大弹窗 |
| `PromptHub.tsx` | 304 | LeftPanel | 社区提示词面板 |
| `PromptLibraryButton.tsx` | 81 | 各参数行 | 打开预设库的入口按钮 |

---

## 二、溯源（Phase 1：症状 vs 病根）

- **两条写路径已收敛**（2026-09-07 修）：`autoLinkAssetsByName`（批量全量重建）与 `commitOccurrencesInRun`（运行期就地 DOM 手术）**共用同一匹配源** `findAutoLinkOccurrences`（最长优先、非重叠），根治了「两套规则分叉」。这是本区**做得对**的地方。
- **输入框单一序列化点**：`emitDOM` 是「所有内容变更」的唯一出口（打字/粘贴/插芯片/删芯片/blur），`autoGrow` 竖向自适应也收敛进它（6 处散调用 → 1 处）。共享同一 `value` 的多个实例用 `el.contains(range.startContainer)` 守卫，避免面板实例抢全屏弹窗的光标（真实事故的根治）。
- **病根 = 类型诚实的第二层盲区**：`base/prompt/` 虽已过 `noImplicitAny` 门禁，但 `PromptInput` 用 `useState(null)` / `useRef(null)` / 裸 `new Map()` —— 在 `strictNullChecks:false` 下这些被推断为 **`any`**，而门禁只认 `noImplicitAny`，**结构性看不见**（与 TD-06-4 同源，见 §五 TD-05-1）。

---

## 三、定海（Phase 2：不变式 / 唯一真相源）

| 不变式 | 唯一真源 | 现状 |
| - | - | - |
| 芯片序列化/解析 | `promptChips`（`PROMPT_CHIP_RE` + serialize/render/resolve） | ✅ 唯一入口，禁散写正则 |
| @名匹配 | `promptChips.findAutoLinkOccurrences` | ✅ 批量重建与运行期就地共用 |
| 终结字符集 | `promptMention.BREAK`（`promptChips.isTerminatedByBreak` 复用） | ✅ 单一真源（文件头已注明） |
| 内容变更序列化 | `PromptInput.emitDOM` | ✅ 单点（autoGrow 亦收敛于此） |
| 预设存储 | `promptManager`（KV `yimao_preset_prompts`） | ✅ 唯一读写口 |
| 社区库缓存 | `promptHubStore`（KV `yimao_prompt_hub_cache`，TTL 1h） | ✅ 网络统一 `httpRequest`，失败写 `lastError` |
| 事件 | `presets-changed`（promptManager 发布 → PromptLibrary 订阅，EVENTS 已登记） | ✅（行号引用漂移，见 TD-05-6） |

---

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- **纯函数层（promptChips/promptMention）判合格**：DOM/事件全留在 PromptInput；匹配与定位可单测。**不切**。
- **PromptInput（756 行）判合格深模块**：承载 contentEditable 光标/IME/芯片/弹层四种复杂度，但序列化单点 + 守卫齐备；**不切**（再切会把光标逻辑撕碎）。
- **真缝隙**：`promptHubStore.subscribePromptHub` 的「按 sourceId 订阅」维度实际只用一个常量 key `'all'`（见 TD-05-2）——收敛口 = 去掉未用维度，或补齐真正的按源订阅。
- **边界观察**：相邻 `@{a}{b}` 两个芯片之间无 ZWSP（`ensureCaretSlotBeforeChip` 只处理前兄弟为空/BR/空文本）→ 光标落不进两芯片之间（TD-05-3）。

---

## 五、探债（Phase 4：技术债账本）

| TD-ID | 决策点 / 问题点 | 归类 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - |
| TD-05-1 | **类型诚实性第二层盲区**（@见 TD-06-4 / 09）：`PromptInput.tsx:96 useState(null)`（mentionPos）/ `:113-114 useRef(null)`（popRef/savedRangeRef）/ `:251 new Map()`（chipMetaMap）→ 在 `strictNullChecks:false` 下推断为 **`any`**；`check:strict-src`（noImplicitAny）结构性看不见，`base/prompt/` 名义「已收口」 | 还债 | `base/prompt`（+全 src 同类写法） | 中（门禁「已诚实」的保证不完整） | 关 `strictNullChecks` + 门禁只认隐式 any | ① 就地补类型（`useState<MentionPlacement\|null>(null)`、`useRef<HTMLDivElement\|null>(null)`、`new Map<string, ChipMeta>()`）；② **09 侧补护栏**：禁 `useState(null)`/`useRef(null)`/裸 `new Map()` 等「strictNullChecks 下塌成 any」的写法 | ⚠️ 待还 |
| TD-05-2 | `promptHubStore.subscribePromptHub` 的「按 sourceId 订阅」维度**从未使用**（`listeners` 恒只放 key `'all'`）→ 过度设计维度；文件头「唯一性声明」却把它描述为主要形态 | 还债 | promptHubStore + PromptHub | 低 | 从原版搬运时保留了未用的泛化 | 收敛为单一订阅集（或补齐真正的按源订阅，二选一，勿保持两不像） | ⚠️ 待还 |
| TD-05-3 | **相邻芯片之间无光标落点**：`promptChips.ensureCaretSlotBeforeChip` 对「前一个兄弟是芯片」不插入 ZWSP，而 `commitOccurrencesInRun` 对相邻命中会产出相邻芯片（`@{a}{b}`）→ 光标无法落在两芯片之间 | 还债 | PromptInput 富文本（@A@B 相邻场景） | 低 | 落点补齐只覆盖「行首/BR 后」，未覆盖「芯片后」 | 补一个分支：前兄弟是芯片 → 插 ZWSP（或 `commitOccurrencesInRun` 组流时保证芯片间有文本/ZWSP） | ⚠️ 待还 |
| TD-05-4 | `promptManager` 末尾 `export { generateId }` —— 把 `core/idGen.generateId` **二次导出**给 UI 走捷径（第二出口）；调用方本应直接从 `core/idGen.ts` 取 | 持平 | promptManager + 其消费方 | 低 | 便利性再导出 | 待裁定（删二次导出，调用方改直取） | ⚠️ 待裁定 |
| TD-05-5 | `promptChips` 文件头自称「纯逻辑层（**无 DOM 环境依赖**，可单测）」，但全文件依赖 `document.createElement` / `Node.ELEMENT_NODE`（实为 DOM 层）；且模块级 `PROMPT_CHIP_RE`（`/g`）**导出**给外部，`lastIndex` 为共享可变状态 | 持平 | promptChips 文档 + 导出面 | 低 | 文档口径与实际不符 | 待裁定（改文档口径为「DOM 工具层」；`PROMPT_CHIP_RE` 视需要改工厂或加复位约定） | ⚠️ 待裁定 |
| TD-05-6 | `promptManager` 兜底 id 两口径：`mapToLibraryCards` 用 `'preset-'+idx`、`ensureIds` 用 `generateId('pp')`；`getRecentCards` 的 `.filter(Boolean)` 无类型谓词（`strictNullChecks` 下需 predicate）；`eventBus.ts`/`contracts.ts` 里 `presets-changed` 的**行号引用已漂移**（写 promptManager:88 → 实际 105；写 PromptLibrary:40 → 实际 81） | 持平 | promptManager + 文档 | 低 | 历史引用漂移 | 待裁定（统一兜底 id 口径；行号引用改为不带行号或对齐） | ⚠️ 待裁定 |

---

## 六、结论与下一步

- **本区域状态**：`待审计` → **`已验证(有债待还)`**（6 债：TD-05-1…6；其中 TD-05-4/5/6 为持平待裁定）。
- **健康项（非债，写入本档防后人误改）**：
  1. **芯片匹配唯一入口** = `findAutoLinkOccurrences`（批量重建 `autoLinkAssetsByName` 与运行期 `commitOccurrencesInRun` **共用**）——禁再写第二套匹配规则。
  2. **`BREAK` 集单一真源** = `promptMention.ts`（`detectMentionQuery` 与 `isTerminatedByBreak` 共用）——禁在组件里另写 break 字符表。
  3. **`emitDOM` = 内容变更唯一序列化点**（含 autoGrow）——新增内容变更路径必须经它，禁直接改 `innerHTML` 后不 emit。
  4. **共享 `value` 的多实例守卫** = `el.contains(range.startContainer)`（防面板实例抢全屏弹窗光标）——勿删。
  5. `cameraParams` 与摄影参数无关此处不再赘述（见区域 06）。
- **DATAFLOW.md 是否需要刷新**：**否**（本段与实测一致）。
- **跨区互指**：TD-05-1 `@见 TD-06-4 / 区域 09`（同为「门禁看不见的显式/隐含 any」）。
- **建议下一步**：继续普查最后一个 `待审计` 区域 **07 3D / 深度视频**；之后按用户指令「自底向上统一修复」。

---

## 七、TD 清偿记录（2026-09-12，统一修复）

> 验证：`npm run type-check` ✅ · `npm run check:strict-src` ✅ 白名单 0 报 · 相关单测 ✅（含 `promptChips` 45 例、`PromptInput.*` 若干）· `eslint` 改动文件 0 error。

| TD | 结论 | 改动 |
| - | - | - |
| TD-05-1 | 🟡 部分已解决 | `PromptInput` 就地补类型：`mentionPos: MentionPlacement \| null`、`popRef: HTMLDivElement \| null`、`savedRangeRef: Range \| null`、`chipMetaMap: Map<string, ChipMeta>`（并 `promptChips` 导出 `ChipMeta`）。**未做**：09 侧「禁 `useState(null)`/`useRef(null)`/裸 `new Map()`」机器护栏（属区域 09，留待） |
| TD-05-2 | ✅ 已解决 | `promptHubStore` 订阅机制**全仓 0 消费方** → 删 `listeners`/`notifyAll`/`subscribePromptHub` + `writeCache` 的通知调用；`core/eventBus.ts` 头注同步（去掉对已删符号的引用） |
| TD-05-3 | ✅ 已解决 | `promptChips.ensureCaretSlotBeforeChip` 增「前兄弟是芯片 → 插 ZWSP」分支（`@{a}{b}` 相邻芯片间可落光标） |
| TD-05-4 | ⏸ 持平待裁定 | 未动（`generateId` 二次导出） |
| TD-05-5 | ⏸ 持平待裁定 | 未动（`promptChips` 文档口径 / `PROMPT_CHIP_RE` lastIndex） |
| TD-05-6 | ⏸ 持平待裁定 | 未动（兜底 id 两口径 / 事件登记行号漂移） |

---

## 八、深化轮（自下而上重探·2026-09-12 二期）

> 方法论：用户指定「自下而上」重走区域 05 —— **契约/类型地基 → 纯函数 → 数据层 → UI 层**逐级 refs 实证，只登记债、不改代码。本轮不重复首轮链路快照（§一已准），只补「底层向顶层的债」。

### 8.1 自下而上实证路径

| 层 | 文件 | 实证动作 | 结论 |
| - | - | - | - |
| 契约/类型地基 | `core/contracts.ts`(`STORAGE_KEYS`) | 三提示词键 `yimao_preset_prompts/yimao_preset_recent/yimao_prompt_hub_cache` 均 `backend:'local'`；`presets-changed` 事件登记行号 `from:promptManager.ts:107`/`to:PromptLibrary.tsx:65` 已漂移（实 `:105`/`:80-81`） | 同步读 KV 禁令（TD-02-12）**不命中**；行号漂移属 TD-05-6 |
| 纯函数 | `promptChips.ts` / `promptMention.ts` | `PROMPT_CHIP_RE`(`/g`) 生产消费面仅模块内 2 处（`renderPromptToNodes`/`resolvePromptChips`）；`isTerminatedByBreak` 复用 `BREAK` 单一真源；`generateId` 不在纯函数层导出 | 债全已在 TD-05-5 覆盖，无新增 |
| 数据层 | `promptManager.ts` | `export { generateId }`(L164) 二次导出；`mapToLibraryCards`(L125) 兜底 `'preset-'+idx` vs `ensureIds`/`createPreset` 的 `generateId('pp')`（L73/98）两口径；`getRecentCards`(L137) `.filter(Boolean)` 无类型谓词 | 债全已在 TD-05-4/05-6 覆盖，无新增 |
| 数据层 | `promptHubStore.ts` | `readCache`(L222-226) `contentGet() as Record<string,SourceCache>|null` 假收窄、无运行期 schema 校验 | **TD-05-10 新债** |
| UI 层 | `PromptHub.tsx` | L241-259 `useCallback(...,[])` 返回值未赋标识、全组件 0 调用点（复刻 L210-239 加载逻辑，本应接刷新按钮未接线） | **TD-05-7 新债** |
| UI 层 | `PromptLibrary.tsx` | L98 `recentIds=useMemo(()=>getRecent(),[])` 空依赖永久锁定；L43 `presetPrompts` prop 全 src 0 传入（唯一调用方 `PromptLibraryButton` 仅传 open/onClose/onUse/onAppend/defaultCategory） | **TD-05-8 / TD-05-9 新债** |

### 8.2 新增债（探债）

| TD-ID | 决策点 / 问题点 | 归类 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - |
| TD-05-7 | `PromptHub.tsx:241-259` **悬空 useCallback**：`useCallback(() => { setStatus('loading'); setError(''); loadPromptHub().then(...).catch(...) }, [])` 返回值未赋给任何标识符、全组件无调用点 → 死刷新 handler；它逐字复刻 L210-239 首屏加载逻辑，本应是「刷新」按钮处理器却从未接线 | 还债 | promptHubStore + PromptHub UI（当前 UI 无刷新按钮） | 低 | 计划中的「刷新」能力未落地，留了未接线的重复逻辑 | 删死 `useCallback`；若产品要「刷新」则在副工具条加按钮并复用该逻辑（单个加载函数抽取） | ⚠️ 待还 |
| TD-05-8 | `PromptLibrary.tsx:98` `recentIds = useMemo(() => getRecent(), [])`：空依赖数组 → 组件实例首次挂载算一次后**永不再算**；`handleNewNode`(L109)/`handleAppend`(L125) 调 `recordRecent(card.id)` 写回存储，但 `recentIds` 不更新 → 「最近使用」tab 不反映本会话最新记录。注释「open 变化无需重算」是**语义误解**：最近使用本应随使用实时更新 | 还债 | PromptLibrary「最近使用」tab（用户可见 bug） | 中 | 把「应实时更新的状态」用 `[]` 永久锁定，注释自我合理化 | `recentIds` 改为随 `open` 或 `recordRecent` 后重算（去 `[]`；或抽 `useRecentIds(open)`） | ⚠️ 待还 |
| TD-05-9 | `PromptLibrary.tsx:43/58/76/152/160/180` **`presetPrompts` 死 prop**：`presetPrompts?: Preset[]` 可选覆盖 prop，全 src **0 调用方传入**（唯一调用方 `PromptLibraryButton.tsx:72-78` 只传 `open/onClose/onUse/onAppend/defaultCategory`）；致 L76 `const presets = presetPrompts || localPresets` 恒取 `localPresets`，L152/160/180 三处 `if (!presetPrompts) setLocalPresets(next)` 假受控分支**恒走**（永为真） | 还债 | PromptLibrary 内部（假受控假象） | 低 | 复刻上游时保留了「外部受控预设」能力但无调用方 | 删 `presetPrompts` prop + 删 L76 短路 + 删三处 `if (!presetPrompts)` 分支（直接 `setLocalPresets`） | ⚠️ 待还 |
| TD-05-10 | `promptHubStore.ts:222-226` **`readCache` 的 `as` 假收窄**（类型诚实性第二层，@见 TD-05-1/05-5/09）：`const c = contentGet(CACHE_KEY) as Record<string, SourceCache> \| null;`——`contentGet` 返 `unknown`，直接 `as` 不校验；L225 `c && typeof c === 'object' ? c : {}` 在 TS 收窄后 `typeof c==='object'` **恒 true**（仅排 `null`），运行期若存储是数组/旧 schema 对象会被误接受，字段（items/fetchedAt/signature）缺失时静默流过（undefined） | 还债 | promptHubStore 缓存读路径 | 低 | 关 `strictNullChecks` + 用 `as` 代替运行期 schema 校验 | 加运行期 schema 校验（类型守卫函数），与 TD-05-1/05-5 同源收口 | ⚠️ 待还 |

### 8.3 评估结论（非债，记录自下而上严谨）

1. **TD-02-2（存储就绪度）对 `promptManager` 未命中**：`loadPresets`(L77) 首次种子写入走 `writeJSON→contentSet`，而 `contentSet`(contentStore L379) 无 `isStorageReady` 守卫、直接 `cache.set+sSet`。但 `PromptLibrary` 仅在用户点「预设」按钮后挂载，此时 `initStorage()` 早已完成 → 种子写入实际不会在未就绪时发生；`contentGet` 未就绪返 undefined 已由 `readJSON` 的 `val===undefined` 兜底。判定：**无实害，不登新债**（与 TD-02-2「三模块就绪后重读」修复名单漏 promptManager 的事实一致，但触发面不存在）。
2. **TD-05-4/05-5/05-6 复核确认仍成立**：`generateId` 二次导出生产侧 0 消费（`PromptLibraryButton` 直取 `core/idGen`）；`PROMPT_CHIP_RE`(`/g`) 模块级导出 `lastIndex` 共享可变状态；兜底 id 两口径 / `.filter(Boolean)` 无谓词 / 事件行号漂移 —— 全部维持首轮「持平待裁定」结论，本轮无新证据推翻。

### 8.4 区域结论更新

- **债数**：`6`（首轮）→ **`10`**（深化轮新增 TD-05-7/8/9/10）。
- **状态**：维持 `已验证(有债待还)`；待还债清单 = TD-05-4/5/6（持平待裁定）/ TD-05-7/8/9/10（待还）。
- **数据/UI 层是本轮新债集中区**（TD-05-7/8/9/10 全部在数据层或 UI 层，纯函数与契约层无新增），印证自下而上「债向顶层漂」的假设。

---

## 九、清偿轮（2026-09-13）：TD-05-4/5/6/7/8/9/10 七债全清

> 用户指令「先做你自己能做的」→ 把 7 条**结构/正确性类**债（不含需要产品判断的「是否加刷新按钮」）全部清偿。

| TD | 修法 | 依据 |
| - | - | - |
| **TD-05-7** | `PromptHub` 悬空 `useCallback`（死刷新 handler，逐字复刻首屏 effect）→ **抽 `runLoad(isAlive?)` 单一实现**，首屏 effect 调用它；**删死 useCallback**。若将来要「刷新」按钮，接线到 `runLoad` 即可（不再复制） | §零.2 能收敛就收敛 |
| **TD-05-8** | `recentIds` 空依赖锁死 → 改 `useState` + `open` 时重读 + `refreshRecent()`；`handleNewNode`/`handleAppend` 的 `recordRecent` 后调它 → 「最近使用」实时更新（**用户可见 bug 修复**） | 正确性 |
| **TD-05-9** | 删死 prop `presetPrompts`（全 src 0 传入）+ 三处恒真的 `if (!presetPrompts)` 假受控分支 + 接口定义 + 解构 | §零.2 能删就删 |
| **TD-05-4** | 删 `export { generateId }` 二次导出（全仓 0 消费方从本模块取） | 唯一入口红线 |
| **TD-05-10** | `readCache` 的 `as Record<string,SourceCache>` 假收窄 → 新增 `isSourceCache()` 运行期类型守卫，**逐条校验形状**（非对象/数组/字段缺失的条目丢弃，不信任坏缓存） | 铁律 2 类型诚实 |
| **TD-05-5** | ① 文件头口径更正「纯逻辑层无 DOM 依赖」→「DOM 工具层（无 React，依赖 document）」；② `PROMPT_CHIP_RE` **const 改工厂函数 `promptChipRe()`**（消除 /g 的 `lastIndex` 共享可变状态），2 处使用点改调 | 诚实 + 结构 |
| **TD-05-6** | ① 兜底 id 两口径统一为 `generateId('pp')`（原 `'preset-'+idx` 与 `ensureIds` 不一致 → 漏补 id 时「最近使用」静默失效）；② `getRecentCards` 的 `.filter(Boolean)` → 显式类型谓词 `(c): c is LibraryCard => c !== undefined`；③ `contracts.ts` 的 `presets-changed` 行号对齐实测（`:107/:65` → `:105/:80`）、`eventBus.ts` 散文清单**去行号**（易漂移的装饰） | 一致性 + 防漂移 |

### 9.1 度量（§零.3）

| 口径 | 改前 | 改后 |
| - | - | - |
| `useCallback` 重复实现（PromptHub 加载逻辑） | 2 份 | **1**（`runLoad`） |
| 死代码（悬空 useCallback） | 1 | **0** |
| 死 prop（`presetPrompts`）+ 恒真分支 | 1 prop + 3 分支 | **0** |
| 第二出口（`generateId` 二次导出） | 1 | **0** |
| `as` 假收窄（readCache） | 1 | **0**（运行期守卫） |
| `/g` 正则共享可变状态 | 1（导出 const） | **0**（工厂） |
| 兜底 id 口径 | 2 | **1** |

### 9.2 验证

| 项 | 结果 |
| - | - |
| `promptChips` / `promptManager` / `promptHub` 测试 | ✅ **64 例全绿**（+1 新测试锁 TD-05-6 兜底口径） |
| `tsc --noEmit` | ✅ 0 错 |
| `check:events`（改了契约表行号） | ✅ 通过 |
| `check:catch` / `eslint`（改动文件） | ✅ 0 违规 / 0 告警 |

### 9.3 区域结论

- **05 区 10 债全清**（TD-05-1…10），状态 → **已还清**。
- **未做（需产品判断）**：TD-05-7 提到的「刷新」按钮——**删死代码不需要判断，加按钮需要**。已把 `runLoad` 备好，产品决定接线时一处即可。

---

## 十、自检修正轮（2026-09-13）：审计 §九 自身 —— 查出 3 处问题（1 处偷懒）

> 用户要求「审计你刚刚的修改是否做到心法、有没有偷懒」。按 §五 静默自检 + §零.5 偷懒判据逐条复盘 §九，**查出 3 处**，已全部修正。

### 10.1 【严重·审计失职】`_error` 是死 state，我却当"修好了"（TD-05-11）

**事实**：`PromptHub.tsx` 的 `const [_error, setError] = useState('')` —— `_` 前缀即"从未被读"的信号。
实测 `grep error`：**只在 3 处 `setError` 写、UI（L277-292）0 处读**。整个 `catch` 分支是死逻辑。

**我的失误**：§九 我抽了 `runLoad` 消除「两份重复」，并在注释里写「**唯一实现**」——
**把"消灭重复"当成了"治愈"**，没查这段代码**到底在干什么**（§五 自检「溯源」项失守）。

**修正**：删除死 state 与其 3 处写入；失败仍由 `status='error'` 表达。
并**登记观察项（非债）**：`status==='error'` 与「加载成功但为空」UI 呈现相同（都 `HubEmpty`）——
错误态无独立视图，**是否补属产品决策，不擅自加**（§零.5）。

### 10.2 【文档债·自造】孤儿注释块

`promptChips.ts` 原有 L40-45 正则格式说明（描述已不存在的 `PROMPT_CHIP_RE`），§九 我**在其下方新增**工厂说明注释
→ 制造「描述不存在事物」的僵尸注释。**已合并为一段**。

**元教训**：§九 让该文件 +26 行，光注释占一半 —— 改代码时**必须同步删掉被取代的旧注释**。

### 10.3 【真偷懒】保留多余别名 `presets = localPresets`

§九 我写「保留别名：下游多处按 presets 读」当作理由。按 §零.5 偷懒三问复核：

| 判据 | 实际 |
| - | - |
| ① 根因证据 | ✅ 有（5 处下游用） |
| ② 替代方案 | **❌ 从未评估「5 处改名」的成本** |
| ③ 决策者的选择 | **❌ 直接自己定了「保留」** |

**判词：这不是架构判断，是偷懒** —— 用"成本更低"包装"没做干净"。
且逻辑更糟：`localPresets` 这个名字本为「区分外部传入 vs 本地」而设，**`presetPrompts` 已删 → 该区分消失 → 名字冗余**，
我却留了「两名指一物」= 我自己在批的 SSOT 第二份。

**修正**：实测改名成本**仅 7 处**（我原判"多处"是虚的）→ state 正名为 `presets`/`setPresets`，删别名。

### 10.4 度量与验证

| 口径 | §九 后 | §十 后 |
| - | - | - |
| 死 state（写入从不被读） | 1（`_error`） | **0** |
| 僵尸注释块 | 1 | **0** |
| 同值双名（`presets`/`localPresets`） | 1 | **0** |

| 验证项 | 结果 |
| - | - |
| prompt 相关测试（4 文件） | ✅ **72 例全绿** |
| `tsc --noEmit` / `eslint`（改动文件） | ✅ 0 错 / 0 告警 |

### 10.5 方法论沉淀（写入本档防重犯）

1. **"消灭重复" ≠ "治愈"**：抽公共实现后，必须再问「这段逻辑真的在被使用吗」——死代码的重复，正确动作是**删除**而非抽取。
2. **改注释必须删旧注释**：新增说明性注释时，同步清理被其取代的旧注释块（否则造僵尸文档）。
3. **别用"成本更低"代替成本核算**：说"保留以省成本"前，**先实测改动点数量**（本轮实测 7 处，与我"多处"的印象差一个量级）。
4. **`_` 前缀变量名是死代码信号**：AI/人留下的 `_xxx`/`_unused` 即"我加了但没用"的痕迹，审计时应追查其写入端。

### 10.6 我提了一个假方案，被用户一句话否掉（重要反面案例）

**过程**：§十 删掉死 state `_error` 后，我登记 TD-05-11「错误态无独立 UI」为**待裁定债**，
并主动推荐**方案 B**（给 `HubEmpty` 加 `reason` prop，失败时文案改「加载失败，请检查网络」），
论证是"刷新后失败 → 空白列表，用户永远不知道为什么"。

**用户反驳（一句命中）**：
> 「刷新后失败，空白列表你都刷新了。难道你不知道刷新后，你没保存它不就变空了？这不很正常吗？」

**复盘——我的论证三处失守**：

1. **把常识说成缺口**：用户**自己刷的新** → 重拉失败 → 空白是**诚实结果**（数据确实没加载到）。
   用户本就知道"自己刷了"，不需要 UI 教他这件事。
2. **忽视已有机制**：`PromptHub` L228 已有 `toastWarning('${n} 个源加载失败…')` —— 失败的**用户可见提示本就在**。
   我上一条论证"toast 已逝"是**为了证明缺口存在而硬找的理由**（§零.5 偷懒的典型形态：先有结论再找证据）。
3. **方案不给用户新行动**：加"加载失败"文案后，用户能做的仍是「等网络 / 再刷新」——**这条文案没有改变任何可能性**。

**根因（比结论更重要）**：我从 TD-05-11 的**登记条目**继承了"这是个债"的前提，
**没有回头质疑前提本身**（§五 自检「溯源」第一问：*如果这个需求消失，我想写的代码还有价值吗？* —— 答案是没有）。

**§零.5 反向判词**：提方案 B 才是「为了做而做」——**没有真实根因证据，纯粹为一致性加代码**。

### 10.7 TD-05-11 改判：非债·已裁定

| TD-ID | 裁定 | 理由 |
| - | - | - |
| TD-05-11 | **非债·已裁定（2026-09-13）** | `status==='error'` 与「成功但空」UI 相同 —— **不是缺陷**。① 刷新后拉取失败 → 空白是**诚实结果**；② 用户**知道自己刷了新**，无需 UI 告知；③ 失败**已有 `toastWarning` 源级提示**；④ 叠加文案**不改变用户可采取的行动** → 属"为一致性而一致性"（§零 拒绝）。**禁后续 AI 当 bug 补错误视图**（除非产品明确要求"重试按钮"这一**新增交互能力**）。 |

