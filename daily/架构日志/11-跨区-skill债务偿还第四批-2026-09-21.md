# 11-跨区 · Skill 债务偿还（第四批：注入面安全 + 三处诚实闸）

> **触发**：用户「继续」→ 按息排序施工 TD-11-17 → 21 / 24 / 28。
> **验证口径**：只跑相关测试（用户裁定 2026-09-21）。
> **范围**：`agent/skill/{skillCatalog,skillHydrate,skillCloudSync,index}` · `agent/canvas/useCanvasAgentTools` · `settings/sections/SkillSettings` · 三个对应测试文件。

## 一 · 改动

| 债 | 改法（一句话） |
| --- | --- |
| **TD-11-17**（安全） | 「不可信条款」收口为**唯一一份字面文本** `UNTRUSTED_SKILL_TEXT_RULE`：块① 清单（此前**零声明**，而它的 `name`／`description` 直入 system）与 `skill_read_file` 工具描述**共用同一常量** —— 同一判据两处各写一套话术 = 两份真相 |
| **TD-11-21** | `reloadSkillsFromDisk` 逐包**装载期对账**：正文引用（`extractResourcePaths`）vs 包内实有（后端 content 范围，不含 `scripts/**`），差集进 `failures` 如实列出（不连坐：该包照常纳入） |
| **TD-11-24** | `applyCloudSkillsToDisk` 的形状不全条目从**静默 `continue`** 改为**计入 `failed`**（带可展示原因）⇒「云端拉回 N 条、实写 M 条」的差额在 UI 上可见 |
| **TD-11-28** | 导出回落成"仅正文"时（`skillMarkdownForExport` 返回 `null`）**如实提示**"frontmatter 声明已丢"（warning），不再只说"已导出" |

## 二 · 探针（先红后绿 · 凭证）

```
🔬 探针结论｜TD-11-17 块① 缺不可信条款
   文件   : src/components/agent/skill/skillCatalog.ts（命中 1 处，已还原 sha=e9367c054a1e）
   命令   : npx vitest run tests/unit/skillCatalog.test.ts
   观测   : exit=1｜输出 26 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「UNTRUSTED_SKILL_TEXT_RULE」 — 命中
```

```
🔬 探针结论｜TD-11-21 装载期引用对账
   文件   : src/components/agent/skill/skillHydrate.ts（命中 1 处，已还原 sha=4eece4ff1276）
   命令   : npx vitest run tests/unit/skillHydrate.test.ts
   观测   : exit=1｜输出 22 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「references/」 — 命中
```

```
🔬 探针结论｜TD-11-24 形状不全不再静默
   文件   : src/components/agent/skill/skillCloudSync.ts（命中 1 处，已还原 sha=2eef4a12296e）
   命令   : npx vitest run tests/unit/skillCloudSync.test.ts
   观测   : exit=1｜输出 23 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「形状不全的条目」 — 命中
```

## 三 · 验证（相关测试）

`skillCatalog · skillHydrate · skillCloudSync · canvasAgentTools` → **115 例绿**（含本轮新增 4 例与**加强 1 例**）；
`type-check` 0 错。

## 四 · 回改原文

- `tests/unit/skillCloudSync.test.ts`：既有用例「形状不全的条目跳过，不因此中断其余」**锁的是旧语义（静默）** ⇒ 就地回改用例名与断言（加"计入 failed"），实现与测试同批改。
- `skillCatalog.test.ts`：块① 用例加"含不可信条款"断言（首末句断言不变 ⇒ 条款插在中间）。

## 五 · 结清与剩余

- **结清**：TD-11-17 · 21 · 24 · 28（本区累计结清 **14** 条）。
- **仍待还 10 条**：TD-11-25 · 26 · 27 · 29 · 34 · 35 · 36 · 37 · 38 · 39。
- 下一批：**TD-11-38**（预算无上界，用户可把自己顶爆）→ **TD-11-26**（`truncateSkillContent(_,0)` 反直觉契约）→ **TD-11-27/34**（两处 SSOT 第二份：2MB 上限、文本扩展名白名单）→ 其余低息（25 / 29 / 35 / 36 / 37 / 39，含后端三项需单独轮次）。
