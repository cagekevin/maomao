# 架构日志 · utils 工具层（横切纯函数） · 2026-09-13

> **文件名**：`16-utils工具层-横切纯函数-2026-09-13.md`。本区为**其他领域**（区域 01–15 仅把单个 util 当作某业务段的脚注，从未把 `base/utils/` 作为主域审计；区域 15 收口笔记显式把本域列为「下轮候选 ④」）。自主追加为第 16 区（编号顺延 `max(NN)+1`，依据 `index.md` 区域治理规则第 1 条）。
> **为何漏列**：`base/utils/` 18 个纯函数工具散落各业务段引用，从未作为「横切纯函数域」整体审计；其中多数已被历史审计收口为单源（assetType / imagePixel / arrangePack / asyncGuard / clipboard），但从未集中取证留痕，下次极易被当「未审」重审或误当债补。
> 审计视角：数据流（纯函数无 fan-in 数据流，按「定义地基 → 消费方」取证）。实证统一用 `node scripts/mv-sync-refs.mjs refs <file>` + 读真实代码（**全程只读不改码**）。

## 一、链路快照（refs 实证，非凭文件名猜）

> 本域为叶纯函数，无跨模块数据流；取证重点是「是否仍是单源 / 有无死参 / 有无第二份真相」。
> `node scripts/mv-sync-refs.mjs refs src/components/base/utils/providerUrlAdapters.ts`（区域 16 首轮 2026-09-13）：

```
① 模块引用 1 处（fan-in）：
   - src/components/base/panels/sections/ApiSettings.tsx   ← 唯一消费方（PROVIDER_PROTOCOL_LABELS）
② 字符串残留引用 2 处（非逻辑）：
   - src/components/agent/runtime/agentRuntime.ts:27（仅注释，指旧 /api/proxy 退役）
   - src/components/base/README.md:53（工具清单列举）
```

> 其余本区文件 fan-in（grep 实证，非逐文件 refs 全量，但均 ≥1 真实消费方，无 0 引用死模块）：
> - asyncGuard（loadImageOrNull 已收口 imageCompress/faceMosaic/OverlayEditor/GridMergeNode 私有实现）→ 多消费方
> - clipboard（copyImageToClipboard/buildNodesFromClipboard/resolveDownloadFilename 等）→ 多消费方（AssetNode/ImageGenerate/useAssetDropPaste/backupStore）
> - providerModels（buildAllModels/resolveProviderModel）→ ApiSettings/ModelSelect/节点模型下拉
> - refToken（encodeRefToken/parseRefTokensFromText）→ agentRuntime/useCanvasAgentTools
> - arrangePack（packComponents）→ useArrangeCanvas
> - assetType（classifyAssetUrlKind/detectAssetType/isAudio 等）→ AssetNode/useAssetDropPaste/resourceStore/useConnectedInputs/VideoProcessNode（已在区域 03/04/12 旁证为单源）
> - imagePixel（RATIO_PIXEL_TABLE/resolveImagePixel）→ generate.ts
> - volumePolicy（applyConversationBudget 等）→ conversationState.ts:136

## 二、溯源（Phase 1：症状 vs 病根）

- 本域是「横切纯函数工具集」，健康项占绝对多数（历史审计已把散落实现收口为单源表：assetType 的 EXT_KIND、imagePixel 的 RATIO_PIXEL_TABLE、asyncGuard 的 loadImageOrNull、clipboard 的统一复制/清洗）。
- 本区不承载用户数据主链路，债多为「局部整洁度」级（死参 / 第二份展示映射），无数据正确性级债。

## 三、定海（Phase 2：不变式 / 唯一真相源）

- **媒体类型判定单源**：`assetType.EXT_KIND` 一张表 + `DATA_PREFIX`，文件头明写「禁止再就地手写 `\.(mp4|webm…)$`」——此前 5 处各写一份已漂移，现统一（区域 03/04/12 旁证健康）。**非债**。
- **比例×清晰度像素单源**：`imagePixel.RATIO_PIXEL_TABLE`，自 imageApi 迁出（L3 收口），供 generate.ts 与测试复用。**非债**。
- **图片加载单源**：`asyncGuard.loadImageOrNull` 收口各模块私有 loadImage 实现（含超时 + 两级 crossOrigin 重试），无散落第二份。**非债**。
- **协议显示名映射**：`providerUrlAdapters.PROVIDER_PROTOCOL_LABELS` 是「机器协议 key → 中文展示名」的前端展示映射，与后端协议标识符是两关注点（key 是机器契约、label 是呈现），fallback 显示「未知」优雅降级。**非债（展示映射，非真相第二份）**。

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- 唯一真缝隙：`volumePolicy.applyConversationBudget` 第三参 `_activeId` 是保留位死参（见 §五 TD-16-1），收敛口 = 删形参。
- 其余文件无需切割。

## 五、探债（Phase 4：技术债账本）

> 本轮**只读**，1 笔债只登记；偿还计划写"将来怎么改"，不执行。

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-16-1 | `volumePolicy.ts:225` `applyConversationBudget(conversations, budget, _activeId?)` 第三参 `_activeId` 全程未引用（文件头注释 line 224 自承「该形参目前未参与计算（保留位）」）；调用方 `conversationState.ts:136` 只传前两参 `applyConversationBudget(normalized, SAFE_BUDGET_BYTES)` → 死参 / 假护栏，误导维护者以为落盘投影按 activeId 做了差异化处理 | 增债 | 结构债 | volumePolicy 落盘投影接口（误导后续改动） | 低 | 孤例（死参数，无母体；5 问④「死代码/死参数/假护栏」命中） | 删 `_activeId` 形参（同步删文件头注释 + volumePolicy.test 若引用第三参则改 2 参）；属纯删参、零行为变化 | 待还 |

> 健康项（已裁定非债，留痕防重审）：
> - `assetType.ts`（EXT_KIND 单源，区域 03/04/12 旁证）🟢非债
> - `imagePixel.ts`（RATIO_PIXEL_TABLE 单源，自 imageApi 迁出）🟢非债
> - `asyncGuard.ts`（loadImageOrNull 收口私有实现 + 超时/crossOrigin 重试单源）🟢非债
> - `clipboard.ts`（复制/清洗/下载统一出口，resolveDownloadFilename 已修「先补扩展名后判空」死分支）🟢非债
> - `providerModels.ts`（buildAllModels/resolveProviderModel 单一真源，兼容旧值回退）🟢非债
> - `providerUrlAdapters.ts`（PROVIDER_PROTOCOL_LABELS 前端展示名映射，fallback「未知」优雅降级；注释指 `PROVIDER_PROTOCOLS` 但 localTool 无该符号 = 轻微 doc 漂移，不单列债）🟢非债
> - `refToken.ts`（encodeRefToken/parseRefTokensFromText 编解码纯函数；re/re2/re3 三态对应新格式/裸 token/旧格式三类，非重复实现）🟢非债
> - `arrangePack.ts`（packComponents 连通分量打包纯函数，单消费方 useArrangeCanvas，O(n²) 可忽略）🟢非债
> - `volumePolicy.ts` 其余（L1 sanitizeLastResults/capConversationMemory · L2 pending 去重 · L3 estimateConversationsBytes/applyConversationBudget 投影降级）🟢非债（仅 `_activeId` 死参，见 TD-16-1）

## 六、覆盖度表（查过什么 / 还剩什么 —— 防重复审、防假装审完）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| src/components/base/utils/volumePolicy.ts | L1 体积治理纯函数 | 深审 | 🔴 | 首轮 09-13 | :225 `_activeId` 死参 + 注释 :224「该形参目前未参与计算」+ conversationState.ts:136 只传两参 | TD-16-1 |
| src/components/base/utils/asyncGuard.ts | L1 异步边界守卫 | 已裁定非债 | 🟢 | 首轮 09-13 | loadImageOrNull 收口私有实现，超时/crossOrigin 重试单源 | 非债 |
| src/components/base/utils/clipboard.ts | L1 剪贴板工具 | 已裁定非债 | 🟢 | 首轮 09-13 | 统一复制/清洗/下载；resolveDownloadFilename 已修死分支 | 非债 |
| src/components/base/utils/providerModels.ts | L1 模型选择工具 | 已裁定非债 | 🟢 | 首轮 09-13 | buildAllModels/resolveProviderModel 单源，兼容旧值 | 非债 |
| src/components/base/utils/providerUrlAdapters.ts | L1 协议展示名 | 已裁定非债 | 🟢 | 首轮 09-13 | refs:1 fan-in(ApiSettings)；展示映射 + fallback「未知」 | 非债 |
| src/components/base/utils/refToken.ts | L1 参考图 token | 已裁定非债 | 🟢 | 首轮 09-13 | 编解码纯函数；re/re2/re3 三态非重复 | 非债 |
| src/components/base/utils/arrangePack.ts | L1 打包纯函数 | 已裁定非债 | 🟢 | 首轮 09-13 | packComponents 单消费方 useArrangeCanvas | 非债 |
| src/components/base/utils/assetType.ts | L0 媒体类型单源 | 已裁定非债 | 🟢 | 首轮 09-13 | EXT_KIND 单源，区域 03/04/12 旁证（他区审） | 非债 |
| src/components/base/utils/imagePixel.ts | L0 像素查表单源 | 已裁定非债 | 🟢 | 首轮 09-13 | RATIO_PIXEL_TABLE 单源，自 imageApi 迁出 | 非债 |

**覆盖账**：本区共 9 个未审文件（区域 15 收口笔记列为候选④，本轮首审）/ 本轮审 9 个 / 未审 0 个（全部列名：volumePolicy 🔴 · asyncGuard 🟢非债 · clipboard 🟢非债 · providerModels 🟢非债 · providerUrlAdapters 🟢非债 · refToken 🟢非债 · arrangePack 🟢非债 · assetType 🟢非债 · imagePixel 🟢非债）。无未审行，本区可称审完（剩 1 债待还）。已在他区审过的 util（assetUrl/uploadDirs/mediaType/genErrors/previewUrl/externalizeInline/imageCompress/imageUpscale/faceMosaic/videoEngine）不重审，状态沿用他区覆盖度。

---

## 七、结论与下一步

- 本区域状态：**已验证(有债待还)**（1 债：TD-16-1 `volumePolicy._activeId` 死参，结构债低息待还）。
- 待还债项已流入 `daily/架构日志/债务.md`：是（TD-16-1）。
- DATAFLOW.md：本域此前无进度节点，已**就地新增「工具层（横切纯函数）」段**（含灯，只放灯未塞明细）。
- 其他候选未登记领域（仍待续）：
  - ⑤ base/ui 基础组件库（20 tsx 叶组件，债密度低，本轮未审；下轮可抽审 Select/RenameDialog/ContextMenu 等关键原语）
  - ⑥ edges 渲染（CustomEdge/Comet/ConnectionLine/CustomHandle，CustomEdge 已随 TD-04-17 审，余 3 未审）
  - ⑦ 全局导航/全屏壳编排（TopNav/canvasSyncBus/FullscreenShell 本轮旁证健康：canvasSyncBus CANVAS_SYNC_CHANNEL 单源、TopNav 诚实处理云同步 partial 失败，非债留痕）
  - ⑧ 日志聚合上报（logger.ts `reportToBackend` 200ms 单槽去重，本轮判定**非债**：仅对「完全相同 category:action:detail」去重避免日志刷屏，console 仍全量输出，设计意图合理）
