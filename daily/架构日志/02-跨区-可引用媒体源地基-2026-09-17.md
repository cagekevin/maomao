# 02 · 存储 / 持久化 · 跨区 · 可引用媒体源（M4）地基落地 · 2026-09-17

> **性质**：`docs/136` 方案 A 的**地基施工轮**（§七 Step 6）—— 建 `base/media/` 横切能力层。
> **触发区**：02（存储/持久化 —— M7 母体刚收口，其消费侧阻塞解除）；**跨区影响**：04（画布节点读取）· 22（视频剪辑器，下一轮消费方）。
> **结论落点**：本文件（唯一写全处）。上游：`docs/136`（方案）· `docs/137`（M7 系统性评估，已收口）。
> **用户口径**：「**地基一定要进到底层，可不要进到中间层**」—— 落点必须是 `base/`（横切地基），不得落 `videoEditor/`（中间业务层）。

- **本区域状态**：`已验证(无债)` —— 本轮地基落地，**无新增债**（M4 母体的**读取层**缺口补齐；消费侧另轮）。

---

## 〇 · 一句话

> M7（工程数据无统一契约）收口后，`docs/136` 的**前置阻塞已解除**；
> 本轮只做**地基**（`base/media/` 5 文件 + 1 处接入 + 1 单测），
> 交付「**列出所有可引用媒体**」这一全库此前**缺席的能力层** ——
> 消费方（剪辑器 / `@提及`）**下一轮**才接入，本轮刻意不动 UI。

---

## 一 · 前置核对（施工前取证）

| 前置（`docs/136` §〇「施工前必读」） | 状态 | 取证 |
| --- | --- | --- |
| **M7 必须先解** | ✅ 已解 | TD-02-30~35 全结清（`02-存储-持久化-跨区-载体收口-2026-09-16.md`） |
| P0-1① 剪辑工程没进备份（TD-02-30） | ✅ 已修 | `backupStore` 新增 `kv` 段 |
| P0-1② 素材元数据在 IndexedDB（TD-02-35） | ✅ 已迁 KV | `video_editor_media_meta_{projectId}` |
| P0-4 / R11 GC 误删（TD-02-31） | ⛔ 已裁定非债 | 心法铁律 7（消费者不约束所有方） |
| 地基本体（§七） | ⬜ 未动工 | `base/media/` 目录此前**不存在**；`MediaRef` 全库 0 命中 |

⇒ **阻塞解除，地基可动工**。

---

## 二 · Step 0 普查（份数 → 处置 · 复用不重写）

| 能力项 | 现有实现 | 份数 | 处置 |
| --- | --- | --- | --- |
| ① 从画布节点取媒体 URL | `base/canvas/nodeMedia.ts::getNodeMedia` | 1 | ✅ 复用（provider 直接调） |
| ② 节点 data 解析显示 URL | `base/utils/assetUrl.ts::resolveAssetDisplayUrl` | 1 | ✅ 复用（**不重写**字段嗅探） |
| ③ 素材库列表（后端） | `base/api/localToolApi.ts::fetchResources` | 1 | ✅ 复用 |
| ⑥ URL 归一（相对 `/files/` → 绝对） | `base/core/utils.ts::toAbsoluteFileUrl` | 1 | ✅ 复用 |
| ⑦ 媒体类型判定 | `base/utils/assetType.ts::detectAssetType` | 1 | ✅ 复用 |
| ⑧ 画布节点 → 树外可读 | **无** | 0 | ❌ 新建（`canvasNodesBridge.ts`） |
| ⑨ 「来源」概念本身 | **无** | 0 | ❌ 新建（本方案主体） |
| ⑩ 来源注册表 | **无** | 0 | ❌ 新建 |

**结论**：①–⑦ **一行不重写**，全部复用（与方案普查表一致）；⑧⑨⑩ 才允许新建。

---

## 三 · Step 6 施工清单（实际落点）

| # | 文件 | 内容 |
| --- | --- | --- |
| 1 | `base/media/mediaRefTypes.ts` | `MediaRef` / `MediaRefSource` / `MediaRefType` / `MediaRefQuery` / `MediaRefProvider` / `MediaRefSearchResult` + `makeMediaRef`/`parseMediaRef` |
| 2 | `base/media/canvasNodesBridge.ts` | 画布节点只读快照（写 / 读 / 订阅三件套） |
| 3 | `base/media/mediaRefRegistry.ts` | 注册表：`register` / `list` / `has` / `query` / `search` + **诚实失败语义** |
| 4 | `base/media/providers/canvasSource.ts` | `source='canvas'`，复用 `getNodeMedia` + `resolveAssetDisplayUrl` |
| 5 | `base/media/providers/librarySource.ts` | `source='library'`，复用 `fetchResources` + `detectAssetType` |
| 6 | `base/media/providers/index.ts` | 唯一 import 点（模块副作用自注册 —— 有唯一清单，不会漏） |
| 7 | `base/media/index.ts` | 统一出口（import 即完成自注册；消费方只认这一个文件） |
| 8 | `src/App.tsx` | 在既有 `nodesRef` effect 旁加 1 行 `setCanvasNodesSnapshot(nodes)` |
| 9 | `tests/unit/mediaRef.test.ts` | 11 例 |

**分层落实（用户口径「进到底层」）**：
- 全部落 `src/components/base/media/`（横切地基）；
- **零** `videoEditor/` 改动（消费侧适配器 `linkMediaRefsToProject` 属业务，**留在剪辑器改造区**，下一轮）；
- `check:arch` 规则 2（base 禁反向依赖业务域）**自动覆盖**，无需新闸。

---

## 四 · Step 7 验证

### 4.1 探针（先红后绿 · `scripts/probe.mjs`）

```
🔬 探针结论｜docs/136 地基 ref 分隔符写错 负例
   文件   : src/components/base/media/mediaRefTypes.ts（命中 1 处，已还原 sha=7762c180ed96）
   命令   : npx vitest run tests/unit/mediaRef.test.ts --reporter=basic
   观测   : exit=1｜输出 82 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「mediaRef.test.ts」 — 命中
```

还原后同命令 **11 例全绿**（后绿）。

### 4.2 测试与门禁

| 项 | 结果 |
| --- | --- |
| `npx tsc --noEmit` | ✅ 0 错 |
| `npm run test:unit` | ✅ **234 文件 / 2913 例**全绿 |
| `npm run test:smoke` | ✅ ALL PASS |
| `npm run check:arch` | ✅ 通过（含规则 2 base 反向依赖） |
| `npm run check:any` / `check:catch` | ✅ 0 违规 |
| `npm run lint` | ✅ 0 error（5 warning 均在 `videoEditor/hooks-cutia`，既有、与本轮无关） |
| `npm run build` | ✅ 成功（6.35s） |

### 4.3 新增测试覆盖（11 例）

| 组 | 覆盖 |
| --- | --- |
| `mediaRefTypes` | ref 编解码往返 · 非法 ref（无分隔符/空 id/未知来源）→ null · id 含冒号不丢信息 |
| `mediaRefRegistry` | 未注册 query 抛错 · 重复注册抛错 · `searchMediaRefs` 部分成功（失败被暴露）· 相对 URL 只留痕不抛 |
| `canvasNodesBridge` | 引用相等短路 · 订阅/退订 · **源码级：不 import 任何存储** |
| providers | canvas 只收有媒体节点 + type/keyword 过滤 · library 非图/视频/音频不收录 + url 已归一 |

---

## 五 · 与 `docs/136` 原文的**形态差异**（A7 回改）

| 位置 | 原文 | 实际 |
| --- | --- | --- |
| §七 文件清单 5 个 | — | **7 个**（多 `providers/index.ts` 唯一 import 点 + `media/index.ts` 统一出口）—— 让"消费方只需 import 一个文件"成立 |
| §七 施工顺序 | 5 步 | 同序，另加 barrel 收口 |
| §八 验收「下一轮消费方可用 3 行接入」 | — | ✅ 成立：`import { queryMediaRefs } from 'base/media'` 即用全部来源 |
| §9.6 第 0 组（M7） | 0-a~0-d | ✅ 全部结清（`docs/137` 方案 A 落地），**不再是前置** |

> **未碰**：§九 消费侧（第 1~3 组）**一行未动** —— 去 `releaseResourceRefs`、`saveMediaAsset` 加"已有 url"分支、
> `linkMediaRefsToProject`、`media.tsx` 三来源切换，全部留待下一轮（消费侧）。

---

## 六 · 覆盖度

| 文件 | 灯 |
| --- | --- |
| `base/media/mediaRefTypes.ts` | 🟢 |
| `base/media/canvasNodesBridge.ts` | 🟢 |
| `base/media/mediaRefRegistry.ts` | 🟢 |
| `base/media/providers/canvasSource.ts` | 🟢 |
| `base/media/providers/librarySource.ts` | 🟢 |
| `base/media/providers/index.ts` | 🟢 |
| `base/media/index.ts` | 🟢 |
| `src/App.tsx`（bridge 接入 1 行） | 🟢 |

---

## 七 · 遗留 / 未做（有意）

1. **消费侧全部未动**（`docs/136` §九 第 1~3 组）—— 明确划界，下一轮。
2. **A-6 的 IndexedDB 专项闸未建**（`docs/137` §八）：现 `src` 全域已 0 处 IndexedDB 引用，随收口自然消失，无需建闸。
3. **`imageBoxNode` 多图只取主媒体**（§十 R1）：如需展开，应扩 `nodeMedia.ts::getNodeMediaList`（**扩真源，不在 provider 旁路**）。
4. **素材库只取第一页**（R2）：`meta.hasMore` 已暴露；全量拉取若需要，应在 provider 内循环（不让消费方各写分页）。
5. **`@` 搜索只能搜已加载页**（R12）：`meta.keywordScope='loaded-page-only'` 如实标注，**不假装搜了全部**。
