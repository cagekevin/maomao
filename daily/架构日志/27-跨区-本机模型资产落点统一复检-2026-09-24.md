# 架构日志 · 跨区复检（localTool 后端 · base 横切层 · 审计工具链）· 2026-09-24

> 主题：**复检 `27-跨区-本机模型资产落点统一-2026-09-24.md`（执行 `docs/plan/147`）**。
> 该轮自报「本轮未新登记债 · 已验证（无债待还）」⇒ 本轮**重开文件、回退状态**（SOP §四.1）。
> 范围：`localTool/src/{paths,index}.ts` · `localTool/src/utils/localOnlyPaths.ts` ·
> `localTool/scripts/{runtime-model,fetch-runtime-models}.mjs` · `localTool/scripts/aliyun-models.py` ·
> `localTool/.gitignore` · `.gitignore` · `.eslintrc.cjs` · `localTool/runtime-models/{mediapipe,three}/` ·
> `src/components/base/core/runtimeModelUrl.ts` · `src/components/image/lib/faceMosaic.ts` ·
> `src/components/director3d/models.tsx` · `vite.config.ts` · `scripts/check-arch.mjs` · `scripts/arch-index.mjs` ·
> `spec/判据登记表.md` · `docs/plan/147` · 原轮次文件。
>
> **复检结论**：原轮次的**主体施工经取证成立**（落点真源唯一、一归/减二/一诚实凭证可复现、
> `check:arch` 实跑通过、sha256 抽验一致、入库规则 `git check-ignore` 实证生效）——
> **但「本轮未新登记债」不成立**：查出 6 笔债（含 1 笔复发母体）+ 1 处就地改正。原轮次 §七 状态已回退。

---

## 一、探债

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话·可选) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-17-37 | `scripts/arch-index.mjs:56`（`AREAS` 止于 `'25'`）· 原轮次文件本体 | 区号越界·无闸 | 审计工具链（元层） | `index.md` 的「区域清单+进度」漏掉 26/27 两轮 ⇒ SOP Step 1「读 index 定起点」看不见本轮；本复检文件同样不可见 | 中 | **母体**：区域清单缺「孤儿轮次文件」守卫（26 号 2026-09-21 已同形 = 复发） | 把 26/27 登记进 `AREAS`（SOP §四.1 编号顺延），或给生成器加「轮次文件编号 ∉ AREAS 即红」的守卫 | 待还 |
| TD-17-38 | 原轮次文件 `:66-75`（覆盖度表 6 行 vs 覆盖账「共 12 / 审 12 / 未审 0」） | 覆盖账不可复现 | 审计流程（元层） | 覆盖度表是「不重复查」的唯一机制：本轮改动的 7 个文件无行 ⇒ 下轮重审或漏审（= 假「审完」） | 中 | 孤例 | 补齐本轮改动文件（`faceMosaic.ts`／`director3d/models.tsx`／`vite.config.ts`／`check-arch.mjs`／两个 `.gitignore`）的覆盖行，覆盖账改「逐个列名」 | 待还 |
| TD-08-72 | `docs/plan/147-本机模型资产统一方案-2026-09-23.md:88-107`（§2.2）· `:343-345`（§十） | 文档漂移·第二份口径 | localTool 后端（MANIFEST 消费方） | 新模型接入者照 §2.2 写清单 ⇒ 四项均与 `fetch-runtime-models.mjs` 不兼容（`path` 基准 `<modelId>/` vs `runtime-models/` · `modelId`↔`tool` · `purpose`↔`description` · `source` 实为空串） | 中 | 孤例 | 回填 §2.2 为实测 schema，并同步 §十 的载体数（原轮次 §五 只承认了 `path` 一项） | 待还 |
| TD-24-11 | `localTool/src/utils/localOnlyPaths.ts:27` | 注释漂移（历史最高频形态 41 例） | base·前缀（跨栈对账项另一侧） | 读者按注释找不到文件；`check:arch` 的 `PREFIX_MODELS` 对账项「另一侧在哪」指向失效 | 低 | 孤例 | 注释改指 `src/components/base/core/runtimeModelUrl.ts`（全仓仅此一处残留旧名） | 待还 |
| TD-23-6 | `.eslintrc.cjs:55`（`'public/mediapipe/wasm/**'`） | 死配置（幽灵预留 27 例） | 仓库根·lint 配置 | 搬迁后该 ignore 命中零文件；plan §4.1 收益③「坑从根上消失」只对了一半 —— 新落点 `localTool/runtime-models/mediapipe/wasm/` 不在任何 ignore 中（当下不触发仅因 `npm run lint` 只扫 `src tests`） | 低 | 孤例 | 删该行，或改指新落点 | 待还 |
| TD-08-73 | `localTool/scripts/runtime-model.mjs:254`（取 `--from` 值）· `:147`（`if (fromArg)`） | 静默吞/失败伪装成功（29 例） | localTool·取件 CLI | `init <modelId> --from`（漏写路径）⇒ `argv[fromIdx+1]` 为 `undefined` ⇒ 跳过接管、仍生成清单并打印「已写出清单」⇒ 用户以为文件已接管 | 低 | 孤例 | `fromIdx >= 0 && !fromArg` 时抛错（工具债，SOP §四.1 要求当场修；本轮按用户指令只登记） | 待还 |

### 1.1 就地改正（活表维护，非债）

| ID | 位置 | 事实 | 动作 | 状态 |
| --- | --- | --- | --- | --- |
| — | `spec/判据登记表.md` §九 #14（原 `:313`） | 原记 `N=3`、备注「另 **2** 处脚本侧锚点」；实测脚本侧 **3** 处（`fetch-runtime-models.mjs:30` · `runtime-model.mjs:38` · `aliyun-models.py:39`）⇒ 总载体 **4**；原轮次 §八 自己写「脚本侧锚点由 2 处 → 3 处」= 自相矛盾 | 按 SOP §一.2「活表失真就地改正」：`N 3→4`、备注「另 3 处」 | 已改正 |

### 1.2 裁定非债（留痕，防重审）

| # | 位置 | 描述 | 裁定 |
| --- | --- | --- | --- |
| N1 | `localTool/src/index.ts:260-264`（`try{decodeURIComponent}catch{}`） | 原轮次 §八「减一」引它作「原样保留」证据 | **非债**：失败**可见**（保原样 → `existsSync` → 404，调用方知情）；与既有 `handleStaticFile:191` 同款，非本轮新增（SOP §零.3 判定只看「失败是否可见」） |
| N2 | `localTool/src/index.ts:268`（`resolvedPath.startsWith(path.resolve(modelDir))`） | 注释自称「防止路径遍历」 | **非债（低价值留痕）**：单层 `..%2f` 可落到同名前缀兄弟目录（`/models/three/..%2fthreeX/x` → `runtime-models/threeX/x`），但该目录本身已由 `/models/<modelId>/` 公开服务 ⇒ **零提权**；跨根/多级 `..%2f` 均被拦（原轮次 curl 实证 `..%2f..%2f..%2fetc/passwd` → 403） |
| N3 | `public/manifest.json:50-51`（`mediapipe/*` 仍在 `web_accessible_resources`） | 已成死声明（扩展包内已无该目录） | **已裁定非债**：用户 2026-09-24 明示「Chrome 生态不是最重要的，那个可以不管」（plan 147 §十 #2） |
| N4 | `localTool/runtime-models/*/MANIFEST.json` 的 `path` 相对 `runtime-models/` | 与 plan §2.2「相对 `<modelId>/` 根」不一致 | **有意分缝**（保 §4「depth-video 零改动」）；但 plan 未回填 ⇒ 已并入 TD-08-72 |
| N5 | `three/x-bot.fbx` · `three/ue-mannequin-retopology.glb`（`src/` 零引用仍搬入） | 幽灵预留形态 | **非债**：plan §4.1 明确「搬进 `three/` 备用」，且已在 MANIFEST `description` 与 plan §4.1 留痕 |

---

## 二、覆盖度表

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `localTool/src/paths.ts` | 后端·落点 | 深审 | 🟢 | 复检 / 2026-09-24 | `getRuntimeModelDir:68` · `DEPTH_VIDEO_MODEL_ID:78` | 真源唯一（`localTool/src` 内 `runtime-models` 字面量仅此处拼装） |
| `localTool/src/index.ts` | 后端·宿主 | 深审 | 🟢 | 复检 / 2026-09-24 | `handleRuntimeModelResource:237-303` · 分派 `:374` | 已销物理目录名副本；N1／N2 裁定非债 |
| `localTool/src/utils/localOnlyPaths.ts` | 后端·前缀 | 深审 | 🔴 | 复检 / 2026-09-24 | `:27` 注释旧名 | **TD-24-11** 注释漂移 |
| `localTool/scripts/runtime-model.mjs`（新） | 后端·取件 CLI | 深审 | 🔴 | 复检 / 2026-09-24 | `:254` / `:147` | **TD-08-73** 静默降级 |
| `localTool/scripts/fetch-runtime-models.mjs` · `aliyun-models.py` | 后端·脚本 | 深审 | 🟡 | 复检 / 2026-09-24 | `:30` / `:39` 自相对锚点 | 跨语言结构必然（非债） |
| `localTool/.gitignore` | 后端·入库 | 深审 | 🟢 | 复检 / 2026-09-24 | `:20-22` | `git check-ignore -v` 实证：清单/说明入库、资产不入库 |
| `.gitignore` | 根·入库 | 深审 | 🟢 | 复检 / 2026-09-24 | `:152-155` | 按扩展名列举的 4 行已销，口径归一 |
| `.eslintrc.cjs` | 根·lint 配置 | 深审 | 🔴 | 复检 / 2026-09-24 | `:55` | **TD-23-6** 死配置 |
| `src/components/base/core/runtimeModelUrl.ts`（新） | base·前缀 | 深审 | 🟢 | 复检 / 2026-09-24 | `:24` `PREFIX_MODELS` · `:32` `runtimeModelUrl` | 前端唯一出口（消费方无裸拼，全仓 grep 已验） |
| `src/components/image/lib/faceMosaic.ts` | 上层·消费 | 深审 | 🟢 | 复检 / 2026-09-24 | `:71` · `:74` | 走 `runtimeModelUrl`；扩展分支已移除（N3 已裁定） |
| `src/components/director3d/models.tsx` | 上层·消费 | 深审 | 🟢 | 复检 / 2026-09-24 | `:86` `BUILT_IN_MODEL_URL` | 走 `runtimeModelUrl`；`BASE_URL` 依赖已摘 |
| `vite.config.ts` | 构建·dev 代理 | 深审 | 🟢 | 复检 / 2026-09-24 | `:153-154` | `/models` · `/depth-video` 同源口径一致 |
| `scripts/check-arch.mjs` | 元层·闸 | 深审 | 🟢 | 复检 / 2026-09-24 | `CROSS_STACK_CONSTS` `PREFIX_MODELS:2211` | **实跑 `npm run check:arch` 通过**（11 项常量 + 2 项类型） |
| `scripts/arch-index.mjs` | 元层·索引 | 深审 | 🔴 | 复检 / 2026-09-24 | `AREAS:56-82` 止于 `'25'` | **TD-17-37** 区号未登记（母体） |
| `localTool/runtime-models/{mediapipe,three}/MANIFEST.json`（新） | 后端·清单 | 深审 | 🟡 | 复检 / 2026-09-24 | `mediapipe` 5 条 · `three` 3 条；sha256 抽验 2/8 一致 | **TD-08-72**（schema）；约定本身有意分缝（N4） |
| `spec/判据登记表.md` §一#23 · §九#14 | 文档·活表 | 深审 | 🟢 | 复检 / 2026-09-24 | `:313` | §九#14 载体数已就地改正（§1.1） |
| `docs/plan/147-…-2026-09-23.md` | 文档·方案 | 深审 | 🔴 | 复检 / 2026-09-24 | §2.2 `:88-107` · §十 `:343-345` | **TD-08-72** schema/载体数未回填 |
| 原轮次文件 `27-…-2026-09-24.md` | 文档·轮次 | 深审 | 🔴 | 复检 / 2026-09-24 | `:66-75` | **TD-17-38** 覆盖账不可复现 |

**覆盖账**：本轮复检共 **18 个文件** / 审 **18 个** / 未审 **0 个**（逐个列名见上表）。

**本轮未取证（不申报）**：`depthVideo.test.ts` / `faceMosaic.test.ts` / `localtool.network.test.js` 的「全绿」与
`curl` 三条探针，沿用原轮次记录 —— 本轮按用户指令**零测试执行**（`测试执行次数 = 0`），不重跑、不背书。

---

## 三、结论与下一步

- 本区域状态：**已验证(无债待还)** —— 本节原先报出 6 笔，已于 2026-09-24 当日**全部修复并结清**：
  逐笔修法与验证见 [`27-跨区-本机模型资产落点统一-2026-09-24.md`](./27-跨区-本机模型资产落点统一-2026-09-24.md) §十；
  `债务.md` 六条均为 `[已解决 2026-09-24]` 并已归档。**本文件 §一 的 6 笔记录原样保留（评审留痕），只更正状态行。**
- 待还债项已流入 `daily/架构日志/债务.md`：否（已归档）。
- 原轮次文件 `27-…-2026-09-24.md` §七 状态行：曾按本文件回退，**6 笔结清后已改回「无债」**（其 §十 为修复记录）。
- `spec/DATAFLOW.md`：本轮无链路漂移（原轮次已补 §九 现状，复检对账一致）。
- `spec/判据登记表.md`：§九 #14 载体数已就地改正（N 3→4）。
- 灯：本文件覆盖度表已更新；区域级灯由 `index.md` 自动派生 —— 区号 27 已登记进 `AREAS`（TD-17-37 已结清）。
- 下一步：① 26/27 已登记进 `AREAS` ✅；② plan §2.2 已回填 ✅；③ 两笔工具债（`.eslintrc.cjs:55` · `runtime-model.mjs --from`）已修 ✅。
  **仅余一项留待用户定**：TD-17-37 的**母体**（`AREAS` 缺「孤儿轮次文件」守卫）—— 新造机器守卫须走**建闸前置评审**（`ADR-0016`），本轮不建。
