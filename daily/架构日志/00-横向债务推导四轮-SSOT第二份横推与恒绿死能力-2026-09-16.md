# 00 · 横向债务推导四轮（历史母体横向推导 · 跨端常量双写 + 恒绿死能力 · 2026-09-16）

> **触发**：用户「从历史已有债务深入推导，未登记 没有被发现的债务」。
> **上游**（本轮不重复其结论）：[00-横向债务推导三轮](./00-横向债务推导三轮-2026-09-16.md)（注释漂移种子）· [17 账本口径与注释漂移集群](./17-跨区-账本口径与注释漂移集群-2026-09-16.md) · [16 静默吞母体横向推导复查](./16-跨区-静默吞母体横向推导复查-2026-09-16.md) · [13 底层SSOT横推饱和](./13-跨区-底层SSOT横推饱和-2026-09-16.md) · [08 文件URL查询串口径错位](./08-跨区-文件URL查询串口径错位-2026-09-16.md) · [22 统一图片出口横展](./22-跨区-统一图片出口横展-2026-09-16.md) · [20 幽灵预留清偿轮](./20-跨区-幽灵预留清偿轮-2026-09-13.md) · [11 历史债母体横推找漏](./11-跨区-历史债母体横推找漏-2026-09-16.md)。
> **方法**：以账本高频形态为种子——SSOT 第二份（33 条·#4）／口径错位（26）／死代码·幽灵预留（34）／假护栏（9）——全仓机械 grep 找未被登记的同源实例。先跑机器闸确认 gateable 形态已收口，再对无闸形态做横推。
> **结果**：补登新债 2 笔（TD-08-19 跨端 `MAX_SEND_DIM=1920` 双写 · TD-22-58 timeline 恒绿死能力 stub）；其余候选全部证伪／非债留痕。

---

## 一、链路快照（取证路径，自底向上）

```
种子母体：SSOT 第二份（账本 33 条·#4）+ 镜像 口径错位（26）/ 幽灵预留（34）/ 假护栏（9）
  └→ 全仓机械 grep：跨 src/localTool 同一字面量·常量多处定义；恒绿 disabled；恒空白名单 Set；恒真 if(true)
机器闸先行：check:arch（✅ 通过）/ check:dead-code·knip（✅ 0 新增）→ gateable 形态已收口
未登记债务只可能落在「无闸形态」= SSOT 第二份 / 口径错位 / 恒绿死能力（注释类已三轮扫）
```

## 二、溯源（逐点实证，证据 = 文件:行 / grep / 闸输出）

### 2.1 机器闸结论（gateable 形态已收口，非债留痕）

- `node scripts/check-arch.mjs` → **✅ 架构校验通过**（无裸写 localStorage · 无循环依赖 · 绕过唯一入口全绿）。⇒ 绕过唯一入口、恒真假护栏已机器守卫，无新违规点。
- `node scripts/check-dead-code.mjs`（knip）→ **✅ 0 新增死代码（基线 0）**。⇒ 死代码·幽灵预留已机器守卫，无新。
- **推论**：未登记债务只存在于「无闸形态」——SSOT 第二份（常量双写）／口径错位／恒绿死能力（注释漂移已三轮扫过）。

### 2.2 SSOT 第二份横推（种子 TD-02-41 / TD-13-4 / TD-08-16）

全仓 grep 跨端共享常量与真值：

| 常量／真值 | 实证 | 判定 |
| --- | --- | --- |
| `JIMP_MIME_BY_EXT`／`isJimpEncodableExt`／`jimpMimeForExt` | `fileStore.ts:281` 唯一真源；`resolveLocalImages.ts:29` import 委托；`files.ts:46` 收口；前端 `assetUrl.ts:45` 仅注释引用 | **非债**（TD-02-41 已饱和） |
| 端口 `18080` | `index.ts` `process.env.PORT \|\| 18080` 单源；其余 `18080` 全在 test fixture（`127.0.0.1:18080`） | **非债**（单源，测试写死是 fixture） |
| **`MAX_SEND_DIM = 1920`** | `src/components/base/utils/assetUrl.ts:66` `export const MAX_SEND_DIM = 1920` ↔ `localTool/src/utils/resolveLocalImages.ts:32` `const MAX_SEND_DIM = 1920`；两处注释均自承「契约双写…勿单边漂移」；无共享源、无机器闸 | **TD-08-19 新债**（跨端双写） |
| 存储键 | 13 区 TD-13-4 已收口为 `contracts.ts` 命名 const | **非债**（已饱和） |

### 2.3 恒绿死能力／假护栏横推（种子 TD-11-13 `disabled={false}`）

全 src grep `disabled={true|false}`：

| 命中 | 实证 | 判定 |
| --- | --- | --- |
| `AssistantTablePanel.tsx:341` `disabled={false}` | 已登记 **TD-11-13**（恒传 false → RowOpsMenu 禁用能力永不触发） | 已知债（不重立） |
| **`timeline-toolbar.tsx:113` `disabled={true}`** tooltip「即将推出」+ `onClick={() => {}}` 空 handler | 永久禁用 + 空 handler = 恒绿死能力／幽灵预留；grep 全仓无对应 `split-horizontal` 动作（:95/101/107 的 split/split-left/split-right 为另三枚独立启用按钮） | **TD-22-58 新债**（恒绿死能力 stub） |

`if (true)` 与「恒空白名单 Set」grep：`.regression-entry.cjs`（库打包产物）外 **0 命中** → 真源码中无恒真守卫／假接缝（非债留痕）。

## 三、定海

- **SSOT**：跨端共享「发送压缩最长边上限 `1920`」被两份独立 `const` 维护，仅靠注释约定同步 → 缺机器守卫必漂移（母体「同一真相抄多份」，与 TD-02-41 同母，仅修了 mime 表未修此常量）。
- **恒绿死能力**：timeline 一枚「即将推出」按钮永久 `disabled` + 空 handler，零消费者。

## 四、切割（本轮不审）

- 注释漂移：三轮已扫（App.jsx 改名 ×39 ／ Temp 文档失效引用）。
- 静默吞／绕过唯一入口／死代码：机器闸全绿，已饱和。
- 存储键／mime 表 SSOT：13 区 ／ TD-02-41 已饱和。

## 五、探债（明细真源）

| TD-ID | 决策点／问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-08-19 | `MAX_SEND_DIM = 1920` 在 `assetUrl.ts:66`（前端图片压缩上限）与 `resolveLocalImages.ts:32`（后端出站压缩上限）各定义一份；两处注释均自承「契约双写，勿单边漂移」；无共享源、无机器闸 | 增债 | 结构债 | 2 文件（前端+后端压缩上限）；漂移→前后端压缩尺寸不一致→AI 收图尺寸不符预期 | 中 | 母体 M-SSOT第二份（TD-02-41 同母体，仅修 mime 表漏此常量） | 抽唯一真源：常量单点定义、两处 import（如 shared contract 经 build 注入前端，或前端读后端契约）；或加 build-time 断言两值相等；改一处即同步 | 待还 |
| TD-22-58 | `timeline-toolbar.tsx:113` `SplitSquareHorizontal` 按钮 `disabled={true}` + tooltip「即将推出」+ `onClick={() => {}}` 空 handler，永久死能力、零消费者 | 增债 | 结构债 | 1 按钮 | 低 | 孤例（为将来铺路 stub，注释≈零消费者） | 若功能将做：接真实 handler + 条件 disabled；若放弃：删按钮及图标引用（零行为变化） | 待还 |

**非债留痕**（禁再当 bug 补）：§二 2.1（闸绿）· 2.2 表（JIMP_MIME 已饱和／18080 单源／存储键已饱和）· 2.3（TD-11-13 已知／if(true) 与恒空白名单 0 命中）。

## 六、结论 + 覆盖账

1. 横向推导补登新债 2 笔（SSOT 第二份 ×1 ＋ 恒绿死能力 ×1）；其余候选全部证伪／非债。
2. **方法价值**：先跑机器闸（check:arch／knip）把 gateable 形态一键收口，把人力集中在「无闸形态」——本轮新债全在无闸区，证明该策略有效。
3. **覆盖账**：本轮审 跨端常量＋恒绿按钮 共 4 重点（2 新债 ＋ 2 类非债集群）；未审：注释漂移（三轮已扫）／静默吞·绕过·死代码（闸绿饱和）。
4. DATAFLOW：无链路漂移，不改。

---

## 覆盖度表（本轮增量，不重抄旧行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| - | - | - | - | - | - | - |
| `src/components/base/utils/assetUrl.ts:66` + `localTool/src/utils/resolveLocalImages.ts:32` | 后端/L0 | 有债待还(TD-08-19) | 🔴 | 09-16 四轮 | 两处 `const MAX_SEND_DIM = 1920`；注释「契约双写」 | TD-08-19 |
| `src/components/videoEditor/ui/editor/panels/timeline/timeline-toolbar.tsx:113` | 上层 | 有债待还(TD-22-58) | 🔴 | 09-16 四轮 | disabled={true}+空onClick+即将推出 | TD-22-58 |
| `localTool/src/utils/fileStore.ts`（JIMP_MIME）· `src/.../assetUrl.ts` 其余 | 后端/L0 | 已裁定非债 | 🟢 | 09-16 四轮 | grep 实证已饱和 | 非债 |
| `localTool/src/index.ts`（18080） | 后端 | 已裁定非债 | 🟢 | 09-16 四轮 | `\|\| 18080` 单源；test 为 fixture | 非债 |
| `src/**` 真源码 `if(true)`／恒空白名单 Set | 多区 | 已裁定非债 | 🟢 | 09-16 四轮 | grep 0 命中（除打包产物） | 非债 |

---

## 七、结论与下一步

- 本区域状态：**已验证(有债待还)**（跨端常量双写 ＋ 恒绿死能力 横推补登 2 债）。
- 待还债项已流入 `daily/架构日志/债务.md`：是（TD-08-19 ／ TD-22-58）。
- DATAFLOW.md：已对账一致，未改。
- 灯：本区覆盖度表已更新（🔴×2 ／ 🟢×3）；区域级灯由 `index.md` 自动派生。
