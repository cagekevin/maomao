# 00 · 跨区 · 债务清偿轮（真债判定 + 施工 · 2026-09-16）

> **触发**：用户「先判断哪些是真的债务，哪些不是，然后你自己开始做」（架构师改码 7 步法）。
> **范围**：对**全部 36 条待办债**逐笔做 Step 0/1 普查+证伪，确认为真债且属架构师可自主施工的当场改；业务性/无法安全施工的裁定并交用户。
> **结果**：**12 条收口/改判**（10 已解决 + 2 改判/修正描述），36 → **24 条待办**；新增 1 道架构闸（规则 11）。
> **落点**：明细 = 本文件（跨区只开一份）；摘要 = `debt.mjs` 各条 note。

---

## 一 · 分层判定（Step 1：真债 / 非债 / 业务债）

| 分组 | TD | 判据与动作 |
| --- | --- | --- |
| **A 真债·架构师可自主施工（本轮做）** | TD-16-2 · TD-22-55 · TD-13-4 · TD-04-28 · TD-19-3 · TD-19-2 · TD-07-6 · TD-07-7 · TD-16-3 · TD-04-29 · TD-23-1 | 结构债，改完**功能不变**，只更少/更对 → 直接做（Step 5） |
| **B 非债·描述失真（改判）** | TD-22-54 · TD-23-5（半） | 读代码取证后推翻原描述 → 改判非债/修正描述，留痕防回补 |
| **C 业务性·交用户拍板** | TD-02-33/34/35/36/37（M7 方案A：迁 KV 改持久化+同步范围）· TD-22-52（缩略图开关语义）· TD-22-57（转写模型代理：引入后端依赖）· TD-23-2/3/4（文档/目录约定）· TD-21-16/17（功能项）· TD-09-2（tests 类型翻新） | 改完**用户能做的事变了** → 摆选项交用户 |
| **D 真债但无法安全施工（本轮不做，留证）** | TD-22-20 | 见 §四 |

---

## 二 · 施工明细（A 组）

### 2.1 TD-16-2 + TD-22-55 · 跨源裁决单点被绕过（母体一收全消）

- **母体**：`setCrossOriginForReadable`（同源不设 / 真跨源才 anonymous）红线**只有注释、无机器守卫** → 6 处恒设 `'anonymous'` 精确重演 TD-22-1 缺陷（同源 `/files/*` 走 CORS → 媒体 opaque → canvas 污染 → `toBlob`/`getImageData` 静默 `null`）。
- **Step 3 定位置**：唯一实现**下沉**到 `base/utils/asyncGuard.ts`（更底层，且 `captureFrame → asyncGuard` 已有单向依赖）；`captureFrame.ts` **re-export** → 6 处既有视频链路消费方**零改动**。
- **Step 6 施工（6 处）**：`asyncGuard.loadImageWithTimeout`（默认改走裁决，保留 `null` 逃生口给 `loadImageOrNull` 二级重试）· `clipboard.copyImageToClipboard` · 剪辑器 `image-node` / `sticker-node` · `ImageZoomDialog` 视频预览（ref 动态设）· `VideoExtractNode` 预览（ref 动态设）。
- **Step 7 验证**：
  - 新增 `check:arch` **规则 11**（反向判据：src 全域禁裸写 `crossOrigin='anonymous'` / `<el crossOrigin="anonymous">`，唯一豁免 = 单点实现自身；带扫描基数自检）。
  - **闸探针先红后绿**（`probe.mjs`，结论块）：
    ```
    🔬 探针结论｜TD-16-2/22-55 闸探针
       文件   : src/components/base/utils/clipboard.ts（命中 1 处，已还原 sha=3051169d654e）
       命令   : node scripts/check-arch.mjs
       观测   : exit=1｜输出 24 行   ✅ 退出码 == 1   ✅ 输出含「clipboard.ts:」 — 命中
    ```
  - **行为探针先红后绿**（还原恒设为 anonymous → 新测试红）：
    ```
    🔬 探针结论｜TD-16-2 行为探针（默认裁决）
       文件   : src/components/base/utils/asyncGuard.ts（命中 1 处，已还原 sha=ae10abf85ce4）
       命令   : npx vitest run tests/unit/asyncGuard.test.ts
       观测   : exit=1｜输出 39 行   ✅ 退出码 == 1   ✅ 输出含「不设 crossOrigin」 — 命中
    ```
  - 补 2 例行为测试（同源不设 / 显式 `null` 逃生口）；`asyncGuard`/`captureFrame`/`clipboard` 81 例全绿。

### 2.2 TD-13-4 · 存储键 SSOT 第二份（母体 M7 · 一次收全局）

- **母体**：`STORAGE_KEYS` 登记表自述 SSOT，但 6 个 store 模块**各自本地复刻同值字面量**且被真实读写；一改名只改一处 → `getLocalKeys()` 生成的备份/云同步清单漂移。
- **Step 6 施工**：`contracts.ts` 为 **7 把固定键**导出命名 const（`KEY_YIMAO_*`，模板照 `CANVAS_STATE_PREFIX` / `VIDEO_EDITOR_*_PREFIX`）并用作 `STORAGE_KEYS` 计算键名；6 模块改 `import`（`resourceStore` / `accountsStore` / `promptManager`（含 `preset_recent`）/ `promptHubStore` / `nodePrefs` / `cloudSync`）；另收口 `cloudSync`/`backupStore` 内 4 处裸字面量。
- **验证**：`grep` 确认 `src/`（除 contracts）裸键**清零**；`check:keys` / `check:arch` / `tsc` 绿；`contracts.test` 计数金丝雀 37→38 同步；全量 226 文件 2815 例绿。
- **顺带**：TD-02-33 的「剪辑器 `use-local-storage` 裸写」同族 —— 本轮**只**收口了可自主的键名 SSOT；把 caption 3 键**登记**进表会改变备份/云同步范围（业务性）→ 留 C 组。

### 2.3 TD-04-28 · 节点尺寸「三写不变量」3 处手抄

- **Step 3 判种类**：三处手抄的是**产出形状**（`{width,height,style:{...}}`），属**可收口的重复**；各处判据（何时写）不同、保留。
- **Step 6 施工**：`nodeDefaults.ts` 新增纯函数 `withNodeSize(node,w,h,{includeInitial})`（**返回尺寸补丁**，不是整个 node —— 三形态都自然：`patchNodeById` 合并式 / `{...node,...patch}` 展开式 / 新建全量）；3 处委托（`uiHooks.onMainBoxResize` / `useArrangeCanvas` 排版写回 / `groupNodes` 建组含 `initial*`）。
- **验证**：补 4 例行为锁（三写齐全 / 不含 initial / includeInitial / 补丁不含 id·position·data）；既有 **45 例** arrange/group 测试全绿 → 行为零变化。

### 2.4 TD-19-3 · 面板孪生复制

- **Step 6 施工**：新增 `base/ui/InlineNameInput.tsx`（唯一实现：卡片 UI + 回车/Esc/失焦语义 + **补 IME 守卫**）；`ResourceLibrary` 与 `GeneratedView` 三块逐字复制改委托。
- **行为保真要点**：原「建夹」路径 Enter 无条件建+toast、Blur 仅改名时静默建 → 组件以 `onCommit`/`onBlurCommit` 两 props 保留该差异。
- **验证**：`GeneratedView.refresh` / `resourceStore` / `useResourceMoveToFolder` 36 例全绿。

### 2.5 TD-19-2 + TD-07-6 · 裸 `<img>` 绕过统一图片出口

- **TD-19-2**：`attachmentCover` 视频首帧封面裸 `<img>` → `LazyImage`（破图兜底 + 契约一致）；poster=blob: 经 `useRenderAssetResolver` 原样透传不劣化。
- **TD-07-6**：`director3d/ShotsPanel` 镜头缩略图裸 `<img>` → `LazyImage`（`thumbnail` 是 `/files/` 工程资源，真获按需小图 + 懒加载 + 破图兜底）；`className`/`imgClassName` 匹配原 `.shot-thumbnail` 尺寸。
- **验证**：`AgentMessage` / `AgentPanel` / `LazyImage` 53 例全绿；`tsc` 绿。

### 2.6 TD-07-7 · 旧键 `stageframe-project` 未登记

- **Step 6 施工**：补登 `STORAGE_KEYS`（`backend:'local'` + `migration:'director3d-project'`，仅读不写），消除「裸 localStorage 闸扫不到」盲区。
- **验证**：`check:keys` 绿；计数金丝雀同步 38。

### 2.7 TD-16-3 · 手写 `catch-ok: RELEASE_FAIL` 绕过原语

- **Step 6 施工**：3 处改调原语 —— `audio-waveform` ×2（`releaseQuietly(WaveSurfer.destroy)`）· `audio-manager`（`releaseQuietly(source.stop)`）· `use-editor-actions`（回滚清理 `releaseQuietlyAsync(deleteMediaAsset)`）。
- **施工中修 1 个自己的类型错误**：跨函数边界后 TS 丢失 `assetId` 收窄 → 提为 `const`（`tsc` 复绿）。
- **验证**：全 `src` 仅剩 `asyncGuard` 内 **2 处**（原语实现本身）—— 正是目标。

### 2.8 TD-04-29 · 裸 `console.error`

- `PanoramaNode.SphereErrorBoundary.componentDidCatch` 的 `console.error` → `logger.error('panoNode',...)`（与同文件他处一致：可查 + 上报后端）。

### 2.9 TD-23-1 · 根级过期产物

- **Step 1 取证**：债描述「自动生成的报告」不准 —— 仓内**无脚本**写这两个文件，是一次性 shell 重定向产物；内容为 2026-09-12 的过期计数（留在库里 = 拿过期数字当现状）。
- **Step 6 施工**：`git rm agent-report.txt gate.txt`（内容完整留 git 历史）；`.gitignore` 反向判据 `/*.txt` 已于前轮落地。

---

## 三 · 改判 / 修正描述（B 组）

| TD | 原描述 | 取证后真相 | 处置 |
| --- | --- | --- | --- |
| TD-22-54 | 「注释残留旧 `window.dispatchEvent` 描述，未同步」 | `playback-manager.ts:195-206` 实为**正确记录历史迁移**（明写「原实现是 `window.dispatchEvent`…违反唯一通道红线…现走 `publish(videoeditor:seek)`」）——是文档，不是残留 | **改判非债**（无需改码） |
| TD-23-5 | 「`download/` 与 `mockup/` 未 gitignore 且未归位，含第三方仓库副本易误提交」 | ① `download/` **已不存在**；② `mockup/`（252 文件）**已被 git 跟踪**，是设计样稿工作目录，本就该入库 | **半证伪**（剩余仅「mockup 是否该在根级」= 组织约定，交用户） |

---

## 四 · 未做·留证（D 组）：TD-22-20 —— 「纯机械重构」结论不成立

> 债原描述：「属性面板 15 处 `draft ref + initial ref + onBlur 两段提交` 逐处手抄，**纯机械重构，行为零变化**」。

**Step 1/3 取证后推翻「机械」判断**（三条硬证据）：

1. **15 处非同一变体**：数字 Input 至少 3 变体 —— text 面板**内联两写**（`handleFontSizeBlur` 直接两次 `updateElements`）/ video 面板 `commitNumberField({draft, initial, apply})` / sticker 面板自成一形。
2. **`initialRef` 被同字段的 `<Slider>` 与 `<Input>` 共享**（实证 `text-properties.tsx:446-466`（Slider）与 `:477-482`（Input）共用 `initialFontSizeRef`）——per-field hook 会**改变该耦合结构**，不是「纯机械」。
3. **3 个面板文件零测试覆盖**（`tests/unit` 无 properties 测试）→ 无「先红后绿」网，15 处盲改必然出现「漏 1 处 = 静默错」（正是 A12 警告的形态）。

**已做且已删（弯路留痕，避免留死代码）**：本轮把设计写成可编译 hook `useDraftCommit`（draft/isEditing/initial 三 ref + `onChange pushHistory:false` + `onBlur` 两段提交）+ **6 例行为测试验证通过**（`display`/实时预览/两段提交先后序/未编辑不写库/空草稿守卫/onFocus 播种）。**因上述第 2/3 条未施工即删除** —— 设计留档于此，下一步照此走。

**下一步（正确顺序）**：① 先给 properties 面板补**面板级行为测试网**（mock `useEditor`，断言 `updateElements` 调用序）→ ② 按变体分 2-3 组迁移 → ③ 每组先红后绿。**在此之前禁止盲改。**

---

## 五 · 交用户拍板的选项（C 组，Step 5）

### 选项 1 · M7 方案 A（TD-02-33/34/35/36/37）—— 改持久化格式 + 同步范围

```
方案 A（推荐）：统一迁 KV（经 contentStore 单入口）—— 成本高（4 期），收益：工程数据进备份/云同步 + 消灭 IndexedDB 跑偏 + GC 盲区，风险：存量数据迁移
方案 B：仅补登记（caption 3 键 + director3d-custom-poses 补 backend）—— 成本低，收益：进备份清单，风险：仍是裸 localStorage 旁路
方案 C：不做 —— 后果：saved sounds / 姿势库 / 字幕偏好换机即丢，且不进备份
请决策。
```

### 选项 2 · TD-22-57 转写模型出站口

```
方案 A（推荐）：localTool 加 /api/hf/* 代理（唯一出站口，同 iconify 先例）—— 成本中，收益：统一出站 + 可缓存
方案 B：保持直连 huggingface.co —— 成本 0，风险：无唯一出站口、公网直连
```

### 选项 3 · TD-23-2/3/4 + mockup 根级位置（组织约定）

文档三目录边界 / `localTool` 语义目录 / 统一生成物目录 / `mockup` 是否归位 —— 均为**约定**，牵动工具输出路径，宜一次性决策。

---

## 六 · 度量（改前/改后）

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| 待办债 | 36 | **24** |
| crossOrigin 裁决实现份数 | 1 单点 + 6 处绕过 | **1**（闸守护） |
| 固定存储键定义份数 | 1 表 + 6 模块本地 + 4 处裸字面量 | **1**（contracts） |
| 尺寸三写不变量实现份数 | 3 手抄 | **1**（`withNodeSize`） |
| 内联改名/建夹输入条实现份数 | 2 逐字复制 | **1**（`InlineNameInput`） |
| 裸 `<img>` 绕过统一出口 | 2（base/ui + director3d） | **0** |
| 手写 `catch-ok: RELEASE_FAIL` | 3（+ 2 原语） | **2**（仅原语） |
| 架构闸 | 10 条 | **11 条**（+跨源裁决单点） |

---

## 七 · 覆盖账 / 未动项

- **本轮改动 22 个源文件 + 4 个测试文件 + 3 个脚本/文档**；`tsc` 0 错 · `check:arch`/`check:catch`/`check:keys`/`check:dead-code`/`check:strict-src` 全绿 · **全量 226 文件 2815 例全绿**。
- **C 组（业务性）与 D 组（TD-22-20）未动**，理由与下一步见 §四/§五。
- **工具债**：新增的 `check:arch` 规则 11 已按 A10 硬要求闭环（`node --check` 通过 + 闸探针先红后绿 + 账本一行）。
