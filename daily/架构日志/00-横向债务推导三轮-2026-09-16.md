# 00 · 横向债务推导三轮（历史母体横向推导 · 未登记盲区补登 · 2026-09-16）

> **触发**：用户「从历史已有债务横向推导，未登记 没有被发现的债务」。
> **上游**（本轮不重复其结论）：[00-横向债务推导](./00-横向债务推导-2026-09-16.md)（5 候选全证伪）· [00-横向债务推导二轮](./00-横向债务推导二轮-2026-09-16.md)（TD-19-3）· [18-core横切基础设施](./18-core横切基础设施-2026-09-13.md)（TD-18-1/2，自称「14 文件 / 0 未审」）。
> **方法**：以账本最高频且**从未系统横扫**的母体「注释/文档漂移（43 条，形态 #1）」为种子做全仓取证；并复查 18 区「0 未审」声称是否仍成立（区域是否在首轮后长大）。
> **结果**：补登新债 2 笔（TD-18-3 `App.jsx` 改名漂移 ×39 · TD-08-15 Temp 文档失效引用）；18 区 5 个首轮后新增地基文件全部深审＝非债留痕；主画布 keydown 绕过唯一入口复查＝非债留痕。

---

## 一 · 链路快照（取证路径，自底向上）

```
种子母体：注释/文档漂移（账本 43 条，形态 #1；13/16/22 等横扫均未覆盖此形态）
  └→ 全仓「注释引用的符号/文件名/路径」vs 真实存在性 → 取证「引用已消失」
盲区复查：18 区 09-13 首轮称「14 文件 / 0 未审」
  └→ ls base/core 现 19 文件 → 5 文件首轮后新增、从未审 → 逐文件五问 + refs 实证
```

## 二 · 溯源（逐点实证，证据 = 文件:行 / grep / refs）

### 2.1 18 区长大盲区（首轮「0 未审」已失效）

`list_dir base/core` → 19 文件；18 首轮覆盖表列 14 文件。差集 5 文件均在其后新增（内容引用 TD-02-26／TD-15-1／docs/133／2026-09-14·15）：

| 文件 | 角色 | fan-in / 消费方（实证） | 判定 |
| --- | --- | --- | --- |
| `catchOk.ts` | 静默 catch 豁免码 SSOT | 闸 `scripts/check-silent-catch.mjs:47` 读本文件、:58 解析 `CATCH_OK`、:74 建白名单；fail-loud（:55-69 抽不到即 exit 1） | 非债（机器闸真消费，非假护栏） |
| `editorSession.ts` | 剪辑器开合会话态 | `App.tsx:76/351/1579/1591` + `modalLayer.ts:3/163/179/192` 消费 | 非债（单写入口 setEditorSessionOpen） |
| `agentKeys.ts` | agentKey 存储键构造 SSOT（TD-15-1） | `App.tsx:53/515/1551` + `conversationState.ts:34-36` + `AgentPanel/useAgentChat/agentCore` 消费 `AGENT_KEY_PREFIX` | 非债（且 `canvas-assistant` 全仓仅此 1 定义处，无第二份） |
| `canvasHotkeys.ts` | 画布 keydown 唯一入口 | `VideoProcessNode.tsx:31/731` 消费 `useCanvasKeydown`；:48 `isCanvasSuppressed()` 强制判据 | 非债 |
| `videoEditorKeys.ts` | 剪辑工程键构造 SSOT（docs/133） | `storage/service.ts:28-30` 11 处调用 | 非债（且 `video_editor_` 全仓仅 `contracts.ts:204-206` 定义，无第二份） |

> 五问⑤（注释与实现一致）：5 文件注释均自陈「禁再拼字面量／闸消费本表」，实证全部成立 → 注释＝实现，非债。

### 2.2 注释/文档漂移横向扫（种子母体）

| 注释引用 | 实证 | 判定 |
| --- | --- | --- |
| `App.jsx`（39 处注释，如 `App.tsx:177`/`director3d/storage.ts:11`/`GhostTargetNode.tsx:12`/`Director3DOverlay.tsx:107,111`） | `search_file App.jsx` 于 src ＝ **0 文件**（实际 `App.tsx` 存在） | **TD-18-3 新债**（改名漂移，文件已消失） |
| `Temp/deepening-localtool-baseurl-seam-20260904.md`（`localTool/src/routes/resources.ts:74`） | `search_file deepening-localtool-baseurl-seam*` ＝ **0 文件**（Temp 文档已删） | **TD-08-15 新债**（失效引用） |
| `backendLogStream.ts`/`assetType.ts`/`modalLayer.ts`/`font-picker.tsx`/`NodeShell.tsx`/`hf.ts`/`localToolBaseUrl.ts` | 均存在 | 非债 |
| `system/index.mjs`（useConnectedInputs.ts:381） | 外部库文件，非本仓 | 非债（域外引用，不归本仓） |
| `docs/106`（App.tsx:748） | `106-画布渲染重渲放大器根治…md` 存在 | 非债（简称） |
| `storage/service.ts`（editor-header.tsx:57） | `videoEditor/engine/services/storage/service.ts` 存在 | 非债 |
| `PRESET_PROTOCOL.md:74` lovart-image → `presets.ts L104-130` | grep `lovart-image` 仅现于 PRESET_PROTOCOL.md + lovart_config.ts:178；`presets.ts` 已无该 preset（文档自承已删） | 非债（doc 内过期表，建议顺手清，不入债） |

### 2.3 绕过唯一入口复查（主画布 keydown）

`canvasHotkeys.ts` 为画布 keydown 唯一入口（:48 `isCanvasSuppressed()` 强制判据）。`isCanvasSuppressed` 仅被 useCanvasShortcuts/modalLayer/editorSession/canvasHotkeys/AssistantTablePanel/App 调用。候选 `PanoramaNode.tsx:500` 自挂 `window` keydown → 读码 :458-459 **`if (!fullscreen) return;` 已守卫**，且仅在全屏层自身活跃时监听 → 非 TD-22-19 类绕过。其余 `window` keydown 均位于独立全屏层（director3d/videoEditor）或 dialog/editor 作用域内。→ **非债留痕**（TD-22-19 已收口，未回潮）。

## 三 · 定海

- 注释引用真实存在性：唯一消失引用＝`App.jsx`（改名）＋ `Temp/deepening-localtool-baseurl-seam-20260904.md`（删）。
- SSOT 第二份纪律在 5 新文件中全部成立（无 `canvas-assistant`／`video_editor_` 第二份硬编码）。
- 机器闸 `check-silent-catch` 真消费 `catchOk.ts`（fail-loud），非假护栏。

## 四 · 切割（本轮不审）

- 18 区原 14 文件（首轮已深审 TD-18-1/2，状态沿用）。
- director3d/videoEditor 自有全屏层 keydown（非主画布，不归本推导）。
- PRESET_PROTOCOL.md 过期表（doc 内清理，不入债）。

## 五 · 探债（明细真源）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-18-3 | src 内 39 处注释仍写 `App.jsx`，但文件 09 某时点已改名 `App.tsx`（`search_file App.jsx`＝0 文件），注释指向不存在文件；误导后续检索／改造 | 增债 | 结构债 | 39 注释点（hooks/nodes/panels/base/director3d） | 低 | 孤例（一次改名事件，39 处未同步；注释漂移 #1 形态实例） | 全仓 grep `App\.jsx` 注释引用统一改为 `App.tsx`（纯注释修正，零行为变化）；可选挂 eslint 注释规则防回潮。触发＝任一处改 App 注释时 | 待还 |
| TD-08-15 | `localTool/src/routes/resources.ts:74` 注释引用 `Temp/deepening-localtool-baseurl-seam-20260904.md`，该 Temp 文档已删（`search_file`＝0），注释指向失效路径 | 增债 | 结构债 | 1 行 | 低 | 孤例（seam 文档清理后注释未跟进） | 删 resources.ts:74 中 ` ／ Temp/...md` 失效片段（或改指正式文档），零行为变化 | 待还 |

**非债留痕**（禁再当 bug 补）：§二 2.1 表 5 文件 ＋ 2.2 表 6 处 ＋ 2.3 主画布 keydown 复查。

## 六 · 结论 + 覆盖账

1. 横向推导补登新债 2 笔（注释漂移 #1 形态）：TD-18-3（App.jsx 改名 ×39）＋ TD-08-15（Temp 文档失效引用）。其余候选全部证伪／非债。
2. **盲区价值**：18 区首轮「0 未审」已失效——区域 09-13 后长大 14→19 文件，5 个地基文件从未审；本轮全部深审＝健康（SSOT/闸均真生效）。结论：低覆盖区「长大」会使旧「审完」声称失真，须以目录实测为准。
3. **覆盖账**：本轮深审 18 区 5 文件（全非债）＋ 注释漂移横扫 src/localTool（命中 2 新债）＋ 主画布 keydown 复查（非债）。未深审：18 区原 14 文件（首轮已审，沿用）／director3d·videoEditor 自有层 keydown（域外，不归本推导）。
4. DATAFLOW：无链路漂移，不改。

---

## 覆盖度表（本轮增量，不重抄旧行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `src/components/base/core/catchOk.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 三轮 | 闸 check-silent-catch.mjs:47/58/74 真消费＋fail-loud | 非债 |
| `src/components/base/core/editorSession.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 三轮 | App.tsx:76/351/1579/1591＋modalLayer:3/179 消费 | 非债 |
| `src/components/base/core/agentKeys.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 三轮 | canvas-assistant 全仓仅 1 定义；App/conversationState 等消费 | 非债 |
| `src/components/base/core/canvasHotkeys.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 三轮 | VideoProcessNode:731 消费；:48 isCanvasSuppressed 强制判据 | 非债 |
| `src/components/base/core/videoEditorKeys.ts` | L0 地基 | 已裁定非债 | 🟢 | 09-16 三轮 | storage/service.ts:28-30 11 调用；video_editor_ 无第二份 | 非债 |
| `src` 内 39 处 `App.jsx` 注释 | 多区 | 有债待还(TD-18-3) | 🔴 | 09-16 三轮 | search_file App.jsx＝0；39 处写 App.jsx | TD-18-3 |
| `localTool/src/routes/resources.ts:74` | 后端 | 有债待还(TD-08-15) | 🔴 | 09-16 三轮 | search_file deepening-localtool-baseurl-seam*＝0 | TD-08-15 |
