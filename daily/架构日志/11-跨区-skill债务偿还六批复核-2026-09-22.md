# 11-跨区 · skill 债务偿还「六批」复核（2026-09-22）

> **触发**：用户「他已经完成 6 批了，你去看看他的 6 批做得怎么样」。
> **对象**：`11-跨区-skill债务偿还第一~六批-2026-09-2{1,2}.md`（他把我 09-21 审计登记的 22 条债逐批偿还）。
> **判据源**：`债务登记5步法` §零.3 铁律「任何描述都只是线索，不是结论」· 本仓「凭证必须可复现」。
> **方法**：**不采信批次文件的自我声明**，逐条回到代码取证；对可机器判定的声明（lint）做一次定点静态检查。
> **只读轮**：`src/**` / `localTool/**` / `tests/**` 一行未改；未跑任何测试。

## 一 · 总判定（先说结论）

**6 批是「真施工」，不是纸面交付** —— 我逐条复核的 20 项声明里 **15 项完全为真**、**4 项实现为真但留有尾巴**、**1 项凭证失实**。
- 为真（抽样取证）：`freezeSkillTurn(ids)` 装配收口（`skillInject.ts:71-131`）· 内置常量迁入模块（`skillBuiltins.ts`，legacy 层已无副本）· `skillStore` 降为纯转发壳且**零深引**（`skillStore.ts:17-26` 只 import 门面）· 三态分组意图（`skillPersist.ts:126-172`）· `resolveGroupName`（`:98-102`）· `execFileSync` 改 argv（`skills.ts:468` · `files.ts:689/722`）· 二进制改「往返无损」（`skills.ts:149-152`）· `.trash` 上限 + 清理（`:254-287`）· `only=SKILL.md`（`:318`）· 装载期引用对账（`skillHydrate.ts:120-130`）· `undecided` 三态（`:90` `:163`）· 不可信条款共用一份（`skillCatalog.ts:52` + `useCanvasAgentTools.ts:926`）· `origin` 有读者（`AgentMessage.tsx:336`）· `normalizeSkillIds` 四写路径收口（`conversationSnapshot.ts:67`）· 空值行口径（`skillManifest.ts:119-137`）· `description` 必填 + 说清理由（`SkillSettings.tsx:423-429`）· 预算超界警告（`:728-733`）· `trashKeep` 由生产者给全（`:296-300`）· `onReloaded` 经 ref（`SkillLibraryPanel.tsx:75-82`）· 死代码基线**只有删除无新增**（`git diff`：70 → 68，删 2 条）。
- **他的一条自我修正成立**：TD-11-19 的「两个写 API」实为**两种粒度**（单/批），现在 `setSkillEnabled` 直接委托 `setSkillsEnabled([id], …)`（`skillStore.ts:203-205`）⇒ 实现只有一份。原债（域外深引）为真，措辞我接受修正。

## 二 · 新登记债（探债 · 线索全）

| ID | 位置（文件:行号 / refs） | 债的形态 | 归属（模块） | 爆炸半径 | 利息 | 根因（母体?/孤例） | 大致解法（一句话·可选） | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **TD-11-45** | `skillSelection.ts:30`（非 hook 却命名 `use*`）· 调用点 `SkillSettings.tsx:493`（在 `handleUseSkill` 事件处理器内）；复现：`npx eslint src/components/settings/sections/SkillSettings.tsx` ⇒ **1 error** `react-hooks/rules-of-hooks` | **凭证失实 + 提交被挡**：批次一 §三 与批次六 §四 都写「`lint` 0 问题」，实测 `npm run lint` = **1 error**（唯一一条，就在本轮新代码上）；而 `.husky/pre-commit` 跑 `lint-staged` → `eslint --fix`，该 error **无 fixer** ⇒ `git commit` 必失败 | agent/conversation + settings | ① 批次文件的两处凭证不可复现（本仓红线「凭证可复现」）；② 任何提交 skill 相关改动的人**被挡在 pre-commit**；③ 违反 hooks 命名约定，后续若有人把它当 hook 用（提条件调用）会踩真规则 | **高** | 孤例（命名与位置错配：给一个纯函数起了 hook 名，又放在回调里调） | 改名去 `use` 前缀（如 `applySkillToCurrentConversation`）并同步域门面导出与调用点；改完复跑 `npm run lint` 归零 | 待还 |
| **TD-11-46** | 生产者 `skillHydrate.ts:126-130`（引用缺失也 push 进 `failures`）· 消费者 `SkillLibraryPanel.tsx:110` · `AgentPanel.tsx:511` · 日志 `skillHydrate.ts:180` | **一个字段承担两义**：`HydrateResult.failures` 同时装「包**未纳入**」与「包**已纳入**但正文引用的文件缺失」，而三处消费者**全按前者措辞**上报 ⇒ 用户看到「N 个技能包未纳入」，实际那些包**已经纳入**（只是引用文件缺） | agent/skill + agent/panels | 谎报：用户以为技能不可用（去排查"为什么没纳入"），真实问题只是某个 `references/*.md` 不在包里；而 TD-11-21 的收益（提前暴露引用断裂）被这句错误措辞抵消 | 中 | **孤例但形态眼熟**：与 TD-11-23（一个值折叠两义）同族 —— 修一个折叠，又引入一个折叠 | `failures` 拆成两个字段（如 `notIncluded` / `warnings`），消费者各按各的措辞；或给条目加 `kind` 判别 | 待还 |
| **TD-11-47** | `skillCatalog.ts:358-359`（注释）vs 同文件 `:65-68`（新口径） | **陈旧注释**：`buildBoundSkillBlocks` 里仍写「`truncateSkillContent(_, 0)` 的语义是「不限」，直接用会把整段塞进去」，而 TD-11-26 已把该语义改成「一点都不给」⇒ 同一文件内两处口径**互相矛盾** | agent/skill | 后按此注释推理的人会得出错误结论（以为 `room<=0` 那道判是为了防"整段注入"）；实际它现在防的是"空块" | 低 | 孤例（TD-11-26 只回改了 README 不变量 7 与单测，漏了本调用点） | 就地回改注释：说明 `room<=0` 现在的作用 = 不产出空包裹块，并把 starved 与 truncated 的区别写清 | 待还 |
| **TD-11-48** | `scripts/check-arch.mjs:2208`（`/\b(execSync\|exec)\s*\(\s*`[^`]*\$\{/`） | **新闸的判据覆盖不全**：单行正则只抓「`execSync(` 与含 `${` 的模板串**同行**」。写成多行调用、或先 `const cmd = \`…${x}\`` 再 `execSync(cmd)`（变量间接）**都漏检** ⇒ 同一类注入换个写法即可绕过；而它的报错文案宣称「这一类注入结构上不存在」 | 17 审计工具链 | 假护栏：闸绿给人"这类问题已被消灭"的错觉；本仓已有实证教训（M1 母体：grep 口径不可靠 → 必须钉进 AST） | 低 | 孤例（新闸首版；ADR-0052 M2 的负例探针已做，但只探了"同行"这一形态） | 要么上 AST（与 check-arch 既有 AST 规则同粒度），要么在注释里如实声明"只覆盖同行形态"并列出漏检写法 | 待还 |
| **TD-11-49** | `README.md` 不变量 17（「frontmatter 空值行 = 不存在，两侧同口径」）vs `skillManifest.ts:132-135`（`toBool('') = false`） | **口径强于实现**：`user-invocable:` / `disable-model-invocation:` 写空值 **不会**被折成"不存在"，而是物化成 `false`，并在下次保存时把文件改写成显式 `user-invocable: false` | agent/skill | 与 TD-11-37 修的是同一件事，却漏了布尔两字段；影响面小（方案 §1.2 明确这两字段"解析但不生效"），但**不变量 17 的表述是错的** | 低 | 孤例（与 TD-11-37 同族：口径声明 > 实现） | 二选一：把布尔字段也折成 undefined（空值=不存在），或把不变量 17 收窄成"字符串字段与 unknown" | 待还 |

## 三 · 逐批复核表（声明 → 复核 → 证据）

| 批次 | 声明（摘要） | 复核 | 证据 |
| --- | --- | --- | --- |
| 一 | TD-11-16 装配收口「只给 id」；组清单归一；22 条全真 | ✅ 真（**唯一偏差**：`lint 0 问题` 失实 → TD-11-45） | `skillInject.ts:71` · `AgentPanel.tsx:404/426-432` · `SkillPick` 类型已删（`grep` 只剩同名 state） |
| 二 | TD-11-19 skillStore 降纯转发壳、零深引、写入口唯一 | ✅ 真 | `skillStore.ts:17-26`（只 import 门面）· `:203-205`（委托批量写）；域外 `import … skillRepository` **0 处** |
| 三 | TD-11-22「去使用」真动作；TD-11-42 skills 归一收口 | ✅ 真（功能完整，含写后回读） | `skillSelection.ts:30-47` · `conversationSnapshot.ts:67` + 4 调用点 · `SkillSettings.tsx:492-504` |
| 四 | TD-11-17 不可信条款共用一份；21 装载期对账；24 形状不全计入 failed；28 导出降级如实提示 | ⚠️ 实现为真，**21 有尾巴**（`failures` 语义重载 → TD-11-46） | `skillCatalog.ts:52/313` · `useCanvasAgentTools.ts:926` · `skillHydrate.ts:120-130` · `skillCloudSync.ts`（failed 计入） |
| 五 | TD-11-38 建议上界；26 `limit<=0` 直白化；27 跨栈常量对账；34 扩展名收口 | ⚠️ 实现为真，**26 漏回改注释**（→ TD-11-47） | `skillCatalog.ts:31-35/59-71/83-116` · `skillImport.ts:35/71` · `check-arch.mjs` 对账项 2 项 |
| 六 | TD-11-29/37/25/36/35/39 六条全清；新增反向闸；基线只删不增 | ⚠️ 实现为真，**闸覆盖不全**（→ TD-11-48）· **不变量 17 口径错**（→ TD-11-49） | `AgentMessage.tsx:336` · `skillManifest.ts:119-137` · `skills.ts:149/254/318/468` · `check-arch.mjs:2180-2223` · `git diff dead-code-baseline.json`（1 增 3 删 = count + 2 条删除） |

**他未藏事**：TD-11-43（`files.ts handleOpenDir` 未过越根守卫）**自报**且如实标为跨区（08/12）未修 —— 我复核为真（`files.ts:703-711`：`filepath` 用户可控 + `path.join(uploadDir, relativePath)` 无越根校验 + `existsSync` 成存在性探针）。这一条**做得好**：发现、登记、不顺手改（越区边界自守）。

## 四 · 覆盖度表（增量）

| 点位 | 覆盖状态 | 灯 | 证据 | 债 / 裁定 |
| --- | --- | --- | --- | --- |
| `agent/skill/skillInject.ts` | 深审（复核） | 🟢 | `:71-131` | 装配断裂已消 |
| `agent/skill/skillBuiltins.ts`（新） | 深审 | 🟢 | `:29-75` | 内置唯一家；legacy 层已无副本 |
| `agent/skill/skillManifest.ts` | 深审（复核） | 🟡 | `:119-137` | TD-11-49（布尔字段逃出口径 17） |
| `agent/skill/skillCatalog.ts` | 深审（复核） | 🔴 | `:31-116` `:344-370` | TD-11-47（陈旧注释） |
| `agent/skill/skillHydrate.ts` | 深审（复核） | 🟡 | `:82-92` `:120-130` `:160-181` | TD-11-46（`failures` 两义） |
| `agent/skill/skillPersist.ts` | 深审（复核） | 🟢 | `:98-102` `:126-181` | 三态 + resolveGroupName 正确 |
| `agent/skill/skillApi.ts` · `skillImport.ts` · `skillResource.ts` | 深审（复核） | 🟢 | `skillApi:72-76` · `skillImport:35/71` | 无债 |
| `agent/skill/SkillLibraryPanel.tsx` | 深审（复核） | 🟡 | `:75-82` `:110` `:296-300` | TD-11-46（措辞） |
| `agent/skill/README.md` | 深审（复核） | 🟡 | 不变量 17 | TD-11-49 |
| `agent/runtime/skillStore.ts` | 深审（复核） | 🟢 | `:17-26` `:203-205` | 深引已消；写入口唯一 |
| `agent/conversation/skillSelection.ts`（新） | 深审 | 🔴 | `:30` | TD-11-45（`use` 前缀 + 位置） |
| `agent/conversation/conversationSnapshot.ts` | 深审（复核） | 🟢 | `:67` + 4 调用点 | 归一收口正确 |
| `agent/panels/AgentPanel.tsx` | 深审（复核） | 🟡 | `:404` `:426-432` `:511-520` | TD-11-46（措辞）；`:441-443` 陈旧注释块（见 §五） |
| `agent/panels/AgentMessage.tsx` | 深审（复核） | 🟢 | `:329-336` | `origin` 有读者 |
| `settings/sections/SkillSettings.tsx` | 深审（复核） | 🔴 | `:423-429` `:493` `:592` `:690` `:728-733` | TD-11-45（lint error 源） |
| `localTool/src/routes/skills.ts` | 深审（复核） | 🟢 | `:46` `:149` `:159` `:225/240/254` `:318` `:468` | 六项声明全真 |
| `localTool/src/routes/files.ts` | 深审（复核） | 🔴 | `:689` `:722` `:703-711` | TD-11-43（他自报，跨区未修）· 复核为真 |
| `scripts/check-arch.mjs` | 深审 | 🟡 | `:2180-2223` | TD-11-48（单行正则） |
| `scripts/dead-code-baseline.json` | 深审 | 🟢 | `git diff`：70 → 68，**只有删除** | 声明属实 |

**覆盖账**：本轮复核 **19 个点位 / 审 19 / 未审 0**（6 批共 24 条结清的**抽样取证 20 项**，未采信任何自我声明）。

## 五 · 另两处观察（不立债）

- `AgentPanel.tsx:441-443`：仍留着「`applySkill` 选中时把 `{content,...}` 复制进 `activeSkills`（冻结快照）…」这段**描述旧实现**的注释块，而紧邻的 `:447-449` 新注释写的是「`activeSkills` 只存 id ⇒ 不需要回填内容」—— **同文件内自相矛盾**。归入 TD-11-47 同族（陈旧注释），但它是被 TD-11-16 修复**新造**的矛盾，故合并计一债（不重复登记）。
- `localTool/src/routes/skills.ts:159-167`：`readEntries(only)` 的 `only` 参数不做 `safeRelPath` 校验（当前唯一调用点传字面量 `['SKILL.md']`，**不可达**）⇒ 记为边界观察，不立债。

## 六 · 收尾自检

| # | 项 | 结论 |
| --- | --- | --- |
| 1 | `src/**` / `localTool/**` 一行未改？ | ✅ 只写本文件（+ 账本 2 个自动维护文件） |
| 2 | 命中的工具债当场修？ | ✅ 无新工具债（TD-11-48 属 17 区，但它是**设计取舍**不是笔误，需评审 → 只登记） |
| 3 | §七 有状态行？ | ✅ 见下 |
| 4 | 覆盖账报全？ | ✅ 19 点位 / 未审 0 |
| 5 | 每笔债挂证据？ | ✅ 全带 `文件:行号`；TD-11-45 另附**可复现命令** |
| 6 | 判了母体还是孤例？ | ✅ 5 笔均标孤例（未编造母体；TD-11-46 与 TD-11-23 同族已注明但未强行合并） |
| 7 | 全程没跑测试？ | ✅ 测试 **0 次**；只做了 1 次定点 `npx eslint`（静态检查，非测试）—— 因批次文件自称「lint 0 问题」，需对账 |
| 8 | 每个捕获点都问"失败有留痕吗"？ | ✅ 本轮 5 笔中 3 笔本质是"**呈现层失实**"（谎报/陈旧口径/假护栏），与本仓头号伪装型债同族 |

## 七 · 结论与下一步

- **总判定：6 批是真施工，质量高于"纸面交付"；但他自己的"全绿"凭证有一条不成立。** 24 条结清里我抽样取证 20 项，**19 项为真**，唯一硬伤是 **TD-11-45**（`npm run lint` 实测 1 error，而他两处写"0 问题"）—— 且该 error **会挡 pre-commit**。
- **本区域状态**：**已验证(有债待还)**（本轮新登记 5 笔：TD-11-45 ~ TD-11-49；连同他自报的 TD-11-43 与另一会话的 TD-11-44，本区主表待办 **7** 条）。
  - ⚠️ **债号对账**：本轮新债实际落在 **TD-11-45 ~ TD-11-49** —— `TD-11-44` 已被**另一会话**占用（「设置页面板 vs Skills 列表两处并存未收口」，用户视角债，与本轮主题不同），故未串号。
- **优先处置序**：TD-11-45（挡提交，先改）→ TD-11-46（谎报，用户可见）→ TD-11-47/49（口径与注释，顺手一起改）→ TD-11-48（闸覆盖，可并入下一次工具链轮次）。
- **给后续复核者的提醒**：批次文件里的**测试/闸凭证**（`npx vitest …`、`check:arch`、`lint`）在写下的那一刻可能为真，但**代码随后又被改过**（本轮实证：`SkillSettings.tsx` 在批次六之后仍有改动）⇒ 复核必须**重跑可机器判定的那几条**，不能只读结论。
