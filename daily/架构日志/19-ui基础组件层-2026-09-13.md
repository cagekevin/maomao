# 架构日志 · UI 基础组件层（base/ui 叶组件库） · 2026-09-13

> **文件名**：`19-ui基础组件层-2026-09-13.md`。本区为**其他领域**（区域 01–18 从未把 `base/ui/` 的 20 个通用叶组件作为主域审计；区域 16 收口笔记已将其列为下一批候选 ⑤「base/ui 基础组件库（20 tsx 叶组件，债密度低，本轮未审）」）。自主追加为第 19 区（编号顺延 `max(NN)+1`，依据 `index.md` 区域治理规则第 1 条；注：区域 17/18 已被审计工具链治理 / core 横切基础设施占用，故本域顺延为 19）。
> **为何漏列**：`base/ui/` 是通用 UI 原语库（下拉/右键菜单/改名框/错误边界/缩略图/开关/Toast/确认/节点外壳等），各业务组件散点引用，从未整体审视，易藏「同款交互抄成两份」的 SSOT 第二份债。
> 审计视角：数据流（UI 原语无 fan-in 数据流，按「组件契约 → 是否被其它组件重复实现」取证）。实证统一用 `node scripts/mv-sync-refs.mjs refs <file>` + 读真实代码（**全程只读不改码**）。

## 一、链路快照（refs 实证，非凭文件名猜）

> `node scripts/mv-sync-refs.mjs refs Select.tsx` / `ModelSelect.tsx`（2026-09-13）：
> - `Select.tsx` fan-in **1 处**：`scriptbox/GearSettings.tsx`（唯一消费方）。
> - `ModelSelect.tsx` fan-in 多（节点模型下拉等，注释明写为「模型选择下拉」唯一组件）。
> - 二者**视觉/交互同款**：触发按钮 / 弹层 / 选项行 / `useOutsideClick` 行为逐字相近（见 §五 TD-19-1）。

## 二、溯源（Phase 1：症状 vs 病根）

- 本区为通用 UI 原语库，多数组件健康（Toggle 已从 OtherSettings/SkillSettings 抽公共、attachmentCover 已合并视频/音频封面、ErrorBoundary/ConfirmContainer/ToastContainer 严格走 store 范式、NodeShell 为高质量节点骨架）。
- 真缝隙：`Select.tsx` 是 `ModelSelect.tsx` 的**复制派生**（文件头注释自承「与 ModelSelect 同款交互/配色」），改一处样式须同步两处 → SSOT 第二份。

## 三、定海（Phase 2：不变式 / 唯一真相源）

- **下拉交互单源缺失**：`Select` 与 `ModelSelect` 各自实现一份「触发按钮 + 弹层 + 选项行 + 外部点击关闭」，无公共基座。**有债**（TD-19-1）。
- 其余组件均满足单源/范式契约（见 §五 健康项）。

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- 唯一真缝隙：抽 `Dropdown` 公共基座（trigger + popup + outside-click + option-row 渲染插槽），`Select`/`ModelSelect` 各传 `renderItem`；或把 `Select` 收编为 `ModelSelect` 的泛型形态。收敛口 = TD-19-1。

## 五、探债（Phase 4：技术债账本）

> 本轮**只读**，1 笔债只登记；偿还计划写"将来怎么改"，不执行。

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-19-1 | `base/ui/Select.tsx` 与 `base/ui/ModelSelect.tsx` 是**同款交互/配色的第二份下拉实现**：`Select.tsx:5-9` 文件头注释自承「与 ModelSelect 同款交互/配色，供工作流等少数固定选项选择用」；二者触发按钮（`h-6 px-2 ... bg-transparent hover:bg-surface-hover border border-transparent hover:border-edge rounded text-caption-sm`）、弹层（`bg-surface-1 border border-edge rounded-lg shadow-xl p-2 z-50 ... max-h-60 overflow-y-auto`）、选项行（`flex items-center gap-1.5 mb-1 last:mb-0 text-left px-2 py-1.5 text-caption-sm rounded-md`）、`useOutsideClick` 行为逐字相近，仅数据形态（SelectOption vs ModelItem+badge/cost）不同 → 改样式/交互须两处同步，违反 SSOT；`Select` 仅 1 消费方（GearSettings），`ModelSelect` 多消费方，复制产生即埋雷 | 增债 | 结构债 | 下拉组件全消费方（改样式须同步 2 文件） | 中 | 孤例（同款交互抄两份，五问①「同一真相被抄成 ≥2 份」命中；非母体） | 抽 `Dropdown` 公共基座（trigger + popup 壳 + outside-click + option-row 渲染插槽），`Select`/`ModelSelect` 改为各传 `renderItem`/`options`；或把 `Select` 收编为 `ModelSelect` 的泛型形态（`options` 支持 badge/cost 可选）。属收敛重构，零行为变化 | 待还 |

> 健康项（已裁定非债，留痕防重审）：
> - `ContextMenu.tsx`（右键菜单基座，防溢出自适应定位 + 子菜单 group-hover 桥，结构清晰）🟢非债
> - `RenameDialog.tsx`（节点改名弹窗，交互契约对齐 ConfirmContainer，无残留态）🟢非债
> - `ErrorBoundary.tsx`（full/node 双粒度，走 logger 上报，非裸 console）🟢非债
> - `LazyImage.tsx`（IntersectionObserver + 破图兜底 + resolve 统一出口）🟢非债
> - `Toggle.tsx`（已从 OtherSettings/SkillSettings 抽公共，消除逐字重复）🟢非债
> - `attachmentCover.tsx`（视频首帧/音频图标逻辑合并，消两处分叉）🟢非债
> - `NodeShell.tsx`（节点外壳高质量基座，尺寸/端口/标题/错误边界统一，含详尽 NEW-NODE 范本）🟢非债
> - `ConfirmContainer.tsx` / `ToastContainer.tsx`（严格走 confirmStore/toastStore 订阅范式，z 层级决策清晰）🟢非债

## 六、覆盖度表（查过什么 / 还剩什么 —— 防重复审、防假装审完）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| src/components/base/ui/Select.tsx | L0 UI 原语 | 有债待还 | 🔴 | 首轮 09-13 | :5-9 自承「与 ModelSelect 同款」+ refs 1 fan-in(GearSettings) | TD-19-1 |
| src/components/base/ui/ModelSelect.tsx | L0 UI 原语 | 有债待还 | 🔴 | 首轮 09-13 | 与 Select 触发/弹层/选项行逐字相近 | TD-19-1 |
| src/components/base/ui/ContextMenu.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | 防溢出自适应 + 子菜单桥，结构清晰 | 非债 |
| src/components/base/ui/RenameDialog.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | 交互契约对齐 ConfirmContainer | 非债 |
| src/components/base/ui/ErrorBoundary.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | full/node 双粒度，走 logger | 非债 |
| src/components/base/ui/LazyImage.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | IO + 破图兜底 + resolve 统一 | 非债 |
| src/components/base/ui/Toggle.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | 已从两处抽公共 | 非债 |
| src/components/base/ui/attachmentCover.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | 视频/音频封面合并 | 非债 |
| src/components/base/ui/NodeShell.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | 节点外壳高质量基座 | 非债 |
| src/components/base/ui/ConfirmContainer.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | 走 confirmStore 范式 | 非债 |
| src/components/base/ui/ToastContainer.tsx | L0 UI 原语 | 已裁定非债 | 🟢 | 首轮 09-13 | 走 toastStore 范式 | 非债 |
| src/components/base/ui/NodeTitle.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/ToolbarButton.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/GenerateButton.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/GeneratingOverlay.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/ExpandablePanel.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/VideoThumbnail.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/ResizeFullscreenHandle.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/CometParticles.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |
| src/components/base/ui/JianyingIcon.tsx | L0 UI 原语 | 抽审 | ⚪ | 首轮 09-13 | 本轮未深读，下轮 | 抽审 |

**覆盖账**：本区共 20 个文件 / 本轮审 11 个（深审） / 抽审 9 个（下轮深审，逐个列名：NodeTitle · ToolbarButton · GenerateButton · GeneratingOverlay · ExpandablePanel · VideoThumbnail · ResizeFullscreenHandle · CometParticles · JianyingIcon）。本轮聚焦最可疑的「两份下拉重复」，9 个抽审不重判为非债，下轮须深审。

---

## 七、结论与下一步

- 本区域状态：**已验证(有债待还)**（1 债：TD-19-1 `Select`/`ModelSelect` 下拉实现重复）。
- 待还债项已流入 `daily/架构日志/债务.md`：是（TD-19-1）。
- DATAFLOW.md：本域此前无进度节点，已**就地新增「UI 基础组件层」段**（含灯，只放灯未塞明细）。
- 其他候选未登记领域（仍待续）：edges 渲染（CustomEdge 已随 TD-04-17 审，余 3 未审）、全局导航/全屏壳编排（区域 16 旁证健康）。
