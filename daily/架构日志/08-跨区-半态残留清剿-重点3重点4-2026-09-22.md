# 08-跨区 · 143 复核「重点 3 + 重点 4」清剿（5 笔） · 2026-09-22

> **触发**：用户「还有其他工作没做完吗？」⇒ 按 `08-跨区-143施工复核-2026-09-22.md` §六.4 的顺序，
> 清完**重点 3**（TD-08-61 / TD-08-58 / TD-08-64）与**重点 4**（TD-08-62 / TD-08-63）。
> **变更面**：`localTool/src/{relay-poll.ts · budget.ts · generateEngine.ts · routes/generate.ts}` ＋ `docs/plan/143-…规划`。**零用户可感知行为变更**。

---

## 一、逐笔处置

| TD | 债态 | 本轮裁定 | 动作 |
| --- | --- | --- | --- |
| **TD-08-64** | 复核文件 §四 称"命中工具债无"，实则该提交含 `scripts/{adr,check-arch}.mjs` | **账本滞后**（该轮**已就地更正**，文件里明写"本行首版陈述有误，已由 §五 补漏轮更正"） | 仅 `resolve`，代码/文档**无需再改** |
| **TD-08-61** | `relay-poll.ts` 注释复述「每段各 10min」，该值已移出本文件 | **成立**（与 143 自立的 TD-08-49「禁复述已移出的数值」冲突） | 注释改为**指向预算真源** `budgetMsFor`，不复述数值 |
| **TD-08-58** | 143 规划 `:385` 仍列「待拍板仅 1 项：决议⑥」、`:11` 仍写「落地前置 D5」 | **成立**（TD-08-48 只修一半 ⇒ 回潮） | 两处**回改**：`:11` 前置随决议⑥ 撤销（与 §7 撤销 G 一致）；`:385` 改「**待拍板：无**」（与下方「无阻塞项」一致） |
| **TD-08-62** | `generateEngine.ts` 注释以「唯一调用点=chat」论证，实现却按 capability 泛化查表 ⇒ "必有一假" | **改判：不互斥** —— ① 注释**成立**（`relayGenerate` 唯一调用点确为 `routes/generate.ts` 的 chat 分支）；② 泛化**正确**（`budgetMsFor` 是**共享原语**，`relay-poll.ts` 的多 capability 路径同样消费，且入参类型不允许写死 `'chat'`）⇒ 真问题是**注释没把二者关系说清** | 注释补一段说明（"唯一调用点=chat"只解释**原值 600_000 为何错**，不构成"泛化无消费者"） |
| **TD-08-63** | `budget.ts:47-51` 与 `routes/generate.ts:44` 各写一份 override 合法性校验 ⇒ 生产路径上第二道**恒不触发**（假守卫） | **成立** | **收口为唯一实现**：`budget.ts` 新增并导出 `normalizeOverrideMs`，`budgetMsFor` 与路由层均改为**调用**它 |

---

## 二、TD-08-63 的收口（本轮唯一代码结构改动）

```ts
// budget.ts —— 判据唯一实现（原先 budget 与 routes 各写一份同口径三条件）
export function normalizeOverrideMs(v: unknown): number | undefined {
  return typeof v === 'number' && Number.isFinite(v) && v > 0 ? v : undefined;
}
export function budgetMsFor(capability: RelayCapability, override?: number): number {
  return normalizeOverrideMs(override) ?? DEFAULT_BUDGET_MS[capability];
}

// routes/generate.ts —— 消费者只调用，不再复制判据
const clientTimeoutMs = normalizeOverrideMs(body.timeoutMs);
```

**判据份数：2 → 1**（`grep -rn "Number.isFinite" localTool/src/` → 仅 `budget.ts` 一处）。

**行为等价凭证**：既有 `localTool/test/budget.test.js:28`
「非法 override（0 / NaN / 负数 / 非数）⇒ 回落默认，不得变成「不超时」」**原样通过** ⇒ 收口没改语义。

---

## 三、凭证

```bash
npx tsc -p localTool/tsconfig.json --noEmit   # → 0
npm run test:localtool                        # → # tests 406 · # pass 406 · # fail 0 · ALL PASS ✔
npm run check:arch                            # → ✅ 架构校验通过
grep -rn "Number.isFinite" localTool/src/     # → 仅 budget.ts:57（判据唯一实现；收口前为 2 处）
```

**零炸三问**：
1. **哪些行为被改了** —— **无**（TD-08-63 是纯重构，行为等价；其余 4 笔是注释 / 文档）。
2. **改前能走的仍能走吗** —— 全量 406 例仍绿（含 `budget.test.js` 的 override 回落用例）。
3. **改前会失败的仍失败吗** —— 非法 override 仍**回落默认**（未顺手放宽）；未新增任何路径。

---

## 四、探债

- **本轮新增债：无。**
- **同区剩余待还（7 笔）**：TD-08-46（段①耗时归因，需数据）· TD-08-52（POST 缺 `budgetMs` 未 fail-fast）· TD-08-53（override 通道仍结构性不可达）· TD-08-54（chat 总预算两份活值）· TD-08-55（`unknown.threadId` 幽灵字段）· TD-08-56（attach 404 判据未回写）· TD-08-57（not-found 被落成 failed）。
  → **143 复核 §六.4 的四个重点至此全部完成**（重点 1 TD-08-59 ✅ · 重点 2 TD-08-60 改判 ✅ · 重点 3 ✅ · 重点 4 ✅）。

---

## 五、交付闸自查

| 闸 | 本轮结论 | 凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | override 合法性判据 **2 份 → 1 份**（`normalizeOverrideMs` 唯一实现，两侧改调用） | §二 grep（`Number.isFinite` 命中 2→1） |
| 减一 · 兜底净减 | **假守卫消失**（第二道恒不触发的校验被删，不是搬家）；新增兜底 0 | §二 代码对照 |
| 减二 · 复杂度实减 | 判据份数 −1；路由层内联三条件 → 一次调用 | §二 |
| 一诚实 · 失败诚实可见 | 不适用（本轮未改失败契约） | — |
| 零炸 · 防回归防副作用 | 见 §三「零炸三问」 | §三 |
| 一凭证 · 证据可复现 | §三 命令原文 | — |

---

## 六、决策留痕

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度 | SSOT | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | TD-08-62 **改判不互斥**（不删泛化查表） | 泛化是类型诚实的写法，删了反而与 `RelayCapability` 冲突 | `budgetMsFor` 归真源层，调用方无权写死 | 不新增结构 | — | **改注释**（不改实现） |
| 2 | TD-08-63 收口到 `normalizeOverrideMs`（而非删掉路由层那道） | 两道都"对"但重复 ⇒ 留一道即够；保留路由层调用（边界仍校验） | 判据归真源层**导出**，消费者只调用 | 判据 2 → 1 | 唯一实现 | **做** |
| 3 | TD-08-64 只 `resolve` 不改文档 | 文档已在该轮改对，再改是 churn | — | — | 账本追平代码 | **做** |

**存在性凭证**：

```bash
grep -n "## 六、决策留痕" daily/架构日志/08-跨区-半态残留清剿-重点3重点4-2026-09-22.md
```
