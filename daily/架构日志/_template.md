# 架构日志 · <区域名> · <YYYY-MM-DD>

> **文件名**：`<NN>-<区域>-<YYYY-MM-DD>.md`（如 `01-生成链路-2026-09-11.md`）。同区多次审计各写新日期文件、不覆盖旧档。
> 审计视角：数据流（链路 = 产生 → 经过 → 消费 → 落盘）。
> 审计依据：架构师心法 Phase 1–4 + 静默自检清单；数据流实证用 `node scripts/mv-sync-refs.mjs refs <file>`。

## 一、链路快照（refs 实证，非凭文件名猜）

> 列本区域主文件 + 谁 import 它（fan-in）+ 字符串残留引用。DATAFLOW.md 是否仍准确？

```
<贴 refs 输出 / 链路图>
```

## 二、溯源（Phase 1：症状 vs 病根）

- 该区域当前结构能否承载业务？若撤掉某需求，相关代码还有价值吗？
- 是否存在「为兼容乱象而在末端写补丁」而非「源头收敛」？

## 三、定海（Phase 2：不变式 / 唯一真相源）

- 数据流里 SSOT 是否唯一？有无多处维护同一状态、消费端写 `if (a != b) b = a` 式副本同步？
- 边界处是否有运行时校验（Guard）洗净脏数据？还是把 `catch` 兜底埋到三公里外？

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- 是否有「第二份真相」在偷偷长（同语义两处实现）？应抽唯一实现收口。
- 是否存在上帝类 / 过度抽象 / 跨层直插大模块（TDZ 风险）？

## 五、探债（Phase 4：技术债账本）

| 决策点 / 问题点 | 归类 | 爆炸半径 | 利息率 | 根因 | 偿还计划 |
| - | - | - | - | - | - |

## 六、结论与下一步

- 本区域状态：`已验证(无债)` / `已验证(有债待还)` / 需转架构5步法翻新。
- 待还债项是否已流入 `spec/TECH-DEBT.md`：是 / 否（列条目）。
- DATAFLOW.md 是否需要刷新：是（已就地更新）/ 否。
