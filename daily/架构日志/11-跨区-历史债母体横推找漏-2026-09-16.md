# 11-跨区·历史债母体横推找漏复核 · 2026-09-16

> 触发：用户「债务登记 5 步法 · 从历史已有债务深入推导，未登记没有被发现的债务」。
> 起点：全账本历史债提取高频母体，逐个横推全库找漏（只读，不改一行代码）。
> 跨区只开一份：新债绑根因区 11（AI 助手 / assistantTable），其余区母体取证为有意非债 → 仅留痕防重审。

## 一 · 链路快照（自下而上 · 历史债母体清单）

```
历史债高频母体（debt.mjs stats 2026-09-16 现算，主表∪归档 301 条）：
  · 静默吞/失败伪装成功（31）→ check-silent-catch 固化闸
  · SSOT 第二份（30）/ 口径错位（25）→ check-storage-keys / check-events 固化闸
  · 死代码/幽灵预留（34）→ knip 固化闸
  · assetType 媒体类型真值源分裂（TD-16-4~11 待还）→ 唯一真源 assetType.ts
本轮对这 5 个母体做全库横推找漏（不重立已登记债，只找未被登记的兄弟实例）。
```

## 二 · 溯源（从历史债出发）

`debt.mjs list --status 待还` 命中 16 条；主体为：
- TD-16-4~11（8 条）= `assetType.ts` 真源之外被内联复制的扩展名/媒体类型判定；
- TD-08-16 = localTool /files/ URL 未剥 ?query 与静态服务/入库/前端 assetType 口径错位；
- TD-21-15/16 = 剪辑器 G4/G5 体验持平；TD-23-2~5 = 仓库卫生（用户暂缓）；TD-24-3 = 注释漂移幽灵债号。
本轮以「这些母体的 grep 信号」为探针，全库复扫找未被登记的兄弟实例。

## 三 · 定海（取证 · 证据闸）

| 母体 | 探针 | 结果 | 判定 |
| --- | --- | --- | --- |
| 静默吞 | `node scripts/check-silent-catch.mjs` | ✅ 0 违规（扫 579 .ts，11 白名单） | 非债（固化闸绿） |
| 存储键 SSOT | `node scripts/check-storage-keys.mjs` | ✅ 存储键契约校验通过（571 文件） | 非债（固化闸绿） |
| 事件总线 SSOT | `node scripts/check-events.mjs` | ✅ 事件契约校验通过（573 文件，登记表双向自洽） | 非债（固化闸绿） |
| 死代码 | `node scripts/check-dead-code.mjs`（knip） | 基线 0，当前 0，无新增死代码 | 非债（固化闸绿） |
| assetType 真源分裂 | grep 稀有扩展名 `mkv/ogv/avi/m4v/webm` + 内联 `/\.(mp4|webm|mov)/` + `split('.').pop()` | src 内仅 `creativeCatalog.ts:83`（=已登记 TD-16-4）；其余 mp4/webm 命中均为 videoEditor 导出格式选择（非分类绕过） | 仅 TD-16-4（已登记）；无新兄弟 |

## 四 · 切割（五问 · 实证结果）

| # | 发现 | 实证 | 判定 |
| --- | --- | --- | --- |
| A | `AssistantTablePanel.tsx:341` 恒传 `disabled={false}` 给 `RowOpsMenu`；`RowOpsMenu.tsx:76` 真消费 `disabled` 透传按钮触发器，全仓唯一调用点恒 false ⇒ 禁用能力永不被触发（恒绿假护栏/死参） | `refs`/读 `RowOpsMenu.tsx:22-83`：仅 1 调用点（AssistantTablePanel:340）且恒 `false`；同目录 `TableTabsBar.tsx:208`/`AssistantTablePanel:564/573` 均用真实条件（`isLast`/`!canUndo`），反证此处 `false` 为遗漏 | **新债**（假护栏/恒绿·孤例） |
| B | `timeline-toolbar.tsx:113` `disabled={true}` + 空 `onClick` + `tooltip={'即将推出'}` | 读 L110-115：明确 coming-soon 有意占位 | **非债**（有意设计，注释已固化） |
| C | `outcome.ts:19` 注释引已删 `MigrationResult`（`engine/services/storage/migrations/...`） | 读 L19-22：注释自陈「随迁移器整层删除，对照对象已不存在，结论保留」 | **非债**（有意历史对照） |
| D | `src` 内 504 处 `TD-xx` 债号注释引用 | 抽样核验：均为有效债号（TD-22-1/TD-04-2…），无幽灵债号（TD-24-3 已清） | **非债**（24 轮已饱和） |

## 五 · 探债（明细）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-11-13 | `AssistantTablePanel.tsx:341` 恒传 `disabled={false}` 给 `RowOpsMenu`（其 `disabled` 透传按钮但唯一调用点永 false）→ 恒绿假护栏/死参，禁用能力永不被触发 | 增债 | 结构债 | 1 文件 / 1 调用点 | 低 | 孤例（假护栏恒绿：守卫恒 false） | 二选一：① 删 `disabled` 透传 + 调用点显式 `false`（确认菜单恒可点）；② 接真实禁用条件（如提交/异步落库中禁点） | 已解决 2026-09-16（采①：删透传，用户可做的事不变） |

## 六 · 结论 + 覆盖账

- **新债 1 笔**（TD-11-13，归 11 区）：`RowOpsMenu` 恒绿假护栏/死参，假护栏(恒绿)形态孤例。
- **非债裁定 3 笔**（B/C/D）：timeline coming-soon 有意占位；outcome.ts 历史对照有意；TD 债号引用无幽灵（24 轮已饱和）。
- **五大历史母体横推达标**：静默吞/存储键/事件/死代码 四闸全绿；assetType 内联副本仅剩已登记 TD-16-4。**未现母体级复发**，无新兄弟实例。

**覆盖度表（本轮增量）**：

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `scripts/check-silent-catch.mjs` 等四闸 | 元 | 已裁定非债 | 🟢 | 本轮 09-16 | 跑闸✅0 | 非债（固化） |
| `src/components/base/utils/assetType.ts` | L0 真源 | 深审（无债） | 🟢 | 本轮 09-16 | grep 稀有扩展名全库 | 非债（仅 TD-16-4 已登记） |
| `src/components/agent/assistantTable/AssistantTablePanel.tsx` | L3 UI | 🟢 已还清(TD-11-13) | 🟢 | 还债 09-16 | 调用点已去显式 `disabled={false}` | 非债 |
| `src/components/agent/assistantTable/RowOpsMenu.tsx` | L3 UI | 🟢 已还清(TD-11-13) | 🟢 | 还债 09-16 | 接口定义 + 解构默认值 + button disabled 三处删净 | 非债 |
| `src/components/videoEditor/ui/editor/panels/timeline/timeline-toolbar.tsx` | L3 UI | 已裁定非债 | 🟢 | 本轮 09-16 | L110-115 coming-soon | 非债 |
| `src/components/videoEditor/types/outcome.ts` | L0 类型 | 已裁定非债 | 🟢 | 本轮 09-16 | L19-22 自陈已删 | 非债 |
| `src`（579 .ts 经四闸） | — | 深审（无债） | 🟢 | 本轮 09-16 | 四闸全绿 | 非债 |

**本轮审了 7 个文件 + 全 src 四闸覆盖；未审 0（聚焦清单已全核）。DATAFLOW 无链路变化，未漂，已对账一致。**

## 七 · 本区域状态

- 本区域状态：**已验证(债已还清)**（TD-11-13 于 2026-09-16 还清；此前 11 区 🟢 已验证(无债) 被推翻——漏判 RowOpsMenu 恒绿假护栏）
