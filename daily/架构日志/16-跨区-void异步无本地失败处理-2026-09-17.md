# 16 · utils 工具层 · 跨区 · void 异步无本地失败处理 · 2026-09-17

> **性质**：按用户铁律「一段代码失败了、却没任何人知道，那就是债」对全仓 `void` 异步（实测 24/30 处）+ 静默箭头 catch（5 处）做专项扫债。
> **结论落点**：本文件（唯一写全处）。
> **关联（不重立）**：
> - `TD-16-33④`（main.tsx 全局监听过滤+节流静默吞）—— 本批 3 处债是其**逐点实例**（站点未本地处理失败，唯一知情者=全局监听）。
> - `TD-24-4`（全 src 错误兜底续审）—— 其枚举清单（persistCanvas/playbook/loadProviders/会话落盘7处/ResourceLibrary初始/GriderMerge/遮罩保存）**不含本批文件**，故为新增。
> - `TD-16-22`（43 处逐点 catch-return-default）—— 本批 `void` 形态不在其口径内，是补完。
> **本轮边界**：只读登记，不改一行 src（含不改闸、不改 spec）。

- **本区域状态**：`已验证(有债待还)`

---

## 〇 · 铁律重申（裁定，2026-09-17）

> 「一段代码失败了、却没任何人知道，那就是债。」→ 无痕迹兜底 = 违约 = 必登记一债。
> 配套：`TD-16-33 §〇` 已立「禁止兜底 / 失败降级留痕 / 确需兜底须留痕」。

---

## 一 · 链路快照（grep 实证，非猜）

```
实测面（src）：
  · 真正空 catch {} 块                          = 0   （闸已收口 ✅）
  · 静默箭头 catch  .catch(()=>…)               = 5  处（tts/PresetGridView/logger/upstreamLink/projectMemoryStore）
  · void 触发的 fire-and-forget                 = 24/30 处（手势/定时器/生命周期触发 async）
```

## 二 · 溯源（母体）

**母体** = `TD-16-33④` 失败吸收层的**逐点实例**：`void` / 点触触发 `async` 函数，函数内**无 catch 或仅 finally 无 catch**，失败时唯一的「知情者」是 `main.tsx` 全局 `error`/`unhandledrejection` 监听（只 `logger.error`，不触达用户）。
与 `TD-16-22`（catch 内 return 默认值）同源不同形：本批是「**连 catch 都没有** / 有 catch 但静默重置」，失败在站点层即消失。

## 三 · 定海（不变式 + 同批合规对照，证明有正确范式）

1. **不变式**：`spec/CONTEXT.md §三`「错误必须真实透传」。
2. **同批已合规（本仓有正确范式，问题只在「部分点触异步未接范式」）**：
   - `editor-header.tsx:69/88` handleSwitchProject/handleCreateProject → `catch` 内 `toast.error('切换/新建作品失败',{description})` ✅
   - `PanoramaNode.tsx:346` doCapture → 未就绪显式提示用户（注释「不静默」）✅
   - `ResourceLibrary.tsx:268` loadMore → `setLoadMoreError` + 重试按钮 ✅
   - `use-local-storage.ts:65` persist → `catch → reportDegrade` ✅
   - `PromptHub.tsx:219` runLoad → `.catch(() => setStatus('error'))`（UI 可见，仅区分弱）✅
   - `audio-waveform.tsx:78` initWaveSurfer → `catch → logger.error + setError(true)` ✅
   - `relayProxy.ts:194` / `upstreamLink.ts:48` → `.catch(logger.warn)` ✅
   - `AssetNode.tsx:345` / `ImageGenerate.tsx:556` / `AssistantTablePanel:460/467` → `.then(outcome => toast)` ✅
   - 5 处静默箭头 catch → 全部带 `catch-ok:` 登记码（LOCK_CHAIN/RECURSION_GUARD/BROWSER_API/PARSE_FALLBACK，见 `catchOk.ts:29-72` SSOT），结构性豁免，**非债** ✅

## 四 · 切割（3 个债站点，逐个 refs/行号）

| # | 站点 | 位置 | 形态 | 失败后果 |
| --- | --- | --- | --- | --- |
| ① | `exitEditorToCanvas` | `EditorShell.tsx:51`（定义）/ `:102`、`:113`（`void` 触发） | `try { prepareExit(); releaseProjectContext(); } finally { onExit() }` —— **无 catch** | 退出/释放失败 → unhandled rejection → 全局监听只 logger；用户可能带着未妥善退出的编辑器状态离开、零反馈 |
| ② | `handleConfirm`（导入）+ `onLocalFiles`（拖入） | `ImportMediaModal.tsx:288`（定义 `async`，`await onPick(picked); onClose?.()` 无 try/catch）/ `:534`（`void handleConfirm`）· `:303`（`void onLocalFiles`） | `async` + `await onPick` **无 try/catch** | 导入/落盘失败 → unhandled；弹窗不关、无错误提示，用户以为成功 |
| ③ | `copyCode`（复制代码块） | `ChatMarkdown.tsx:176`（定义）/ `:191`（`void copyCode`） | `catch { setCopied(false) }` —— **静默重置，无 toast/error** | 剪贴板写入失败（权限等）→ 用户点了复制、图标不变化、无任何失败提示 |

## 五 · 探债（技术债账本）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-16-34 | `void` 异步无本地失败处理（① exitEditorToCanvas ② ImportMediaModal.handleConfirm/onLocalFiles ③ ChatMarkdown.copyCode），失败依赖全局监听静默吞 | 增债 | 结构债 | 编辑器退出 / 素材导入 / 代码复制 | 中 | 缺「点触 async 必须本地处理失败」机器守卫（与 TD-16-33④ 同母体：全局监听只 logger 不触达用户） | ① 3 处补 `.catch`：exitEditorToCanvas 至少 `logger.warn`+`toast.error`（退出失败用户应知）；handleConfirm/onLocalFiles 包 `try/catch` 呈现导入失败；copyCode 失败时 `toast.error` 提示 ② （可选·并 TD-16-33④）在 `check-silent-catch.mjs` 加规则：`void <async调用>` 或 `onClick={() => void fn()}` 处，fn 若自身无 catch 则判违规 | 待还 |

> **不重立（已合规/受治理，仅挂链接）**：上述 §三 全部对照项；5 处静默箭头 catch（catch-ok 治理）；`TD-16-22`（43 处逐点）/ `TD-24-4`（全 src 续审，未含本批文件）。
> **诚实边界**：本轮扫 35 处候选（5 静默 catch + 30 void），**仅 3 处确为债**—— 证明 `catch-ok` 治理 + `TD-16-22`/`TD-24-4` 已覆盖绝大部分；本批是 `TD-16-33④` 的遗漏逐点实例，非新母体。

## 六 · 覆盖度表（增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `EditorShell.tsx` | L3 编排 | 深审 | 🔴 | 本轮 09-17 | `:51` 无 catch / `:102` `:113` void | TD-16-34① |
| `ImportMediaModal.tsx` | L3 UI | 深审 | 🔴 | 本轮 09-17 | `:288` 无 catch / `:534` `:303` void | TD-16-34② |
| `ChatMarkdown.tsx` | L3 UI | 深审 | 🔴 | 本轮 09-17 | `:176` catch 静默重置 / `:191` void | TD-16-34③ |
| `editor-header.tsx` | L3 UI | 深审 | 🟢 | 本轮 09-17 | `:69`/`:88` toast.error | 已裁定非债 |
| `PanoramaNode.tsx` | L3 节点 | 深审 | 🟢 | 本轮 09-17 | `:346` 显式提示 | 已裁定非债 |
| `ResourceLibrary.tsx` | L3 面板 | 深审 | 🟢 | 本轮 09-17 | `:268` setLoadMoreError+重试 | 已裁定非债 |
| `use-local-storage.ts` | L2 hook | 深审 | 🟢 | 本轮 09-17 | `:65` catch→reportDegrade | 已裁定非债 |
| `PromptHub.tsx` | L3 面板 | 深审 | 🟢 | 本轮 09-17 | `:219` .catch→setStatus error | 已裁定非债 |
| `audio-waveform.tsx` | L3 UI | 深审 | 🟢 | 本轮 09-17 | `:78` .catch→logger+setError | 已裁定非债 |
| `tts/service.ts` `PresetGridView.tsx` `logger.ts` `upstreamLink.ts` `projectMemoryStore.ts` | 各层 | 深审 | 🟢 | 本轮 09-17 | 5 处静默 catch 均带 catch-ok 登记码 | 已裁定非债 |
| `src/**`（其余 20 处 void） | 各层 | 抽审 | ⚪ | 本轮 09-17 | `relayProxy`/`AssetNode`/`ImageGenerate`/`AssistantTablePanel` 等已合规（见 §三）；余未逐点深审 | 待下轮核（不断言为债） |

**覆盖账**：本轮审 **35** 候选（5 静默 catch + 30 void）/ 本批共 **35** / 未审 **0**（仅本批 `void`+静默 catch 形态；不含 `TD-16-22` 已覆盖的 43 处逐点 catch-return-default）。

---

## 七 · 结论与下一步

- 本区域状态：`已验证(有债待还)`。
- 新债 **1 笔**：**TD-16-34（void 异步无本地失败处理，3 站点）**。
- **诚实结论**：本轮扫 35 处候选仅 3 处为债，证明既有 `catch-ok` 治理 + `TD-16-22`/`TD-24-4` 已覆盖绝大部分静默兜底；本批是 `TD-16-33④`（全局监听静默吞）的**逐点遗漏**，母体已立、只需补站点。
- 下一步（择一，走「改码 7 步法」）：① 就 TD-16-34 三处补 `.catch`（小改动、零架构风险）；② 把「void 点触 async 必须本地处理失败」加进 `check-silent-catch.mjs` 判据面（与 TD-16-33④ 的闸扩展一并施工）。
