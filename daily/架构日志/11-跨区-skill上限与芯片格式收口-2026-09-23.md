# 架构日志 · 11 AI 助手（跨区） · 2026-09-23

> 本轮：清偿 11 区全部 4 条待还债（TD-11-78／79／80／81），并就地更新 `spec/判据登记表.md`（§四 追加 #46、§八 #4 降为 N=1）。
> 三条（78/79/80）上一轮被判「交用户定」，本轮按 7 步法重判后**直接做**（账本解法不是判据 · Step 1）。

## 一、链路快照（refs 实证）

```
前端（早退侧）
src/components/agent/skill/rules/skillDirName.ts
  isLegalDirSegment（围栏）· slugifySkillName（产出）· SKILL_MAX_DIR_SEGMENT_LEN（跨栈常量）
src/components/agent/skill/write/skillImport.ts
  importSkillPackages → packageLimitError（新增提交前预检）· 3 个跨栈上限常量
src/components/agent/panels/ChatMarkdown.tsx
  INLINE_PATTERN ← 芯片段改为 CHIP_PATTERN_SOURCE 派生

后端（权威侧）
localTool/src/utils/skillSegment.ts（**新文件**：isSafeSegment + SKILL_MAX_DIR_SEGMENT_LEN，纯函数）
  ← localTool/src/routes/skills.ts（改为 import；safeRelPath / handleSkillsSave 用另三常量）

对账设施
scripts/skill-segment-vectors.json（**共享向量**，两侧各自断言）
scripts/check-arch.mjs → CROSS_STACK_CONSTS（+4 项，共 10 项）
```

**DATAFLOW 对照**：链路未变（无文件增删、无新入口）⇒ 现状图无需改；`utils/skillSegment.ts` 是**同一判据的换位**（从带副作用的路由文件抽到纯函数），不是新链路节点。

## 五、探债（本轮清偿明细 · 真源 = `scripts/debt.mjs`）

| TD-ID | 中心数（改前 → 改后） | 处置 |
| - | - | - |
| TD-11-78 目录名上限 64 | **3 → 2** | 前端 2 处内联（`slice`/围栏）合并为 `SKILL_MAX_DIR_SEGMENT_LEN`；后端同名，纳入 `check:arch` 跨栈对账（跨栈无共享模块 ⇒ 2 是结构下限，由闸守住相等） |
| TD-11-79 跨栈谓词无对账 | 谓词实现 **2（跨栈必然，禁合并）**；**对账载体 0 → 1** | 后端谓词抽为纯函数（原在带 fs/db 依赖的路由文件里，**对账测试根本加载不了**）；两侧共享向量 + 各自对账测试（前端 vitest / 后端 node:test） |
| TD-11-80 上限前端无预检 | 预检 **0 → 1**；上限载体 3 → 前后端各 1（闸对账） | 三上限入跨栈常量 + `packageLimitError` 提交前预检（口径含 `SKILL.md`，与后端一致） |
| TD-11-81 ChatMarkdown 第二份正则 | **2 → 1** | 芯片段改用 `promptChipFormat.CHIP_PATTERN_SOURCE`（真源 `.source`） |

**用户可感知的行为变化（显式声明）**：
- **TD-11-81**：带 `\|thumb` 段的芯片串此前**不匹配**（当普通文本），现在**会**高亮为胶囊 —— 这是把弱化副本改回真源格式的结果。
- **TD-11-80**：超限的技能包现在在**提交前**就报具体原因（文件数／层级／段长），此前要等后端 400 且只说"写入失败"。

**上轮判断被推翻处**：78/79/80 上轮以「会让导出名 +1／要建闸／加行为」为由**交用户定**。重判：
- 78 的"导出名 +1"是真成本，但收益是**3 处静默分叉面归零** → 做；
- 79 不必建闸 —— **对账测试**（手段优先级 4）就够，且更稳；
- 80 的"加行为"是**把错误提前**（错误离成因更近），不是改能力 → 做。

## 六、覆盖度表

| 板块 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- |
| §四 agent 与 prompt | 深审 | 🟢 | 2026-09-23 | 逐行核真源 file:line + N | TD-11-78／79／80／81 已清；新增判据 #46 |
| §八 文本与任务中心 | 深审 | 🟢 | 2026-09-23 | （本轮附带） | 芯片格式 N 2 → 1（#4） |

**覆盖账**：本轮只动上述两处；其余板块沿用上轮结论。

## 七、结论与下一步

- 本区域状态：`已验证(无债)`。
- 待还债项已流入 `daily/架构日志/债务.md`：否（11 区清零）。
- DATAFLOW.md：已对账一致（链路未变）。
- 灯：覆盖度表已更新（🟢）；区域级灯由 `index.md` 自动派生。

## 八、交付闸自查（本轮有改动）

| 闸 | 本轮结论 | 可复现凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | 成立：78 上限 3→2（跨栈下限，闸守）、81 正则 2→1、79 对账载体 0→1 | 见 §五表；`grep -rn "promptChipRe\|@\\\\{" src/components/agent/` 只剩派生引用 |
| 减一 · 兜底净减 | 无兜底增减（0 → 0）；新增预检是**早退**（失败可见），不是掩盖 | 预检失败走 `failed[]`（如实上报），非吞 |
| 减二 · 复杂度实减 | 成立：78 三处写死 → 1 常量 + 闸；79 谓词从带副作用文件抽出（可测）；80 三个上限 + 3 处预检 → 单原语 | 见 §五表（改前/改后并列） |
| 一诚实 · 失败诚实可见 | 成立（80）：预检失败**带具体原因**返回 `failed[]`，不静默、不伪装成功 | 探针 ② 与 §五"行为变化"两条 |
| 零炸 · 防回归防副作用 | 成立：前端 250 项相关测试 + 后端 410 项全绿；8 道闸通过 | 下方「反向探针」 |
| 一凭证 · 证据可复现 | 3 个探针结论块 + 反向探针块可复现 | 下方「探针归档」 |

**探针归档（SOP §7.1 三项：①结论块 ②注入语义 ③还原后绿）**：

① TD-11-78/80 注入：`SKILL_MAX_DIR_SEGMENT_LEN = 64` → `= 65`（后端单边漂移）
```
🔬 探针结论｜TD-11-78/80 跨栈常量单边漂移
   文件   : localTool/src/utils/skillSegment.ts（命中 1 处，已还原 sha=0588ce6693f1）
   命令   : node scripts/check-arch.mjs
   观测   : exit=1｜输出 63 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「已漂移」 — 命中
```
② TD-11-79 注入：前端 `if (name.startsWith('.')) return false;` → `if (false) return false;`（谓词少一条判据）
```
🔬 探针结论｜TD-11-79 跨栈对账失效（前端去掉前导点判据）
   文件   : src/components/agent/skill/rules/skillDirName.ts（命中 1 处，已还原 sha=9aec6ac75f67）
   命令   : npx vitest run tests/unit/skillDirSegment.parity.test.ts
   观测   : exit=1｜输出 23 行
   ✅ 退出码 == 1 — 实际 1
```
③ 还原后绿：
```
$ npx vitest run <21 个相关测试文件>   → Test Files 21 passed · Tests 250 passed
$ cd localTool && npm test             → # tests 410 · # pass 410 · # fail 0
$ node scripts/check-arch.mjs          → ✅ 架构校验通过（跨栈对账 10 项）
```

**反向探针（零炸）**：前端 250 项（含 skill* 全套 + ChatMarkdown + AgentMessage + settings）· 后端 410 项 · 8 道闸（arch／node-types／node-handles／dead-code／any／node-data／gates／gate-vitals）全绿。

**零炸三问**：
1. **哪些行为被改了？** 仅两处、且都显式声明（见 §五"用户可感知的行为变化"）：带 thumb 段的芯片现在高亮；超限包改为提交前报因。
2. **改前能走的路径仍能走吗？** 是 —— 上述 660 项测试 + 8 闸全绿。
3. **改前会失败的仍失败吗？** 是 —— 非法目录名仍被拒（两侧一致，向量表覆盖）；未顺手改任何无关失败。

## 九、决策留痕

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度（→ §八「减二」） | SSOT（→ §八「一归」） | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 78/80 用**跨栈常量 + 闸对账**（而非合并两侧） | 跨栈无共享模块是物理约束 | 前端早退／后端权威各自留判据 | 3 → 2 | 1 份取值 | 做 |
| 2 | 79 用**对账测试**而非建闸 | 对账测试能真红（探针已证） | — | 对账载体 0 → 1 | 1 份向量表 | 做（建闸前置评审：手段优先级 4 > 6） |
| 3 | 79 的谓词**不合并**（保留两侧） | 判据重复 ⇒ 禁合并（Step 3） | 早退与权威是不同的层 | — | 2（跨栈必然） | 做（只收口可对账面） |
| 4 | 81 用真源片段派生（接受行为变化） | 格式契约只有一份 | — | 2 → 1 | 1 份 | 做 |
| 5 | 80 的"加行为"照做 | 错误提前 = 失败更诚实 | 预检用后端同一常量 | — | 1 份 | 做 |
