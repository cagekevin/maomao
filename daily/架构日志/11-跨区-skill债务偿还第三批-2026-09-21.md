# 11-跨区 · Skill 债务偿还（第三批：假按钮修真 + skills 归一三处收口）

> **触发**：用户「继续」→ 施工 TD-11-22；施工中挖出 TD-11-16 的**尾巴**（skills 的三条写路径各写一份归一）。
> **用户裁定（2026-09-21）**：**不再每次跑全量**，只跑相关测试（SOP §7.2 本就这么要求 —— 是我执行错了，不是文档要改）。
> **范围**：`agent/conversation/{conversationSnapshot,conversationStore,skillSelection}` · `agent/index.ts` · `settings/sections/SkillSettings` · `tests/unit/{skillSelection,agentDraftTd17}`。

## 一 · 改动

| 项 | 改法（一句话） |
| --- | --- |
| **TD-11-22** | 新增用例 `useSkillInCurrentConversation(skillId)`（`conversation/skillSelection.ts` + agent 域门面导出）：把 id **追加**进当前对话（幂等）＋**请求打开 AI 面板**（`setSetting('agentOpen')`）＋**写后回读确认**（写不进去 ⇒ `ok:false`，不假成功）。设置页「去使用」改调它 —— 旧实现只弹一句 toast、零状态变更（假按钮） |
| **TD-11-42**（本轮新登记即修） | `skills` 归一收口为**唯一实现** `normalizeSkillIds`，四处调用（`setCurrentSnapshot` / `setCurrentSkills` / `resetCurrentConversationToEmpty` / `importLegacy`）。旧实现三处各写一份「对象浅拷贝」`{...s}`，而契约是 id 列表 ⇒ `{...'s1'}` 展开成 `{0:'s',1:'1'}` ⇒ **刷新/切对话后已选技能静默丢失**（TD-11-16 的尾巴） |

## 二 · 探针（先红后绿 · 凭证）

```
🔬 探针结论｜TD-11-22 打开面板这一步不许省
   文件   : src/components/agent/conversation/skillSelection.ts（命中 1 处，已还原 sha=a62551acdb06）
   命令   : npx vitest run tests/unit/skillSelection.test.ts
   观测   : exit=1｜输出 23 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「面板被请求打开」 — 命中
```

```
🔬 探针结论｜TD-11-16 尾巴 skills 落盘形状
   文件   : src/components/agent/conversation/conversationSnapshot.ts（命中 1 处，已还原 sha=299202a220bb）
   命令   : npx vitest run tests/unit/skillSelection.test.ts
   观测   : exit=1｜输出 97 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「回归」 — 命中
```

**还原后的绿**：`tests/unit/skillSelection.test.ts` → `Tests 5 passed`。

## 三 · 验证（相关测试口径）

受影响面 = conversation 快照写入口 + skill 装配 + 设置页接线 ⇒ 实跑：

- `agentDraftTd17(7) · agentMessages(9) · conversationStore(19) · conversationState(12) · skillSelection(5)` → **52 例绿**
- 本区前述：`skillStore(14) · AgentPanel(29) · useAgentChat.hook(67) · skillInject(13)` → **123 例绿**
- `type-check` 0 错 · `check:dead-code` **0 新增** · `check:arch` ✅ · `adr audit` 0 问题

## 四 · 弯路留痕（反向探针抓到的既有语义冲突）

本轮最后一次全量跑抓到 `agentDraftTd17 ④/⑤` 红：**夹具用对象形状的 skills**（`[{ id: 's1' }]`）。

- 判定：**不是我的实现越界，是夹具过时** —— 那两条测的是"窄接口只动 draft、不碰其它字段"，与 `skills` 的形状无关；新契约是 id 列表（`AgentPanel` 只传 id、`useAgentChat` 签名 `skills?: string[]`）。
- 处置：改夹具为 `['s1']` / `['keep']`，并顺带把 **`resetCurrentConversationToEmpty` 也归一**（否则第三个写入口继续漏网 = 归一又成了 N 份）。
- 教训：改形状类契约时，"三条写路径"这种**多点写入**必须一次收口；只改读侧会留下静默丢数据的尾巴（本轮的尾巴正是上一轮漏的）。

## 五 · 结清与剩余

- **结清**：TD-11-22 · TD-11-42（本区累计结清 **10** 条）。
- **仍待还 14 条**：TD-11-17 · 21 · 24 · 25 · 26 · 27 · 28 · 29 · 34 · 35 · 36 · 37 · 38 · 39。

## 六 · 决策留痕

- 用户「不要每次跑全量」= **验证口径裁定**（只跑相关测试）⇒ 已按此执行；SOP §7.2 原本如此，本轮前几轮是我执行偏差（全量 3 次），记为执行问题。
- TD-11-22 采用**归档时已定的 A 方案**（真接切换）；落地形态比原设想更省：**不需要新事件/新键** —— 面板开关的真源 `agentOpen` 本就有唯一入口 `setSetting`，会话选中态走既有 `setCurrentSkills`，故只新增一个"用例函数"。
