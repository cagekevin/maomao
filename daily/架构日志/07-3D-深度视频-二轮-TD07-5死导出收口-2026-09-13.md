# 07 区 · 二轮 — TD-07-5 死导出收口（并更正一处「记录层漂移」）

> 日期：2026-09-13 ｜ 触发：用户「继续」→ 按七步法 Step 1 取证候选批次，**用新建的死代码闸（knip）核对**时发现：`TD-07-5` 声称已解决，**实际未落地**。
> 范围：`director3d/project.ts` · `rig.ts` · `panels/controls.tsx` · `d3dPersistence.ts`（共 18 处摘 `export`）；连带 `scripts/dead-code-baseline.json`（158→140）；文档更正 3 处。
> 首轮：[07-3D-深度视频-2026-09-12.md](./07-3D-深度视频-2026-09-12.md) ｜ 前置审计：[docs/director3d-死导出收口审计-2026-09-09.md](../docs/director3d-死导出收口审计-2026-09-09.md)。

---

## 一、Step 1 取证 —— 发现「声称完成 ≠ 代码完成」

| 来源 | 声称 |
| - | - |
| 09-09 审计文档 §〇 | 「B 类 51 条**全部处置完毕**」+ 全表打 `[x]` |
| 07 区首轮日志（TD-07-5 行） | 「[已解决 2026-09-12]（48 摘 export + 2 整删 + 1 改判 A 类）」 |
| **死代码闸（knip）实测** | director3d **仍有 18 条**导出态未使用 |

**直接核验**（不靠描述）：
- `grep '^export … <symbol>'` → `POSE_LABELS` / `DEFAULT_CAMERA_POSITION` / `PROPERTY_REGISTRY` / `uniqueSortedKeyframes` / `normalizePerson` / `normalizePathPoint` / `rotateVectorXYZ` / `finiteVector3` / `ease` / `ENTITY_FIELD_NAMES` / `LEGACY_PROJECT_STORAGE_KEY` / `LEGACY_DEFAULT_CAMERA_TARGET` **全部仍带 `export`**（正是 09-09 审计标 `[x]` 的条目）。
- `git log -- src/components/director3d/project.ts` → 只有 `74e8dd2`（09-09 的 **D/C 类**收口）；**没有任何提交对应「09-12 的 B 类 51 条」**；该目录 `git status` **无未提交改动**。
- 反证「部分确实做了」：`PROJECT_VERSION`/`CUSTOM_ASPECT_PATTERN`/`cleanAspectPart`/`normalizeCamera`/`normalizeShot`/`normalizePathPoints`/`RIG_PRESETS`/`HISTORY_LIMIT`/`TOAST_DURATION_MS`/`SceneList`/`smoothDepthValues` **已无 `export`**（所以它们没进 knip 报告）→ **B 类只落地了一部分，18 条被漏/被回退**。

> **结论（Step 1）**：TD-07-5 是**真债且仍待还**；同时暴露一处 **记录层漂移（M3）**——审计文档与区域日志都写「已完成」，与代码不符。**债台账 `债务.md` 保持「待还」是对的**（这次要以台账 + 代码为准，不以区域日志结论为准）。

### 逐条证死（Step 6「删旧先证死」）

对 18 个符号做「定义文件之外」引用扫描，命中项逐条甄别（**不做符号名文本匹配**，见审计 §一 警告）：

| 命中 | 判定 |
| - | - |
| `normalizePerson` ×2 | `App.tsx:34/87` **注释**（讲解归一化入口），非 import |
| `ease` ×7 | 其它文件的**自有 `ease`** 与 CSS 字符串 `ease-in-out`（审计 §二 陷阱 2 已预警） |
| `JOINT_DEFINITIONS` ×3 | 三个测试文件的**注释**（"cloneJointPose 只拷贝 JOINT_DEFINITIONS 登记的关节"） |
| `PROJECT_KEY_DEFAULT` ×0 | 全仓零外部引用；且 `storage.ts` **并未** import 它 → 09-09 审计的 A 类判定（"被 storage.ts 引用 / knip 误报"）**不实** |

其余 14 条外部引用均为 0。且 18 条**在各自文件内都有使用** → 处置一律「**摘 `export` 保实现**」，**零删除**。

---

## 二、施工

用一次性脚本按「声明行模式」精确摘 `export`（`^export ((const|function|type|interface) <name>\b)`），**逐条断言存在（缺失即 fail-loud）**：

| 文件 | 条目 |
| - | - |
| `director3d/project.ts` | 12 导出 + 3 类型（`ChannelDefinition`/`PropertyLayer`/`PropertyMeta`） |
| `director3d/rig.ts` | `JOINT_DEFINITIONS` |
| `director3d/panels/controls.tsx` | `AxisSlider`（`App.tsx` 只 import 同文件的 `ToolButton`） |
| `director3d/d3dPersistence.ts` | `PROJECT_KEY_DEFAULT` |

**净效果 = 0 处删除、0 处逻辑改动**，只收窄模块边界。

---

## 三、验证（死代码闸端到端驱动 + 行为安全网）

| 项 | 结果 |
| - | - |
| 摘除后复核 | ✅ 18 个符号全部无 `^export` |
| `tsc --noEmit` | ✅ 0 错（去 export 后若仍被外部引用必报错） |
| **死代码闸** | ✅ 精确报出 **18 条已消失**（stale 提示）；`--update-baseline` 后 **基线 158 → 140、director3d 归零** |
| director3d/深度视频相关测试 | ✅ **160 例绿**（channel×4 / d3dPersistence×2 / depthVideo / director3d×2 / tracks，10 文件） |
| `check:push` | ✅ **7 道全绿**（type-check/any/catch/events/strict-src/arch/dead-code） |

> **本轮副产物**：证明新建的死代码闸**真能驱动清偿** —— 它既在改前指认出「声称已完成实则未做」的 18 条，又在改后逐条确认消失。

**度量**：

| 口径 | 改前 | 改后 |
| - | - | - |
| director3d 未使用导出 | **18** | **0** |
| 死代码基线（全局） | 158 | **140** |
| 模块导出面（`project.ts` 等 4 文件） | 宽（含内部实现细节） | 窄（仅真实跨模块契约） |
| 记录层一致性 | 审计/日志称"已完成"，代码未做 | 已更正 3 处，与代码一致 |

---

## 四、文档更正（不留失实记录）

1. `docs/director3d-死导出收口审计-2026-09-09.md` §〇：加「更正（2026-09-13）」块，列明仍在导出态的 18 条 + 指出 §一 A 类对 `PROJECT_KEY_DEFAULT` 的判定亦不实。
2. `daily/架构日志/07-3D-深度视频-2026-09-12.md` 的 TD-07-5 行：把「[已解决 2026-09-12]」更正为「[已解决 2026-09-13 · 更正本行原结论]」。
3. `daily/架构日志/债务.md` TD-07-5：改为已解决并写明漂移与新证据；`index.md` 07 区：纳入二轮。

---

## 五、结论与边界

- **TD-07-5 已清**：18 条全部摘 `export`（定义保留），死代码基线相应下降。
- **教训（值得复用）**：**「已解决」是描述，不是结论**——本轮若不做 knip 复核，就会相信审计文档的 `[x]` 而漏掉这 18 条。**台账（`债务.md`）与代码一致时才可信**。
- **未做（仍待裁定）**：`TD-07-3`（`project.ts` 巨模块是否拆）/ `TD-07-4`（`App.tsx` 上帝组件）—— 判断题，须先量化，保持不动。
