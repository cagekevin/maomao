# 架构日志 · 跨区（25 ADR 合规横扫）· 2026-09-21 —— 按 ADR 审计 2026-09-20 性能优化

> 本轮 = **纯只读审计**（无源码改动；两条负例探针注入后已还原，`git status src/` 无新增脏文件）。
> 判据源 = 现行 39 条 ADR（`node scripts/adr.mjs list`），不引用 memory / 轮次文件的自我声索作依据。

## 一、审计对象（昨天两簇性能优化 · 实测定位）

| # | 簇 | 内容 | 落点 |
| - | --- | --- | --- |
| A | 产物体积（1B） | `EditorShell` 静态 import → `React.lazy` + `Suspense`（首屏 gzip −19.5%） | `src/App.tsx:103/1697`（提交 `d233a506`） |
| B | React 重渲偿债（04 区 · 6 轮） | 探债 16 债 → 复核 +3 → 施工 → 契约根治 → 收口 | 见下「已结清清单」 |

**B 簇已结清 14 笔**（实测不在 `debt.mjs list` 待还表内）：TD-04-35/36/37/38/39/41/43/44/47/48/49/51/52/53。
**改判不修但仍挂待还 2 笔**：TD-04-42（关闭态不读 `items`，代价≈0）· TD-04-50（N=节点数，量级不足）。
**04 区现待还 8 笔**：TD-04-40/42/45/46/50/54/56/57（其中 46/56 = 已施工待全量零炸）。

**本轮新增原语（幽灵预留检查用 · `refs` 实证）**

| 原语 | ① 模块引用 | 判定 |
| --- | --- | --- |
| `src/hooks/useAssetInsertion.ts` | 5（4 节点 + 1 测试） | 真消费 |
| `src/hooks/useInlineNameEditing.tsx` | 3（2 面板 + 1 测试） | 真消费 |
| `useAppSettingsSelector`（`base/store/appSettings.ts:134`） | 3（App / assetUrl + 测试） | 真消费 |
| `useTaskBadge`（`base/store/taskStore.ts:849`） | 1（LeftPanel）+ `computeTaskCounts` 与 TaskCenter 共用 | 真消费 |

## 二、逐条 ADR 判定（13 条）

| # | ADR | 审计对象 | 判定 | 证据（本轮实测） |
| - | --- | --- | --- | --- |
| 1 | **0041** 机器可判定 + 负例探针 | 全部施工 | ✅ 合规 | 独立复现 2 条探针**均真红**（见 §三）；另定点 10 文件 / **89 测试全绿** |
| 2 | **0047**「删」的前提 | 删 `memo(NodeShell)` | ✅ 合规 | ① 用户能做的事没变少（行为等价，非入口/命令/契约）② 20/20 调用点传 JSX children ⇒ memo 从未命中＝假护栏，不是"有人用" |
| 3 | **0030** 幽灵预留 | 4 个新原语 | ✅ 合规 | 见上表 `refs` 实证，均非零消费 |
| 4 | **0045** 不许第二份入口 | `useAppSettingsSelector` / `useTaskBadge` | ✅ 合规 | 二者由 **store 层**提供（`subscribe`/`getSnapshot` 是模块内实现，消费方无从自组）＝原子订阅口，非绕环中转；且把角标口径 3 处收口成 1 份 |
| 5 | **0046** 让 AI 不猜 | `NodeShell.icon` / `ModelSelect.icon` 改契约 | ✅ 合规 | 落在**类型层**（`ReactNode` → `LucideIcon`），传 element 时 `tsc` 报 TS2741 ⇒ 不靠文档 |
| 6 | **0033** 常量 SSOT | `fitViewOptions` / `nodeOrigin` | ✅ 合规 | 已提模块常量（`App.tsx:1536/1546`），并回改了 `:213-219` 的**假收口注释**（A7） |
| 7 | **0016** 闸不许凌驾正确性 | 无 | ✅ 不适用 | 本轮未放宽任何闸；M1「不建闸」走了建闸前置评审 6 问（②⑤不过），未写 ADR（README §二「不必写」）✅ |
| 8 | **0019** 净减口径 | B 簇验收 | ⚠️ **不符** | 验收口径是「探针先红后绿」＝**症状消失**；仅 TD-04-56 给了净减（−135 行）。Step 0 普查的 87 memo / 1212 内联箭头 / 146 数组 / 213 对象**口径未固化** ⇒ 今复测口径已不可比（裸 `memo(` 37 vs 52） |
| 9 | **0049** 测试只锁用户可感知契约 | 新增用例 3 处 | ⚠️ **待清点** | 见 §四（ADR-0049 自身把「存量 UI 存在性用例待清点」列为待办②） |
| 10 | **0048** 运行时守卫 / 可见失败 | `Suspense fallback={null}` | ⚠️ **待拍板** | 首次打开剪辑器在 chunk（489.79 kB）下载期渲染**空** ⇒ 用户可感知退化；提交说明称「无行为差异」，此项不成立<br>▶ **2026-09-21 清偿轮实测推翻「可感知」**：网络仅 **3–17 ms**、总空白 60–120 ms（多为**搬家**的解析+执行，非新增）⇒ 见 `25-跨区-性能优化审计债务清偿-2026-09-21.md` §3.4 |
| 11 | **0040/0044** 归属与落点 | 新 hook 落点 | ✅ 合规 | `useAssetInsertion`（4 节点域消费）/ `useInlineNameEditing`（generate + resource 两域）⇒ 跨域消费，落 `src/hooks/` 与既有 `useNodeRename`/`useDisconnectSource` 同形态 |
| 12 | **0040** 债的区归属 | TD-04-38/44/47 | ⚠️ 观察 | 实现落点是 `src/hooks/useStoreSelector.ts` 与 `base/store/*`（地基层），债却登在 **04 画布/节点** ⇒ 按「数据落点」判据应属 13/18 区，按区复检会漏 |
| 13 | **0021** 禁 `as` | `useStoreSelector.ts:69` | ⚠️ 观察 | `let memoizedSelection: T = null as T;` —— 适用面是判别联合，**此处不算违规**；但 `strict` 打开即炸，留痕不动 |

## 三、本轮独立负例探针（不复述昨天声索）

| 探针 | 注入 | 结果 |
| --- | --- | --- |
| A · memo 有效性 | `LeftPanel.tsx:185` `memo(LeftPanel)` → `LeftPanel` | `panelMemoBail` **红**：`expected 2 to be 1` ✅ |
| B · selector 陈旧（TD-04-44） | `useStoreSelector.ts:63-64` 注释掉 ref 渲染期赋值（还原旧实现） | `useStoreSelector` **红**：`expected 1 to be 2`（id 换后仍返回旧值）✅ |

两条均已还原，`git status src/` 仅剩本轮之前既有的未提交改动。
**结论**：昨天的两条关键断言是**真会红的闸**，不是自证式装饰。

## 四、ADR-0049 边界用例（唯一需要人拍板的一组）

ADR-0049 于 2026-09-20 21:50 裁定、2026-09-21 落盘，而下列用例写在 20:00–21:00（**判据晚于用例**）⇒ 按 ADR-0049 §后果「待办② 存量 UI 存在性用例待清点」，判**待清点**，不判"违规"：

| 用例 | 断言内容 | 按 ADR-0049 的判读 | 建议 |
| --- | --- | --- | --- |
| `NodeTitle.test.tsx:54-61` | 图标 svg `width=11` + class 含 `text-muted` | **UI 元素与样式细节**，图标换了用户不会拿错结果；且类型层已拦 ⇒ 第二道冗余 | 删（UI 靠手测） |
| `useImageHoverActions.test.tsx:51-73` | 「按钮应存在 / `show` 应为 true」 | 存在性断言 | 改行为断言（点击后状态变化，该文件已有部分） |
| `panelMemoBail.test.tsx` / `useCanvasHistory.test.ts:111` | 渲染次数 / 引用同一 | **行为断言**（数子树渲染次数，探针真红）与**正确性**（陈旧值＝用户拿到错数据） | **保留** —— 与"元素存在性"不同类 |

> ▶ **2026-09-21 清偿轮已处置**：`NodeTitle.test.tsx` 那条**已删**（连 `Orbit` import 一起清）、`useImageHoverActions.test.tsx` 那两条**已删**（保留 `show` 可用性断言）、`panelMemoBail`/`useCanvasHistory` **保留**。
> **其余存量未清点**（审计只点 3 处）⇒ 已登记 **TD-25-43**（实测 36 个 test.tsx 用 `toBeTruthy` / `querySelector` 173 行 / `className` 23 处 / `getAttribute` 54 处）。凭证见 `25-跨区-性能优化审计债务清偿-2026-09-21.md` §3.2。
>
> ▶ **2026-09-21 13:50 用户裁定：TD-25-43 改判非债**（原话「存量 ui 测试这个不查，把这个列为非债」）⇒ **存量不做清点、不追**。
> ⚠️ **审计当时的读法已作废**：本条据 ADR-0049「新增与存量同此判据」判「待清点」，而 ADR-0049 已于同日 **12:42** 改写为**判据默认只约束新增/新改**（明文「判存量不算违规」）+ 待办②「存量待清点」**作废**；
> 其新增的 Lessons Learned 直接点名了这次误读：「正确的判据溯及存量，就变成**大面积误删的授权书** ⇒ 立判据默认只约束新增」。

## 五、探债（本轮登记 2 笔 · 线索全 / 解法从简）

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因 | 大致解法(一句话) | 状态 |
| - | --- | --- | --- | --- | --- | --- | --- | --- |
| **TD-25-42** | `04-跨区-M1母体建闸前置评审-2026-09-20.md` §Step 0（87 memo / 1212 内联箭头 / 146 数组 / 213 对象）+ 本轮复测（裸 `memo(` **37 → 52**，口径不可比） | 验收口径缺**可复算基线**：停在「探针先红后绿」＝症状消失，达不到 ADR-0019「验收看数量净减」 | 25 元层（性能轮的验收方法） | 04 区性能轮全部（14 笔已结 + 8 笔待还的验收口径同此） | 中 | 孤例（口径未固化，**非母体**） | 把 M1 普查口径固化为脚本，施工前后各跑一次，净减写进轮次 §八「减二」 | 待还 |
| **TD-04-58** | `src/App.tsx:1697`（`React.Suspense fallback={null}`）· 提交 `d233a506` | 用户可感知退化：chunk **489.79 kB** 下载期渲染空白；提交说明称「无行为差异」⇒ 描述与实况不符 | App 根装配（04 画布/节点） | 每个首次打开剪辑器的用户（一次性、稳定可复现） | 中 | 孤例 | A/B/C 见下 | 待用户拍板 |

**TD-04-58 的选项（业务债 · 只给选项标题，不比选）**

- **A** 补加载反馈（骨架屏 / spinner）—— 空白期可见
- **B** 维持现状由用户显式接受（改判非债；须回改 `d233a506` 提交说明里的「无行为差异」）
- **C** 空闲预取 chunk（打开前预加载）—— 空白期消失，代价是首屏多一次请求

> 本轮判**非债 / 合规**的结论同样留痕于 §二（13 条逐条挂证据），其中 4 个新原语用 `refs` 实证有真消费（非幽灵预留）、2 条关键断言用独立探针证真会红（非自证式）。

## 六、覆盖度表（本轮增量）

| 文件 / 判据点 | 层 | 覆盖状态 | 灯 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- |
| `src/App.tsx`（lazy + 常量 + selector 订阅） | 上层 | 深审 | 🟡 | `:103 / :1697 / :387 / :1536 / :1546` | 待拍板（fallback 空白） |
| `src/hooks/useStoreSelector.ts` | 地基 | 深审 | 🟢 | 探针 B 真红 | TD-04-44 已还 |
| `src/components/base/store/appSettings.ts` / `taskStore.ts` | 地基 | 深审 | 🟢 | 原子订阅 + 口径收口 | TD-04-38/47 已还（区归属错位·观察） |
| `src/components/canvas/parts/NodeShell.tsx` / `NodeTitle` | 上层 | 深审 | 🟢 | `icon?: LucideIcon`，memo 已删 | TD-04-37/51 已还 |
| `src/components/base/panels/LeftPanel.tsx` | 上层 | 深审 | 🟢 | 探针 A 真红 | TD-04-46 待全量零炸 |
| `src/hooks/useAssetInsertion.ts` / `useInlineNameEditing.tsx` | 中间 | 深审 | 🟢 | refs 5 / 3 | TD-04-52/56 已施工 |
| 新增测试 10 文件 | 元层 | 抽审 | 🟡 | 89 测试绿 | ADR-0049 待清点（3 处） |
| 性能轮验收口径（`M1母体建闸前置评审` §Step 0） | 元层 | 深审 | 🔴 | 口径不可比实测 | **TD-25-42** |
| `src/App.tsx:1697` Suspense fallback | 上层 | 深审 | 🔴 | `:1697` + 提交 `d233a506` | **TD-04-58** 待拍板 |
| B 簇未施工债（TD-04-40/45/54/57） | — | 未审 | ⚪ | 本轮只审"已施工部分是否合规" | 下轮 |

**覆盖账**：本轮共 9 个点位 / 审 8 / 未审 1 —— 未审的 1 项 = **B 簇 4 笔未施工债（TD-04-40/45/54/57）**，理由：本轮范围是「已做的性能优化是否合规」，未施工部分属下一轮施工审计，不是本轮边界。

> ▶ **后记（2026-09-21 13:12 补 · 「未审 1 项」已闭合）**：B 簇 4 笔随后由**两个轮次**全部清偿 ——
> TD-04-54 / TD-04-45 / TD-04-57 → `04-跨区-B簇未施工债清偿-2026-09-21.md`；
> TD-04-40 → `04-跨区-useEditor订阅面收窄-2026-09-21.md`（单独立轮）。
> 账本状态以 `node scripts/debt.mjs show <TD-ID>` 为准（现均为 `[已解决]`）。

---

## 七、结论与下一步

- **总判定**：昨天两簇性能优化在**方向与方法上合规** —— 未越权删功能（ADR-0047）、未造幽灵预留（ADR-0030）、未造第二份入口（ADR-0045）、判据落在类型层/唯一入口而非文档层（ADR-0046）、关键断言经本轮独立探针验证**真会红**（ADR-0041）。
- **两处口径问题**：① 验收停在「症状消失」而非 ADR-0019 要求的「数量净减」；② 1B 引入的空白窗口属**用户可感知变化**，提交说明未声明。
- **一处判据适用性**：3 处新增用例落在 ADR-0049 边界（判据晚于用例）⇒ 判待清点，建议按 §四 表处置。
- 本区域状态：`已验证(有债待还)`
- 待还债项已流入 `daily/架构日志/债务.md`：**是**（TD-25-42 · TD-04-58，均走 `debt.mjs add`）。
- DATAFLOW：本轮**未改动** —— 审计对象是渲染/引用层性能改动，不涉及链路漂移（故未做数据流对账，非遗漏）。
- 下一步（按优先级）：① 拍板 TD-04-58 的 A/B/C（业务债，交用户）；② 跑全量 `vitest` 后销 TD-04-46 / TD-04-56；③ 还 TD-25-42（固化 M1 净减口径脚本）。
  - ▶ **2026-09-21 已全部执行**（见 `25-跨区-性能优化审计债务清偿-2026-09-21.md`）：① TD-04-58 **改判非债**（实测推翻，非拍板）；② 全量 `vitest` **252 files / 3073 passed** ⇒ TD-04-46 / TD-04-56 已销；③ TD-25-42 已还（`scripts/m1-count.mjs`）。另：§四 的 2 处已处置，其余登记 **TD-25-43**。

**决策留痕**：本轮无取舍（纯只读审计，探针已还原）。

> 旁注（**不属本轮、未处置**）：`debt.mjs audit` 现存 4 项**历史**违规 —— 锚点断链 3（TD-01-27 / TD-02-67 / TD-04-55）+ 归类非规范 1（TD-22-68）。与本轮两笔新债无关（新债锚点文件内均含其债号，`audit` 未报），留痕给下一轮治理，不在本轮边界内顺手改。
