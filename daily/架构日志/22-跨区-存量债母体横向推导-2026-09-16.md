# 22-跨区 · 存量债母体横向推导 · 2026-09-16

> **触发**：用户指令「根据现有债务横向推导，还有没有其他债务，从底层开始找」。
> **方法**：取主表 17 条待还债的母体模式（登记表之外的事实键／绕过唯一出口／公网直连／观测断链），对全仓做**底层面横切扫描**（grep + refs + debt.mjs），每条候选结论挂 `文件:行号` 证据；命中债 → 登记，判非债 → 留痕。
> **跨区声明**：本轮命中 22/04/07 三区根因，按规则 9 只开本文件一份；债 ID 绑根因区。

---

## 一 · 链路快照（扫描面 = 底层横切，非单链路）

| 扫描面 | 手段 | 命中 |
| --- | --- | --- |
| 存储直写面 | grep `localStorage.` / `sessionStorage.` / `indexedDB` / `yimao_*` 字面量 | 14 处 localStorage / 0 sessionStorage / 9 文件 indexedDB / `yimao_*` 全对上 STORAGE_KEYS |
| 出站面 | grep `fetch('http…` / `dispatchEvent(` / `new (BroadcastChannel|Worker|EventSource)` | fetch 字面量 0 / dispatchEvent 0（仅注释）/ 通道 5 处 |
| 观测面 | grep `console.(log|warn|error|info|debug)`（src） | 9 文件 16 处 |
| 登记表对账 | 读 `contracts.ts` 全文（EVENTS/STORAGE_KEYS/apiRegistry/NODE_TYPES）+ 逐键比对 | 1 键漏登（TD-07-7） |
| 生命周期面 | grep `setInterval(` | 9 处，均有清理或属已审区 |
| 死代码面 | `find-dead --strict` + `check-dead-code.mjs`（knip 6.35.1） | 1 误报（见 §六.6 证伪） |

## 二 · 溯源（从哪些存量债的母体推过来的）

- TD-02-33（裸 localStorage 未登记）→ 横扫全仓裸存储写 → 命中 TD-07-7（`stageframe-project`）。
- TD-22-47（iconify 公网直连无自建代理）→ 横扫第三方出站 → 命中 TD-22-57（huggingface.co 模型下载）。
- TD-07-1 / TASK-056（崩溃日志不进统一 logger，均已修）→ 横扫 console 直写 → 命中 TD-04-29（PanoramaNode componentDidCatch）。

## 三 · 定海（判据锚点）

- 唯一日志出口 = `base/core/logger.ts`（`contracts.ts` 分层 ③；87 fan-in）；崩溃/告警类必须落 `/api/logs`（先例：`ErrorBoundary.tsx:44-46` 注释明载 TASK-056 修的就是同一形态）。
- 存储键登记规则 4（`contracts.ts:158`）：「迁移键 → 添加 migration 字段记录迁移历史」。
- 出站唯一口先例：iconify → localTool `/api/iconify/*` 代理（TD-22-47 收口轮⑧用户裁定）。

## 四 · 切割（债 vs 非债的边界）

见 §六 逐条留痕；边界判据 = 有无 `文件:行号` 证据 + 是否已有闸/裁定接管。

## 五 · 探债（明细真源）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-22-57 | `videoEditor/engine/services/transcription/worker.ts:1-5` import `@huggingface/transformers`；`constants/transcription-constants.ts:21-57` 五个模型 `huggingFaceId: 'onnx-community/whisper-*'`（120MB~1604MB）；service/worker **无** `env.remoteHost` 覆写（grep 0 命中）→ 浏览器运行时直连 huggingface.co CDN 拉模型；活调用链 `captions.tsx:96 → transcriptionService.transcribe → worker` | 增债 | **业务债 · 待用户拍板** | 剪辑器「字幕」面板一键转写（网络不可达即 init-error，功能级瘫痪） | 中 | TD-22-47 同母体：第三方公网直连、无唯一出站口 | **A** 按图标先例收口：localTool 增 `/api/hf-models/*` 代理 + worker 设 `env.remoteHost` 指向本机（推荐，与 TD-22-47 裁定一致）；**B** 维持直连（接受 VPN/网络依赖，UI 已诚实报错）；**C** 裁撤转写域（stub 化，回归 09-14 曾做过的「范围外移除」） | 待用户拍板 |
| TD-04-29 | `nodes/PanoramaNode.tsx:223-225` `componentDidCatch` 裸 `console.error('[Pano] 全景球体渲染失败', error)`；同文件 `:30` 已 import logger 且 `:357/:436/:440` 均走 logger —— 唯独崩溃路径绕过唯一日志出口，不落 `/api/logs` | 增债 | 结构债 | 全景球体渲染崩溃在系统日志不可见 | 低 | TD-07-1 / TASK-056 同母体（观测层唯一出口被绕过） | `console.error` 改 `logger.error('panoNode', '全景球体渲染失败', …)`（1 行，与同文件其余日志同形）；还债时顺带核对全部节点级 componentDidCatch 是否同族 | 待还 |
| TD-07-7 | `director3d/project.ts:208` `LEGACY_PROJECT_STORAGE_KEY = 'stageframe-project'`（`:1279` readJson 迁移读、`:1285` removeKey）—— 旧键未登记 `STORAGE_KEYS`（`contracts.ts` director3d 段仅 3 键），违反登记规则 4「迁移键须 migration 登记」；且读取经 `director3d/storage.ts:23` 裸 `localStorage.getItem` → `check-storage-keys` 闸（只扫 content/s/storage/kv 调用字面量）对此**结构性失明**（TD-02-33 同盲区第二实例） | 增债 | 结构债 | 登记表完整性 1 键；迁移行为无档可查 | 低 | 登记表之外的事实键 + 闸盲区（TD-02-33 同母体） | `contracts.ts` director3d 段补登记：`'stageframe-project'`（backend: native，`migration: 'director3d-project'`，note 注明仅旧版迁移读）；或在 02 区裸 localStorage 闸收口轮统一处理 | 待还 |

## 六 · 非债留痕（判非债必须留痕，防下轮重审）

1. **`videoEditor/lib/logger.ts` 非第二日志真相**：文件头 4-12 行自证「纯转发适配，实现 100% 落在 base/core/logger，category 固定 videoEditor」——是收口形态本身，非绕行。非债。
2. **`accountsStore.ts:381-450` 遍历 localStorage 非未登记键**：经 `chrome.scripting` MAIN world 注入读写**目标站点页面**的 localStorage（账号环境快照切换），不属本应用存储域；02 区六轮已裁定同族非债（「accountsStore 注入 MAIN world 操作目标站点存储」）。非债。
3. **`CANVAS_SYNC_CHANNEL` 双 BroadcastChannel 实例非串扰**：`canvasSyncBus.ts:45` 每次广播临时建/即关（唯一发送方 `broadcastCanvasSaved`），`useCanvasSync.ts:55` 常驻监听且有 `type === 'CANVAS_SAVED'` + projectId + tabId 三重过滤（`:57-61`）。一发送方一监听方，通道名同源常量。非债。
4. **`agent_draft` 墓碑**：键已退役（TD-17/2026-09-11，草稿唯一真源 = `conv.draft`），`cloudSync.ts:913-915` 保留退役注释属有意 tombstone；`contracts.ts:500` 注释轻微滞后但真源说明在 cloudSync。非债（不登记）。
5. **console 直写其余 5 处非债**：`useConnectedInputs.ts:466-469`（P2-G dev 警示，覆盖性已由 `uncoveredOutputNodeTypes()` 单测 + 闸接管，02 区五轮收口）· `backendLogStream.ts`（后端日志镜像，刻意不走 logger 防上报递归，文件头 10-13 行）· `logger.ts` 本体 · `director3d/log.ts:43`（debug 门控，TD-07-1 裁定保留）· `modalLayer.ts:135` + `director3d/project.ts:505`（dev 诊断，所在文件域 18 区/07 区已深审非债，无新证据不翻案）。均非债。
6. **「转写域死代码」证伪（本轮最重要的教训留痕）**：`find-dead --strict` 曾报 `engine/services/transcription/service.ts` 为僵尸、refs ① 报 0 模块引用——**均因 `mv-sync-refs.mjs:154` `DEFAULT_ALIASES = [['@/','src/']]` 缺 `@videoEditor` 映射**，别名导入对 refs/find-dead 不可见（TD-22-53 在 check-arch 修过同类洞，refs 工具未同步）。实况：`captions.tsx:28` 活引用 `transcriptionService`，转写域是活代码（knip 6.35.1 全绿佐证）。**2026-09-16 用户删除全部别名导入，触发条件归零**（正式留痕：[22-视频-剪辑器-别名收口·2026-09-16](./22-视频-剪辑器-别名收口-2026-09-16.md)） → 工具债不登记、不当场修（无可证实的错误输出可产）；留痕：未来若重新引入别名导入，须 `--alias '@videoEditor/:/src/components/videoEditor/'` 显式扩展，否则 refs/find-dead 对该域静默失真。铁律实证：**任何描述只是线索，refs ① = 0 ≠ 死代码**。
7. **面扫零命中的定性**：`sessionStorage` 0 命中 · 裸 `fetch('http…')` 字面量 0 命中 · `document.cookie` 0 命中 · `dispatchEvent` 仅存于 contracts/playback-manager 注释（TD-22-23 已收口）· `yimao_*` 裸字面量 18 处全部对上 STORAGE_KEYS（`check-storage-keys` 闸在册）· IndexedDB 9 处全部集中在 videoEditor 存储服务（假陈述面已由 TD-02-32 覆盖，不另立债）。

## 七 · 工具维护（本流程唯一动手项）

- `check-dead-code.mjs --update-baseline`：22 区多轮清偿后基线陈旧（34 条中 27 条已消失，闸仅提示不拦截）。按该闸申诉口 Q2①「清偿后重生成（正途）」收缩基线 34→7，闸守卫确认 0 新增，不需 `--allow-new`。执行记录见下方执行块。

## 八 · 结论 + 覆盖度表

**本轮新登记 3 笔**（TD-22-57 业务债待拍板 / TD-04-29 / TD-07-7），非债留痕 7 条，工具维护 1 项。**本轮零代码改动**（src/localTool 未动；scripts 仅基线收缩）。

### 覆盖度表（增量，仅本轮状态变化行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(文件:行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `videoEditor/engine/services/transcription/{service,worker}.ts` | engine 服务 | 抽审（欠深审） | ⚪ | 横推 09-16 | worker.ts:1-5；service.ts:19-50；无 remoteHost 覆写 | TD-22-57 |
| `videoEditor/constants/transcription-constants.ts` | 契约常量 | 抽审 | ⚪ | 横推 09-16 | :21-57 huggingFaceId 表 | TD-22-57 |
| `videoEditor/ui/editor/panels/assets/views/captions.tsx` | UI 面板 | 抽审 | ⚪ | 横推 09-16 | :28/:96/:104 活调用链 | TD-22-57 |
| `videoEditor/engine/lib/transcription/caption.ts` | 纯函数 | 未审 | ⚪ | — | 仅 refs 确认被 captions.tsx 引用 | 下轮随 22 区字幕深审 |
| `nodes/PanoramaNode.tsx` | 节点本体 | 抽审（定向） | 🔴 | 横推 09-16 | :30/:223-225/:357/:436/:440 | TD-04-29 |
| `director3d/project.ts` | 领域真源 | 抽审（定向） | 🔴 | 横推 09-16 | :208/:1279/:1285 | TD-07-7 |
| `director3d/storage.ts` | 持久化封装 | 深审（定向） | 🟢 | 横推 09-16 | 全文 70 行；键分流 isProjectPersistenceKey；3 键均在册（legacy 键除外→TD-07-7） | 封装本体非债 |
| `videoEditor/lib/logger.ts` | 观测适配 | 深审（定向） | 🟢 | 横推 09-16 | 文件头 4-12 | 非债（转发壳） |
| `base/store/accountsStore.ts`（localStorage 段） | store | 抽审（定向） | 🟢 | 横推 09-16 | :381-450 MAIN world 注入 | 非债（已裁定领域） |
| `hooks/useCanvasSync.ts` + `core/canvasSyncBus.ts` | 同步通道 | 抽审（定向） | 🟢 | 横推 09-16 | bus:45 / sync:55-61 | 非债（一收一发+过滤） |
| `scripts/mv-sync-refs.mjs`（aliasTable） | 工具 | 抽审（定向） | 🟢 | 横推 09-16 | :154 DEFAULT_ALIASES；:170-190 resolveSpec | 别名触发条件已随用户删别名归零（§六.6） |
| `scripts/dead-code-baseline.json` | 工具基线 | 已维护 | 🟢 | 横推 09-16 | 34→7 收缩 | 陈旧项已清 |

### 覆盖账

本轮横切面扫了 **6 个面**、定向取证 **12 个文件**、登记 **3 笔**、非债留痕 **7 条**；上表外无「未审」隐瞒项——`caption.ts` 1 处未深审已列名（下轮随 22 区字幕域深审），其余扫描面（setInterval 9 处 / EVENTS 配对 / apiRegistry）由既有闸在册覆盖。
