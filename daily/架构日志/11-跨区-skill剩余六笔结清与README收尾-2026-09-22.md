# 架构日志 · 11 区（agent/skill 剩余六笔 + README 收尾） · 2026-09-22

> **触发**：用户「把 skill 的债务完结，然后把 skill 里 readme 的收尾工作做完」。
> **对象**：`src/components/agent/skill/**`（含 `README.md`）· `docs/plan/140` · `docs/plan/141` · `AgentPanel.tsx`。
> **范围**：TD-11-72 / 73 / 74 / 75 / 76 / 77 六笔（**均低息、均"声明面或视图判据未收口"**）+ 模块 README 收尾
> ＋ 复核 **TD-11-63**（取证后判"现象已过期、工作已完成" ⇒ 一并结清，见 §五）。
> **判据源**：SOP Step 3 第 8 条（判据适用面）· `ADR-0044 §8`（导出面按用例）· `ADR-0049`（测试只锁用户可感知契约）·
> `ADR-0053`（不许造→判→删前四关）· 本仓「一归 / 一诚实 / 零炸」。
> **改动轮**：`src/**` 5 个文件 + `tests/**` 2 个文件 + 文档 3 份。**未跑全量测试**（跑 20 个相关文件 299 例 + `选文件` 组 7 例 + 四闸）。
> ⚠️ **读数时间戳**：本轮全部读数取自 **2026-09-22 19:30–20:01**（工作区 `git status` 干净，HEAD = `b27c5cf8`）。

## 一、链路快照（拓扑未变，只动视图层与声明面）

```
磁盘 SKILL.md ──skillApi──▶ skillHydrate ──▶ skillRepository（键唯一读写口）
                                                    │
                                        skillRegistry.listAllSkills（缓存 ∪ 内置）
                                        ├─ buildSkillLibraryView（设置页：行/组判定）  ← enabledMap 折入（必填）
                                        └─ buildSkillPickerGroups（面板下拉：选得动的那些）
                                                    │
                        选中只存 id ──▶ skillInject.freezeSkillTurn(ids) ──▶ 模型
```

本轮**未动**：存储键、frontmatter、门面导出面、磁盘布局、注入链路。

## 二、溯源（Phase 1：症状 vs 病根）

六笔的**共同病根只有一个**：**改了代码实例，没改"同一条判据的其它份"**（SOP Step 3 第 8 条命中项）。
按"那份住在哪"分三类：

| 类 | 笔 | 那一份住在哪 |
| --- | --- | --- |
| 声明面 | 72 · 73 · 74 · 76 | 头注释 / 用例编号 / 组语义头声明 / 方案文件规格块 |
| 类型面 | 75 | 函数签名（必填逼编值 · 缺省即危险值） |
| 代码面 | 77 | 两个函数各定义一条"什么可读"的口径，彼此没对账 |

## 三、定海（Phase 2：唯一真相源）

- **用例编号**的唯一真源 = **门面 `index.ts` 头注释**（它**就是**导出面本身；ADR-0044 §8 + `plan/140 §2.3`）。
  ⇒ `README §2` 与 `plan/140 §2.2` **只写指针**。
- **组序位次**的唯一真源 = `skillGroup.orderOfGroup`（视图层只"按位次排"）。
- **启用态**的唯一真源 = `agent_skill_enabled` 键（`skillRepository.isSkillEnabledIn`）；
  `skillStore.isSkillEnabled(id) ≡ isSkillEnabledIn(readSkillEnabledMap(), id)`（`:87` 实证，**同一真源**）。
- **"能不能选"** = `usable`（结构）× `enabled`（开关），**只判一处**（`buildSkillPickerGroups`）。

## 四、切割（Phase 3：真缝隙）

- `enabledMap` 的三种给法**逐条判过**（用户要求"自己判、不照抄债里的解法"）：
  | 给法 | 类型层能否漏传 | 是否编值 | 结论 |
  | --- | --- | --- | --- |
  | 必填 + picker 传 `{}` | 不能 ✅ | **有**（类型层说是启用态，实给占位符）❌ | 原状，否 |
  | 可选（缺省=不判） | **能** ❌ | 无 | **否**：漏传 ⇒ 静默"全部启用"，与刚删掉的 `state='ok'` 默认值**同形** |
  | **必填 + 传真值** | 不能 ✅ | 无 ✅ | **做** |
- ⇒ `buildSkillPickerGroups` 收**全集 + 真启用态**，"能不能选"两半合到一处；面板删预过滤。
  **不动**视图层的启用态折入 —— `plan/142 §A8` 已裁定「`row.enabled` / `group.toggleIds` 由视图层给，组件不另判」。

## 五、探债（Phase 4：本轮结清七笔，无新债）

| TD-ID | 问题点 | 处置（一句话） | 状态 |
| --- | --- | --- | --- |
| TD-11-72 | 门面头写"五类"却列 ①–⑦；"其余用例那一期加入"而导出已在本文件 | 改七类；删过时句；补 `buildSkillPickerGroups` 与两个机制性导出的如实留痕 | 已解决 |
| TD-11-73 | 用例编号三处互不一致（README §2 还把两个文件都标 ①） | README §2 **去掉编号**只留职责 + 指针；`plan/140 §2.2` 标 P0 快照 + 指针 | 已解决 |
| TD-11-74 | `skillGroup.ts` 头自称"排序"唯一家，但官方/仅索引两条位次住在视图文件 | 新增 `orderOfGroup`（位次唯一家），`compareSkillGroups` 用它；视图只"按位次排" | 已解决 |
| TD-11-75 | `enabledMap` 必填逼编值 + `rowFromIndex` 的 `state` 默认 `'ok'` | `state` 去默认值（调用点显式传）；`enabledMap` 回**必填**且要求真值；picker 收真值并一处判"选得动" | 已解决 |
| TD-11-76 | `plan/140 §3.2` 把 5 个函数挂在已删的 `skillCatalog.ts` 名下 | §3 章首加统一指针（覆盖 §3.1–§3.4）+ §3.2 就地写清 5 个函数现在住哪 | 已解决 |
| TD-11-77 | 提取口径要求含 `/`、安全口径允许裸文件名 ⇒ 包根资料读不到且**报错说谎** | **保留**提取口径（`plan/140 §1.4` 明定附属资料 = `references/**`，且有锁定测试），改**报错说清是哪条口径拒的** | 已解决 |
| **TD-11-63** | 面板 `.md` 导入路径不可达（上传 UI 被注释）+ 缺组件测试 | **取证判「现象已过期、工作已完成」**：触发点已接回（`AgentPanel:1969-1993` 活代码）+ 同批组件测试已在（`AgentPanel.test.tsx:849` 起，四条判据） | 已解决 |

> ⚠️ **本轮我自己推翻过一条结论（留痕）**：写 §五 初稿时我把 TD-11-63 判成"仍未做、交用户拍板"
> —— 那是**照抄账本的现象**（SOP 附 A1/A2：能查的一律不许靠描述）。取证后发现现象已过期
> （UI 是活代码、测试已在），**已回改 README §5 与本文件**。凭证：`npx vitest run tests/unit/AgentPanel.test.tsx -t 选文件` → **7 passed**。

### 五·补 · README §5 尾巴四条对账（用户追问「这些都收好了吗？」）

**不能"看着像收好了"** —— 逐条取证（SOP 附 A1/A2：能查的一律不许靠描述）。**两条真、两条过期**：

| # | README §5 的原话 | 取证结论 | 处置 |
| --- | --- | --- | --- |
| 1 | 内置 skill 已迁进本模块；**`skillStore.getBuiltinSkills` 现为纯转发，消费方零改动** | ❌ **过期**：壳的导出面只剩 `Skill` / `findSkill` / `isSkillEnabled`（`getBuiltinSkills` **不在壳里**）；消费者（`skillRegistry:78` · `SkillSettings:351` · `skillStore.test:37`）一律从**门面**取 | 回改 README（改为"壳里已没有它"+ 导出面实况） |
| 2 | `BuiltinSkill` / `RuntimeSkill` / `isUserSkill` 仍删除、仍无消费者 | ✅ **真**：全仓只在注释里出现（`skillTypes:6` 的"曾经"）⇒ 零活代码消费者 | 保留（补上 `AllSkill` 也属现行形状） |
| 3 | 导入只收文本；二进制走 base64 通道（不提前预留） | ✅ **真**：`isImportablePackageFile` 只放行 `TEXT_RESOURCE_EXT`；模块内无 base64 通道（`SkillPackageFile.encoding` 的 `base64` 是**读侧**形状，不是导入通道） | 保留（把这点写清，防误读） |
| 4 | **UI 仍用 legacy `Skill` 形状**（住转发壳里） | 🟡 **半真**：面板**不 import 那个类型**，但 `findSkill()` 的返回类型是它（`AgentPanel:439` 只读 `.name`）⇒ 形状经返回值流动 | 精确化措辞（否则读者会去找一个不存在的 import） |

**顺带横扫出的第三处漂移（同一条判据的另一份）**：
`skillTypes.ts:5` 仍写「内置 skill **目前仍住** `agent/runtime/skillStore.ts`」—— 与 §5 第 1 条同一事实、
同一时刻过期 ⇒ 一并回改（并补出模块内现行的三个形状：`BuiltinSkillDef` / `UserSkill` / `AllSkill`）。

**另收掉两处代码里的旧用例编号**（TD-11-73 的同一条判据在代码面的副本）：
`model/skillResource.ts:2`（曾标"用例⑤"）· `model/skillInject.ts:2`（曾标"用例①"）—— 都改成**只写用例名**；
`index.ts:77` 的 `②/④` 也去掉。**口径升级**：README 不变量 25 从"README/方案只写指针"扩为
**"任何非门面处都不写编号"**（名字自解释、编号要查，且抄一份必漂移）。
**凭证**：`src/**` 内 `用例[①-⑦]` 命中 = **0 处**。

**新发现的遗留（已记入 README §5，未登记新债）**：`runtime/skillStore.ts` 转发壳**本身待退役**
（消费者只剩 `AgentPanel` 的两个调用点 + 两个订阅键，门面全有）—— `docs/plan/142 §10` 已挂"排 B 批后删"，
属**已排期**项 ⇒ 不重复登记（登记 = 第二份进度真相）。

---

## 六、覆盖度表（只登记本轮变化行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `skill/index.ts` | 门面 | 深审 | 🟢 | 11 / 2026-09-22 | 头注释 vs 导出面逐条对齐 | TD-11-72 结清 |
| `skill/rules/skillGroup.ts` | 纯规则 | 深审 | 🟢 | 11 / 2026-09-22 | `orderOfGroup` + 5 例单测 | TD-11-74 结清 |
| `skill/view/skillLibraryView.ts` | 视图 | 深审 | 🟢 | 11 / 2026-09-22 | `state` 无默认值；组序走 `compareSkillGroups` | TD-11-74/75 结清 |
| `skill/view/skillPickerView.ts` | 视图 | 深审 | 🟢 | 11 / 2026-09-22 | `usable ∧ enabled` 一处判；两参必填 | TD-11-75 结清 |
| `skill/model/skillResource.ts` | 给模型 | 深审 | 🟢 | 11 / 2026-09-22 | 报错分两种原因；探针 sha=0a47c989462e | TD-11-77 结清 |
| `skill/rules/skillResourcePath.ts` | 纯规则 | 深审 | 🟢 | 11 / 2026-09-22 | 两函数射程在头注释写清 | TD-11-77 结清 |
| `skill/README.md` | 文档 | 深审（全读） | 🟢 | 11 / 2026-09-22 | §2 去编号 / 不变量 4 补口径 / 新增 24–26 / §5 对账 | TD-11-73 结清 |
| `docs/plan/140` | 文档 | 深审（§2/§3） | 🟢 | 11 / 2026-09-22 | 删重复节标题；§2.2/§3/§3.2 补指针 | TD-11-73/76 结清 |
| `docs/plan/141` | 文档 | 抽审（§9） | 🟢 | 11 / 2026-09-22 | picker 新签名同步 | 顺带 |
| `agent/panels/AgentPanel.tsx` | 消费方 | 深审（skill 段） | 🟢 | 11 / 2026-09-22 | 删预过滤；`allSkills`→`enabledSkills`；39 例绿 | TD-11-75 结清 |

**覆盖账**：本区 24 个源文件 + 1 份 README / 本轮深审 6 个源文件 + README + 2 份方案 / 未审 18 个
（理由：本轮六笔只落在视图层与声明面，其余文件本轮 mtime 未变、上一轮已深审）。

## 七、结论与下一步

- **本区域状态**：`已验证(无债)`（本轮结清七笔：TD-11-72~77 + TD-11-63；11 区主表待还 = 0）。
- **待还债项已流入 `daily/架构日志/债务.md`**：否（11 区已无待还）。
- **DATAFLOW.md**：本轮拓扑未变 ⇒ 无需改（未追加叙事段）。
- **灯**：本区覆盖度表已更新；区域级灯由 `index.md` 自动派生（本轮后应为 🟢）。
- **下一步**：无待办。若要把 TD-11-77 的**另一条路**（放宽提取口径，让包根资料也可读）作为**能力新增**
  落地，须单独立项 —— 它会让"用户能做的事变多"，归 Step 5 摆选项。

---

## 八、交付闸自查

| 闸 | 本轮结论（≤1 行） | 可复现凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | 用例编号 3 份 → 1 份（门面）· 组序位次 2 处 → 1 处 · "能不能选" 2 层 → 1 处 | `grep -n "用例编号的唯一真源" src/components/agent/skill/README.md src/components/agent/skill/index.ts` |
| 减一 · 兜底净减 | 无（本轮不减逻辑分支；**净减的是"编值/默认值"两处隐患**） | 见 §九 决策 1 |
| 减二 · 复杂度实减 | 判据份数：编号 3→1 · 位次 2→1 · 选得动 2→1；`AgentPanel` 少一个预过滤 + 少一个中间量（`allSkills`→`enabledSkills` 仅服务块①） | 上表 + §六 |
| 一诚实 · 失败诚实可见 | TD-11-77：拒绝时**说清是哪条口径拒的**（旧：包根资料被报成"正文没引用"= 原因说谎） | 探针结论块（sha=0a47c989462e · exit=1 · 命中「1 failed」） |
| 零炸 · 防回归防副作用 | 改前能走的仍绿：20 文件 **299 例全绿** + `AgentPanel` 39 例；改前会失败的仍失败（未动失败路径） | 见下"机器凭证" |
| 一凭证 · 证据可复现 | 即上面五条凭证本身 | 下列命令输出 |

**机器凭证（本轮实跑）**：

- `npm run type-check` → **退出 0、无输出（0 错）**
- `npm run lint` → **无输出（0 问题）**
- `npm run check:arch` → **✅ 架构校验通过**（跨栈对账 3 项 · 入口名闸 · 禁 exec · 组哨兵闸）
- `npm run check:dead-code` → **67/67，无新增**
- `npx vitest run <20 个 skill 相关文件>` → **Test Files 20 passed · Tests 299 passed**
- `npx vitest run tests/unit/AgentPanel.test.tsx -t 选文件` → **7 passed**（TD-11-63 的四条判据）
- 探针 3 条（全部"命中"）：TD-11-77 `sha=0a47c989462e` · TD-11-75 `sha=41030457a68f` · TD-11-74 负例 `sha=de08113effc0`

**零炸三问**：

1. **哪些行为被改了？** ① 面板下拉的准入判据从"面板预过滤 `enabled` + picker 判 `usable`"改为
   "picker 一处判 `usable ∧ enabled`"（**输出不变**，见下）；② `buildSkillPickerGroups` /
   `buildSkillLibraryView` 的签名（编译期约束收紧）；③ TD-11-77 的**拒绝话术**（仅文案）。
2. **改前能走的路径，改后仍能走吗？** 是 —— 20 文件 299 例全绿（含 `cloudSync` 51 例、`AgentPanel` 39 例）。
3. **改前会失败的，改后仍失败吗？** 是 —— 未触碰任何失败路径；且**没顺手改好**任何东西
   （TD-11-77 的"放宽提取"能多读到包根资料 = 能力新增，**故意不做**，交用户定）。

**用户可感知的行为变化**：**无**（下拉内容集合与顺序均不变；唯一变化是读包根资料时的**报错文案**更准确）。
**性能**：面板下拉现在对**全集**建视图（含被关掉的技能），N = 技能条数（个位数~几十）⇒ 可忽略。

---

## 九、决策留痕

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度（→ §八 减二） | SSOT（→ §八 一归） | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `enabledMap` **必填且必须真值**；picker 收全集 + 真映射，"能不能选"一处判 | "不判"是**结构事实**，不能靠"传空/省略"表达；`{}` 编值、可选参数=危险默认值 | 面板只**转发**真值；判据留在视图层（`plan/142 §A8`：组件不另判） | 判据 2 层 → 1 层；面板少一预过滤 | `isSkillEnabledIn` 仍唯一（`skillStore.isSkillEnabled` 与它同源，`:87`） | **做** |
| 2 | TD-11-77 **保留**"提取只收带目录引用"的口径，只改报错 | 该口径有 `plan/140 §1.4` 依据 + 锁定测试（防把代码片段当引用）；"放宽"= 能力新增 | 安全面/提取面**射程不同**，头注释写清；拒绝必须说清哪条拒的 | 不动提取逻辑（改动半径最小） | 口径落一处（提取侧），README 不变量 4 同口径 | **做**（放宽那条路交用户） |
| 3 | TD-11-63：判"仍未做、交用户拍板" | ❌ **这条我判错了，已推翻**（见 §五 末注）—— 取证发现触发点已接回、组件测试已在 ⇒ 无需定权 | — | — | — | **撤回**（改为：**取证后结清**） |
| 4 | 用例编号：README/方案**只写指针**，不各抄一份 | 编号是导出面分类，抄了必漂移（已实证） | 门面是导出面本体 ⇒ 编号住它 | 3 份 → 1 份 | 一归 | **做** |

**存在性凭证**：

```bash
grep -n "## 九、决策留痕" daily/架构日志/11-跨区-skill剩余六笔结清与README收尾-2026-09-22.md
```
