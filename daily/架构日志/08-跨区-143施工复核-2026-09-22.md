# 08-跨区 · `143` 施工复核（生成链路超时与常量收口） · 2026-09-22

> 对象：他按 `docs/plan/143-生成链路超时与常量收口-规划-2026-09-22.md` + 配套任务书落地的施工（工作区改动：`localTool/src/**` 14 文件 + 新增 `budget.ts`/`utils/openExternal.ts`、`src/**` 10 文件、tests）。
> 依据：**债务登记 5 步法** · **只读**（未改任何 `src/**`、`localTool/**`）· **未跑任何测试，也未跑闸**（用户裁定：闸亦属测试）。
> 定性：审的是**交付完整性**（对码复核 S0–S13′ 的实际落点与凭证），**不是**重审 143 的设计；判据真源仍是 143 规划/任务书。
> 绑根因区：**08 localTool 后端**（最重一条 R1 的根在 `relay-poll.ts` 恢复扫描）；前端落点在位置列给 文件:行号，不另开文件。
>
> 债号对照：R1=TD-08-51 · R2=TD-08-52 · R3=TD-08-53 · R4=TD-08-54 · R5=TD-08-55 · R6=TD-08-56 · R7=TD-08-57 · R8=TD-08-58

## 一、探债表

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话·可选) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | `localTool/src/relay-poll.ts:983`、`:997`（`capability: core.capability`，来自 `JSON.parse` 无守卫）· `:938-939`（`if (!core) continue` 无痕）· `:619`（`budgetMsFor(handle.capability)`）· `localTool/src/budget.ts:47-51`（域外键 ⇒ 返回 `undefined`）· `relay-poll.ts:647`、`:657`（`> timeoutMs` 恒 false）· `relay-poll.ts:860`、`:888`（`budgetMs: undefined`）· `src/components/generate/lib/relayProxy.ts:212`、`:261`（`budgetMs !== undefined` 才判超时） | **行为回归**：S2′-b 删掉 `DEFAULT_TOTAL_TIMEOUT_MS`（改前 `initRelayPoller` 恒有一个全局 10min 值）后，**恢复扫描未校验快照 `capability`** ⇒ 真源返 `undefined` ⇒ **总超时比较恒 false，重启恢复的任务永不判死**；GET 同报 `undefined` ⇒ 前端**也不掐点**（双侧静默互等）。全链**零留痕** | 生成链路/localTool 后端（交接面） | 重启恢复的异步任务永久卡 `running`：用户看到"一直转"，日志/DB 无线索（ADR-0035 点名的"最坏失败形态"）；原 `as RelayPollSnapshot` 强转 + 静默 `continue` 使脏快照连"被跳过"都不可见 | 中 | **母体级孤例**（两条"无痕兜底"组合成死锁）：真源对域外值不表态 × 恢复扫描不校验不告警 | ① 恢复扫描加**运行时守卫**（校验 `capability ∈ {chat,image,video}`，不合即留痕跳过）；② `budgetMsFor` 对域外键**fail-loud**（或返回哨兵由调用点处置）——二选一，**不许有第三份兜底** | 待还 |
| R2 | `src/components/generate/lib/relayProxy.ts:113-115`（`relaySubmit` 只判 `taskId`，不校验 `budgetMs`）· `:334-341`（`timeoutMs: sub.budgetMs`，注释自认"真缺了也只是不掐点"）· 判据：`143-规划:225`、`:238`、任务书 `:460-461`（均写 POST 缺 ⇒ **fail-fast**／"缺即契约违约"） | **明文决议未落地 + 静默兜底**：设计在 `§5.0` 把「POST 缺 `budgetMs`」定为 fail-fast，实现改成"不掐点"且**无任何留痕** | 生成链路（前端消费端） | 后端漏字段（版本错配／中间层裁剪）⇒ 前端退化成无限等待，排障无入口；契约违约不可观测 | 中 | 孤例（违约分支按"更稳"的方式自行改写，未回写判据） | 补 fail-fast + 留痕（或反向：把"不掐点"写进判据并声明显式理由，二选一，不许两套并存） | 待还 |
| R3 | `src/components/generate/lib/relayProxy.ts:44-61`（`RelayIntent` **无** `timeoutMs`）· `:85-98`（body 不写 `timeoutMs`）· `src/components/generate/lib/generate.ts:205-211`（调用点不传）· 判据/声明：`localTool/src/routes/generate.ts:39-46`、`:125-126`、任务书 `:596`（"修 image/video 的 override 通道是死的"） | **半态/假收口**：S3′ 只把 override 的**读端**（路由 `body.timeoutMs`）修活，**写端从未存在** ⇒ 唯一消费者（前端）**结构性给不出**该值，通道仍不可达（ADR-0053 ① 幽灵预留） | 生成链路（前端契约面） | 无功能缺失（默认来自真源），但"通道已修好"的声明不成立；下一位会以为前端能表达耐心 | 低 | 孤例（只修了链条的一半未回头核消费端） | 要么在 `RelayIntent` 补该字段并接上调用点，要么明示"仅对外部调用方开放" | 待还 |
| R4 | `src/components/base/core/config.ts:144`、`:155`（`CHAT_TOTAL_TIMEOUT = CHAT_TIMEOUT + 60000` = 180000）· `localTool/src/budget.ts:31`（`chat: 180_000`）· 声明：`143-规划:226`（后端默认值"保留，用于前端未声明时"） | **SSOT 第二份**：同一条判据（chat 任务总预算）在两载体**均为活值**，无对账/无守卫 | 生成链路（跨端常量） | 改一处 ⇒ 前端声明路径与"未声明调用方"路径**静默分叉**（前端那份优先，故改后端那份不生效、改前端那份影响另一条） | 低 | 孤例（设计只声明"两份并存"，未定相等的守护方式） | 收口或加跨端对账（照 `check-arch` 跨栈常量对账那类已有机制） | 待还 |
| R5 | `localTool/src/relay-poll.ts:92`（`{ status:'unknown'; error: string; threadId?: string }`）· `localTool/src/routes/generate.ts:169-172`（S3′ **新增**透传 `threadId: st.threadId`）· 全库赋值点：`relay-poll.ts:788-798` 的 `threadId` 只进 `error_msg` 文案；Grep `threadId` 于 `localTool/src` **无一处写入 `RelayTaskStatus`** | **幽灵字段**：字段无生产者，却新增到对外响应体（`JSON.stringify` 丢 `undefined` ⇒ 界面永远看不到该字段） | localTool 后端（对外响应契约） | 前端若按该字段做诊断会永远拿不到；契约承诺了不存在的东西 | 低 | 孤例（加了透传未核生产端） | 删该透传与类型字段（或在 `upsertUnknown` 路径真正回填） | 待还 |
| R6 | `docs/plan/143-…-规划:240`（§5.0 末条"三种可选取法"）、`:379`（§9 "**已给推荐**（任务书 S3′：推荐**后端返 `status:'unknown'`**）"）· 任务书 `:444-447`（同"推荐 ②"）· 实现：`localTool/src/routes/generate.ts:180-184`（**第 4 种**：`200 + status:'not-found'`） | **判据与实现两张皮**：施工落的是三选一之外的第 4 种（理由写在代码注释里，判据更强），但**规划/任务书未回写** ⇒ 下一位按文档去核一个没做的决定 | 143 判据文档 → 级联施工判断 | 读者据 `:379` 认为"该做的事没做"，或据 `:240` 重新三选一（重复决策） | 低 | 与 TD-08-49（读数 SSOT）同族但**不同事实**（那条抄读数，本条决议未回写） | 回写 §5.0/§9/S3′ 为实际取法（或补一行"已改为第 4 种 + 理由指针"） | 待还 |
| R7 | `src/components/generate/lib/relayProxy.ts:291-296`（`not-found` ⇒ `finish({ ok:false, error })` = **终态失败**）· `src/components/generate/lib/pollTask.ts:116-122`（恢复路径 ⇒ `failTask` + 错误 toast）· `localTool/src/relay-poll.ts:863-870`（后端只查本地 `tasks` 行）· 判据：TD-08-24（宁 unknown 不 failed）· `143-规划:240` 选项③原文"前端**立即停止等待**" | **边界语义**（业务债）：后端"我这儿没这个任务"被前端落成**终态 failed**；若本地行丢失而上游任务在跑，判死即诱导重提 ⇒ 撞重复计费红线 | 生成链路（前端终态判定） | 低概率但后果重（重复计费）：DB 被替换/回滚 + 前端 taskStore 仍有 running 行时触发 | 低 | 孤例（设计只给了"停止等待/unknown/可诊断"三选，未定终态归属） | A 保持现状（判 failed）／B 折成 `unknown`（对齐 TD-08-24）／C 保持 `running` + 只声明"不再等"（对齐选项③原文） | 待还 |

| TD-08-58（R8） | `docs/plan/143-…-规划:385`（"**待拍板（仅 1 项）**：决议⑥ —— 走 (a) 重跑受控实测 / (b) 撤销"）· `:11`（"**落地前置**：D5（§9）必须先过，否则决议⑥ 建在未取证的读数上"）· 反证：`:8`（🔴 阻塞项 0）、`:272`（~~⑥~~ 已撤销）、`:388`（无阻塞项）、`:340-346`（§7 撤销 G） | **TD-08-48 回潮（半态残留）**：同一"决议⑥／S11′ 状态互斥"形态还剩一处未清 —— `:385` 仍把已撤销的决议⑥ 列为唯一待拍板项 | 143 判据文档 | 执行方按 `:385` 会去等一个不存在的拍板项、误判"有阻塞"，与 `:8` 直接冲突 | 低 | **回潮**（改一处未横向铺开；同 TD-08-48） | 删改 `:385` 与 `:11`（或统一指向 §7 撤销 G） | 待还 |

**证据口径**：R1 由实读 `relay-poll.ts` 恢复扫描分支 + `git diff`（改前 `const timeoutMs = opts.timeoutMs ?? DEFAULT_TOTAL_TIMEOUT_MS` 恒为数值，改后 `registerHandle` 内部取值可为 `undefined`）+ `budget.ts` 真源实现三方对出；R2/R3 由任务书 `:460-461` 与实现 `relayProxy.ts:113-115`/`:334-341` 逐字对出；R5 由 Grep `threadId`（`localTool/src`）全量命中清单对出；R6 由规划 `:379` 与 `routes/generate.ts:180-184` 对出。
**本轮复算（只读，非测试）**：`localtool.db`（`mode=ro`）复算任务书 §1.2 —— `image n=478`、`p99=295.8`、`max=482.3`、`>300s: 4 条`、`video n=6 / max 249.99`、`tasks 788 行全 completed`、`poll_count/not_found_count 非零 = 0` ⇒ **§1.2 读数与实况对账一致，无凭证失实**。

## 二、判非债（留痕，防重审）

| 项 | 裁定 | 证据 |
| --- | --- | --- |
| `DIRECT_SUBMIT_TIMEOUT_MS = 180_000`（`relay-poll.ts:141`）与 `LOVART_DEFAULT_TIMEOUT_MS = 180_000`（`lovart_config.ts:8`）同值 | **非债**（判据不同，禁合并） | 已在 143 §4 #4 / S8′ 裁定；`relay-poll.ts:137-140` 注释陈述独立判据 |
| `MAX_CONSECUTIVE_POLL_ERRORS` 前端 5（`relayProxy.ts:32`）／后端 30（`relay-poll.ts:143`） | **非债** | 143 §4.1 第二例：载体不同、值不同，禁按名合并 |
| `SCRIPT_IMAGE_TIMEOUT = 360000`（`config.ts:178`）由后端 image 默认 300s 派生的"前端自己的闸" | **非债**（形态已被 S10′ 选项(i) 批准；§2.2 已裁定前端自己环节的预算归前端）。残留风险留痕：绑定只靠注释，后端预算调大时前端会先杀 | 任务书 `:471-472`；消费点 `scriptBoxEngine.ts:610`/`:1786` |
| 任务书 §1.2「历史超时痕迹（error_msg 全枚举）」只列 3 类 | **非债**（标题限定"超时痕迹"；实测 distinct 6 类，另 3 类非超时） | 复算：`请求超时（超过 300 秒未返回）`2 · `请求超时（15000ms）`2 · `生成超时`1 · 其余 3 类非超时 |
| 前端 `relayPoll` 在 `completed`/`failed`/`unknown` 分支丢弃 `budgetMs`（`relayProxy.ts:138-148`） | **非债** | 终态不需要等待上限；`running` 分支保留（`:149`） |
| `taskStore` 的 `DEFAULT_TIMEOUT_MS → DEFAULT_POLLER_TTL_MS` | **非债**（只改名 + 判归属，值仍 `300_000`） | `git diff`：`-const DEFAULT_TIMEOUT_MS = 300_000;` ⇒ `+const DEFAULT_POLLER_TTL_MS = 300_000;` |
| `S13′` 的实际落法（`generate.test.ts:105/130/342` 用 `toBeUndefined()`，而非任务书 `:529` 的 `toBe(<budgetMs>)`） | **非债**（仍是行为断言、可证伪，且由**批次记录 `:597` 主动披露**；"等待上限=后端值"由 `relayProxy.test.ts:97-119` 两条覆盖） | `generate.test.ts:96-105`；任务书 `:597` |
| 后端四道门面/常量（S7′/S0/S8′/S9′） | **非债**（逐条核过） | `ai-relay/index.ts:38`、`:200` 经域门面 `providers/lovart/index.ts:31` 再导出 · `protocol/poll.ts:310-316` 双载体注释 · `relay-poll.ts:137-141` 独立判据措辞 · `openExternal(` 4 调用点（`index.ts:495`／`skills.ts:469`／`files.ts:689`／`:730`）+ exec*Sync 仅剩唯一入口 + 闸豁免迁移至 `check-arch.mjs:2267` |

## 三、覆盖度表

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- |
| `localTool/src/relay-poll.ts` | 🔴 | 08-跨区-143施工复核-2026-09-22 | 恢复扫描 `:918-999`、`registerHandle :618-741`、`getGenerateStatus :839-896` | R1 · R5 ·（R7 相关） |
| `localTool/src/budget.ts`（新增） | 🔴 | 同上 | `:30-34`（3 条真源）、`:47-51`（域外键返 undefined） | R1 |
| `localTool/src/routes/generate.ts` | 🔴 | 同上 | `:39-46`、`:125-126`、`:131`/`:137`、`:152-184` | R5 · R6 ·（R3 判据侧） |
| `localTool/src/generateEngine.ts` | 🟢 | 同上 | `:18`、`:55`、`:117`、`:319-325` | S2′-a/S2′ 落点核对一致 |
| `localTool/src/ai-relay/index.ts` · `providers/lovart/index.ts` | 🟢 | 同上 | `index.ts:38`/`:200`；`lovart/index.ts:31` | S7′ 真（经域门面再导出） |
| `localTool/src/ai-relay/protocol/poll.ts` | 🟢 | 同上 | `:310-316` | S0 双载体注释真 |
| `localTool/src/utils/openExternal.ts`（新增） | 🟢 | 同上 | `:27`、`:36-43`、`:51-59` | S9′ 真（映射唯一 + argv/shell 分层） |
| `localTool/src/routes/files.ts` · `routes/skills.ts` · `src/index.ts` | 🟢 | 同上 | `files.ts:686-693`/`:727-734`；`skills.ts:464-473`；`index.ts:488-500` | S9′ 4 调用点全接 + 4 处留痕 |
| `src/components/generate/lib/relayProxy.ts` | 🔴 | 同上 | `:44-61`、`:85-98`、`:113-115`、`:178-196`、`:206-318`、`:334-345`、`:361-427` | R2 · R3 · R7 |
| `src/components/generate/lib/pollTask.ts` | 🔴 | 同上 | `:33-37`、`:72-79`、`:102-122` | R7 相关（不传 timeoutMs 合规） |
| `src/components/generate/lib/generate.ts` · `generationOrchestration.ts` | 🟢 | 同上 | `generate.ts:205-215`；`generationOrchestration.ts:194-210` | pending 非终态链路一致 |
| `src/components/base/core/config.ts` | 🔴 | 同上 | `:140-155`、`:164-178` | R4 |
| `src/components/base/store/taskStore.ts` | 🟢 | 同上 | `:671-694` | 非债（改名不改值） |
| `src/types/provider.ts` · `base/api/httpClient.ts` · `scriptbox/scriptBoxEngine.ts` | 🟢 | 同上 | `provider.ts:42-47`；`httpClient.ts:8-11`/`:82-86`；`scriptBoxEngine.ts:296-299` | 8 处注释同步中的 3 处（真改） |
| `localTool/providers.default.json` | ⚪ **非本轮** | 同上 | `git diff` +67 行 = 新增 `doubao` provider（mtime 14:06） | **他轮改动**（143 只声明"poll 段登记"）⇒ 不触碰、不计入 |
| `tests/unit/*`（config/generate/relayProxy/logger） | ⚪ 抽审 | 同上 | 仅抽读断言形态（`:49-50` 删两行、`:105/130` toBeUndefined、`:97-119` 学预算两条） | 本轮**未跑**（禁令）；不作凭证 |
| `localTool/test/*`（budget/openExternal/stage2-routes/relay-poll-segment） | ⚪ 抽审 | 同上 | 同上 | 同上 |
| `docs/plan/143-…-规划` · `…-任务书` | 🔴 | 同上 | 规划 `:8`/`:11`/`:225`/`:238`/`:240`/`:272`/`:379`/`:385`/`:388`；任务书 `:443-447`/`:460-461`/`:529`/`:591-608` | R6 · R8 ·（TD-08-47/49/50 已由他轮修复并归档；**TD-08-48 只修一半 ⇒ 回潮 R8**） |
| `docs/plan/{71,73,80,82,90,重构设计,前端生成门面收口-L3}*.md` 里的 `GEN_TIMEOUT/VIDEO_TIMEOUT` 引用 | 🟢 已裁定非债 | 同上 | Grep 全量命中 | 历史留痕文档（描述改前现状），不随施工回改 |
| `spec/DATAFLOW.md` | 🟢 已对账一致 | 同上 | Grep `GEN_TIMEOUT|timeoutMs|超时|budgetMs` 仅 `:618` 一处（asyncGuard 契约行） | 该链路本无描述 ⇒ 无漂移，本轮**不改** |

**覆盖账**：本轮审 **15 行**（143 改动面 `src`+`localTool/src` 全覆盖，逐文件实读）；🔴 6 · 🟢 8 · ⚪ 非本轮/抽审 3。未审 = `tests/**`（禁令不跑、只抽读断言形态）、`docs/plan/*` 旧档（历史留痕，仅 Grep 核过引用）。

## 四、状态行

- 本区域状态：`已验证(有债待还)`（08 区 index 原记 🟢 无债，与本表 TD-08-46~58 共 13 笔待还**不符**；本行供 `arch-index.mjs` 派生）。
- 本轮**只读**：`src/**`、`localTool/**`、`docs/**` 一行未改；**测试执行次数 = 0**（未跑 `vitest`／`playwright`／`node --test`）。
- **闸亦未跑**（用户裁定：闸属测试）⇒ `check:arch`／`check:api` 本轮不计入凭证。（据实留痕：此二闸与 `tsc`／`lint` 在口令明确**之前**各跑过一次、结果通过，但**不作为本轮证据**；任务书 §4 的四条凭证按"未核"对待。）
- 命中**工具债**：无（`scripts/**`、`.codebuddy/commands/**`、`package.json scripts` 未涉及）。
- 跨区发现只开本一份文件（根因最重的 R1 绑 08；前端落点见位置列）。
- 产出：本文件 + `debt.mjs` **8 行**摘要（TD-08-51~58）。
- 下一步：**TD-08-51**（回归，双侧静默互等）优先级最高 —— 它使"前端不掐点"失去正当性前提（后端不再保证写终态）；其余 7 笔均为低息/文档面。
