# 架构日志 · 18 跨区 · TD-18-15 深拷贝唯一入口收口 · 2026-09-19

> **触发债**：TD-18-15（`nodeDataSchema.ts:120` 裸 `structuredClone` 拷业务对象，绕过唯一入口
> `core/utils.ts:131 deepClone` · ADR-0005 决议 1 / 判据 3）。
> **性质**：**纯结构收口轮**（行为等价，零运行期变化）· 走《架构师改码 7 步法》。
> **结论落点**：本文件（唯一写全处）。
> **一句话**：`deepClone` 的实现**就是** `structuredClone`，所以"裸调"在运行时**完全等价**——
> 它坏在**结构**：入口之外多一处实现 ⇒ 将来改拷贝策略时必漏。而现有 6 条用例**判别不出**这件事
> （探针实证：注回旧实现仍全绿）⇒ 必须补一条"断言它真的调了 `deepClone`"的锁。

- **本区域状态**：`已验证(有债待还)` —— 本轮 **TD-18-15 结清**。

---

## 一 · Step 0 普查（同一件事被实现成几份）

`grep -n structuredClone src`（**改前**，只看代码行、不含注释）：

| 站点 | 性质 |
| --- | --- |
| `base/core/utils.ts:131` | **唯一入口** `deepClone` 的实现体（合法） |
| `base/canvas/nodeDataSchema.ts:120` | **裸调**，拷贝业务对象 `NODE_DATA_DEFAULTS[type]`（＝本债） |
| `director3d/d3dPersistence.ts` | ADR-0005 决议 2 已收口（现只剩说明注释） |

**结论**：裸调用点 **1 处**。改后 `src` 内 `structuredClone` 的**代码**站点 = **1**（即 `deepClone` 内部）。

## 二 · Step 1 证伪（"描述"一律重新取证）

| 待验命题 | 取证 | 判定 |
| --- | --- | --- |
| 「它拷的是业务对象」 | `:120` = `structuredClone(defaults)`，`defaults = NODE_DATA_DEFAULTS[type]`；`:117-119` 注释自承「必须深拷贝」 | ✅ **属实** |
| 「行为上有差别（可能有回归）」 | `deepClone<T>(v: T): T` 的函数体**就是** `return structuredClone(value);`（`utils.ts:131`） | ❌ **证伪**：两者**逐字等价** ⇒ 纯结构改动，**运行期零变化** |
| 「直接 import `core/utils` 会成环」 | `core/utils` 的依赖 = {`react`, `core/config`, `base/utils/asyncGuard` → `core/config` + `@/types`(纯类型 barrel)}；且 `base/canvas` → `../core/` 已有 `idGen` / `config` / `logger` / `contracts` **多处先例** | ❌ **证伪**：不成环、无分层问题 |
| 「去掉这处能让测试变红」 | 探针（见 §四②）：**注回旧实现，原有 6 条仍全绿** | ❌ **证伪**：原有用例**无判别力** ⇒ 需另加锁 |

## 三 · Step 2~5 归因 / 定位 / 定契 / 定权

| 项 | 内容 |
| --- | --- |
| 归因 | ADR-0005 判据 3「出现裸 `structuredClone` 用于拷贝业务对象」= 违反。它**不是**"实现写错"，是**入口被绕过**（一归闸） |
| 定位 | `src/components/base/canvas/nodeDataSchema.ts:120` |
| 契约 | 深拷贝**唯一入口** = `core/utils.deepClone`（ADR-0005 决议 1；文件头已明列） |
| 定权 | 纯结构、无行为变化、有先例 ⇒ **架构师自定**（Step 5）；**无闸覆盖** ⇒ 自补防回潮锁 |

> **⚠️ 附带发现（无闸）**：`grep deepClone|structuredClone scripts/*.mjs` → **零命中**。
> 即「唯一入口」这条规则**只写在文件头注释与 ADR 里**，没有任何机器校验 ⇒ 与 TD-17-20 同族盲区。

## 四 · Step 6 最小改动

| # | 文件 | 改动 |
| --- | --- | --- |
| ① | `src/components/base/canvas/nodeDataSchema.ts` | `structuredClone(defaults)` → `deepClone(defaults)`；加 import + 一句"为什么要走入口" |
| ② | `src/components/base/core/utils.ts:126` | 回改"能力取证"里那句已失效的**先例指针**（它指的两处现已都收口到本函数） |
| ③ | `src/components/base/core/utils.ts:8` | 文件头规则行：`JSON 深拷贝唯一入口` → `深拷贝唯一入口（禁 JSON 往返，也禁裸 structuredClone）`（前一句在 ADR-0005 之后**本就失真**：实现早已不是 JSON 往返） |
| ④ | `tests/unit/nodeDataSchemaDeepCloneEntry.test.ts` | **新增**（2 条）：锁"必经 `deepClone`"这个**结构**性质 |

## 五 · Step 7 验证（探针三块 + 反向对照 + 门禁）

**① 正向：收口是否真的走到入口**（往 `deepClone` 注入抛错 ⇒ `nodeDataSchema` 用例必须红）

```
🔬 探针结论｜TD-18-15 深拷贝收口真的走到唯一入口 行为探针
   文件   : src/components/base/core/utils.ts（命中 1 处，已还原 sha=9cd0329b3c62）
   命令   : npx vitest run tests/unit/nodeDataSchema.test.ts
   观测   : exit=1｜输出 77 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「PROBE_DEEPCLONE」 — 命中
```

**② 反向对照：原有用例其实判别不了**（注回旧实现 ⇒ 应**仍绿**，否则①不具判别力）

```
🔬 探针结论｜TD-18-15 反向对照：旧实现下该测试无法判别（仍绿）
   文件   : src/components/base/canvas/nodeDataSchema.ts（命中 1 处，已还原 sha=0102c8af8ac1）
   命令   : npx vitest run tests/unit/nodeDataSchema.test.ts
   观测   : exit=0｜输出 6 行
   ✅ 退出码 == 0 — 实际 0
```

**③ 新增锁的先红后绿**（注回旧实现 ⇒ 锁必须红）

```
🔬 探针结论｜TD-18-15 唯一入口锁 先红后绿
   文件   : src/components/base/canvas/nodeDataSchema.ts（命中 1 处，已还原 sha=0102c8af8ac1）
   命令   : npx vitest run tests/unit/nodeDataSchemaDeepCloneEntry.test.ts
   观测   : exit=1｜输出 19 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「at least once」 — 命中
```

**门禁（全绿）**：

| 命令 | 结论 |
| --- | --- |
| `npx vitest run` | **244 文件 / 3035 测试通过**（本轮 +1 文件 +2 条 = 新增锁） |
| `npm run test:localtool` | **ALL PASS** |
| `npx tsc --noEmit` | **0 错** |
| `npx eslint <两个改动文件>` | 0 问题 |
| `check:arch` / `check:any` / `check:strict-src` | ✅ / **0 违规** / ✅ |
| `check:dead-code` | 基线 76 → 当前 **76**（无新增） |
| `check:doc-refs` / `check:gates` | **0 违规** / **15 闸带申诉口** |

## 六 · 度量（改前 / 改后）

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| `src` 内 `structuredClone` **代码**站点 | **2**（`deepClone` 内 + `nodeDataSchema` 裸） | **1**（仅 `deepClone` 内） |
| 深拷贝唯一入口份数 | **1 入口 + 1 绕过点** | **1 入口 + 0 绕过点** |
| 「必经入口」的**机器判据** | **0**（规则只在注释与 ADR） | **1**（新增锁，注回即红） |
| 运行期行为 | — | **零变化**（两实现逐字等价） |

## 七 · 交付六闸自查

- **一归**：深拷贝入口 **1 + 1 绕过 → 1**（唯一实现 = `deepClone`）✅
- **减一**：**未新增任何兜底 / 守卫 / 兼容分支**（新增的是**断言**，不是兜底）✅
- **减二**：绕过点 −1；顺带修掉文件头一处**已失真**的规则描述（"JSON 深拷贝"）✅
- **一诚实**：无失败路径改动；诚实性体现在**把无判别力这件事说破**（探针②）并补锁 ✅
- **零炸**：见下三问 ✅
- **一凭证**：3 块探针结论 + 8 项门禁 + 2 组 grep 计数 ✅

**零炸三问**：

1. **哪些行为被改了？** —— **运行期零变化**（`deepClone` ≡ `structuredClone`）。改动只落在：调用点、注释、新增测试。
2. **改前能走的路径，改后仍能走吗？** → 是（3035 通过；`test:localtool` ALL PASS）。
3. **改前会失败的，改后仍失败吗？** → 是。含函数 / Symbol / DOM 的克隆仍抛 `DataCloneError`（同一实现）。

**用户可感知的行为变化（显式声明）**：**无**。

## 八 · 结论与下一步

- TD-18-15 **结清**。
- **债描述被修正处（A7）**：
  1. **ADR-0005 正文未改** —— 全仓 ADR **无「已修正」先例**，SOP 定「改主意不改历史」；且本轮**未改 ADR 的任何结论**
     （判据 3 正是本轮的执行依据）。但需记一笔：**ADR-0005「Step 0 附带发现」称实有 2 份实现，漏算了
     `nodeDataSchema.ts:120`** —— 它当时被误分类为「能力先例」而非「第三处裸调」。
     教训：**普查"实现份数"要按语义数（谁在深拷业务对象），不能只数"看起来像实现的命名函数"。**
  2. `core/utils.ts` 两处注释已就地回改（§四 ②③）—— 它们引用的是被本轮改动**改陈旧**的指针。
- **未纳入本轮**：
  1. 「唯一入口」仍**无闸覆盖**（规则只在注释与 ADR）。补闸要定义"哪些入口算唯一、如何枚举绕过点"，
     属闸体系设计，超出本轮最小改动半径 —— 已用**单点锁**先止血。
  2. `d3dPersistence.ts` 的 `cloneProjectValue` re-export 是**域内语义名**（ADR-0005 决议 4 明确允许），**非绕过**，不动。
