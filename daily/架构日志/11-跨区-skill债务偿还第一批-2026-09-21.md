# 11-跨区 · Skill 债务偿还（第一批：装配断裂 + 组清单归一 + 四条诚实/契约）

> **触发**：用户「看看关于 skill 的债务，有人觉得你做得不好」→ 逐条核验审计（`11-跨区-skill系统方案落地审计-2026-09-21.md`）
> → 追问「两处云同步、两处 skill 同时存在，那不是两份真相吗？」→ 「好」（授权按 7 步法施工）。
> **判据源**：`ADR-0056`（四问）· `ADR-0053`（删的前置）· `docs/plan/140` · 本仓「诚实优先 / 禁静默兜底 / 一归」铁律。
> **范围**：`agent/skill/**` · `agent/runtime/{useAgentChat,skillStore}` · `agent/panels/AgentPanel` · `settings/sections/SkillSettings` · `tests/unit/skill*`。

## 一 · 核验结论（22 条审计债，逐条取证）

**22 条全部成立（0 条假债）**；唯一需要修正的是 TD-11-19 的表述（"两个写 API"实为单个／批量两种粒度，不是同义重复实现）。

最重的三条：

| 债 | 事实 | 可复现证据 |
| --- | --- | --- |
| **TD-11-16** | P4「按需读资料」对**所有** skill 恒失败，而阶段表标了 ✅ | `grep -n "category\|slug" src/components/agent/panels/AgentPanel.tsx` ⇒ 仅 1 处命中且为注释；`skillResource.ts:91` 恒走"没有分组/目录"分支 |
| TD-11-18 | 空技能库被谎报成"磁盘内容与缓存一致" | `skillHydrate.ts:64`（无未决标记）+ AgentPanel 重载 toast 二选一 |
| **TD-11-41**（本轮新登记即修） | 组清单有**两个出生地**（磁盘目录 ∪ 成员 `category`）⇒ 磁盘上删掉组目录后侧栏**仍显示该组** | 我上一轮引入的并集；用户提问「那不是两份真相吗」直接命中 |

用户提问的裁定：

- **云同步两处 = 分层，不是两份真相**：`base/store/cloudSync.ts`（同步编排：拉取／写回／台账／汇总）↔ `skill/skillCloudSync.ts`（skill 域写回策略）。编排侧只经门面调用（`cloudSync.ts:54` import、`:879` 调用），真相仍只有一份。
- **skill 两处 = 真问题**：`runtime/skillStore.ts`（转发层 ＋ 内置常量）与 `agent/skill/**`。本轮收掉"内置常量住 legacy 层"这一半（迁 `skillBuiltins.ts`），转发层深引挂 **TD-11-19** 待偿。

## 二 · 探针（先红后绿 · 凭证）

```
🔬 探针结论｜TD-11-16 装配落点丢失回归
   文件   : src/components/agent/skill/skillInject.ts（命中 1 处，已还原 sha=0d84263e30b1）
   命令   : npx vitest run tests/unit/skillInject.test.ts
   观测   : exit=1｜输出 47 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「客户项目」 — 命中
```

```
🔬 探针结论｜TD-11-23 空串被当成「没有分组意图」
   文件   : src/components/agent/skill/skillPersist.ts（命中 1 处，已还原 sha=e1a41cd134f6）
   命令   : npx vitest run tests/unit/skillPersist.test.ts
   观测   : exit=1｜输出 22 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「移回未分组」 — 命中
```

```
🔬 探针结论｜组清单归一：不许凭缓存造组
   文件   : src/components/agent/skill/skillCatalog.ts（命中 1 处，已还原 sha=d0869d2c5abe）
   命令   : npx vitest run tests/unit/skillCatalog.test.ts
   观测   : exit=1｜输出 43 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「幽灵组」 — 命中
```

**③ 还原后的绿**：`npx vitest run tests/unit/skillInject.test.ts tests/unit/skillPersist.test.ts tests/unit/skillCatalog.test.ts` → `Tests 60 passed`（含本轮新增 12 例）。

## 三 · 反向探针（零炸）

`npx vitest run`（全量）→ `Test Files 263 passed ｜ Tests 3210 passed` ⇒ **改前能走的路径全都仍绿**。
静态闸：`type-check` 0 错 · `lint` 0 问题 · `check:arch` ✅ · `check:keys` ✅ · `check:api`（error 0）· `check:dead-code` **0 新增**（且顺带清偿 `skillStore::exports::findSkill`）· `test:tools` 58 例 ✅。

## 四 · 度量（改前 → 改后）

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| 装配处提供的字段 | UI 镜像 4 字段（`{id,name,description,content}[]`） | **只给 `string[]`（id）**；正文与落点由模块现查 |
| "全部 skill" 的来源 | 缓存（模块可达）+ 内置常量（**legacy 层，模块不可达**） | 两处**都在模块内**（`skillRepository` + `skillBuiltins`） |
| 组的存在性出生地 | **2**（磁盘目录／成员 `category`） | **1**（磁盘目录）；越界成员进 `orphaned` 如实收纳 |
| "未决"态 | 折叠进 `changed:false`（被读成"一致"） | `HydrateResult.undecided` ／ `diskGroups: string[]｜null` 显式 |
| 分组意图 | 1 个值承担 2 义（`undefined` 与空串折叠） | **3 态显式**：未提／要求未分组／有名字 |
| 类型层防线 | 被 `as SkillPick[]` 断言**拆掉** | 断言与 `SkillPick` 同批删除；`skills: string[]` 由类型层拦住漏传 |

## 五 · 弯路留痕

1. **探针注入点选错（一次，已纠正）**：TD-11-23 首次只注入 `targetCategory` 一行 ⇒ 测试**仍全绿**（当时推理："这就是旧实现"）。错因：该修复是**两处配合**（`wantGroup` 也已把空串映成 `_未分类`），单点还原不足以复现旧行为。处置：改注入 `wantGroup` 的空串分支（真正的旧判据）⇒ 命中。**教训**：注入点必须落在**判据本身**，而不是它的下游。
2. **一条断言写错（一次）**：`reconcileSkillGroups([], [...])` 我断言 `groups` 为空，但兜底组 `_未分类` **有成员时本就该出现** ⇒ 改断言，不是改实现。
3. 未产生假绿测试；无"被替代的老实现"被回退；探针三次注入全部经 sha256 还原校验。

## 六 · 回改原文

- `agent/skill/README.md`：三处漂移就地回改 —— ①`skillTypes` 行删掉已不存在的 `RuntimeSkill` 表述、②新增 `skillBuiltins.ts` 行、③「已知例外（待 P3 收拾）」改成事实描述「**仍未收口**」并挂 TD-11-19；另修 §5 两段同义重复，不变量 15 补"骨架唯一 + 未决"。
- `docs/plan/140`：**未改**（方案没错，是实现在漂）；其 §6 验收 #26-28 所缺的"端到端"已由本轮的装配用例补上第一段（面板 → hook 参数）。
- **ADR：用户裁定「不落盘」**（2026-09-21，理由：它是**过程日记**，做完之后不影响全局，属**非常局部的细节**）⇒ 判据本体留在**代码 JSDoc**（`freezeSkillTurn` 头部：为什么只给 id）与**模块 README 不变量 15**（骨架唯一 + 未决）；本文件只留由来与弯路。**未落盘 ⇒ 无编号、无索引条目**（`adr audit` 仍 53 条可核）。
  - ⚠️ 自查（承认判据落点选错）：我用"消费者镜像字段是跨域通病"论证"涉及 3 个面"，其实是在**为落盘找理由** —— 那条通病已被 `ADR-0056`（真相源）与 `ADR-0044 §8`（门面按用例）覆盖，再补一条就是**把同一判据抄成第二份**（正是本轮在批的形态）。⓿ 问的正确答法是：它只影响本模块的装配与归组 ⇒ **区域日志 + 模块 README**，不占编号。

## 七 · 结清与剩余

- **本轮结清 7 条**：TD-11-16 · TD-11-18 · TD-11-20 · TD-11-23 · TD-11-33 · TD-11-40 · TD-11-41（新登记即修）。
- **仍待还 16 条**（11 区主表）：TD-11-17 · 19 · 21 · 22 · 24 · 25 · 26 · 27 · 28 · 29 · 34 · 35 · 36 · 37 · 38 · 39。
- **下一批优先序**：TD-11-22（假动作，动手前需先定"真接切换"的跨域契约 = 用户拍板项）→ TD-11-19（skillStore 收口）→ TD-11-17（块① 不可信条款）→ TD-11-21／24／28（诚实闸三处）→ 其余低息随轮次清。

## 八 · 收尾自检（施工 8 条）

①新增复杂度**下降**（+1 文件换来"全部 skill 一个来源"）②兜底**净减**（`as` 断言、并集造组两条隐式兜底被删，未新增）③无越权（UI 不再替模块"取正文"）④无 SSOT 第二份（技能来源／组骨架各归一）⑤无 `_` 前缀残留、无死 state ⑥注释与实现同步（含本轮回改三处）⑦`git diff` 逐行可解释（改动半径＝9 源文件 + 5 测试文件，全在目标能力内）⑧**无半态**（`SkillPick` 类型与 `as` 断言同批删除）。

## 九 · 决策留痕

- 用户「好」= 授权按修正后顺序施工（`TD-11-16 → 组清单归一 → 23／33／18／40 → 20／19 → 其余`）。
- **ADR 落盘：用户裁定「不同意」** —— 原话「属于过程日记；做完之后又不会影响全局，这只是一个非常局部的细节」。依据 = 六问 ⓿（只影响一处 ⇒ 不写 ⇒ 区域日志）。我原答卷里"消费者镜像字段是跨域通病 ⇒ 涉及 3 个面"的论证被推翻（那把已被 `ADR-0056`／`ADR-0044 §8` 覆盖的族说成"需要新条" = 判据抄第二份）；自查已记入 §六。
- **TD-11-22（"去使用"假动作）**：用户上一轮「好」= 认可推荐 A（真接切换）。它要新增跨域契约（设置页写"当前会话选中态" + 请求打开 AI 面板）⇒ 下一批**先落契约再接线**，不在无契约时直连面板内部 state。
