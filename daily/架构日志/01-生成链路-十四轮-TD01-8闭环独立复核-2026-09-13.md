# 01 · 生成链路 — 十四轮：TD-01-8 闭环独立复核（声明成立 · 无代码变更）

> 日期：2026-09-13 · 区域：01 生成链路 · 类型：**复核（不改代码）**
> 目的：TD-01-8 台账自称「主体已完成 · **本母体全部清偿**」「剧本盒内手写/裸绕编排点 → 0」——
> 该声明**此前未独立取证**（九/十轮验证跑的是测试，不是「绕点计数」）。本轮**只做证伪式复核**。
> 方法：`架构师改码7步法` 铁律「任何描述都是线索，不是结论」——含**我自己上一轮写的结论**。

---

## 一、复核项与取证

| 声明 | 取证手段 | 实测 | 判定 |
| --- | --- | --- | --- |
| 编排原语消费方「严格 2 模块」 | `grep -rln runGenerationContract src` | **恰好 2**：`hooks/useNodeGeneration.ts` · `scriptbox/scriptBoxEngine.ts`（+ 定义处 `generationContract.ts`） | ✅ |
| 剧本盒内「手写/裸绕编排点 → 0」 | `grep -nE 'reportGenerate\|saveResultToTasks\|completeTask\|reportProgress' scriptBoxEngine.ts` | **0 处** | ✅ |
| 节点侧无裸绕 | 同式 grep `useNodeGeneration.ts` | **0 处**（仅注释提及 `reportGenerate`） | ✅ |
| 图像生成全走原语 | grep `generateImage` | **仅 2 处**（`scriptBoxEngine.ts` L544 资产图 / L1656 尾帧图），**均在 `runGenerationContract` 的 `run` 回调内** | ✅ |
| 文本 5 处 `chatCompletions` 属「有意例外」 | 读 R7 + 核依据 | R7 实据成立：`taskCompletionBus.publishTaskCompleted` **L35 拒空 `resultUrl`** → 文本 `done('')` 不广播（文本无 URL，进任务中心只增噪音，@见 TD-01-11 改判） | ✅ 非漏接 |
| 运行期 abort/超时/失败分级已统一 | 读 `runAbortable`（L265） | 确认：`abortMap` 中止注册 + `withTimeout` 总闸 + `classifyError` 分级（abort→warn / 其它→toast+error），**与生成原语正交**（原语管任务中心序列，`runAbortable` 管运行期） | ✅ |
| 成片路径不裸绕 | 读 `onGenerateMergedVideo`（L1424） | 仅 `chatCompletions` **生成合并提示词**（文本，R7）+ `addNodes` 建下游 `videoGenerateNode` → **实际视频生成发生在下游节点**（走 hook→原语） | ✅ |

---

## 二、发现的「疑似重复」与其判定（Step 3 分辨）

5 处 `chatCompletions` 的**请求构造**（`provider/model/temperature:0.7/signal/messages`）字面重复 —— 但按 TD-04-21 教训分辨：

| 种类 | 内容 | 判定 |
| --- | --- | --- |
| **探测重复**（怎么调） | 请求字面 5 字段 | 形式上重复，但**仅 5 字段字面量**；抽象成 helper 只省 ~4 行 ×5，且新增一层间接 |
| **判据重复**（做什么） | **后处理各不相同**：L359 `genMask=false`+logger / L729 `failedCount++`+patch / L890 `parseJsonText` / L1001 清 `promptLoading` / L1468 toast+return | ❌ **不可合并**（合并须削掉各点必要能力） |

⇒ **不改**。判据：方法 Step 6「只改真风险点，**不为一致性而改**；答不出『改了它复杂度下降了吗』就别改」。
（abort/超时/异常分级已由 `runAbortable` 统一，**不存在散落 catch**。）

---

## 三、度量（§零.3）

| 口径 | 复核前（台账声明） | 复核后（实测） |
| --- | --- | --- |
| 编排实现份数 | 2→1（自称） | **确认 1**（消费方恰 2 模块共用同一原语） |
| 剧本盒内裸绕点 | 0（自称） | **0**（独立计数确认） |
| 生成的 API 直调点 | — | `generateImage` 2 处，**全在原语 `run` 内** |
| 代码变更行数 | — | **0**（纯复核） |

---

## 四、诚实边界

1. **本轮不改一行代码** —— 产出是**独立验证**（把「自称」变成「可查」），并把「已知例外」与「已知不可合并点」写进台账，**防后续 AI 重复审计或误合并**。
2. **未复跑测试**：本轮是静态结构复核（grep/读码），未跑测试；理由——无代码变更，测试面无回归风险。九/十轮已跑 189+71 例。
3. **未覆盖**：`onGenerateShotImage` / `onReviewShotPrompt` 的**业务行为正确性**（本轮只判「有没有绕过编排」，不判「提示词质量 / JSON 解析边界」）——若有业务疑问属另立议题。
4. 一处**未深究**：L890 处 `parseJsonText` 未设 `responseFormat:'json_object'`（L359/L729 设了）。**仅登记，未改** —— 无法判定是「模型兼容性考虑」还是「遗漏」，且改动属 API 参数（近业务面）。
   - **【2026-09-26 结案】** 该机制已**整条删除**（TD-01-35）。**本线索关闭** —— 详见 `01-跨区-模型输出JSON契约分散-2026-09-26.md §五-b`。
5. **流程偏差登记（用户质询后补）**：本轮属**纯文档改动**，我却跑了 `check:push`（7 道代码闸）—— 违反 §7.3「只跑**与改动相关**的闸」，且正踩 §7.2「**禁止**为一次小改动跑全量；全量绿会给你『很安全』的错觉」。**那次执行证明不了任何事**（代码零变更），属**自证式动作**。
   - **文档改动真正该跑的校验**（本轮已补）：① **链接可解析**（`index.md` 96 条 / `债务.md` 173 条）；② 表格结构完整（列数 / 无旧新状态并存 / 无重复区行）；③ 台账状态与 `index` 状态一致。
   - **该校验当场抓到一条真断链**（**代码闸永远抓不到**）：上轮 TD-02-23 台账链接写 `02-存储-持久化-十三轮-TD02-23双真收口`，而实际文件名为 `…-十六轮-…` → **已修正**，复跑断链归零。
   - ⇒ 教训：**验「我做了什么」要用与它同类的尺子**，而不是抓一把现成的闸来刷绿。

---

## 五、结论

- **TD-01-8 台账「本母体全部清偿」「裸绕点 → 0」声明成立**（已独立取证）→ 状态升级为 **[已解决 2026-09-13 · 主体已完成]**。
- **无残余工作、无代码变更**；已知例外（文本 R7）与已知不可合并点（5 处文本后处理）已留痕。
