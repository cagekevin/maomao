# 01 生成链路 · 四轮深探（生图入口普查 + 合约骨架逐行差异 + 第四形态）· 2026-09-12

> 关联：三轮（TD-01-8 升级能力不对等 + 立 TD-01-9/10/11）。本轮目标：① 生图/生视频入口全量普查，确认有无隐藏第三入口；② 两入口合约骨架**逐行差异**，建抽 `runGenerationContract` 的开关清单；③ 挖出 scriptBoxEngine:1680 尾帧图是第四形态。本轮只登记债务，不改源码。

## 一、生图/生视频入口全量普查（grep `generateImage(`/`generateVideo(`）
全库 10 处命中，分类：
- **定义**：`generate.ts:213` / `:239`。
- **节点侧（全部走 `useGenerateNode`→`useNodeGeneration` 合约）**：`ImageGenerate.tsx:315` / `VideoGenerate.tsx:221` / `_template/TemplateNode.tsx:272`。
- **脚本盒内（非 React，手写）**：`scriptBoxEngine.ts:536`（资产图，手写 5 步合约 506-617）+ **`scriptBoxEngine.ts:1680`（尾帧综合图，完全裸绕任务中心，见 §三）**。
- 注释示例：`useNodeGeneration.ts:140`。
- **结论**：节点侧生图**无隐藏第三入口**（全经合约）；但 `scriptBoxEngine` 内**≥2 处**生图，其中 1680 完全绕任务中心 → 合约形态不是 3 种是 **4 种**。

## 二、合约骨架逐行差异清单（抽 contract 的六开关依据）
对比 `useNodeGeneration.start`（:199-355）vs `scriptBoxEngine` 资产图（506-617）：

| 步骤 | hook（React） | scriptBox 资产图（非 React） | contract 开关 |
|---|---|---|---|
| 并发锁 | `claimNodeRun` + `runningRef`（:202/:208） | 无（伪 nodeId `nodeId-asset-<id>` 自然隔离） | `mutex: boolean` |
| abort | `AbortController`+`signal`（:222-225） | 无 signal（runAbortable 超时兜底，:524） | `abortable: boolean` |
| reportGenerate | ✅ :228 | ✅ :506 | — |
| progress(5,'准备中…') | ✅ :231 | ✅ :507 | — |
| run 执行器 | `runRef`（注入，:247） | `generateImage`（:536） | `run` 注入 |
| 结果本地化 | 无（直接 `saveResultToTasks`） | `localizeAndStoreToResourceLibrary`（:555） | `localize: boolean` |
| 写回 | `patchData`×2 阶段（:268 即时原始 + :292 落盘后持久） | `commit`×1（:568 落盘后） | `patcher(resultUrl)` 回调 |
| saveResultToTasks+reportDegrade | ✅ :278 | ✅ :578 | — |
| done | ✅ :294 | ✅ :584 | — |
| **失败分类** | `classifyError`（:307/:336，区分 abort/timeout/network/http/business） | **❌ 无 classifyError** | `classifyError` 内聚进 contract |
| recover 订阅 | ✅ :393 | ❌ 无 | 内聚进 contract |
| finally 清理 | `updateNodeRuntime`+`runningRef`复位+`releaseNodeRun`（:350-353） | `taskSettled` 防御 fail（无 releaseNodeRun） | 内聚进 contract |

→ **六开关**：`mutex` / `abortable` / `localize` / `patcher` / `recoverable` / `classifyError`（前 5 显性参数，后 2 内聚固化）。

## 三、第四形态：尾帧综合图完全裸绕任务中心（scriptBoxEngine:1680）
- **现象**：:1680 直接 `generateImage`，仅 `localizeAndStoreToResourceLibrary` + `patchShot` 写回剧本 `shots`；**无 reportGenerate / done / saveResultToTasks / recover / 失败分类**。
- **abort 自矛盾**：尾帧图 :1697 传 `signal`（在 `runAbortable(async signal)=>` 内，可中止），但同文件资产图 :524 注释「用户要求停止不做——不传 AbortSignal」→ **脚本盒内部生图 abort 语义不一致**（尾帧可中止、资产图不可）。
- **归类**：还债（比文本第三形态更裸：文本类结果落 shots 有合理性，尾帧图是资产生图却不上任务中心，纯属遗漏）。
- **利息**：中（尾帧生成无进度/完成广播/失败分类，刷新后靠剧本 data 自身恢复，但任务中心完全不可见、无 recover 订阅）。
- **状态**：待还（抽 contract 时尾帧图改走合约 + `patcher` 写 shot；或至少补 `reportGenerate`+`done` 使任务中心可见）。

## 四、TD-01-8 再升级
- **形态 3→4 种**：全（hook）/ hook 资产 / scriptBox 资产图裸 / 尾帧图裸 / 文本绕。
- **能力不对等 4 点**：①recover 订阅（仅 hook:393）②abort（hook 有；scriptBox 资产图无/尾帧图有→内部自矛盾）③**失败分类 `classifyError` 仅 hook 用、scriptBox 全缺** ④结果本地化（scriptBox 有、hook 无）。
- **六开关清单已立**（§二），抽 `runGenerationContract` 设计从"要点"升级为"可落地签名"。
- **爆炸半径仍稳**：合约真源严格 2 模块（hook + scriptBox）；`useGenerateNode` 干净适配壳；`director3d` 0 接触；任务中心原子方法纯 TS 可复用。

## 五、深化债登记
- **TD-01-8（升级）**：形态 4 种 + 能力不对等 4 点 + 六开关清单（见上）。
- **TD-01-12（新）**：尾帧综合图完全裸绕任务中心（第四形态，含 abort 自矛盾）。

## 六、待用户拍板
1. 执行 TD-01-8（抽 `runGenerationContract`，六开关固化）→ 顺修 TD-01-9（recover 内聚）/TD-01-10（abortable）/TD-01-12（尾帧改走合约），并统一 TD-01-8 内含的 `classifyError` 缺失。
2. TD-01-11（文本第三形态）是否纳入同一 contract（patcher 写 shots）或维持"文本不上任务中心"文档化？
