# 17 审计工具链治理 · 五轮 · TD-17-4 端口豁免 SSOT 收口（2026-09-14）

## 一、证伪（Step 1）：是 SSOT 第二份吗？

- **现象**：端口豁免数据两处维护——真源 `contracts.ts::NODE_HANDLE_CONTRACT` 注释写「唯一豁免」+ 闸内 `HANDLE_EXEMPT` 自建表（2 条：ScriptBoxNode + NodeShell），已半漂移（真源说 1 条，闸内 2 条）。
- **取证**：
  - `ScriptBoxNode.tsx:194` `showHandles={false}` + `:209` `overlayHandles={<CustomHandle .../>}` → 真触发闸规则 1（组合违例），豁免确实必需。
  - `NodeShell.tsx:341/342` 同样含 `showHandles={false}` + `<CustomHandle>` → 它是端口渲染**实现定义文件**（非 node type），豁免合法且**不该进 NODE_HANDLE_CONTRACT**。
  - 真源与闸对 `scriptBoxNode` 的豁免**语义重复** → 母体 M3 SSOT 第二份，改一处漏另一处 = 静默错（闸放行但真源未登记 → App 补边/占位骨架拿不到 handle → 边不渲染 / code-008）。

## 二、处置（Step 3：收敛——真源 machine-readable，闸派生）

| 文件 | 改动 |
| --- | --- |
| `src/components/base/core/contracts.ts` | ① `NodeHandleContract` 接口加 `customHandles?: boolean`；② `scriptBoxNode` 条目加 `customHandles: true`；③ 注释由"唯一豁免"改为指向 machine-readable 字段 |
| `scripts/check-node-handles.mjs` | ① `HANDLE_EXEMPT` 删 `ScriptBoxNode` 重复项，仅留 `NodeShell`（非 node type 实现定义，注释澄清）；② 扫描前由真源 `NODE_HANDLE_CONTRACT[].customHandles` **运行时派生**豁免文件集合 `customHandleNodeFiles`（PascalCase 推导 + 磁盘存在性校验，fail-safe）；③ 规则 1 判定并入派生集合；④ 头部文档 Q2/Q3 改为引用真源 `customHandles` |

## 三、度量（改前 / 改后，可比）

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| 端口豁免数据维护点 | 2（真源注释 + 闸内表） | **1**（仅真源 `customHandles`） |
| 闸内手抄豁免清单 | 2 条 | **1 条**（NodeShell，且明确非 node type） |
| 漂移风险 | 半漂移（1 vs 2） | 0（单一来源） |

## 四、探针先红后绿（§7.1，证据块）

> 🔬 探针结论｜TD-17-4 端口豁免派生
>    文件   : src/components/base/core/contracts.ts（命中 1 处，已还原 sha=6ebfc027f582）
>    命令   : node scripts/check-node-handles.mjs
>    观测   : exit=1｜输出 4 行
>    ✅ 退出码 == 1 — 实际 1
>    ✅ 输出含「ScriptBoxNode」 — 命中
>
> ✅ 探针命中（观测与期望一致）—— 可直接粘贴本块进区域日志作为"先红后绿"证据。

- **红**：注入「移除真源 `scriptBoxNode` 的 `customHandles: true`」→ 闸精确报 `ScriptBoxNode`（exit=1）。证明豁免**确实由真源派生**（不是闸内手抄副本）。
- **绿**：自动还原后 `check-node-handles` → `节点端口契约校验通过 ✔（特例 2 个已登记（含真源派生））`。

## 五、债描述修正

- 债务.md TD-17-4「真源注释写『唯一豁免』+ 闸内 HANDLE_EXEMPT 自建表（已半漂移）」**属实**；修正：真源现已 machine-readable，闸内手抄项已删，漂移根因消除。
  ── 更新(2026-09-16)：本轮 M7 裸写收口按 A10 改了本区工具 `scripts/debt.mjs`（TD-17-8 已解决）+ 新立 TD-17-9；
  **本区状态行不写在此文件**（`listRounds` 按「日期+文件名」排序时「五轮」排在「四轮」**之前**，故状态源是四轮文件）—— @见 `02-跨区-M7裸写收口-2026-09-16.md`。
