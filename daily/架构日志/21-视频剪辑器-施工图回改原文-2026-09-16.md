# 21 · 剪辑器 · 施工图回改原文（TD-21-18）· 2026-09-16

> **性质**：A7 债务（「修正必须回改原文，只记新文档 = 亲手造 SSOT 第二份」）的**偿还**。
> **触发区**：21。**结论落点**：本文件（本债原锚点为 `21-...§16.5`，**该文件里并无本债明细** —— 见 §二）。
> 改动文档：`docs/123`（施工图）· `docs/120`（数据流契约）。

---

## 〇 · 一句话

> 债说「`docs/123` **三处**待回改」——取证后真相是：**该文档的命名体系与目录树整体落后于实现**
> （`core/`/`export/`/`panels/dock/`/`hooks/` 四个名字在仓库里**一个都不存在**；
> §一.4 的 `relayoutSequential`/`settle`/`magneticOf` **全仓零命中**）。
> ⇒ 处置：**不重写 75KB 施工图**（那是重做方案，不是还一笔低息债），而是按该文档既有的
> `★更新(日期 · 原因)` 约定**就地加"本文与实现不一致、以代码为准"的对照标注**，
> 并把**契约级**的那处（`docs/120` C4.8 字段名）**改正**。

---

## 一 · 三处回改（逐条）

| # | 位置 | 原文 | 改后 | 取证 |
| --- | --- | --- | --- | --- |
| 1 | `docs/120` **C4.8**（原债记为 `docs/123`，**文档指错**） | `data` 至少含 **`videoUrl`** | **`assetUrl`** + 勘误段（含为什么） | 产物经 `spawnAndCommit` 生成 **`assetNode`**；`useConnectedInputs::SINGLE_OUTPUT_FIELDS` 里 `assetNode: ['assetUrl']`（全表只有 `videoGenerateNode` 用 `videoUrl`） |
| 2 | `docs/123` §二.1 目录树 | `core/` `data/` `export/` `panels/dock/` `hooks/` | 就地保留原树 + **实际布局对照**（`ls` 取证） | 实际顶层：`constants/ data/ engine/ hooks-cutia/ lib/ stores/ types/ ui/ utils/`；`engine/` 下才是 `commands/ core/ lib/ services/ timeline/` |
| 3 | `docs/123` §一.4 B 组（`relayoutSequential` / `settle` / `magneticOf`） | 三个原语名 | 文件头 ★更新块里写清：**本仓无此三符号**，对应能力现落在 **`rippleEditingEnabled`（波纹编辑）** | 全 `videoEditor/**` grep 三符号**零命中**；`ripple` 命中 `stores/timeline-store.ts` + `timeline-toolbar.tsx` |

另在 `docs/123` **文件头**加了一处**总标注**：本文是「方案图」→ 命名/落点以代码为准；
并列出**仍然有效**的部分（§一 契约族 / §二.5 拒绝清单 / §二.7–二.9 审计结论）与
**不再作为**索引的部分（"照着找文件"）。

## 二 · 债描述被修正的两点（A7）

1. **文档指错**：债写「`docs/123` 三处」，但第 3 处（C4.8 字段名）原文在 **`docs/120`**。
2. **严重低估**：债写「三处」，实际该文档的**命名体系 + 目录树整体过期**（不是三处笔误）。
   ⇒ 本轮**不**把它们逐条重列（那会变成"本文的第二份"），而是一处总标注 + 明确"以代码为准"。

## 三 · 弯路留痕（强制）

### 弯路 1：先按锚点去读区域日志 → **该文件里没有本债**

债的锚点是 `21-...§16.5`，grep `TD-21-18` 在 21 区日志**零命中**（`docs/123` 里也只在本债描述中出现）。
⇒ 结论：**锚点只是"文件存在"，不保证"债在里面"**（与 TD-22-52 的"错号假证据链"同族）。
本轮的取证实际靠：`债务-全文归档` 里的摘要 + **直接 `grep` 代码**.

**教训**：`debt.mjs audit` 查的是"锚点路径是否存在"，**不是"锚点里有没有这条债"** ——
读债时"锚点打不开/找不到"要在轮次日志留痕，别默默绕过去。

### 弯路 2：一度准备"只改那三行"

第一直觉是把债描述里的三处逐字改掉就收工。取证到第 2 处时发现 `hooks/` **根本不存在**，
第 3 处发现 `C4.8` 不在这个文件里 ⇒ 才意识到"三处"是**症状**，母体是"**文档写于 09-13，
此后实现换了命名与落点，而没人回改**"。

**教训**：债里的"N 处"是**线索**。当第 2 处就开始"对不上"时，应当**停下重新问范围**
（是不是整份文档过期？），而不是把对不上的部分"按描述硬改"。

## 四 · 未做（有意）

1. **未重写 `docs/123`**：它是 75KB 的方案图，逐节改写 = 重做方案（不是还一笔低息债）。
   本轮给它加了"以代码为准"的总标注 + 逐条对照，**信息已可用**。
2. **未逐条核对 §一.4 的其余原语**（只抽验了 B 组的三个）。理由：全量核对会得出
   "整份清单要重列"的结论，而**正确的结论恰恰是"这份清单不该再被当索引用"** ——
   已写进文件头标注。若将来有人要按它施工，以代码为准即可。
3. **未动 `docs/113` / `docs/124` 等同期文档**（同批产出，可能有同类漂移）——
   属"另一次普查"的规模，不在本债范围。

## 五 · 度量

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| `docs/120` C4.8 字段名正确性 | `videoUrl`（**错**，产物节点读不出 URL） | `assetUrl` ✅ |
| `docs/123` 能否当"落点索引"用 | 名义上可以（实际四个目录名全不存在） | **文件头明示"不作索引"** + 实际布局对照 |
| `docs/123` 原语名的可信度 | 名义上可以（`relayoutSequential` 等零命中） | 明示"以代码为准" + 抽验三例对照 |
| 本债的锚点连通性 | 锚点指向的文件里**无本债明细** | 已在本文件（新锚点）落地 |
