# 架构日志 · 25 跨区 · ADR 合规横扫（ADR-0001 / 0038 / 0040）· 2026-09-19

> 触发：用户指令「审计 ADR-0040（归属判据）/ ADR-0038（改名字符串边界）/ ADR-0001（既有约定非判据），不符合的登记成债务」。
> 性质：跨区 ADR 合规横扫（元层）。本轮覆盖 3 条 2026-09-19 新建 ADR（0001/0038/0040）；与上一轮（0002–0032）共同覆盖全部现行 ADR。
> 方法：逐条读 ADR「📌 违反时的判据」→ 对 `base/{core,utils,ui,api,storage}` **全量**跑 `refs` 取 fan-in 实测消费者按域归并（ADR-0040 L4）；`git log -S` + diff 验改名批是否动运行时字符串（ADR-0038）；grep 代码层与计划层「照抄/目录即分类/既有约定」措辞（ADR-0001）。
> **本轮关键升级**：上一轮只验了 ADR-0040 点名的 4 个文件；本轮对横切层 **76 个文件全量扫描**，并对每个命中做**桶文件感知复核**（见 §八 排除项），避免 false positive / false negative。

## 探债

| ID | 位置(文件 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-25-2 | `base/core/agentKeys.ts`（refs: agent/conversationState·AgentPanel·agentCore·projectMemoryStore·useAgentChat + App 壳） | ADR-0040 L4：agent 单域键构造件留横切 `base/core` | base/横切层 | 域模块化按目录名判归属易误判为「通用契约」 | 中 | 孤例·同类母体：`base/*` 缺「单域件须迁出」机器守卫 | 迁 `agent/runtime/`（同 agentModelStore 落点） | 待还 |
| TD-25-3 | `base/ui/attachmentCover.tsx`（refs: AgentMessage·AgentPanel，仅 agent 2 处） | ADR-0040 L4：agent 气泡附件封面留横切 `base/ui` | base/横切层 | agent UI 件散落横切层 | 中 | 同 TD-25-2 根 | 迁 `agent/panels/` | 待还 |
| TD-25-4 | `base/utils/volumePolicy.ts`（refs: conversationSnapshot·conversationState，仅 agent/conversation） | ADR-0040 L4：agent 会话体积预算件留横切 `base/utils` | base/横切层 | 会话预算逻辑散落横切层 | 中 | 同 TD-25-2 根 | 迁 `agent/conversation/` | 待还 |
| TD-25-11 | `base/utils/faceMosaic.ts`（refs: FaceMosaicNode·FaceMosaicEditor，仅图片能力 2 处） | ADR-0040 L4：图片能力打码件留横切 `base/utils` | base/横切层 | 图片能力件散落横切层 | 中 | 同 TD-25-2 根 | 迁图片能力域（与 `editors` 同落点） | 待还 |
| TD-25-14 | `base/utils/timeline/sourceTime.ts`·`timeScale.ts`·`videoEngine.ts`·`useMediaLoadFailed.ts`（refs: 全部仅 `video/nodes/VideoProcessNode.tsx` 或 videoEditor UI） | ADR-0040 L4：视频域时间轴/引擎/媒体错误件留横切 `base/utils` | base/横切层 | 视频能力件散落横切层；头注称「多域复用」实为单域 | 中 | 同 TD-25-2 根 + 头注过期（见 TD-25-18） | 迁视频能力域 | 待还 |
| TD-25-15 | `base/utils/encoderProbe.ts`（refs: **仅** `director3d/App.tsx:244`；头注称「director3d+videoEditor 两域共用」实测 videoEditor 零引用） | ADR-0040 L4 + ADR-0001：director3d 单域编码探针留横切 `base/utils`；头注谎称跨域共用未自证 | base/横切层 | 单域件留横切；误导性注释 | 中 | 同 TD-25-2 根 + 头注过期（TD-25-18） | 迁 `director3d/`（或证实确为跨域再留） | 待还 |
| TD-25-16 | `base/utils/imageUpscale.ts` | 已归并：与 TD-18-35 重叠（二扫裁定@见 TD-18-35，随 media 域收口即消），本债撤销 | — | — | — | — | — | 已解决(转 TD-18-35) |
| TD-25-20 | `base/core/nodeSizePatch.ts`（refs: `canvas/groupNodes`·`hooks/useArrangeCanvas`） | ADR-0040 L4：canvas 单域件留横切 `base/core` | base/横切层 | 画布专用件散落横切层 | 中 | 同 TD-25-2 根 | 迁画布域 | 待还 |
| TD-25-12 | `base/core/contracts.ts`（STORAGE_KEYS/EVENTS/NODE_TYPES 各登记行） | ADR-0038：键名冻结声明只在文件头(:5-9)，逐登记行无「键名冻结(ADR-0038)」标注 ⇒ 维护者易顺手统一键名致存量数据失联 | 结构(contracts) | 全仓持久化键寻址 | 低 | 缺逐行护栏（ADR-0038 §违反判据③未落实） | 各登记行补「键名冻结(ADR-0038)」一行标注 | 待还 |
| TD-25-17 | `base/utils/providerUrlAdapters.ts`（refs: `base/panels/sections/ApiSettings.tsx`；`PROVIDER_PROTOCOL_LABELS` 协议显示名表） | ADR-0040 L4：settings 域显示名表留横切 `base/utils` | base/横切层 | settings UI 件散落横切层 | 中 | 同 TD-25-2 根 | 迁设置域 | 待还 |
| TD-25-19 | `base/ui/Toggle`·`Select`·`ContextMenu`·`CustomHandle`·`ErrorBoundary`·`ToolbarButton`·`CometParticles`（refs: 各仅 1 个域消费，无桶文件稀释） | ADR-0040 L4：base/ui 通用件实测仅被单域消费，留横切层 | base/横切层 | 通用件误置横切；找件成本高 | 中 | 同 TD-25-2 根（系统件） | 逐件归位到消费域，或自证确为横切原语 | 待还 |
| TD-25-18 | 母体：`base/utils`/`base/api` 多文件头注声称跨域/通用共用，refs 实测仅单域消费（`encoderProbe`/`videoEngine`/`timeline/*`） | ADR-0001：引用「共用/横切」标签未自证；ADR-0040 L4 根因 | 注释层 | 误导性注释掩盖单域事实 | 中 | 母体（注释过期） | 逐一跑 refs 核对头注声称，不实者改注或迁件 | 待还 |
| — | `base/api/httpClient.ts` | **排除（非债）**：`httpRequest` 被引用 65 处，全经 `base/api/index.ts` 桶文件再导出（refs 只追直接 import 故漏计）；实测为真正横切件 | — | — | — | — | 无需处理 | 非债 |
| — | `base/ui/InlineNameInput.tsx` | **排除（非债）**：消费者 `GeneratedView`+`ResourceLibrary` 同属 `base/panels` 内部共享，非单域 | — | — | — | — | 无需处理 | 非债 |
| — | `base/api/generate.ts` | **观察（不强行发债）**：仅 `agentRuntime.ts` 一处 `import type relayChatMessage`；`generateImage/generateVideo/streamChat` 零引用⇒疑似被 `relayProxy` 取代的死代码，建议独立核查是否删除 | — | — | — | — | 单独排查死代码 | 观察 |
| TD-25-7 | `base/core/videoEditorKeys.ts` | **非债（保持 resolved）**：键构造唯一真源（contracts.ts:623 明示），与 agentKeys 同族键构造器（DOMAIN-MODULES §7） | — | — | — | — | 见下方「开放冲突」 | 已解决 |
| TD-25-10 | `base/utils/providerUrlAdapters.ts` | **误判更正**：原 resolve 为「待删 moot」系误判；该件仍被 ApiSettings 活引用，债已转 **TD-25-17** | — | — | — | — | — | 已解决(转 TD-25-17) |

## 覆盖度表（ADR 维度）

| ADR | 判据点 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号/git) | 债/裁定 |
| --- | --- | --- | --- | --- | --- |
| 0040 L4 | 单域件不许留横切层（base/{core,utils,ui,api,storage}） | 🔴 有债待还 | 本轮(全量扫描) | 全量扫 76 文件：确认单域消费件 **11 笔债**（TD-25-2/3/4/11/14/15/16/17/19 + 母体 TD-25-18）；排除 httpClient(桶文件稀释)/InlineNameInput(base/panels 内共享) | TD-25-2/3/4/11/14/15/16/17/19/18 |
| 0040 L5 | 同形态不拆（FullscreenEditor/ResourceStrip 随 PromptInput 同域） | 🟢 已裁定非债 | 上一轮 | refs：`ResourceStrip` 被 ≥5 节点≥3 域消费；`FullscreenEditor` 被 ≥4 节点≥3 域消费→真横切（L3）。ADR-0040 背景表称「属 PromptInput」为描述过期 | 合规 |
| 0040 L2/L7 | 目录名/文档标签不作判据；App 消费≠横切 | 🟢 已裁定非债(形态复用) | 本轮 | L4 各债根因即「按目录名判归属」已并入 TD-25-2/3/4/11/14/15/16/17/19 根因；本论未另发独立债 | 合规（根因见 L4 债） |
| 0038 | 改名只动符号+路径，运行时字符串（STORAGE_KEYS/EVENTS/NODE_TYPES/prefs键/uploads子目录/URL）默认不改 | 🟡 核心合规·缺逐行护栏 | 本轮(深查)+二扫 | 改名批为纯 `git R100` 移位、键值零变化（`git log -S "STORAGE_KEYS"` 命中仅新增 helper 函数），`contracts.ts:5-9`/`uploadDirs.ts:18` 有冻结注 ⇒ **"改名动键"违规不成立**；但各登记行缺「键名冻结(ADR-0038)」逐行标注（ADR-0038 §违反判据③）→ **有债待还 TD-25-12** | 有债待还(TD-25-12) |
| 0001 | 既有约定非判据（引用前须自证「凭什么成立」） | 🟢 代码层合规 · 🟡 文档层 1 处异味 | 本轮(深查) | 代码层 grep「照抄/既有约定/历来」零作为挡箭牌的判据；但 `docs/plan/114-skill本地文件双向编辑方案.md:144/455` 写「目录即分类」「分类名清洗（目录即分类）」——正是 ADR-0040 L2 禁止的按目录名判归属模式（仅记录为观察，不另发债，因其为计划文档非已落地判据） | 合规(代码) + 文档异味观察 |

## §七 本区域状态

- 本区域状态：审计中（ADR-0001/0038/0040 已裁定；**待还 11 笔**：TD-25-2/3/4/11/14/15/16/17/19 + 母体 TD-25-18）。
- 覆盖账：本轮审 ADR 维度 5（0040 L4 / 0040 L5 / 0040 L2+L7 / 0038 / 0001）/ 共关注 ADR 3 条（0001/0038/0040）/ 未审 0。
- 本轮深审 3 条新 ADR（0001/0038/0040）；与上一轮（0002–0032）合计覆盖全部现行 ADR。
- 待还 11 笔，同根因 = 按目录名/既有标签判归属（ADR-0040 L4 + ADR-0001），建议收口：① 逐件迁到对应域落点；② 在 `base/{core,utils,ui,api,storage}` 增设「单域消费件须迁出」机器守卫（防回潮）；③ 对头注声称「跨域/通用共用」的 base 文件逐一跑 refs 核对（TD-25-18）；④ `contracts.ts` 各登记行补「键名冻结(ADR-0038)」逐行标注（TD-25-12）。
- **开放冲突 1（待用户拍板）**：`agentKeys`(TD-25-2) 与 `videoEditorKeys`(TD-25-7) 判定相反——ADR-0040 背景表把二者都列为 L4 命中，但 `DOMAIN-MODULES §7` 把二者都判为横切契约（键构造 SSOT）。TD-25-7 已按 §7 裁为非债；若保持一致，TD-25-2 也应同样 resolve 为非债（键构造原语）。当前保留 TD-25-2 为待还，等用户裁定。
- **开放冲突 2（faceMosaic / TD-25-11 与二扫矛盾）**：本表 TD-25-11 判 faceMosaic 为 L4 债（仅图片能力 2 处消费）；「二扫」文件据 refs（consumer 在 `canvas/nodes` + `editors` = 2 目录）判为「≥2 域 ⇒ 非债」。按 ADR-0040 L1/L2（域按能力/界面位置定，**目录名不作判据**），`FaceMosaicNode`（画布节点）+ `FaceMosaicEditor`（编辑器）均属「face-mosaic 图片能力」单一能力域 ⇒ 单域 ⇒ 维持 TD-25-11 为债；二扫的「canvas vs editors = 2 域」是按目录名切域，恰为 ADR-0040 L2 所禁。若用户认可目录切域口径则 TD-25-11 应转非债——待裁定。
- 观察项（不强行发债）：`generate.ts` 疑似被 `relayProxy` 取代的死代码（仅 agentRuntime 一处 type-import）；`httpClient.ts` 经桶文件实为真正横切件（已排除）。

## §八 方法论与排除项（防 false positive / false negative）

**全量扫描**：对 `base/{core,utils,ui,api,storage}` 76 个文件逐一 `refs`，取非测试 fan-in，按消费方路径归域（agent/canvas/nodes/imageCap/videoEditor/director3d/scriptbox/settings/resource/panels），统计**不同业务域数**：=1 ⇒ L4 候选；≥2 ⇒ 横切（合规）；=0（仅 base/shell/backend/hooks）⇒ 横切原语（合规）。

**桶文件感知复核（关键，避免 false negative）**：
- `base/api` 存在 `index.ts` 桶文件，`httpClient.ts` 的 `httpRequest` 经桶再导出被引用 **65 处**；`refs` 只看直接 import 会误判为「仅 3 处」。经桶感知后确认 `httpClient` 为真正横切件 ⇒ **排除**，非 L4。
- `base/ui` **无**桶文件（ls 确认无 `index.ts`），故其单域计数准确。

**排除项（已验证非债）**：
1. `httpClient.ts` — 桶文件稀释，真横切。
2. `InlineNameInput.tsx` — 消费者 `GeneratedView`+`ResourceLibrary` 同属 `base/panels` 内部共享。
3. `videoEditorKeys.ts`(TD-25-7) — 键构造 SSOT（contracts.ts:623），与 agentKeys 同族。

**待核查（观察，未发债）**：
- `generate.ts` — 仅 `agentRuntime` 一处 type-import，运行函数零引用 ⇒ 死代码嫌疑，独立排查。
- 横切层其余约 39 件 ≥2 域消费件 + 16 件零业务域消费件（如 storageAdapter/logger/contracts）经判定为合规横切原语，未逐一列示。

> 清理记录（2026-09-19）：TD-25-5 · TD-25-6 · TD-25-8 · TD-25-9（重复/误判）与 TD-25-7(videoEditorKeys·键构造器非债) 已 resolve；TD-25-10(providerUrlAdapters) 原误判「待删 moot」，实测仍被 ApiSettings 活引用，已更正并转 **TD-25-17**；TD-25-13(videoEngine) 与 TD-25-14 重复已 resolve；TD-25-16(imageUpscale) 与 TD-18-35 重叠已归并、nodeSizePatch 重立 **TD-25-20**。
