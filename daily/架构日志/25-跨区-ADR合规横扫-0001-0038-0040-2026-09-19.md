# 架构日志 · 25 跨区 · ADR 合规横扫（ADR-0001 / 0038 / 0040）· 2026-09-19

> 触发：用户指令「审计 ADR-0040（归属判据）/ ADR-0038（改名字符串边界）/ ADR-0001（既有约定非判据），不符合的登记成债务」。
> 性质：跨区 ADR 合规横扫（元层）。本轮覆盖 3 条 2026-09-19 新建 ADR（0001/0038/0040）；与上一轮（0002–0032）共同覆盖全部现行 ADR。
> 方法：逐条读 ADR「📌 违反时的判据」→ 用 `refs` 取 fan-in 实测消费者按域归并、用 `git diff --stat` 验改名批是否动运行时字符串、用 grep 扫「既有约定」措辞。

## 探债

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属(模块) | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-25-2 | `src/components/base/core/agentKeys.ts`（refs: 模块引用 App.tsx·conversationState.ts·AgentPanel.tsx·agentCore.ts·projectMemoryStore.ts·useAgentChat.ts·test，全 agent 域 + App 壳） | ADR-0040 L4：单域（agent）件留在横切层 `base/core`；按目录名/既有标签判归属，未自证「凭什么成立」（ADR-0001） | base/横切层(base/core) | 后续域模块化把 agent 域件翻到 base/ 找件成本高；同形态残留累积 → 横切层越堆越脏 | 中 | 孤例·同类母体：`base/{core,utils,ui}` 缺「单域件须迁出」机器守卫（见 TD-25-2/3/4 同根）；根因形态=ADR-0001（按目录名判归属） | 迁到 `src/components/agent/runtime/`（与 agentModelStore/skillStore 同域落点） | 待还 |
| TD-25-3 | `src/components/base/ui/attachmentCover.tsx`（refs: 模块引用 AgentMessage.tsx·AgentPanel.tsx，仅 agent 域 2 处） | ADR-0040 L4：单域（agent 气泡附件封面）件留在横切层 `base/ui`；按目录名判归属未自证（ADR-0001） | base/横切层(base/ui) | 同上；agent 气泡 UI 件散落横切层 | 中 | 孤例·同类母体（同 TD-25-2 根） | 迁到 `src/components/agent/panels/`（附件封面随 AgentMessage/AgentPanel 同域） | 待还 |
| TD-25-4 | `src/components/base/utils/volumePolicy.ts`（refs: 模块引用 conversationSnapshot.ts·conversationState.ts·test，仅 agent/conversation 域） | ADR-0040 L4：单域（agent 会话体积预算）件留在横切层 `base/utils`；按目录名判归属未自证（ADR-0001） | base/横切层(base/utils) | 同上；会话预算逻辑散落横切层 | 中 | 孤例·同类母体（同 TD-25-2 根） | 迁到 `src/components/agent/conversation/`（与 conversationState 同域落点） | 待还 |
| TD-25-11 | `src/components/base/utils/faceMosaic.ts`（refs: 模块引用 FaceMosaicNode.tsx·FaceMosaicEditor.tsx，仅图片能力 2 处） | ADR-0040 L4：单域（图片能力）件留在横切层 `base/utils`；按目录名判归属未自证（ADR-0001） | base/横切层(base/utils) | 图片打码件散落横切层，找件成本高 | 中 | 孤例·同类母体（同 TD-25-2 根） | 迁到图片能力域（与 `components/editors` 同域落点） | 待还 |
| TD-25-7 | `src/components/base/core/videoEditorKeys.ts`（refs: 模块引用 videoEditor/engine/services/storage/service.ts · test，全 videoEditor 单域） | ADR-0040 L4：单域（videoEditor 工程 KV 键构造真源）件留在横切层 `base/core`；按目录名/既有标签判归属未自证（ADR-0001） | base/横切层(base/core) | 横切层堆积单域件，域模块化按目录名判归属时易误判为「通用契约」 | 中 | 孤例·同类母体（同 TD-25-2 根＝缺「单域件须迁出」守卫） | 随域模块化 StepA 搬迁至 `videoEditor/` 域（符号+路径改，键串冻结 ADR-0038） | 待还 |
| TD-25-10 | `src/components/base/utils/providerUrlAdapters.ts`（refs: 模块引用 base/panels/sections/ApiSettings.tsx · test，仅 settings/provider 单域 UI） | ADR-0040 L4：单域（设置面板协议显示名标签表）件留在横切层 `base/utils`；实为 L3b 退役适配器的残留标签表 | base/横切层(base/utils) | 横切层堆积单域件 | 低 | 孤例·同类母体（同 TD-25-2 根） | 随域模块化搬迁至设置域或并入 ApiSettings 同目录 | 待还 |

## 覆盖度表（ADR 维度）

| ADR | 判据点 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- |
| 0040 L4 | 单域件不许留横切层（base/{core,utils,ui,api,storage}） | 🔴 有债待还 | 本轮 | refs 实测 5 件单域消费却留 base 横切层（agentKeys/attachmentCover/volumePolicy/videoEditorKeys/providerUrlAdapters）⇒ 违 L4 | TD-25-2/3/4/7/10 |
| 0040 L5 | 同形态不拆（FullscreenEditor/ResourceStrip 应随 PromptInput 同域） | 🟢 已裁定非债 | 本轮 | refs：`ResourceStrip` 渲染于 ImageGenerate:676/TextGenerate:450/VideoGenerate:400/TemplateNode:383/StepShots:134（≥5 节点≥3 域）；`FullscreenEditor` 渲染于 ImageGenerate:873/TextGenerate:529/VideoGenerate:546/TemplateNode:438（≥4 节点≥3 域）→ 真横切（L3 满足）。ADR-0040 背景表称「属 PromptInput」与实测不符（描述过期，非债） | 合规 |
| 0040 L2/L7 | 目录名/文档标签不作判据；App 消费≠横切 | 🟢 已裁定非债(形态复用) | 本轮 | L4 三债之根因即「按目录名判归属」已并入 TD-25-2/3/4 根因；本论未另发独立债 | 合规（根因见 L4 债） |
| 0038 | 改名只动符号+路径，运行时字符串（STORAGE_KEYS/EVENTS/NODE_TYPES/prefs键/uploads子目录/URL）默认不改 | 🟢 已裁定非债 | 本轮 | `git diff --stat -- contracts.ts` 无变化；本轮改名批（`base/store`→`agent/runtime`·`resource`·`settings`）仅改文件路径（git R100，内容零改），`STORAGE_KEYS`/`EVENTS`/`NODE_TYPES` 值零变化；ADR-0038 §违反判据①（diff 出现键名变化）不成立 | 合规 |
| 0001 | 既有约定非判据（引用前须自证「凭什么成立」） | 🟢 已裁定非债(本论) | 本轮 | grep `既有约定\|仓里一直这么写\|与现有写法保持一致\|历来如此\|一直这么` src 零命中；代码层无「以既有约定为挡箭牌」措辞。L4 三债之根因形态=ADR-0001（按目录名判归属），已并入 TD-25-2/3/4/11 根因，不另发债 | 合规（根因见 0040 L4 债） |

## §七 本区域状态

- 本区域状态：审计中（ADR-0001/0038/0040 已裁定；TD-25-2/3/4/11 待还）。
- 覆盖账：本轮审 ADR 维度 5（0040 L4 / 0040 L5 / 0040 L2+L7 / 0038 / 0001）/ 共关注 ADR 3 条（0001/0038/0040）/ 未审 0。
- 本轮深审 3 条新 ADR（0001/0038/0040）；与上一轮（0002–0032）合计覆盖全部现行 ADR。
- 待还 4 笔（TD-25-2 agentKeys / TD-25-3 attachmentCover / TD-25-4 volumePolicy / TD-25-11 faceMosaic），均属「单域件滞留横切 `base/` 层」，同根因=按目录名/既有标签判归属（ADR-0040 L4 + ADR-0001），建议收口：① 迁到对应域落点；② 在 `base/{core,utils,ui,api,storage}` 增设「单域消费件须迁出」机器守卫（防回潮）。
- 备注（非债·观察）：横切层仍可能藏更多单域件，应系统性 sweep 而非只验 ADR-0040 点名件。清理（2026-09-19）：TD-25-5 · TD-25-6 · TD-25-8 · TD-25-9（重复/误判）与 TD-25-7(videoEditorKeys·键构造器非域物) · TD-25-10(providerUrlAdapters·待删 moot) 均已 `resolve` 撤销；faceMosaic 为新增有效债（TD-25-11）。
