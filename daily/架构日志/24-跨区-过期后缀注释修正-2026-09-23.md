# 架构日志 · 24 注释漂移横推（跨区） · 2026-09-23

> 本轮：清偿 24 区唯一待还债 `TD-24-10`（描述层文件后缀过期）。
> 主线改动在 11 区（见 `11-跨区-skill上限与芯片格式收口-2026-09-23.md`），本条随该轮顺手清掉。

## 一、链路快照（refs 实证）

```
settings/ 实际文件（ls 实证）：settingRegistry.ts · SettingsFrame.tsx · accountsStore.ts · providerStore.ts
sections/ · providerUrlAdapters.ts
  ⇒ 被注释误称为 .js／.jsx 的三处「文件」——settingRegistry / appSettings / OtherSettings
     现分别住 settingRegistry.ts · base/store/appSettings.ts · settings/sections/OtherSettings.tsx
误称注释 7 行（本轮全部修正）：
  settings/sections/OtherSettings.tsx:32 · settings/settingRegistry.ts:9／:12／:13／:139
  · base/core/contracts.ts:14 · base/store/appSettings.ts:23
```

## 五、探债（本轮清偿明细 · 真源 = `scripts/debt.mjs`）

| TD-ID | 复述处（改前 → 改后） | 处置 |
| - | - | - |
| TD-24-10 | **7 → 0** | 注释里的 `settingRegistry.js`／`appSettings.js`／`OtherSettings.jsx` 一律改为实际后缀 `.ts`／`.tsx` |

**判定**：纯**描述层**修正（注释指错文件名 ⇒ 读者按注释去找会落空）。**不改任何代码路径、不改判据**，
故无探针（SOP §7.1：纯文档/纯结构改动的无行为变化者不硬造探针），以 `grep` 归零为凭证。

## 六、覆盖度表

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| settings/settingRegistry.ts · sections/OtherSettings.tsx · base/store/appSettings.ts · base/core/contracts.ts | 上层/地基 | 深审 | 🟢 | 2026-09-23 | `grep` 后缀残留 = 0 | TD-24-10 已清 |

**覆盖账**：本区本轮只登记上述 4 个文件（TD-24-10 的全部载体）。

## 七、结论与下一步

- 本区域状态：`已验证(无债)`。
- 待还债项已流入 `daily/架构日志/债务.md`：否（24 区清零）。
- DATAFLOW.md：无需改（纯注释，链路无关）。
- 灯：覆盖度表已更新（🟢）；区域级灯由 `index.md` 自动派生。

## 八、交付闸自查（本轮有改动）

| 闸 | 本轮结论 | 可复现凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | 不适用（无判据改动，纯注释） | — |
| 减一 · 兜底净减 | 无兜底增减（0 → 0） | — |
| 减二 · 复杂度实减 | 成立：**7 处错误指路 → 0**（读者按注释找文件即可命中） | 改前 7 命中 → 改后 0 |
| 一诚实 · 失败诚实可见 | 不适用 | — |
| 零炸 · 防回归防副作用 | 成立：仅改注释文本，零代码路径变化；`tsc` 0 错 · lint 0 | 下方凭证 |
| 一凭证 · 证据可复现 | 成立 | 下方 grep |

**凭证（改前 → 改后）**：
```bash
$ grep -rn "settingRegistry\.js\|appSettings\.js\|OtherSettings\.jsx" src/ localTool/src/
（改前 7 行命中）→（改后 0 行）
$ npx tsc --noEmit     → 无输出（通过）
```

**零炸三问**：
1. **哪些行为被改了？** 无（注释文本不影响运行时）。
2. **改前能走的路径仍能走吗？** 是（`tsc` + 相关测试全绿）。
3. **改前会失败的仍失败吗？** 是（未触碰任何逻辑）。

## 九、决策留痕

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度 | SSOT | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 只改注释文本、不顺手重构 settings 模块 | 描述层失真就该只修描述 | — | 7 → 0 | — | 做（无取舍） |
