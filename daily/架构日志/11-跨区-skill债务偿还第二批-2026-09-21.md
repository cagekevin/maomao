# 11-跨区 · Skill 债务偿还（第二批：门面收口 + ADR-0056 读侧补充）

> **触发**：用户「请继续执行未完成的任务」→ 施工 TD-11-19；中途用户裁定「ADR 不许为局部细节新开条，但**允许给既有的那两条补一两句话**」。
> **判据源**：`ADR-0044 §8`（门面按用例）· `ADR-0056 Q2`（真相源）· 本仓「消费者只给引用、真相不许搬到消费者」。
> **范围**：`agent/skill/{index,skillUsage}` · `agent/runtime/skillStore` · `docs/adr/ADR-0056` · `agent/skill/README.md`。

## 一 · 本轮改动

| 项 | 改法（一句话） |
| --- | --- |
| **TD-11-19** | `skillStore` 降为**纯转发壳**（零业务逻辑、零键字面量、**零深引**）：读（列表／启用态／用量）全走门面；启用态写**只剩一条** `setSkillsEnabled`（单个开关 = 批量写 1 个）——此前那个"第二个写 API"已删；新增模块内 `skillUsage.ts`（用例⑧ 使用计数）并由门面导出 |
| **ADR-0056** | 只补**读侧**（用户裁定的形态）：Q2 判据补"真相源同样是**引用**的唯一来源（消费者只许传 id，不许镜像字段）／「谁存在·属于谁」只能由真相源派生（副本不得参与存在性判定）"；决议表 Q2 反例补两条；违反判据加第 5 条（可 grep） |

## 二 · 凭证（可复现）

① **域外深引 = 0**（本债的核心判据）：

```bash
grep -rn "skillRepository" src --include="*.ts" --include="*.tsx" | grep -v "^src/components/agent/skill/"
# 命中仅：skillStore.ts:13（注释：改为"不再深引"）· contracts.ts:410-437（键登记的 store 字段+注释）
# ⇒ **import 命中 0 处**
```

② **启用态写入口唯一**：

```bash
grep -rn "writeEnabledMap\|writeEnabledMany" src --include="*.ts" --include="*.tsx"
# skillRepository.ts:149 定义 · :160/:163 定义与内部调用（唯一实现）
# skill/index.ts:47 门面别名导出
# skillStore.ts:189 注释（说明"第二个写 API 已删"）
# ⇒ 旁路调用点 **0 处**
```

③ **测试与闸**：`skillStore / AgentPanel / useAgentChat.hook / skillInject` = **123 例绿**；`check:dead-code` **0 新增**（本轮门面新增的 6 个导出全部有真实消费者，半态已消）；`check:arch` ✅；`adr audit` **0 问题** · `hygiene` ①空壳 **0** ②近义簇 **0**。

## 三 · 度量（改前 → 改后）

| 口径 | 改前 | 改后 |
| --- | --- | --- |
| 域外深引 `skillRepository` | 1 处（skillStore：5 个函数 + re-export 2 个键） | **0** |
| 启用态**写入口**数量 | 2（skillStore 自写 map／门面批量） | **1**（门面 `setSkillsEnabled`） |
| `skillStore` 的职责 | 转发层 + 使用计数语义 + 启用态本地实现 | **纯转发壳**（零业务逻辑、零键字面量） |
| 使用计数语义的落点 | `runtime/skillStore.ts`（域外） | `skill/skillUsage.ts`（与键的唯一读写口同模块） |

## 四 · 回改原文

- `agent/skill/README.md`：§2 表加 `skillUsage.ts` 行；§4「⚠️ 仍未收口」→「✅ 已收口（TD-11-19）」并给出可核命令；§5 更新遗留表述。
- `docs/adr/ADR-0056`：见 §一。**未新开编号**（用户裁定），`adr search "真相不许搬到消费者"` 命中 **0** 可核。
- `docs/plan/140`：未改（方案没错）。

## 五 · 结清与剩余

- **结清**：TD-11-19（本区累计结清 **8** 条）。
- **仍待还 15 条**：TD-11-17 · 21 · 22 · 24 · 25 · 26 · 27 · 28 · 29 · 34 · 35 · 36 · 37 · 38 · 39。

## 六 · 决策留痕

- **ADR 落盘：用户裁定「不同意新开」** —— 原话「属于过程日记；做完之后又不会影响全局，这只是一个非常局部的细节」，并**授权给既有父条补句** ⇒ 本轮按此执行（补充落在 `ADR-0056 Q2`）。
  自查：我原答卷用"消费者镜像字段是跨域通病"论证"涉及 3 个面"，实为**为落盘找理由**——那条通病已被 `ADR-0056` / `ADR-0044 §8` 覆盖，新开条 = 把同一判据抄第二份。
- **TD-11-22**（"去使用"假动作）：用户已认可推荐 A（真接切换）⇒ 需**先落跨域契约**（设置页写"当前会话选中态" + 请求打开 AI 面板），再接线；不在无契约时直连面板内部 state。
