# 13-跨区-底层 SSOT / 唯一出口横推饱和（2026-09-16）

> **触发**：用户要求「根据现有债务横向推导还有没有其他的债，从底层开始找」。
> **方法**：取现有 258 债中高频母体，自底向上（contracts → eventBus → contentStore → logger → agentKeys → stores）逐族取证，判断底层（地基层）是否还有同母体残留债。
> **结论一句话**：底层覆盖的 6 个高频母体**全部已饱和**（受机器闸兜底或已结清），底层无新增债；这些母体不应再在底层重审。

---

## 一 · 链路快照（本轮实证 · refs / grep）

- `base/core/eventBus.ts` ← **26 fan-in**（refs 实证）；唯一广播通道，文件头红线「禁止自建第二套广播（window.dispatchEvent / 手写 Map 监听）」。
- `base/core/contracts.ts` EVENTS / STORAGE_KEYS ← **24 fan-in**；注册表为 EVENTS / STORAGE_KEYS 唯一真源。
- `base/core/logger.ts` ← **93 fan-in**（refs 实证）；统一日志出口。
- `base/core/agentKeys.ts` ← TD-15-1 收口，`AGENT_KEY_PREFIX` 单源。
- 机器闸已落地：`scripts/check-storage-keys.mjs`（check:keys）· `check:events`（事件名双向校验）· `check:arch` 规则 10（禁 window.dispatchEvent 自建广播）· `catchOk.ts` 豁免白名单。

## 二 · 溯源（本轮横推的母体清单，来自现有债）

| 母体 | 现有债先例 | 本轮落点层 |
| --- | --- | --- |
| M-A 第二份形状 / SSOT 第二份 | TD-22-28/30/35/36/39 · TD-02-23 · TD-15-1 | L0 契约 / L2 store |
| M7 同语义两处写点 | TD-02-23 · TD-15-1 | L0 契约 |
| 静默吞 / 假成功 | TD-02-26/27 · TD-12-10 · TD-22-2 | L0 基建 |
| 第二套广播通道 | TD-22-23 | L0 契约 |
| 统一日志出口绕过 | TD-07-1 | L0 基建 |
| 假护栏 / 幽灵预留 / 死代码 | TD-17-5 · TD-16-1 · TD-18-1/2 | L0 基建 |

## 三 · 探债（逐族结论 + 证据）

### 1 · EVENTS 注册表 SSOT —— 非债（已饱和）
全仓 `subscribe('…')` / `publish('…')` 字面量事件名 22 处，逐一比对 EVENTS 注册表（contracts.ts:73-137）：`agent:task-completed` / `upstream:updated` / `presets-changed` / `resource:sent` / `project:import` / `project:export` / `persist:failed` / `videoeditor:seek` **全部已登记**。check:events 双向校验（TD-13-1 已支持常量引用解析）。
→ 与 TD-13-1 同母体，已闭环，无裸事件名漏网。
**证据**：contracts.ts:73-137 · refs eventBus.ts（26 fan-in）。

### 2 · STORAGE_KEYS 注册表 SSOT —— 非债（已饱和）
check:keys 校验每个 `contentSet/Get` 键须在 STORAGE_KEYS 登记。全仓裸字面量键仅 5 处：
`providerStore.ts:466` `'active_api_endpoint'`（contracts.ts:252 已登记）·
`backupStore.ts:154/252` + `cloudSync.ts:534/605` `'yimao_accounts'`（contracts.ts:333 已登记）。
均为「已登记键的裸字面量」——check:keys 通过，属受机器守卫的既定用法（同 EVENTS 字面量机制），**非 SSOT 第二份**（键名真源仍在 STORAGE_KEYS 注册表，注册表形状由 check:keys 兜底校验）。
→ 与 TD-13-1 同母体，已闭环。
**证据**：scripts/check-storage-keys.mjs · contracts.ts:208-366 · refs contracts.ts（24 fan-in）。

### 3 · agentKeys 存储键构造 SSOT —— 非债（已饱和）
TD-15-1 收口：`AGENT_KEY_PREFIX` 单源（agentKeys.ts:17），禁再拼 `canvas-assistant` 字面量。全仓 grep `canvas-assistant` 仅存于注释（App.tsx:510 / backupStore.ts:52 / cloudSync.ts:898 / conversationState.ts:53/187 / useAgentChat.ts:264）。
`projectMemoryStore.ts:58` 自建 `memoryKey` 构造 `agent_project_memory_v1_{agentKey}`，其产物键已在 STORAGE_KEYS 登记（contracts.ts:355，pattern:true），check:keys 兜底校验，无漂移风险（构造器未收口进 agentKeys 属一致性瑕疵，但受机器闸兜住，不构成债）。
→ 与 TD-15-1 同母体，已闭环。
**证据**：agentKeys.ts · refs 全文 grep。

### 4 · eventBus 第二套广播 —— 非债（已饱和）
全仓 `window.dispatchEvent` / `document.dispatchEvent` 自定义事件广播仅存于注释（eventBus.ts:128 / audio-manager.ts:34 / playback-manager.ts:196-204），皆为 TD-22-23 收口说明。生产代码零自定义 dispatch 广播；check:arch 规则 10 已禁。
**证据**：refs eventBus.ts · grep 全仓。

### 5 · 静默吞 / 假成功 —— 非债（已饱和）
base/core 内 `.catch(()=>…)` 仅 3 处：
`PresetGridView.tsx:46`（catch-ok:BROWSER_API 豁免白名单）·
`upstreamLink.ts:45`（已改 logger 留痕，注释记原全吞）·
`logger.ts:76`（catch-ok:RECURSION_GUARD 递归守卫）。
均落在 catch-ok 豁免通道（TD-02-26/27 成本层收口）。
→ 与 TD-02-26/27 同母体，已闭环。
**证据**：catchOk.ts 豁免表 · grep。

### 6 · 统一日志出口绕过 —— 非债（已饱和）
logger.ts 93 fan-in（统一出口）。`director3d/log.ts` 已由 TD-07-1（2026-09-12）收为 base/core/logger 薄封装；`videoEditor/lib/logger.ts` 是纯转发适配（`import { logger as baseLogger }`，实现 100% 落 base/core/logger，videoEditor/lib/logger.ts:13,30-40）。两处新模块均无独立日志器。
→ 与 TD-07-1 同母体，已闭环。
**证据**：videoEditor/lib/logger.ts:13,30-40 · director3d/log.ts:10,32-45 · `debt show TD-07-1`。

### 7 · base/core 死代码 / 幽灵预留 —— 非债（已饱和）
`node scripts/mv-sync-refs.mjs find-dead src/components/base/core` → 「未发现明确的无引用代码」。TD-18-1/2（DEBUG_ASSET 死别名 / reportDegrade 字符串死兼容分支）已于清偿轮20 解决。
**证据**：mv-sync-refs find-dead 输出 · index 区域 18。

## 四 · 结论

本轮自底向上横推（contracts / eventBus / contentStore / logger / agentKeys / base-core / stores）覆盖现有 258 债的 6 个高频母体，结论一致：**全部已饱和**（受机器闸 check:keys / check:events / check:arch 规则 10 / catch-ok 兜底）或已结清（TD-07-1 / TD-13-1 / TD-15-1 / TD-02-23 / TD-02-26/27）。**底层（地基层）无新增债。**

→ 这些母体不应再在底层重审；若未来复发，只会出现在「新接入模块未走统一出口 / 注册表」（如第三方模块），届时由对应机器闸拦截，无需人工横推。

## 五 · 覆盖度表（本轮实证文件）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `base/core/contracts.ts` | L0 契约 | 深审 | 🔴(未全量) | 横推 09-16 | EVENTS/STORAGE_KEYS 注册表完整+受闸 | 非债(饱和) |
| `base/core/eventBus.ts` | L0 契约 | 深审 | 🟢 | 横推 09-16 | 26 fan-in；零第二广播 | 非债(饱和) |
| `base/core/logger.ts` | L0 基建 | 深审 | 🟢 | 横推 09-16 | 93 fan-in；新模块均转发 | 非债(饱和) |
| `base/core/agentKeys.ts` | L0 契约 | 深审 | 🟢 | 横推 09-16 | canvas-assistant 仅存注释 | 非债(饱和) |
| `base/core/utils.ts` | L0 基建 | 深审 | 🟢 | 横推 09-16 | 30 fan-in | 非债 |
| `base/store/*`（project/resource/skill/cloudSync/task） | L2 store | 抽审 | — | 横推 09-16 | 双写家族仅 TD-02-23 已清 + 受控镜像 | 非债(饱和) |
| `videoEditor/lib/logger.ts` | L2 | 深审 | 🟢 | 横推 09-16 | 纯转发 baseLogger | 非债 |
| `director3d/log.ts` | L2 | 深审 | 🟢 | 横推 09-16 | TD-07-1 已收口 | 非债(已解决) |

> 注：`contracts.ts` 灯维持 🔴（DATAFLOW §十一）——本轮仅横推注册表家族，未全量重审 NODE_TYPES / apiRegistry；如需翻灯需补审这两张表（不在本轮范围内）。
