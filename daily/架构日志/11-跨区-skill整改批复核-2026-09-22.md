# 11-跨区 · skill「补丁思维整改」批复核（2026-09-22）

> **触发**：用户「他现在又改得差不多了，继续用我们刚那些审计方法审他的工作」。
> **对象**：`11-跨区-skill补丁思维整改-2026-09-22.md`（他对我上一轮判定的整改）· 模块 14 文件 · 设置页 · 新增 2 道闸 · 新增测试。
> **判据源**：`ADR-0056 Q2`（真相源）· `ADR-0044 §8`（门面按用例）· `ADR-0053`（幽灵预留）· 六条试金石 + 选落点三问 · 本仓「一凭证 / 一诚实 / 一归」。
> **只读轮**：`src/**` / `localTool/**` 一行未改。**机器可判定的凭证重跑过 2 条**（`npm run lint` / `npm run check:arch`）。

## 一 · 覆盖账（先报读了什么）

| 范围 | 覆盖 |
| --- | --- |
| 模块（14 文件） | **全读**：`index` · `skillTypes`(结构) · `skillManifest` · `skillCatalog` · `skillRepository` · `skillApi` · `skillEntry` · `skillText` · `skillRegistry` · `skillBuiltins` · `skillInject` · `skillHydrate` · `skillPersist` · `skillImport` · `skillMigration` · `skillResource` · `skillCloudSync` · `skillLibraryView`(507 行全读) |
| 新增闸（2 道） | **全读**：`check-arch.mjs` 的「入口文件名字面量」「组哨兵字面量」「禁 exec/execSync」三段判据与豁免 |
| 消费方 | `SkillSettings`（数据层/动作层/渲染判据段全读）· `AgentPanel`（skill 相关 6 段）· `useAgentChat`/`useCanvasAgentTools`/`cloudSync`/`conversationSnapshot`/`skillSelection`/`AgentMessage`（skill 段） |
| 测试 | `skillSettings.test.tsx`(213 行全读) · `skillLibraryView.test.ts`(picker/kind/categoryInput/counts 段) · `skillStore.test.ts`(listAllSkills 段) |
| 文档/账本 | 模块 `README`(全读) · 账本 11 区 · 两轮轮次文件 |

**未跑测试**（用户裁定）；重跑的 2 条是静态闸。

## 二 · 声明 vs 实况（逐条复核）

| 他的声明 | 复核 | 证据 |
| --- | --- | --- |
| 结清 TD-11-48/50/51/52/53/58/61/62（8 条） | ✅ **8 条全为真** | ① `isSkillEnabledIn` 真住 `skillRepository:156`（跟键走），壳 `:102` 与视图 `:257` 都**委托**它 ② `listAllSkills` 真成唯一组合点（`skillRegistry:36`，被 `skillInject:72`/壳/面板共用）③ 白名单+乱码修复真搬进 `skillImport`/`skillText`（壳里实现计数 = 0）④ 新闸「禁 exec/execSync」**一律禁**（判据面 ≥ 问题面，且注释如实记下首版 `e(xec\|execSync)` 永不匹配的坑）⑤ 哨兵真不导出 + `kind` 字段 + 哨兵闸 ⑥ `labelOfGroup`/`kindOfGroup`/`categoryInputOf` 真成唯一映射点（`skillLibraryView:190-199`），消费者真改读 `kind`/`categoryInput` ⑦ `rowFromIndex` 真改成 `state` 形参派生 ⑧ `isLegalDirSegment` 真成了 `input.slug` 的围栏 ⑨ `skillSettings.test.tsx` 真存在且**保持视图层真身**（只 mock 门面 IO） |
| 机器凭证：`lint` 0 · `check:arch` 全绿 | ✅ **这次为真**（上一轮是假凭证） | `npm run lint` → 无输出（0 问题）；新闸的规则文本与豁免理由齐备 |
| **「剩余 3 条：TD-11-55（本批明确不做）· 59 · 60」** | ⚠️ **55 的账陈旧**（代码/测试/README 全完成，账本仍标 [待还]）· **59 他已于 ~01:30 自行 resolve**（我 01:19 读到的 [待还] 是那一刻的真实状态；01:35 复核确认其落地）· 60 确实未做 | **TD-11-55**：`AgentPanel:492-506` 挂 `open` 读 `readDiskSkillPackages()` → `setDiskSnapshot`；`:585` 把快照传给 `buildSkillPickerGroups(all, diskSnapshot)`；视图用 `row.usable` 过滤 ⇒ "仅索引"不进下拉；失败 ⇒ `null` 未决（不把下拉变空）；`skillLibraryView.test.ts:340-354` **两条用例锁死**；`README §5:118` 已标 ✅。**TD-11-59**：`skillEntry.ts` 三件套 + `check-arch:2151` 跨栈对账 + 全量替换（`findSkillEntryFile` 9 处 / `skillEntryRelPath` 2 处），`src/**` 内已无 `'SKILL.md'` 代码字面量 |
| 自曝的"功能空洞"（面板拖 `.md` 导入是谎报） | ✅ 真、且已修 | `AgentPanel:1174` 改走 `importSkillText` 真落盘 + `applySkill(r.skill.id)`；`applySkill` 签名窄化成 `(id: string)` |
| 「`refs`/`grep` 的结论」 | ⚠️ **他自己的两条命令是 shell grep**，本轮我在同一仓**两次**被 shell grep 静默骗过（`isSkillEnabledIn` 明明在用却报 0）⇒ 结论未受影响，但方法有风险 | 见 §六 自检 |

## 三 · 新登记债（探债 · 线索全）

| ID | 位置（文件:行号 / refs） | 债的形态 | 归属 | 爆炸半径 | 利息 | 根因（母体?/孤例） | 大致解法 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **TD-11-64** | `check-arch.mjs:2222`（`/['"`]SKILL\.md['"`]/`，**大小写敏感**）vs `skillImport.ts:121` `:144`（`baseOf(...).toLowerCase() === 'skill.md'`） | **新闸判据面 < 问题面**：闸只抓"规范大小写"的入口文件名字面量，而**大小写不敏感的比较**必须写小写 ⇒ 两处真实例当场漏检 | 17 审计工具链 | 同一"协议名散落"的病只堵了一半：这两处若在改名时漏改，**整个整目录导入静默失效**（`roots` 找不到包 ⇒ 报"没找到 SKILL.md"，用户以为选错文件夹）；且**闸永远绿**，后人无从发现 | 中低 | 孤例（闸的枚举式判据，正是他在 shell 闸上刚批过的形态：**枚举必漏**） | 判据改成"不含路径分隔符、且与 `SKILL_ENTRY_FILE` 同形（大小写不敏感）的字面量"；或让 `skillImport` 复用常量 + `toLowerCase()` 只作用于常量值 | 待还 |
| **TD-11-65** | `skillCloudSync.ts:60`（`listSkillPackages({ withContent: true })`） | **取数范围 > 判据需要**：云写回只比**入口正文**指纹，却把每包 `references/**` 全量拉回 | agent/skill | 云同步一跑就把全库资料拉一遍（库大时是纯浪费）；**同一判据 `detectSkillDrift` 已改用 `readDiskSkillPackages()`（TD-11-39）⇒ 该判据只应用了一半**，形态复发 | 中低 | 孤例（TD-11-39 的判据没横向铺开） | 改调 `readDiskSkillPackages()`（现成的用例函数，`skillApi:106`） | 待还 |
| **TD-11-66** | `skillRegistry.ts:39`（`readSkillList().list`，**丢弃 `ok`**）· 消费者 `skillStore.getAllSkills` → `AgentPanel:396` | **静默降级**：缓存损坏（形状违约 / 读失败）时 `listAllSkills` 只返回内置，**面板无任何提示** ⇒ 用户看到"我的技能全没了"（实为读失败） | agent/skill + agent/panels | 违反 M5（失败不可见）。对照：`SkillSettings` 因为**另读** `readUserSkills()` 才有 `indexError` 面 ⇒ 同一损坏在两处一个可见一个不可见 | 中低 | 孤例（组合层吞掉了它上游的失败信号） | `listAllSkills` 返回 `{ok, list, error}`（或抛给调用方），面板如实提示 | 待还 |
| **TD-11-67** | `SkillSettings.tsx:445`（`group.rows.filter(r => r.state === 'ok').map(r => r.id)`）vs `skillLibraryView.ts:388`（`toggleable = bucket.filter(r => r.state === 'ok')`） | **同一判据两份**："组内哪些行可开关"在视图层（给计数/三态）与消费者（给批量写的 id 列表）各判一次 | settings + agent/skill | 判据一分歧就出"组头显示 2/2 全开，但点开关只动 1 个"这类**跨面不一致**（正是 TD-11-50 的同一形态，只是换了个判据） | 低 | 孤例（新代码里又长出一份） | 视图层把 id 列表一起给出（`SkillGroupView.toggleIds`），消费者只转发 | 待还 |
| **TD-11-68** | `skillLibraryView.ts`（**507 行**，模块内最大） | **职责混居**：行模型（21+ 字段）· 组模型（含 2 个私有哨兵）· 组语义映射（3 个私有函数）· 可用性判据 · 两个用例函数（列表 / 下拉）· 计数 | agent/skill | 与被登记的 **TD-11-60**（`skillCatalog` 325 行 / 六类）**同形且更大**，却未登记 ⇒ 判据只应用到一个文件；继续长下去就是"什么都能放" | 低 | **孤例但形态已登记过**（判据未横向铺开） | 与 TD-11-60 一起做：按"行/组/选择器"拆文件，或先按 TD-11-60 的办法把"实际住着哪几类"写进头声明 | 待还 |
| **TD-11-69** | `skillCatalog.ts:127`（`skillGroupLabel` JSDoc：**"唯一措辞源"**）vs `skillLibraryView.ts:190`（`labelOfGroup` 才是唯一映射点） | **注释漂移**：自称"唯一"的那一处已降级为"只兜 `_未分类`" | agent/skill | 新读者按 JSDoc 去 `skillCatalog` 改措辞 ⇒ 改不到真正生效的那层（`labelOfGroup` 的两个伪组特例） | 低 | 孤例（TD-11-58 搬家时漏回改被搬走那一侧的注释 —— A7「修正必须回改原文」） | 就地回改：`skillGroupLabel` 标"只兜 `_未分类`；完整映射见 `labelOfGroup`" | 待还 |
| **TD-11-70** | `skillPersist.ts:383-398`（`skillMarkdownForExport` 只拼 id/name/description/whenToUse/version） | **导出与导入不对称**：`importSkillText`/`importSkillPackages` 明确"原样带过 `allowed-tools`/布尔字段/未知字段"，而导出**只带 5 个字段** ⇒ 导出→再导入一轮，这些声明就没了 | agent/skill | 用户拿导出的 `.md` 当备份/搬家，会**静默丢声明**（文件看着完整）；且 `unknown` 字段在 hydrate 时**根本没进缓存** ⇒ 必丢 | 低 | 孤例（"原样带过"只做了进口那一侧） | 导出时从**磁盘整包**读 `SKILL.md`（而不是从缓存拼），或在缓存里保留 `unknown` | 待还 |
| **TD-11-71** | `skillLibraryView.ts:497`（`enabledMap: {}` + `:503` `filter(r => r.usable && r.enabled)`） | **隐式耦合**：用"空 map ⇒ 默认启用恒真"来表达"此处不判启用" ⇒ `r.enabled` 的过滤效果**依赖 `skillRepository` 的默认值** | agent/skill | 若"默认启用"改成"默认关"（一个合理的产品决定），本函数**静默返回空下拉**（`items` 全被 `r.enabled` 滤掉），而它的 docblock 恰好承诺"不把下拉变空" | 低 | 孤例（用一条判据的副作用代替"不判"） | 直接 `filter(r => r.usable)`（调用方已按 `isSkillEnabled` 过滤过） | 待还 |

## 四 · 非债裁定（判过但不算 · 留痕）

| 判据点 | 裁定 | 理由 |
| --- | --- | --- |
| `failed` 同时装"形状不全"与"写盘失败"（`skillCloudSync:87-92/115`） | **非债** | 两者是**同一语义**（这个 skill 没写回）的两种原因，且 message 已区分；与 TD-11-46（字段名说"未纳入"、内容却是"已纳入"）**不同族** |
| ~~`freezeSkillTurn` 先 `readSkillList()` 再 `listAllSkills()`（读两次缓存）~~ | ~~**非债**~~ | ⚠️ **本裁定已过期（2026-09-22 · A7 回改原文）**：原理由"第一次读是为区分缓存损坏 vs 为空"成立，但**前提已被 TD-11-66 消掉** —— `listAllSkills()` 现在连 `{ok, error}` 一起给，那次预读**已删**（同一次事实读两遍）。留在原地的删除痕迹见 `skillInject.ts` 的注释 |
| `skillStore.ts` 仍是 legacy 形状适配壳（103 行） | **非债** | 有真实消费者（`AgentPanel`）、有明确纪律（"每搬走一段删一段，不许新增实现"），且**零业务逻辑**已实测成立 |
| `skillEntry.ts` / `SKILL_MAX_FILE_BYTES` 的两侧双写 | **非债** | 两个独立构建产物无法共享模块 ⇒ 双写是结构必然；缺口是"无对账"，而 `CROSS_STACK_CONSTS` + 闸已补上（与 `MAX_SEND_DIM` 同形） |
| `restoreSkillFromIndex` 把"索引正文写回磁盘" | **非债** | 显式动作 + 确认框写明代价（`references/**` 无法恢复）+ 复用 `saveSkillToDisk` 唯一写路径 ⇒ 比"保存的副作用静默复活"正确 |
| 冲突检测的基线是**索引里的 contentHash** | **非债** | 它就是"界面渲染那一版"的指纹，语义正确（外部改过 ⇒ 真冲突）；未决时行不可编辑 ⇒ 结构上不会拿它当基线 |

## 五 · 覆盖度表

| 点位 | 覆盖 | 灯 | 债/裁定 |
| --- | --- | --- | --- |
| `skillLibraryView.ts` | 深审（全读 507 行） | 🔴 | TD-11-68 · 71 ·（58/62/55/56/57 已修 ✅） |
| `skillRegistry.ts` · `skillRepository.ts` | 深审 | 🔴 | TD-11-66（吞损坏）· 50/51 已修 ✅ |
| `skillPersist.ts` | 深审 | 🔴 | TD-11-70 · 53/23/28/46 已修 ✅ |
| `skillCloudSync.ts` | 深审 | 🔴 | TD-11-65 · 24 已修 ✅ |
| `skillImport.ts` | 深审 | 🔴 | TD-11-64 · 34/52 已修 ✅ |
| `skillCatalog.ts` | 深审 | 🟡 | TD-11-69 · 60（他自己登记）· 26/27/34 已修 ✅ |
| `skillEntry.ts` · `skillText.ts` · `skillBuiltins.ts` | 深审 | 🟢 | 无债 |
| `skillInject.ts` · `skillHydrate.ts` · `skillManifest.ts` · `skillApi.ts` · `skillMigration.ts` · `skillResource.ts` · `skillTypes.ts` · `index.ts` | 深审 | 🟢 | 16/17/18/21/37/39/46/49 已修 ✅ |
| `check-arch.mjs`（3 道新/改闸） | 深审 | 🟡 | TD-11-64（入口名闸漏）· 48/25 已修 ✅ |
| `SkillSettings.tsx` | 深审 | 🔴 | TD-11-67 · 58/61/62 已修 ✅ |
| `AgentPanel.tsx`（skill 段） | 深审 | 🟡 | 55 已修 ✅（面板真读了磁盘）· 16/45/62/63 已修/已登 |
| 测试（`skillSettings` / `skillLibraryView` / `skillStore`） | 深审 | 🟢 | **无自证式断言**（断言都依赖真视图层 + 真输入；`'name' in groups[0]` 这类结构断言可证伪） |

## 六 · 收尾自检

| # | 项 | 结论 |
| --- | --- | --- |
| 1 | `src/**` / `localTool/**` 一行未改？ | ✅ 只写本文件 + 账本 |
| 2 | 每笔债挂证据？ | ✅ 全带 `文件:行号`；TD-11-64/65/66 附可复算位置 |
| 3 | 判了母体还是孤例？ | ✅ 8 笔全孤例（未编造母体）；其中 64/65/68 注明"**同形已在别处登记过**（判据未横向铺开）" |
| 4 | 主动纠正自己的错判？ | ✅ **本轮我曾据过期读数判"`disk` 参数生产零传值"，重读 `AgentPanel:585` 后推翻**（见 §二）—— 记在此处，防后人复用错结论 |
| 5 | 全程没跑测试？ | ✅ 测试 0 次；只重跑 2 条**静态闸**（lint / check:arch）作凭证对账 |
| 6 | 判非债也留痕？ | ✅ 6 条（§四） |
| 7 | 账实不符是否纠正？ | ✅ **TD-11-55** 经取证 `resolve`（旧状态确为 `[待还]`）· **TD-11-59** 他已于 ~01:30 自行 `resolve`，本轮复核**确认其落地**并把探针凭证**恢复回账**（我的 `resolve` 一度覆盖了它带 sha 的原注 —— 已用 `edit --field solution` 还原 + 追加复核结论） |
| 8 | 目标在动时的自保动作 | ✅ 本轮**两次**因"读数过期"自我推翻（① `buildSkillPickerGroups(disk)` 我误判为幽灵参数 ② TD-11-59 的账本状态）；⇒ 结论均以 **01:35 重读**为准，并在文中标明过期点 |

## 七 · 结论与下一步

- **总判定：这一批是「把判定落成结构」的高质量整改** —— 8 条结清**全部为真**，且我上一轮判的 3 条未结（P2/P6/P7）全部按**结构方式**结掉（语义跟键走 / 整类禁 exec / 语义提成 `kind`）；连"上一批已改但没落账"和"面板拖 `.md` 是谎报"这两处**自己账目/自己代码**的洞都主动挖出来了。**TD-11-55 与 TD-11-59 也已实际完成**（代码 + 测试 + README 齐），只是账本与轮次文件没跟上。
- **⚠️ 最该改的一条（本轮唯一"形态级"问题）**：**"判据横向铺开"没做完**。三处同形复发——`'skill.md'` 漏检（闸只堵规范大小写）、云写回仍拉全量（TD-11-39 只改了一处）、`skillLibraryView` 507 行未登记（TD-11-60 只登记了 skillCatalog）。⇒ 他修一个点很扎实，但**没问"同一个判据还有哪些地方该一起改"**。这与上一轮"选落点"是**同一根**：**动手前先问"这条判据的适用面有多大"**（比"这个结构会不会被替换"更常见）。
- **账目**：本轮新登记 **8 笔**（TD-11-64 ~ TD-11-71，全为孤例、利息中低为主）；**纠正 1 条陈旧账**（TD-11-55 → `resolve`；TD-11-59 他已在 ~01:30 自行 resolve，本轮只复核并恢复凭证）。
  > ⚠️ **两轮审计的共同教训（给双方）**：**目标在动时，"账实不符"的判词本身会过期**。本轮我据 01:19 的账本判 TD-11-59 "陈旧"，01:35 复核发现他刚补完；上一轮他据 23:0x 的代码判某条"已修"，我也在 23:11 复核时发现已变。⇒ **凡写"账实不符"，必须附读数时间戳**（本文件已照此办）。
- **本区域状态**：**已验证(有债待还)**。
- **优先处置序**：TD-11-64（闸漏，两处真实例）→ TD-11-66（静默降级，用户可见）→ TD-11-65（形态复发，一行改）→ TD-11-67（判据两份）→ 其余低息随轮次清。
