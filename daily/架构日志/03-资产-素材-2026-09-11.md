# 架构日志 · 资产 / 素材 · 2026-09-11（底层地基轮）

> **文件名**：`03-资产-素材-2026-09-11.md`。区域 03 审计——自底向上第 1 轮（类型/契约/utils 地基）。
> 审计视角：数据流（素材产生 → 判型/归一 → store → 卡片 → 拖入节点）。本档只覆盖**底层地基**；中层 `filesApi`/`assetStore` 与 UI 消费待后续轮。
> 实证工具：`refs` + `grep`（2026-09-11 实跑）。

## 一、链路快照（refs 实证，非凭文件名猜）

| 底层文件 | refs 实证生产 fan-in | 判定 |
| - | - | - |
| `src/components/base/utils/mediaType.ts` | 7 处（AssetLibrary/GeneratedView/assetStore/AssetNode/VideoProcessNode/useAssetDropPaste/useConnectedInputs）+ 测试 | ✅ 活跃地基（判型唯一真源，源头收敛正确） |
| `src/types/media.ts` | 经 `types/index.ts` barrel（1 模块引用）+ 字符串引用 169（多为注释）；`mediaType.ts` import `MediaKind/MediaType/ResultKind` | ✅ 活跃（类型真源） |
| `src/components/base/utils/imageUrl.ts` | 36 处（filesApi/generate/taskStore/LazyImage/各节点…） | ✅ 活跃（URL 归一大地基） |
| `src/components/base/utils/previewUrl.ts` | 5 处（FaceMosaicNode/TextGenerate/VideoExtractNode/VideoProcessNode/AgentPanel）+ 测试 | ✅ 活跃 |
| `src/components/base/utils/resultUrlExtractor.ts` | **0 生产 import**，仅自身单测 `resultUrlExtractor.test.ts` | 🔴 **死代码（已删）** |

链路图：素材判型走 `utils/mediaType`，URL 归一走 `utils/imageUrl`；结果 URL 渲染由 localTool 后端落盘 `/files/` 直返 / `t.data[0].url` 契约直读，**无独立提取器**。

## 二、溯源（Phase 1：症状 vs 病根）

- 底层结构总体**能承载**业务：判型（mediaType）/ URL 归一（imageUrl）均收口到唯一实现，符合「源头收敛」。
- 病根误判的样本是 `resultUrlExtractor`：头部注释自称「唯一准出口，今后一律走本模块」，但**全仓 0 生产调用**——这是「假设中的深模块」（为未来预留、从未被采用），是懒惰式收敛的反面。真实链路早已改走 `mediaType` 判型 + 后端 `/files/` 直返，「唯一准出口」从未成立。

## 三、定海（Phase 2：不变式 / 唯一真相源）

- SSOT：判型唯一真值源 = `mediaType.ts`，URL 归一 = `imageUrl.ts`，均唯一，无第二份真相。
- `useConnectedInputs.ts:42` 注释宣称「委托 resultUrlExtractor.resolveMediaType」，实际 import 的是 `mediaType.ts`——**注释漂移**（已修，改指向 mediaType）。

## 四、切割（Phase 3：深模块 / 真缝隙 / 收敛口）

- 无「第二份实现悄悄长」；`resultUrlExtractor` 的「结果 URL 提取」职责本就空置（无消费方），收口回 `mediaType` + 后端契约即正确收敛口，不补第二套实现。

## 五、探债（Phase 4：技术债账本）

| TD-ID | 决策点 / 问题点 | 归类 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - |
| TD-03-1 | `resultUrlExtractor.ts` 生产级死代码（0 import，仅自证单测） | 还债 | 1 模块 + 1 自证测试 | 低 | 判型已收口 mediaType.ts，遗留 `extractResultUrl` 从未被调用，头部「唯一准出口」言过其实 | **已删**模块 + 单测；全仓引用（DATAFLOW/CLAUDE/README/useConnectedInputs 注释）已同步 | ✅ 已解决 2026-09-11 |
| TD-03-2 | `AgentPanel.test.tsx` 的 config `vi.mock` 整模块替换，缺 TD-7 新增 `KV_TIMEOUT` → 测试桩回归（TD-7 只验 d3d/contentStore 两组，漏全量） | 还债 | AgentPanel 测试套件（破坏全量 `test:unit`） | 中 | config.ts 加 KV_TIMEOUT 后，mock 仍只回 `{AGENT_MODELS}` | **已修**：改 `importOriginal` 部分 mock（保留真实导出、仅覆盖 AGENT_MODELS），向前兼容未来 config 新增字段 | ✅ 已解决 2026-09-11 |
| TD-03-3 | TD-03-2 的修复本身残留类型缺陷：`const actual = await importOriginal()` 未收窄类型（vitest 返回 `unknown`），`{...actual}` spread 报 `TS2698` → `type-check:tests` 长期 1 错（运行时无碍、`type-check` 总门禁红） | 还债 | 1 处（tests/tsconfig 编译门） | 中 | 未按既有范式（`useAssetMoveToFolder.test.tsx` 注释「importOriginal 返回 unknown，直接 spread 报 TS2698」）断言 `as Record<string, unknown>` | **已修**：补 `as Record<string, unknown>` 收窄 + 注释留痕；`npm run type-check` src+tests 双向 0 错 | ✅ 已解决 2026-09-11 |

## 六、结论与下一步

- 本区域（底层）状态：**底层已验证(无债已还)**；区域 03 整体 = `审计中`（中层 `filesApi`/`assetStore` 与 UI 消费**待后续轮审计**）。
- 三项已还债的决策价值：已同步进 `spec/TECH-DEBT.md`（TD-14/TD-15，均标 `[已解决 2026-09-11]`）；**TD-03-3 为 2026-09-11 AI 助手区域审计时发现并当场还清**（前两项之外的 TD-03-2 残留类型错，`type-check:tests` 由 1 错恢复到 0 错）。
- DATAFLOW.md 是否需要刷新：**是，已就地更新**（2026-09-11，生成链路段删 `resultUrlExtractor ◄── useConnectedInputs`，改为 mediaType + 后端 /files/ 契约，并附 refs 实证注）。
- 验证：`type-check` 0 错 · `test:unit` 189 文件 / 2405 用例全绿 · `build` ✅ · `test:smoke` ALL PASS。
- 下一步：区域 03 中层——`api/filesApi.ts`（上传/刮削/落盘）→ `api/localToolApi.ts` → `store/assetStore.ts`（`migrated/` 目录本地化落盘）。