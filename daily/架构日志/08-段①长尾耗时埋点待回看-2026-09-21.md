# 08-段①长尾耗时埋点待回看-2026-09-21

> 本文件 = 债务登记轮次（探债 + 覆盖度）。触发：用户发现图片生成「发出去之前」偶发极长耗时，
> 已在 localTool 提交路径加 `submitTiming` 埋点，登记一笔「待积累样本后回看归因」的跟踪债。
> 性质：本轮回填一笔债 + 覆盖度更新，**非深审**。
>
> 🎯 **用户诉求（第一眼先看这条）**：诊断并解决「段① 长尾慢」（图片生成偶发数十~数百秒卡在「发送中」），
> 不是流程/文档本身。下面所有查询、基线、防骗红线都是为这个「慢」做诚实归因的护栏——目标永远是定位长尾谁主导、再偿债。

## 一、探债

| ID | 位置(文件:行号 / refs) | 债的形态 | 归属 | 爆炸半径 | 利息 | 根因(母体?/孤例) | 大致解法(一句话) | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-08-46 | relay-poll.ts:runDirectSubmit（落 `submitTiming`）· lovart/index.ts:submitLovartTask（mode/attach/sendChat 计时）· lovart_attachments.ts:resolveLovartAttachments（下载回环+传CDN） | 段①(发送 Lovart 成功前本地耗时)长尾未归因：图片任务 n=477 avg6.8s median4.8s 但 max249s 长尾离谱 | 结构(性能) | 图生图/多参考图生成偶发数十~数百秒卡在「发送中」，易被误判卡死 | 中 | 孤例(现象)；根因待样本确认，头号嫌疑 attach=参考图下载+传CDN | **归因优先于动手**：回看 submitTiming 定长尾谁主导。若只是网络问题（CDN/Lovart 链路延迟，本地代码不可控）→ **债务就地关闭，不出馊主意**；仅当确认是本地代码可控（如串行同步处理参考图）才考虑预压缩/并发上传/异步补图 | 待还 |

## 二、覆盖度表

| 文件层 | 覆盖状态灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- |
| localTool/src/relay-poll.ts | 🔴 有债待还 | 08-段①长尾耗时埋点待回看-2026-09-21 | runDirectSubmit 落 `submitTiming`（实测 max249s） | TD-08-46 |
| localTool/src/ai-relay/providers/lovart/index.ts | 🔴 有债待还 | 同上 | submitLovartTask 子步骤计时 | TD-08-46 |
| localTool/src/ai-relay/providers/lovart/lovart_attachments.ts | 🔴 有债待还(嫌疑点) | 同上 | resolveLovartAttachments 下载回环+传CDN（长尾头号嫌疑） | TD-08-46 |

> 注：三文件此前均为 area 08 深审(无债)；本轮仅因新增「待回看」跟踪债而挂 TD，非新审出代码债。

## 七、状态行

- 本轮：登记 1 债（段①长尾耗时待回看），覆盖 3 文件。
- 覆盖账：本轮回填 3 文件（均挂 TD-08-46）；其余 area 08 文件沿用既往 🟢 深审，无状态变化。
- 下一步（用户执行）：`cd localTool && npm run build && npm run start` 使埋点生效 → 正常使用几天 →
  `node scripts/seg1_query.cjs` 复跑，看「子步骤」段（queue/egress/mode/attach/sendChat）谁主导长尾，再决定偿债。

## 八、现状基线（冻结快照 · 已验证 · 对比锚点）

> **本节能比什么、怎么比，下面写死。所有数字来自 2026-09-21 真跑 `scripts/seg1_query.cjs`，不再重查、不补新数。**
> 段① = 发送 Lovart 成功前本地耗时 = `(submit_ack_at - _relayPoll.startedAt)/1000` 秒。

### 8.1 查询口径（重跑必须一字不差，否则不可比）
- 数据源：`tasks` 表，过滤 `submit_ack_at IS NOT NULL AND request_data LIKE '%_relayPoll%'`。
- 再解析 `request_data` JSON，只留 `capability==='image'` 的行。
- 段① = `(submit_ack_at - _relayPoll.startedAt)/1000`。
- ⚠️ 这套口径下，**n 会随时间增长**（库里 image 任务越积越多）。所以「基线」是冻结在下面的快照，不是「永远的 477」。

### 8.2 冻结基线（2026-09-21）

| 指标 | 值 | 说明 |
| --- | --- | --- |
| 样本数 n | **477** | 截止 2026-09-21 库内全部 image 任务，样本足够稳 |
| 均值 avg | **6.8s** | 被长尾拉高，参考用 |
| 中位数 median | **4.8s** | 典型用户体感，对比主锚点 |
| 最大值 max | **249s** | 长尾最坏情况，对比主锚点 |
| 子步骤明细 | **无样本** | `submitTiming` 新增埋点，仅基线之后新任务才上报 |

### 8.3 怎么对比（回看 / 偿债后看效果）
1. **同口径重跑**：`cd localTool && node scripts/seg1_query.cjs`，得新 n / avg / median / max。
2. **比谁**：别只比 avg（n 变大就会漂）。主看 **median 与 max** 是否下降；长尾是否收敛看 max 与「最慢 10 条」段。
3. **基线 vs 修复效果**（若要验证某次偿债真的有用）：
   - 基线 477 = 修复**前**全量；修复**后**应单独看「基线日期之后、带 `submitTiming` 的新队列」的子步骤段，
     直接比新队列的 median/max，避免被历史样本稀释。
4. **子步骤归因对比**：只有新任务有 `queue/egress/mode/attach/sendChat` 明细。等子步骤样本够（建议 n≥50）再下结论，
   看哪一段占 `totalMs` 比最大；目前 attach 仅头号嫌疑，非结论（见十）。
5. **不可比情形**：若有人改了 `seg1_query.cjs` 的过滤/口径，旧基线即失效，需在 8.2 重记新基线并标注日期。

## 九、重放方法（下次回看照抄，别另起炉灶）

1. 库在哪：`%USERPROFILE%/.maomao-localtool/localtool.db`（sql.js 内存库，**不是 mysql/postgres，没有 host/账号/密码**，
   别去 `psql`/`mysql` 连，连不上）。
2. 怎么查：`cd localTool && node scripts/seg1_query.cjs`（脚本自己 `require('sql.js')` 读文件、解析 `request_data` JSON、
   过滤 `capability==='image'`、算段①与子步骤统计并打印）。
3. 想改口径：只改 `scripts/seg1_query.cjs` 里的 SQL/过滤，别在对话里手写查询——手写极易因「查询被取消/没返回」而编出假数。

## 十、防骗红线（给下次 AI：先看这条再说话）

- 🔴 **「109s（或任何具体秒数）来自一次被取消、从未真正返回的查询」——是凭空编造，已作废。段①真实最大值 = 249s。**
- 🔴 任何段①数字（avg/median/max/n）**必须以跑通 `seg1_query.cjs` 的输出为准**；没跑就给数 = 编，等于骗用户。
- 🟡 「attach（参考图下载+传 CDN）是长尾头号嫌疑」目前只是**嫌疑/假设**，不是结论；结论要等子步骤样本够了再下。
- 🟡 本债状态是「待还/待回看」，不是「已定位根因」；偿债动作（预压缩/并发上传/异步补参考图）等归因确认后再做。
- 🟢 **闭环判定（回看时照此执行）**：重跑 `seg1_query.cjs` 看子步骤谁主导长尾。
  - 若主导项是**网络链路**（CDN 上传耗时、Lovart API 往返延迟，本地代码无阻塞/无串行浪费）→ **债务就地关闭（状态改「已消」/ resolved）**，原因记「纯网络问题，本地不可控」，**不要给用户出预压缩/并发上传/异步补图这类馊主意**。
  - 若主导项是**本地代码可控**（如参考图串行同步下载+上传、可并发未并发、可先返回后补）→ 才进入偿债，按具体瓶颈选解法。
  - 判断标准一句话：**瓶颈在「我们的代码效率」才修；瓶颈在「网/对方服务」就关债，别瞎改。**
