# 16 跨区 · 消费者自造 SSOT 第二份续查（producer 提供 / consumer 自造）— 2026-09-17

> **触发**：用户「查一查哪些是本来生产者应该提供的，但消费者自己权宜了一套」＝ SOP 的「SSOT 第二份」＋「绕过唯一入口」两类。
> **根因区绑 16**（utils 工具层 · M4 母体：同一真相抄多份）；idGen 那笔是 TD-18-6 收口的漏检回潮（@见 18 区）。
> **结论唯一落点 = 本文件**；其余区 `@见` 本文，不另开日志。

---

## 一、链路快照（取证路径，自底向上）

```
种子母体：M4 同一真相抄成多份（账本高频 · #2 SSOT 第二份）
└→ 以「canonical producer 已被提供、consumer 仍自造」为判据，对 4 个已知唯一入口做 grep 复核：
   idGen.generateId/generateUUID（base/core/idGen.ts:8 唯一入口，禁散落 Math.random）
   │  └→ 消费者自造：scriptBoxPlaybookStore.ts:166 `custom-${Date.now()}${Math.random()}`
   │              ＋ scriptBoxPlaybookManager.tsx:121 `pb-import-${Date.now()}${Math.random()}`
   fileNameFromUrl（base/core/utils.ts:52 唯一入口，TD-16-14 收口）
   │  └→ 消费者自造：librarySource.ts:68 `rawUrl.split('/').pop()`
   │              ＋ link-media-refs.ts:96 `ref.url.split('/').pop()`
   detectFileType（base/utils/assetType.ts，TD-16-18 收口）
   │  └→ 消费者自造：useAssetDropPaste.ts:157/488/552 `type.startsWith('image/'|'video/'|'audio/')`
   idGen ↔ director3d/project.ts:359 私有 `uid`（外部移植域，待裁定）
反例（已收口/非债，留痕防重审）：STORAGE_KEYS 全走 contracts.KEY_YIMAO_*；resolveAssetDisplayUrl/toAbsoluteFileUrl 被广泛复用；
   ResourceLibrary.tsx:202 `'migrated'` 仅注释（真实走 libraryBrowseArgs）；GeneratingOverlay.tsx:219 Math.random 仅 UI 提示
```

## 二、溯源（母体）

- **M4（SSOT 第二份）**：同一真相在消费层各起一份。16 区「TD-16-4..14 一收全消」与 18 区「TD-18-6」已立先例。
- 本轮 = **漏检残党 + 新 domain 回潮**：media provider 层（librarySource/link-media-refs）与 scriptBox/拖拽层在收口 sweep 之后仍各自内联了一份 canonical 早已提供的能力。

## 三、定海（唯一真源/契约）

- `idGen.generateId(prefix)` / `generateUUID()`：`idGen.ts:8` 明文「集中 ID 生成 —— 唯一入口，禁止散落 Math.random()」。
- `fileNameFromUrl(url)`：`core/utils.ts:52`，统一「URL→文件名」唯一实现（剥 ?# ＋ decode 一次）；其文件头注释（L42）**点名** `u.split('/').pop()` 这种「更简陋形式」正是被收口的问题形态。
- `detectFileType({name}|{type})`：`assetType.ts`，类型判型唯一入口；`{type}` 入参即处理 MIME 串（image/video/audio）。

## 四、切割（实证结果）

| # | 发现 | 实证 | 判定 |
| - | --- | --- | --- |
| A | `librarySource.ts:68` `item.name \|\| rawUrl.split('/').pop() \|\| '素材'` | 读 L59-68：`rawUrl` 来自 library（`/files/migrated/...`），`fileNameFromUrl(rawUrl)` 是 canonical；此处内联 `split('/').pop()` 漏 decode/?# 剥离，正是 SSOT 注释点名的形态 | 新债（SSOT 第二份） |
| B | `link-media-refs.ts:96` `ref.name \|\| ref.url.split('/').pop() \|\| 'media'` | 读 L96：同 A，ref.url 为 `/files/` 绝对地址 | 新债（SSOT 第二份） |
| C | `useAssetDropPaste.ts:157/488/552` `type.startsWith('image/'\|'video/'\|'audio/')` | 读 L155-158/484-491/550-552：剪贴板/拖拽 MIME 前缀手写三分支，绕 `detectFileType({type})`；与 TD-16-18 已收口的 ImageBoxNode/AgentPanel/AssetNode 同一形态，属**漏检回潮** | 新债（回潮 @见 TD-16-18） |
| D | `scriptBoxPlaybookStore.ts:166` `custom-${Date.now()}${Math.random()}` ＋ `scriptBoxPlaybookManager.tsx:121` `pb-import-${Date.now()}${Math.random()}` | 读 L166/L121：持久化 playbook id 手写 `Date.now()+Math.random()`，绕 `idGen.generateId(prefix)`；与 TD-18-6 收口结论（10 消费文件改走 idGen）冲突＝**漏检回潮** | 新债（回潮 @见 TD-18-6） |
| E | `director3d/project.ts:359` 私有 `uid = () => ${Date.now()}-${Math.random()}` | 读 L359：功能＝第二份 idGen；但 director3d 为外部移植域，有维护宽限（CLAUDE.md §3.0 更新） | 待裁定（边界：dir3d 域是否并入 idGen，需用户拍板） |
| F | `core/utils.ts:216` `clamp` 唯一入口；8 处消费点内联 `Math.min(Math.max())` | 读 L216：canonical `clamp(v,lo,hi)`；drop-utils:339 / PromptInput:204 / promptMention:117 / ImageZoomDialog:105 / DepthVideoModal:179 / scriptBoxEngine:2118 / AgentPanel:1112 / ImageBoxNode:102 均手写等价 `Math.min(Math.max(x,lo),hi)`，director3d 已在 TD-18-5 收口改用 clamp，其余 8 处漏网 | 新债（SSOT 第二份 @见 TD-18-5） |
| G | `ResourceLibrary.tsx:202` `'migrated'` | 读 L202/L205-210：仅注释；真实 fetch 走 `libraryBrowseArgs(currentFolder)`（与导入弹窗同一份），已收口 | 非债（注释，不重审） |
| H | `GeneratingOverlay.tsx:219` `Math.random()` | 仅 UI 随机提示轮播，非身份 id，无持久化 | 非债 |
| I | 存储键 `yimao_*` | 全仓走 `contracts.KEY_YIMAO_*`（search 实测 42 处均 import，无裸字面量） | 非债（已收口） |
| J | `resolveAssetDisplayUrl` / `toAbsoluteFileUrl` | 101 处命中均走 canonical（无裸 `/files/` 构造内联） | 非债（已收口） |
| K | `engine/lib/time.ts:8` `formatTimeCode` 唯一入口；ruler-utils:234 `formatTimestamp` ／ media.tsx:661 `formatDuration` ／ VideoProcessNode:124 `formatDuration` | 读 time.ts:8：canonical 时间码格式化；三处各重写 mm:ss（等同 formatTimeCode MM:SS），director3d:961 为外部域副本 | 新债（SSOT 第二份，跨域 area 22 @见 TD-22-64）**→ 2026-09-18 复核修正：三处与 canonical **判据不同**（取整+小时进位／帧输入／紧凑+小数秒），**真重复只有 media.tsx ↔ VideoProcessNode 一对**，已收口** |
| L | `base/canvas/nodeDefaults.ts`（`withNodeSize`/`applyNodeTypeDefaults` 唯一入口）；useCanvasAgentTools:429 / canvasPlanExecutor:535 / depthVideo/spawn:64 | 读 nodeDefaults:40：imageGenerateNode 默认 420×420；三处造节点硬编码 420×420（前两者逐字重复）/ 420×380（后者与 director3dNode 420×300 漂移），均未走 withNodeSize | 新债（SSOT 第二份） |
| M | `base/canvas/nodeDefaults.ts`（`withNodeSize`/`applyNodeTypeDefaults` 单源表）；GridSplit:644 / GridMerge:471 / VideoProcess:933,964,995 / Director3D:139 / Panorama:417 / FaceMosaic:132 | 读 nodeDefaults:39：assetNode/imageBoxNode 等类型未进 NODE_TYPE_DEFAULTS；8 处 spawn 硬编码 style 尺寸且互不一致（assetNode 320×320×2 / 420×380 / 320×200 / 360×260；imageBoxNode 420×420×2），均压过/替代单源表 | 新债（SSOT 第二份 @见 TD-16-43） |
| N | `base/utils/clipboard.ts:343` `copyText`（封装 `navigator.clipboard.writeText` + ClipResult 错误处理）；CameraStudioPanel:507 / export-button:254 / media:523 / useTableSelection:69 / AssistantTablePanel:291 / ChatMarkdown:180 / AgentMessage:313 / ImageBoxNode:455 / VideoExtractNode:380,403 | 读 clipboard.ts:343：canonical 纯文本复制；10 处手写 `try{ navigator.clipboard.writeText(x) }catch` 绕 copyText，违反 utils.ts:91 禁手写约定 | 新债（SSOT 第二份 @见 TD-16-44） |
| O | `base/utils/clipboard.ts` `downloadBlob`/`downloadUrl`（封装 `<a download>`+revoke）；director3d/App.tsx:1835 / 1887 / 1994 | 读 clipboard.ts:353/372：canonical 下载；director3d 外部域手写 `createObjectURL`+`link.click()` 下载（另 L1836 文件名清洗 `replace(/[\\/:*?"<>|]+/g,'-')` 亦第二份），绕 downloadBlob | 待裁定（边界：dir3d 外部域，@见 TD-18-7） |
| P | `base/core/utils.ts` `debounce`/`throttle`/`useDebouncedEffect` 唯一入口；main.tsx:29-74（全局错误 throttle 带 suppress 计数）/ transcription/worker.ts:210-241（手动时间门）/ videoEditor/.../save-manager.ts:116（类内自写 debounce） | 读 utils.ts：canonical 防抖/节流；3 处 bespoke 手写（语义特殊：suppress 计数 / worker 时间门 / 类内 save 语义），疑违反 utils.ts:91 禁手写 | 待裁定（疑似第二份，语义特殊需用户拍板） |
| Q | `base/core/config.ts`（env 单源，L4 明令"禁止散落"）；useConnectedInputs:492 / director3d/models:81(BASE_URL) / project:517 / contentStore:255 / modalLayer:129 / conversationAiState:104 / conversationState:122 / tableWorkspaceState:120 / videoEditor/editor-constants:1（自建 IS_DEV） | 读 config.ts:4：env 读取单源；但 config.ts 未导出 IS_DEV，9 处直接裸 `import.meta.env.DEV` / `process.env.NODE_ENV`（含 videoEditor 域自建 IS_DEV 常量），违反"集中"约定 | 新债（SSOT 第二份 @见 TD-16-45） |
| R | `base/core/utils.ts:348` `formatBytes`（KB/MB/GB 三档）；useImageHoverActions:113（手写 `(n/1024).toFixed(0)KB`）/ link-media-refs:93（手写 `Math.round(x/1024/1024)MB`） | 读 utils.ts:348：canonical 字节→人类可读；2 处手写除法+toFixed 绕它（toFixed(0) 与 canonical toFixed(1) 漂移；link-media-refs 同文件已在 TD-16-39 列过），纠正此前"无第二份"误判 | 新债（SSOT 第二份 @见 TD-16-46） |

## 五、探债（明细真源）

| ID | 决策点/问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-16-39 | media provider 层显示名 fallback 内联 `split('/').pop()` 绕 `fileNameFromUrl`（librarySource:68 / link-media-refs:96） | 增债 | 结构债 | 2 处消费点（可引用媒体源 / 剪辑器引用登记） | 中 | M4：URL→文件名唯一实现在消费层被复制（漏 decode/?#） | 改走 `fileNameFromUrl(url) \|\| '素材'/'media'`；并入 TD-16-14 母体一收 | 待还 |
| TD-16-40 | 拖拽/剪贴板 MIME 判型内联 `startsWith image/video/audio` 绕 `detectFileType`（useAssetDropPaste:157/488/552） | 增债 | 结构债 | 3 处消费点 | 中 | M4（回潮 TD-16-18）：收口 sweep 漏掉 scriptBox/拖拽层 | 改 `detectFileType({type: it.type})?.kind === 'image'` 等；并入 TD-16-18 | 待还 |
| TD-16-38 | 持久化 playbook id 手写 `Date.now()+Math.random()` 绕 `idGen`（scriptBoxPlaybookStore:166 / scriptBoxPlaybookManager:121） | 增债 | 结构债 | 2 处持久化 id 生成（自定义/导入工作流） | 中 | M4（回潮 TD-18-6）：idGen 收口漏掉 scriptBox 域 | 改 `generateId('custom')` / `generateId('pb-import')`；并入 TD-18-6 收口范围 | 待还 |
| TD-18-7 | director3d 私有 `uid` 第二份 idGen（project.ts:359） | 增债 | 结构债 | 1（dir3d 内部 uid 调用） | 低 | M4：外部移植域未并入 idGen | 评估并入 `idGen.generateUUID()`（考虑 dir3d 外部域维护边界）；待用户拍板 | 待还 |
| TD-16-41 | 钳制 clamp 第二份：8 处内联 Math.min(Math.max()) 绕 core/utils.clamp（drop-utils / PromptInput / promptMention / ImageZoomDialog / DepthVideoModal / scriptBoxEngine / AgentPanel / ImageBoxNode） | 增债 | 结构债 | 8 处消费点 | 低 | M4：数值钳制唯一实现在消费层被复制（公式等价，无行为漂移） | 改 `clamp(x, lo, hi)`；并入 TD-18-5 收口范围（与 director3d 同 canonical） | 待还 |
| TD-22-64 | 时间码格式化第二份：formatTimeCode 为唯一入口，ruler-utils:234 formatTimestamp 与 media.tsx:661 / VideoProcessNode:124 本地 formatDuration 重写 mm:ss（director3d:961 外部域副本） | 增债 | 结构债 | 3 处消费点（含 dir3d 外部域） | 中 | M4：时间码格式化唯一实现在消费层被复制（mm:ss 逻辑重复） | **【已解决 2026-09-18 · 收窄结清】** 取证后**真重复只有一对**：`media.tsx` ↔ `VideoProcessNode` 的**同名同义** `formatDuration`（已收口到 `base/core/utils.ts`，两域唯一可共用层 —— 规则 2 反向判据）。**原拟解法被否**：`formatTimeCode('MM:SS')` 的分钟到 60 进位且**丢小时**（3661s → `'01:01'`），照改会让 ≥1h 的标尺/时长**同时出错** ⇒ 另三处判据不同（引擎标尺「取整 + 小时进位」· dir3d「帧输入 + 帧精确」· base `rulerTicks`「紧凑 + 小数秒」），**改判非债**。顺带删一处发明值（非有限 → 哨兵 `'—'`）。见 `22-跨区-时长格式化收口-2026-09-18.md`；标尺两套并存另登记 `TD-22-65` | 已解决 |
| TD-16-42 | 节点结构默认 nodeDefaults 第二份：useCanvasAgentTools:429 / canvasPlanExecutor:535 造 imageGenerateNode 硬编码 420×420（与 nodeDefaults:40 逐字重复）；depthVideo/spawn:64 硬编码 420×380（与 director3dNode 420×300 漂移） | 增债 | 结构债 | 3 处节点创建点 | 中 | M4：节点结构默认唯一实现在消费层被复制（图节点 420×420 重复 / 深度视频 420×380 分歧） | 改 `withNodeSize('imageGenerateNode')` / `applyNodeTypeDefaults`；并入 nodeDefaults 单源表 | 待还 |
| TD-16-43 | 节点结构默认 nodeDefaults 第二份（续）：assetNode/imageBoxNode 等类型未进 NODE_TYPE_DEFAULTS，8 处 spawn 硬编码 style 尺寸且互不一致（assetNode 320×320×2 / 420×380 / 320×200 / 360×260；imageBoxNode 420×420×2），FaceMosaic:132 甚至裸 setNodes 绕 commitNewNodes | 增债 | 结构债 | 8 处 spawn 点（含 1 处裸 setNodes） | 中 | M4：节点结构默认唯一实现在消费层被复制（类型缺表项 + 尺寸散落分歧） | 补 NODE_TYPE_DEFAULTS 表项 ＋ 改 `withNodeSize(type)` 委托；FaceMosaic 改走 spawnAndCommit | 待还 |
| TD-16-44 | 剪贴板纯文本复制 copyText 第二份：clipboard.ts:343 `copyText` 为唯一封装（ClipResult+错误处理），10 处裸 `navigator.clipboard.writeText` 绕它（CameraStudioPanel:507 / export-button:254 / media:523 / useTableSelection:69 / AssistantTablePanel:291 / ChatMarkdown:180 / AgentMessage:313 / ImageBoxNode:455 / VideoExtractNode:380,403），违反 utils.ts:91 禁手写约定 | 增债 | 结构债 | 10 处消费点 | 低 | M4：剪贴板写入唯一实现在消费层被复制（手写 try/writeText，错误处理/反馈不统一） | 改 `copyText(text)`；并入 clipboard 真源 | 待还 |
| TD-16-45 | env 配置读取未收口：config.ts（L4 明令"所有 import.meta.env 读取必须集中此文件"）声明单源，但自身未导出 IS_DEV，9 处业务代码直接裸 `import.meta.env.DEV` / `process.env.NODE_ENV`（useConnectedInputs:492 / director3d/models:81(BASE_URL) / project:517 / contentStore:255 / modalLayer:129 / conversationAiState:104 / conversationState:122 / tableWorkspaceState:120 / videoEditor/editor-constants:1 自建 IS_DEV），违反集中约定 | 增债 | 结构债 | 9 处 env 读取点 | 低 | M4：env 单源声明但缺 IS_DEV 导出，消费者绕过集中点直读 Vite/Node env（约定违反，当前无行为漂移） | config.ts 增 `export const IS_DEV = import.meta.env?.DEV === true`（及 BASE_URL 出口），9 处改 `import { IS_DEV }` | 待还 |
| TD-16-46 | 字节格式化 formatBytes 第二份：utils.ts:348 `formatBytes`（KB/MB/GB 三档，toFixed(1/2)）为唯一封装；useImageHoverActions:113 手写 `(n/1024).toFixed(0)KB`、link-media-refs:93 手写 `Math.round(x/1024/1024)MB` 绕它（toFixed(0) 与 canonical toFixed(1) 漂移；link-media-refs 同文件已在 TD-16-39 列），纠正前轮"无第二份"误判 | 增债 | 结构债 | 2 处字节格式化点 | 低 | M4：字节→人类可读唯一实现在消费层被复制（除法+toFixed 手搓，精度/档位漂移） | 改 `formatBytes(n)`；并入 utils 真源 | 待还 |

> ID 由 `debt.mjs add` 取号，登记后回填真实 TD 号（下表 ID 占位）。

## 六、结论 + 覆盖账

1. 续查补登新债 9 笔（area 16：fileNameFromUrl / detectFileType / idGen / clamp / nodeDefaults×2（建节点 TD-16-42 ＋ 派生点 TD-16-43）/ copyText / env读取 / formatBytes（纠正前误判非债） 消费者自造）＋ 1 笔边界（area 18：director3d uid，待拍板）＋ 1 笔边界（area 22：director3d 内联下载＋文件名清洗，待裁定）＋ 1 笔疑似（area 16：debounce/throttle 手写 main/worker/save-manager，待裁定）。
2. 九笔均为「M4 同一真相抄多份」的**漏检残党/回潮**：media provider 层、scriptBox/拖拽层、通用工具层（clamp / debounce·throttle 疑似 / env 配置读取散落）、节点结构默认层（nodeDefaults×2）、剪贴板层（copyText）、字节格式化层（formatBytes，前误判非债已纠正）在既有收口 sweep（TD-16-14 / 16-18 / 18-6 / 18-5）之后仍各自内联了一份，根因未彻底清零；director3d 外部域下载＋文件名清洗为边界待裁定（@见 TD-18-7 同类）。
3. 覆盖账：本轮审 9 个 canonical producer（idGen / fileNameFromUrl / detectFileType / clamp / nodeDefaults / copyText / env配置读取 / formatBytes / 存储键·URL 归一）＋ 边界 director3d uid / dir3d 内联下载＋文件名清洗 ＋ 非债留痕 5 类（G/H/I/J + generationContract/scriptBoxEngine 已收口）；未审：contentStore / useNodeData / eventBus（canvasHost·commitNewNodes 本轮已核：agent 路径 AI-undo 为设计、FaceMosaic 裸 setNodes 入 TD-16-43）；已由 check:arch 机器守卫。
4. DATAFLOW：无链路漂移，不改。
5. 跨域追加：时间码/时长格式化第二份（TD-22-64，area 22）：formatTimeCode 为 videoEditor 域唯一时间码格式化，ruler-utils:234 / media.tsx:661 / VideoProcessNode:124 各重写 mm:ss，director3d:961 为外部域副本。

---

## 覆盖度表（本轮增量，不重抄旧行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| - | - | - | - | - | - | - |
| `src/components/base/media/providers/librarySource.ts:68` | 上层/media | 有债待还 | 🔴 | 09-17 续查 | `rawUrl.split('/').pop()` 绕 fileNameFromUrl | TD-16-39 |
| `src/components/videoEditor/ui/editor/panels/assets/link-media-refs.ts:96` | 上层/视频 | 有债待还 | 🔴 | 09-17 续查 | `ref.url.split('/').pop()` 绕 fileNameFromUrl | TD-16-39 |
| `src/components/hooks/useAssetDropPaste.ts:157/488/552` | 中间/hooks | 有债待还 | 🔴 | 09-17 续查 | `startsWith image/video/audio` 绕 detectFileType | TD-16-40（回潮 TD-16-18） |
| `src/components/scriptbox/scriptBoxPlaybookStore.ts:166` | 上层/scriptBox | 有债待还 | 🔴 | 09-17 续查 | `${Date.now()}${Math.random()}` 绕 idGen | TD-16-38（回潮 TD-18-6） |
| `src/components/scriptbox/scriptBoxPlaybookManager.tsx:121` | 上层/scriptBox | 有债待还 | 🔴 | 09-17 续查 | `pb-import-${Date.now()}${Math.random()}` 绕 idGen | TD-16-38（回潮 TD-18-6） |
| `src/components/director3d/project.ts:359` | 上层/3D | 有债待还(待裁定) | 🔴 | 09-17 续查 | 私有 `uid` 第二份 idGen | TD-18-7（dir3d 域待拍板） |
| `src/components/base/core/utils.ts:216` ↔ 8 处消费点（drop-utils / PromptInput / promptMention / ImageZoomDialog / DepthVideoModal / scriptBoxEngine / AgentPanel / ImageBoxNode） | 地基/上层 | 有债待还 | 🔴 | 09-17 续查 | 内联 Math.min(Math.max()) 绕 clamp | TD-16-41（@见 TD-18-5） |
| `src/components/videoEditor/engine/lib/time.ts:8`（formatTimeCode 唯一入口）↔ ruler-utils:234 / media.tsx:661 / VideoProcessNode:124 | 上层/视频 | 有债待还 | 🔴 | 09-17 续查 | 本地 formatTimestamp / formatDuration 重写 mm:ss 绕 formatTimeCode | TD-22-64（跨域 area 22） |
| `src/components/base/canvas/nodeDefaults.ts`（withNodeSize/applyNodeTypeDefaults 唯一入口）↔ useCanvasAgentTools:429 / canvasPlanExecutor:535 / depthVideo/spawn:64 | 地基/上层 | 有债待还 | 🔴 | 09-17 续查 | 造节点硬编码 420×420 / 420×380 绕 nodeDefaults | TD-16-42（@见 nodeDefaults:40） |
| `src/components/base/canvas/nodeDefaults.ts`（withNodeSize/applyNodeTypeDefaults 单源表）↔ GridSplit:644 / GridMerge:471 / VideoProcess:933,964,995 / Director3D:139 / Panorama:417 / FaceMosaic:132 | 地基/上层 | 有债待还 | 🔴 | 09-17 续查 | assetNode/imageBoxNode 未进表，spawn 尺寸散落 320×320×2/420×380/320×200/360×260/420×420×2 绕 withNodeSize | TD-16-43（@见 nodeDefaults:39） |
| `src/components/base/utils/clipboard.ts:343`（`copyText` 唯一封装）↔ CameraStudioPanel:507 / export-button:254 / media:523 / useTableSelection:69 / AssistantTablePanel:291 / ChatMarkdown:180 / AgentMessage:313 / ImageBoxNode:455 / VideoExtractNode:380,403 | 地基/上层 | 有债待还 | 🔴 | 09-17 续查 | 裸 `navigator.clipboard.writeText` 绕 copyText（10 处，违反 utils.ts:91） | TD-16-44 |
| `src/components/base/utils/clipboard.ts`（downloadBlob/downloadUrl）↔ director3d/App.tsx:1835 / 1887 / 1994 | 上层/3D | 有债待还(待裁定) | 🔴 | 09-17 续查 | dir3d 外部域手写 createObjectURL+link.click 下载，绕 downloadBlob（L1836 文件名清洗亦第二份） | 待裁定（@见 TD-18-7 同类） |
| `src/components/base/core/utils.ts`（debounce/throttle 唯一入口）↔ main.tsx:29-74 / transcription/worker.ts:210-241 / save-manager.ts:116 | 地基/上层 | 待裁定(疑似) | 🟡 | 09-17 续查 | bespoke 手写 throttle/debounce（语义特殊）疑违 utils.ts:91 | 待裁定 |
| `src/components/base/core/config.ts`（env 单源，L4 明令集中）↔ useConnectedInputs:492 / director3d/models:81 / project:517 / contentStore:255 / modalLayer:129 / conversationAiState:104 / conversationState:122 / tableWorkspaceState:120 / videoEditor/editor-constants:1 | 地基/上层 | 有债待还 | 🔴 | 09-17 续查 | 9 处裸 `import.meta.env.DEV` / `process.env.NODE_ENV`（config.ts 未导出 IS_DEV，videoEditor 自建 IS_DEV）绕集中约定 | TD-16-45 |
| `src/components/base/core/utils.ts:348`（`formatBytes` 唯一封装）↔ useImageHoverActions:113 / link-media-refs:93 | 地基/上层 | 有债待还 | 🔴 | 09-17 续查 | 手写 `(n/1024).toFixed(0)KB` / `Math.round(x/1024/1024)MB` 绕 formatBytes（toFixed 漂移；纠正前误判非债） | TD-16-46 |
| `src/components/base/panels/ResourceLibrary.tsx:202` | 上层 | 已裁定非债 | 🟢 | 09-17 续查 | `'migrated'` 仅注释，真走 libraryBrowseArgs | 非债 |
| `src/components/base/ui/GeneratingOverlay.tsx:219` | 上层 | 已裁定非债 | 🟢 | 09-17 续查 | Math.random 仅 UI 提示，非 id | 非债 |
| `src/components/base/core/contracts.ts`（STORAGE_KEYS） | 地基 | 已裁定非债 | 🟢 | 09-17 续查 | 全仓 yimao_* 走 KEY_YIMAO_* | 非债 |
| `src/components/base/utils/assetUrl.ts` / `core/utils.ts`（URL 归一） | 地基 | 已裁定非债 | 🟢 | 09-17 续查 | 101 处均走 canonical | 非债 |

## 七、状态行

- 本区域（16）状态：**已验证(有债待还)**（本轮补登 9 笔消费者自造 SSOT 第二份：fileNameFromUrl / detectFileType / idGen / clamp / nodeDefaults（TD-16-42 ＋ TD-16-43）/ copyText / env读取 / formatBytes（纠正前误判非债）；另 director3d 内联下载＋文件名清洗、debounce/throttle 手写 为边界/疑似待裁定；均与既有母体债 TD-16-14 / 16-18 / 18-6 / 18-5 同源，归其名下统一收口）。
- area 18：维持**已验证(有债待还)**，新增 1 笔边界（director3d uid，待用户拍板）。
- area 22：维持**已验证(有债待还)**，跨域追加 1 笔（时间码格式化第二份 TD-22-64，ruler-utils:234 / media.tsx:661 / VideoProcessNode:124 绕 formatTimeCode）。
- 本轮为**改存量（漏检残党）**，未新增区域覆盖；覆盖账见 §六。
- 待还债项已流入 `daily/架构日志/债务.md`（轻量索引）。
- DATAFLOW.md：已对账一致，未改。
