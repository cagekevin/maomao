# 架构日志 · 11 区（agent/skill 数据层） · 2026-09-22

> 范围：**只动数据层**（存储键 / frontmatter / 幽灵机制）。界面批次（`docs/plan/142` B1–B4）未动。

## 一、链路快照（拓扑未变）

```
磁盘 SKILL.md ──skillApi──▶ skillHydrate ──▶ skillRepository（键唯一读写口）
                                                    │
                                        skillRegistry.listAllSkills（缓存 ∪ 内置）
                                        ├─ skillLibraryView（设置页：行/组判定）
                                        └─ skillPickerView （面板下拉：选得动的那些）
                                                    │
                        选中只存 id ──▶ skillInject.freezeSkillTurn(ids)
                                        └─ skillInjectText（受 skillBudget 限）──▶ 模型
```

`spec/DATAFLOW.md` 已就地改正（§十一 的 `agent/runtime/skillStore.ts` 行：删 `agent_skill_usage`、写出真实键真源文件）。

## 二、溯源（Phase 1：症状 vs 病根）

用户提问："那个 AI 搞了这么多文件，是不是把简单问题做复杂了？"

逐件取证结论：**逻辑顺序本身没有错** —— 一条主线（磁盘 → 缓存 → 列表/下拉 → 选中 → 冻结 → 模型）+ 三条支线（写线 / 导入线 / 云线），分层清楚。
"多"在两处：① **件数**（23 个源文件 ≈ 3360 行，其中 8 个 < 80 行）；② **能力项**（预算三参数、多选链、6 状态徽章、伪组特例）。
本轮只清第 ② 类里**零消费**的那批 —— 件数合并另案。

## 三、定海（Phase 2：SSOT）

`agent_skill_usage` 的零消费已实证：写点只有 `AgentPanel.applySkill`（选中即写盘 + notify），
而 `getSkillUsage` **无任何显示点**（设置页与面板都查不到）⇒ "写进去就是死数据"，不是"暂未接显示"。

## 四、切割（Phase 3：真缝隙）

frontmatter 的 4 个"已知字段"与 `unknown` 是**同一份数据的两条路**（前者解析进类型、后者原样保留）。
⇒ 删前者，只留 `unknown` 一条保真通道。`skillPersist` 的 manifest 合并本就按 `unknown` 并集 ⇒ 往返不丢（测试锁死）。

## 五、探债（Phase 4）

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-11-72 | `runtime/skillStore.ts` 转发壳未退役（方案已排 B 批后） | 还债 | 结构债 | 低 | 低 | 收口期留的过渡壳 | 与 UI 批同批 | 待还 |

## 六、覆盖度表（只登记本轮变化行）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据 | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `skillRepository.ts` | 存储 | 深审 | 🟢 | 11 / 2026-09-22 | 删 usage 后 3 键唯一读写口 | 非债 |
| `skillManifest.ts` | 解析 | 深审 | 🟢 | 11 / 2026-09-22 | 已知字段 8→4，`unknown` 保真 | 非债 |
| `skillHydrate.ts` | 收敛 | 深审 | 🟢 | 11 / 2026-09-22 | `detectSkillDrift` 复核为**必需** | 原判"删"被推翻 |

**覆盖账**：本区 23 个源文件 / 本轮深审 3 / 未审 20（其余属 UI 层，B1–B4 时再审）。

## 七、结论与下一步

- 本区域状态：`已验证(有债待还)`。
- 待还债项已流入 `daily/架构日志/债务.md`：否（TD-11-72 待随 UI 批一并登记）。
- DATAFLOW.md：已就地改正。
- 灯：本区覆盖度表已更新。

## 八、交付闸自查

| 闸 | 本轮结论（≤1 行） | 可复现凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | frontmatter 从"类型 + unknown 两条路"归为一条（未知字段一律 `unknown`） | `skillManifest.ts` 的 `KNOWN_FIELDS` 只剩 4 个键 |
| 减一 · 兜底净减 | 删 1 个存储键 + 1 个模块 + 4 个类型字段 + 1 个作废留痕通道 | 死代码基线 68 → 67 |
| 减二 · 复杂度实减 | 登记键 45→44 · 模块文件 −1 · `SkillManifest` 字段 −4 | `docs/plan/142 §10` 对照表 |
| 一诚实 · 失败诚实可见 | 未新增任何判词（只删零消费能力），失败路径原样 | — |
| 零炸 · 防回归防副作用 | 全量单测全绿；面板"选中技能"不再写计数（用户不可感知） | `Test Files 272 passed` / `Tests 3286 passed` |
| 一凭证 · 证据可复现 | `type-check` 0 · `build` ✅ · `check:keys` ✅ · `check:dead-code` 无新增 | 上述命令输出 |

**零炸三问**：

1. **哪些行为被改了？** ① 选中技能不再写 `agent_skill_usage`；② frontmatter 解析不再认识那 4 个字段（改走 `unknown`）。
2. **改前能走的路径，改后仍能走吗？** 是 —— 全量单测 + "导出=磁盘逐字"用例仍绿。
3. **改前会失败的，改后仍失败吗？** 是 —— 冲突闸、读失败横幅、漂移提示均未动。

**用户可感知的行为变化**：**无**。唯一变化是"不再记使用次数"，而使用次数从未对用户显示过。

## 九、决策留痕（落账时回填）

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度（→ §八 减二） | SSOT（→ §八 一归） | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 删 `agent_skill_usage` 键 + `skillUsage.ts` | 读侧零显示点（实证） | 键语义跟 `skillRepository` 走 | −1 键 −1 文件 | 一归 | 做 |
| 2 | frontmatter 已知字段 8 → 4 | 无行为读点（读点在存取转发路径） | 解析层只管"认识的字段" | −4 类型字段 | 一归 | 做 |
| 3 | **拒删** `detectSkillDrift`（推翻 `142 §3.10`） | 云同步保护闸 + 外部改盘后的唯一提示通道 | 漂移侦测属 hydrate 职责 | 保留 1 函数（原计划 −1 函数 −1 UI 块） | — | **不做**（不删） |
| 4 | 单选 `§3.7.1` 与壳退役 | 涉运行时调用链与面板 UI | 归 UI 批 | — | — | 交 B 批 |

**存在性凭证**：

```bash
grep -n "## 九、决策留痕" daily/架构日志/11-跨区-skill数据层收口-2026-09-22.md
```
