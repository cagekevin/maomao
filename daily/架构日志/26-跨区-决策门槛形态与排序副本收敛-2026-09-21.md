# 26 · 决策门槛形态与排序副本收敛（ADR-0001 / 0050）

> 触发：`ADR-0001` 加档（插入「不越权」）后，用户问「从架构来说，这个排序合理么，有漏洞吗」→ 裁定「新写一条 ADR」。
> 取证：`node scripts/adr.mjs refs` / `refs --check` + `grep` + 逐文件回读原文。

## 一 · 排序表副本实测（写这条 ADR 的直接理由）

| # | 位置 | 版本 |
| --- | --- | --- |
| 1 | `docs/adr/ADR-0001` | 正确性 ＞ 不越权 ＞ 复杂度 ＞ SSOT ＞ 既有约定 |
| 2 | `.codebuddy/commands/架构师改码7步法.md:41` | 物理红线 ＞ 正确性职责 ＞ 边界 ＞ 复杂度 ＞ SSOT ＞ 工期 ＞ 兼容性 ＞ 既有约定 |
| 3 | `.codebuddy/commands/架构师心法.md:44` | 物理红线 ＞ 正确性/安全 ＞ 复杂度 ＞ 工期 ＞ 观感 |
| 4 | `.codebuddy/commands/ADR守护者.md:46` | = ADR-0001 |
| 5 | `docs/adr/ADR-0044-域内落点的四条边界口径.md:59` | = ADR-0001 |

⇒ **5 份副本 / 3 个版本**：这条判据在违反它自己排第 4 档的 SSOT。

## 二 · 线性「高者胜」的 3 条反证

1. **与停手规则冲突**：`架构师改码7步法.md:19-21`「越权就是结构错误，不是风格问题 → **停**」。线性序下"结果更正确、但越权" = 正确性赢 ⇒ 做。
2. **与仓内判据形态冲突**：`ADR-0044` §9 是合取（复杂度变低 **+** SSOT 变好）；三铁律是三条都要；`ADR-0047` 是前置关 ⇒ 仓内真实判据是**门槛的合取**，不是比较。
3. **档间不互斥**：`架构师改码7步法.md:1015` 的小节标题即「**SSOT职责边界**」—— SSOT 与职责边界常是同一件事 ⇒ 判第 2 档还是第 4 档会翻转结论。

（附带两条：`既有约定` 不构成判据（ADR-0001 本体）⇒ 只能是资格筛；`复杂度` 档无可量化定义 ⇒ 最软一档。）

## 三 · 处置

- 新落 `ADR-0050`（形态真源：门槛 + 择优）；`ADR-0001` 保留原文 + 形态指针（`ADR-0001:30`）。
- 副本收敛为「插入位置 + 指针」：`架构师改码7步法.md:41` · `架构师心法.md:44` · `ADR守护者.md:46` · `ADR-0044:59`。
- `adr.mjs audit` 0 问题；排序串 `grep` 命中只剩 `ADR-0001`（+ `README.md:295` 的自动索引块）。
- **写崩了一次**：首版 ADR-0050 写成**论证体**（3 条反证 + 5 份副本清单全塞进正文），被用户指出「不像 SOP，是论证过程」⇒ 按 `ADR守护者`「要/不要」表重写为判据体，过程一律搬到本文件（用户 2026-09-21 裁定）。

## 四 · 本轮命中的工具缺陷（**未决 · 待裁定**）

**编号回收**：`scripts/adr.mjs:387` 取号 = `现存 max + 1` ⇒ `rm` 掉的号会被下一次 `add` 复用，且无墓碑。

- 实证：`ADR-0050` 曾于 2026-09-20 用于一条误建 ADR（当场 `rm`），本轮 `add` 又取到 0050。
- 后果：`node scripts/adr.mjs refs 0050` 把 `docs/adr/README.md:48`（"…就落了 ADR-0050，前提错，当场 rm 撤回"）列进「**生效判据（其他 ADR）—— 必须回改**」—— 工具把**历史号**当成**现行条**；而 `refs --check` 报 **0 断链**（号"存在"了 ⇒ 遮蔽成立，看不见）。
- 同族：TD-17-2（新债撞已归档 ID，被 `loadAll` 静默遮蔽）。
- 已做止损：`docs/adr/README.md:48` 补「**已删号**作废，勿复用」措辞（命中 `EXEMPT_REF`，断链检查不再误算）。
- 待裁定：① 治本 —— `rm` 写墓碑、`add` 取号跳过（需定墓碑数据落点）；② 保留现状。

## 五 · 状态行

- 本区（决策门槛形态）状态：**已裁定并落盘**（`ADR-0050` 生效），副本收敛完成。
- 覆盖账：排序表副本 5 处全核（3 个版本）；引用面 `refs 0050` 11 处（须回改 5 / 过程文档 6）。
- 未决：编号回收缺陷（§四）—— 等用户裁定治本或保留。
- 结论：排序形态改为「门槛 + 择优」；核心反证是"线性高者胜会放开越权"，与 7 步法底线②「越权 → 停」直接冲突。
- **追加（同日）**：判据适用时机**收窄为两个观察点**（拍板时 / 落账时）；"AI 内部每个决定"作为**不可判的假判据删掉**；内容副本收敛为「真源 1 处 + 其余纯指针」（判据串实测命中 3 处，全合法）。

## 九、决策留痕

> 骨架见 `daily/架构日志/_template.md` §九；三关真源 = `docs/adr/ADR-0050`。

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度（量化口径） | SSOT | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 排序形态改「门槛 + 择优」（ADR-0050） | 不降：越权不再被收益买走 | 不越权：排序本体只留 ADR-0050，其余全是指针 | 降：排序表副本 5 → 1（判据串命中 4 → 3，全合法） | 变好：消除 3 个不一致版本 | 做 |
| 2 | 判据时机收窄为两个观察点 | 不降：拍板体验不变（选项仍带代价） | 不越权：不再宣称管"AI 内部决定"（无观察点） | 降：删掉 1 条不可判的假判据 | 变好：假判据出清 | 做（用户裁定） |
| 3 | 编号回收缺陷（`rm` 后号可复用，无墓碑） | — | — | — | — | **交用户**（未裁） |
| 4 | 清模板重复项（§三↔§四 划边界；§八↔§九 禁重写证据） | 不降：判据不变，只划边界 | 不越权：凭证真源仍在 §八 / SOP，模板不夺权 | 降：同轮证据由"两处各写"变"一处写" | 变好：§八/§九 不再各存一份 | 做 |
| 5 | 编号回收治本（`adr.mjs` 取号改**高水位线**） | 不降：不复用号 ⇒ 历史引用不再指错 | 不越权：从既有「删号留痕」反推，**不新建墓碑文件** | 降：0 新机制 / 0 新文件 | 变好：编号出现即不复用（ADR-0043 式空缺成常态） | 做（自判 · 承接第 3 行；负例探针：删掉最大号后仍取 0051） |
| 6 | 给用户选项的形态（`ADR-0051`） | 不降：拍板恢复「用户行使判断」 | 不越权：架构师该自判的不再披三选一外衣 | 降：删掉旧模板 `方案 A（推荐）` 的诱导形态 | 变好：判据进 ADR，Step 5 只留动作 + 指针 | 做（用户裁定） |

**可复现凭证**：

```bash
grep -n "## 九、决策留痕" daily/架构日志/26-跨区-决策门槛形态与排序副本收敛-2026-09-21.md
```
