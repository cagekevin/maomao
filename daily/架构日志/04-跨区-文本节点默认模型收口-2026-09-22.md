# 架构日志 · 04 画布/节点（跨区：canvas contract → text 节点） · 2026-09-22

> 本轮清偿 **TD-04-64**（判据登记表 §八 #5，**用户当场裁定：「改成空」**）。
> 口径：`ADR-0057`（开工前数载体 → 建真源后销副本 → 净减才算过）。

## 一、链路快照

```
文本节点 selectedModel 的两条来路：
  新建 → App.addNode → injectNodePrefs ← PREFS_DEFAULTS.textGenerateNode.model = ''（nodePrefs.ts:88，真源）
  存量 → TextGenerate.tsx:139 useNodePrefs(...) + useState(data.selectedModel ?? '')（缺字段兜底）
       → :521 <ModelSelect value={selectedModel} onChange={handleModelChange} models={models} />
```

## 二、溯源（症状 vs 病根）

- **症状**：同一件事「没选模型时默认用哪个」有**两个答案** —— `''`（`nodePrefs.ts:88`）与 `'gpt-4o-mini'`（`TextGenerate.tsx:138`）。
- **病根**：`??` **只认 `null`/`undefined`、不认空串** —— 新建路径注入的是 `''`（有值）⇒ 回退**不触发**；
  故 `'gpt-4o-mini'` 是**只在旧快照上活的死回退**，同一语义被 `undefined` / `''` 两种表示承载 ⇒ 行为随数据来源而异。
- 佐证：`canvasPlanExecutor.ts:466` 注释「model 为空时不写 selectedModel（不污染）」⇒ 「空 = 未选」是系统既有语义，`'gpt-4o-mini'` 是孤例。

## 三、定海（用户裁定）

**2026-09-22 用户裁定：默认 = 空串（未选，用户自己选）。** 原话：「空就空吧，空了反正自己就填，选一个不就行了」。

## 四、切割

- **载体账（ADR-0057 第 1 步）**：`grep -rn "gpt-4o-mini" src/` 命中 **2 处**（`TextGenerate.tsx:131` 注释 · `:138` 代码，同判据）；
  真源 `nodePrefs.ts:88` 的 `''` 是第 3 个载体 ⇒ **开工前 N=3，改后 `gpt-4o-mini` 归零、只剩 1 个答案**。
  （`tests/` 另 3 处是 provider 模型桩，不同判据，不计。）
- **不是新约定，是向同族收敛**：`ImageGenerate.tsx:180` 与 `VideoGenerate.tsx:139` 本来就是 `?? ''`。
- 角色分层保留：`nodePrefs.ts:88` = 新建注入真源 · `TextGenerate.tsx:139` = 存量缺字段兜底（两个角色、同一个值）——
  按 `nodePrefs.ts:98` 注释，组件侧 `?? 纯常量` 是**有意设计**（防记忆反向改写存量节点）⇒ 不合并（ADR-0031）。

## 五、探债

| TD-ID | 决策点 / 问题点 | 归类 | 归属 | 爆炸半径 | 利息率 | 根因 | 偿还计划 | 状态 |
| - | - | - | - | - | - | - | - | - |
| TD-04-64 | 文本节点默认模型两值矛盾（`''` vs `'gpt-4o-mini'`） | 还债 | 业务债（值由用户裁定） | 1 文件 | 中 | M3 SSOT 第二份 | 存量兜底改 `''` + 补断言 | **已清偿** |

## 六、覆盖度表（增量）

| 文件 | 层 | 覆盖状态 | 灯 | 轮次/日期 | 证据(refs/行号) | 债/裁定 |
| --- | --- | --- | --- | --- | --- | --- |
| `src/components/text/TextGenerate.tsx` | 上层 | 深审 | 🟢 | 2026-09-22 | `:139` `?? ''` · `:131-133` 注释同步（不再提 gpt-4o-mini） | TD-04-64 已清偿 |
| `src/components/canvas/contract/nodePrefs.ts` | 地基 | 深审 | 🟢 | 2026-09-22 | `:88` PREFS_DEFAULTS 真源（值未变，成为唯一答案） | 非债 |

**覆盖账**：本轮为**单点跨区收口**，审 2 文件；其余沿用上轮（`04-跨区-任务状态视觉与失败谓词收口-2026-09-22`）。

---

## 七、结论与下一步

- 本区域状态：`已验证(有债待还)`（04 区仍有 TD-04-42／50／59／60／63／65～71 等待还）。
- 待还债项已流入 `daily/架构日志/债务.md`：是（TD-04-64 本轮结清）。
- DATAFLOW.md：已对账一致（节点链路未变，无需改）。
- 判据登记表：§八 #5 已就地更新（真源 `nodePrefs.ts:88` · N 3→2 · 未收口 24→23、文本 2→1）· §八 小结与 §十 汇总同步。

---

## 八、交付闸自查

| 闸 | 本轮结论 | 可复现凭证 |
| --- | --- | --- |
| 一归 · 判据归一 | 默认模型答案 **2 → 1** | 改前 `grep -rn "gpt-4o-mini" src/` → 2 处（`TextGenerate.tsx:131/138`）；改后同命令 → **0 处** |
| 减一 · 兜底净减 | 删掉一个**隐式默认值**（只在旧快照生效的死回退），未新增任何兜底 | `git diff` 仅 `?? 'gpt-4o-mini'` → `?? ''`（一行）+ 注释 |
| 减二 · 复杂度实减 | ⚠️ **载体账 2 → 2（未净减）** —— 减掉的是「**第二个答案**」（`'gpt-4o-mini'`），**不是载体**：真源 + 存量兜底两个角色仍在；答案是 2 → 1 | 改前：真源 `''` + 兜底 `'gpt-4o-mini'`（两答案）；改后：真源 `''` + 兜底 `''`（一答案） |
| 一诚实 · 失败诚实可见 | **不适用** —— 本轮不涉成功/失败契约（无判别联合、无错误路径） | — |
| 零炸 · 防回归防副作用 | 相邻测试全绿；改前会失败的仍失败（无失败路径变更） | `npm run type-check` ✅ · `npx eslint tests/unit/TextGenerate.test.tsx` ✅ · `npm run check:dead-code` → `67 = 67`、无新增 · `npx vitest run`（TextGenerate / upstream / nodePrefs / nodePrefsRegression / ssrRegression）→ `35 passed` |
| 一凭证 · 证据可复现 | 见下探针结论块 + 上述命令原文 | — |

**探针（TD-04-64 · 默认模型回退 gpt-4o-mini 必红）** —— 注入语义：`TextGenerate.tsx` 的 `?? ''` 改回 `?? 'gpt-4o-mini'`：

```
🔬 探针结论｜TD-04-64 默认模型回退 gpt-4o-mini 必红
   文件   : src/components/text/TextGenerate.tsx（命中 1 处，已还原 sha=ed920921849c）
   命令   : npx vitest run tests/unit/TextGenerate.test.tsx
   观测   : exit=1｜输出 22 行
   ✅ 退出码 == 1 — 实际 1
   ✅ 输出含「未选模型时默认为空串」 — 命中
```

**还原后的绿**：`npx vitest run tests/unit/TextGenerate.test.tsx` → `Tests 2 passed (2)`。

**零炸三问**：

1. **哪些行为被改了？** 只改「**旧快照**缺 `selectedModel` 字段时的默认值」：`gpt-4o-mini` → **空串（未选）**。
   新建路径**不变**（`injectNodePrefs` 注入的本就是 `''`，`??` 不认空串 ⇒ 回退从不触发）。
2. **改前能走的路径改后仍能走吗？** 是 —— 35 tests 全绿（含 `TextGenerate` 生成调用、上游连线、nodePrefs 合并、SSR 回归）。
3. **改前会失败的改后仍失败吗？** 是 —— 无失败路径变更；死代码闸 `67 = 67` 无新增。

**用户可感知的行为变化（显式声明，且系用户有意裁定）**：文本节点在**旧快照**上不再自动带出 `gpt-4o-mini`，默认显示「未选」。
（新建节点行为一字不变。按「不为存量用户做兼容」的项目约定，此代价不构成损失。）

---

## 九、决策留痕

| # | 决定 | ① 正确性 | ② 职责边界 | ③ 复杂度（→ §八 减二） | SSOT（→ §八 一归） | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 文本节点默认模型 = **空串** | 不退：与图片/视频节点同形态，消除「同一语义两种表示」 | 值属业务 ⇒ **交用户裁定**（本行即留痕） | 矛盾分支消失 | 2→1 个答案 | **交用户** → 用户裁定「改成空」 |
| 2 | 不改 `PREFS_DEFAULTS`、也不让组件读它作兜底 | 不退 | 两个角色（新建注入 / 存量兜底）本就分层，合并会削掉「防记忆反向改写存量节点」的能力 | 改动半径 = 1 行 | 不制造新耦合 | 做（ADR-0031 判据重复禁合并） |
| 3 | 补行为断言（`ModelSelect` 桩局部记录 value） | 不退 | 测试桩自有，不动共享 `_nodeMocks` | — | 给裁定加防线（ADR-0052） | 做 |
