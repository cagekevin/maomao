# 04 区 · 十七轮 — TD-04-22 假字段清理（lod context）

> 日期：2026-09-13 ｜ 触发：**用户裁定「没用的就删」**（问"这些功能还打算做吗"）｜ 方法：按 `架构师改码7步法.md`。

---

## Step 1 · 证伪（这些字段真的没人用吗）

`grep -n 'useThumbnail|viewportMoving|handleFollowLimit|edgeFxLimit'` 全 src，实测：

| 字段 | 声明 | 消费端 |
| - | - | - |
| `lodLevel` | ✅ | `useAssetDegrade` / `ConnectionLine` **真读** |
| `viewportMoving` | 恒 `false` | **0 处** |
| `nodeCount` | `App.tsx:1346` 传入 | **0 处读取**（仅测试断言） |
| `handleFollowLimit` | 常量 `60`（`LOD_LIMITS`） | **0 处** |
| `edgeFxLimit` | 常量 `50` | **0 处** |
| `useThumbnail` | 恒 `false` | **0 处** |

→ **成立**：4 个字段是「当年以为要做、后来没做」的**占位**；而 `lod.test.tsx` / `assetTools.test.ts` / `useAssetDegrade.test.ts` 还在断言它们存在 = **给假实现背书**。

## Step 3 · 定位

**删**（而非"保留以防万一"）：占位字段 + 测试背书 = 负资产；将来真要做（跟随上限 / 边特效上限 / 缩略图降级）应**新写实现并同时接消费端**，而不是复活空字段。

## Step 6 · 施工

- **`lod.tsx`**：删 `LOD_LIMITS` 常量表；`DEFAULT_LOD` 收窄为 `{ lodLevel: 0 }`；删 `LodProviderProps.nodeCount` 与解构；`v` 对象 + `eslint-disable` 一并删除（改 `React.useMemo(() => ({ lodLevel }), [lodLevel])`）；头注释记录删因与「要做请新写」。
- **`App.tsx`**：去掉 `<LodProvider … nodeCount={nodes.length}>` 的传参。
- **测试**：`lod.test.tsx` 重写（2 例，只测 `lodLevel`）；`assetTools.test.ts` 5 处 lod mock 收窄为 `{ lodLevel: X }`；`useAssetDegrade.test.ts` 的 Provider value 收窄为 `{ lodLevel }`；`ConnectionLine.test.tsx` 本就只 mock `lodLevel` → 未动。

## Step 7 · 验证与度量

| 项 | 结果 |
| - | - |
| `tsc --noEmit` | ✅ 0 错 |
| 相关 4 测试文件 | ✅ **29 例全绿**（lod 2 / assetTools 18 / useAssetDegrade 3 / ConnectionLine 6） |

| 度量口径 | 改前 | 改后 |
| - | - | - |
| LOD context 字段数 | 6 | **1** |
| 假实现字段（零消费/恒常量） | 5 | **0** |
| `LOD_LIMITS` 常量表 | 1 | **0** |
| 「给假实现背书」的测试文件 | 3 | **0** |

**结论**：TD-04-22 从「部分已解决」→ **已解决**（字段本体连同测试背书一并清理）。
