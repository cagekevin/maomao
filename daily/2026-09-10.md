# 工作日志 / 交接 — 2026-09-10

> 主题：**Lovart GPT Image 2.5 / GPT Image 2 的「模型名不带质量后缀 + 质量独立子句」修复**。
> 范围：仅 `localTool/src/ai-relay/providers/lovart/` 下 `lovart_config.ts`、`lovart_prompt.ts` + 两个测试文件。
> 状态：代码改动已完成；`tsx --test` 受影响单测 **27 passed**，零失败；**未经用户本地预览实跑验证**。

---

## 〇、当前工作区状态（先看这个）

本次会话新增/修改：

- `localTool/src/ai-relay/providers/lovart/lovart_config.ts`（新增 `LOVART_QUALITY_MODELS` 派生表；`LOVART_IMAGE_RULES` 工具名去后缀；`LOVART_PROMPT_MODEL_NAMES` 删 4 条带后缀 2.5 映射）
- `localTool/src/ai-relay/providers/lovart/lovart_prompt.ts`（`buildLovartPrompt` 按派生表拼"中文质量子句 + 英文质量双保险"）
- `localTool/test/lovart_prompt.test.js`、`localTool/test/lovart_index.test.js`（断言同步更新）
- `daily/2026-09-10.md`（本文件）

> ⚠️ 改动未经用户实际在 Lovart 生成界面验证计费/档位生效；接手者请本地起 `npm run dev`，用 `gpt-image-2.5-sunburst-medium` / `gpt-image-2-low` 等跑一张图，确认 prompt 含「用 GPT Image 2.5 Sunburst，质量 medium。」且 tool_config 发基础工具名。

---

## 一、根因（看懂这个才懂为什么改）

咨询 Lovart AI 后明确其真实契约：

- **模型选择器 = `tool_hint`**，取值只有基础工具名：`generate_image_gpt_image_2_5_sunburst` / `generate_image_gpt_image_2_5_flare`（**不带质量后缀**）；`generate_image_gpt_image_2`。
- **质量 = `quality`**，是独立档位，6 档：auto / low / medium / high / xhigh / max（xhigh、max 仅 2.5 有效）。
- Lovart 解析我们 prompt 里的自然语言（如"用 GPT Image 2.5 Sunburst，质量 max"）自行拆出 `tool_hint` + `quality`。**`tool_hint`/`quality` 是 Lovart 内部解析结果，不是我们要发的结构化字段**——我们要做的只是把 prompt 写成它给的例句格式。
- **image-2 与 image-2.5 机制一模一样**：质量都不烤进工具名，工具名都是基础名、质量独立下发（之前误以为 image-2 工具名带 `_low` 后缀是真工具，已纠正）。

我们之前的实现有 3 处错位：

1. 下发带质量后缀的**假工具名**（`generate_image_gpt_image_2_5_sunburst_low` 等），上游并不真实存在。
2. **quality 从未传过**——全 `providers/lovart` 没有把质量写进请求，prompt 可读名还把 Low/Medium 烤进模型名。
3. 把"模型 × 质量"拆成 4 个假模型 id，质量未作为独立轴。

## 二、最终方案（用户拍板）

**模型 id 作单一数据源**，prompt 与 tool_config 都从它派生，不新增模型、不拆 quality 轴。

- 新增 `LOVART_QUALITY_MODELS: Record<模型id, { base, quality }>`。命中即：prompt 用 `base`（去后缀基础名）+ 单独质量子句；tool_config 发基础工具名（去后缀）。
- 未命中质量模型的（nano / 无质量模型）走原 `LOVART_PROMPT_MODEL_NAMES` 逻辑，不变。
- image-2（low/medium/high）与 2.5（当前仅 low/medium，按用户"用不了那么多"保留）同套逻辑。

### prompt 输出格式（以 `gpt-image-2.5-sunburst-medium` 为例）
```text
target_size: 1024x1024
用 GPT Image 2.5 Sunburst，质量 medium。
Generate exactly ONE image using the GPT Image 2.5 Sunburst model, quality medium.
<user_prompt>
{用户原文}
</user_prompt>
以上为用户提示词原文，直接使用，请勿修改
```
中文句给 Lovart 解析器、英文句作双保险硬约束；有参考图时首句前插 `Reference image attached. `（避免误走 edit_media 改图）。

### tool_config（全部去后缀，发基础工具名）
| 模型 id | 工具名 |
|---|---|
| `gpt-image-2-low/medium/high` | `generate_image_gpt_image_2` |
| `gpt-image-2.5-sunburst-low/medium` | `generate_image_gpt_image_2_5_sunburst` |
| `gpt-image-2.5-flare-low/medium` | `generate_image_gpt_image_2_5_flare` |

> 注释里已写明 **image 2.5 支持的全部能力**：2 基础模型 + 6 档质量；当前仅收 low/medium，要暴露 high/xhigh/max 只需在 `LOVART_QUALITY_MODELS` 追加对应 id 行（并在 manifest 加模型条目）。

---

## 三、改动文件速查

- `localTool/src/ai-relay/providers/lovart/lovart_config.ts`
  - 文件头注释：工具名不带质量后缀、质量经 prompt 独立下发。
  - `LOVART_IMAGE_RULES`（~32-75）：image-2 与 2.5 共 7 条工具名去后缀；旧"工具名带质量后缀"注释更正。补 `gpt-image-2-high` 规则（原漏，结构化路对 high 返回 undefined）。
  - 新增 `LOVART_QUALITY_MODELS`（~78 起）：派生表 + 详注。
  - `LOVART_PROMPT_MODEL_NAMES`（~127）：删 4 条带后缀 2.5 映射，改由派生表驱动。
- `localTool/src/ai-relay/providers/lovart/lovart_prompt.ts`
  - `buildLovartPrompt`（~100）：查 `LOVART_QUALITY_MODELS`，命中则拼中文质量子句 + 英文质量双保险；未命中走原逻辑。
- 测试：`test/lovart_prompt.test.js`、`test/lovart_index.test.js` 断言同步改为基础工具名 + 去后缀中文质量子句，新增 2.5 质量模型测试。

---

## 四、如何回退

改动高度局部、无架构变化，仅 3 个文件 + 2 个测试：

- `git checkout -- localTool/src/ai-relay/providers/lovart/lovart_config.ts localTool/src/ai-relay/providers/lovart/lovart_prompt.ts localTool/test/lovart_prompt.test.js localTool/test/lovart_index.test.js`

回退后即恢复"工具名带后缀 + 模型名嵌质量"旧行为（Lovart 真实接口不认，属回归）。

---

## 五、收尾提醒

- 本次是**行为修正**（对齐 Lovart 真实契约），非重构；对外函数签名不变。
- `npm run test:unit` 在本机 `.tsx` 存在 jsdom 环境抖动（见 2026-09-08 文末"测试环境注意事项"），本次只动 `.ts`/`.js` 逻辑文件，受影响单测全绿。
- 仍未在真实 Lovart 生成界面验证质量档位真正生效，需本地预览实跑确认。
