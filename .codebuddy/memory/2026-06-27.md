### 校准日志
- Kevin 说"日志+流水账"= Daily Notes，不是 wiki 的 log/hot。我理解错了两次才改对。
- 精简 wiki 示例时不能修改示例原文，只能减少数量。用户原始内容必须一字不差写入。
- 多个概念内容重叠时优先扩充而非新建（微表情、情绪蒙太奇、构图、打破摆拍感）
- 完全独立的方法论才新建概念页（声音控制、创意方法论、室内光控制）

## 2026-06-27

### 重复造轮子
- 处理 .doc 文件时没有先看 Tools/ 目录，直接想自己装 olefile 写脚本
- 修完 bm25-index.py 后只跑了 build，忘了先跑 contextual-prefix.py 生成 chunk
- **改进**：CLAUDE.md 已加"工具优先原则"；ingest.md 索引重建步骤已补全

### 无计划安装依赖
- 装 faster-whisper 时没先问清楚环境，直接装了 torch CUDA 11.8（2.8GB）到 C 盘，导致 Kevin 空间告急
- **教训**：以后装任何东西前，先列清楚"装什么、多大、装哪里、影响什么"，确认后再动手

### 概念页定位偏差
- 新创建的 13 个概念页偏"理论/注意事项"，缺乏可执行的操作步骤和提示词模板
- Kevin 指出"这些只是概念，没有深入的方法"
- **改进**：概念页保持现状作为参考索引，需要具体操作时从源文件提取模板

### Windows 兼容修复
- 发现 scripts/ 下 bm25-index.py、rerank.py、tiling-check.py 都用了 fcntl（Linux/macOS only）
- 全部改为跨平台：os.name == "nt" → msvcrt，否则 fcntl
- ingest skill 原本是为 Mac/Linux 写的，加了 Windows 替代方案标注
- 新建 Tools/MarkItDown文档转换/scripts/convert-doc.py 处理旧版 .doc
