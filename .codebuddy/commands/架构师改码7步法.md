# 架构师改码 7 步法（改存量专用 · 可执行 SOP）

> **读法**：`Step 0` 是**开工前置**，不计入 7 步 —— **7 步 = Step 1–7**；`Step 0 + 7 步` 才是完整流程（先查"仓库里已有什么"，再动手）。

> **定位**：`架构师心法.md` 是**价值观与铁律**（为什么这么改）；`写代码4步法.md` 是**从零写新功能**的协议（契约→数据流→测试→施工）。
> **本文件补的缺口**：**改存量**（修 bug / 还债 / 重构 / 响应"这里不对劲"）时的**操作步骤**。
> 它存在的直接原因：多轮实战中，架构师（AI）在 **"这个需求到底存不存在"** 上连续翻车
> （TD-05-11 登记为债实为非债 / TD-06-7 说"抽新模块"实为下沉 / TD-06-8 说"无实害"实测有实害 /
> TD-04-21 债描述「两条入口 + 两套标准不一致」半真半假 —— 实为**分层判据 + 探测重复**）。
> **心法讲"要证伪需求"，但没给"怎么一步步做到"**——本文就是那套步骤。
>
> **效力边界**：本步骤是心法的**可执行展开**，与心法冲突时以心法为准（`CLAUDE.md` 物理红线 > 心法 > 本步骤）。
>
> **更新(2026-09-13 · 经"视频剪辑器方案"整轮审计实战后加固)**：把"动手前最容易翻车的三件事"补进来 ——
> ① **新增 `Step 0 · 普查`**（先查仓库里**已经有什么**；本仓实测：抽帧 **9 份** / 时间轴刻度 **2 份** / 历史栈 **5 份**）；
> ② **新增 `附录 A · 准确性铁律`**（9 条踩坑实录：引用未取证符号、把"方案"当"实现"、把证据能定的拿去"待用户拍板"、自己提的守卫本身就是母体…）；
> ③ **铁律加固**：「任何描述都是线索」补上 **"任何名称也是描述"**（符号名 / 文件名 / 能力名同样必须先 `grep` 再写）。
>
> **范畴声明**：本文件以**改存量**为主；**从零新建**走 `写代码4步法`。但 **Step 0 / 3 / 4 / 5 / 7 对新建同样适用**（**Step 2 / 6 是存量专属**——硬套会得出错误结论，见附录 A9）。
>
> **更新(2026-09-14 · 经"发送到素材库"收口实战后加固)**：新增 **「用户报症状时怎么开工：把这个能力重新写正确」** 一节 ——
> 主线 = **不要找那个 bug，去找这段设计正确时该长什么样，然后照着重写**。
> 正确 = **拆掉错误兜底 + 把判据/契约放回该在的层 + 补齐链路缺失的那一步**。
> 判据：**症状消失只是副产品**；交付物是「该能力判据收口为 1 份 + 契约定义在拥有真相的层 + 兜底净减 + 入口可见可点」。
>
> **更新(2026-09-15 · 经"素材导入只能导一个"整轮实战后重写该节)**：把该节从**「正确 vs 错误」**改写为
> **「一条正面的开工指令 + 正确形态对照清单」**，并加入三件本次实证：
> ① **检查"代码里写了、UI 上出不来"的东西**（非法嵌套 / 塌陷高度链 / 被 `disabled`·`pointer-events:none` 吞掉的入口）；
> ② **弯路留痕（强制）**：改错的推理链必须与结论同等记录，且**锁着"已撤销设计"的假绿测试必须删**；
> ③ **⛔ 四种"修症状"的滑法**（改一版试一次 / 猜环境 / 在错误位置做深度优化 / 靠探针反复证伪假设）。
> **另**：Step 7 的探针**只在"证明我改对了"时用**，不再是"证伪假设"的默认手段 —— 定位靠**读代码 + 向用户问关键观察**。

---

## 贯穿全程的一条铁律（不是某一步，是每一步）

> ### 🔒 任何"描述"都是线索，不是结论。**任何"名称"也是描述。**
>
> 债登记、issue 标题、注释、上一轮审计结论、甚至**架构师自己上一小时的判断** —— 全都可能是错的。
> **动手前必须用工具重新取证**（`node scripts/mv-sync-refs.mjs refs <file>` / `grep` / 读真实代码）。
> **把描述当"去哪儿看"，不当"是什么"。**
>
> **读债同理（账本入口收口 2026-09-14）**：TD 描述（含归档里的）只是线索 ——
> 先 `node scripts/debt.mjs show <TD-ID>` 取**该债最新真相**（可能已改判非债 / 已解决 / 描述被修正），
> 再 `grep` 代码取证。**禁 ReadFile 全量账本**（主表已只留待办；历史在归档，命令已打通）。
>
> **找"同类问题以前怎么解的"（Step 0 普查的加速器）**：
> `node scripts/debt.mjs search <机制名|症状词>` —— 如 `search SSOT` / `search 静默` / `search 假收窄` / `search 死代码`。
> 命中行的**状态列就是当时的解法一句话**；`show <TD-ID>` 给**解法入口**（锚点区域日志，含探针、refs 与文件:行号）。
> 也可按区看：`node scripts/debt.mjs area <NN>`（该区全部历史债，含已归档）。
>
> **且：符号名 / 文件名 / 能力名同样是描述** —— 写进代码或文档之前，**必须先 `grep` 确认它真实存在、且真的导出**。
> 本仓实证：`mediaType.ts`（**不存在**，真源是 `assetType.ts`）· `contentStore.checkRegistered`（**未导出**，是内部函数）·
> `UPLOAD_DIRS.videoEditor`（**未登记**）· `videoEditorOpen`（**未登记**）· `13-文件生命周期` 的引用感知 GC（**是方案，`src/` 0 命中**）。

**本仓实证（连续三轮）**：

| 描述 | 取证后的真相 |
| --- | --- |
| TD-05-11「错误态无独立 UI，是债」 | **非债**（用户一句话点破：刷新后空白是诚实结果） |
| TD-06-7「需抽最底层 URL 原语消环」 | 只需**下沉**到已有叶模块（3 文件，而非新建模块改 40 处） |
| TD-06-8「派生写成副作用，现无实害」 | 是**异步 stale closure 覆盖**，且**有实害**（行为测试先红实证） |
| TD-04-21「A/B 两套标准不一致、改一处易漏」+「调用链有两条入口」 | 半真半假：①「两条入口」**取证不实**（实测只有 window 一个；ReactFlow 无 `onPaste`）；② 两套判据**不是漂移、而是有意的分层**（B 必须先放行才能走到 `clipboard.read()` 兜底）；③ **真债只是探测原语被抄成 2 份** |

---

## 用户报症状时怎么开工：把这个能力**重新写正确** · 🆕2026-09-14（"发送到素材库"） / 加固 2026-09-15（"素材导入"）

> **触发场景**：用户说「X 不好用 / 我测了还是不行 / 这里不对劲 / 只能做一次」。
> **这是最高频、也最容易跑偏的一类开工。**
>
> ### 🔒 唯一的开工指令（不要再读别的）
>
> > **不要去找那个 bug。去找这段设计正确时该长什么样，然后照着把它重写。**
>
> **症状不是要修的对象，是"这段设计错了"的报信人。** 你的任务不是让症状消失，
> 而是**把这个能力做成正确的样子** —— 症状消失只是它的副产品。
>
> 读法上的差别很关键：
> - ❌「这个 `if` 为什么没进去？」→ 你在**修症状**（会滑向补丁 + 兜底）。
> - ✅「这个能力正确落地时该是什么结构？现在这段结构为什么表达不了它？」→ 你在**做正确**。

### 开工动作（顺序即优先级）

1. **Step 0 普查**这条链路上"同一件事被实现成几份"（**份数就是"我要不要再写一份"的判据**）。
   有 ≥2 份 → **先定共用层，再动手**（否则你就是第 N+1 份）。
2. **把这段能力的"正确形态"写下来**（一屏以内）：入口是什么、链路几步、判据在哪一层、
   真相归谁。**写不出来 = 你还没理解这个能力** → 回去读代码，别动手。
3. **把现状与"正确形态"逐条对照**，凡是"表达不了正确形态"的结构 → **删掉重写**，而不是缝补。
4. **拆掉为掩盖症状而存在的兜底**，逐个问"它在掩盖哪条地基缺陷"（A6）：
   静默 catch / 失败归一 `null` / 占位对象 + 事后打补丁 / 默认值兜底 / UI 层提前宣告成功 /
   只在某一入口补的"补偿步骤"。
5. **把判据与契约放回它该在的层**：**谁拥有真相，契约就定义在谁那**（实证：`PersistOutcome` 从 store 移回文件域 `filesApi`）。
6. **补齐链路缺失的那一步**：病灶常常**不是"某步写错"，而是缺了一步** —— 落盘 ≠ 入库，缺"归位（context-only 改 folder）"。
7. **检查"代码里写了、UI 上出不来"的东西**（2026-09-15 新增）：
   结构写对了却看不见/点不动，几乎总是**结构或布局层面的错**，不是"功能没写完"。
   必查：非法嵌套（如 Tooltip 套 Tooltip）· 塌陷的高度链（`flex-1` 缺 `h-full`）·
   被 `disabled` 或 `pointer-events:none` 吞掉的入口 · 被上层盖住的图层。
8. **只读实测取证**，且**用与真实调用一致的口径**（教训：用错参数探测得出相反结论，见 A1/A3）。
9. 账本登记 / 结清；**遗留的弯路也要写进轮次文件**（见下方"弯路留痕"）。

### ⛔ 最容易滑进去的四种"修症状"（识别出来就退回去做 ②）

| 滑法 | 它为什么是错的 |
| --- | --- |
| **改一版试一次**：改 A → 用户说没变 → 改 B → 再试…… | 你在用试错代替设计。每猜一次都在加一层兜底，症状被埋得更深。 |
| **猜环境**：「是不是缓存 / 没重启 / 环境问题」 | 环境解释几乎总是错的，且它会让你去加"兜底"而不是改结构。 |
| **在错误的位置做深度优化** | 例：病灶是"这个动作不该由菜单承载"，却去优化菜单封装本身 —— **越"正确"离解越远**。 |
| **靠探针反复证伪假设** | 探针是**证明"我改对了"**的工具，不是**"我猜对了吗"**的猜谜机。定位靠**读代码 + 向用户问关键观察**。 |

> **自查判据**：**如果你的下一步动作是"先试试看"，就已经错了。**
> 正确的下一步永远是：**回去把这个能力的正确形态写清楚。**

### 判据：什么时候算"做完了"

| 口径 | 修症状的"做完" | 做正确的"做完" |
| --- | --- | --- |
| 症状 | 不出现（可能是被兜底埋了） | 不出现（且**兜底数量净减**） |
| 该能力 | 还是 N 份判据 | **1 份**判据 |
| 链路 | 缝补后更长/更绕 | **更短、更直**（环节数下降） |
| 契约 | 散在各层、靠猜 | 定义在**拥有真相的那层** |
| 结构 | 非法嵌套 / 塌陷布局仍在 | **逐条对照②清掉了** |
| 失败 | 仍是假成功 / 静默 | 判别联合 + 根部炸开 + 读者正确 |
| 证据 | "我试了，好像好了" | **用户实测通过** + 只读实测 + 闸/测试 |

> **一句话**：**不要服务那个症状，要服务这段能力的正确性。**
> 正确的交付物是「结构对照正确形态重写 + 判据收口为 1 份 + 契约在正确层 + 兜底净减 + 入口可见可点」。

### 弯路留痕（强制 · 2026-09-15 新增）

> **改错的弯路必须写进轮次文件**，与结论同等对待。
> 理由：弯路的**思路往往非常有道理**（否则你不会走进去），后人极易重走 ——
> 只留结论、不留弯路，等于把最贵的经验丢掉。
>
> 记三件事：**① 当时的推理链 ② 为什么它是错的 ③ 是否已完整回退（含"假绿"测试要删）**。
> **判据**：若某个测试锁的是"已被撤销的设计"，**它必须删掉** ——
> 测试通过 ≠ 那件事存在，留着就是给后人一个"这个契约存在"的假象。

#### 本仓实证一（2026-09-14）：「发送到素材库」
用户报「债显示已解决，但实测还是不成功」——
债本身没记错（"反馈诚实"确已修），真根因是 **判据被抄 4 份 + 缺"归位"一步 + 3 处兜底 + 目录判据前后端两套**；
**不猜 bug、按上述步骤把这个能力重做**，症状自然消失（九轮 / 十轮）。

#### 本仓实证二（2026-09-15）：「素材导入只能导一个」
用户报「只能导入一个素材，导入第 2 个就导不了了」，并**四次纠正方法**：
「不要修 bug，按正常应该怎么设计就直接改了删了重写」·
「有错误就找同类东西，先给它收口」·「不要找 bug，找同类东西收口」·
「老测来测去有啥好测的？把事情做正确，然后检查哪些代码上有、但 UI 显示不出来的东西」。

- **真根因**：把"打开文件选择框"这个**需要用户手势的命令式动作**（`input.click()`）
  挂在了一个 Radix 下拉菜单里 —— 菜单选中时会**同步**「关层 + FocusScope 归还焦点」，
  该动作随即被判为不在手势栈内 → **浏览器静默丢弃**。
  叠加 `Button` 基类含 `disabled:pointer-events-none`（入口禁用即指针穿透，
  菜单连 `pointerdown` 都收不到；而 `opacity-50` 在深色主题下几乎看不出）。
- **正确形态**：「导入」**直连** `openFilePicker`（链路 4 步 → **2 步**，无中介）；
  入口**一律不 `disabled`**；URL 入口用**看得见、点得到**的样式（不可用透明 `text` 变体）；
  顺带查出 `AssetsPanel` 高度链缺 `h-full`（内容为空时塌成 0 高 = 用户"看不到拖放区"）。
- **弯路（必须记）**：
  ① **包一层 `DropdownMenu` 把 `onClick` 归一为 `onSelect`** —— 读 Radix 源码后认定"封装层没把
     Item 语义摆正"。**改完仍然弹不出来，且新引入"菜单彻底打不开"的 bug（包装层把 `open` 变受控）**。
     错在：**在错误的位置（菜单封装）做深度优化** —— 病灶是"这个动作不该由菜单承载"。
     已完整回退（`git diff` 为空），其"假绿"测试已删。
  ② **反复写探针证伪假设**：逐一证伪了 4 个假设，又试图用 jsdom 验证 `disabled` / Radix 时序
     —— **jsdom 测不出浏览器的手势栈与 `pointer-events` 语义**，探针两次"没红"。
     教训：**测试服务于"我改对了什么"，不是"我猜对了吗"**。
- **真正定位靠什么**：**读代码 + 向用户问关键观察**。本例中用户的四句话
  （「按钮外观无变化」「菜单也没关」「看不到拖放区」「出现十字星光标」）
  直接锁定病灶 —— 比 4 个探针文件都有效。

> **反例警示（同一实战）**：探针第一次**没红** —— 因为端到端用例走了**另一条分支**（`dataUri` 而非 `multipart`），
> 缺陷不在那条分支上。**"测试不红 = 没测到东西"**，必须先修用例再谈修复（Step 7.1）。

---

# Step 0 · 普查 —— 仓库里已经有什么？（**开工前置**）

> **对应心法** 铁律 5（不虚构）/ §一.2（空间收敛）/ 本文件 Step 1 的"重新取证"
> **为什么排在最前**：Step 1 证伪的是"**这个需求**是否存在"；Step 0 问的是**更前一层** —— "**这件事，仓库里是不是已经有人做了**"。
> 不先问这一层，动手就必然长出新的一份（本仓实测：抽帧 **9 份**、时间轴刻度 **2 份**、历史栈 **5 份**）。

**要回答的问题**：我要的这项能力，**仓库里已经存在几份？真源在哪？我该复用、改造现有、还是新建？**

**动作**：
1. **枚举能力**（不是枚举文件）：把要做的事拆成能力项（"抽帧" / "时间↔像素换算" / "历史栈" …）。
2. **全库找实现**：每项用 `grep` / `refs` 找**全部**实现点，记 `文件:行` + 符号名 + 达成程度。
3. **数份数**：这份数就是"我要不要再写一份"的判据。
4. **判种类**（沿用 Step 3 的判别）：**探测/运算重复**（怎么读、怎么算）可收口 · **判据/宿主重复**（要不要做、在谁的地盘做）不可合并。
5. **定处置**：`复用` / `改造现有`（含**下沉 + 原处 re-export**，让消费方零改动） / `新建`。

**判据（红线）**：

| 情形 | 动作 |
| --- | --- |
| 已有 **1 份**，且它就是真源 | **复用**，一行都不写 |
| 已有 **≥2 份**（真重复） | **收口**：选最完整的那份**下沉**为唯一实现，其余 re-export / 分域迁入 |
| 已有 **N 份但判据/宿主不同** | **保留分层**，只收口底层探测原语 |
| 真的**没有** | 才**新建**；且必须写出"**现有为什么承载不了**"——写不出 = 你正在造第 N+1 份 |

**产出（强制）**：一张表 `能力 → 现有实现(文件:行) → 份数 → 处置`。**未出表不进入施工。**

**优先顺序**：**改造现有 > 新建**；"新建唯一实现"优于"在旧入口旁加分支"，但**劣于改造旧入口**。

**本仓实证（2026-09-13 视频剪辑器第 0 步，产出 `docs/124`）**：

| 能力 | 份数 | 结论 |
| --- | --- | --- |
| 抽帧 / 缩略图 | **9** | 不先下沉 `captureFrame`，剪辑器胶片条就是**第 10 份** |
| 时间↔像素 / 刻度 | **2**（+待建=3） | 收口到 `base/utils/timeline/`（3 域共用） |
| 历史栈 | **5** | **复用**泛型 `HistoryStack<T>`，不新写第 6 套 |
| 编码器探针 | 1（散在 3D 域） | 已有先例 → 收口共用，**不新写** |

> **与 Step 1 的分工**：Step 1 问"**该不该做**"，Step 0 问"**该不该由我做**"。两者都通过，才进 Step 2。
> **先例**：`docs/119-节点层收口机会普查-重复逻辑收敛清单` · `docs/124-视频剪辑器-能力重叠普查与时间轴真源裁决`。

---

# Step 1 · 证伪 —— 这个需求真的存在吗？

> **对应心法** §零.5 偷懒判据 / §五 自检「溯源」

**要回答的问题**：如果这个需求消失，我想写的代码还有存在价值吗？

**动作**：
1. **重新取证**：不看描述，直接读代码 / 跑 `refs`，确认"问题"在当前代码里**真实存在**（不是上一轮已修、不是描述漂移）。
2. **跑三问**（§零.5，缺一不可）：
   - ① **根因证据**：拿得出"它真的坏了/真的多余"的硬证据吗？（能复现的失败 / 死代码的零引用计数 / 实测数据）
   - ② **替代方案**：现状（已有机制）是否**已经足够**？我加的这段是否**只是换个说法**？
   - ③ **决策者的选择**：若涉及业务能力变化 → 必须交用户（Step 5）。
3. **问一句致命的话**：**"我加了这个，用户/系统能多做哪一件原来做不到的事？"** 答不出 → 假需求。

**三种产出（必须归入其一）**：

| 结论 | 动作 |
| --- | --- |
| **真需求** | 进入 Step 2 |
| **假需求（非债/伪需求）** | **就地改判 + 落盘留痕**（防后续 AI 再当 bug 补），流程结束 |
| **业务性需求** | 记下，跳到 Step 5 交用户拍板 |

**不过关怎么办**：拿不出根因证据 → **不要做**。但"不做"必须在账本里写明理由（拿不出证据的"不做"= 偷懒，会打回）。

**真实案例**：
- ✅ 正确证伪：TD-05-11 我提议"给 `HubEmpty` 加失败文案"，被质疑后复核 → 失败**已有 toast + logger**，加文案**不改变用户可行动作** → 改判**非债**。
- ❌ 失败证伪：TD-06-8 我继承"现无实害"直接开修 → 实测**有实害**（对照）。

---

# Step 2 · 归因 —— 是孤例，还是母体？

> **对应心法** §零（复杂度目标）/ §一.2（空间收敛）
> **用户明确要求**：**看到一堆问题时，第一反应必须是"是不是某条规则出问题了 / 它们的底层根因是什么"，先找根因。**

**要回答的问题**：我手上是 N 个独立问题，还是 1 个母体的 N 个症状？

**动作**：
1. **按"它为什么会产生"归堆**（**不是**按症状表面分类）。把同类症状放一起，问：它们共享哪一条**规则/契约/机制**？
2. **判据：某修复能同时消灭 ≥3 个债 → 它就是母体。** 母体几乎总在**契约层/规则层/机制层**，不在"某一行代码"。
3. **查"规则层"的问法**（用户原话）：
   - "是不是**某条规则**出问题了？"（例：不是"8 次忘了挂闸"，而是"**闸清单靠手写**"这条规则有问题）
   - "是不是**缺一条机器守卫**？"（例：红线只写在注释里 → 必回潮）
   - "是不是**同一真相被抄成了多份**？"（SSOT 母体，见下）
4. **母体优先**：先治母体（一次收口），再用母体给出的契约去清存量症状——**不要逐个打补丁**。

**三种产出**：
| 结论 | 动作 |
| --- | --- |
| **母体问题** | 设计"一次消灭 ≥3 症状"的收口方案（进入 Step 3） |
| **孤例** | 按最小改动处理，但记下"是否该升为规则" |
| **症状群但无母体** | 老实登记为 N 个债（**不要编造母体**） |

**本仓三大母体（可直接复用这条经验）**：

| 母体 | 一句话 | 覆盖债 |
| --- | --- | --- |
| **M1 结果契约缺位** | 操作"成没成功/降没降级/真源在哪"未在类型层定型 | TD-02-15/19/24/25 → 一个 `StorageOpResult` 全清 |
| **M2 唯一入口无机器守卫** | 红线只在注释里，绕过零成本 | TD-03-5/02-1/04-15 回潮链 → 补 AST 闸 |
| **M3 SSOT 第二份** | 同一真相被抄成多份，靠"约定"同步 | 代码层(store vs cache) / 契约层(5 种返回形状) / **闸层(清单手写)** / **记录层(规则指向死档案)** |

> **M3 的可怕之处**：它**在每一层都现身**。凡"同一真相被抄成多份"，无论抄在代码、契约、闸还是文档里，**都会漂移**。

---

# Step 3 · 定位 —— 复杂度该落到哪个物理位置？

> **对应心法** §一.2（空间收敛法则）/ **铁律 7（职责定位）** / 铁律 5（不虚构）/ 铁律 6（可逆性）

**要回答的问题**：这个复杂度**应该住在哪**？是下沉、收敛、还是删除？

**动作 —— 六问定位置**：
1. **职责归属？** 该数据的所有方是谁？我是所有方还是消费者（铁律 7 三步判定）？消费者执行所有方的动作 → 停。
2. **真源在哪？** 谁产生这个数据？（产生方 = 真相源，消费方只能派生）
3. **变化速率？** 变化快的和变化慢的**不该同居**（真缝隙的判据）。
4. **有几种实现？** 只有 1 种实现的接缝 = **假接缝**（禁抽象，铁律 5）。
5. **改动半径？** 放这里，改动要碰几个文件/几层？**改动半径最小处 = 最优位置**。
6. **接口窄不窄？** 窄接口 + 厚实现 = 深模块（好）；宽接口 + 薄实现 = 泄漏（坏）。

**三种动作（按优先级）**：
| 动作 | 何时用 | 例 |
| --- | --- | --- |
| **删** | 死代码 / 死参数 / 死 state（零消费） | 本轮删 `_error` 死 state、`presetPrompts` 死 prop |
| **下沉** | 实现只依赖更底层的叶模块 | TD-06-7 → `toAbsoluteFileUrl` 下沉 `core/utils`（环消除，40+ 消费方**零改动**） |
| **收敛** | 同语义 N 处 → 1 处（真复用，非假抽象） | `matchPatternEntry` 合一；`StorageOpResult` 统一 5 函数 |

**关键判断（防"搬新家而非消灭复杂度"）**：
> **下沉/收敛后，我得给出"改动半径"的改前/改后对比。** 若半径没变小，我只搬了家。

**收敛前必须先分辨"重复的种类"（TD-04-21 教训）**：
> 看到"同一件事写了两遍"**不要直接合一** —— 先问它是哪一种：

| 种类 | 特征 | 处置 |
| --- | --- | --- |
| **探测重复**（怎么读数据） | 两处都在回答"这是不是图片 / 是不是 JSON / 是不是在编辑区" | ✅ **收口**为单一原语（纯函数） |
| **判据重复**（要不要放行 / 做什么动作） | 两处看似同一判断，但**所处层级、可用数据不同** | ❌ **不可合并**：合并会削掉某一层的必要能力（如异步兜底） |

> 实证：`useGlobalPaste`（window 层，只看**同步** `items`）与 `onPaste`（items 为空时**必须继续**走 `navigator.clipboard.read()` **异步兜底**）的放行判据**必须不同**；能收口的只有底层探测原语。
> **收口探测、保留判据** —— 且差异要在注释里写明"为什么不同"，否则下一个人会再试图统一一次。

**不过关怎么办**：若"最优位置"需要动 ≥10 个消费方，先问 **"能不能让旧入口 re-export"**（本轮 TD-06-7 正是如此：改 3 文件，40+ 消费方零改动）。

---

# Step 4 · 定契 —— 正确性与"错误透传"怎么设计？

> **对应心法** §零.4（正确性 > 复杂度）/ 铁律 2（类型与边界的绝对诚实）
> **取舍顺序**：物理红线 > **正确性/安全** > 复杂度 > 工期。

**要回答的问题**：失败怎么**诚实**表达？错误在**哪一层炸开**？

**四条禁令（违反即打回）**：
| 禁令 | 反例（本仓真实） |
| --- | --- |
| **禁静默吞** | `.catch(() => {})` 零日志 → 已用 `check:catch` 闸封死 |
| **禁假成功** | 缩略图失败 `copyFileSync` 原图冒充缩略图（TD-03-7）→ 失败返 `null` / `500` |
| **禁错误→错误结果** | 失败时产出"看起来成功但语义相反"的输出 |
| **禁重分类** | 代码 bug 的 `TypeError` 被当"网络错误"重试（TD-03-12）→ 按文案白名单区分 |
| **禁为过闸而重复** | 闸拦住收口 / 下沉时，把同一语义抄成 N 份去买绿灯 —— **闸在生产它本要禁止的行为**（TD-22-7：`videoEditor/core` 导不进纯函数层 → 内联重写 7 处 → **漏 1 处 = 静默错**）。正解 = 按心法 §零.4.3 三步**改闸**（定性 → 清单转反向判据 + 只收窄 → 闸探针先红后绿） |

**动作 —— 三问定契约**：
1. **成功信号诚实吗？** 一次操作的"成没成功 / 降没降级 / 真源在哪"必须在**类型层**可判别。
   - 正解形态：**判别联合**（`{ok:true, source:'kv'} | {ok:true, source:'local', degraded:true} | {ok:false, error:'rejected'}`）
   - 反形态：返回 `unknown` / `'a'|'b'` / `{value, from}` 各发明信号 → 消费方靠脑补 → 复杂度漏进散落 catch
2. **错误在哪炸开？** 必须在**根因发生地**（源头：API 边界 / 表单提交 / 存储入口）**抛错或红日志**。
   - **禁止**把 `catch` 埋到三公里外的 UI 层兜底（那是把复杂度搬家）。
   - 唯一例外：**结构性空 catch**（锁链防断 / logger 防递归 / 浏览器 API 预期不可用）→ 必须显式标 `// catch-ok: <理由>`。
   - ⚠️ **豁免通道口径（2026-09-14 修正 · TD-02-26）**：`// catch-ok:` 目前是**自由文本**、闸只判"标记是否存在" → **理由从不被验证、一句话即可绕过**（实测：全 src **99 处**标记中 **7 处贴在非空 catch 上**，`标记量 ≠ 豁免量`）。
     - **① 写 `catch-ok` 前必须回读被 catch 的代码**，确认失败**真的不需要可见**。**禁写未验证的理由** —— 实测 7 处 `contentSet 内部已分类/留痕` 是错的（`contentStore` 持久化失败**不抛**，catch 实吞的是 bug 级异常）。
     - **② 优先"收口原语"而非逐处标记**：同一"吞"语义手写 ≥3 次即应收口成原语（原语自带理由 + 测试 + 唯一实现），别复制粘贴第 N 份。
     - **③ 禁止用守卫替代 catch**：守卫只管 **contract 违约 → fail-fast**；**运行时可预期失败（IO/网络/解析/浏览器 API）一律 `catch + reportDegrade / logger.warn`**。用"前置守卫"去预防运行时意外，就是**假守卫**的成因（代价更高、还防不住）。
3. **失败可见性给对读者了吗？**
   - **开发者** → `logger`（可查）
   - **用户** → toast（瞬时动作反馈）或 UI 状态（持续状态）
   - ⚠️ **不要混用**：拿 logger 当"用户可见性"= 给错读者；拿 toast 表达"持续状态"= 逝去即失明。**但也不要为"我觉得该有提示"而加**（见 Step 1 三问——先证明用户真的需要）。

---

# Step 5 · 定权 —— 我自己改，还是问用户？

> **对应心法** §零.5（权限边界）/ 铁律 1（依赖也是决策）

**唯一判据（用户原话）**：
> **「结构应该怎么摆？这是架构师应该判断的问题 —— 架构直接判断；但跟业务相关，一定要问用户。」**

| | 架构师**直接定** | 架构师**必须问用户** |
| --- | --- | --- |
| **对象** | 代码结构、函数位置、删重复/死代码、抽 helper、改注释、补守卫、命名 | **删需求、砍功能、增加/减少业务能力、改交互、改对外契约与持久化格式、引入重依赖** |
| **判据** | 改完**功能不变**，只是结构更少/更对 | 改完**用户能做的事变了** |
| **动作** | 直接做，账本记"还债" | **摆成带成本的 A/B/C 选项 + 推荐项**，交用户拍板 |

**摆选项的格式（强制）**：
```
方案 A（推荐）：<做法> —— 成本 <X>，收益 <Y>，风险 <Z>
方案 B：<做法> —— ...
方案 C：不做 —— 后果是 ...
请决策。
```
**禁止**：把真决策退化成问答题；也禁止用"复杂度高"单方面替用户决定不做。

**本仓真实对照**：
- ✅ 自主判断（未打扰用户）：删死 state / 抽 `matchPatternEntry` / 下沉 URL 原语 / 补 `check:catch` 闸
- ✅ 交用户拍板：素材 label 化改造（业务能力）/ TD-01-8 生成链路 contract（业务能力）/ "刷新按钮"是否加（交互）

---

# Step 6 · 施工 —— 最小改动，不留尾巴

> **对应心法** §零.2（能删就删）/ 铁律 5（不虚构）/ **铁律 7（数据权责单向）**

**动作清单**：
1. **只改真风险点**，**不为一致性而改**：
   - 本轮 TD-06-8：7 处 `onChange` 只有 **1 处**（异步路径）有 stale 风险 → **只改那 1 处**。其余 6 处同步处理器无风险 → 不动。
   - 判据：**"改了它，系统整体复杂度下降了吗？"** 答不上 → 别改。
2. **不留尾巴**（每一条都有真实翻车）：
   - 加新注释 → **同步删掉被取代的旧注释**（否则造僵尸文档）
   - 删了 prop/参数 → **删干净**（接口定义、解构、所有分支）
   - 换了实现 → **删旧实现**（死代码不保留"以防万一"）
   - **不要用"成本更低"保留双名/双态** → 先**实测改动点数量**（本轮实测"多处"其实只有 7 处）
3. **删旧不如先证死**：删任何东西前，`grep` 确认**零消费**（含测试引用）。

**自查（施工完立刻做）**：
> - [ ] 我新加的东西，**降低了**整体复杂度，还是把它**藏进了新黑盒**？
> - [ ] 我操作的数据，所有方是谁？该动作的所有方是不是我？我是数据所有方还是消费者？（铁律 7 三步判定；越权代码 → 删）
> - [ ] 我有没有留下"两名指一物"/"两处维护同一真相"？（= 我自己在批的 SSOT 第二份）
> - [ ] 有没有 `_` 前缀变量 / 未被读取的 state（**死代码信号**）？
> - [ ] 注释里描述的东西，**代码里还存在吗**？

---

# Step 7 · 验证与落账 —— 探针 / 测试 / 闸 / 账本

> **对应心法** §零.3（度量口径）/ 铁律 2（诚实）

## 7.0 先分清"验证"与"猜谜"（2026-09-15 加固）

> ### 🔒 探针是**证明"我改对了什么"**的工具，**不是"我猜对了吗"的猜谜机**。
>
> 正确顺序永远是：**① 读懂代码 → ② 写出这个能力的正确形态 → ③ 按它改 → ④ 用探针证明我改的那点被测到。**
> **不是**：① 猜一个假设 → ② 写探针看它对不对 → ③ 再猜一个……
>
> 「老测来测去」的代价不是时间，是**它会替代掉"把这个能力想清楚"**这一步 ——
> 而后者才是唯一能真正解决问题的动作（用户原话：**"把代码改正确，bug 自然而然就解了"**）。
>
> **定位根因靠什么**（按性价比排序）：
> 1. **读代码**（含依赖库源码）—— 最可靠；
> 2. **向用户问关键观察** —— 用户能看到你测不到的东西（真实浏览器行为、界面形态）。
>    实证：2026-09-15 用户的四句话（「按钮外观无变化」「菜单也没关」「看不到拖放区」
>    「出现十字星光标」）**直接锁定病灶**，比 4 个探针文件都有效；
> 3. **探针** —— 只用于**确认修复有效**，或在读代码后**验证一个已经很有把握的判断**。
>
> **反例警示（2026-09-15）**：jsdom **测不出浏览器的手势栈与 `pointer-events` 语义** ——
> 那类问题写多少探针都"不会红"，两轮白费。**先问"这个环境能不能承载我要测的语义"**，
> 不能就别测，去读代码或者问用户。

## 7.1 探针（先红后绿）—— 证明"我的修复真的有用"

**为什么必须做**：不做探针的测试 = 自证式测试（实现断言，测了个寂寞）。

**做法**：
1. **写行为断言测试**（不是"函数被调用"，而是"给定输入 → 期望的可观察结果"）。
2. **先红**：把修复**临时改回旧实现**（或注入违规），**确认测试变红**，且**精确命中**我修的那一点。
3. **后绿**：恢复修复，确认转绿。
4. **若"先红"没红** → 不是"探针没问题"，是**你的断言没测到那一点**：回去重写断言
   （**别跳过**）。但先自查一件事：**这个环境能不能承载这个语义？**
   （见 7.0：jsdom 测不了浏览器手势栈/`pointer-events` —— 那种情况是"测不了"，
   不是"没测到"，正确处置是**改用手工/用户实测**并在轮次文件写明。）

> 🔧 **"临时改回 / 注入"一律走 `node scripts/probe.mjs`，禁手改源码** —— 改文件这个动作本身就是风险源。
> 它把「注入 → 跑 → 断言 → 还原」收成一条命令，并强制三件事：
> **① 注入点唯一**（0 处 / 多处即拒 → 防"红的不是那点"）；**② 还原 sha256 自校验**（防忘还原 = 假账，见附录 A8）；
> **③ journal 兜底**（`scripts/.probe/` 存原文全文，被 Ctrl+C / 崩溃打断也能在下次启动自愈还原）。
> ```bash
> node scripts/probe.mjs --label "TD-xx 负例" \
>   --file <相对路径> --find "<旧实现>" --replace "<新实现>" \
>   --run "<闸 / 单测命令>" --expect-exit 1 --expect-out "<精确关键词，如 x.ts:384>"
> ```
> 结论块（含 exit / 命中行 / 已还原 sha）**直接粘贴进区域日志**当"先红后绿"证据。`--dry` 可先看注入 diff。

**本仓范例**：
- `check:catch` 探针 → 注入 `catch {}` → 闸精确报该行
- TD-06-8 探针 → 还原对象形式 → `expected '#000000' to be '#ffffff'`（**精确证明用户改动被覆盖**）
- TD-02-17 探针 → 注入坏正则 → 加载时带键名抛错
- **2026-09-14 `probe.mjs` 首次自检**：抹掉 `resourceStore.ts:384` 的 `catch-ok` 标记 → 闸精确报 `resourceStore.ts:384`，跑完工作区零污染（`git status src` 空 + journal 已清）

> **反例警示**：如果"先红"步骤没红，说明**我的测试没测到真东西** —— 回去重写，不要跳过。

> **反向探针（TD-04-21 新增）**：**既有测试 = 行为契约**。
> 做「统一 / 收敛 / 合并判据」这类改动时，把既有测试当探针用：
> 改完跑相关测试 → **若变红，先假定"我的收口越界了"，而不是"测试该改了"**。
> 实证：我抽 `shouldPassContentEditablePaste` 两处共用 → `useAssetDropPaste.test.tsx` 立即 1 红
> （CE 内 `items:[]` + `read()` 返回内容的兜底用例被"提前 return"掐死）→ **回退判据统一，仅保留探测收口**。
> 口诀：**测试红 ≠ 测试错；测试红 = 既有语义被破坏。**

## 7.2 测试 —— 只跑相关的

**禁止**：为了一次小改动跑全量（浪费；且全量绿会给你"很安全"的错觉）。
**做法**：跑**受影响的测试文件** + 受影响链路的关键测试。
**新增测试的判断**：我修的是**正确性问题**（数据丢失/失败伪装） → **必须**加行为断言；纯结构重构（无行为变化） → 现有测试覆盖即可。

## 7.3 门禁

跑与改动相关的闸（本仓：`check:catch` / `check:arch` / `check:any` / `tsc` / `lint`）。
**改了契约表/存储键/事件 → 必跑对应闸**（`check:events` / `check:keys`）。

## 7.4 落账（不做=白干）

| 落点 | 写什么 |
| --- | --- |
| 债务索引 `daily/架构日志/债务.md` | 跑 `node scripts/debt.mjs resolve <TD-ID> --note "…"`（**就地改状态；禁手写、禁再追一行**）→ 收尾跑 `node scripts/debt.mjs archive`（已完成项自动移入 `债务-归档.md`，**主表只留待办**） |
| 读债（本步前置） | `node scripts/debt.mjs show <TD-ID>` 取最新真相；`list [--area NN] [--status 待还]` 看待办清单 |
| 区域日志 `<NN>-<区域>-<日期>.md` | 改动、度量（改前/改后）、探针结果、**债描述被修正的地方**。跨区发现 → **只写一份** `<触发区NN>-跨区-<主题>-<日期>.md`，其余区 `@见` 它（不为每个根因区各开一份） |
| `index.md` | **只改该区状态 + 「日志」列链接**（≤2 行）；**禁写"说明段"**（结论唯一落点 = 区域日志；双写会让 index 变叙事堆） |
| DATAFLOW | 链路有变**就地改现状图**；退役/改名改 §已退役表一行；**禁追加 `更新(<日期>…)` 叙事段**（规则唯一出口 = DATAFLOW §维护规矩；过程写区域日志） |
| **工具债**（`scripts/**` · 流程文档 · `package.json` scripts） | **不延后 → 当场修**（细则 + "修完硬要求"三件见 **附录 A10**，唯一真源）；`src/**` · `localTool/**` 仍只登记不动手 |

**度量（§零.3，强制）**：给出**改前/改后可比**的口径——改动半径 / fan-in / 跨层跳跃 / 认知单元 / 重复份数。

---

# 附 A：准确性铁律（本仓踩坑实录 · 2026-09-13 加固）

> 全部来自同一次真实审计（视频剪辑器方案，`docs/123` / `docs/124`）。**每条都真的翻过车。**

| # | 铁律 | 翻车现场（本仓实证） |
| --- | --- | --- |
| **A1** | **引用任何符号前，先 `grep` 确认它存在、且真的导出** | 引 `mediaType.ts`（**不存在**，真源是 `assetType.ts`）· 让外部调用 `contentStore.checkRegistered`（它是 `contentStore.ts:232` 的**内部函数、未导出**）· 引 `UPLOAD_DIRS.videoEditor` 与 `videoEditorOpen`（**均未登记**） |
| **A2** | **方案 ≠ 实现**。"某模块已有 X 能力"必须落到 `文件:行`；只有文档编号 = **未实现** | 我说"复用 `13-文件生命周期` 的引用感知 GC"→ 该能力在 `src/` **0 命中**，13 号是**方案文档** |
| **A3** | **先问"证据能不能自己裁定"，再问用户**。把已由代码事实决定的事拿去"待签"，是浪费决策权 | 我把 Q1「时间单位：秒 / 帧」列为"待你拍板"→ 取证发现 `videoEngine.ts:57+` **早就用秒** → 属架构判断，**直接定**（Step 5） |
| **A4** | **补守卫前，先问"这条守卫本身是不是母体"** | 我提 G-1「在 `check-arch.mjs` 的业务域名单里加 `videoEditor`」→ **它本身就是"手写清单"母体**（下一个业务域必重演）→ 改为**反向判据**「base 不得 import 任何非 base 目录」 |
| **A5** | **先查仓内，再查外部参考**。外部仓库是"参考实现"，不是"仓库里没有"的证据 | 我准备从外部 A 搬「时间轴刻度 / 吸附 / 播放钟」→ 仓内 `director3d/panels/Timeline.tsx` 与 `director3d/App.tsx:1069` **已有**（120 原文只写成"UI 参考"） |
| **A6** | **兜底 / 守卫的数量 = 地基的体温计**。每加一条，先回答"它在掩盖哪条地基缺陷"；答得出 → **改地基、删兜底** | 13 条兜底里 **5 条**是地基缺陷的影子（id 重铸**无根因** / 断链三件套掩盖 `I6` 缺位 / `BoundedConcurrencySource` 根因是 WebKit 而我们跑 Blink / `MIN_SPLIT_MARGIN` / 两套 schema 机制） |
| **A7** | **修正必须回改原文**。把修正只记在"新文档的修正表"里 = **亲手制造 SSOT 第二份** | 我把 6 处修正只记在 123 §四，`120` 原文没动 → 下一个 AI 读 120 仍拿到错的那份（**正是我自己在批的母体**） |
| **A8** | **状态不许预支**："已执行"就必须已执行，"待做"就写待做 | 一次编辑里我先写了"（已执行）"再补做 → 正确顺序是**先做、后写状态**；反过来就是假账 |
| **A9** | **7 步法是"改存量"的**；从零新建走 4 步法，但 **Step 0/3/4/5/7 通用** | 用"改存量"的尺子量"新建方案"时，Step 2（存量归因）/ Step 6（最小改动）**不适用**；硬套会得出错误结论 |
| **A10** | **工具债不延后**：命中 `scripts/**`（闸 / 账本 CLI / 审计工具）· 流程文档 · `package.json` scripts 的缺陷 → **当场修**，只登记一行 `已解决`（**细则见下表后的「A10 执行细则」**） | 2026-09-14 `debt.mjs add` 取号只读主表 → 新债撞已归档 ID；**若延后，此后每次登记都在往账本写坏行**，且读命令按 ID 去重会**静默遮蔽**归档同名债 |
| **A11** | **动工具前先问"它的产物会不会被当真相用"**：会 → **必须修**；判不准 → 按业务债处理（只登记） | `check:catch` 只判"标记存在" → 99 处标记里 7 处贴在非空 catch 上；**闸假绿会污染此后所有"失败可见性"结论**（不只是它自己错） |
| **A12** | **闸不许凌驾正确性**：闸拦住"收口 / 下沉 / 去重"时 —— **改闸，不许把同一语义抄成 N 份**（心法 §零.4.1/§零.4.3） | TD-22-7：`check:arch` 规则 4 只放行 `base/core/idGen.ts` → `core/` 调不到已下沉的映射原语 → 文件头写"严禁在此重写"、正文**内联重写 7 处**；后果「M2 变速要改 7 处，**漏 1 处 = 静默错**」。原文结论：**闸把作者逼成了它禁止的样子** |

> **一句话**：**"存在性"要 grep，"名称"要 grep，"份数"要 grep —— 凡是能查的，一律不许靠记忆和描述。**

### A10 执行细则（**工具债不延后**的唯一真源 · 其他文档只引用本处）

> **范围**：`scripts/**`（闸 / 账本 CLI / 审计工具 / baseline）· `.codebuddy/commands/**`（流程文档）· `package.json` 的 `scripts` 条目。
> **为什么不能延后**：① **工具每次被用都收一次利息**（不像业务债只在对应链路生效）；② **工具的产物会被下游当真相用**（闸假绿 / 账本口径错 → 污染此后所有结论）。
> 实证：`debt.mjs add` 取号只读主表 → 新债撞已归档 ID，而读命令按 ID 去重（主表优先）会**静默遮蔽**归档同名债 —— 延后 = 此后每次登记都在往账本写坏行。
> **红线（例外 ≠ 放开）**：🚫 `src/**` · `localTool/**` · 借机重构工具（"顺手把 X 也整理了"）· 改业务流程 / 对外契约 / 持久化格式。**判不准是不是工具债 → 按业务债处理（只登记，不动手）。**

**修完的硬要求（三件，缺一即未闭环）**：

1. `node --check <file>`（或等价语法/自检）通过 + **实跑一次**证明行为已对；
2. **先红后绿证据**：修复前错误行为 + 修复后正确行为，**同命令同参数**对照，落轮次文件。**注入 / 临时还原一律走 `node scripts/probe.mjs`**（禁手改源码 —— 它强制注入点唯一 + 还原 sha256 自校验 + journal 兜底），结论块直接粘贴：
   ```bash
   node scripts/probe.mjs --label X --file <f> --find "<旧>" --replace "<新>" \
     --run "<cmd>" --expect-exit 1 --expect-out "<精确关键词>"
   ```
3. 账本 **一行 `已解决`**（含"怎么修的"一句），**不进主表待办**。

> **注意**：`债务登记5步法.md`（审 / 只读）**只规定"发现工具债不延后"这一句，不含本细则** —— 施工规范写进只读流程 = 让审流程被当成施工流程。要改这段话，改这里。

---

# 附：退回机制（任一步发现前面错了，立即退回）

| 在做什么 | 发现 | 退回 |
| --- | --- | --- |
| Step 2 归因 | 找不到共同的母体 | 退回 Step 1：可能这批"问题"里混了假需求（先逐个证伪） |
| Step 3 定位 | 最优位置要动 ≥10 个消费方 | 退回 Step 3 重选：能否用 re-export / 下沉让消费方零改动？ |
| Step 4 定契 | 契约形状要改（判别联合等） | 退回 Step 3：契约是地基，位置错了契约白定 |
| Step 6 施工 | 签名/结构兜不住需求 | 退回 Step 3/4 重定，**禁止硬填** |
| Step 3 收敛 / Step 6 施工 | 收口后**既有测试变红** | 退回 Step 3：很可能是把"**判据重复**"误当"**探测重复**"合并了 → 只收口探测、恢复判据分层 |
| Step 7 探针 | "先红"没红 | 退回 7.1 重写测试；测试不红 = 没测到东西 |
| Step 0 普查 | 同类能力**已有 ≥2 份**，而我正准备再写一份 | **退回 Step 0**：先定共用层（下沉 / re-export），再动手——否则我就是第 N+1 份 |
| 任何一步 | 引用到的**符号 / 文件 / 能力其实不存在**（或未导出） | **退回当前步重新取证**（附录 A1/A2）：先把名称落实，再谈方案 |
| Step 7 落账 | 修正**只记在本轮文档**、不回改被修正的原文 | **退回 7.4**：回改原文——否则制造新的 SSOT 第二份（附录 A7） |
| **任何一步** | 命中 `scripts/**` · 流程文档 · `package.json` scripts 的缺陷 | **不停工**：当场修 —— 细则与"修完硬要求"三件全在 **附录 A10**（唯一真源） |
| Step 3 定位 / Step 6 施工 | 最优位置 / 收口动作**被某道闸拦住** | **别绕**：按心法 §零.4.3 三步裁决 —— ① 定性（红线闸 or 结构偏好闸，查 `CLAUDE.md` 级证据）② 结构偏好 → **直接改闸**（清单→反向判据、**只收窄不放宽**）+ 闸探针先红后绿 ③ 红线闸 → 交用户。**禁止**退化成"就地重写 N 份" |
| Step 7 落账 | 我想在 `index.md` 写本轮"说明段"，或为每个根因区各开一份区域日志 | **删 / 并**：结论只写**一份**轮次文件（跨区 → `<触发区NN>-跨区-<主题>-<日期>.md`），index 只留状态 + 链接 |
| **任何一步** | **发现"需求"本身不存在** | **退回 Step 1**（这是最常见也最该退的一步） |

---

# 附：速查决策树

```
拿到一个问题/债/需求
  │
  ├─ Step 0 仓库里已有什么？───── 同类已 ≥2 份 → 先定共用层（下沉/re-export），不做第 N+1 份
  ├─ Step 1 它真存在吗？────────── 否（假需求）→ 改判非债 + 留痕，结束
  │                              └ 是业务性 → Step 5 问用户
  ├─ Step 2 孤例还是母体？──────── 母体 → 一次收口（≥3症状）
  ├─ Step 3 该放哪？───────────── 先判数据/动作所有方（铁律 7）→ 删 / 下沉 / 收敛（分清"探测重复=收口 / 判据重复=保留"）
  ├─ Step 4 失败怎么诚实？──────── 判别联合 + 根部炸开 + 读者正确
  ├─ Step 5 我改还是问？───────── 结构→自己定；业务→摆A/B/C选项
  ├─ Step 6 最小改动不留尾巴
  └─ Step 7 探针先红后绿 → 相关测试 → 闸 → 落账+度量（含回改原文）
       └ 途中命中 `scripts/**` 工具债？→ **当场修、不延后**（只留一行"已解决"）
          途中想在 index 写说明段 / 开多份区域日志？→ **删/并**（结论只写一份轮次文件）
```

> **一句话总结**：**先普查，再证伪，后归因，再定位，定契，定权，施工，验证。**
> 其中**最容易跳过、代价最大的是 Step 1** —— "这个需求不存在"是架构师能给出的最高价值判断，
> 也是最难给出的判断（因为它要求你**否定手上已有的东西**）。
