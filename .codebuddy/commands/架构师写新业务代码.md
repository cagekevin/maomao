---

# 架构师新建 4 步法 · **V2**（从零构建 SOP）

> **版本**：本文即 **V2**（原地取代 V1；V1 原文见 git 历史，不再维护）。
> **共用部分**：与 `架构师改码7步法.md` 共用的判据 / 命令 / 模板，**真源分工见 §0.2**（改指 ADR 与既有命令文件；原指向的 `架构师共用判据.md` **不存在**，2026-09-24 已回改）。

**核心心法**：改码是「治病排雷」，新建是「优生防御」。新建没有历史病灶可对照，
必须靠**前置模型设计 + 机器门禁**杜绝把新代码写成明天的技术债。
**全局铁律**：**先想清最优解 → 再定真相 → 再设防腐 → 死磕失败 → 最后画 UI。** 敢从 UI 组件往下写，一律打回。

---

## 0 · 总纲（**先看这一节，否则后面全是形式**）

### 0.1 本文边界

| 场景 | 用哪份 |
| --- | --- |
| **从零构建**新业务 / 新链路 / 新模块 | **本文（V2）** |
| 存量改造 / 修 bug / 清债 | `架构师改码7步法` |
| 单次改动的交付纪律 | `写代码4步法` / `架构5步法` |
| 债怎么登记 / 怎么归档 | `债务登记5步法` |
| 日常排错 | `排查5步法` |

### 0.2 判据真源分工（防两份 —— 本仓最贵的病）

> ⚠️ **2026-09-24 回改**：本表原 7 行真源全指 `架构师共用判据.md` §1–§8，
> **该文件全仓不存在**（`grep -rl "架构师共用判据" --include="*.md" .` **只有本文自己命中**）
> ⇒ 7 个**空指针**，照原表查必然扑空。现改为指向**真实存在的落点**（已逐条实测）。

| 判据 | 真源 | 本文 |
| --- | --- | --- |
| 最优解 / 防重复（A14） | **`架构师改码7步法.md` Step 0**（`grep -n "先写出最优解" .codebuddy/commands/架构师改码7步法.md`）· 心法 §③ | 只引用 |
| 禁自证式断言（A15） | 本文 §4.7 + `架构师改码7步法.md` Step 7.1（探针先红后绿） | 只引用 |
| 失败契约字段口径（判别联合） | `ADR-0002` · `ADR-0003` · `ADR-0021` | 只引用 |
| 生产者↔消费者三铁律 · 兜底六形态与净减 | `ADR-0019`（兜底六形态）· `CLAUDE.md` §5.1（三铁律） | 只引用 |
| 探针 / 凭证归档 | `架构师改码7步法.md` Step 7（`grep -n "反向探针" .codebuddy/commands/架构师改码7步法.md`） | 只引用 |
| 建闸前置评审 / 成本守恒 | `ADR-0016` · `架构师心法` §零.4 | 只引用 |
| 内容落点（判据→ADR…） | `ADR-0025` | 只引用 |
| **四零一毁 / 目录与层归属 / 幽灵预留必接线 / 新建模板** | **本文（定义段）** | **持有** |

⇒ **改动任一判据，须同时检查另一份是否仍成立。**

> **ADR 是判据本体的真实落点**（`docs/adr/`）：查法 `node scripts/adr.mjs search <关键词>`。
> **ADR 不可变**（改主意须写新条取代），故它**不会像文件路径那样漂移** —— 这正是本表改指 ADR 的理由。

### 0.3 红线 还是 结构偏好（写规则前先问）

> 取自 ADR-0025：**撬不动 ⇒ 红线；撬得动但要有代价 ⇒ 判据/偏好**。
> 本文每条标准都标了类别。**红线不可让步；偏好可在"写下理由"后背离**（写理由即可，不需批准）。

### 0.4 本文判据的写法要求（**V2 的硬约束**）

每条标准必须同时给出三件事，缺一不许写进本文：

1. **红线**（禁止什么）；
2. **验收**（合规时长什么样）；
3. **🔍 违反时怎么发现**（`grep` 命令 / 可执行步骤 / 代码形态）—— **答不出 ⇒ 这条是口号，删或降级为建议**。

### 0.5 不变量下沉（**本文第一原则 —— 决定规则有没有用**）

> **规则写在文档里 = 没有强制力。** 写任何规则前先问：**它能下沉到哪一层？**

| 层 | 强制力 | 手段 | 成本 | 用途 |
| --- | --- | --- | --- | --- |
| **L0 文字** | **无** | ADR / 文件头 / 注释 | 极低 | **只解释"为什么"**，不作防线 |
| **L1 类型层** | **编译期报错** | 必填字段 · 判别联合 · `as const` + 穷尽分支 | 低 | **优先** |
| **L2 唯一入口收口** | 结构上绕不过 | 单一出口 + **内部分流**（`filesApi` / `contentSet` / `httpRequest`） | 中 | 同一能力只留一个出口 |
| **L3 闸** | CI | `check:keys` / `check:events` / `check:node-types`… | 高 | **默认不建** |

**判据：能下沉就下沉；停在 L0 的规则必然会漂移。**

- **实证（2026-09-19，同一份文件里 L0 与 L1/L2 打架）**：`filesApi.ts` 头写「失败一律返回 `null` 不抛」，
  实际已是 `UploadOutcome` 判别联合；头写 `persistUrlToUploads` 是★唯一原语，
  实际 `uploadFileToLocal` 被 **11** 处消费、它只有 **4** 处 ⇒ L0 那半边没人强制，于是背离。

**与 `架构师心法` 的对应**（本表即把心法映射成"新建时的落地顺序"）：

| 心法 | 对应本表 |
| --- | --- |
| Phase 2 筑基与定海（划定**不变式**与**唯一真相**） | **L2** 唯一入口收口 |
| 三.2 捍卫**类型**与边界的"绝对诚实" | **L1** 类型层 |
| 零.4.1 **闸不是红线本身** + 成本守恒律 | **L3**（闸 ≠ 红线；能用 L1/L2 守住的**不许建闸**） |

> ⚠️ 心法零.4.1 的反例：`check:catch` 因「贴一句 `// catch-ok:` 的**合法通过成本低于真正改造**」
> 而**生产它本要禁止的行为** ⇒ 已于 2026-09-17 删除。**建闸前必读该节。**

### 0.6 本文自身的验证与维护（**治本：不再存快照**）

> ⚠️ **2026-09-24 回改（原因实证）**：本节原写「谁改了被本文引用的东西，谁负责回头更新本文」+
> 「本文引用了 15+ 个落点，它们会过期」—— **该防线已实证失败**：本次实测本文已腐 **12 处**
> （6 处子目录化 + 4 处跨域搬迁 + 1 处漏前缀 + 1 处行号），而 V2 落盘后**无人回来改过一次**。
> 结论：**「靠自觉回头更新」= `ADR-0052` 第 6 级（无机器防线）；而"存快照"本身就是在制造腐坏面。**

**治法：不存快照。** 本文的落点一律写成 **`grep` 查法**（查符号名，不查行号）：

| 不写（会腐） | 改写成（不腐） |
| --- | --- |
| `contentSet` @ `base/core/contentStore.ts:582` | `grep -rn "export function contentSet" src/components/base` |
| `NodeShell` @ `base/ui/NodeShell.tsx:324` | `grep -n "ErrorBoundary" src/components/canvas/parts/NodeShell.tsx` |

**为什么查法不腐**：`grep` 匹配**符号名** —— 符号名是真源（改了它就该改引用），
行号只是它的**瞬时坐标**（每次无关改动都会平移）。**"必然腐的东西"不进文档，是唯一治本的做法。**

**仍存的少量路径**（如 `localTool/src/router.ts`、`src/hooks/`）只到**目录/文件名**粒度 ——
它比行号稳得多，且**改名会被 `check:arch` / `tsc` 立刻发现**。

**若本文件的某处仍腐了，最小动作**（改动 `base/` 或本仓落点后）：

```bash
# ① 扫本文残留的 文件:行号 快照（应为 0；有 ⇒ 补改成查法）
grep -nE "\`[A-Za-z0-9_./-]+\.(ts|tsx)(:[0-9-]+)\`" .codebuddy/commands/架构师写新业务代码.md

# ② 引用过的 ADR / 债号 / 工具是否仍存在
node scripts/adr.mjs search <关键词> · node scripts/debt.mjs search <关键词>
ls scripts/<工具名>.mjs
```

- **描述与签名冲突时，以函数签名为准**（`filesApi.ts` 头部与 `@returns` 都曾与真实签名漂移）。
- 发现漂移 ⇒ **就地改**（A7），并在 note 里写明"旧描述错在哪"，**不要只在新文档里加修正表**。

---

## 1 · 交付金标准：四零一毁

### 1.1 数据 0 副本（状态绝对收敛）—— **结构偏好（强）**

- **红线**：同一个真相（SSOT）在物理内存只能有一份；UI 不许 `useState` 复制核心业务数据。
- **边界**：
  - **核心业务数据** = 会在别处被读/被落盘/被别人改的（如 items 列表、选中 id、落盘结果）⇒ **必须住 store**；
  - **纯 UI 状态** = 只在组件内用、不落盘、无他人关心（受控输入草稿、折叠展开、hover、局部筛选）⇒ **允许 `useState`**。
  - 判据问句：**「这个 state 删掉，别处会失忆吗？」** 会 ⇒ 是业务数据，不许放组件。
- **🔍 违反时怎么发现**：
  - `grep -n "useState" <组件>` 且该 state 初值来自 store/props ⇒ 副本嫌疑；
  - 出现 `useEffect(() => setLocal(x), [x])` 式**双向同步** ⇒ 铁证；
  - 手测：改 store 的值，UI 局部 state 不同步 ⇒ 已分裂。

### 1.2 异常 0 猜测（类型逼迫处理）—— **🔴 红线**

- **红线**：契约自带强迫性，不让调用方猜。禁止 `Data | null` / 隐式 `throw Error`；
  失败分支必须带**可展示文案**（否则消费者只能自己编话术 —— 那是"编造真相"）。
- **契约形状 / 字段口径（`code` + `message`）/ throw 边界 / 🔍 违反时怎么发现** ⇒ **真源 = `ADR-0002` · `ADR-0003` · `ADR-0021`**（`node scripts/adr.mjs search 判别联合`；本文不再复制）。
- **ACL 侧**：第三方 / 浏览器 API 的 throw 必须在**边界**转成判别联合（模板见 §3.2）。

### 1.3 UI 0 业务（无脑视图）—— **结构偏好**

- **红线**：UI 只做「状态投影仪 + 指令发射器」；业务条件由 core 算完，只吐视图指令。
- **本仓真实例子**（本仓**无角色系统**，勿套用"权限判断"类举例）：
  - ❌ **反例（真实发生过）**：首屏加载失败时，UI 写 `if (items.length === 0) return <空态/>`
    ⇒ **把"加载失败"渲染成"该目录暂无素材"**（失败被伪装成空）。
  - ✅ **正例**：`ResourceLibrary`（`grep -rn "class ResourceLibrary\|function ResourceLibrary" src/components`）——
    数据层给出 **`state.phase`（`ok`/`failed`）** 按 phase 投影文本；
    且首屏失败 `loadError` 与翻页失败 `loadMoreError` **分开两个 state**（查法：`grep -n "loadError\|loadMoreError" <该文件>`），
    注释写明原因：**「两者的重试动作不同（重试第 1 页 vs 续下一页），共用一个状态会把重试接到错的动作上」**。
- **🔍 违反时怎么发现**：
  - 组件里出现对**数据层字段的语义推断**（`length === 0` 当"没有"、`!x` 当"失败"）⇒ 越权；
  - 组件里出现跨实体判断（额度/权限/状态机/重试语义）⇒ 越权。

### 1.4 架构 1 键毁灭（可废弃性）—— **结构偏好，且只适用于业务模块**

- **红线**：新业务可随时安全切除：删掉业务目录 + 改路由/注册表 1~2 行，项目零错跑通。
- **⚠️ 适用范围**：这条**只约束"业务模块"**，**不适用于横切地基**
  （`logger` / `contentStore` / `idGen` / `contracts` / `httpClient`…）。地基本就该被依赖，
  拿"能不能删"量它们 ⇒ 得出错误结论（会去拆地基）。
- **🔍 违反时怎么发现（可执行）**：删目录 + 改注册 ⇒ 跑 `npx tsc --noEmit` 必须 **0 错**；
  再跑 `npm run check:arch` + `npm run check:dead-code`（确认没留孤儿引用）。

---

## 2 · 施工 4 步法

### Step 0 · 先写最优解 + 普查拦截（唯一开工前置）

> 🔒 **第一动作不是"查"，是"先写出最优解"**（真源：`架构师改码7步法` Step 0 + A14）。

**0.1 一屏以内写出「正确落地时长什么样」** → **真源 = `架构师改码7步法.md` Step 0**（查法：`grep -n "先写出最优解" .codebuddy/commands/架构师改码7步法.md`；本文不再复制）。

**0.2 新建三问**（存量改造不会有）：
1. **真的需要"新"吗**？（新建模块是最贵的选项，能加几行就别建）
2. **边界在哪**？（哪些**确定**属于它、哪些**确定**不属于；模糊 ⇒ 会长成万能模块）
3. **一键毁灭点在哪**？（需要它不与谁耦合）

**0.3 普查（三查）**：查原语（§5-B 速查表）→ 查相似链路 → **🔥 横向盘查**（同需求在别入口怎么满足的）。
→ **真源 = `架构师改码7步法.md` Step 0 第 3 步**（查法：`grep -n "横向盘查" .codebuddy/commands/架构师改码7步法.md`；本文不再复制）。

**0.4 放行判据（红线）** → **真源 = `架构师改码7步法.md` Step 0 开工指令**（查法：`grep -n "开工指令" .codebuddy/commands/架构师改码7步法.md`）。
**产出**：表①最优解（一屏）· 表②入口对照表 —— **未出不进 Step 1**。

### Step 1 · 定真相与边界

1. **SSOT**：锁定核心数据的唯一物理位置。
2. **所有权**：生产者/消费者划分；**消费者严禁执行所有方的动作**，只能发指令。
3. **禁幽灵预留（YAGNI）** —— **本文定义段，其余处只引用本节**。
   - 只为当前真实需求写接口；禁为"以后可能"预留 `interface AbstractX` / 无效参数。
   - 判据：**指不出"现在谁在什么场景用它" ⇒ 删**（本仓实证 TD-05-16：`onApply` 的 `fragment` 参数三处上抛恒等于 `item.prompt`）。
   - 🔍 **怎么发现**：`grep -rn "<新符号>" src` ⇒ **除定义处外 0 命中** 即为幽灵预留；
     `grep -n "interface Abstract\|TODO\|以后" <新模块>` 抓"为未来预留"的痕迹。
4. **钉死模型**：强类型写死，消灭 `any` 与无意义 `Record<string, unknown>`。
   - **类型权威下沉到"最低变化速率层"**（纯逻辑层，无 React/存储依赖）；快照层/节点 props/UI **引用它**。
   - 判据：**具名类型没人 import ⇒ 权威虚设**（实证 TD-05-18）。
   - 🔍 **怎么发现**：`grep -rn "import .*<MyType>" src` 是否 **0 命中**；
     `grep -rn "Record<string, " src` 是否各处仍在手抄（手抄 = 权威没下沉）。
5. **🔥 建了原语 ≠ 完成 —— 必须同时规定「谁必须调它」**（本仓最痛教训 · TD-05-12 母体）
   **本节为定义段；Step 4 第 3 条 / 自检 / 审计 prompt 第 8 条只引用本节。**

   每个新增原语写两行：**① 谁必须调它（路径清单，必须穷尽）② 验收方式**。
   - **只列一条 = 假接线**（实证 TD-05-13：漏了 Agent 路径，单测照样绿）；
   - 验收 = **生产消费 > 0**（`grep` 除定义处外真有命中；**只有测试命中 = 幽灵预留**）；
   - 能用类型就别用力（`as const` + 穷尽 switch ⇒ 忘接线**编译期报错**）。

### Step 2 · 定契约与防腐层

1. **ACL 防腐层**：外部/旧接口/第三方数据**必须在边界洗脱**，转成 Step 1 的纯净类型后才准进 core。
   - 🔍 **怎么发现**：`grep -n "as unknown as" <新模块>` / `grep -n "as any" <新模块>`（绕过类型洗脱）；
     core / adapter 之外出现 `Record<string, unknown>` 形参 ⇒ 脏类型已进 core。
2. **失败契约（判别联合）**：见 §1.2（**`code` + `message`**）。
   - 🔍 **怎么发现**：`grep -nE "\): (Promise<)?[A-Za-z]+ \| null" <新模块>`（`T | null`）· 逐个看 `ok:false` 分支是否带 `message`。
3. **切断默认值兜底**：查不到 ⇒ 抛错或 `NotFound`；**严禁私自塞 `""` / `{}` 让逻辑跑通**。
   - 🔍 **怎么发现**：`grep -nE "\?\? *([\"']{2}|\{\}|0)" <新模块>`（静默补默认值）· `grep -n "?? \[\]" <新模块>`。
4. **异常也要洗脱**：第三方/浏览器 API 的 **throw 在 ACL 转成判别联合**
   （`try { … } catch (e) { return { ok:false, code:'InvalidPayload', message: String(e) } }`），不许把裸 throw 放给 core。
   - 🔍 **怎么发现**：`grep -n "throw " <新模块>` ⇒ 逐个确认是「不该发生」的编程错误（ADR-0005 那种根部抛），
     凡是"预期会失败"的分支都必须改成判别联合返回。

### Step 3 · 核心组装与 UI 投影

1. **纯化运算**：「怎么算」抽成无外部依赖的纯函数（可单测）。
   - 🔍 **怎么发现**：`core.ts` 里出现 `use*`（hook）、`from 'react'`、直接读 store ⇒ 已被污染。
2. **中心调度黑盒（Store / Core）**：收敛数据与规则，对外**窄接口**；外部"无脑传参"，不在外部拼业务逻辑再调。
   - **store 选型见 §4.3**（两种模式，**按域一致**）；真源 + 快照引用稳定（范本查法同 §4.3）。
   - 🔍 **怎么发现**：`grep -n "^export" store.ts` 看导出面是否过宽（理想只剩「指令 + 1 个视图 hook」）；
     `grep -rn "<新模块>" src` 看调用方有没有"先拼一段业务逻辑再调"（拼装逻辑应在 core）。
3. **UI 极致降级**：UI 只做投影 + 派发（见 §1.3）；横切出口见 §4.6 表。
4. **生命周期 / 并发 / 取消**（V1 完全没有，V2 补齐 —— 新业务最容易忽视的一类）：
   - **订阅必须可退订**：`useSyncExternalStore` / `contentSubscribe` 的退订函数要在 effect cleanup 里释放；
   - **落盘要节流**：高频写入用 `createDebouncedPersist`（查法：`grep -rn "createDebouncedPersist" src/components/base`），
     不要每次 mutation 都直接 `contentSet`；
   - **异步必须可取消 + 有超时**：`AbortController`（本仓 9 处在用）+ `withTimeout`（查法：`grep -rn "export function withTimeout" src/components/base`）；
   - **并发**：本仓**没有通用并发锁原语**（`withLock` 仅 `projectMemoryStore` 内部私有，查法：`grep -rn "withLock" src/components/base`）
     ⇒ 需要串行化时要么复用该模式、要么明确论证"不会有并发"，**不许口头假设**。
   - 🔍 **怎么发现**：`grep -n "useEffect" <新模块>` ⇒ 每个订阅是否都有 cleanup；
     `grep -n "await " <新模块>` ⇒ 每个长任务是否都有超时或取消路径。

### Step 4 · 机器验收与落闸

1. **测试闭环**：核心纯函数 + Core 边界必须覆盖；**只测黑盒输入/输出**；**失败分支必须断言**。
   **🔒 禁自证式断言**（判据见 §4.7 末）：判据 —— **把被测实现改坏，断言仍绿 ⇒ 它是装饰，删**。
   验收入口 = **探针先红后绿**（真源：`架构师改码7步法.md` Step 7.1；查法：`grep -n "反向探针" .codebuddy/commands/架构师改码7步法.md`）。
   - 🔍 **怎么发现**：对每条断言跑一次探针（改坏实现 ⇒ 必须红，见共用 §6 命令与**退出码**坑）。
2. **落闸**：**默认不建**；先过「建闸前置评审」（真源：`ADR-0016` · `架构师心法` §零.4）。能用**结构 / 类型 / 唯一入口 / 对账测试**守住的，**不许用闸**。
   - 🔍 **怎么发现**：该闸脚本头部是否有【申诉口】三问（可 `grep "★闸的申诉口" scripts/`）；是否登记在
     `gates.manifest.json`；**没登记 = 从没跑过**（本仓实证：曾有两道闸不在 manifest 里、躺着真实违规）。
3. **接线验收**：对应 Step 1 第 5 条 —— 逐个新原语 `grep` 确认生产消费 > 0；每条写入口逐条核对真的接了。
   - 🔍 **怎么发现**：`grep -rn "<原语>" src` 去掉定义处与 `*.test.*` 后是否仍有命中（**只有测试命中 = 没接**）。
4. **全局登记**：链路有全局影响 → `spec/DATAFLOW.md`；新存储键/事件名 → 中心登记表；
   确立/推翻约定或判据 → `node scripts/adr.mjs add`（**判据归 ADR，说明归区域日志**）。
5. **裁决闭环**：施工图里标了「待/TODO/TBD」的，交付前**必须给结论**（或明确"不做"）。
   判据：`grep` 施工图「待」⇒ 逐个有结论（实证 TD-05-17）。

---

## 3 · 发版前自检（有一项 ❌ 即打回）

- [ ] **最优解**：动手**前**写下了吗？还是边写边想？
- [ ] **横向盘查**：列了「入口 → 现有实现 → 已满足 → 处置」表吗？别处已有更强实现而我**又写了一份**？
- [ ] **接线**（Step 1.5）：每个新原语**生产消费 > 0**？该真相的**每条写入口都列全并接上**？
- [ ] **裁决闭环**：还有「待/TODO/TBD」没结论吗（结论想好但没回写 = 下一个人按旧的做）？
- [ ] **数据 0 副本**：只有 1 个真源？组件里没偷偷存副本？
- [ ] **防腐**：外部数据全经 Adapter 洗脱？**异常也在边界转成了判别联合**？
- [ ] **异常**：失败路径都用判别联合诚实上报？没有 `null` / `catch {}`？
- [ ] **UI**：已掏空业务判断？**没有把"失败"渲染成"空态"**？
- [ ] **测试**：每条断言都能**改坏实现即变红**？新边界用机器闸固化了吗（该闸过了评审吗）？
- [ ] **毁灭**：（业务模块）删掉它，系统还能跑通吗？（**横切地基不适用本项**，见 §1.4）
- [ ] **渲染异常**（§4.9）：节点走 `NodeShell`（自带边界）？**没自己再造一个 ErrorBoundary**？
      非渲染期异常走 `logger`／判别联合，**没有**指望错误边界兜？
- [ ] **生命周期／并发／取消**（Step 3.4）：订阅都有退订？高频落盘用了节流？长任务有超时或取消？
      说"不会有并发"有论证吗（本仓**无通用锁原语**）？
- [ ] **遥测**：**没有**自己引入第三方 SDK？（本仓暂无遥测，见 §5-B；已登记 TD-18-33，纯本机方向）
- [ ] **若 = 新节点**（§4.8）：`NODE_TYPES` / `NODE_HANDLE_CONTRACT` / `nodeDataSchema` 都登记了？
      跑过 `check:node-types` / `check:node-handles` / `check:node-data` 吗（**在 build 阶段，不在 push**）？

---

## 4 · 模板集（**落点均为本仓实测**）

> ⚠️ **4.1–4.7 只示范结构，不是最优解。**
> `myBusiness` 是**玩具域**——它能保证"结构不违规"，但保证不了"这是本仓该有的样子"。
> **动手前先读 §5-H 的真实范本**（`projectStore` / `scriptBoxPlaybookStore` / `ResourceLibrary` 等），
> **理解了范本之后**再把本模板当骨架用。
> 本模板**已知的两处刻意简化**：① 错误码（`'NotFound'|'Timeout'|'InvalidPayload'`）是示例，
> 真实模块用**本域**的字面量码并在 `contracts.GEN_ERRORS` 登记；② 没有演示订阅/清理（真实 store 多半需要）。
> 另外：**若新业务 = 新画布节点，直接看 §4.8**（那是本仓最常见形态，另有一套必备接线）。

### 4.1 目录（物理隔离 + 一键删除）

```
src/components/<域>/        # 例：creative/ · scriptbox/ · director3d/ · agent/
├─ types.ts      # Step1 唯一模型（SSOT）
├─ adapter.ts    # Step2 ACL（数据 + 异常 双重洗脱）
├─ core.ts       # Step3 纯函数（无 React / 无存储）
├─ store.ts      # Step3 状态真源（**按域选型，见 4.3**）
├─ index.ts      # 对外窄 API
└─ views/        # UI 木偶
```
**跨域 hooks** 放 `src/hooks/`（本仓既有，例 `patchNodeDataById`——查法：`grep -rn "export function patchNodeDataById" src/hooks`）。
**import 约定**：外部只 `import { x } from '@/components/<域>/index.ts'`（禁直指内部文件）；
**显式 `.ts`/`.tsx` 后缀**（本仓 `src` 实测带后缀说明符 **1524 处**）。
⚠️ 别引错：ADR-0037 管的是「**工具不得替调用方决定 import 后缀**」，不是"必须写显式后缀"这条约定本身。

**命名强制项 —— 域专用物必须带域前缀**（用户裁定 2026-09-19，**强制，非可选**）：

> 凡是**只服务某一个域**的导出（原语 / 适配壳 / 常量 / 组件），**名字里必须带该域名**。
> 目的：避免与通用原语**撞名** ⇒ 调用方 `import { x }` 出现两个来源，误引且难查。
> - ❌ 反例（本仓实证）：`videoEditor/lib/logger.ts` 导出的也叫 `logger`，与统一 `base/core/logger` 撞名 ⇒ **TD-18-42**。
> - ✅ 正例：`videoEditorLogger`（域适配壳）、`scriptBoxEngine` / `scriptBoxPlaybookStore`（域前缀 + 域内聚）—— 名字自带域名。
> - ⚠️ **反例**：`canvasHost` 名里**没有**域前缀（它只在 Agent 域用，却叫通用名）⇒ 应改名或至少在文档里写明作用域（见 §5-B 与 TD-18-34）。
> 🔍 **怎么发现**：`grep -rn "export \(const\|function\|class\) <通用名>" src` 若**命中 ≥2 处**且分属不同域 ⇒ 违规（需加前缀或证明是同一实现）。
> 相关：完整前缀与目录规范见 **TD-18-41**。

### 4.2 types.ts（模型 + 失败契约）

```ts
export interface MyItem { readonly id: string; readonly title: string; readonly count: number }

/** 错误码：字面量联合，**不用 enum**（本仓 `src` 全域 `export enum` = 0，实测） */
export type MyBizErrorCode = 'NotFound' | 'Timeout' | 'InvalidPayload';

export type BizResult<T> =
  | { ok: true; data: T }
  | { ok: false; code: MyBizErrorCode; message: string };   // ✅ message；❌ 禁用 msg

export interface MyViewState {
  readonly items: MyItem[];
  readonly isDisabled: boolean;
  readonly warningText: string | null;
  /** **失败态必须单独有字段** —— 与"空"分开（否则 UI 只能靠 `items.length === 0` 猜，把失败渲染成空态） */
  readonly errorText: string | null;
  readonly loading: boolean;
}
```
❌ 禁止：`export enum` · `interface AbstractX`（YAGNI）· `Record<string, unknown>` 当业务对象 ·
`MyItem | null` 代替 `BizResult<MyItem>` · 失败分支无可展示文案。

### 4.3 store.ts —— **两种模式并存，按域选型**

| 模式 | 本仓实测 | 适用 |
| --- | --- | --- |
| 模块级真源 + `useSyncExternalStore` | **22 处**（`base/store` 8 · `base/core` 3 · `src/hooks` 2 · videoEditor 非 stores 部分 5+） | **默认** |
| `zustand` `create` | **7 处，全部在 `src/components/videoEditor/stores/`** | **仅该目录内** |

> **判据**：**跟同域/同层的既有 store 保持一致** —— 一致性优先于"全仓统一一种"。
> 新建 store **默认走 `useSyncExternalStore`**；只有落在 `videoEditor/stores/` 内才跟 zustand；**禁止同一域混用两种**。
> 快照必须**引用稳定**（否则无限重渲染）：本仓范本 `projectStore`（查法：`grep -n "getSnapshot" src/components/base/store/projectStore.ts`）。

### 4.4 adapter.ts（数据 + 异常 双重洗脱）

```ts
export function mapExternalToMyItem(raw: unknown): BizResult<MyItem> {
  if (typeof raw !== 'object' || raw === null) return { ok:false, code:'InvalidPayload', message:'外部数据格式非法' };
  const r = raw as Record<string, unknown>;
  // ⚠️ 必填字段缺失 = 失败；**不许 `?? ""` / `?? 0` 静默补**（那正是本文禁的默认值兜底）
  if (typeof r.id !== 'string' || typeof r.title !== 'string' || typeof r.count !== 'number')
    return { ok:false, code:'InvalidPayload', message:'字段类型不符' };
  return { ok:true, data:{ id:r.id, title:r.title, count:r.count } };
}
```

### 4.5 io.ts（不裸 fetch / 不裸 throw / 不吞错）

```ts
export async function fetchMyList(): Promise<BizResult<MyItem[]>> {
  try {
    const body = await httpRequest('/api/my-list', { label: 'fetchMyList' });   // 禁裸 fetch
    // ⚠️ 先校验**形状**再逐个过 ACL：**不要把整个数组塞给单元素 mapper**
    // （数组 `'id' in []` 为 false ⇒ 那样写会永远返回 ok:false —— 本模板前一版就犯过这个错）
    if (!Array.isArray(body)) return { ok:false, code:'InvalidPayload', message:'响应不是数组' };
    const items: MyItem[] = [];
    for (const raw of body) {
      const r = mapExternalToMyItem(raw);
      if (!r.ok) return r;                    // 生产者给全 ⇒ 只转发，不重编话术
      items.push(r.data);
    }
    return { ok:true, data: items };
  } catch (e) {
    const message = e instanceof HttpError ? e.message : String(e);
    logger.warn('myBusiness', 'fetchMyList 失败', { message });                  // 给开发者
    reportDegrade({ layer:'myBusiness', key:'fetchMyList', e: e as Error,
                    toast:'列表加载失败，请重试' });                               // 给用户（两个读者分开）
    return { ok:false, code:'Timeout', message };
  }
}

/** 落盘 —— 本仓新业务**最常见的收尾动作**：写 + 自确认，绝不吞失败 */
export function saveMyItems(items: MyItem[]): boolean {
  const outcome = contentSet(MY_KEY, items);          // 返 PersistWriteOutcome（判别联合）
  return confirmPersist(outcome, {                    // 仅在 landed ∈ {local,kv} 时 true
    layer:'myBusiness', key: MY_KEY,
    toast:'保存失败（本地存储不可用）',
  });
}
```
> ⚠️ `MY_KEY` **必须先在 `base/core/contracts.ts` 的 `STORAGE_KEYS` 登记**（否则 `check:keys` 闸红；
> `contentSet` 内部 `checkRegistered` 也会拦）。返回 `boolean` 是给调用方的**真话**：落盘失败就是 `false`，
> 不许无条件 `return true`（那会让 UI 宣布"已保存"）。

### 4.6 UI（只投影）

```tsx
export function MyBusinessView() {
  const view = useMyBizView();          // core 已算好的视图指令（含 errorText）
  const onRefresh = async () => {
    const r = await load();
    // 动作失败 ⇒ 提示走 toastStore（UI 只发指令，不自己实现提示）
    if (!r.ok) toastError(`刷新失败：${r.message}`);
  };
  return (
    <div>
      {view.loading && <div>加载中…</div>}
      {/* 失败态优先于空态；**没有 `items.length === 0` 的推断** */}
      {view.errorText && <div className="error">{view.errorText}</div>}
      {view.warningText && <div className="warning">{view.warningText}</div>}
      <button disabled={view.isDisabled} onClick={() => void onRefresh()}>刷新</button>
      {view.items.map(i => <div key={i.id}>{i.title}:{i.count}</div>)}
    </div>
  );
}
```
❌ `const [local] = useState(view.items)`（副本）· ❌ `items.length === 0` 推断"没数据"（应看核心层给的态）。

**横切出口 —— UI 只发指令，不自己实现**（ADR-0036 六类意图）：

| 意图 | 出口 | 查法（`grep -rn "export function <符号>" src/components/base`）|
| --- | --- | --- |
| 弹提示 | `showToast` / `toastError` / `toastWarning` / `toastSuccess` | `showToast` |
| 记日志 | `logger` | `logger` |
| 广播 | `publish` | `publish` |
| 存数据 | `contentSet` / `contentSetAsync` | `contentSet` |
| 算 / 转换 | 本模块 `core.ts` 纯函数 | — |
| 下载 / 导出 | `downloadUrl` | `downloadUrl` |
| 落盘结果自确认 | `confirmPersist` | `confirmPersist` |

### 4.7 测试（`tests/unit/`，本仓约定：测试不进源码目录）

```ts
import { describe, it, expect } from 'vitest';
import { computeViewState } from '@/components/myBusiness/core.ts';
import { mapExternalToMyItem } from '@/components/myBusiness/adapter.ts';

describe('computeViewState', () => {
  it('空列表 → isDisabled=true', () => expect(computeViewState([], false).isDisabled).toBe(true));
  it('超 10 条 → 有 warningText', () => {
    const many = Array.from({ length: 11 }, (_, i) => ({ id: `id-${i}`, title: 't', count: i }));
    expect(computeViewState(many, false).warningText).not.toBeNull();
  });
});

describe('mapExternalToMyItem', () => {
  it('脏输入（null）→ ok:false 且带可展示 message', () => {
    const res = mapExternalToMyItem(null);
    expect(res.ok).toBe(false);
    if (!res.ok) expect(res.message).toBeTruthy();
  });
  it('必填字段缺失 → ok:false（不许静默补默认值）', () => expect(mapExternalToMyItem({ id: 'x' }).ok).toBe(false));
});
```
**上例非自证式的判据**：输入是现实给的（空/11 条/`null`），断言的是行为；改坏实现必红（阈值 `> 10` 改 `> 100` ⇒ 第 2 条红）。
❌ 自证式（禁止）：`expect(typeof fn).toBe('function')` · `expect(fetchMock).toHaveBeenCalled()` · 只断"没抛错"。
⚠️ 环境：`.test.tsx` 自动 jsdom；**`.ts` 若需 DOM 必须显式写 `// @vitest-environment jsdom`**（默认 node）。
⚠️ **桩必须跟契约走**：mock 模块时用 `importOriginal` **展开整个模块**再覆盖要控的几项；
**只 mock"当前用到的导出" ⇒ 本模块一加导出，别处的桩会整片崩**（本仓实证：`17-跨区-判据归位与CONTEXT定位-2026-09-18.md` §17.6，新增 1 个 hook 崩 3 个测试文件 27+19+13 例）。

---

### 4.8 若新业务 = **新画布节点**（本仓最常见形态，另有一套必备接线）

> 上面 4.1–4.7 是通用模块模板；**新节点还多出下列必做项**，漏一项就是"能跑但没接上"。

| 必做 | 查法（路径现场定位，勿抄快照） | 校验 |
| --- | --- | --- |
| 外壳（禁手写） | `NodeShell`（覆盖 **16/17** 节点）：`grep -rn "function NodeShell" src/components` | review |
| 默认结构（宽高/style/className） | `nodeDefaults`：`grep -rn "nodeDefaults" src/components/canvas` | — |
| 节点类型登记 | `contracts.NODE_TYPES` | `npm run check:node-types` |
| 端口契约 | `contracts.NODE_HANDLE_CONTRACT` | `npm run check:node-handles` |
| data 字段与默认值对齐 | 节点本地 `interface XxxData` ↔ `NODE_DATA_DEFAULTS`：`grep -rn "NODE_DATA_DEFAULTS" src/components/canvas` | `npm run check:node-data`（含 `--strict`） |
| 节点 id | `idGen.generateId`（**禁 `Date.now()`** 当唯一键） | — |
| data 写回 | `patchNodeDataById`：`grep -rn "export function patchNodeDataById" src/hooks` | — |
| 资产落盘 | `persistUrlToUploads`（唯一原语，见 §5-B） | — |

> ⚠️ **节点域的闸在 build 阶段，不在 push 阶段**
> （`scripts/gates.manifest.json` 的 `assignment.build`：api / node-types / node-handles / node-data / keys / upload-dirs —— 共 6 道，**实测**；原列的 `jsx` 已随 2026-09-15 删除）。
> ⇒ **只在 push 前跑 `check:arch` 那批会漏掉它们**；改完节点至少跑 `npm run build`，或单独跑那三个 `check:node-*`。
> 🔍 复核这几道闸的当前分配：`grep -n "assignment" scripts/gates.manifest.json`

### 4.9 渲染异常与全局兜底（**别自己造一套**）

| 场景 | 怎么办 | 查法 |
| --- | --- | --- |
| **新画布节点**渲染崩溃 | **不用自己包** —— `NodeShell` 已内置节点级边界（包 `ErrorBoundary variant="node"`） | `grep -n "ErrorBoundary" src/components/canvas/parts/NodeShell.tsx` |
| 其它 UI 子树要局部降级 | 显式包 `<ErrorBoundary variant="node">` | `grep -rn "class ErrorBoundary" src/components/base` |
| 根级全屏崩溃页 | 已由 `main.tsx` 包好（新业务**不要**再套一层） | `grep -n "ErrorBoundary" src/main.tsx` |
| 非渲染期异常（异步回调 / 定时器 / `unhandledrejection`） | **已有全局兜底**，不要自己挂 `window.onerror` | `grep -n "unhandledrejection" src/main.tsx` → `logger.error` → localTool `/api/logs`，5s 内同 `type+name+message` 合并 |

> ⚠️ **ErrorBoundary 只能抓渲染期与生命周期内的同步错误**；事件回调 / Promise 里的错误它**罩不到**
> ⇒ 那些错走 §1.2 的判别联合 + `logger` / `reportDegrade`，**不是**靠错误边界兜。
> ⚠️ director3d 域例外：自有 `director3d/ErrorBoundary.tsx`（用 `./log.ts`），属已登记的"director3d 不纳入收口"。

### 4.10 若新业务落在 **localTool（后端）**（两套世界，别照抄前端模板）

> 前提：**先确认它真的该在后端**。文件域已**整体收口到前端 `filesApi`**
> （`localToolApi` 回归纯 CRUD + kv + providers）⇒ **后端不要再造落盘/文件操作**，走 `POST /api/files/upload`。

| 项 | 约定 | 落点（实测） |
| --- | --- | --- |
| 分层 | `routes/`（HTTP 入口）· `utils/`（工具）· `ai-relay/`（供应商域）· `db/`（SQLite） | `localTool/src/` |
| 路由登记 | 路由表项 `{ method, pattern, handler }`（**不写在 npm script / husky 里**） | `localTool/src/router.ts`（查法：`grep -n "export const routes" localTool/src/router.ts`） |
| 返回契约 | `{ code, data }` 信封 | 与前端 `httpRequest` 剥离逻辑对齐 |
| 测试 | `node --test`（`*.test.js`，经 `tsx`） | `localTool/test/` |
| import 后缀 | **`.js`**（Node ESM，合法且必要） | — |
| 路径安全 | 后端以 `normalizeSubfolder` **顶层根白名单**强校验（前端 `isKnownUploadDir` 只是早失败，**不信任客户端**） | 后端 `normalizeSubfolder` |
| 重试纪律 | 大文件上传 `retries: 0`（**不自动重试，防重复上传**）；幂等靠 **sha1 contentId** 去重 | `filesApi.UPLOAD_OPTS` |

> ⚠️ 后端**不能**引用前端原语（如 `genErrors` / `tryParse`）—— 两套世界（§5-E）。
> 🔍 **怎么发现**：`grep -rn "from '.*src/" localTool/src` ⇒ 若命中前端路径即为越界引用。

---

## 5 · 本仓落地快查（**每条 2026-09-19 实测**）

### A. 侦察工具

| 想知道 | 用什么 |
| --- | --- |
| 谁引用它（fan-in） | `node scripts/mv-sync-refs.mjs refs <文件>` ⚠️ 曾因别名解析 bug 系统性少报（TD-17-19，已修）—— **报 0 先怀疑工具** |
| 既有债/裁定 | `node scripts/debt.mjs search <关键词>` |
| 判据归哪条 ADR | `node scripts/adr.mjs search <关键词>`（**搜不到 = 判据可能没归位**） |
| 现状链路/唯一入口 | `spec/DATAFLOW.md`（查法：`grep -n "<链路关键词>" spec/DATAFLOW.md`） |
| base 层红线 | `src/components/base/README.md` §一 |
| **原语定位**（符号 → 文件:行）| `grep -rn "export function <符号>" src/components/base`（见 §5-B）|
| 全库文本搜 | 内置 `Grep` ⚠️ **别用 shell `grep "a\|b"`** —— 本机 BSD grep 不支持 `\|`，**静默返回空**（看起来像"没有"） |
| 性能 / 节点设计规范 | `docs/CANVAS_PERFORMANCE.md` · `docs/NODE-DESIGN-SPEC.md`（**均在 `docs/` 根**，2026-09-19 从 `docs/plan/` 移出） |

### B. 核心原语 / 唯一入口（**先查这里再决定建不建**）

> ⚠️ **2026-09-24 回改（治法：去快照、留查法）**：本表原列 `文件:行号` 快照，实测**已腐 12 处**
> （6 处子目录化：`core/` 加 `log/` `event/`、`utils/` 加 `net/`；4 处跨域搬迁：`base/ui/`→`canvas/parts/` 等）。
> **行号逐次改动必腐，而"谁改了回来更新"（§0.6）已实证失败** ⇒ 现改为**查法**：路径不再进表，读者现场定位。
> **为什么查法不会腐**：`grep` 匹配的是**符号名**，符号名是真源的一部分；行号只是它的瞬时坐标。

**统一查法**（把 `<符号>` 换成你找的那个）：

```bash
# 找唯一入口的定义处（路径 + 行号一次拿全）
grep -rn "export function <符号>\|export async function <符号>\|export const <符号>" src/components/base
```

| 能力 | 唯一入口 | 查法（`<符号>` 替换上式） |
| --- | --- | --- |
| **状态落盘**（键值/状态持久化，域①） | `contentSet`（返 `PersistWriteOutcome`）/ `contentSetAsync` / `contentGet` / `contentDeleteAsync` | `contentSet` |
| 落盘结果自确认 | `confirmPersist(outcome,{layer,key,toast})` → **boolean**（仅 `landed ∈ {local,kv}` 为真） | `confirmPersist` |
| 降级留痕 + 提示 | `reportDegrade({layer,key,e,toast})` | `reportDegrade` |
| 网络请求（禁裸 fetch） | `httpRequest` | `httpRequest` |
| **文件落盘**（二进制/URL → uploads，域②）★**唯一原语** | `persistUrlToUploads(url,{folder,name,type,projectId})` —— 内部自分流 `data:`/`blob:`/`http(s)`/已是本机 `/files/`，**返判别联合、不归一 null**；上层用例一律走它，**禁再抄一份分流判据** | `persistUrlToUploads` |
| ↳ 底层：原始 File/Blob → multipart | `uploadFileToLocal`（返 `UploadOutcome`） | `uploadFileToLocal` |
| ↳ 底层：dataURL → multipart | `saveInlineToLocal`（返 `UploadOutcome`） | `saveInlineToLocal` |
| ↳ 节点资产 URL 落盘 | `resolveNodeAssetUrl` | `resolveNodeAssetUrl` |
| `/files/` → 完整 URL | `toAbsoluteFileUrl` | `toAbsoluteFileUrl` |
| 日志（禁 console） | `logger` | `logger` |
| **用户提示**（弹 toast） | `showToast` / `toastSuccess` / `toastError` / `toastWarning` | `showToast` |
| **广播**（跨模块事件） | `publish` / `subscribe` / `subscribeOnce` | `publish` |
| **下载 / 导出** | `downloadUrl` | `downloadUrl` |
| **中心登记**（键/事件/错误/节点类型） | `STORAGE_KEYS` / `EVENTS` / `GEN_ERRORS` / `NODE_TYPES` / `NODE_HANDLE_CONTRACT` | `STORAGE_KEYS` |
| 文案语言 | **直写中文**（本仓已移除 i18next） | 例外：`videoEditor` 域自有 `engine/lib/i18n` |
| **渲染崩溃边界** | `ErrorBoundary`（`variant="full"` 根级 / `"node"` 节点级；**节点级 `NodeShell` 已自动包**） | `class ErrorBoundary` |
| **遥测 / 埋点** | ⚠️ **本仓暂无遥测** —— `analytics` / `telemetry` / `sentry` / `trackEvent` **均 0 命中**；已登记 **TD-18-33** 待建（**已定：纯本机、不出网**） | ⇒ 当前可观测性走 `logger` → `POST /api/logs` + `GET /api/logs/stream`；**TD-18-33 落地前，不要自己引第三方 SDK** |
| 错误 → 类型（禁关键词判定） | `classifyError` | `classifyError` |
| 超时 | `withTimeout` / `isTimeoutError` / `TimeoutError` | `withTimeout` |
| 解析兜底 | `tryParse` | `tryParse` |
| 静默释放 | `releaseQuietly` / `releaseQuietlyAsync` | `releaseQuietly` |
| id | `generateId` / `generateUUID` | `generateId` |
| 深拷贝 | `deepClone` | `deepClone` |
| 文件名净化 | `safeFileName` | `safeFileName` |
| 上传子目录常量 | `UPLOAD_DIRS` | `UPLOAD_DIRS` |
| 节点 data 写回 | `patchNodeDataById` | `patchNodeDataById`（搜 `src/hooks`）|
| **AI 操作画布**唯一入口 | `agentCanvasHost` | `agentCanvasHost` |

> ✅ **作用域要写准**：`canvasHost` = **「AI 操作画布」的唯一入口**（不是"画布写操作唯一入口"）。
> 实测：它**只有 Agent 侧约 4 处消费**（`useCanvasAgentTools` / `canvasPlanExecutor` / `useAgentChat`）；
> 而 **43 个文件直写 `setNodes`**（其中 `nodes/` 15 个）—— 那些属**人工/UI 侧写入**，不归它管。
> ⇒ **AI 改画布走 `canvasHost`（禁裸 `useReactFlow`）**；写文档/注释时别把它的作用域夸成全域，否则就是假收口（TD-18-34）。

> ⚠️ **描述层也会漂移（2026-09-19 实测）**：
> ① `filesApi.ts` 头部仍写「失败一律返回 `null` 不抛」—— 但 `uploadFileToLocal` / `saveInlineToLocal`
> **已升级为判别联合 `UploadOutcome`**；② `uploadFileToLocal` 的 JSDoc 仍写 `Promise<string|null>`，
> 实际签名是 `Promise<UploadOutcome>`。
> ⇒ **判契约以函数签名为准，不以文件头 / JSDoc 为准**（这也正是本表改用查法的理由：**签名可查，描述会腐**）。

### C. 交付前必跑（**只跑 tsc 不算验完**）

```
npx vitest run · npm run test:localtool · npx tsc --noEmit
npm run check:arch / check:any / check:dead-code
npm run check:push（含元层闸 gate-vitals：申诉口三问 + 扫描基数自检）
```
⚠️ 全量 tsc（`npm run type-check`，含 `--noImplicitAny`）与全量 `vitest` **别并发**（实测内存争用被 SIGTERM，exit 137）。

### D. 探针（先红后绿 + 反向探针）—— 命令形态不猜不到

→ **命令形态 / 注入点唯一 / 还原 sha 自校验 / 退出码坑 / 反向探针 / 凭证归档三项见 `架构师改码7步法.md` Step 7.1**（查法：`grep -n "先红后绿\|反向探针" .codebuddy/commands/架构师改码7步法.md`；本文不再复制）。

### E. src 与 localTool 是两套世界

| | `src/`（前端） | `localTool/`（后端） |
| --- | --- | --- |
| 配置 | 根 `package.json`/`tsconfig` | **自带**（`"type":"module"`） |
| 测试 | `vitest`（`tests/unit`） | `node --test`（`localTool/test/*.test.js`） |
| import 后缀 | `.ts`/`.tsx` | **`.js`**（Node ESM，合法且必要） |
| 互引原语 | ❌ | ❌ **不能引前端原语**（实测 TD-18-28：想改调 `tryParse` 在此不可得） |

### F. 别去的地方

- `director3d/`：整体**排除单测覆盖率**、零组件挂载先例 ⇒ 别承诺行为断言（写明"测不了"+ 给静态凭证）。
- 组织约定（目录排布 / `.gitignore`）：用户已裁定**暂缓**（TD-23-2/3/4）。
- 未接线预留链路（如 `relay.streamChat`）：先确认是不是预留，别当现行 bug 修。

### G. 落账收尾（别只交代码）

```
node scripts/debt.mjs resolve <TD-ID> --status 已解决 --date YYYY-MM-DD --note "…"
node scripts/debt.mjs add --area NN --summary "≤120字，禁竖线" --anchor "<区域日志文件名>"
node scripts/arch-index.mjs --write · node scripts/debt.mjs archive
node scripts/adr.mjs add --title "…" --conclusion "…"    # 仅当确立/推翻判据
```
⚠️ 摘要 **>120 字**被拒 · `--anchor` **只准写文件名** · **改摘要没有命令**（用 `resolve --status 待还 --note` 补正，禁手改表格行）。

### H. 照抄范本（不要只看假名模板）

| 抄什么 | 范本 | 查法 |
| --- | --- | --- |
| store 真源 + 快照稳定 | `projectStore` | `grep -n "getSnapshot" src/components/base/store/projectStore.ts` |
| 判别联合范本 | `PersistWriteOutcome` | `grep -rn "export type PersistWriteOutcome" src/components/base` |
| 落盘自确认 + 返真实结果 | `scriptBoxPlaybookStore.persist` | `grep -rn "scriptBoxPlaybookStore" src/components/scriptbox` |
| 失败态与空态分离（UI 不推断） | `ResourceLibrary`（`phase` 态 + 首屏/翻页失败两个 state） | `grep -rn "class ResourceLibrary\|function ResourceLibrary" src/components` |
| 节点范式 | 任意 `src/components/<域>/nodes/*.tsx`（`NodeShell` 覆盖 **16/17**） | `ls src/components/*/nodes/*.tsx` |

---

## 6 · AI 审计 prompt（复制即用）

```
# 新建4步法 V2 审计
四零一毁：数据0副本｜异常0猜测｜UI0业务｜架构1键毁灭（只约束业务模块，不含横切地基）
逐条校验；命中问题标 BUG 并写明违反哪一条 + "怎么发现"；全通过输出 PASSED

1 数据0副本：唯一真源？组件是否 useState 复制业务数据？有无双向同步 useEffect？
2 异常0猜测：判别联合？字段是 code+message（禁 msg）？失败分支带可展示文案？
   throw 是否只用于"不该发生"？第三方 throw 是否在 ACL 转成判别联合？
3 UI0业务：UI 是否只投影？有无把"失败"渲染成"空态"？有无跨实体判断？
4 一键毁灭：业务模块删目录后 tsc 是否 0 错？（横切地基不适用本条）
5 幽灵预留/接线：每个新原语 grep 生产消费 > 0？写入口是否穷尽（别漏 Agent 路径）？具名类型有人 import 吗？
6 测试：断言改坏实现会红吗？（同义反复/断实现细节/断自己mock/零生产消费/断源码文本/只断没抛错 = 自证式）
7 建闸：过了建闸前置评审吗（默认不建）？能用类型/结构/对账测试守住的，是否误用了闸？
8 最优解/防重复：动手前写了最优解吗？横向盘查过别入口吗（别处已有更强实现而我重写 = BUG）？
9 裁决闭环：施工图还有「待/TODO/TBD」没结论吗？
10 失败判据交叉校验：ADR-0011(禁静默吞，禁以标记合法化) · ADR-0002(压平必须留痕) ·
   ADR-0003(logger之下失败由返回值表达) · ADR-0019(兜底六形态，验收看净减) · ADR-0036(分层不越层)
   ⚠️ 不要找「TS+React 异步 Catch 禁止项」执行 —— 其闸 check-silent-catch.mjs 已于 2026-09-17 裁定删除

11 渲染异常（§4.9）：节点是否走 NodeShell（自带 ErrorBoundary）？是否自己又造了一个错误边界？
    非渲染期异常是否错用错误边界兜（应走 logger / 判别联合）？
12 生命周期/并发/取消（Step 3.4）：订阅都有退订？高频落盘有节流？长任务有超时或取消？
    "不会有并发"有无论证（本仓无通用锁原语）？
13 禁自造基础设施：
    - 遥测：**没有**引第三方 SDK 吧？（本仓暂无遥测，已登记 TD-18-33，方向=纯本机）
    - 提示/广播/下载/落盘是否走了 §4.6 横切出口，而非自己实现一套？
    - 若 = 新节点（§4.8）：NODE_TYPES / NODE_HANDLE_CONTRACT / nodeDataSchema 是否登记？
      是否跑过 check:node-*（**build 阶段，不在 push**）？
```
