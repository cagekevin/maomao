// TODO(全局, 无需 import): children, position
import { Up, t, e, Nt } from './shared.js';
export default function Kp({
  children: e,
  position: t
}) {
  const Component2046 = `div`;
  return <Nt center={true} distanceFactor={Up} pointerEvents={`none`} position={t} sprite={true} transform={true} zIndexRange={[0, 1]}>
      <Component2046 className={`role-label`}>{e}</Component2046>
    </Nt>;
}