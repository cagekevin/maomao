// TODO(全局, 无需 import): children, position
import { um, t, e, Nt } from './shared.js';
export default function _Component75({
  children: e,
  position: t
}) {
  const Component2068 = `div`;
  return <Nt center={true} distanceFactor={um} pointerEvents={`none`} position={t} sprite={true} transform={true} zIndexRange={[0, 1]}>
      <Component2068 className={`role-label`}>{e}</Component2068>
    </Nt>;
}