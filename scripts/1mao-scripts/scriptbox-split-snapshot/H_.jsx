// TODO(全局, 无需 import): projectId, textApiUrl, textApiKey, imageApiUrl, imageApiKey, videoApiUrl, videoApiKey, builtinApiUrl, builtinApiKey, sd2VideoApiUrl, sd2VideoApiKey, discountVideoApiUrl, discountVideoApiKey, aiAppApiUrl, aiAppApiKey, audioApiUrl, audioApiKey, textModel, drawingModel, videoModel, sd2VideoModel, discountVideoModel, videoDurations, audioModel, showToast, transitResources, addTransitResource, presetPrompts, membership, type, expiry, globalTasks, updateGlobalTasks, onSendToActiveTab, customNodeTemplates, onAddCustomNodeTemplate, onDeleteCustomNodeTemplate, globalPollingInterval, globalMaxPollingDuration, globalSyncTimeout, setShowTaskList, cloudStorageConfig, sd2Token, useThumbnail, isLoggedIn, proxyMode, localToolBaseUrl, proxyBaseUrl, appId, licenseToken, enablePerformanceMode, onTogglePerformanceMode, agentCanvasRef, agentPanelOpen, agentPanelWidth, de, me, fe, localPort, open, width, ye, screenToFlowPosition, getNodes, getEdges, fitView, zoomIn, zoomOut, setViewport, getViewport, setCenter, ot, n, gt, x, et, nodeId, resultUrl, status, errorMsg, loading, i, r, data, o, s, l, style, height, position, ln, zt, padding, duration, maxZoom, minZoom, k, url, initialTool, imageUrlRef, imageUrlThumbRef, imageUrl, q, animated, assets, shots, promptLoading, nodes, edges, originalKey, thumbKey, imageUrlUploaded, success, skipped, conflictVersion, preservedDueToEmptyOverwrite, restoredFromHistory, kn, tabId, pn, vn, hn, kt, detail, b, source, sourceHandle, targetHandle, aspectRatio, size, selectedSeconds, zIndex, selected, tn, g, m, p, v, f, label, u, startX, startY, onCrop, onZoom, onEdit, zn, audioUrl, audioName, thumb, createdAt, images, activeIndex, expanded, onShowToast, ee, videos, text, inlineData, mimeType, selectedModel, fallbackModel, actualModel, hasNodeDrawingModel, method, headers, Authorization, body, model, messages, role, content, max_tokens, temperature, contents, parts, generationConfig, responseModalities, z, oe, ie, prompt, FormData, se, image_size, quality, progress, channelName, modelName, requestData, done, value, stream, SyntaxError, subfolder, preferThumbnail, thumbMaxDim, thumbQuality, thumbnailUrl, ce, length, Uint8Array, image, video, audio, Accept, asset_type, name, audios, apiUrl, apiKey, metadata, reference_images, reference_videos, reference_audios, generate_audio, ratio, watermark, responseData, taskId, videoUrl, imageAvailable, errorMessage, signal, wr, resolution, aspect_ratio, video_length, image_url, hasChanged, seconds, input_reference, input_video, ArrayBuffer, response_format, scriptProgressChars, index, description, shotType, lighting, dialogue, sound, motion, videoPrompt, category, story, projectName, globalStyle, imageSize, noProxy, maxDim, onGenerateScript, onGenerateAssetImage, onGenerateAllAssetImages, onRetryVideoAssetUpload, onUploadAllVideoAssets, onGenerateShotPrompts, onStopScriptItem, drawingModelForScript, video_url, useBuiltin, hasKey, isShareRuntime, title, onGenerateText, splitSourceId, splitNodes, clonedNodes, newEdges, resultData, customOutputType, customResultData, customRawResponse, nodeExecOrder, completedNodes, runningTasks, updatedAt, hasText, hasImage, hasVideo, hasAudio, hasResult, message, targetProjectId, results, errors, videoUploadedAssets, videoAssetUploadStatus, videoAssetUploadErrors, videoAssetUploadProgress, completed, total, uploadedAssets, scriptAssetUploadPending, durationFromScript, parentId, extent, onGenerate, onConnectShot, onConnectShots, onGenerateVideo, onGenerateSD2Video, onGenerateDiscountVideo, onGenerateCustom, onAIAssist, onSaveTemplate, config, onSplit, onSplitOne, onSplitManual, onSplitOneManual, onSpawnImageNode, onSpawnAudioNode, onAddImage, onPushImagesToImageBox, onStop, onRefresh, onCaptureToBox, onUploadAsset, membershipType, videoName, ve, listNodes, listEdges, addNode, updateNode, deleteNode, connectNodes, triggerGeneration, selectNode, selectNodes, clearSelection, deleteEdge, moveNode, duplicateNode, setNodeLocked, draggable, selectable, _locked, isNodeLocked, groupNodes, ungroupNodes, undo, redo, focusNode, zoom, qe, uploadAsset, icon, cat, badge, tone, mode, sourceOrder, audioFormat, trimStart, trimEnd, resizeWidth, resizeHeight, targetFps, fontSize, bgColor, textColor, key, originalIds, clientX, clientY, opacity, pointerEvents, connection, dropPosition, folder, coverUrl, graphData, nodeCount, isPublic, compound, rankdir, nodesep, ranksep, align, left, className, relatedToSelected, edgeFxLimit, hidden, lodLevel, yt, viewportMoving, handleFollowLimit, display, bi, ke, ki, on, cn, hideAttribution, tt, dn, un, step, webappName, webappDesc, openAppSelectorOnMount, dt, at, pt, yi, vt, configMode, variables, rt
import _cmp__Component131 from "./_Component131.jsx";
import _cmp_B_ from "./B_.jsx";
import _cmp__Component118 from "./_Component118.jsx";
import _cmp__Component127 from "./_Component127.jsx";
import _cmp_D_ from "./D_.jsx";
import _cmp__Component129 from "./_Component129.jsx";
import _cmp__Component130 from "./_Component130.jsx";
import _cmp_E_ from "./E_.jsx";
import _cmp_Tr from "./Tr.jsx";
import _cmp_Un from "./Un.jsx";
import _cmp_Er from "./Er.jsx";
import _cmp_Vn from "./Vn.jsx";
import _cmp_xi from "./xi.jsx";
import _cmp_Ti from "./Ti.jsx";
import _cmp_Pg from "./Pg.jsx";
import _cmp_Ai from "./Ai.jsx";
import _cmp_Oi from "./Oi.jsx";
import _cmp_Si from "./Si.jsx";
import _cmp_Di from "./Di.jsx";
import _cmp_Bn from "./Bn.jsx";
import { Gl, we, pe, he, Mn, Ce, bt, L_, sn, R_, be, ze, We, lt, _t, Ht, Vt, Ut, Yt, Le, Qt, Zt, K, An, Ne, U, De, nn, rn, Ue, Dn, wn, Cn, Ae, F, Tn, En, On, jn, It, Mt, G, Rt, Ze, Kr, Qe, id, X, bn, le, jr, _n, gn, fn, Pn, Pe, Ct, pr, hr, mr, Wn, P_, N_, D, Jn, O, J, ue, Xn, Ie, it, $t, H, sr, Y, or, ar, Rn, xr, ca, Qi, Ee, Hi, Ra, Ia, za, E, Bl, te, j, V, ae, I, I_, hi, vr, Jl, W, Cr, L, La, Zg, ka, xa, Aa, Mr, Yg, ci, mi, Pr, Qg, Fa, Nr, Ar, Fr, oi, ai, Ir, Lr, Rr, zr, F_, Vr, Br, Sr, jm, Wr, kr, Dr, Xr, si, Qr, zs, ei, ni, Te, ti, M_, A_, Bt, ii, di, Hr, Ur, li, ui, Gr, qr, Jr, Zr, lr, cr, ur, dr, fr, gr, _r, Yr, br, fi, pi, Ve, Hn, Ye, Je, Ge, Ke, Fe, gi, Ci, Nn, Ni, ji, Or, g_, Pi, Mi, Li, Nt, Fi, __, Ri, Ii, S_, $r, rr, ir, nt, l_, ri, Jt, Kn, Gn, Gi, Ui, Zi, $i, Yi, ea, Re, Xi, ta, Me, Yn, Vi, O_, k_, tr, Zn, Qn, $n, er, nr, Wt, Kt, xt, ge, Sn, Ki, _e, qn, $e, Ji, ft, vi, _i, Ln, In, Wi, qi, Ei, St, Xe, Fn, At, zi, Lt, Bi, Tt, _Component120, Be, _Component30, Et, _Component45, _Component2, _Component29, _Component26, _Component56, _Component113, Pt, _Component49, _Component121, _Component128, _Component122, _Component123, _Component20, _Component17, _Component124, He, _Component47, Xt, _Component125, Dt, _Component10, Oe, Gt, _Component18, _Component41, _Component48, Ft, Se, Ot, _Component0, _Component126, _Component7 } from "./shared.js";
import * as _shared from "./shared.js";
import * as Z from "react";
import * as Q from "react";
export default function H_({
  projectId: e = `default`,
  textApiUrl: t,
  textApiKey: r,
  imageApiUrl: i,
  imageApiKey: a,
  videoApiUrl: s,
  videoApiKey: c,
  builtinApiUrl: l,
  builtinApiKey: u,
  sd2VideoApiUrl: d,
  sd2VideoApiKey: f,
  discountVideoApiUrl: p,
  discountVideoApiKey: m,
  aiAppApiUrl: h,
  aiAppApiKey: _,
  audioApiUrl: y,
  audioApiKey: b,
  textModel: x = `gpt-3.5-turbo`,
  drawingModel: S = `gemini-3.1-flash-image-preview`,
  videoModel: C = `grok-video-3-pro`,
  sd2VideoModel: w = `seed-2`,
  discountVideoModel: T = `seedance_2_fast`,
  videoDurations: D = `10
15`,
  audioModel: O = `whisper-1`,
  showToast: k,
  transitResources: A = [],
  addTransitResource: j,
  presetPrompts: M = [],
  membership: N = {
    type: `FREE`,
    expiry: 0
  },
  globalTasks: F = [],
  updateGlobalTasks: I,
  onSendToActiveTab: ee,
  customNodeTemplates: L = [],
  onAddCustomNodeTemplate: te,
  onDeleteCustomNodeTemplate: ne,
  globalPollingInterval: B = 3,
  globalMaxPollingDuration: re = 600,
  globalSyncTimeout: ae = 600,
  setShowTaskList: H,
  cloudStorageConfig: U,
  sd2Token: W,
  useThumbnail: le = true,
  isLoggedIn: G = false,
  proxyMode: de = `local-tool`,
  localToolBaseUrl: fe,
  proxyBaseUrl: pe,
  appId: me,
  licenseToken: he,
  enablePerformanceMode: ge = true,
  onTogglePerformanceMode: _e,
  agentCanvasRef: ve,
  agentPanelOpen: ye = false,
  agentPanelWidth: be = 0
}) {
  let Ce = Gl();
  let we = !!(typeof window < `u`) && !!window.__CANVAS_RUNTIME__?.disableLocalTool;
  let Te = Z.useRef([]);
  let Ee = Z.useMemo(() => {
    if (de === `server-proxy` || we) {
      return {
        proxyMode: `server-proxy`,
        proxyBaseUrl: pe,
        appId: me,
        licenseToken: he
      };
    }
    let e = fe || Mn();
    return {
      proxyMode: `local-tool`,
      localPort: Ce.status.isConnected ? Ce.status.port : undefined,
      localToolBaseUrl: e
    };
  }, [de, pe, me, he, Ce.status.isConnected, Ce.status.port, fe, we]);
  let [De, K, ke] = bt(L_());
  let [Ae, q, Me] = sn(R_);
  let [Ne, Pe] = Z.useState(false);
  let [Fe, Ie] = Z.useState(null);
  let Le = Z.useRef(null);
  let Re = Z.useRef(null);
  let ze = Z.useRef({
    open: !!ye,
    width: be || 0
  });
  Z.useEffect(() => {
    ze.current = {
      open: !!ye,
      width: be || 0
    };
  }, [ye, be]);
  let {
    screenToFlowPosition: Ve,
    getNodes: J,
    getEdges: Y,
    fitView: Ue,
    zoomIn: Ge,
    zoomOut: Ke,
    setViewport: qe,
    getViewport: Je,
    setCenter: Ye
  } = We();
  let [Xe, Ze] = Z.useState(null);
  let [X, Qe] = Z.useState(null);
  let [$e, et] = Z.useState(false);
  let [tt, nt] = Z.useState(null);
  let [rt, it] = Z.useState(false);
  let [at, ot] = Z.useState(false);
  let lt = Z.useRef(null);
  let dt = Z.useCallback(() => {
    clearTimeout(lt.current);
    lt.current &&= null;
    ot(true);
  }, []);
  let ft = Z.useCallback(() => {
    if (lt.current) {
      clearTimeout(lt.current);
    }
    lt.current = setTimeout(() => {
      return ot(false);
    }, 220);
  }, []);
  let [pt, gt] = Z.useState([`imageBoxNode`, `gridSplitNode`, `panoramaNode`]);
  let _t = Z.useRef(false);
  Z.useEffect(() => {
    let e = false;
    (async () => {
      try {
        let t = await Ce.getKV(`canvas_pinned_tools`);
        let n = t == null ? null : typeof t == `string` ? JSON.parse(t) : t;
        if (!e && Array.isArray(n)) {
          gt(n.filter(e => {
            return typeof e == `string`;
          }));
        }
      } catch {} finally {
        _t.current = true;
      }
    })();
    return () => {
      e = true;
    };
  }, []);
  let vt = Z.useCallback(e => {
    gt(t => {
      let n = t.includes(e) ? t.filter(t => {
        return t !== e;
      }) : [...t, e];
      Ce.saveKV(`canvas_pinned_tools`, n).catch(() => {});
      return n;
    });
  }, [Ce]);
  let [yt, xt] = Z.useState(0);
  let [St, Ct] = Z.useState(true);
  let [Tt, kt] = Z.useState(null);
  let [At, Mt] = Z.useState(false);
  let [Nt, It] = Z.useState([]);
  let [Lt, Rt] = Z.useState(false);
  let zt = Z.useRef(new Map());
  let Bt = Z.useRef(new Map());
  let Vt = Z.useRef(null);
  let Ht = Z.useRef(false);
  let Ut = Z.useCallback(e => {
    if (Ht.current !== e) {
      Ht.current = e;
    }
  }, []);
  let Wt = Z.useCallback(() => {
    if (Vt.current !== null) {
      window.clearTimeout(Vt.current);
      Vt.current = null;
    }
    Ut(true);
  }, [Ut]);
  let Kt = Z.useCallback(() => {
    if (Vt.current !== null) {
      window.clearTimeout(Vt.current);
    }
    Vt.current = window.setTimeout(() => {
      Vt.current = null;
      Ut(false);
    }, 120);
  }, [Ut]);
  let Jt = Z.useRef({
    x: 0,
    y: 0
  });
  let Yt = Z.useRef(null);
  let Zt = Z.useRef(null);
  let Qt = Z.useCallback(() => {
    Yt.current = Le.current?.getBoundingClientRect() ?? null;
  }, []);
  Z.useEffect(() => {
    Qt();
    window.addEventListener(`resize`, Qt);
    return () => {
      return window.removeEventListener(`resize`, Qt);
    };
  }, [Qt]);
  Z.useEffect(() => {
    return () => {
      if (Zt.current !== null) {
        cancelAnimationFrame(Zt.current);
      }
      if (Vt.current !== null) {
        window.clearTimeout(Vt.current);
      }
    };
  }, []);
  let [$t, tn] = Z.useState(null);
  Z.useEffect(() => {
    let e = () => {
      return et(true);
    };
    window.addEventListener(`open-shortcuts-modal`, e);
    return () => {
      return window.removeEventListener(`open-shortcuts-modal`, e);
    };
  }, []);
  Z.useEffect(() => {
    let e = e => {
      let {
        nodeId: t,
        resultUrl: n,
        type: r,
        status: i,
        errorMsg: a
      } = e.detail;
      if (t) {
        K(e => {
          return e.map(e => {
            if (e.id === t) {
              let t = {
                ...e.data,
                loading: false
              };
              if (i === `failed`) {
                t.errorMsg = a || `生成失败`;
                t.errorMessage = a || `生成失败`;
              } else if (n) {
                t.progress = 100;
                t.errorMsg = undefined;
                t.errorMessage = undefined;
                if (r === `sd2Video` || r === `video` || r === `discountVideo`) {
                  t.videoUrl = An(n);
                } else if (r === `image`) {
                  t.imageUrl = An(n);
                } else if (r === `custom`) {
                  t.resultData = n;
                }
              }
              return {
                ...e,
                data: t
              };
            }
            return e;
          });
        });
      }
    };
    window.addEventListener(`mutiwindow-task-completed`, e);
    return () => {
      window.removeEventListener(`mutiwindow-task-completed`, e);
    };
  }, [K]);
  Z.useEffect(() => {
    if (Ne) {
      console.log(`Sync cloudStorageConfig to all imageToUrlNode instances`);
      K(e => {
        let t = false;
        let n = e.map(e => {
          if (e.type === `fileToUrlNode`) {
            let n = e.data?.cloudStorageConfig;
            if (JSON.stringify(n) !== JSON.stringify(U)) {
              t = true;
              return {
                ...e,
                data: {
                  ...e.data,
                  cloudStorageConfig: U
                }
              };
            }
          }
          return e;
        });
        if (t) {
          return n;
        } else {
          return e;
        }
      });
    }
  }, [U, Ne, K]);
  let nn = Z.useRef(false);
  let rn = Z.useCallback(() => {
    let e = De.filter(e => {
      return e.type === `group`;
    });
    if (e.length === 0) {
      return false;
    }
    let t = false;
    let n = [...De];
    for (let r of e) {
      if (r.data?.collapsed) {
        continue;
      }
      let e = n.filter(e => {
        return e.parentId === r.id;
      });
      if (e.length === 0) {
        continue;
      }
      let i = Infinity;
      let a = Infinity;
      let o = -Infinity;
      let s = -Infinity;
      e.forEach(e => {
        let t = e.position.x;
        let n = e.position.y;
        let r = e.measured?.width || e.style?.width || 300;
        let c = e.measured?.height || e.style?.height || 200;
        if (t < i) {
          i = t;
        }
        if (n < a) {
          a = n;
        }
        if (t + r > o) {
          o = t + r;
        }
        if (n + c > s) {
          s = n + c;
        }
      });
      i -= 40;
      a -= 40;
      o += 40;
      s += 40;
      let c = o - i;
      let l = s - a;
      if (e.some(e => {
        return e.dragging;
      })) {
        let e = Math.max(c, r.style?.width || 0, o);
        let i = Math.max(l, r.style?.height || 0, s);
        if (e !== r.style?.width || i !== r.style?.height) {
          t = true;
          let a = n.findIndex(e => {
            return e.id === r.id;
          });
          n[a] = {
            ...r,
            style: {
              ...r.style,
              width: e,
              height: i
            }
          };
        }
      } else if (Math.abs(i) > 1 || Math.abs(a) > 1 || Math.abs(r.style?.width - c) > 1 || Math.abs(r.style?.height - l) > 1) {
        t = true;
        let e = n.findIndex(e => {
          return e.id === r.id;
        });
        n[e] = {
          ...r,
          position: {
            x: r.position.x + i,
            y: r.position.y + a
          },
          style: {
            ...r.style,
            width: c,
            height: l
          }
        };
        if (Math.abs(i) > 1 || Math.abs(a) > 1) {
          for (let e = 0; e < n.length; e++) {
            if (n[e].parentId === r.id) {
              n[e] = {
                ...n[e],
                position: {
                  x: n[e].position.x - i,
                  y: n[e].position.y - a
                }
              };
            }
          }
        }
      }
    }
    if (t) {
      setTimeout(() => {
        return K(n);
      }, 0);
      return true;
    } else {
      return false;
    }
  }, [De, K]);
  let on = Z.useCallback(() => {
    nn.current = true;
    rn();
  }, [rn]);
  let cn = Z.useCallback(() => {
    nn.current = true;
    rn();
  }, [rn]);
  let ln = Z.useMemo(() => {
    return De.filter(e => {
      return e.type === `group` || !!e.parentId;
    }).map(e => {
      return `${e.id}:${e.parentId || ``}:${e.position.x}:${e.position.y}:${e.measured?.width || 0}:${e.measured?.height || 0}:${+!!e.dragging}`;
    }).join(`,`);
  }, [De]);
  Z.useEffect(() => {
    if (!Ht.current) {
      if (ln) {
        rn();
      }
    }
  }, [ln, rn]);
  Z.useEffect(() => {
    return () => {
      zt.current.forEach(e => {
        return e.abort();
      });
      zt.current.clear();
    };
  }, [e]);
  Z.useEffect(() => {
    if (Ne && De.length > 0) {
      setTimeout(() => {
        Ue({
          padding: 0.2,
          duration: 800,
          maxZoom: 1,
          minZoom: 0.05
        });
      }, 100);
    }
  }, [Ne, Ue]);
  let [un, dn] = Z.useState(true);
  let [fn, pn] = Z.useState([]);
  let [hn, gn] = Z.useState(-1);
  let _n = Z.useRef(false);
  let vn = Z.useRef(-1);
  let bn = Z.useRef(new Map());
  let [Sn, Cn] = Z.useState(false);
  let wn = Z.useRef(`tab-${Date.now()}-${Math.random()}`);
  Z.useEffect(() => {
    let e = new BroadcastChannel(`yimao_canvas_sync`);
    e.onmessage = e => {
      if (e.data && e.data.type === `CANVAS_SAVED` && e.data.projectId === Dn.current && e.data.tabId !== wn.current) {
        Cn(true);
      }
    };
    return () => {
      return e.close();
    };
  }, []);
  let Tn = Z.useRef(De);
  let En = Z.useRef(Ae);
  let Dn = Z.useRef(e);
  let On = Z.useRef(Ne);
  let kn = Z.useRef(0);
  let jn = Z.useRef(F);
  let Nn = Z.useRef(() => {});
  let Pn = Z.useRef(null);
  Z.useEffect(() => {
    Tn.current = De;
  }, [De]);
  Z.useEffect(() => {
    En.current = Ae;
  }, [Ae]);
  Z.useEffect(() => {
    Dn.current = e;
  }, [e]);
  Z.useEffect(() => {
    On.current = Ne;
  }, [Ne]);
  Z.useEffect(() => {
    jn.current = F;
  }, [F]);
  let In = Z.useCallback(() => {
    if (!G) {
      k(`请先登录后再创建模板`);
      return;
    }
    let e = Tn.current.filter(e => {
      return e.selected && e.type !== `group`;
    });
    if (e.length === 0) {
      k(`请先框选要保存的节点`);
      return;
    }
    It(e);
    Mt(true);
  }, [k, G]);
  let Ln = Z.useCallback(() => {
    if (!G) {
      k(`请先登录后再使用模板库`);
      return;
    }
    Rt(true);
  }, [k, G]);
  let Rn = Z.useCallback(async (e, t, n) => {
    if (!t) {
      Ze(n);
      return;
    }
    Ze((await Kr.getConfig(t)) || n);
  }, []);
  let zn = Z.useCallback(async (e, t, n, r) => {
    if (!t) {
      Qe({
        id: e,
        url: n,
        initialTool: r
      });
      return;
    }
    Qe({
      id: e,
      url: (await Kr.getConfig(t)) || n,
      initialTool: r
    });
  }, []);
  let Bn = Z.useCallback(e => {
    if (X) {
      K(t => {
        return t.map(t => {
          if (t.id === X.id) {
            let {
              imageUrlRef: n,
              imageUrlThumbRef: r,
              ...i
            } = t.data;
            return {
              ...t,
              data: {
                ...i,
                imageUrl: e
              }
            };
          }
          return t;
        });
      });
      bn.current.delete(X.id);
    }
    Qe(null);
  }, [X, K]);
  let Vn = Z.useCallback(e => {
    let t = Array.from(zt.current.keys()).filter(t => {
      return t === e || t.startsWith(`${e}_`);
    });
    if (t.length > 0) {
      t.forEach(e => {
        let t = zt.current.get(e);
        if (t) {
          t.abort();
          zt.current.delete(e);
        }
      });
      k(`已取消生成`);
      K(t => {
        return t.map(t => {
          if (t.id === e) {
            return {
              ...t,
              data: {
                ...t.data,
                loading: false,
                errorMsg: `已取消`
              }
            };
          } else {
            return t;
          }
        });
      });
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    }
  }, [K, q, k]);
  let Un = Z.useCallback((e, t, n) => {
    let r = `${e}_${t}_${n}`;
    let i = zt.current.get(r);
    if (i) {
      i.abort();
      zt.current.delete(r);
    }
    K(r => {
      return r.map(r => {
        if (r.id === e) {
          if (t === `asset`) {
            return {
              ...r,
              data: {
                ...r.data,
                assets: (r.data.assets || []).map(e => {
                  if (e.id === n) {
                    return {
                      ...e,
                      loading: false
                    };
                  } else {
                    return e;
                  }
                })
              }
            };
          } else {
            return {
              ...r,
              data: {
                ...r.data,
                shots: (r.data.shots || []).map(e => {
                  if (e.id === n) {
                    return {
                      ...e,
                      promptLoading: false
                    };
                  } else {
                    return e;
                  }
                })
              }
            };
          }
        } else {
          return r;
        }
      });
    });
  }, [K]);
  let Wn = Z.useCallback(async () => {
    if (!On.current) {
      return;
    }
    let e = Tn.current.some(e => {
      return e.id !== `ghost-target`;
    });
    let t = En.current.some(e => {
      return e.id !== `ghost-edge`;
    });
    if (!e && !t) {
      console.log(`[Canvas] 画布为空，跳过保存以保护已有数据`);
      return;
    }
    let n = true;
    let r = {
      nodes: Tn.current.filter(e => {
        return e.id !== `ghost-target`;
      }).map(e => {
        let t = {
          ...e.data
        };
        Object.keys(t).forEach(e => {
          if (typeof t[e] == `function`) {
            delete t[e];
          }
        });
        delete t.loading;
        delete t.progress;
        return {
          ...e,
          data: t
        };
      }),
      edges: En.current.filter(e => {
        return e.id !== `ghost-edge`;
      })
    };
    try {
      let e = await Promise.all(r.nodes.map(async e => {
        if (e.data.hasChanged) {
          n = true;
          e.data.hasChanged = false;
        }
        let t = {
          ...e
        };
        let r = t.data.imageUrl;
        if (r && r.startsWith(`data:image`)) {
          n = true;
          let e = bn.current.get(t.id);
          let i = t.data.imageUrlRef;
          if (!e && !i) {
            let e = `img_orig_${t.id}_${Date.now()}`;
            console.log(`[Canvas] 存储原图 key=${e}, 大小=${r.length}bytes, useThumbnail=${le}`);
            await Kr.setObject(e, r);
            bn.current.set(t.id, {
              originalKey: e,
              thumbKey: ``
            });
            if (le) {
              console.log(`[Canvas] 开始生成缩略图 nodeId=${t.id}, 原图大小=${r.length}bytes`);
              let n = await _cmp_Tr(r, 200, 0.6);
              console.log(`[Canvas] 缩略图生成完成 nodeId=${t.id}, 缩略图大小=${n.length}bytes, 压缩率=${(n.length / r.length * 100).toFixed(1)}%`);
              let i = `img_thumb_${t.id}_${Date.now()}`;
              console.log(`[Canvas] 存储缩略图 key=${i}, 大小=${n.length}bytes`);
              await Kr.setObject(i, n);
              bn.current.set(t.id, {
                originalKey: e,
                thumbKey: i
              });
              let a = Tn.current.find(e => {
                return e.id === t.id;
              });
              if (a) {
                a.data = {
                  ...a.data,
                  imageUrlUploaded: true,
                  imageUrlRef: e,
                  imageUrlThumbRef: i,
                  imageUrl: n
                };
              }
              t.data.imageUrlUploaded = true;
              t.data.imageUrlRef = e;
              t.data.imageUrlThumbRef = i;
              t.data.imageUrl = n;
            } else {
              let n = Tn.current.find(e => {
                return e.id === t.id;
              });
              if (n) {
                n.data = {
                  ...n.data,
                  imageUrlUploaded: true,
                  imageUrlRef: e,
                  imageUrl: r
                };
              }
              t.data.imageUrlUploaded = true;
              t.data.imageUrlRef = e;
              t.data.imageUrl = r;
            }
          } else {
            let n = e || {
              originalKey: t.data.imageUrlRef,
              thumbKey: t.data.imageUrlThumbRef
            };
            if (!e) {
              bn.current.set(t.id, n);
            }
            t.data.imageUrlUploaded = true;
            t.data.imageUrlRef = n.originalKey;
            if (le && n.thumbKey) {
              t.data.imageUrlThumbRef = n.thumbKey;
              delete t.data.imageUrl;
            } else if (le) {
              delete t.data.imageUrl;
            } else {
              delete t.data.imageUrlThumbRef;
              let e = await Kr.getConfig(n.originalKey);
              if (e) {
                t.data.imageUrl = e;
              }
            }
          }
        } else if (t.data.imageUrlUploaded && le && !t.data.imageUrlThumbRef) {
          delete t.data.imageUrl;
          n = true;
        }
        return t;
      }));
      if (!n) {
        console.log(`No changes detected, skipping save.`);
        return;
      }
      console.log(`Saving canvas state...`);
      r.nodes = e;
      let t = Date.now();
      let {
        success: i,
        skipped: a,
        conflictVersion: o,
        preservedDueToEmptyOverwrite: s,
        restoredFromHistory: c
      } = await Kr.saveCanvasStateWithVersion(Dn.current, r, t);
      if (a && o) {
        console.warn(`[Canvas] 检测到版本冲突！远程版本 ${o}，本地版本 ${t}`);
        k?.(`检测到其他页面已更新画布，最新数据已加载，请重新操作`);
        let e = jr(Dn.current);
        let n = await Kr.getObject(e);
        if (n && n.nodes) {
          let {
            nodes: e,
            edges: t
          } = n;
          Tn.current = e;
          K([...e]);
          if (t) {
            En.current = t;
            q([...t]);
          }
        }
      } else if (a && s) {
        console.warn(`[Canvas] 迁移保护：检测到历史数据，已自动恢复并阻止空画布覆盖`);
        k?.(c ? `检测到历史数据不一致，已自动恢复最新画布数据` : `空画布覆盖风险，已保留当前历史数据`);
        let e = jr(Dn.current);
        let t = await Kr.getObject(e);
        if (t && t.nodes && t.nodes.length > 0) {
          let {
            nodes: e,
            edges: n
          } = t;
          Tn.current = e;
          K([...e]);
          if (n) {
            En.current = n;
            q([...n]);
          }
        }
      } else if (!a) {
        if (i) {
          kn.current = t;
          console.log(`[Canvas] 画布保存成功，版本号: ${t}`);
        } else {
          console.error(`保存画布状态失败`);
        }
      }
    } catch (e) {
      console.error(`Save failed`, e);
    }
    try {
      let e = new BroadcastChannel(`yimao_canvas_sync`);
      e.postMessage({
        type: `CANVAS_SAVED`,
        projectId: Dn.current,
        tabId: wn.current
      });
      e.close();
    } catch (e) {
      console.warn(`Failed to broadcast canvas sync`, e);
    }
    if (!_n.current) {
      pn(e => {
        let t = e.slice(0, vn.current + 1);
        t.push({
          nodes: Tn.current,
          edges: En.current
        });
        if (t.length > 15) {
          t.shift();
        }
        return t;
      });
      gn(e => {
        let t = Math.min(e + 1, 14);
        vn.current = t;
        return t;
      });
    }
  }, [Ce.status.isConnected, Ce.saveKV, K, k]);
  let Gn = Z.useCallback(() => {
    if (hn > 0) {
      _n.current = true;
      let e = fn[hn - 1];
      K(e.nodes);
      q(e.edges);
      gn(hn - 1);
      vn.current = hn - 1;
      setTimeout(() => {
        _n.current = false;
      }, 600);
    }
  }, [fn, hn, K, q]);
  let Kn = Z.useCallback(() => {
    if (hn < fn.length - 1) {
      _n.current = true;
      let e = fn[hn + 1];
      K(e.nodes);
      q(e.edges);
      gn(hn + 1);
      vn.current = hn + 1;
      setTimeout(() => {
        _n.current = false;
      }, 600);
    }
  }, [fn, hn, K, q]);
  Z.useEffect(() => {
    if (Pn.current !== e && (!!Ce.status.isConnected || !!we)) {
      Pn.current = e;
      Pe(false);
      Ct(true);
      (async () => {
        let t = jr(e);
        try {
          let n = await Kr.getObject(t);
          if (n && n.nodes && n.nodes.length > 0) {
            let {
              nodes: t,
              edges: r
            } = n;
            let i = await Promise.all(t.map(async e => {
              let t = {
                ...e
              };
              t.data.loading &&= false;
              delete t.data.progress;
              let n = t.data;
              let r = n.imageUrl;
              let i = n.thumbnailUrl;
              let a = e => {
                return !!e && /^https?:\/\//i.test(e);
              };
              if (t.data.imageUrlRef) {
                let e = t.data.imageUrlRef;
                if (!a(r) && !a(i)) {
                  if (le) {
                    if (t.data.imageUrlThumbRef) {
                      let n = t.data.imageUrlThumbRef;
                      let r = await Kr.getConfig(n);
                      if (r) {
                        t.data.imageUrl = r;
                      } else {
                        console.warn(`[Canvas] 缩略图恢复失败 nodeId=${t.id}, thumbKey=${n} 未找到，尝试加载原图`);
                        let r = await Kr.getConfig(e);
                        if (r) {
                          t.data.imageUrl = r;
                        }
                      }
                    } else {
                      let n = await Kr.getConfig(e);
                      if (n) {
                        try {
                          let e = await _cmp_Tr(n, 200, 0.6);
                          let r = `img_thumb_${t.id}_${Date.now()}`;
                          await Kr.setObject(r, e);
                          t.data.imageUrlThumbRef = r;
                          t.data.imageUrl = e;
                        } catch (e) {
                          console.warn(`[Canvas] 动态生成缩略图失败 nodeId=${t.id}, 使用原图:`, e);
                          t.data.imageUrl = n;
                        }
                      } else {
                        console.warn(`[Canvas] 原图恢复失败 nodeId=${t.id}, originalKey=${e} 未找到`);
                      }
                    }
                  } else {
                    let n = await Kr.getConfig(e);
                    if (n) {
                      t.data.imageUrl = n;
                    } else {
                      console.warn(`[Canvas] 原图恢复失败 nodeId=${t.id}, originalKey=${e} 未找到`);
                    }
                  }
                }
              }
              return t;
            }));
            let a = await Kr.getCanvasVersion(e);
            kn.current = a;
            console.log(`[Canvas] 加载画布数据，当前版本号: ${a}`);
            let o = i.filter(e => {
              return e.type !== `director3DNode`;
            });
            let s = new Set(o.map(e => {
              return e.id;
            }));
            let c = (r || []).filter(e => {
              return s.has(e.source) && s.has(e.target);
            });
            Tn.current = o;
            K([...o]);
            En.current = c;
            q([...c]);
          } else {
            Tn.current = [];
            K([]);
            En.current = [];
            q([]);
          }
        } catch (e) {
          console.error(`Failed to load canvas state`, e);
          Tn.current = [];
          K([]);
          En.current = [];
          q([]);
        } finally {
          setTimeout(() => {
            Pe(true);
            Ct(false);
            setTimeout(() => {
              return Ue({
                duration: 800,
                maxZoom: 1,
                padding: 0.2
              });
            }, 100);
            if (Ce.status.isConnected) {
              (async () => {
                try {
                  let t = await pr(e);
                  if (t && t.status === `interrupted` && t.nodeExecOrder.length > 0) {
                    let n = t.completedNodes.length;
                    let r = t.nodeExecOrder.length;
                    console.log(`[InfiniteCanvas] 检测到中断的工作流: ${n}/${r} 节点已完成`);
                    if (t.nodeExecOrder.find(e => {
                      return !t.completedNodes.includes(e);
                    })) {
                      kt(t);
                    } else {
                      console.log(`[InfiniteCanvas] 工作流已完成，清理检查点`);
                      await hr(e);
                    }
                  } else if (t && t.status === `running` && t.nodeExecOrder.length > 0) {
                    if (t.completedNodes.length >= t.nodeExecOrder.length) {
                      console.log(`[InfiniteCanvas] 工作流已完成(running状态)，清理检查点`);
                      await hr(e);
                    } else {
                      t.status = `interrupted`;
                      await mr(t);
                      kt(t);
                    }
                  }
                } catch (e) {
                  console.error(`[InfiniteCanvas] 检查点恢复检测失败:`, e);
                }
              })();
            }
          }, 300);
        }
      })();
    }
  }, [e, K, q, Ue, Ce.status.isConnected]);
  Z.useEffect(() => {
    if (!Ne || we || Ht.current) {
      return;
    }
    let e = setTimeout(() => {
      if (!Ht.current) {
        Wn();
      }
    }, 1800);
    return () => {
      return clearTimeout(e);
    };
  }, [De, Ae, Wn, Ne, we]);
  Z.useEffect(() => {
    let e = e => {
      let t = e.detail || {};
      if (!t.projectId || t.projectId === Dn.current) {
        Promise.resolve(Wn()).finally(() => {
          window.dispatchEvent(new CustomEvent(P_, {
            detail: {
              projectId: Dn.current
            }
          }));
        });
      }
    };
    window.addEventListener(N_, e);
    return () => {
      return window.removeEventListener(N_, e);
    };
  }, [Wn]);
  let Jn = Z.useRef({});
  Z.useEffect(() => {
    K(e => {
      let t = false;
      let n = e.map(e => {
        let n = false;
        let r = {
          ...e.data
        };
        if (e.type === `videoNode`) {
          if (!r.videoModel && C) {
            r.videoModel = C;
            n = true;
          }
          if (!r.videoDurations && D) {
            r.videoDurations = D;
            n = true;
          }
        }
        if (e.type === `sd2VideoNode`) {
          if (!r.sd2VideoModel && w) {
            r.sd2VideoModel = w;
            n = true;
          }
          if (!r.videoDurations && D) {
            r.videoDurations = D;
            n = true;
          }
        }
        if (e.type === `promptNode` && !r.drawingModel && S) {
          r.drawingModel = S;
          n = true;
        }
        if (e.type === `textNode` && !r.textModel && x) {
          r.textModel = x;
          n = true;
        }
        if (e.type === `scriptBoxNode`) {
          if (r.textModel !== x) {
            r.textModel = x;
            n = true;
          }
          if (r.drawingModelForScript !== S) {
            r.drawingModelForScript = S;
            n = true;
          }
          if (r.onGenerateScript !== Jn.current.onGenerateScript) {
            r.onGenerateScript = Jn.current.onGenerateScript;
            n = true;
          }
          if (r.onGenerateAssetImage !== Jn.current.onGenerateAssetImage) {
            r.onGenerateAssetImage = Jn.current.onGenerateAssetImage;
            n = true;
          }
          if (r.onGenerateAllAssetImages !== Jn.current.onGenerateAllAssetImages) {
            r.onGenerateAllAssetImages = Jn.current.onGenerateAllAssetImages;
            n = true;
          }
          if (r.onRetryVideoAssetUpload !== Jn.current.onRetryVideoAssetUpload) {
            r.onRetryVideoAssetUpload = Jn.current.onRetryVideoAssetUpload;
            n = true;
          }
          if (r.onUploadAllVideoAssets !== Jn.current.onUploadAllVideoAssets) {
            r.onUploadAllVideoAssets = Jn.current.onUploadAllVideoAssets;
            n = true;
          }
          if (r.onGenerateShotPrompts !== Jn.current.onGenerateShotPrompts) {
            r.onGenerateShotPrompts = Jn.current.onGenerateShotPrompts;
            n = true;
          }
          if (r.onStopScriptItem !== Jn.current.onStopScriptItem) {
            r.onStopScriptItem = Jn.current.onStopScriptItem;
            n = true;
          }
          if (r.transitResources !== A) {
            r.transitResources = A;
            n = true;
          }
        }
        if (e.type === `audioNode`) {
          if (r.audioModel !== O) {
            r.audioModel = O;
            n = true;
          }
          if (r.audioApiUrl !== y) {
            r.audioApiUrl = y;
            n = true;
          }
          if (r.audioApiKey !== b) {
            r.audioApiKey = b;
            n = true;
          }
        }
        if (e.type === `fileToUrlNode` && JSON.stringify(r.cloudStorageConfig) !== JSON.stringify(U)) {
          r.cloudStorageConfig = U;
          n = true;
        }
        if (n) {
          t = true;
          return {
            ...e,
            data: r
          };
        } else {
          return e;
        }
      });
      if (t) {
        return n;
      } else {
        return e;
      }
    });
  }, [C, w, D, S, x, O, y, b, U, A, K]);
  let Yn = Z.useCallback(e => {
    let t = J();
    let n = t.find(t => {
      return t.id === e.source;
    });
    let r = t.find(t => {
      return t.id === e.target;
    });
    let i = (e, t) => {
      let n = e?.internals;
      if (!n?.handleBounds) {
        return null;
      }
      let r = [...(n.handleBounds.source || []), ...(n.handleBounds.target || [])];
      return (t ? r.find(e => {
        return e.id === t;
      }) : r[0])?.position ?? null;
    };
    let a = i(n, e.sourceHandle);
    let o = i(r, e.targetHandle);
    if (a && o && a === o) {
      k(`请连接节点的不同侧 (右侧输出 → 左侧输入)`);
      return;
    }
    if (a === `left` && o === `right`) {
      e = {
        ...e,
        source: e.target,
        target: e.source,
        sourceHandle: e.targetHandle ?? null,
        targetHandle: e.sourceHandle ?? null
      };
    }
    let s = t.find(t => {
      return t.id === e.source;
    });
    ((e, t, n) => {
      if (e?.type !== `scriptBoxNode` || !n?.startsWith(`shot-`) || t?.type !== `promptNode` && t?.type !== `discountVideoNode`) {
        return;
      }
      let r = n.slice(5);
      let i = (e.data.shots || []).find(e => {
        return e.id === r;
      });
      let a = String(e.data.aspectRatio || `16:9`);
      let o = a === `custom` ? String(e.data.customAspectRatio || `16:9`) : a;
      let s = t.type === `promptNode` ? {
        aspectRatio: o === `4:4` ? `1:1` : o
      } : {
        size: o === `4:4` ? `1:1` : o,
        selectedSeconds: String(Math.max(1, Number.parseInt(i?.duration || `5`, 10) || 5))
      };
      K(e => {
        return e.map(e => {
          if (e.id === t.id) {
            return {
              ...e,
              data: {
                ...e.data,
                ...s
              }
            };
          } else {
            return e;
          }
        });
      });
    })(s, t.find(t => {
      return t.id === e.target;
    }), e.sourceHandle);
    if (s?.selected) {
      let n = t.filter(e => {
        return e.selected;
      });
      if (n.length > 1) {
        q(t => {
          let r = [...t];
          n.forEach(t => {
            if (t.id !== e.target) {
              r = ue({
                ...e,
                source: t.id,
                type: `default`
              }, r);
            }
          });
          return r;
        });
        return;
      }
    }
    let c = {
      ...e,
      type: `default`
    };
    q(e => {
      return ue(c, e);
    });
  }, [q, K, J, k]);
  let Xn = e => {
    let t = e.target;
    if (!t) {
      return false;
    }
    let n = t.tagName;
    return n === `INPUT` || n === `TEXTAREA` || !!t.isContentEditable || !!t.closest && !!t.closest(`input, textarea, [contenteditable="true"]`);
  };
  let Zn = Z.useCallback(e => {
    if (Xn(e)) {
      return;
    }
    e.preventDefault();
    let t = Le.current?.getBoundingClientRect();
    if (t) {
      let n = e.clientX || e.clientX;
      let r = e.clientY || e.clientY;
      Ie({
        x: n - t.left,
        y: r - t.top,
        type: `canvas`
      });
      it(false);
    }
  }, [Ie]);
  let Qn = Z.useCallback((e, t) => {
    if (Xn(e)) {
      return;
    }
    e.preventDefault();
    let n = Le.current?.getBoundingClientRect();
    if (n) {
      Ie({
        x: e.clientX - n.left,
        y: e.clientY - n.top,
        type: `node`,
        nodeId: t.id
      });
      it(false);
    }
  }, [Ie]);
  let $n = Z.useCallback((e, t) => {
    if (Xn(e)) {
      return;
    }
    e.preventDefault();
    let n = Le.current?.getBoundingClientRect();
    if (n) {
      Ie({
        x: e.clientX - n.left,
        y: e.clientY - n.top,
        type: `selection`
      });
      it(false);
    }
  }, [Ie]);
  let er = Z.useCallback(e => {
    setTimeout(() => {
      if (Tn.current.filter(e => {
        return e.selected;
      }).length > 1) {
        let t = Le.current?.getBoundingClientRect();
        if (t) {
          Ie({
            x: e.clientX - t.left,
            y: e.clientY - t.top,
            type: `selection`
          });
          it(false);
        }
      }
    }, 50);
  }, [Ie]);
  let tr = Z.useCallback((e, t) => {
    K(e => {
      let n = Math.max(0, ...e.map(e => {
        return Number(e.zIndex) || 0;
      }));
      return e.map(e => {
        if (e.id === t.id) {
          return {
            ...e,
            zIndex: n + 1,
            selected: true
          };
        } else {
          return e;
        }
      });
    });
    if ($t && $t.length > 0) {
      e.preventDefault();
      q(e => {
        let n = [...e];
        $t.forEach(e => {
          if (e !== t.id) {
            n = ue({
              source: e,
              target: t.id,
              type: `default`,
              id: `e-${e}-${t.id}-${Date.now()}`
            }, n);
          }
        });
        return n;
      });
      tn(null);
      k(`连接成功`);
    }
  }, [$t, q, K, k]);
  let nr = Z.useCallback(() => {
    Ie(null);
    it(false);
    if ($t) {
      tn(null);
      k(`已取消多项连接`);
    }
    H?.(false);
  }, [Ie, $t, k, H]);
  let rr = Z.useCallback((e, t) => {}, []);
  let ir = Z.useCallback(e => {}, []);
  let ar = Z.useCallback(async (e, t, n) => {
    Qe(n ? {
      id: e,
      url: (await Kr.getConfig(n)) || t,
      initialTool: `crop`
    } : {
      id: e,
      url: t,
      initialTool: `crop`
    });
  }, []);
  let or = Z.useRef(() => {
    return null;
  });
  let sr = Z.useCallback((e, t) => {
    if (En.current.filter(t => {
      return t.source === e;
    }).some(e => {
      return Tn.current.find(t => {
        return t.id === e.target;
      })?.type === `imageBoxNode`;
    }) || t) {
      return `imageBox`;
    } else {
      return `newNodes`;
    }
  }, []);
  let cr = Z.useCallback(async (e, t, n, r, i, a, o) => {
    let s;
    let c;
    let l;
    let u;
    let d;
    if (typeof i == `number`) {
      s = n;
      c = r;
      l = i;
      if (typeof a == `string`) {
        u = a;
      } else {
        u = undefined;
      }
      if (typeof a == `object`) {
        d = a;
      } else {
        d = o;
      }
    } else {
      s = n;
      c = n;
      l = r;
      if (typeof i == `string`) {
        u = i;
      } else {
        u = undefined;
      }
      d = undefined;
    }
    let f = sr(e, !!(d?.sendToImageBox ?? d?.outputTarget === `imageBox`));
    try {
      let n = t;
      if (!n) {
        let t = Y().filter(t => {
          return t.target === e;
        });
        if (t.length > 0) {
          let e = J().find(e => {
            return e.id === t[0].source;
          });
          if (e && e.data.imageUrl) {
            n = e.data.imageUrl;
          }
        }
      }
      if (!n) {
        k(`请先连接包含图片的节点`);
        return;
      }
      let r = new Image();
      r.crossOrigin = `Anonymous`;
      r.src = n;
      await new Promise((e, t) => {
        r.onload = e;
        r.onerror = () => {
          return t(Error(`Failed to load image`));
        };
      });
      let i = r.naturalWidth;
      let a = r.naturalHeight;
      if (i === 0 || a === 0) {
        throw Error(`Image has 0 dimensions`);
      }
      let o = i / c;
      let d = a / s;
      let p = Math.floor(l / c);
      let m = l % c;
      let h = document.createElement(`canvas`);
      h.width = Math.max(1, Math.round(o));
      h.height = Math.max(1, Math.round(d));
      let g = h.getContext(`2d`);
      if (!g) {
        throw Error(`Failed to get canvas context`);
      }
      g.drawImage(r, m * o, p * d, o, d, 0, 0, h.width, h.height);
      let _ = h.toDataURL(`image/png`);
      let v = J().find(t => {
        return t.id === e;
      });
      let y = (v?.position.x || 0) + 400;
      let b = (v?.position.y || 0) + 50;
      if (f === `imageBox`) {
        or.current(e, [{
          url: _,
          label: u
        }], {
          startX: y,
          startY: b
        });
        k(`已推送 1 张到图片盒子`);
        return;
      }
      let x = {
        id: `split-one-${e}-${l}-${Date.now()}`,
        type: `imageNode`,
        position: {
          x: y,
          y: b
        },
        data: {
          imageUrl: _,
          onCrop: ar,
          onZoom: Rn,
          onEdit: zn,
          label: u
        },
        style: {
          width: 420,
          height: 420
        }
      };
      K(e => {
        return e.concat(x);
      });
      q(t => {
        return t.concat({
          id: `e-${e}-${x.id}`,
          source: e,
          target: x.id
        });
      });
      k(`已切出第 ${l + 1} 格`);
    } catch (e) {
      console.error(`Split One Error:`, e);
      k(`切片失败: ${e.message}`);
    }
  }, [J, K, ar, k, Y, sr]);
  let lr = Z.useCallback(async (e, t, n, r, i, a) => {
    let o;
    let s;
    let c;
    let l = false;
    if (typeof r == `number`) {
      o = n;
      s = r;
      c = (typeof i == `string` ? i : undefined) || `id{num}`;
      let e = typeof i == `object` ? i : a;
      l = !!(e?.sendToImageBox ?? e?.outputTarget === `imageBox`);
    } else {
      o = n;
      s = n;
      c = (typeof r == `string` ? r : undefined) || `id{num}`;
    }
    let u = sr(e, l);
    try {
      let n = t;
      if (!n) {
        let t = Y().filter(t => {
          return t.target === e;
        });
        if (t.length > 0) {
          let e = J().find(e => {
            return e.id === t[0].source;
          });
          if (e && e.data.imageUrl) {
            n = e.data.imageUrl;
          }
        }
      }
      if (!n) {
        k(`请先连接包含图片的节点`);
        return;
      }
      let r = new Image();
      r.crossOrigin = `Anonymous`;
      r.src = n;
      await new Promise((e, t) => {
        r.onload = e;
        r.onerror = () => {
          return t(Error(`Failed to load image`));
        };
      });
      let i = r.naturalWidth;
      let a = r.naturalHeight;
      if (i === 0 || a === 0) {
        throw Error(`Image has 0 dimensions`);
      }
      let l = i / s;
      let d = a / o;
      let f = document.createElement(`canvas`);
      f.width = Math.max(1, Math.round(l));
      f.height = Math.max(1, Math.round(d));
      let p = f.getContext(`2d`);
      if (!p) {
        throw Error(`Failed to get canvas context`);
      }
      let m = J().find(t => {
        return t.id === e;
      });
      let h = m?.position.x ?? 100;
      let g = m?.position.y ?? 100;
      let _ = h + 400;
      let v = g;
      let y = [];
      for (let e = 0; e < o; e++) {
        for (let t = 0; t < s; t++) {
          p.clearRect(0, 0, f.width, f.height);
          p.drawImage(r, t * l, e * d, l, d, 0, 0, f.width, f.height);
          let n = f.toDataURL(`image/png`);
          let i = e * s + t + 1;
          let a = c.replace(`{num}`, i.toString());
          y.push({
            url: n,
            label: a
          });
        }
      }
      if (u === `imageBox`) {
        or.current(e, y, {
          startX: _,
          startY: v
        });
        k(`已推送 ${y.length} 张到图片盒子`);
        return;
      }
      let b = y.map((t, n) => {
        let r = Math.floor(n / s);
        let i = n % s;
        return {
          id: `split-${e}-${r}-${i}-${Date.now()}-${n}`,
          type: `imageNode`,
          position: {
            x: _ + i * 350,
            y: v + r * 350
          },
          data: {
            imageUrl: t.url,
            onCrop: ar,
            onZoom: Rn,
            onEdit: zn,
            label: t.label
          },
          style: {
            width: 420,
            height: 420
          }
        };
      });
      K(e => {
        return e.concat(b);
      });
      let x = b.map(t => {
        return {
          id: `e-${e}-${t.id}`,
          source: e,
          target: t.id
        };
      });
      q(e => {
        return e.concat(x);
      });
      k(`已生成 ${y.length} 张切片`);
    } catch (e) {
      console.error(`Split Error:`, e);
      k(`切片失败: ${e.message}`);
    }
  }, [J, K, ar, k, Y, sr]);
  let ur = Z.useCallback(async (e, t, n = `id{num}`, r) => {
    if (!t || t.length === 0) {
      k(`没有可用的切片，请先连接图片`);
      return;
    }
    let i = r?.cols ?? Math.max(1, Math.ceil(Math.sqrt(t.length)));
    let a = J().find(t => {
      return t.id === e;
    });
    let o = a?.position.x ?? 100;
    let s = a?.position.y ?? 100;
    let c = o + 400;
    let l = s;
    let u = t.map((e, t) => {
      return {
        url: e,
        label: n.replace(`{num}`, (t + 1).toString())
      };
    });
    if (sr(e, !!(r?.sendToImageBox ?? r?.outputTarget === `imageBox`)) === `imageBox`) {
      or.current(e, u, {
        startX: c,
        startY: l
      });
      k(`已推送 ${u.length} 张到图片盒子`);
      return;
    }
    let d = u.map((t, n) => {
      let r = Math.floor(n / i);
      let a = n % i;
      return {
        id: `split-manual-${e}-${n}-${Date.now()}`,
        type: `imageNode`,
        position: {
          x: c + a * 350,
          y: l + r * 350
        },
        data: {
          imageUrl: t.url,
          onCrop: ar,
          onZoom: Rn,
          onEdit: zn,
          label: t.label
        },
        style: {
          width: 420,
          height: 420
        }
      };
    });
    K(e => {
      return e.concat(d);
    });
    q(t => {
      return t.concat(d.map(t => {
        return {
          id: `e-${e}-${t.id}`,
          source: e,
          target: t.id
        };
      }));
    });
    k(`已生成 ${u.length} 张切片`);
  }, [J, K, q, ar, Rn, zn, k, sr]);
  let dr = Z.useCallback(async (e, t, n, r, i) => {
    if (!t) {
      return;
    }
    let a = J().find(t => {
      return t.id === e;
    });
    let o = (a?.position.x || 0) + 400;
    let s = (a?.position.y || 0) + 50;
    if (sr(e, !!(i?.sendToImageBox ?? i?.outputTarget === `imageBox`)) === `imageBox`) {
      or.current(e, [{
        url: t,
        label: r
      }], {
        startX: o,
        startY: s
      });
      k(`已推送 1 张到图片盒子`);
      return;
    }
    let c = {
      id: `split-manual-one-${e}-${n}-${Date.now()}`,
      type: `imageNode`,
      position: {
        x: o,
        y: s
      },
      data: {
        imageUrl: t,
        onCrop: ar,
        onZoom: Rn,
        onEdit: zn,
        label: r
      },
      style: {
        width: 420,
        height: 420
      }
    };
    K(e => {
      return e.concat(c);
    });
    q(t => {
      return t.concat({
        id: `e-${e}-${c.id}`,
        source: e,
        target: c.id
      });
    });
    k(`已切出第 ${n + 1} 块`);
  }, [J, K, q, ar, Rn, zn, k, sr]);
  let fr = Z.useCallback((e, t, n) => {
    if (!t) {
      return;
    }
    let r = J().find(t => {
      return t.id === e;
    });
    let i = (r?.position.x ?? 100) + 480;
    let a = r?.position.y ?? 100;
    let o = J().filter(e => {
      return Math.abs(e.position.x - i) < 40 && e.position.y >= a;
    });
    let s = o.length ? Math.max(...o.map(e => {
      return e.position.y + (Number(e.measured?.height) || Number(e.style?.height) || 220);
    })) + 20 : a;
    let c = `image-${Date.now()}-${Math.random().toString(36).slice(2, 6)}`;
    let l = {
      id: c,
      type: `imageNode`,
      position: {
        x: i,
        y: s
      },
      data: {
        imageUrl: t,
        onCrop: ar,
        onZoom: Rn,
        onEdit: zn,
        label: n
      },
      style: {
        width: 420,
        height: 420
      }
    };
    K(e => {
      return e.concat(l);
    });
    q(t => {
      return t.concat({
        id: `e-${e}-${c}`,
        source: e,
        sourceHandle: r?.type === `videoProcessNode` ? `main-output` : undefined,
        target: c
      });
    });
  }, [J, K, q, ar, Rn, zn]);
  let gr = Z.useCallback((e, t, n) => {
    if (!t) {
      return;
    }
    let r = J().find(t => {
      return t.id === e;
    });
    let i = (r?.position.x ?? 100) + 480;
    let a = r?.position.y ?? 100;
    let o = J().filter(e => {
      return Math.abs(e.position.x - i) < 40 && e.position.y >= a;
    });
    let s = o.length ? Math.max(...o.map(e => {
      return e.position.y + (Number(e.measured?.height) || Number(e.style?.height) || 220);
    })) + 20 : a;
    let c = `audio-${Date.now()}-${Math.random().toString(36).slice(2, 6)}`;
    let l = {
      id: c,
      type: `audioPlayerNode`,
      position: {
        x: i,
        y: s
      },
      data: {
        audioUrl: t,
        audioName: n,
        label: n
      },
      style: {
        width: 360,
        height: 220
      }
    };
    K(e => {
      return e.concat(l);
    });
    q(t => {
      return t.concat({
        id: `e-${e}-${c}`,
        source: e,
        sourceHandle: r?.type === `videoProcessNode` ? `main-output` : undefined,
        target: c
      });
    });
  }, [J, K, q]);
  let _r = Z.useCallback((e, t, n, r) => {
    let i = J().find(t => {
      return t.id === e;
    });
    if (i) {
      let a = `image-${Date.now()}`;
      let o = {
        id: a,
        type: `imageNode`,
        position: {
          x: i.position.x - 400,
          y: i.position.y
        },
        data: {
          imageUrl: t,
          imageUrlRef: n,
          imageUrlThumbRef: r,
          onCrop: ar,
          onZoom: Rn,
          onEdit: zn
        },
        style: {
          width: 420,
          height: 420
        }
      };
      K(e => {
        return e.concat(o);
      });
      let s = {
        id: `e-${a}-${e}`,
        source: a,
        target: e
      };
      q(e => {
        return e.concat(s);
      });
    }
  }, [J, K, q, ar, Rn]);
  let vr = Z.useCallback(async (e, t, n) => {
    if (!t) {
      return;
    }
    let r = En.current.filter(t => {
      return t.source === e;
    }).map(e => {
      return e.target;
    }).filter(e => {
      return Tn.current.find(t => {
        return t.id === e;
      })?.type === `imageBoxNode`;
    });
    if (r.length === 0) {
      return;
    }
    let i;
    try {
      i = await _cmp_Tr(t, 256, 0.7);
    } catch {
      i = undefined;
    }
    K(e => {
      return e.map(e => {
        if (!r.includes(e.id)) {
          return e;
        }
        let a = e.data?.images || [];
        if (a.some(e => {
          return e.url === t;
        })) {
          return e;
        }
        let o = [...a, {
          id: `img-${Date.now()}-${Math.random().toString(36).slice(2, 6)}`,
          url: t,
          thumb: i,
          label: n,
          source: `gen`,
          createdAt: Date.now()
        }];
        return {
          ...e,
          data: {
            ...e.data,
            images: o,
            activeIndex: o.length - 1
          }
        };
      });
    });
  }, [K]);
  let br = Z.useCallback(async (e, t) => {
    if (!!t && t.length !== 0) {
      or.current?.(e, t);
    }
  }, []);
  let xr = Z.useCallback((e, t, n) => {
    if (!t || t.length === 0) {
      return null;
    }
    let r = En.current.filter(t => {
      return t.source === e;
    }).map(e => {
      return e.target;
    }).filter(e => {
      return Tn.current.find(t => {
        return t.id === e;
      })?.type === `imageBoxNode`;
    });
    let i = (e, t) => {
      return {
        id: `img-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
        url: e,
        label: t,
        source: `gen`,
        createdAt: Date.now()
      };
    };
    if (r.length > 0) {
      let e = r[0];
      K(n => {
        return n.map(n => {
          if (n.id !== e) {
            return n;
          }
          let r = n.data?.images || [];
          let a = new Set(r.map(e => {
            return e.url;
          }));
          let o = t.filter(e => {
            return !a.has(e.url);
          }).map(e => {
            return i(e.url, e.label);
          });
          if (o.length === 0) {
            return n;
          }
          let s = [...r, ...o];
          return {
            ...n,
            data: {
              ...n.data,
              images: s,
              activeIndex: s.length - 1
            }
          };
        });
      });
      Promise.all(t.map(async e => {
        try {
          return {
            url: e.url,
            thumb: await _cmp_Tr(e.url, 256, 0.7)
          };
        } catch {
          return {
            url: e.url,
            thumb: undefined
          };
        }
      })).then(t => {
        let n = new Map(t.map(e => {
          return [e.url, e.thumb];
        }));
        K(t => {
          return t.map(t => {
            if (t.id !== e) {
              return t;
            }
            let r = (t.data?.images || []).map(e => {
              if (e.thumb || !n.has(e.url)) {
                return e;
              } else {
                return {
                  ...e,
                  thumb: n.get(e.url)
                };
              }
            });
            return {
              ...t,
              data: {
                ...t.data,
                images: r
              }
            };
          });
        });
      });
      return e;
    }
    let a = Tn.current.find(t => {
      return t.id === e;
    });
    let o = n?.startX ?? (a?.position.x ?? 100) + 400;
    let s = n?.startY ?? a?.position.y ?? 100;
    let c = `imageBoxNode-${Date.now()}`;
    let l = {
      id: c,
      type: `imageBoxNode`,
      position: {
        x: o,
        y: s
      },
      style: {
        width: 420,
        height: 420
      },
      data: {
        images: t.map(e => {
          return i(e.url, e.label);
        }),
        activeIndex: t.length - 1,
        expanded: t.length > 1,
        label: `图片盒子`,
        onShowToast: k,
        onZoom: Rn,
        onSendToActiveTab: ee
      }
    };
    K(e => {
      return e.concat(l);
    });
    q(t => {
      return t.concat({
        id: `e-${e}-${c}`,
        source: e,
        target: c,
        targetHandle: null
      });
    });
    Promise.all(t.map(async e => {
      try {
        return {
          url: e.url,
          thumb: await _cmp_Tr(e.url, 256, 0.7)
        };
      } catch {
        return {
          url: e.url,
          thumb: undefined
        };
      }
    })).then(e => {
      let t = new Map(e.map(e => {
        return [e.url, e.thumb];
      }));
      K(e => {
        return e.map(e => {
          if (e.id !== c) {
            return e;
          }
          let n = (e.data?.images || []).map(e => {
            if (e.thumb || !t.has(e.url)) {
              return e;
            } else {
              return {
                ...e,
                thumb: t.get(e.url)
              };
            }
          });
          return {
            ...e,
            data: {
              ...e.data,
              images: n
            }
          };
        });
      });
    });
    return c;
  }, [K, q, k, Rn, ee]);
  Z.useEffect(() => {
    or.current = xr;
  }, [xr]);
  let Sr = Z.useCallback(async (e, n, o = `1024x1024`, s, c = `auto`, d = 1) => {
    let f = (s || S.split(`
`)[0] || ``).trim();
    let p = ca(f);
    let m = !!Qi().image.length || !!Qi().text.length || !!Qi().video.length;
    if (p && f && Ee.proxyMode !== `server-proxy` && m) {
      let t = Hi(f);
      if (t) {
        k(t);
        K(t => {
          return t.map(t => {
            if (t.id === e) {
              return {
                ...t,
                data: {
                  ...t.data,
                  loading: false
                }
              };
            } else {
              return t;
            }
          });
        });
        return;
      }
    }
    let h = p ? l : i;
    let g = p ? u : a;
    if (!g && !p && !we) {
      k(`请先在设置中配置图像大模型 API Key`);
      return;
    }
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              loading: true,
              errorMsg: undefined
            }
          };
        } else {
          return t;
        }
      });
    });
    q(t => {
      return t.map(t => {
        if (t.target === e) {
          return {
            ...t,
            animated: true
          };
        } else {
          return t;
        }
      });
    });
    try {
      k(`正在生成图片，请稍候...`);
      let i = Y();
      let a = J();
      let o = i.filter(t => {
        return t.target === e;
      });
      let l = [];
      let u = [];
      let f = [];
      o.forEach(e => {
        let t = a.find(t => {
          return t.id === e.source;
        });
        if (t) {
          let {
            images: n,
            videos: r
          } = Ra(t, e.sourceHandle);
          l.push(...n);
          u.push(...r);
          if (t.type === `scriptBoxNode` || !n.length && !r.length) {
            let n = Ia(t, e.sourceHandle);
            if (n) {
              f.push(n);
            }
          }
        }
      });
      let p = a.find(t => {
        return t.id === e;
      });
      if (p && p.data.selectedContextResources) {
        p.data.selectedContextResources.forEach(e => {
          if (e.type.startsWith(`image`)) {
            if (!l.some(t => {
              return t.id === e.id;
            })) {
              l.push({
                id: e.id,
                url: e.url
              });
            }
          } else if (e.type === `text`) {
            f.push(e.url);
          }
        });
      }
      let m = p?.data.resourceOrder || [];
      let _ = (e, t) => {
        let n = m.indexOf(e.id);
        let r = m.indexOf(t.id);
        if (n === -1 && r === -1) {
          return 0;
        } else if (n === -1) {
          return 1;
        } else if (r === -1) {
          return -1;
        } else {
          return n - r;
        }
      };
      l.sort(_);
      u.sort(_);
      let v = l.map(e => {
        return e.url;
      });
      let y = u.map(e => {
        return e.url;
      });
      let b = e => {
        if (!e) {
          return ``;
        }
        let t = e.replace(/[\u0000-\u0008\u000B-\u000C\u000E-\u001F\u007F-\u009F\u200B-\u200D\uFEFF]/g, ``).trim();
        if (t.length > 6000) {
          t = t.substring(0, 6000);
          console.warn(`Prompt truncated to 6000 characters`);
        }
        return t;
      };
      let C = (f.length > 0 ? `${f.join(`
`)}\n${n}` : n).trim();
      let w = b(C);
      let T = [];
      if (w) {
        T.push({
          text: w
        });
      } else if (v.length === 0 && y.length === 0) {
        T.push({
          text: ` `
        });
      }
      let E = [];
      let D = [];
      for (let e of v) {
        if (e.startsWith(`http://`) || e.startsWith(`https://`)) {
          D.push(e);
        }
        let t = await za(e);
        E.push(t);
        let n = t.match(/^data:(image\/[a-zA-Z+]+);base64,(.+)$/);
        if (n) {
          T.push({
            inlineData: {
              mimeType: n[1],
              data: n[2]
            }
          });
          if (!e.startsWith(`http://`) && !e.startsWith(`https://`)) {
            D.push(n[2]);
          }
        } else if (!e.startsWith(`http://`) && !e.startsWith(`https://`)) {
          D.push(t);
        }
      }
      for (let e of y) {
        e = await za(e);
        let t = e.match(/^data:(video\/[a-zA-Z+]+);base64,(.+)$/);
        if (t) {
          T.push({
            inlineData: {
              mimeType: t[1],
              data: t[2]
            }
          });
        }
      }
      let O = J().find(t => {
        return t.id === e;
      });
      let A = O?.data?.aspectRatio || `Auto`;
      let j = O?.data?.imageSize || `1K`;
      let M = O?.data?.quality || `auto`;
      let N = O?.data?.drawingModel;
      let P = (N || S || `gemini-3.1-flash-image-preview`).split(`
`)[0].trim();
      let F = s || _shared.P;
      console.log(`[handleGenerate] MODEL_SELECT`, JSON.stringify({
        selectedModel: s,
        fallbackModel: _shared.P,
        actualModel: F,
        hasNodeDrawingModel: !!N
      }));
      let ee = F.toLowerCase();
      let L = ee.includes(`banana`) || ee.includes(`gemini`) || ee.includes(`香蕉`) || ee.includes(`芭蕉`);
      let R = c === `openai` || c === `auto` && !L;
      if (A === `Auto` && !_shared.R) {
        let e = `Auto`;
        try {
          let n = [`1:1`, `1:4`, `1:8`, `2:3`, `3:2`, `3:4`, `4:1`, `4:3`, `4:5`, `5:4`, `8:1`, `9:16`, `16:9`, `21:9`];
          if (v.length > 0) {
            k(`正在分析参考图比例...`);
            let t = new Image();
            await new Promise((e, n) => {
              t.onload = e;
              t.onerror = n;
              t.src = v[0];
            });
            let r = t.width / t.height;
            let i = `1:1`;
            let a = Infinity;
            for (let e of n) {
              let [t, n] = e.split(`:`).map(Number);
              let o = Math.abs(r - t / n);
              if (o < a) {
                a = o;
                i = e;
              }
            }
            e = i;
            k(`自动匹配参考图比例: ${e}`);
          } else if (C.trim()) {
            k(`正在根据提示词分析最佳比例...`);
            let n = t.replace(/\/$/, ``);
            let i = x.split(`
`)[0].trim();
            let a = `请根据我的提示词，推荐一个最适合生成画面的图片比例。
可选的比例只有以下几种：
1:1
1:4
1:8
2:3
3:2
3:4
4:1
4:3
4:5
5:4
8:1
9:16
16:9
21:9

你的回复必须只包含比例本身（比如 "16:9"），不要输出任何其他文字、标点符号或解释。

我的提示词是：
${C}`;
            let o = await Bl(`${n}/v1/chat/completions`, {
              method: `POST`,
              headers: {
                Authorization: `Bearer ${r || ``}`,
                'Content-Type': `application/json`
              },
              body: JSON.stringify({
                model: i,
                messages: [{
                  role: `user`,
                  content: a
                }],
                max_tokens: 50,
                temperature: 0.1
              }),
              ...Ee
            });
            if (o.ok) {
              let t = (await o.json()).choices?.[0]?.message?.content?.trim()?.match(/(1:1|1:4|1:8|2:3|3:2|3:4|4:1|4:3|4:5|5:4|8:1|9:16|16:9|21:9)/);
              if (t) {
                e = t[1];
                k(`AI分析提示词得出比例: ${e}`);
              } else {
                k(`大模型未提供明确比例，将使用其原生默认画幅`);
              }
            } else {
              console.warn(`Auto aspect ratio API failed`, await o.text());
            }
          }
          A = e;
        } catch (e) {
          console.error(`Auto aspect ratio failed`, e);
          A = `Auto`;
        }
      }
      let te = {};
      if (A && A !== `Auto`) {
        te.aspectRatio = A;
      }
      if (j) {
        te.imageSize = j;
      }
      let z = {
        contents: [{
          role: `user`,
          parts: T
        }],
        generationConfig: {
          responseModalities: [`IMAGE`]
        }
      };
      if (Object.keys(te).length > 0) {
        te.aspectRatio ||= `1:1`;
        z.generationConfig.imageConfig = te;
      }
      let ne = ``;
      let B = {};
      let re = false;
      let V = h.replace(/\/$/, ``);
      let ie = _shared.R;
      let oe = {
        '1:1': {
          '1K': `1024x1024`,
          '2K': `2048x2048`,
          '4K': `2880x2880`
        },
        '16:9': {
          '1K': `1776x880`,
          '2K': `2048x1152`,
          '4K': `3840x2160`
        },
        '9:16': {
          '1K': `880x1776`,
          '2K': `1152x2048`,
          '4K': `2160x3840`
        },
        '3:2': {
          '1K': `1536x1024`,
          '2K': `2048x1360`,
          '4K': `3504x2336`
        },
        '2:3': {
          '1K': `1024x1536`,
          '2K': `1360x2048`,
          '4K': `2336x3504`
        },
        '21:9': {
          '1K': `2048x880`,
          '2K': `2048x880`,
          '4K': `3840x1648`
        },
        '9:21': {
          '1K': `880x2048`,
          '2K': `880x2048`,
          '4K': `1648x3840`
        },
        '1:3': {
          '1K': `688x2048`,
          '2K': `688x2048`,
          '4K': `1280x3840`
        },
        '3:1': {
          '1K': `2048x688`,
          '2K': `2048x688`,
          '4K': `3840x1280`
        },
        '2:1': {
          '1K': `2048x1024`,
          '2K': `2048x1024`,
          '4K': `3840x1920`
        },
        '1:2': {
          '1K': `1024x2048`,
          '2K': `1024x2048`,
          '4K': `1920x3840`
        },
        '4:3': {
          '1K': `1024x768`,
          '2K': `2304x1728`,
          '4K': `2880x2160`
        },
        '3:4': {
          '1K': `768x1024`,
          '2K': `1728x2304`,
          '4K': `2160x2880`
        }
      };
      let se = async e => {
        return await (await fetch(e)).blob();
      };
      if (_shared.R) {
        let e = !A || A === `Auto`;
        let t = String(e ? `Auto` : A);
        let n = e ? `auto` : oe[t]?.[String(j)] || oe[t]?.[`1K`] || `1024x1024`;
        if (ie) {
          ne = `${V}/v1/draw/completions`;
          B = {
            model: F,
            prompt: w,
            aspectRatio: t === `Auto` ? undefined : t
          };
          if (D.length > 0) {
            B.urls = D;
          }
        } else if (v.length > 0) {
          ne = `${V}/v1/images/edits`;
          re = true;
          B = new FormData();
          B.append(`model`, F);
          B.append(`prompt`, w || ` `);
          B.append(`n`, `1`);
          if (n !== `Auto`) {
            B.append(`size`, n);
          }
          B.append(`image_size`, String(j).toUpperCase());
          B.append(`quality`, M);
          for (let e = 0; e < v.length; e++) {
            try {
              console.log(`referenceImages`, v[e]);
              let t = await se(v[e]);
              B.append(`image`, t, `image_${e}.png`);
            } catch (e) {
              console.error(`Failed to fetch image as blob for edit`, e);
            }
          }
        } else {
          ne = `${V}/v1/images/generations`;
          B = {
            model: F,
            prompt: w,
            n: 1,
            image_size: String(j).toUpperCase(),
            quality: M
          };
          if (n !== `Auto`) {
            B.size = n;
          }
        }
      } else {
        ne = `${V}/v1beta/models/${F}:generateContent?key=${g}`;
        B = z;
      }
      let ce = null;
      if (d > 1) {
        let t = Y();
        let n = J();
        let r = t.filter(t => {
          return t.source === e;
        }).find(e => {
          let t = n.find(t => {
            return t.id === e.target;
          });
          return t && t.type === `imageBoxNode`;
        });
        if (r) {
          ce = r.target;
        } else {
          let t = n.find(t => {
            return t.id === e;
          });
          if (t) {
            let n = `imageBox-${Date.now()}`;
            let r = {
              x: t.position.x + 500,
              y: t.position.y
            };
            K(e => {
              return [...e, {
                id: n,
                type: `imageBoxNode`,
                position: r,
                style: {
                  width: 420,
                  height: 420
                },
                data: {
                  images: []
                }
              }];
            });
            q(t => {
              return [...t, {
                id: `e-${e}-${n}`,
                source: e,
                target: n,
                animated: false
              }];
            });
            ce = n;
          }
        }
      }
      let U = async t => {
        let n = `${e}_${t}`;
        let r = `${e}_${Date.now()}_${t}`;
        try {
          let i = new AbortController();
          zt.current.set(n, i);
          let a = (ae || 600) * 1000;
          let o = setTimeout(() => {
            if (zt.current.has(n) && d === 1) {
              k(`生成耗时较长，已转入后台运行，请稍后在“任务清单”中查看。`);
              K(t => {
                return t.map(t => {
                  if (t.id === e) {
                    return {
                      ...t,
                      data: {
                        ...t.data,
                        loading: false,
                        errorMsg: `已转入后台运行，完成后可在任务清单中查看`
                      }
                    };
                  } else {
                    return t;
                  }
                });
              });
            }
          }, a);
          if (I) {
            if (d === 1) {
              H?.(true);
            }
            I(t => {
              let n = t.filter(e => {
                return e.id !== r;
              });
              return [{
                id: r,
                nodeId: e,
                type: `image`,
                status: `running`,
                progress: 0,
                createdAt: Date.now(),
                prompt: z.contents[0].parts[0].text,
                channelName: I_(h),
                modelName: F,
                // 修复：图生图 B 是 FormData，JSON.stringify 会得空 {}，侧边栏日志显示为空；
                // 转成普通可序列化对象再记录（Blob/File 字段转可读占位符），不影响实际请求发送。
                requestData: re ? Object.fromEntries(Array.from(B.entries()).map(([k, v]) => [k, v instanceof Blob ? `[${k}:${v.type || 'file'}:${v.name || 'blob'}]` : v])) : B
              }, ...n];
            });
          }
          let s = _shared.R ? {
            Authorization: `Bearer ${g}`
          } : {};
          if (!re) {
            s[`Content-Type`] = `application/json`;
          }
          let c = re ? `[FormData]` : JSON.stringify(B);
          console.log(`[生图调试] fetchUrl:`, ne, `| bodyLength:`, c.length, `| isFormData:`, re, `| localPort:`, Ce.status.isConnected ? Ce.status.port : `none`);
          let l = await Bl(ne, {
            method: `POST`,
            headers: s,
            body: re ? B : c,
            ...Ee
          }).finally(() => {
            clearTimeout(o);
          });
          console.log(`[生图调试] response status:`, l.status, `| content-type:`, l.headers.get(`content-type`));
          zt.current.delete(n);
          if (!l.ok) {
            let e = `API 请求失败: ${l.status} ${l.statusText}`;
            try {
              let t = await l.json();
              if (t.error && t.error.message) {
                e = `API 请求失败: ${t.error.message}`;
              } else {
                if (t.message) {
                  e = `API 请求失败: ${t.message}`;
                } else {
                  e = `API 请求失败: ${l.status} - ${JSON.stringify(t)}`;
                }
              }
            } catch {}
            throw Error(e);
          }
          let u = ``;
          if (ie && l.body) {
            let t = l.body.getReader();
            let n = new TextDecoder();
            let i = ``;
            try {
              while (true) {
                let {
                  done: a,
                  value: o
                } = await t.read();
                if (a) {
                  break;
                }
                i += n.decode(o, {
                  stream: true
                });
                let s = i.split(`
`);
                i = s.pop() || ``;
                for (let t of s) {
                  let n = t.trim();
                  if (n.startsWith(`data:`) && n !== `data: [DONE]`) {
                    let t = n.substring(5).trim();
                    if (!t) {
                      continue;
                    }
                    try {
                      let n = JSON.parse(t);
                      if (n.progress !== undefined) {
                        let t = parseInt(n.progress, 10);
                        if (I) {
                          I(e => {
                            return e.map(e => {
                              if (e.id === r || e.taskId === r) {
                                return {
                                  ...e,
                                  status: `running`,
                                  progress: t
                                };
                              } else {
                                return e;
                              }
                            });
                          });
                        }
                        if (d === 1) {
                          K(n => {
                            return n.map(n => {
                              if (n.id === e) {
                                return {
                                  ...n,
                                  data: {
                                    ...n.data,
                                    progress: t
                                  }
                                };
                              } else {
                                return n;
                              }
                            });
                          });
                        }
                      }
                      if (n.status === `succeeded` && n.results && n.results[0] && n.results[0].url) {
                        u = n.results[0].url;
                      }
                      if (n.status === `failed` || n.error) {
                        throw Error(n.failure_reason || n.error || `生成失败`);
                      }
                    } catch (e) {
                      if (e.message !== `Unexpected token` && !(e instanceof SyntaxError)) {
                        console.error(`SSE parse error`, e, t);
                      }
                    }
                  }
                }
              }
            } finally {
              t.releaseLock();
            }
            if (!u) {
              throw Error(`流式请求未返回最终图片URL`);
            }
          } else {
            let e = await l.text();
            if (!e || e.trim() === ``) {
              throw Error(`API 返回空响应，请检查模型是否支持生图或 API Key 是否有效`);
            }
            let t;
            try {
              t = JSON.parse(e);
            } catch {
              console.error(`JSON parse error, raw response:`, e.substring(0, 500));
              throw Error(`API 响应解析失败: ${e.substring(0, 100)}`);
            }
            if (_shared.R) {
              let e = t.data?.[0];
              if (e?.b64_json) {
                u = `data:image/png;base64,${e.b64_json}`;
              } else if (e?.url) {
                u = e.url;
              }
            } else {
              let e = t.candidates?.[0];
              if (!e) {
                throw Error(`API 返回格式错误：找不到 candidates`);
              }
              let n = e.content?.parts?.find(e => {
                return e.inlineData;
              });
              let r = e.content?.parts?.find(e => {
                return e.text;
              });
              if (n && n.inlineData) {
                u = `data:${n.inlineData.mimeType || `image/png`};base64,${n.inlineData.data}`;
              } else if (r && r.text) {
                let e = r.text.match(/!\[.*?\]\((data:image\/[^;]+;base64,[^)]+)\)/);
                if (e && e[1]) {
                  u = e[1];
                } else {
                  console.warn(`Model returned text:`, r.text);
                  if (d === 1) {
                    k(`API返回文本: ${r.text.substring(0, 100)}...`);
                  }
                  throw Error(`未生成图片，请检查提示词或模型设置`);
                }
              }
            }
          }
          if (!u) {
            throw Error(`未生成图片`);
          }
          let f;
          if (u.startsWith(`data:`)) {
            try {
              let e = await hi(u, {
                subfolder: `tasks`,
                preferThumbnail: true,
                thumbMaxDim: 480,
                thumbQuality: 75
              });
              if (e.url && /^https?:\/\//i.test(e.url)) {
                u = e.url;
                f = e.thumbnailUrl;
              }
            } catch (e) {
              console.warn(`[handleGenerate] urlifyAsset failed for generated image, keep base64:`, e);
            }
          }
          if (u) {
            let n = new Image();
            n.onload = () => {
              let i = 320;
              let a = 320;
              let o = n.width / n.height;
              if (A === `Auto` || !A) {
                a = Math.min(n.height, 400);
                i = a * o;
                if (i > 800) {
                  i = 800;
                  a = i / o;
                }
              } else if (A === `16:9`) {
                i = 600;
                a = 9 / 16 * i;
              } else if (A === `9:16`) {
                a = 400;
                i = 9 / 16 * a;
              } else if (A === `4:3`) {
                i = 500;
                a = 3 / 4 * i;
              } else if (A === `3:4`) {
                a = 400;
                i = 3 / 4 * a;
              } else {
                a = 320;
                i = a;
              }
              if (I) {
                I(e => {
                  return e.map(e => {
                    if (e.id === r || e.taskId === r) {
                      return {
                        ...e,
                        status: `completed`,
                        progress: 100,
                        resultUrl: u,
                        thumbnailUrl: f
                      };
                    } else {
                      return e;
                    }
                  });
                });
              }
              if (d > 1 && ce) {
                K(e => {
                  return e.map(e => {
                    if (e.id === ce) {
                      let n = [...(e.data.images || []), {
                        id: `img_${Date.now()}_${t}`,
                        url: u,
                        thumbnailUrl: f,
                        source: `gen`,
                        createdAt: Date.now()
                      }];
                      return {
                        ...e,
                        data: {
                          ...e.data,
                          images: n,
                          activeIndex: n.length - 1
                        }
                      };
                    }
                    return e;
                  });
                });
                vr(e, u);
                if (d === 1) {
                  k(`生成成功！`);
                }
              } else {
                K(t => {
                  return t.map(t => {
                    if (t.id === e) {
                      let {
                        imageUrlRef: e,
                        imageUrlThumbRef: n,
                        ...r
                      } = t.data;
                      return {
                        ...t,
                        style: {
                          ...t.style,
                          width: i,
                          height: a
                        },
                        data: {
                          ...r,
                          imageUrl: u,
                          thumbnailUrl: f,
                          loading: false,
                          errorMsg: undefined
                        }
                      };
                    }
                    return t;
                  });
                });
                bn.current.delete(e);
                q(t => {
                  return t.map(t => {
                    if (t.target === e) {
                      return {
                        ...t,
                        animated: false
                      };
                    } else {
                      return t;
                    }
                  });
                });
                vr(e, u);
                if (d === 1) {
                  k(`生成成功！`);
                }
              }
            };
            n.onerror = () => {
              if (I) {
                I(e => {
                  return e.map(e => {
                    if (e.id === r || e.taskId === r) {
                      return {
                        ...e,
                        status: `completed`,
                        progress: 100,
                        resultUrl: u,
                        thumbnailUrl: f
                      };
                    } else {
                      return e;
                    }
                  });
                });
              }
              if (d > 1 && ce) {
                K(e => {
                  return e.map(e => {
                    if (e.id === ce) {
                      let n = [...(e.data.images || []), {
                        id: `img_${Date.now()}_${t}`,
                        url: u,
                        thumbnailUrl: f,
                        source: `gen`,
                        createdAt: Date.now()
                      }];
                      return {
                        ...e,
                        data: {
                          ...e.data,
                          images: n,
                          activeIndex: n.length - 1
                        }
                      };
                    }
                    return e;
                  });
                });
                vr(e, u);
                if (d === 1) {
                  k(`生成成功 (预览加载失败)`);
                }
              } else {
                K(t => {
                  return t.map(t => {
                    if (t.id === e) {
                      let {
                        imageUrlRef: e,
                        imageUrlThumbRef: n,
                        ...r
                      } = t.data;
                      return {
                        ...t,
                        data: {
                          ...r,
                          imageUrl: u,
                          thumbnailUrl: f,
                          loading: false,
                          errorMsg: undefined
                        }
                      };
                    }
                    return t;
                  });
                });
                bn.current.delete(e);
                q(t => {
                  return t.map(t => {
                    if (t.target === e) {
                      return {
                        ...t,
                        animated: false
                      };
                    } else {
                      return t;
                    }
                  });
                });
                vr(e, u);
                if (d === 1) {
                  k(`生成成功 (预览加载失败)`);
                }
              }
            };
            n.src = u;
          }
        } catch (t) {
          if (t.name === `AbortError`) {
            if (zt.current.has(n)) {
              zt.current.delete(n);
              console.log(`Fetch timed out`);
              if (d === 1) {
                k(`生成超时，请重试`);
              }
              if (I) {
                I(e => {
                  return e.map(e => {
                    if (e.id === r || e.taskId === r && (e.status === `running` || e.status === `pending`)) {
                      return {
                        ...e,
                        status: `failed`,
                        errorMsg: `请求超时`
                      };
                    } else {
                      return e;
                    }
                  });
                });
              }
              if (d === 1) {
                K(t => {
                  return t.map(t => {
                    if (t.id === e) {
                      return {
                        ...t,
                        data: {
                          ...t.data,
                          loading: false,
                          errorMsg: `请求超时`
                        }
                      };
                    } else {
                      return t;
                    }
                  });
                });
                q(t => {
                  return t.map(t => {
                    if (t.target === e) {
                      return {
                        ...t,
                        animated: false
                      };
                    } else {
                      return t;
                    }
                  });
                });
              }
            } else {
              console.log(`Fetch aborted by user`);
              if (I) {
                I(e => {
                  return e.map(e => {
                    if (e.id === r || e.taskId === r && (e.status === `running` || e.status === `pending`)) {
                      return {
                        ...e,
                        status: `failed`,
                        errorMsg: `已取消`
                      };
                    } else {
                      return e;
                    }
                  });
                });
              }
            }
            return;
          }
          console.error(t);
          if (d === 1) {
            k(`生成失败: ${t.message}`);
          }
          if (I) {
            I(e => {
              return e.map(e => {
                if (e.id === r || e.taskId === r && (e.status === `running` || e.status === `pending`)) {
                  return {
                    ...e,
                    status: `failed`,
                    errorMsg: t.message
                  };
                } else {
                  return e;
                }
              });
            });
          }
          if (d === 1) {
            K(n => {
              return n.map(n => {
                if (n.id === e) {
                  return {
                    ...n,
                    data: {
                      ...n.data,
                      loading: false,
                      errorMsg: t.message
                    }
                  };
                } else {
                  return n;
                }
              });
            });
            q(t => {
              return t.map(t => {
                if (t.target === e) {
                  return {
                    ...t,
                    animated: false
                  };
                } else {
                  return t;
                }
              });
            });
          }
        }
      };
      if (d > 1) {
        await Promise.all(Array.from({
          length: d
        }).map((e, t) => {
          return U(t);
        })).finally(() => {
          K(t => {
            return t.map(t => {
              if (t.id === e) {
                return {
                  ...t,
                  data: {
                    ...t.data,
                    loading: false
                  }
                };
              } else {
                return t;
              }
            });
          });
          q(t => {
            return t.map(t => {
              if (t.target === e) {
                return {
                  ...t,
                  animated: false
                };
              } else {
                return t;
              }
            });
          });
        });
      } else {
        await U(0);
      }
    } catch (t) {
      console.error(`Outer generation error:`, t);
      if (d === 1) {
        k(`生成准备失败: ${t.message}`);
      }
      K(n => {
        return n.map(n => {
          if (n.id === e) {
            return {
              ...n,
              data: {
                ...n.data,
                loading: false,
                errorMsg: t.message
              }
            };
          } else {
            return n;
          }
        });
      });
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    }
  }, [a, i, l, u, S, k, J, Y, K, j, N, ae, I, vr]);
  let Cr = Z.useCallback(async (e, t, n = 0) => {
    if (e.startsWith(`asset://`)) {
      return e;
    }
    let r = e;
    if (e.startsWith(`data:`) || e.startsWith(`blob:`) || /:\d+\/files\//.test(e)) {
      if (!U || !U.accessKey) {
        throw Error(`未配置七牛云对象存储，无法上传本地素材。请在"数据管理"中配置。`);
      }
      k(`正在上传${t === `image` ? `图片` : t === `video` ? `视频` : `音频`} [${n + 1}] 到对象存储...`);
      let i;
      if (e.startsWith(`data:`)) {
        let t = e.split(`,`);
        let n = t[0].match(/:(.*?);/);
        let r = n ? n[1] : `application/octet-stream`;
        let a = atob(t[1]);
        let o = a.length;
        let s = new Uint8Array(o);
        while (o--) {
          s[o] = a.charCodeAt(o);
        }
        i = new Blob([s], {
          type: r
        });
      } else {
        i = await (await fetch(e)).blob();
      }
      r = await Jl(i, U);
    }
    if (!W) {
      throw Error(`未配置 SD2 素材库 Token，无法转换素材。请在"数据管理"中配置。`);
    }
    k(`正在转换${t === `image` ? `图片` : t === `video` ? `视频` : `音频`} [${n + 1}] 为素材 ID...`);
    let i = d || `https://ai.kkidc.com`;
    if (i.includes(`ai-api.kkidc.com`)) {
      i = `https://ai.kkidc.com`;
    }
    let a = i.replace(/\/$/, ``);
    let o = {
      image: `Image`,
      video: `Video`,
      audio: `Audio`
    }[t] || `Image`;
    let s = await Bl(`${a}/api/assets/upload`, {
      method: `POST`,
      headers: {
        Authorization: W,
        'Content-Type': `application/json`,
        Accept: `*/*`
      },
      body: {
        url: r,
        asset_type: o,
        name: `自动上传_${t}_${Date.now()}`
      },
      ...Ee
    });
    if (!s.ok) {
      let e = await s.json().catch(() => {
        return {};
      });
      throw Error(`素材转换失败: ${e.error?.message || e.error || e.message || s.status}`);
    }
    let c = await s.json();
    let l = c?.data?.Id || c?.Id || c?.id;
    if (!l) {
      console.error(`Unexpected asset API response:`, c);
      throw Error(`素材转换接口未返回有效 ID`);
    }
    await new Promise(e => {
      return setTimeout(e, 1000);
    });
    return `asset://${l}`;
  }, [U, W, d, Ce.status.isConnected, Ce.status.port, k]);
  let wr = Z.useCallback(async (e, t, n = 0) => {
    if (!e.startsWith(`data:`) && !e.startsWith(`blob:`) && !/:\d+\/files\//.test(e)) {
      return e;
    }
    if (!m && Ee.proxyMode !== `server-proxy`) {
      throw Error(`未配置特惠视频 API Key，无法上传素材`);
    }
    let r;
    let i = `upload_${Date.now()}`;
    if (e.startsWith(`data:`)) {
      let t = e.split(`,`);
      let n = t[0].match(/:(.*?);/);
      let a = n ? n[1] : `application/octet-stream`;
      let o = a.split(`/`)[1] || `bin`;
      i += `.${o}`;
      let s = atob(t[1]);
      let c = s.length;
      let l = new Uint8Array(c);
      while (c--) {
        l[c] = s.charCodeAt(c);
      }
      r = new Blob([l], {
        type: a
      });
    } else {
      r = await (await fetch(e)).blob();
      let t = e.split(`/`);
      i = t[t.length - 1] || i;
    }
    if (p) {
      let e = new FormData();
      e.append(`file`, r, i);
      let t = await Bl(`${p.replace(/[`\s]/g, ``).replace(/\/$/, ``)}/v1/gateway/upload`, {
        method: `POST`,
        headers: {
          Authorization: `Bearer ${m || ``}`
        },
        body: e,
        ...Ee
      });
      if (!t.ok) {
        let e = await t.json().catch(() => {
          return {};
        });
        throw Error(`网关上传失败: ${e.error?.message || e.message || e.error || t.status}`);
      }
      return (await t.json()).file_url;
    }
    return null;
  }, [m, p, Ee]);
  let Er = Z.useCallback(async (e, t, n, r, i) => {
    if (!f) {
      k(`请先在设置中配置 SD2 视频大模型 API Key`);
      return;
    }
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              loading: true,
              progress: 0,
              errorMsg: undefined
            }
          };
        } else {
          return t;
        }
      });
    });
    q(t => {
      return t.map(t => {
        if (t.target === e) {
          return {
            ...t,
            animated: true
          };
        } else {
          return t;
        }
      });
    });
    try {
      k(`正在提交视频生成任务...`);
      let a = Y();
      let o = J();
      let s = a.filter(t => {
        return t.target === e;
      });
      let c = o.find(t => {
        return t.id === e;
      });
      let l = c?.data.internalResources || [];
      let u = [];
      let p = [];
      let m = [];
      let h = [];
      l.forEach(e => {
        if (e.type.startsWith(`image`)) {
          u.push({
            id: e.id,
            url: e.url
          });
        } else if (e.type.startsWith(`video`)) {
          p.push({
            id: e.id,
            url: e.url
          });
        } else if (e.type.startsWith(`audio`)) {
          m.push({
            id: e.id,
            url: e.url
          });
        }
      });
      s.forEach(e => {
        let t = o.find(t => {
          return t.id === e.source;
        });
        if (t) {
          let {
            images: n,
            videos: r,
            audios: i
          } = Ra(t, e.sourceHandle);
          u.push(...n);
          p.push(...r);
          m.push(...i);
          let a = Ia(t, e.sourceHandle, `video`);
          if (a) {
            h.push(a);
          }
        }
      });
      let g = o.find(t => {
        return t.id === e;
      });
      if (g && g.data.selectedContextResources) {
        g.data.selectedContextResources.forEach(e => {
          if (e.type.startsWith(`image`)) {
            if (!u.some(t => {
              return t.id === e.id;
            })) {
              u.push({
                id: e.id,
                url: e.url
              });
            }
          } else if (e.type.startsWith(`video`)) {
            if (!p.some(t => {
              return t.id === e.id;
            })) {
              p.push({
                id: e.id,
                url: e.url
              });
            }
          } else if (e.type.startsWith(`audio`)) {
            if (!m.some(t => {
              return t.id === e.id;
            })) {
              m.push({
                id: e.id,
                url: e.url
              });
            }
          } else if (e.type === `text`) {
            h.push(e.url);
          }
        });
      }
      let _ = g?.data.resourceOrder || [];
      let v = (e, t) => {
        let n = _.indexOf(e.id);
        let r = _.indexOf(t.id);
        if (n === -1 && r === -1) {
          return 0;
        } else if (n === -1) {
          return 1;
        } else if (r === -1) {
          return -1;
        } else {
          return n - r;
        }
      };
      u.sort(v);
      p.sort(v);
      m.sort(v);
      let y = u.map(e => {
        return e.url;
      });
      let b = p.map(e => {
        return e.url;
      });
      let x = m.map(e => {
        return e.url;
      });
      if (y.length > 9) {
        y = y.slice(0, 9);
      }
      if (b.length > 3) {
        b = b.slice(0, 3);
      }
      if (x.length > 3) {
        x = x.slice(0, 3);
      }
      let S = c?.data.uploadedAssets || {};
      try {
        if (y.length > 0) {
          y = await Promise.all(y.map((e, t) => {
            return Cr(S[e] || e, `image`, t);
          }));
        }
        if (b.length > 0) {
          b = await Promise.all(b.map((e, t) => {
            return Cr(S[e] || e, `video`, t);
          }));
        }
        if (x.length > 0) {
          x = await Promise.all(x.map((e, t) => {
            return Cr(S[e] || e, `audio`, t);
          }));
        }
      } catch (e) {
        throw Error(e.message);
      }
      let C = (d || `https://ai-api.kkidc.com`).replace(/\/$/, ``);
      let T = `${C}/v1/video/generations`;
      let E = new AbortController();
      zt.current.set(e, E);
      let O = r || (w || `seed-2`).split(`
`)[0].trim();
      let A = h.length > 0 ? `${h.join(`
`)}\n${t}` : t;
      let M = parseInt(i || (D || `10`).split(`
`)[0].trim());
      let N = `${e}_${Date.now()}`;
      if (I) {
        H?.(true);
        I(t => {
          return [{
            id: N,
            nodeId: e,
            type: `sd2Video`,
            status: `pending`,
            progress: 0,
            createdAt: Date.now(),
            prompt: A,
            channelName: I_(d),
            modelName: O,
            apiUrl: d,
            apiKey: f
          }, ...t];
        });
      }
      let P = {
        model: O,
        prompt: A,
        metadata: {
          reference_images: y,
          reference_videos: b,
          reference_audios: x,
          generate_audio: true,
          ratio: n,
          duration: M,
          watermark: false
        }
      };
      if (I) {
        I(e => {
          return e.map(e => {
            if (e.id === N) {
              return {
                ...e,
                requestData: _shared.P
              };
            } else {
              return e;
            }
          });
        });
      }
      let F = await Bl(T, {
        method: `POST`,
        headers: {
          Authorization: `Bearer ${f}`,
          'Content-Type': `application/json`,
          Accept: `*/*`
        },
        body: JSON.stringify(_shared.P),
        ...Ee
      });
      if (!F.ok) {
        let e = {};
        let t = ``;
        try {
          t = await F.text();
          try {
            e = JSON.parse(t);
          } catch {
            e = {
              detail: t
            };
          }
        } catch {}
        if (I) {
          I(n => {
            return n.map(n => {
              if (n.id === N) {
                return {
                  ...n,
                  responseData: e ?? t
                };
              } else {
                return n;
              }
            });
          });
        }
        let n = e.detail || e.error?.message || e.message || e.error || t || `请求失败: ${F.status}`;
        throw Error(typeof n == `string` ? n : `请求失败: ${F.status}`);
      }
      let ee = await F.json();
      let L = ee.task_id || ee.id;
      if (!L) {
        throw Error(`未返回任务 ID`);
      }
      if (I) {
        I(e => {
          return e.map(e => {
            if (e.id === N) {
              return {
                ...e,
                taskId: L
              };
            } else {
              return e;
            }
          });
        });
      }
      k(`任务提交成功，正在排队...`);
      K(t => {
        return t.map(t => {
          if (t.id === e) {
            return {
              ...t,
              data: {
                ...t.data,
                progress: 10
              }
            };
          } else {
            return t;
          }
        });
      });
      let R = 0;
      let te = Math.ceil((re || 600) / (B || 3));
      let z = async () => {
        if (_shared.R >= te) {
          throw Error(`生成超时，请稍后再试`);
        }
        let t = jn.current || [];
        if (t.length > 0) {
          let n = t.find(e => {
            return e.id === L || e.taskId === L;
          });
          if (n && (n.status === `success` || n.status === `succeeded` || n.status === `completed`) && n.resultUrl) {
            K(t => {
              return t.map(t => {
                if (t.id === e) {
                  return {
                    ...t,
                    data: {
                      ...t.data,
                      loading: false,
                      progress: 100,
                      videoUrl: An(n.resultUrl),
                      thumbnailUrl: An(n.thumbnailUrl),
                      imageAvailable: !!n.thumbnailUrl,
                      errorMsg: undefined,
                      errorMessage: undefined
                    }
                  };
                } else {
                  return t;
                }
              });
            });
            return;
          }
        }
        await new Promise(e => {
          return setTimeout(e, (B || 3) * 1000);
        });
        if (E.signal.aborted) {
          throw Error(`已取消`);
        }
        let n = await fetch(`${C}/v1/video/generations/${L}`, {
          headers: {
            Authorization: `Bearer ${f}`,
            Accept: `*/*`
          },
          signal: E.signal
        });
        if (!n.ok) {
          let e = await n.json().catch(() => {
            return {};
          });
          throw Error(e.error?.message || e.message || `查询失败: ${n.status}`);
        }
        let r = await n.json();
        if (r.code === `success` && r.data) {
          let t = r.data;
          let n = t.status;
          if (n === `SUCCESS` || n === `succeeded`) {
            let n = t.data?.content?.video_url || t.fail_reason;
            if (!n) {
              throw Error(`视频生成成功，但未找到视频链接`);
            }
            if (I) {
              I(e => {
                return e.map(e => {
                  if (e.id === L || e.taskId === L) {
                    return {
                      ...e,
                      status: `completed`,
                      progress: 100,
                      resultUrl: n
                    };
                  } else {
                    return e;
                  }
                });
              });
            }
            K(t => {
              return t.map(t => {
                if (t.id === e) {
                  return {
                    ...t,
                    data: {
                      ...t.data,
                      loading: false,
                      progress: 100,
                      videoUrl: n,
                      errorMsg: undefined
                    }
                  };
                } else {
                  return t;
                }
              });
            });
            q(t => {
              return t.map(t => {
                if (t.target === e) {
                  return {
                    ...t,
                    animated: false
                  };
                } else {
                  return t;
                }
              });
            });
            k(`视频生成完成`);
            if (typeof chrome < `u` && chrome.runtime && chrome.runtime.id) {
              j?.(n, `video`, `generated`);
            }
            zt.current.delete(e);
            return;
          } else if (n === `IN_PROGRESS` || n === `running`) {
            let n = 50;
            if (t.progress) {
              n = parseInt(t.progress.replace(`%`, ``)) || 50;
            }
            if (I) {
              I(e => {
                return e.map(e => {
                  if (e.id === L || e.taskId === L) {
                    return {
                      ...e,
                      status: `running`,
                      progress: n
                    };
                  } else {
                    return e;
                  }
                });
              });
            }
            K(t => {
              return t.map(t => {
                if (t.id === e) {
                  return {
                    ...t,
                    data: {
                      ...t.data,
                      progress: n
                    }
                  };
                } else {
                  return t;
                }
              });
            });
            _shared.R++;
            return z();
          } else if (n === `FAILED` || n === `failed`) {
            throw Error(t.fail_reason || `任务执行失败`);
          } else {
            _shared.R++;
            return z();
          }
        } else if (r.error) {
          throw Error(r.error.message || `查询失败`);
        } else {
          _shared.R++;
          return z();
        }
      };
      await z();
    } catch (t) {
      if (t.name === `AbortError` || t.message === `已取消`) {
        console.log(`Video generation aborted`);
        return;
      }
      console.error(t);
      if (I && t.message !== `已取消`) {
        I(n => {
          return n.map(n => {
            if (n.nodeId === e && (n.status === `running` || n.status === `pending`)) {
              return {
                ...n,
                status: `failed`,
                errorMsg: t.message
              };
            } else {
              return n;
            }
          });
        });
      }
      k(t.message || `生成视频时出错，请检查网络或 API 配置`);
      K(n => {
        return n.map(n => {
          if (n.id === e) {
            return {
              ...n,
              data: {
                ...n.data,
                loading: false,
                errorMsg: t.message || `生成失败`,
                errorMessage: t.message || `生成失败`
              }
            };
          } else {
            return n;
          }
        });
      });
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    } finally {
      zt.current.delete(e);
    }
  }, [f, d, w, k, J, Y, K, j, Cr, I]);
  let Dr = Z.useCallback(async (e, t, n, r, i, a) => {
    if (!m && Ee.proxyMode !== `server-proxy` && !we) {
      k(`请先在设置中配置特惠视频大模型 API Key`);
      return;
    }
    let o = Hi(r || (T || `seedance_2_fast`).split(`
`)[0].trim());
    if (o && Ee.proxyMode !== `server-proxy`) {
      k(o);
      return;
    }
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              loading: true,
              progress: 0,
              errorMsg: undefined,
              errorMessage: undefined,
              videoUrl: undefined
            }
          };
        } else {
          return t;
        }
      });
    });
    q(t => {
      return t.map(t => {
        if (t.target === e) {
          return {
            ...t,
            animated: true
          };
        } else {
          return t;
        }
      });
    });
    try {
      k(`正在提交特惠视频生成任务...`);
      let o = Y();
      let s = J();
      let c = o.filter(t => {
        return t.target === e;
      });
      let l = s.find(t => {
        return t.id === e;
      });
      let u = l?.data.internalResources || [];
      let d = [];
      let f = [];
      let h = [];
      let g = [];
      u.forEach(e => {
        if (e.type.startsWith(`image`)) {
          d.push({
            url: e.url,
            type: `image`
          });
        } else if (e.type.startsWith(`video`)) {
          f.push({
            url: e.url,
            type: `video`
          });
        } else if (e.type.startsWith(`audio`)) {
          h.push({
            url: e.url,
            type: `audio`
          });
        }
      });
      c.forEach(e => {
        let t = s.find(t => {
          return t.id === e.source;
        });
        if (t) {
          let {
            images: n,
            videos: r,
            audios: i
          } = La(t, e.sourceHandle);
          d.push(...n.map(e => {
            return {
              url: e,
              type: `image`
            };
          }));
          f.push(...r.map(e => {
            return {
              url: e,
              type: `video`
            };
          }));
          h.push(...i.map(e => {
            return {
              url: e,
              type: `audio`
            };
          }));
          if (new Set([`textNode`, `audioNode`, `textConcatNode`, `scriptBoxNode`]).has(t.type)) {
            let n = Ia(t, e.sourceHandle, `video`);
            if (n) {
              g.push(n);
            }
          }
        }
      });
      let _ = l?.data.uploadedAssets || {};
      let v = e => {
        return typeof e == `string` && /^https?:\/\//i.test(e) && !e.startsWith(`data:`) && !e.startsWith(`blob:`) && !/:\d+\/files\//.test(e);
      };
      let y = async (e, t, n) => {
        let r = _[e];
        if (r && v(r)) {
          return r;
        } else if (v(e)) {
          return e;
        } else {
          return await wr(e, t, n);
        }
      };
      let b = [];
      try {
        for (let e = 0; e < d.length; e++) {
          let t = d[e];
          let n = await y(t.url, `image`, e);
          if (n && !n.startsWith(`data:`)) {
            b.push({
              url: n,
              type: `image`,
              name: t.name
            });
          }
        }
        for (let e = 0; e < f.length; e++) {
          let t = f[e];
          let n = await y(t.url, `video`, e);
          if (n && !n.startsWith(`data:`)) {
            b.push({
              url: n,
              type: `video`,
              name: t.name
            });
          }
        }
        for (let e = 0; e < h.length; e++) {
          let t = h[e];
          let n = await y(t.url, `audio`, e);
          if (n && !n.startsWith(`data:`)) {
            b.push({
              url: n,
              type: `audio`,
              name: t.name
            });
          }
        }
      } catch (e) {
        throw Error(`素材上传失败: ${e.message}`);
      }
      let x = (p || ``).replace(/[`\s]/g, ``).replace(/\/$/, ``);
      let S = `${x}/v1/gateway/generate`;
      let C = new AbortController();
      zt.current.set(e, C);
      let w = r || (T || `seedance_2_fast`).split(`
`)[0].trim();
      let E = g.length > 0 ? `${g.join(`
`)}\n${t}` : t;
      let O = parseInt(i || (D || `5`).split(`
`)[0].trim());
      let A = `${e}_${Date.now()}`;
      if (I) {
        H?.(true);
        I(t => {
          return [{
            id: A,
            nodeId: e,
            type: `discountVideo`,
            status: `pending`,
            progress: 0,
            createdAt: Date.now(),
            prompt: E,
            channelName: I_(p),
            modelName: w
          }, ...t];
        });
      }
      let M = {
        model: w,
        prompt: E,
        ratio: n || `16:9`,
        duration: O,
        resolution: a || `720p`,
        aspect_ratio: n || `16:9`,
        video_length: O,
        messages: [{
          role: `user`,
          content: []
        }]
      };
      for (let e of b) {
        if (e.type === `image`) {
          M.messages[0].content.push({
            type: `image_url`,
            image_url: {
              url: e.url
            }
          });
        }
      }
      M.messages[0].content.push({
        type: `text`,
        text: E
      });
      if (b.length > 0) {
        M.files = b;
      }
      if (I) {
        I(e => {
          return e.map(e => {
            if (e.id === A) {
              return {
                ...e,
                requestData: M
              };
            } else {
              return e;
            }
          });
        });
      }
      let N = await Bl(S, {
        method: `POST`,
        headers: {
          Authorization: `Bearer ${m}`,
          'Content-Type': `application/json`,
          Accept: `*/*`
        },
        body: JSON.stringify(M),
        ...Ee
      });
      if (!N.ok) {
        let e = {};
        let t = ``;
        try {
          t = await N.text();
          try {
            e = JSON.parse(t);
          } catch {
            e = {
              detail: t
            };
          }
        } catch {}
        if (I) {
          I(n => {
            return n.map(n => {
              if (n.id === A) {
                return {
                  ...n,
                  responseData: e ?? t
                };
              } else {
                return n;
              }
            });
          });
        }
        let n = e.detail || e.error?.message || e.message || e.error || t || `请求失败: ${N.status}`;
        throw Error(typeof n == `string` ? n : `请求失败: ${N.status}`);
      }
      let P = await N.json();
      // [fix:task_id] 原误用共享 _shared.P（非本次提交响应），导致"未返回任务 ID"；改用刚解析的局部 P。
      // （docs/01 变更#2。官方重打须按"特惠视频提交后解析响应取 task_id"语义定位。）
      let F = P.task_id || P.id;
      if (!F) {
        throw Error(`未返回任务 ID`);
      }
      if (I) {
        I(e => {
          return e.map(e => {
            if (e.id === A) {
              return {
                ...e,
                taskId: F,
                responseData: _shared.P
              };
            } else {
              return e;
            }
          });
        });
      }
      k(`任务提交成功，正在生成...`);
      K(t => {
        return t.map(t => {
          if (t.id === e) {
            return {
              ...t,
              data: {
                ...t.data,
                progress: 10
              }
            };
          } else {
            return t;
          }
        });
      });
      let ee = 0;
      let L = Math.ceil((re || 600) / (B || 3));
      let R = async () => {
        if (ee >= L) {
          throw Error(`生成超时，请稍后再试`);
        }
        let t = (jn.current || []).find(e => {
          return e.id === F || e.taskId === F;
        });
        if (t) {
          let n = t.status === `success` || t.status === `succeeded` || t.status === `completed`;
          let r = t.status === `failed`;
          if (n && t.resultUrl) {
            K(n => {
              return n.map(n => {
                if (n.id === e) {
                  return {
                    ...n,
                    data: {
                      ...n.data,
                      loading: false,
                      progress: 100,
                      videoUrl: An(t.resultUrl),
                      thumbnailUrl: An(t.thumbnailUrl),
                      imageAvailable: !!t.thumbnailUrl,
                      errorMsg: undefined,
                      errorMessage: undefined
                    }
                  };
                } else {
                  return n;
                }
              });
            });
            q(t => {
              return t.map(t => {
                if (t.target === e) {
                  return {
                    ...t,
                    animated: false
                  };
                } else {
                  return t;
                }
              });
            });
            k(`视频生成完成`);
            if (typeof chrome < `u` && chrome.runtime && chrome.runtime.id) {
              j?.(t.resultUrl, `video`, `generated`);
            }
            zt.current.delete(e);
            return;
          }
          if (r) {
            throw Error(t.errorMsg || `任务执行失败`);
          }
        }
        await new Promise(e => {
          return setTimeout(e, (B || 3) * 1000);
        });
        if (C.signal.aborted) {
          throw Error(`已取消`);
        }
        let n = await Bl(`${x}/v1/gateway/task/${F}`, {
          headers: {
            Authorization: `Bearer ${m}`,
            Accept: `*/*`
          },
          signal: C.signal,
          ...Ee
        });
        if (!n.ok) {
          let t = (jn.current || []).find(e => {
            return e.id === F || e.taskId === F;
          });
          if (t && (t.status === `success` || t.status === `succeeded` || t.status === `completed`) && t.resultUrl) {
            K(n => {
              return n.map(n => {
                if (n.id === e) {
                  return {
                    ...n,
                    data: {
                      ...n.data,
                      hasChanged: true,
                      loading: false,
                      progress: 100,
                      videoUrl: An(t.resultUrl),
                      thumbnailUrl: An(t.thumbnailUrl),
                      imageAvailable: !!t.thumbnailUrl,
                      errorMsg: undefined,
                      errorMessage: undefined
                    }
                  };
                } else {
                  return n;
                }
              });
            });
            q(t => {
              return t.map(t => {
                if (t.target === e) {
                  return {
                    ...t,
                    animated: false
                  };
                } else {
                  return t;
                }
              });
            });
            k(`视频生成完成`);
            if (typeof chrome < `u` && chrome.runtime && chrome.runtime.id) {
              j?.(t.resultUrl, `video`, `generated`);
            }
            zt.current.delete(e);
            return;
          }
          if (n.status === 404 && ee < L) {
            ee++;
            return R();
          }
          let r = await n.json().catch(() => {
            return {};
          });
          throw Error(r.detail || r.error?.message || r.message || r.error || `查询失败: ${n.status}`);
        }
        let r = await n.json();
        let i = r.status;
        if (i === `success` || i === `succeeded` || i === `completed`) {
          // [fix:特惠视频取链接] apimart-gateway 的视频链接映射到顶层 video_url（main.py task_view
          // 把 Lovart result.videos[0].url[0] 显式映射出 video_url）。此前只取 r.result?.url / r.resultUrl
          // 均 undefined → 误报「未找到视频链接」。补 r.video_url 与 result.videos[0].url[0] 兜底。
          let t = r.video_url || r.result?.url || r.resultUrl || r.result?.videos?.[0]?.url?.[0];
          if (!t && r.responseData?.debug_extracted_url) {
            t = r.responseData.debug_extracted_url;
          }
          if (!t) {
            throw Error(`视频生成成功，但未找到视频链接 (前端获取空值，接口返回数据: ${JSON.stringify(r)})`);
          }
          if (I) {
            I(e => {
              return e.map(e => {
                if (e.id === F || e.taskId === F) {
                  return {
                    ...e,
                    status: `completed`,
                    progress: 100,
                    resultUrl: t
                  };
                } else {
                  return e;
                }
              });
            });
          }
          K(n => {
            return n.map(n => {
              if (n.id === e) {
                return {
                  ...n,
                  data: {
                    ...n.data,
                    hasChanged: true,
                    loading: false,
                    progress: 100,
                    videoUrl: t,
                    errorMsg: undefined
                  }
                };
              } else {
                return n;
              }
            });
          });
          q(t => {
            return t.map(t => {
              if (t.target === e) {
                return {
                  ...t,
                  animated: false
                };
              } else {
                return t;
              }
            });
          });
          k(`视频生成完成`);
          if (typeof chrome < `u` && chrome.runtime && chrome.runtime.id) {
            j?.(t, `video`, `generated`);
          }
          zt.current.delete(e);
          return;
        } else if (i === `pending` || i === `running`) {
          if (I) {
            let e = (jn.current || []).find(e => {
              return e.id === F;
            });
            if (e?.status != i || e?.progress != 50 || e?.errorMsg) {
              I(e => {
                return e.map(e => {
                  if (e.id === F || e.taskId === F) {
                    return {
                      ...e,
                      status: `running`,
                      progress: 50,
                      errorMsg: undefined
                    };
                  } else {
                    return e;
                  }
                });
              });
            }
          }
          let t = De.find(t => {
            return t.id === e;
          });
          if (t?.data?.progress != 50) {
            console.log(`setNodes`, 50, t);
            K(t => {
              return t.map(t => {
                if (t.id === e) {
                  return {
                    ...t,
                    data: {
                      ...t.data,
                      hasChanged: true,
                      progress: 50
                    }
                  };
                } else {
                  return t;
                }
              });
            });
          }
          ee++;
          return R();
        } else if (i === `failed`) {
          throw Error(r.error || r.message || `任务执行失败`);
        } else {
          ee++;
          return R();
        }
      };
      await R();
    } catch (t) {
      if (t.name === `AbortError` || t.message === `已取消`) {
        console.log(`Video generation aborted`);
        return;
      }
      console.error(t);
      if (I && t.message !== `已取消`) {
        I(n => {
          return n.map(n => {
            if (n.nodeId === e && (n.status === `running` || n.status === `pending`)) {
              return {
                ...n,
                status: `failed`,
                errorMsg: t.message
              };
            } else {
              return n;
            }
          });
        });
      }
      k(t.message || `生成视频时出错，请检查网络或 API 配置`);
      K(n => {
        return n.map(n => {
          if (n.id === e) {
            return {
              ...n,
              data: {
                ...n.data,
                loading: false,
                errorMsg: t.message || `生成失败`,
                errorMessage: t.message || `生成失败`
              }
            };
          } else {
            return n;
          }
        });
      });
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    } finally {
      zt.current.delete(e);
    }
  }, [m, p, T, k, J, Y, K, I]);
  let kr = Z.useCallback(async (e, t, n = `16:9`, r = `1280x720`, i, a, o = `auto`) => {
    let d = (i || C.split(`
`)[0] || ``).trim();
    let f = ca(d);
    let p = !!Qi().image.length || !!Qi().text.length || !!Qi().video.length;
    if (f && d && Ee.proxyMode !== `server-proxy` && p) {
      let t = Hi(d);
      if (t) {
        k(t);
        K(t => {
          return t.map(t => {
            if (t.id === e) {
              return {
                ...t,
                data: {
                  ...t.data,
                  loading: false
                }
              };
            } else {
              return t;
            }
          });
        });
        return;
      }
    }
    let m = f ? l : s;
    let h = f ? u : c;
    if (!h && !f && !we) {
      k(`请先在设置中配置视频大模型 API Key`);
      return;
    }
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              loading: true,
              progress: 0,
              errorMsg: undefined
            }
          };
        } else {
          return t;
        }
      });
    });
    q(t => {
      return t.map(t => {
        if (t.target === e) {
          return {
            ...t,
            animated: true
          };
        } else {
          return t;
        }
      });
    });
    try {
      k(`正在提交视频生成任务...`);
      let s = Y();
      let c = J();
      let l = s.filter(t => {
        return t.target === e;
      });
      let u = [];
      let d = [];
      let f = [];
      l.forEach(e => {
        let t = c.find(t => {
          return t.id === e.source;
        });
        if (t && (t.type === `imageNode` || t.type === `promptNode` || t.type === `gridMergeNode`) && t.data.imageUrl) {
          let e = t.data.imageUrl;
          if (e.startsWith(`data:image/`) || /\.(jpg|jpeg|png|webp|gif)($|\?)/i.test(e)) {
            u.push(e);
          } else if (e.startsWith(`data:video/`) || /\.(mp4|webm|mov|ogg)($|\?)/i.test(e)) {
            f.push(e);
          }
        } else if (t && (t.type === `videoNode` || t.type === `videoExtractNode`) && t.data.videoUrl) {
          let e = t.data.videoUrl;
          f.push(e);
        } else if (t && t.type === `videoExtractNode` && t.data.allExtractedImages) {
          if (e.sourceHandle && e.sourceHandle.startsWith(`frame-`)) {
            let n = parseInt(e.sourceHandle.replace(`frame-`, ``), 10);
            if (!(t.data.hiddenIndices || []).includes(n)) {
              let e = t.data.allExtractedImages;
              if (e && e[n]) {
                u.push(e[n]);
              }
            }
          } else {
            t.data.extractedImages.forEach(e => {
              u.push(e);
            });
          }
        }
        if (t && t.type === `customNode` && t.data.resultData) {
          let e = t.data.resultData;
          if (t.data.config?.outputType === `image`) {
            if (Array.isArray(e)) {
              e.forEach(e => {
                if (typeof e == `string`) {
                  u.push(e);
                }
              });
            } else if (typeof e == `string`) {
              u.push(e);
            }
          } else if (t.data.config?.outputType === `text`) {
            d.push(typeof e == `object` ? JSON.stringify(e, null, 2) : String(e));
          }
        } else if (t && t.type === `textNode` && t.data.text) {
          d.push(t.data.text);
        } else if (t && t.type === `imageBoxNode` && Array.isArray(t.data.images)) {
          let e = t.data.images;
          let n = t.data.selectedIds || [];
          if (n.length > 0) {
            let t = new Set(n);
            e.forEach(e => {
              if (e?.url && t.has(e.id)) {
                u.push(e.url);
              }
            });
          } else {
            let n = e[typeof t.data.activeIndex == `number` ? t.data.activeIndex : 0]?.url;
            if (n) {
              u.push(n);
            }
          }
        }
      });
      let p = c.find(t => {
        return t.id === e;
      });
      if (p && p.data.selectedContextResources) {
        p.data.selectedContextResources.forEach(e => {
          if (e.type === `image` || e.type.startsWith(`image/`)) {
            u.push(e.url);
          }
        });
      }
      let g = m.replace(/[`'"]/g, ``).trim().replace(/\/$/, ``);
      let _ = `${g}/v1/videos`;
      let v = new AbortController();
      zt.current.set(e, v);
      let y = i || (C || `grok-video-4.2`).split(`
`)[0].trim();
      let b = d.length > 0 ? `${d.join(`
`)}\n${t}` : t;
      let x = a || (D || `10`).split(`
`)[0].trim();
      let S = o === `auto` ? [`json`, `formdata`] : [o];
      let w = null;
      let T = ``;
      let E = `${e}_${Date.now()}`;
      if (I) {
        H?.(true);
        I(n => {
          return [{
            id: E,
            nodeId: e,
            type: `video`,
            status: `pending`,
            progress: 0,
            createdAt: Date.now(),
            prompt: t,
            channelName: I_(m),
            modelName: y,
            apiUrl: m,
            apiKey: h
          }, ...n];
        });
      }
      for (let e of S) {
        let t;
        let i = {
          Authorization: `Bearer ${h}`
        };
        if (e === `json`) {
          i[`Content-Type`] = `application/json`;
          t = JSON.stringify({
            model: y,
            prompt: b,
            size: r,
            aspect_ratio: n,
            seconds: x,
            input_reference: u.length > 0 ? u : undefined,
            input_video: f.length > 0 ? f : undefined
          });
        } else {
          let e = new FormData();
          e.append(`model`, y);
          e.append(`prompt`, b);
          e.append(`size`, r);
          e.append(`aspect_ratio`, n);
          e.append(`seconds`, x);
          if (u.length > 0) {
            for (let t = 0; t < u.length; t++) {
              let n = u[t];
              try {
                let r = n.split(`,`)[1];
                let i = n.split(`,`)[0].split(`:`)[1].split(`;`)[0];
                let a = atob(r);
                let o = new ArrayBuffer(a.length);
                let s = new Uint8Array(o);
                for (let e = 0; e < a.length; e++) {
                  s[e] = a.charCodeAt(e);
                }
                let c = new Blob([o], {
                  type: i
                });
                let l = `png`;
                if (i === `image/jpeg`) {
                  l = `jpg`;
                } else if (i === `image/webp`) {
                  l = `webp`;
                }
                e.append(`input_reference`, c, `reference_${t + 1}.${l}`);
              } catch (r) {
                console.error(`Error processing reference image:`, r);
                let i = await (await fetch(n)).blob();
                e.append(`input_reference`, i, `reference_${t + 1}.png`);
              }
            }
          }
          if (f.length > 0) {
            for (let t = 0; t < f.length; t++) {
              let n = f[t];
              try {
                let r = n.split(`,`)[1];
                let i = n.split(`,`)[0].split(`:`)[1].split(`;`)[0];
                let a = atob(r);
                let o = new ArrayBuffer(a.length);
                let s = new Uint8Array(o);
                for (let e = 0; e < a.length; e++) {
                  s[e] = a.charCodeAt(e);
                }
                let c = new Blob([o], {
                  type: i
                });
                let l = `mp4`;
                if (i === `video/webm`) {
                  l = `webm`;
                }
                e.append(`input_video`, c, `reference_video_${t + 1}.${l}`);
              } catch (r) {
                console.error(`Error processing reference video:`, r);
                let i = await (await fetch(n)).blob();
                e.append(`input_video`, i, `reference_video_${t + 1}.mp4`);
              }
            }
          }
          t = e;
        }
        w = await Bl(_, {
          method: `POST`,
          headers: i,
          body: t,
          ...Ee
        });
        if (w.ok) {
          break;
        }
        try {
          let e = await w.json();
          if (e.error && e.error.message) {
            T = `API 请求失败: ${e.error.message}`;
          } else {
            if (e.message) {
              T = `API 请求失败: ${e.message}`;
            } else {
              T = `API 请求失败: ${w.status} - ${JSON.stringify(e)}`;
            }
          }
        } catch {
          try {
            T = `API 请求失败: ${w.status} - ${await w.text()}`;
          } catch {
            T = `API 请求失败: ${w.status} ${w.statusText}`;
          }
        }
      }
      if (!w || !w.ok) {
        throw Error(T);
      }
      let O = await w.json();
      if (O.error) {
        throw Error(O.error.message || `提交任务失败`);
      }
      let A = O.id;
      if (I) {
        I(e => {
          return e.map(e => {
            if (e.id === E) {
              return {
                ...e,
                taskId: A
              };
            } else {
              return e;
            }
          });
        });
      }
      k(`任务提交成功，正在生成中...`);
      let M = false;
      let N = 0;
      let P = 0;
      let F = Math.ceil(re / B);
      while (!M && N < F) {
        if (v.signal.aborted) {
          throw Error(`生成已取消`);
        }
        await new Promise(e => {
          return setTimeout(e, B * 1000);
        });
        N++;
        if (N % 120 == 0 && N < F) {
          k(`视频生成仍在进行中，请耐心等待...`);
        }
        try {
          let t = await Bl(`${g}/v1/videos/${A}`, {
            headers: {
              Authorization: `Bearer ${h}`
            },
            ...Ee
          });
          if (t.ok) {
            let n = await t.json();
            _shared.P = 0;
            if (n.status === `completed`) {
              M = true;
              let t = e => {
                if (typeof e != `string`) {
                  return ``;
                }
                let t = e.replace(/[`\s]/g, ``);
                if (t.startsWith(`"`) && t.endsWith(`"`) || t.startsWith(`'`) && t.endsWith(`'`)) {
                  t = t.slice(1, -1);
                }
                return t;
              };
              let i = (...e) => {
                for (let n of e) {
                  let e = t(n);
                  if (e) {
                    return e;
                  }
                }
                return ``;
              };
              let a = n.metadata || {};
              let o = n.data || {};
              let s = n.output || {};
              let c = i(n.video_url, a.video_url, a.url, n.url, n.result_url, o.video_url, o.url, s.video_url, s.url, n.video, n.result?.video);
              let l = i(n.thumbnail_url, a.thumbnail_url, a.cover_url, a.poster_url, n.cover_url, n.poster_url, o.thumbnail_url, s.thumbnail_url);
              if (I) {
                I(e => {
                  return e.map(e => {
                    if (e.id === A || e.taskId === A) {
                      return {
                        ...e,
                        status: `completed`,
                        progress: 100,
                        resultUrl: c,
                        thumbnailUrl: l
                      };
                    } else {
                      return e;
                    }
                  });
                });
              }
              let u = 320;
              let d = 320;
              if (r) {
                let e = r.split(`x`);
                if (e.length === 2) {
                  let t = parseInt(e[0]);
                  let n = parseInt(e[1]);
                  if (!isNaN(t) && !isNaN(n) && n > 0) {
                    let e = t / n;
                    if (e > 1) {
                      u = Math.min(600, e * 400);
                      d = u / e;
                    } else if (e < 1) {
                      d = 400;
                      u = d * e;
                    } else {
                      d = 320;
                      u = d;
                    }
                  }
                }
              }
              K(t => {
                return t.map(t => {
                  if (t.id === e) {
                    return {
                      ...t,
                      style: {
                        ...t.style,
                        width: u,
                        height: d
                      },
                      data: {
                        ...t.data,
                        videoUrl: c,
                        thumbnailUrl: l,
                        imageAvailable: !!l,
                        loading: false,
                        progress: 100
                      }
                    };
                  } else {
                    return t;
                  }
                });
              });
              if (c && typeof chrome < `u` && chrome.runtime && chrome.runtime.id) {
                j?.(c, `video`, `generated`);
              }
              k(`视频生成成功！`);
              break;
            } else if (n.status === `failed` || n.status === `error`) {
              let e = `视频生成失败`;
              if (n.error) {
                if (typeof n.error == `string`) {
                  e = n.error;
                } else if (typeof n.error == `object` && n.error.message) {
                  e = n.error.message;
                }
              }
              if (I) {
                I(t => {
                  return t.map(t => {
                    if (t.id === A || t.taskId === A) {
                      return {
                        ...t,
                        status: `failed`,
                        errorMsg: e
                      };
                    } else {
                      return t;
                    }
                  });
                });
              }
              throw Error(`[TASK_FAILED]${e}`);
            } else {
              let t = n.progress ? parseInt(n.progress) : 0;
              if (I) {
                I(e => {
                  return e.map(e => {
                    if (e.id === A || e.taskId === A) {
                      return {
                        ...e,
                        status: `running`,
                        progress: t
                      };
                    } else {
                      return e;
                    }
                  });
                });
              }
              K(n => {
                return n.map(n => {
                  if (n.id === e) {
                    return {
                      ...n,
                      data: {
                        ...n.data,
                        progress: t
                      }
                    };
                  } else {
                    return n;
                  }
                });
              });
            }
          } else {
            throw Error(`Polling failed: ${t.status}`);
          }
        } catch (e) {
          console.warn(`Polling error:`, e);
          if (e.message && e.message.startsWith(`[TASK_FAILED]`)) {
            throw Error(e.message.replace(`[TASK_FAILED]`, ``));
          }
          _shared.P++;
          if (_shared.P >= 5) {
            throw Error(`查询任务状态失败次数过多 (${_shared.P}次)，已停止`);
          }
        }
      }
      if (!M) {
        throw Error(`生成超时 (1小时)，请重试`);
      }
    } catch (t) {
      console.error(t);
      if (t.name !== `AbortError`) {
        k(t.message || `生成失败，请检查网络或配置`);
        if (I) {
          I(n => {
            return n.map(n => {
              if (n.nodeId === e && n.status === `pending`) {
                return {
                  ...n,
                  status: `failed`,
                  errorMsg: t.message
                };
              } else {
                return n;
              }
            });
          });
        }
        K(n => {
          return n.map(n => {
            if (n.id === e) {
              return {
                ...n,
                data: {
                  ...n.data,
                  loading: false,
                  errorMsg: t.message
                }
              };
            } else {
              return n;
            }
          });
        });
      }
    } finally {
      zt.current.delete(e);
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    }
  }, [c, s, l, u, C, D, k, J, Y, K, j, N, I]);
  let Ar = Z.useCallback(async (e, n, i) => {
    if (!n.trim()) {
      k(`请先输入剧情`);
      return;
    }
    let a = i && i.trim() || x.split(`
`)[0].trim();
    let o = ca(a);
    if (o && a) {
      let e = Hi(a);
      if (e) {
        k(e);
        return;
      }
    }
    let s = o ? l : t;
    let c = o ? u : r;
    if (!c && !o) {
      k(`请先在设置中配置文本大模型 API Key`);
      return;
    }
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              loading: true,
              errorMsg: undefined
            }
          };
        } else {
          return t;
        }
      });
    });
    let d = J().find(t => {
      return t.id === e;
    });
    let f = (d?.data?.customScriptPrompt || ``).trim() || `你是顶级爆款短剧编剧 + 资深影视分镜师，集编剧、导演、制片人视角于一身，精通短剧/网剧/短视频的爆款公式。
你的创作哲学：节奏第一、情绪至上、悬念不断、前3秒定生死、强冲突×高密度爽点×持续悬念×极致情绪。

【创作流程（必须先想清楚再出分镜，禁止直接平铺直叙）】
1. 先在脑中规划一条清晰故事线：明确题材类型、主角的欲望与成长弧线、核心冲突、反派动机；
2. 用「事件→反应→反转→再反应（设局→入局→破局→新局）」组织剧情，保证冲突逐级升级；
3. 按时间结构铺排：开场即冲突锚定 → 情绪爆破 → 反转打脸 → 结尾留悬念钩子；
4. 再把这条故事线拆成连续、有因果递进的分镜，每个分镜承担明确的叙事功能，不要无效镜头、不要重复镜头、不要流水账；
5. 镜头语言要有变化：景别（大远景/全景/中景/近景/特写）与运镜（推/拉/摇/移/跟/升降）按情绪需要切换，关键情绪点用特写。

【输出格式】严格输出一个 JSON 对象（只返回纯 JSON，不要解释、不要 Markdown 代码块）：
{"projectName":"根据故事生成的简洁项目名称，2至8个中文字符，例如：小红帽","globalStyle":"整部片子的统一视觉风格，例如：中世纪童话·皮克斯3D","logline":"一句话故事核心（用于自检，可选）","shots":[{"index":1,"duration":"5s","description":"画面描述：聚焦这一镜要呈现的画面与动作，凡出现 assets 中的角色/场景/道具，必须写成 @名称 形式，例如 @小红帽 走进 @幽暗森林","shotType":"景别","lighting":"光影氛围","dialogue":"该镜对白或旁白（如有）","sound":"音效（如有）","motion":"运镜"}],"assets":[{"category":"character|scene|prop","name":"名称","description":"主体外观描述，详细具体（角色：体型/发型/五官/瞳色/肤色/服装/配饰/神态；场景：环境/前景背景/氛围/光线；道具：形状/材质/颜色/细节），只描述主体本身，不要写构图/视角/布光/负面词，这些由系统自动补全"}]}
【硬性要求】assets 的 name 必须与 shots 的 description 中 @ 引用的名称完全一致；分镜数量与时长要与剧情体量匹配，叙事连贯、有头有尾。`;
    let p = d?.data?.shotCount;
    let m = typeof p == `number` && p > 0 ? `\n【镜头数量要求】请严格生成约 ${p} 个分镜（shots 数组长度尽量接近 ${p}），按剧情节奏合理分配，不要为凑数硬加无意义镜头。` : `
【镜头数量要求】根据剧情体量自动决定合理的分镜数量。`;
    let h = (d?.data?.globalStyle || ``).trim();
    let g = h ? `\n【统一视觉风格（用户指定，最高优先级）】用户已明确指定整部片子的统一视觉风格为「${h}」。你必须在返回的 globalStyle 字段中原样输出「${h}」，不得自行更换、翻译或扩写风格名；同时所有分镜画面与资产外观都要严格贴合该风格。` : ``;
    let _ = f + m + g;
    try {
      let t = `${s.replace(/\/+$/, ``).replace(/\/(api|v1)$/i, ``).replace(/\/+$/, ``)}/v1/chat/completions`;
      let r = !a.toLowerCase().includes(`deepseek`) && !a.toLowerCase().includes(`claude`);
      let i = new AbortController();
      zt.current.set(e, i);
      let o = await Bl(t, {
        method: `POST`,
        headers: {
          Authorization: `Bearer ${c}`,
          'Content-Type': `application/json`
        },
        body: JSON.stringify({
          model: a,
          messages: [{
            role: `system`,
            content: _
          }, {
            role: `user`,
            content: n
          }],
          temperature: 0.7,
          stream: true,
          response_format: r ? {
            type: `json_object`
          } : undefined
        }),
        signal: i.signal,
        localPort: Ce.status.isConnected ? Ce.status.port : undefined
      });
      if (!o.ok) {
        let e = ``;
        try {
          e = await o.text();
        } catch {}
        throw o.status === 401 || o.status === 403 ? Error(`未登录或鉴权失败（请先登录/检查文本模型 API Key）`) : Error(`API Error: ${o.status} ${o.statusText}${e ? ` - ${e.slice(0, 160)}` : ``}`);
      }
      let l = ``;
      if (o.body) {
        let t = o.body.getReader();
        let n = new TextDecoder();
        let r = ``;
        let i = 0;
        while (true) {
          let {
            done: a,
            value: o
          } = await t.read();
          if (a) {
            break;
          }
          r += n.decode(o, {
            stream: true
          });
          let s = r.split(`
`);
          r = s.pop() || ``;
          for (let e of s) {
            let t = e.trim();
            if (!t || !t.startsWith(`data:`)) {
              continue;
            }
            let n = t.slice(5).trim();
            if (n !== `[DONE]`) {
              try {
                let e = JSON.parse(n);
                let t = e.choices?.[0]?.delta?.content || e.choices?.[0]?.message?.content || ``;
                if (t) {
                  l += t;
                }
              } catch {}
            }
          }
          let c = Date.now();
          if (c - i > 200) {
            i = c;
            let t = l.length;
            K(n => {
              return n.map(n => {
                if (n.id === e) {
                  return {
                    ...n,
                    data: {
                      ...n.data,
                      scriptProgressChars: t
                    }
                  };
                } else {
                  return n;
                }
              });
            });
          }
        }
      } else {
        l = (await o.json()).choices?.[0]?.message?.content || ``;
      }
      zt.current.delete(e);
      let u = l.replace(/```json/gi, ``).replace(/```/g, ``).trim();
      let f = u.indexOf(`{`);
      let p = u.lastIndexOf(`}`);
      if (f >= 0 && p > f) {
        u = u.slice(f, p + 1);
      }
      let m;
      try {
        m = JSON.parse(u);
      } catch {
        throw Error(`模型输出的 JSON 不完整或格式有误，请重试（可换更稳定的文本模型）`);
      }
      let g = Array.isArray(m) ? m : m.shots || [];
      let v = m.assets || [];
      let y = g.filter(e => {
        return e && typeof e == `object` && (e.description || e.prompt || e.videoPrompt);
      }).map((t, n) => {
        return {
          id: `${e}-shot-${n}`,
          index: typeof t.index == `number` ? t.index : n + 1,
          duration: t.duration || `5s`,
          description: t.description || ``,
          shotType: t.shotType || ``,
          lighting: t.lighting || ``,
          dialogue: t.dialogue || ``,
          sound: t.sound || ``,
          motion: t.motion || ``,
          prompt: t.prompt || ``,
          videoPrompt: t.videoPrompt || ``
        };
      });
      let b = h || (typeof m.globalStyle == `string` ? m.globalStyle : ``);
      let x = typeof m.projectName == `string` && m.projectName.trim() ? m.projectName.trim().slice(0, 20) : n.trim().replace(/[，。！？,.!?\s].*$/, ``).slice(0, 8) || `剧本项目`;
      let S = d?.data?.customAssetTemplates || undefined;
      let C = v.filter(e => {
        return e && e.name;
      }).map((t, n) => {
        let r = [`character`, `scene`, `prop`].includes(t.category) ? t.category : `character`;
        let i = t.description || ``;
        return {
          id: `${e}-asset-${n}`,
          category: r,
          name: t.name,
          description: i,
          prompt: Zg(r, i, b, S)
        };
      });
      K(t => {
        return t.map(t => {
          if (t.id === e) {
            return {
              ...t,
              data: {
                ...t.data,
                loading: false,
                story: n,
                label: x,
                projectName: x,
                shots: y,
                assets: C,
                globalStyle: b,
                errorMsg: undefined,
                scriptProgressChars: undefined
              }
            };
          } else {
            return t;
          }
        });
      });
      k(`已生成 ${y.length} 个分镜`);
    } catch (t) {
      zt.current.delete(e);
      let n = t?.name === `AbortError` || /abort/i.test(t?.message || ``);
      K(r => {
        return r.map(r => {
          if (r.id === e) {
            return {
              ...r,
              data: {
                ...r.data,
                loading: false,
                scriptProgressChars: undefined,
                errorMsg: n ? undefined : t?.message || `生成失败`
              }
            };
          } else {
            return r;
          }
        });
      });
      k(n ? `已停止生成` : `脚本生成失败`);
    }
  }, [r, t, l, u, x, k, K, J, Ce.status.isConnected, Ce.status.port]);
  let Mr = Z.useCallback(async (e, t) => {
    let n = t?.apiFormat || `auto`;
    let r = t?.aspectRatio || `Auto`;
    let o = t?.imageSize || `1K`;
    let s = (S ? S.split(`
`).map(e => {
      return e.trim();
    }).filter(Boolean) : [`gemini-3.1-flash-image-preview`])[0] || `gemini-3.1-flash-image-preview`;
    let c = t?.model && t.model.trim() ? t.model : s;
    let d = ca(c);
    if (d && c) {
      let e = Hi(c);
      if (e) {
        throw Error(e);
      }
    }
    let f = d ? l : i;
    let p = d ? u : a;
    if (!p && !d) {
      throw Error(`请先在设置中配置图像大模型 API Key`);
    }
    let m = c.toLowerCase();
    let h = m.includes(`banana`) || m.includes(`gemini`) || m.includes(`香蕉`) || m.includes(`芭蕉`);
    let g = n === `openai` || n === `auto` && !h;
    let _ = f.replace(/\/$/, ``);
    let v = {
      '1:1': {
        '1K': `1024x1024`,
        '2K': `2048x2048`,
        '4K': `2880x2880`
      },
      '16:9': {
        '1K': `1776x880`,
        '2K': `2048x1152`,
        '4K': `3840x2160`
      },
      '9:16': {
        '1K': `880x1776`,
        '2K': `1152x2048`,
        '4K': `2160x3840`
      },
      '4:3': {
        '1K': `1024x768`,
        '2K': `2304x1728`,
        '4K': `2880x2160`
      },
      '3:4': {
        '1K': `768x1024`,
        '2K': `1728x2304`,
        '4K': `2160x2880`
      }
    };
    let y = ``;
    let b = {};
    if (g) {
      let t = !r || r === `Auto` ? `auto` : v[r]?.[o] || v[r]?.[`1K`] || `1024x1024`;
      y = `${_}/v1/images/generations`;
      b = {
        model: c,
        prompt: e,
        n: 1,
        image_size: String(o).toUpperCase()
      };
      if (t !== `auto`) {
        b.size = t;
      }
    } else {
      let t = {};
      if (r && r !== `Auto`) {
        t.aspectRatio = r;
      }
      if (o) {
        t.imageSize = o;
      }
      let n = {
        contents: [{
          role: `user`,
          parts: [{
            text: e
          }]
        }],
        generationConfig: {
          responseModalities: [`IMAGE`]
        }
      };
      if (Object.keys(t).length > 0) {
        t.aspectRatio ||= `1:1`;
        n.generationConfig.imageConfig = t;
      }
      y = `${_}/v1beta/models/${c}:generateContent?key=${p}`;
      b = n;
    }
    let x = await Bl(y, {
      method: `POST`,
      headers: {
        Authorization: `Bearer ${p}`,
        'Content-Type': `application/json`
      },
      body: JSON.stringify(b),
      localPort: !t?.noProxy && Ce.status.isConnected ? Ce.status.port : undefined,
      signal: t?.signal
    });
    if (!x.ok) {
      throw x.status === 401 || x.status === 403 ? Error(`未登录或鉴权失败（请先登录/检查 API Key）`) : Error(`生图失败: ${x.status} ${x.statusText}`);
    }
    let T = ``;
    // 兼容两种响应：网关 wait 模式返回 SSE 流（text/event-stream），旧/非流式返回纯 JSON。
    // 参考 Sr(L2786) 的 SSE 解析：逐块读 data: 事件，succeeded 取 results[0].url。
    let contentType = (x.headers.get(`content-type`) || ``);
    if (contentType.includes(`text/event-stream`) && x.body) {
      let reader = x.body.getReader();
      let dec = new TextDecoder();
      let buf = ``;
      try {
        while (true) {
          let {
            done,
            value
          } = await reader.read();
          if (done) {
            break;
          }
          buf += dec.decode(value, {
            stream: true
          });
          let lines = buf.split(`
`);
          buf = lines.pop() || ``;
          for (let ln of lines) {
            let t = ln.trim();
            if (!t.startsWith(`data:`)) {
              continue;
            }
            let payload = t.slice(5).trim();
            if (!payload || payload === `[DONE]`) {
              continue;
            }
            try {
              let ev = JSON.parse(payload);
              if (ev.status === `succeeded` && ev.results?.[0]?.url) {
                T = ev.results[0].url;
              }
              if (ev.status === `failed` || ev.error) {
                throw Error(ev.error || ev.failure_reason || `生成失败`);
              }
            } catch (e) {
              if (e.message === `Unexpected token` || e instanceof SyntaxError) {
                continue;
              }
              throw e;
            }
          }
        }
      } finally {
        reader.releaseLock();
      }
      if (!T) {
        throw Error(`流式响应未返回最终图片URL`);
      }
    } else {
      let C = await x.text();
      let w;
      try {
        w = JSON.parse(C);
      } catch {
        throw Error(`生图响应不完整（可能被网络/代理截断），请重试`);
      }
      if (g) {
        let e = w.data?.[0];
        if (e?.b64_json) {
          T = `data:image/png;base64,${e.b64_json}`;
        } else {
          T = e?.url || ``;
        }
      } else {
        let e = w.candidates?.[0];
        let t = e?.content?.parts?.find(e => {
          return e.inlineData;
        });
        let n = e?.content?.parts?.find(e => {
          return e.text;
        });
        if (t?.inlineData) {
          T = `data:${t.inlineData.mimeType || `image/png`};base64,${t.inlineData.data}`;
        } else if (n?.text) {
          let e = n.text.match(/!\[.*?\]\((data:image\/[^;]+;base64,[^)]+)\)/);
          if (e?.[1]) {
            T = e[1];
          }
        }
      }
    }
    if (!T) {
      throw Error(`未生成图片，请检查提示词或模型设置`);
    }
    return T;
  }, [a, i, l, u, S, Ce.status.isConnected, Ce.status.port]);
  let Nr = Z.useCallback((e, t, n) => {
    let r = t.map(e => {
      return e.name;
    }).filter(Boolean);
    let i = r.length ? `本分镜涉及以下资源，请在画面里自然呈现，并在画面描述里用 @名称 引用：${r.map(e => {
      return `@${e}`;
    }).join(`、`)}` : `本分镜未引用具体资源，按画面描述生成即可，不要凭空加入无关角色/道具。`;
    return [`镜头编号：${e.index}`, `时长：${e.duration || `5s`}`, e.shotType ? `景别：${e.shotType}` : ``, e.lighting ? `光影：${e.lighting}` : ``, e.motion ? `运镜：${e.motion}` : ``, e.description ? `画面描述：${e.description}` : ``, e.dialogue ? `【必须原样带入 videoPrompt 的对白/旁白】\n${String(e.dialogue).split(`
`).map(e => {
      let t = e.trim();
      let n = t.match(/^\[([^|\]]*)\|([^\]]*)\]\s?(.*)$/);
      if (n) {
        return `${n[1] === `旁白` ? `旁白` : `说话者：${n[2] || `未指定角色`}`}，完整原句：${n[3]}`;
      }
      let r = t.match(/^([^：:]+)\s*[：:]\s*(.*)$/);
      if (r) {
        return `说话者：${r[1].trim()}，完整原句：${r[2]}`;
      } else {
        return `完整原句：${t}`;
      }
    }).join(`
`)}` : ``, e.sound ? `【必须带入 videoPrompt 的音效】环境音/动作音：${e.sound}` : ``, n ? `统一风格：${n}` : ``, i, `prompt 只描述可见画面（主体/动作/场景/景别/光影/色彩/视角），不要包含对白、旁白、音效、字幕等文字；videoPrompt 在画面基础上补充镜头运动与动态，并把本镜头的对白/旁白台词与音效自然融入（如“角色说：…”“环境音：…”），使其可直接用于视频生成。只返回 JSON。`].filter(Boolean).join(`
`);
  }, []);
  let Pr = Z.useCallback(async (e, t, n) => {
    let r = J().find(t => {
      return t.id === e;
    });
    if (!r) {
      return;
    }
    let i = (r.data.assets || []).find(e => {
      return e.id === t;
    });
    if (!i) {
      return;
    }
    let a = r.data.assetModelSettings || {};
    let o = a.globalModel || undefined;
    let s = o ? ka(o) : null;
    if (s) {
      let e = xa().find(e => {
        return e.id === s && e.enabled;
      });
      o = (e ? Aa(e) : [])[0] || undefined;
    }
    let c = a.globalAspectRatio || `16:9`;
    let l = a.globalSize || `2K`;
    K(n => {
      return n.map(n => {
        if (n.id === e) {
          return {
            ...n,
            data: {
              ...n.data,
              assets: (n.data.assets || []).map(e => {
                if (e.id === t) {
                  return {
                    ...e,
                    loading: true
                  };
                } else {
                  return e;
                }
              })
            }
          };
        } else {
          return n;
        }
      });
    });
    let u = `${e}_asset_${t}`;
    let d = new AbortController();
    zt.current.set(u, d);
    try {
      let a = r?.data?.globalStyle || ``;
      let s = [i.name, i.description].filter(Boolean).join(`，`);
      let f = await Mr(i.prompt && i.prompt.trim() ? Yg(i.prompt, a) : Zg(i.category === `scene` || i.category === `prop` ? i.category : `character`, s, a, r?.data?.customAssetTemplates || undefined), {
        model: o,
        aspectRatio: c,
        imageSize: l,
        noProxy: n,
        signal: d.signal
      });
      zt.current.delete(u);
      let p;
      let m = `migrated/${i.category === `character` ? `人物` : i.category === `scene` ? `场景` : `道具`}`;
      try {
        let e = null;
        if (/^https?:\/\//i.test(f) && !f.includes(`/files/`)) {
          e = await ci(f, {
            subfolder: m
          });
          if (e && e.url) {
            let t = await mi(e.url, {
              maxDim: 480,
              quality: 75
            });
            e.thumbnailUrl = t || undefined;
          }
        } else {
          e = await hi(f, {
            subfolder: m,
            preferThumbnail: true,
            thumbMaxDim: 480,
            thumbQuality: 75
          });
        }
        if (e?.url) {
          f = e.url;
          p = e.thumbnailUrl;
        }
      } catch (e) {
        console.warn(`[handleAssetImageGenerate] 本地化图片失败，回退原始地址:`, e);
      }
      let h = [];
      K(n => {
        return n.map(n => {
          if (n.id === e) {
            h = (n.data.assets || []).map(e => {
              if (e.id === t) {
                return {
                  ...e,
                  loading: false,
                  imageUrl: f,
                  thumbnailUrl: p
                };
              } else {
                return e;
              }
            });
            return {
              ...n,
              data: {
                ...n.data,
                assets: h
              }
            };
          } else {
            return n;
          }
        });
      });
    } catch (n) {
      zt.current.delete(u);
      K(n => {
        return n.map(n => {
          if (n.id === e) {
            let e = n.data.assets || [];
            return {
              ...n,
              data: {
                ...n.data,
                assets: e.map(e => {
                  if (e.id === t) {
                    return {
                      ...e,
                      loading: false
                    };
                  } else {
                    return e;
                  }
                })
              }
            };
          }
          return n;
        });
      });
      if (n?.name !== `AbortError` && !/abort/i.test(n?.message || ``)) {
        k(n?.message || `资产参考图生成失败`);
      }
    }
  }, [J, K, Mr, k]);
  let Fr = Z.useCallback(async (e, t) => {
    let n = J().find(t => {
      return t.id === e;
    })?.data?.assets || [];
    let r = t && t.length > 0 ? n.filter(e => {
      return t.includes(e.id);
    }) : n.filter(e => {
      return !e.imageUrl;
    });
    if (r.length === 0) {
      k(n.length === 0 ? `暂无资产可生成，请先在第1步生成脚本` : `没有需要生成的项（可勾选指定资产）`);
      return;
    }
    k(`开始批量生成 ${r.length} 张参考图…`);
    await Promise.all(r.map(t => {
      return Pr(e, t.id);
    }));
  }, [J, Pr, k]);
  let Ir = Z.useCallback(async (e, n, i) => {
    let a = J().find(t => {
      return t.id === e;
    });
    if (!a) {
      return;
    }
    let o = a.data?.shots || [];
    let s = a.data?.assets || [];
    let c = a.data?.globalStyle || ``;
    let d = [...(a.data?.globalConstraints || []).filter(Boolean), String(a.data?.customGlobalConstraint || ``).trim()].filter(Boolean).join(`；`);
    let f = String(a.data?.imageGlobalConstraint || ``).trim();
    let p = String(a.data?.videoGlobalConstraint ?? d).trim();
    let m = n ? o.filter(e => {
      return e.id === n;
    }) : i && i.length > 0 ? o.filter(e => {
      return i.includes(e.id);
    }) : o;
    if (m.length === 0) {
      k(`没有可生成的分镜`);
      return;
    }
    let h = (a.data?.selectedModel || x.split(`
`)[0] || ``).trim();
    let g = ca(h);
    if (g && h) {
      let e = Hi(h);
      if (e) {
        k(e);
        return;
      }
    }
    let _ = g ? l : t;
    let v = g ? u : r;
    if (!v && !g) {
      k(`请先在设置中配置文本大模型 API Key`);
      return;
    }
    let y = `${_.replace(/\/+$/, ``).replace(/\/(api|v1)$/i, ``).replace(/\/+$/, ``)}/v1/chat/completions`;
    let b = ((a.data?.customShotPrompt || ``).trim() || `你是资深电影导演、分镜设计师、AI绘画与AI视频提示词工程师。根据给定的单个分镜资料，输出一个严格 JSON 对象：
{"prompt":"用于生成静态画面的详细图像提示词","videoPrompt":"用于生成该镜头视频的详细提示词"}

【总体要求】
1. prompt 与 videoPrompt 必须围绕同一个镜头，主体身份、服装、道具、场景、时间、光线和空间关系完全一致。
2. 两个字段都要信息充足、语言连贯、可直接交给生成模型使用；每个字段建议 450 至 700 个中文字符，最低不得少于 400 个中文字符，禁止用空洞形容词凑字数。
3. 画面资料中的 @名称 是资产绑定标记，必须逐字原样保留，不能删减、改写、合并或替换成“角色”“人物”“主体”等泛称。

【prompt（生图）要求】
只写单帧中能够被摄像机看见的内容。按主体身份与外观、精确动作和姿态、角色间距离与视线关系、前中后景环境、关键道具位置、景别与构图、镜头焦段和视角、光源方向与明暗层次、色彩关系、材质纹理、空气透视、电影美术风格的顺序组织。明确每个 @资产 在画面中的位置、朝向、遮挡和互动。不得写对白、旁白、声音、音效、配乐、字幕、心理活动、画外信息或生成操作说明。

【videoPrompt（生视频）要求】
以 prompt 的画面状态为起点，详细描述镜头在指定时长内如何连续发展：起始画面、运镜方向和速度、焦点迁移、主体逐步动作、表情与视线变化、衣物毛发和环境物体的次级运动、光影与粒子变化、动作节奏、结束画面和停顿方式。避免突然跳切、瞬移、身份漂移和无因果动作。
必须加入本镜头提供的对白/旁白和音效，而且必须保留具体说话者姓名与完整原句。格式写成“角色名说：‘完整台词’”，旁白写成“旁白：‘完整原句’”，音效写成“环境音/动作音：具体声音内容”。严禁把具体姓名泛化为“角色说”“人物说”“他说/她说”，严禁遗漏、缩写或擅自改写台词。若资料明确没有对白或音效，才可不写。

只返回可解析的纯 JSON，不要解释，不要 Markdown，不要在 JSON 前后添加任何文字。`) + Qg;
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              shots: (t.data.shots || []).map(e => {
                if (m.find(t => {
                  return t.id === e.id;
                })) {
                  return {
                    ...e,
                    promptLoading: true
                  };
                } else {
                  return e;
                }
              })
            }
          };
        } else {
          return t;
        }
      });
    });
    await Promise.all(m.map(async t => {
      try {
        let n = `${t.description || ``} ${t.dialogue || ``} ${t.prompt || ``} ${t.videoPrompt || ``}`;
        let r = s.filter(e => {
          return e?.name && Fa(n, e.name);
        });
        let i = Math.max(1, Number.parseInt(String(t.duration || `5`), 10) || 5);
        let a = `${Nr(t, r, c)}${f ? `\n【生图强制约束，仅作用于 prompt】\n${f}` : ``}${p ? `\n【生视频强制约束，仅作用于 videoPrompt】\n${p}` : ``}\n【videoPrompt 格式硬性要求】videoPrompt 必须以“【时长 ${i}秒】”单独一行开头，随后换行书写视频内容。`;
        let o = !h.toLowerCase().includes(`deepseek`) && !h.toLowerCase().includes(`claude`);
        let l = `${e}_shot_${t.id}`;
        let u = new AbortController();
        zt.current.set(l, u);
        let d = await Bl(y, {
          method: `POST`,
          headers: {
            Authorization: `Bearer ${v}`,
            'Content-Type': `application/json`
          },
          body: JSON.stringify({
            model: h,
            messages: [{
              role: `system`,
              content: b
            }, {
              role: `user`,
              content: a
            }],
            temperature: 0.7,
            response_format: o ? {
              type: `json_object`
            } : undefined
          }),
          localPort: Ce.status.isConnected ? Ce.status.port : undefined,
          signal: u.signal
        });
        if (!d.ok) {
          zt.current.delete(l);
          let e = ``;
          try {
            e = await d.text();
          } catch {}
          throw d.status === 401 || d.status === 403 ? Error(`未登录或鉴权失败（请先登录/检查文本模型 API Key）`) : Error(`API Error: ${d.status}${e ? ` - ${e.slice(0, 120)}` : ``}`);
        }
        let m = await d.json();
        zt.current.delete(l);
        let g = m.choices?.[0]?.message?.content || ``;
        g = g.replace(/```json/gi, ``).replace(/```/g, ``).trim();
        let _ = g.indexOf(`{`);
        let x = g.lastIndexOf(`}`);
        if (_ >= 0 && x > _) {
          g = g.slice(_, x + 1);
        }
        let S = JSON.parse(g);
        let C = S.prompt || ``;
        let w = S.videoPrompt || ``;
        K(n => {
          return n.map(n => {
            if (n.id === e) {
              return {
                ...n,
                data: {
                  ...n.data,
                  shots: (n.data.shots || []).map(e => {
                    if (e.id === t.id) {
                      return {
                        ...e,
                        prompt: C,
                        videoPrompt: w,
                        promptLoading: false
                      };
                    } else {
                      return e;
                    }
                  })
                }
              };
            } else {
              return n;
            }
          });
        });
      } catch (n) {
        zt.current.delete(`${e}_shot_${t.id}`);
        K(n => {
          return n.map(n => {
            if (n.id === e) {
              return {
                ...n,
                data: {
                  ...n.data,
                  shots: (n.data.shots || []).map(e => {
                    if (e.id === t.id) {
                      return {
                        ...e,
                        promptLoading: false
                      };
                    } else {
                      return e;
                    }
                  })
                }
              };
            } else {
              return n;
            }
          });
        });
        if (n?.name !== `AbortError` && !/abort/i.test(n?.message || ``)) {
          k(n?.message || `分镜提示词生成失败`);
        }
      }
    }));
    k(n ? `已生成该分镜提示词` : `已生成 ${m.length} 个分镜提示词`);
  }, [r, t, l, u, x, J, K, k, Nr, Ce.status.isConnected, Ce.status.port]);
  Z.useEffect(() => {
    Jn.current = {
      onGenerateScript: Ar,
      onGenerateAssetImage: Pr,
      onGenerateAllAssetImages: Fr,
      onRetryVideoAssetUpload: oi,
      onUploadAllVideoAssets: ai,
      onGenerateShotPrompts: Ir,
      onStopScriptItem: Un
    };
    K(e => {
      return e.map(e => {
        if (e.type === `scriptBoxNode`) {
          return {
            ...e,
            style: {
              ...e.style,
              width: Math.max(Number(e.style?.width) || 0, 860)
            },
            data: {
              ...e.data,
              textModel: x,
              drawingModelForScript: S,
              onGenerateScript: Ar,
              onGenerateAssetImage: Pr,
              onGenerateAllAssetImages: Fr,
              onRetryVideoAssetUpload: oi,
              onUploadAllVideoAssets: ai,
              onGenerateShotPrompts: Ir,
              onStopScriptItem: Un
            }
          };
        } else {
          return e;
        }
      });
    });
  }, [Ar, Pr, Fr, Ir, Un, K, x, S]);
  let Lr = Z.useCallback(async (e, n, i = false, a) => {
    let o = (a || x.split(`
`)[0] || ``).trim();
    let s = ca(o);
    let c = !!Qi().image.length || !!Qi().text.length || !!Qi().video.length;
    if (s && o && Ee.proxyMode !== `server-proxy` && c) {
      let t = Hi(o);
      if (t) {
        k(t);
        K(t => {
          return t.map(t => {
            if (t.id === e) {
              return {
                ...t,
                data: {
                  ...t.data,
                  loading: false
                }
              };
            } else {
              return t;
            }
          });
        });
        return;
      }
    }
    let d = s ? l : t;
    let f = s ? u : r;
    if (!f && !s && !we) {
      k(`请先在设置中配置文本大模型 API Key`);
      return;
    }
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              loading: true,
              errorMsg: undefined
            }
          };
        } else {
          return t;
        }
      });
    });
    q(t => {
      return t.map(t => {
        if (t.target === e) {
          return {
            ...t,
            animated: true
          };
        } else {
          return t;
        }
      });
    });
    try {
      let t = Y();
      let r = J();
      let o = t.filter(t => {
        return t.target === e;
      });
      let c = [];
      let l = [];
      let u = [];
      o.forEach(e => {
        let t = r.find(t => {
          return t.id === e.source;
        });
        if (t) {
          let {
            images: n,
            videos: r
          } = La(t, e.sourceHandle);
          c.push(...n);
          u.push(...r);
          let i = Ia(t, e.sourceHandle);
          if (i) {
            l.push(i);
          }
        }
      });
      let p = r.find(t => {
        return t.id === e;
      });
      if (p && p.data.selectedContextResources) {
        p.data.selectedContextResources.forEach(e => {
          if (e.type.startsWith(`image`)) {
            c.push(e.url);
          } else if (e.type === `text`) {
            l.push(e.url);
          }
        });
      }
      let m = (l.length > 0 ? `${l.join(`
`)}\n${n}` : n).trim();
      let h = (a || x.split(`
`)[0]).trim().toLowerCase();
      let g = h.includes(`deepseek`) || h.includes(`o1-mini`) || h.includes(`o3-mini`);
      let _ = [];
      if (i) {
        _.push({
          role: `system`,
          content: `你是一个智能内容拆分助手。用户会输入一段文本或要求，你必须将内容拆分成多个独立的部分。你必须返回一个严格的JSON对象，包含一个 'items' 数组。数组中的每个对象必须包含 'title' (最多8个字符) 和 'content' (详细内容) 字段。示例：{"items": [{"title": "场景一", "content": "这是第一部分的详细内容..."}]}。请直接返回纯JSON字符串，不要包含任何额外的解释文字或Markdown代码块。`
        });
      } else {
        _.push({
          role: `system`,
          content: `You are a helpful assistant.`
        });
      }
      if ((c.length > 0 || u.length > 0) && !g) {
        let e = [];
        for (let t of c) {
          try {
            t = await za(t);
            let n = t;
            if (!t.startsWith(`data:image/`) && !t.startsWith(`http`)) {
              n = `data:image/jpeg;base64,${t.split(`,`).pop()}`;
            }
            e.push({
              type: `image_url`,
              image_url: {
                url: n
              }
            });
          } catch (e) {
            console.warn(`Failed to process image for text node:`, t, e);
          }
        }
        for (let t of u) {
          try {
            t = await za(t);
            t.startsWith(`data:`);
            e.push({
              type: `video_url`,
              video_url: {
                url: t
              }
            });
          } catch (e) {
            console.warn(`Failed to process video for text node:`, t, e);
          }
        }
        if (m) {
          e.push({
            type: `text`,
            text: m
          });
        }
        if (e.length === 0) {
          _.push({
            role: `user`,
            content: m || ` `
          });
        } else {
          _.push({
            role: `user`,
            content: e
          });
        }
      } else {
        _.push({
          role: `user`,
          content: m || ` `
        });
      }
      console.log(`Sending Text API payload:`, `${JSON.stringify(_, null, 2).substring(0, 1000)}...(truncated)`);
      let v = `${d.replace(/\/$/, ``)}/v1/chat/completions`;
      let y = new AbortController();
      zt.current.set(e, y);
      let b = (ae || 600) * 1000;
      let S = setTimeout(() => {
        if (zt.current.has(e)) {
          k(`生成耗时较长，已转入后台运行，请稍后在“任务清单”中查看。`);
          K(t => {
            return t.map(t => {
              if (t.id === e) {
                return {
                  ...t,
                  data: {
                    ...t.data,
                    loading: false,
                    errorMsg: `已转入后台运行，完成后可在任务清单中查看`
                  }
                };
              } else {
                return t;
              }
            });
          });
        }
      }, b);
      K(t => {
        return t.map(t => {
          if (t.id === e) {
            return {
              ...t,
              data: {
                ...t.data,
                loading: true,
                errorMsg: undefined
              }
            };
          } else {
            return t;
          }
        });
      });
      let C = `${e}_${Date.now()}`;
      let w = a || x.split(`
`)[0].trim();
      if (I) {
        H?.(true);
        I(t => {
          let n = t.filter(e => {
            return e.id !== C;
          });
          return [{
            id: C,
            nodeId: e,
            type: `text`,
            status: `running`,
            progress: 0,
            createdAt: Date.now(),
            prompt: m || `生成文本`,
            channelName: I_(d),
            modelName: w,
            requestData: {
              model: w,
              messages: _
            }
          }, ...n];
        });
      }
      console.log(`[InfiniteCanvas] text proxyFetch ->`, v, {
        useBuiltin: s,
        hasKey: !!f,
        isShareRuntime: we,
        proxyMode: Ee.proxyMode,
        appId: Ee.appId
      });
      let T = await Bl(v, {
        method: `POST`,
        headers: {
          Authorization: `Bearer ${f}`,
          'Content-Type': `application/json`
        },
        body: JSON.stringify({
          model: a || x.split(`
`)[0].trim(),
          messages: _,
          temperature: 0.7,
          response_format: i && !(a || x).toLowerCase().includes(`deepseek`) && !(a || x).toLowerCase().includes(`claude`) ? {
            type: `json_object`
          } : undefined
        }),
        ...Ee
      }).finally(() => {
        clearTimeout(S);
      });
      zt.current.delete(e);
      if (!T.ok) {
        let e = `API Error: ${T.status} ${T.statusText}`;
        try {
          let t = await T.json();
          if (t.error && t.error.message) {
            e = `API Error: ${t.error.message}`;
          } else {
            if (t.message) {
              e = `API Error: ${t.message}`;
            } else {
              e = `API Error: ${T.status} - ${JSON.stringify(t)}`;
            }
          }
        } catch {
          try {
            let t = await T.text();
            e = `API Error: ${T.status} - ${t}`;
          } catch {}
        }
        throw Error(e);
      }
      let E = (await T.json()).choices?.[0]?.message?.content || ``;
      if (i) {
        try {
          let n = E.replace(/```json/g, ``).replace(/```/g, ``).trim();
          let i = [];
          try {
            let e = JSON.parse(n);
            i = e.items || e;
          } catch (e) {
            console.warn(`JSON parsing failed, attempting fallback regex parsing`, e);
            let t = /"title"\s*:\s*"([^"]+)"\s*,\s*"content"\s*:\s*"([^"]+)"/g;
            let r;
            while ((r = t.exec(n)) !== null) {
              i.push({
                title: r[1],
                content: r[2]
              });
            }
            if (i.length === 0) {
              throw Error(`Failed to parse JSON and fallback failed`);
            }
          }
          if (Array.isArray(i)) {
            let n = J().find(t => {
              return t.id === e;
            });
            let a = (n?.position.x || 0) + 400;
            let o = n?.position.y || 0;
            let s = i.map((t, n) => {
              let r = typeof t == `string` ? t : t.content;
              let i = typeof t == `string` ? `Text ${n + 1}` : t.title;
              return {
                id: `text-split-${e}-${n}-${Date.now()}`,
                type: `textNode`,
                position: {
                  x: a,
                  y: o + n * 250
                },
                data: {
                  text: r,
                  label: i,
                  expanded: false,
                  onGenerateText: Lr,
                  presetPrompts: M,
                  splitSourceId: e
                }
              };
            });
            let c = s.map(t => {
              return {
                id: `e-${e}-${t.id}`,
                source: e,
                target: t.id,
                type: `default`
              };
            });
            let l = new Set();
            let u = new Set();
            let d = e => {
              J().forEach(t => {
                if (t.data.splitSourceId === e) {
                  if (!l.has(t.id)) {
                    l.add(t.id);
                    d(t.id);
                  }
                }
              });
            };
            d(e);
            Y().forEach(e => {
              if (l.has(e.source) || l.has(e.target)) {
                u.add(e.id);
              }
            });
            let f = [];
            let p = [];
            s.forEach(i => {
              let a = new Map();
              a.set(e, i.id);
              let o = [e];
              let s = new Set();
              while (o.length > 0) {
                let c = o.shift();
                let u = t.filter(e => {
                  return e.source === c;
                });
                for (let t of u) {
                  if (s.has(t.id)) {
                    continue;
                  }
                  s.add(t.id);
                  let u = r.find(e => {
                    return e.id === t.target;
                  });
                  if (u) {
                    if (l.has(u.id)) {
                      continue;
                    }
                    if (!a.has(u.id)) {
                      let t = `${u.type}-clone-${Date.now()}-${Math.random().toString(36).substr(2, 5)}`;
                      a.set(u.id, t);
                      o.push(u.id);
                      let r = {
                        ...u,
                        id: t,
                        position: {
                          x: i.position.x + (u.position.x - n.position.x),
                          y: i.position.y + (u.position.y - n.position.y)
                        },
                        selected: false,
                        data: {
                          ...u.data,
                          loading: false,
                          errorMsg: undefined,
                          imageUrl: u.type === `imageNode` || u.type === `promptNode` ? undefined : u.data.imageUrl,
                          expanded: false,
                          splitSourceId: e
                        }
                      };
                      f.push(r);
                    }
                    p.push({
                      id: `e-${a.get(c)}-${a.get(t.target)}`,
                      source: a.get(c),
                      sourceHandle: t.sourceHandle,
                      target: a.get(t.target),
                      targetHandle: t.targetHandle
                    });
                  }
                }
              }
            });
            K(e => {
              return e.filter(e => {
                return !l.has(e.id);
              }).concat(s).concat(f);
            });
            q(e => {
              return e.filter(e => {
                return !u.has(e.id);
              }).concat(c).concat(p);
            });
            k(`已自动拆分为 ${i.length} 个节点并复制后续节点`);
            // 修复：拆分成功提前 return，会跳过下方 6478 的任务完成标记，导致任务中心一直停在 running
            if (I) {
              I(e => {
                return e.map(e => {
                  if (e.id === C || e.taskId === C) {
                    return {
                      ...e,
                      status: `completed`,
                      progress: 100,
                      resultUrl: E
                    };
                  } else {
                    return e;
                  }
                });
              });
            }
            return {
              splitNodes: s.map(e => {
                return e.id;
              }),
              clonedNodes: f.map(e => {
                return e.id;
              }),
              newEdges: c.concat(p)
            };
          } else {
            throw Error(`Result is not an array`);
          }
        } catch (t) {
          console.warn(`Auto split failed, fallback to normal text`, t);
          k(`自动拆分失败 (格式不符)，已显示完整文本`);
          K(t => {
            return t.map(t => {
              if (t.id === e) {
                return {
                  ...t,
                  data: {
                    ...t.data,
                    text: E,
                    loading: false
                  }
                };
              } else {
                return t;
              }
            });
          });
        }
      } else {
        K(t => {
          return t.map(t => {
            if (t.id === e) {
              return {
                ...t,
                data: {
                  ...t.data,
                  text: E,
                  loading: false
                }
              };
            } else {
              return t;
            }
          });
        });
      }
      if (i) {
        K(t => {
          return t.map(t => {
            if (t.id === e) {
              return {
                ...t,
                data: {
                  ...t.data,
                  loading: false
                }
              };
            } else {
              return t;
            }
          });
        });
      }
      if (I) {
        I(e => {
          return e.map(e => {
            if (e.id === C || e.taskId === C) {
              return {
                ...e,
                status: `completed`,
                progress: 100,
                resultUrl: E
              };
            } else {
              return e;
            }
          });
        });
      }
      try {
        if (Ce && Ce.status.isConnected && E) {
          let e = new Blob([E], {
            type: `text/plain`
          });
          let t = `text_result_${Date.now()}.txt`;
          await Ce.uploadFile(e, t, `tasks`);
        }
      } catch (e) {
        console.warn(`Failed to save text result to local disk`, e);
      }
    } catch (t) {
      if (t.name === `AbortError`) {
        if (zt.current.has(e)) {
          zt.current.delete(e);
          console.log(`Fetch timed out`);
          k(`生成超时，请重试`);
          if (I) {
            I(t => {
              return t.map(t => {
                if (t.nodeId === e && (t.status === `running` || t.status === `pending`)) {
                  return {
                    ...t,
                    status: `failed`,
                    errorMsg: `请求超时`
                  };
                } else {
                  return t;
                }
              });
            });
          }
        } else {
          console.log(`Fetch aborted by user`);
          if (I) {
            I(t => {
              return t.map(t => {
                if (t.nodeId === e && (t.status === `running` || t.status === `pending`)) {
                  return {
                    ...t,
                    status: `failed`,
                    errorMsg: `已取消`
                  };
                } else {
                  return t;
                }
              });
            });
          }
        }
        return;
      }
      console.error(t);
      k(`生成失败: ${t.message}`);
      if (I) {
        I(n => {
          return n.map(n => {
            if (n.nodeId === e && (n.status === `running` || n.status === `pending`)) {
              return {
                ...n,
                status: `failed`,
                errorMsg: t.message
              };
            } else {
              return n;
            }
          });
        });
      }
      K(n => {
        return n.map(n => {
          if (n.id === e) {
            return {
              ...n,
              data: {
                ...n.data,
                loading: false,
                errorMsg: t.message
              }
            };
          } else {
            return n;
          }
        });
      });
      throw t;
    } finally {
      K(t => {
        return t.map(t => {
          if (t.id === e) {
            return {
              ...t,
              data: {
                ...t.data,
                loading: false
              }
            };
          } else {
            return t;
          }
        });
      });
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    }
  }, [r, t, l, u, x, J, Y, K, q, k, j, ae, I]);
  let Rr = Z.useRef(new Set());
  let zr = Z.useCallback(async e => {
    let t = e => {
      return new Promise(t => {
        return setTimeout(t, e);
      });
    };
    let n = Date.now();
    while (Date.now() - n < 3000 && J().find(t => {
      return t.id === e;
    })?.data?.loading !== true) {
      await t(120);
    }
    let r = Date.now();
    while (Date.now() - r < 900000) {
      let n = J().find(t => {
        return t.id === e;
      });
      if (!n) {
        return `节点已删除`;
      }
      if (n.data?.loading === false || n.data?.loading === undefined) {
        let e = n.data?.errorMsg;
        if (e) {
          return String(e);
        } else {
          return null;
        }
      }
      await t(500);
    }
    return `调度超时`;
  }, [J]);
  let Br = Z.useCallback(async (e, t, n) => {
    if (Rr.current.has(e)) {
      return;
    }
    Rr.current.add(e);
    let r = Aa(t);
    try {
      for (let i = 0; i < r.length; i++) {
        let a = r[i];
        k(`调度「${t.name}」：尝试 ${i + 1}/${r.length} · ${a}`);
        await n(a);
        let o = await zr(e);
        if (!o) {
          k(`调度成功：${a}`);
          return;
        }
        if (F_(o)) {
          k(`调度已中断：${o}`);
          return;
        }
        if (i < r.length - 1) {
          k(`${a} 失败，正在切换下一个模型…`);
        } else {
          k(`调度结束：全部 ${r.length} 次尝试均失败`);
        }
      }
    } finally {
      Rr.current.delete(e);
    }
  }, [k, zr]);
  let Vr = Z.useCallback(e => {
    let t = ka(e);
    return t && xa().find(e => {
      return e.id === t && e.enabled;
    }) || null;
  }, []);
  let Hr = Z.useCallback((e, t, n = `1024x1024`, r, i = `auto`, a = 1) => {
    let o = Vr(r);
    if (o) {
      Br(e, o, r => {
        return Sr(e, t, n, r, i, 1);
      });
      return;
    }
    return Sr(e, t, n, r, i, a);
  }, [Vr, Br, Sr]);
  let Ur = Z.useCallback((e, t, n = false, r) => {
    let i = Vr(r);
    if (i) {
      Br(e, i, r => {
        Lr(e, t, n, r);
      });
      return;
    }
    Lr(e, t, n, r);
  }, [Vr, Br, Lr]);
  let Wr = Z.useCallback(async (e, n) => {
    let i = (n || x.split(`
`)[0] || ``).trim();
    let a = ca(i);
    let o = !!Qi().image.length || !!Qi().text.length || !!Qi().video.length;
    if (a && i && Ee.proxyMode !== `server-proxy` && o) {
      let e = Hi(i);
      if (e) {
        throw Error(e);
      }
    }
    let s = a ? l : t;
    let c = a ? u : r;
    if (!c && !a) {
      throw Error(`请先在设置中配置文本大模型 API Key`);
    }
    let d = await Bl(`${s.replace(/\/$/, ``)}/v1/chat/completions`, {
      method: `POST`,
      headers: {
        Authorization: `Bearer ${c}`,
        'Content-Type': `application/json`
      },
      body: JSON.stringify({
        model: i,
        messages: [{
          role: `system`,
          content: `You are a helpful assistant that outputs strict JSON.`
        }, {
          role: `user`,
          content: e
        }],
        temperature: 0.1
      }),
      ...Ee
    });
    if (!d.ok) {
      let e = `API Error: ${d.status} ${d.statusText}`;
      try {
        let t = await d.json();
        if (t?.error?.message) {
          e = `API Error: ${t.error.message}`;
        } else if (t?.message) {
          e = `API Error: ${t.message}`;
        }
      } catch {}
      throw Error(e);
    }
    let f = await d.json();
    let p = ``;
    if (f?.choices?.[0]?.message?.content) {
      p = f.choices[0].message.content;
    } else if (f?.candidates?.[0]?.content?.parts?.[0]?.text) {
      p = f.candidates[0].content.parts[0].text;
    } else if (f?.response) {
      p = f.response;
    } else if (typeof f == `string`) {
      p = f;
    } else {
      throw Error(`无法从大模型返回结果中提取文本`);
    }
    return p;
  }, [x, l, u, t, r, Ce]);
  Z.useEffect(() => {
    jm(Wr);
    return () => {
      return jm(null);
    };
  }, [Wr]);
  let Gr = Z.useCallback((e, t, n = `16:9`, r = `1280x720`, i, a, o = `auto`) => {
    let s = Vr(i);
    if (s) {
      Br(e, s, i => {
        return kr(e, t, n, r, i, a, o);
      });
      return;
    }
    return kr(e, t, n, r, i, a, o);
  }, [Vr, Br, kr]);
  let qr = Z.useCallback((e, t, n, r, i) => {
    let a = Vr(r);
    if (a) {
      Br(e, a, r => {
        return Er(e, t, n, r, i);
      });
      return;
    }
    return Er(e, t, n, r, i);
  }, [Vr, Br, Er]);
  let Jr = Z.useCallback((e, t, n, r, i, a) => {
    let o = Vr(r);
    if (o) {
      Br(e, o, r => {
        return Dr(e, t, n, r, i, a);
      });
      return;
    }
    return Dr(e, t, n, r, i, a);
  }, [Vr, Br, Dr]);
  let Yr = Z.useCallback(e => {
    if (!F) {
      return;
    }
    let t = F.find(t => {
      return t.nodeId === e;
    });
    if (t) {
      if (t.status === `success` || t.status === `completed`) {
        k(`已从全局任务恢复成功状态`);
        K(n => {
          return n.map(n => {
            if (n.id === e) {
              return {
                ...n,
                data: {
                  ...n.data,
                  loading: false,
                  progress: 100,
                  videoUrl: An(t.resultUrl),
                  thumbnailUrl: An(t.thumbnailUrl),
                  imageAvailable: !!t.thumbnailUrl,
                  imageUrl: An(t.resultUrl),
                  text: t.resultUrl,
                  errorMsg: undefined
                }
              };
            } else {
              return n;
            }
          });
        });
        q(t => {
          return t.map(t => {
            if (t.target === e) {
              return {
                ...t,
                animated: false
              };
            } else {
              return t;
            }
          });
        });
        if (t.resultUrl) {
          vr(e, An(t.resultUrl));
        }
      } else if (t.status === `failed`) {
        k(`任务已失败: ${t.errorMsg || `未知错误`}`);
        K(n => {
          return n.map(n => {
            if (n.id === e) {
              return {
                ...n,
                data: {
                  ...n.data,
                  loading: false,
                  errorMsg: t.errorMsg || `生成失败`
                }
              };
            } else {
              return n;
            }
          });
        });
        q(t => {
          return t.map(t => {
            if (t.target === e) {
              return {
                ...t,
                animated: false
              };
            } else {
              return t;
            }
          });
        });
      } else {
        k(`任务仍在进行中，进度: ${t.progress}%`);
        K(n => {
          return n.map(n => {
            if (n.id === e) {
              return {
                ...n,
                data: {
                  ...n.data,
                  loading: true,
                  progress: t.progress
                }
              };
            } else {
              return n;
            }
          });
        });
      }
    } else {
      k(`未找到关联的后台任务，已重置节点状态`);
      K(t => {
        return t.map(t => {
          if (t.id === e) {
            return {
              ...t,
              data: {
                ...t.data,
                loading: false,
                progress: 0,
                errorMsg: undefined
              }
            };
          } else {
            return t;
          }
        });
      });
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    }
  }, [F, K, q, k, vr]);
  let Xr = Z.useRef(true);
  Z.useEffect(() => {
    let e = Xr.current;
    if (e) {
      Xr.current = false;
    }
    if (!Ht.current && F) {
      let t = new Map();
      for (let e of F) {
        if (!e?.nodeId) {
          continue;
        }
        let n = t.get(e.nodeId);
        if (!n || (e.createdAt || 0) > (n.createdAt || 0)) {
          t.set(e.nodeId, e);
        }
      }
      K(n => {
        let r = false;
        let i = n.map(n => {
          if (n.data?.loading) {
            let i = t.get(n.id);
            if (i) {
              if (i.status === `success` || i.status === `completed`) {
                r = true;
                return {
                  ...n,
                  data: {
                    ...n.data,
                    loading: false,
                    progress: 100,
                    videoUrl: An(i.resultUrl),
                    imageUrl: An(i.resultUrl),
                    text: i.resultUrl,
                    errorMsg: undefined
                  }
                };
              }
              if (i.status === `failed`) {
                r = true;
                return {
                  ...n,
                  data: {
                    ...n.data,
                    loading: false,
                    errorMsg: i.errorMsg || `生成失败`
                  }
                };
              }
              if (n.data.progress !== i.progress) {
                r = true;
                return {
                  ...n,
                  data: {
                    ...n.data,
                    progress: i.progress
                  }
                };
              }
            } else if (e) {
              r = true;
              return {
                ...n,
                data: {
                  ...n.data,
                  loading: false,
                  progress: 0
                }
              };
            }
          }
          return n;
        });
        if (r) {
          return i;
        } else {
          return n;
        }
      });
    }
  }, [F, K]);
  let Zr = Z.useCallback(async (e, n, i) => {
    let a = (i || x.split(`
`)[0] || ``).trim();
    if (!a) {
      throw Error(`请选择文本模型`);
    }
    let o = ca(a);
    let s = !!Qi().image.length || !!Qi().text.length || !!Qi().video.length;
    if (o && Ee.proxyMode !== `server-proxy` && s) {
      let e = Hi(a);
      if (e) {
        throw Error(e);
      }
    }
    let c = o ? l : t;
    let d = o ? u : r;
    if (!d && !o) {
      throw Error(`请先在设置中配置文本大模型 API Key`);
    }
    let f = `${c.replace(/\/$/, ``)}/v1/chat/completions`;
    let p = ``;
    if (n) {
      p = `当前节点的已有配置如下，请在这个配置的基础上进行修改或补充，而不是完全从零生成（除非用户要求重置）：\n\`\`\`json\n${JSON.stringify(n, null, 2)}\n\`\`\`\n\n`;
    }
    let m = [{
      role: `system`,
      content: `你是一个智能API配置助手。用户会描述一个API的需求，你需要理解意图并严格返回一个包含完整配置的 JSON 对象。不要有任何额外的解释文字，不要使用Markdown代码块包裹，必须直接返回纯 JSON 字符串。
返回的 JSON 结构必须符合以下格式：
{
  "apiUrl": "请求的完整URL",
  "method": "GET 或 POST 或 PUT",
  "headers": "请求头对象的JSON字符串格式，例如 '{\\n  "Content-Type": "application/json",\\n  "Authorization": "Bearer xxx"\\n}'",
  "body": "请求体的JSON字符串格式（如果有）。请务必将用户可能需要动态传入的值替换为 {{变量名}} 形式，例如 '{\\n  "text": "{{prompt}}"\\n}'",
  "outputType": "text 或 image 或 video 或 audio，根据你推测的 API 返回内容决定",
  "executionMode": "sync 或 async，如果需要轮询则为 async，一般默认为 sync",
  "resultPath": "推测的用于提取核心返回内容的 JSON Path，例如 'data.url' 或 'choices[0].message.content'",
  "taskIdPath": "如果是 async 模式，提取任务ID的路径，例如 'data.task_id'，否则为空字符串",
  "pollingUrl": "如果是 async 模式，轮询状态的API地址，例如 'https://api.example.com/v1/tasks/{{task_id}}'，否则为空字符串",
  "pollingMethod": "如果是 async 模式，轮询状态的请求方法，例如 'GET' 或 'POST'。默认通常是 'GET'。",
  "pollingHeaders": "如果是 async 模式，轮询时专属的请求头JSON字符串，如果不填或为空则默认继承主请求的 headers。如果遇到轮询必须传不同鉴权信息的，可以填这个字段。",
  "pollingBody": "如果是 async 模式且 pollingMethod 为 POST，轮询时的请求体JSON字符串格式。可以使用 {{task_id}} 代替任务ID。如果不填则为空。",
  "pollingResultPath": "如果是 async 模式，轮询返回结果中判断状态的字段路径，例如 'data.status'，否则为空字符串",
  "pollingCompletedValue": "如果是 async 模式，代表任务完成的状态值，例如 'completed' 或 'success'，否则为空字符串",
  "pollingFailedValue": "如果是 async 模式，代表任务失败的状态值，例如 'failed' 或 'error'，如果不确定可为空",
  "pollingErrorPath": "如果是 async 模式，提取错误信息的路径，例如 'data.error'，否则为空字符串",
  "pollingProgressPath": "如果是 async 模式，提取进度百分比的路径，例如 'data.progress'，否则为空字符串",
  "pollingResultDataPath": "如果是 async 模式，代表轮询成功后最终结果提取的字段路径，例如 'data.output_url'。如果与主请求一致或不确定，可为空字符串",
  "rawTextOutput": "布尔值，是否纯文本输出，通常在 outputType 为 text 时使用"
}
请注意：
1. 【核心参数变量化】：不要把所有输入都变成变量！只需提取最核心的 2-3 个参数作为变量（如 {{prompt}}, {{image_1}} 等），其他次要参数（如分辨率、模型版本、负面提示词等）请直接写死一个合理的默认值，或者如果不必要则直接省略。
2. 【异步智能填充】：如果你判断该接口（如生成视频、长音频、绘图等）通常是异步的（即提交后返回任务ID，需要轮询结果），请务必将 executionMode 设为 async，并一键推断填好 taskIdPath, pollingUrl, pollingResultPath, pollingCompletedValue 这四个字段。如果 pollingUrl 和 apiUrl 类似只是后面拼个ID，可以写成带有 {{task_id}} 的格式。
3. 【异步轮询 Headers 和 Body】：如果用户提供了轮询专用的 curl 或明确要求，请务必生成 pollingHeaders。如果轮询是 POST 请求且需要在 body 中传递 task_id，请务必设置 pollingMethod 为 "POST"，并正确生成 pollingBody，如 '{
  "taskId": "{{task_id}}"
}'。
4. resultPath 需要根据常见 API 的返回结构（如 OpenAI 格式为 choices[0].message.content，常见图生图为 data.image_url）进行合理推测。如果是异步且结果在轮询返回里，请填到 pollingResultDataPath。
5. 必须确保输出是合法的 JSON 格式。`
    }, {
      role: `user`,
      content: p + e
    }];
    let h = await Bl(f, {
      method: `POST`,
      headers: {
        Authorization: `Bearer ${d}`,
        'Content-Type': `application/json`
      },
      body: JSON.stringify({
        model: a,
        messages: m,
        temperature: 0.1
      }),
      ...Ee
    });
    if (!h.ok) {
      throw Error(`API 请求失败: ${h.status}`);
    }
    let g = (await h.json()).choices?.[0]?.message?.content || ``;
    g = g.replace(/```json\n?/gi, ``).replace(/```\n?/g, ``).trim();
    return g;
  }, [r, t, u, l, x, Ee]);
  let Qr = Z.useCallback(async e => {
    let t = J().find(t => {
      return t.id === e;
    });
    if (!t || t.type !== `customNode` || !t.data.config) {
      return;
    }
    let n = t.data.config;
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              loading: true,
              progress: 0,
              errorMsg: undefined,
              resultData: undefined
            }
          };
        } else {
          return t;
        }
      });
    });
    q(t => {
      return t.map(t => {
        if (t.target === e) {
          return {
            ...t,
            animated: true
          };
        } else {
          return t;
        }
      });
    });
    let r = null;
    let i = async e => {
      if (!j || n.outputType !== `video`) {
        return;
      }
      let t = Array.isArray(e) ? e : [e];
      for (let e of t) {
        let t = typeof e == `string` ? e.trim() : ``;
        if (!t) {
          continue;
        }
        let n = t;
        try {
          let e = await si(t, {
            subfolder: `tasks`
          });
          if (e?.url) {
            n = e.url;
          }
        } catch {}
        j(n, `video`, `generated`);
      }
    };
    try {
      let a = Y();
      let o = J();
      let s = a.filter(t => {
        return t.target === e;
      });
      let c = {
        ...(t.data.config?.variables || {})
      };
      let l = [];
      let u = [];
      s.forEach(e => {
        let t = o.find(t => {
          return t.id === e.source;
        });
        if (t) {
          let n = ``;
          if (t.data.resultData) {
            n = String(t.data.resultData);
            if (t.data.audioUrl) {
              n = t.data.audioUrl;
            } else if (t.data.videoUrl) {
              n = t.data.videoUrl;
            } else if (t.data.imageUrl) {
              n = t.data.imageUrl;
            }
          } else if (t.data.audioUrl) {
            n = t.data.audioUrl;
          } else if (t.data.videoUrl) {
            n = t.data.videoUrl;
          } else if (t.data.imageUrl) {
            n = t.data.imageUrl;
          } else if (t.data.extractedImages) {
            n = t.data.extractedImages[0] || ``;
          } else if (t.type === `imageBoxNode` && Array.isArray(t.data.images)) {
            let e = t.data.images;
            let r = t.data.selectedIds || [];
            if (r.length > 0) {
              let t = new Set(r);
              n = e.filter(e => {
                return t.has(e.id);
              }).map(e => {
                return e.url;
              }).filter(Boolean).join(`
`);
            } else {
              n = e[typeof t.data.activeIndex == `number` ? t.data.activeIndex : 0]?.url || ``;
            }
          } else if (t.data.text) {
            n = t.data.text;
          } else if (t.data.resultUrl) {
            n = t.data.resultUrl;
          }
          if (e.targetHandle && e.targetHandle.startsWith(`var-`)) {
            let t = e.targetHandle.replace(`var-`, ``);
            c[t] = n;
          } else if (!e.targetHandle || e.targetHandle === 'default' || e.targetHandle === ``) {
            if (t.type !== `fileToUrlNode` && (t.data.imageUrl || t.data.videoUrl || String(n).startsWith(`http`) || String(n).startsWith(`data:image/`))) {
              l.push(n);
            } else {
              u.push(n);
            }
          }
        }
      });
      let d = n.body || ``;
      let f = n.apiUrl || ``;
      let p = async e => {
        try {
          let t = await (await fetch(e)).blob();
          return new Promise((e, n) => {
            let r = new FileReader();
            r.onloadend = () => {
              return e(r.result);
            };
            r.onerror = n;
            r.readAsDataURL(t);
          });
        } catch (t) {
          console.warn(`Failed to fetch and convert URL to Base64:`, e, t);
          return e;
        }
      };
      for (let [e, t] of Object.entries(c)) {
        if (typeof t == `string` && t.startsWith(`http`) && (e.startsWith(`image`) || e.startsWith(`audio`) || e.startsWith(`video`))) {
          c[e] = await p(t);
        }
      }
      let m = n.headers || `{}`;
      let h = n.pollingHeaders || `{}`;
      Object.entries(c).forEach(([e, t]) => {
        let r = String(t);
        let i = r.replace(/"/g, `\\"`).replace(/\n/g, `\\n`);
        if (e === `prompt` && u.length > 0) {
          i = `${u.join(`\\n`)}\n${i}`;
        }
        let a = RegExp(`"\\{\\{${e}(?:\\|[^}]+)?\\}\\}"`, `g`);
        let o = RegExp(`\\{\\{${e}(?:\\|[^}]+)?\\}\\}`, `g`);
        if (n.variableFormats && n.variableFormats[e] === `json`) {
          try {
            JSON.parse(r);
            d = d.replace(a, r);
            d = d.replace(o, r);
          } catch {
            d = d.replace(a, r);
            d = d.replace(o, r);
          }
        } else {
          d = d.replace(o, i);
        }
        f = f.replace(o, r);
        m = m.replace(o, r);
        h = h.replace(o, r);
      });
      if (u.length > 0 && !Object.keys(c).includes(`prompt`)) {
        d = d.replace(/\{\{prompt\}\}/g, u.join(`\\n`).replace(/"/g, `\\"`).replace(/\n/g, `\\n`));
      }
      l.forEach((e, t) => {
        d = d.replace(RegExp(`\\{\\{image_${t + 1}\\}\\}`, `g`), e);
      });
      let g = {};
      try {
        if (m) {
          g = {
            ...JSON.parse(m)
          };
        }
      } catch (e) {
        console.warn(`Failed to parse headers`, e);
      }
      let _ = g;
      try {
        if (h && h !== `{}`) {
          _ = {
            ...JSON.parse(h)
          };
        }
      } catch (e) {
        console.warn(`Failed to parse polling headers`, e);
      }
      let v = n.method === `GET` ? undefined : d;
      if (v && g[`Content-Type`] === `multipart/form-data`) {
        try {
          let e = JSON.parse(d);
          let t = new FormData();
          for (let n in e) {
            let r = e[n];
            if (typeof r == `string` && r.startsWith(`data:`)) {
              let e = r.split(`,`);
              let i = e[0].match(/:(.*?);/);
              let a = i ? i[1] : `application/octet-stream`;
              let o = atob(e[1]);
              let s = o.length;
              let c = new Uint8Array(s);
              while (s--) {
                c[s] = o.charCodeAt(s);
              }
              let l = a.split(`/`)[1] || `bin`;
              t.append(n, new Blob([c], {
                type: a
              }), `upload.${l}`);
            } else {
              t.append(n, typeof r == `object` ? JSON.stringify(r) : String(r));
            }
          }
          v = t;
          delete g[`Content-Type`];
        } catch (e) {
          console.warn(`Failed to parse body as JSON for FormData conversion`, e);
          throw Error(`FormData 请求的 Body 必须是有效的 JSON 格式，以便自动转换为文件`);
        }
      }
      let y = new AbortController();
      zt.current.set(e, y);
      let b = await Bl(f, {
        method: n.method,
        headers: g,
        body: v,
        ...Ee
      });
      if (!b.ok) {
        throw Error(`API 请求失败: ${b.status}`);
      }
      let x = b.headers.get(`content-type`) || ``;
      let S = false;
      let C;
      if (x.includes(`audio/`) || x.includes(`image/`) || x.includes(`video/`) || x.includes(`application/octet-stream`)) {
        S = true;
        let e = await b.blob();
        C = await new Promise((t, n) => {
          let r = new FileReader();
          r.onloadend = () => {
            return t(r.result);
          };
          r.onerror = n;
          r.readAsDataURL(e);
        });
      }
      let w = null;
      if (!S) {
        w = await b.json();
      }
      let T = (e, t) => {
        if (!t || t === `$`) {
          return e;
        } else {
          return t.replace(/\{\{|\}\}/g, ``).replace(/\[(\d+)\]/g, `.$1`).replace(/^\./, ``).split(`.`).reduce((e, t) => {
            if (e && typeof e == `object`) {
              return e[t];
            }
          }, e);
        }
      };
      if (n.executionMode === `sync`) {
        let t;
        if (S) {
          t = C;
        } else {
          t = T(w, n.resultPath);
          if (typeof t == `object` && t) {
            if (n.rawTextOutput && Array.isArray(t) && t.every(e => {
              return typeof e != `object`;
            })) {
              t = t.join(`
`);
            } else {
              t = JSON.stringify(t, null, 2);
            }
          }
        }
        if (t === undefined) {
          throw Error(`无法提取结果`);
        }
        r = `custom-sync-${Date.now()}`;
        if (I) {
          H?.(true);
          I(i => {
            return [...i, {
              id: r,
              nodeId: e,
              type: `custom`,
              status: `completed`,
              progress: 100,
              createdAt: Date.now(),
              prompt: n.apiUrl,
              customOutputType: n.outputType,
              customResultData: t,
              customRawResponse: w,
              channelName: I_(n.apiUrl),
              modelName: `万能节点`,
              requestData: {
                method: n.method,
                url: n.apiUrl,
                body: n.body
              },
              responseData: w
            }];
          });
        }
        K(r => {
          return r.map(r => {
            if (r.id === e) {
              let e = {
                ...r.data,
                resultData: t,
                loading: false
              };
              if (n.outputType === `text`) {
                e.text = t;
              }
              if (n.outputType === `image`) {
                e.imageUrl = An(Array.isArray(t) ? t[0] : t);
              }
              if (n.outputType === `video`) {
                e.videoUrl = An(t);
              }
              if (n.outputType === `audio`) {
                e.audioUrl = An(t);
              }
              return {
                ...r,
                data: e
              };
            }
            return r;
          });
        });
        await i(t);
        try {
          if (n.outputType === `text` && t && typeof t == `string`) {
            let e = new Blob([t], {
              type: `text/plain`
            });
            let n = `custom_text_${Date.now()}.txt`;
            if (Ce && Ce.status.isConnected) {
              await Ce.uploadFile(e, n, `tasks`);
            }
          }
        } catch (e) {
          console.warn(`Failed to save custom text result to local disk`, e);
        }
        k(`节点执行成功`);
      } else {
        let t = T(w, n.taskIdPath || ``);
        if (!t) {
          throw Error(`无法提取 Task ID`);
        }
        r = `custom-${t || Date.now()}`;
        if (I) {
          H?.(true);
          I(t => {
            return [...t, {
              id: r,
              nodeId: e,
              type: `custom`,
              status: `pending`,
              progress: 0,
              createdAt: Date.now(),
              prompt: n.apiUrl,
              customOutputType: n.outputType,
              customRawResponse: w,
              channelName: I_(n.apiUrl),
              modelName: `万能节点`,
              requestData: {
                method: n.method,
                url: n.apiUrl,
                body: n.body
              },
              responseData: w
            }];
          });
        }
        let a = false;
        let o = 0;
        let s = (n.pollingUrl || n.apiUrl).replace(`{{task_id}}`, t);
        Object.entries(c).forEach(([e, t]) => {
          let n = RegExp(`\\{\\{${e}(?:\\|[^}]+)?\\}\\}`, `g`);
          s = s.replace(n, String(t));
        });
        let l = Math.ceil(re / B);
        while (!a && o < l) {
          if (y.signal.aborted) {
            throw Error(`已取消`);
          }
          await new Promise(e => {
            return setTimeout(e, B * 1000);
          });
          o++;
          let l = n.pollingMethod || `GET`;
          let u = n.pollingBody || ``;
          if (u) {
            u = u.replace(/\{\{task_id\}\}/g, t);
            Object.entries(c).forEach(([e, t]) => {
              let n = RegExp(`\\{\\{${e}(?:\\|[^}]+)?\\}\\}`, `g`);
              u = u.replace(n, String(t).replace(/"/g, `\\"`).replace(/\n/g, `\\n`));
            });
          }
          let d = {
            method: l,
            headers: _
          };
          if (l !== `GET` && u) {
            d.body = u;
          }
          let f = await Bl(s, {
            ...d,
            ...Ee
          });
          if (!f.ok) {
            continue;
          }
          let p = await f.json();
          let m = n.pollingResultPath || ``;
          let h = p;
          if (m.includes(`{{task_id}}`)) {
            m = m.replace(/\{\{task_id\}\}/g, t);
          }
          let g = T(h, m);
          if (g === n.pollingCompletedValue) {
            a = true;
            let o = n.pollingResultDataPath || n.resultPath;
            if (o && o.includes(`{{task_id}}`)) {
              o = o.replace(/\{\{task_id\}\}/g, t);
            }
            let s = T(p, o);
            if (typeof s == `object` && s) {
              if (n.rawTextOutput && Array.isArray(s) && s.every(e => {
                return typeof e != `object`;
              })) {
                s = s.join(`
`);
              } else {
                s = JSON.stringify(s, null, 2);
              }
            }
            if (I && r) {
              I(e => {
                return e.map(e => {
                  if (e.id === r) {
                    return {
                      ...e,
                      status: `completed`,
                      progress: 100,
                      customResultData: s,
                      customRawResponse: p
                    };
                  } else {
                    return e;
                  }
                });
              });
            }
            K(t => {
              return t.map(t => {
                if (t.id === e) {
                  let e = {
                    ...t.data,
                    resultData: s,
                    loading: false,
                    progress: 100
                  };
                  if (n.outputType === `text`) {
                    e.text = s;
                  }
                  if (n.outputType === `image`) {
                    e.imageUrl = An(Array.isArray(s) ? s[0] : s);
                  }
                  if (n.outputType === `video`) {
                    e.videoUrl = An(s);
                  }
                  if (n.outputType === `audio`) {
                    e.audioUrl = An(s);
                  }
                  return {
                    ...t,
                    data: e
                  };
                }
                return t;
              });
            });
            await i(s);
            try {
              if (n.outputType === `text` && s && typeof s == `string`) {
                let e = new Blob([s], {
                  type: `text/plain`
                });
                let t = `custom_text_${Date.now()}.txt`;
                if (Ce && Ce.status.isConnected) {
                  await Ce.uploadFile(e, t, `tasks`);
                }
              }
            } catch (e) {
              console.warn(`Failed to save custom text result to local disk`, e);
            }
            k(`节点执行成功`);
            a = true;
            break;
          } else if (n.pollingFailedValue && g === n.pollingFailedValue) {
            let e = n.pollingErrorPath || ``;
            if (e && e.includes(`{{task_id}}`)) {
              e = e.replace(/\{\{task_id\}\}/g, t);
            }
            let r = e ? T(p, e) : `任务执行失败`;
            throw Error(typeof r == `object` ? JSON.stringify(r) : String(r));
          } else {
            let i = Math.min(99, o * 2);
            let a = n.pollingProgressPath || ``;
            if (a) {
              if (a.includes(`{{task_id}}`)) {
                a = a.replace(/\{\{task_id\}\}/g, t);
              }
              let e = T(p, a);
              if (e != null) {
                let t = String(e).replace(/[^0-9.-]/g, ``);
                if (t && !isNaN(Number(t))) {
                  i = Number(t);
                }
              }
            }
            if (I && r) {
              I(e => {
                return e.map(e => {
                  if (e.id === r) {
                    return {
                      ...e,
                      status: `running`,
                      progress: i,
                      customRawResponse: p
                    };
                  } else {
                    return e;
                  }
                });
              });
            }
            K(t => {
              return t.map(t => {
                if (t.id === e) {
                  return {
                    ...t,
                    data: {
                      ...t.data,
                      progress: i
                    }
                  };
                } else {
                  return t;
                }
              });
            });
          }
        }
        if (!a) {
          throw Error(`轮询超时`);
        }
      }
    } catch (t) {
      if (t.name !== `AbortError`) {
        if (I && r) {
          I(e => {
            return e.map(e => {
              if (e.id === r) {
                return {
                  ...e,
                  status: `failed`,
                  errorMsg: t.message
                };
              } else {
                return e;
              }
            });
          });
        }
        K(n => {
          return n.map(n => {
            if (n.id === e) {
              return {
                ...n,
                data: {
                  ...n.data,
                  loading: false,
                  errorMsg: t.message
                }
              };
            } else {
              return n;
            }
          });
        });
        k(`节点执行失败: ${t.message}`);
      }
    } finally {
      zt.current.delete(e);
      q(t => {
        return t.map(t => {
          if (t.target === e) {
            return {
              ...t,
              animated: false
            };
          } else {
            return t;
          }
        });
      });
    }
  }, [J, Y, K, q, k, j]);
  let $r = Z.useCallback(async e => {
    let t = J().find(t => {
      return t.id === e;
    });
    if (t && t.data.onExtractFrames) {
      await t.data.onExtractFrames(e);
    }
  }, [J]);
  let ei = Z.useCallback(async t => {
    let n = Array.isArray(t) ? t : [t];
    if (n.length === 0) {
      return;
    }
    let r = new Set();
    let i = Y();
    let a = [...n];
    while (a.length > 0) {
      let e = a.shift();
      if (!r.has(e)) {
        r.add(e);
        i.filter(t => {
          return t.source === e;
        }).forEach(e => {
          return a.push(e.target);
        });
      }
    }
    let o = {
      id: `checkpoint_${e}_${Date.now()}`,
      projectId: e,
      nodeExecOrder: Array.from(r),
      completedNodes: [],
      runningTasks: [],
      status: `running`,
      createdAt: Date.now(),
      updatedAt: Date.now()
    };
    mr(o);
    let s = [...n];
    let c = new Set();
    let l = [];
    try {
      while (s.length > 0) {
        let e = [...s];
        s = [];
        let t = new Set();
        let n = e.map(async e => {
          if (c.has(e)) {
            return;
          }
          if (i.filter(t => {
            return t.target === e;
          }).some(e => {
            return r.has(e.source) && !c.has(e.source);
          })) {
            t.add(e);
            return;
          }
          c.add(e);
          let n = J().find(t => {
            return t.id === e;
          });
          if (!n) {
            return;
          }
          let a = null;
          try {
            let t = n.data;
            console.log(`node`, n);
            console.log(`[InfiniteCanvas] 准备执行节点:`, e, `type=`, n.type, `data=`, {
              loading: t?.loading,
              progress: t?.progress,
              hasText: !!t?.text,
              hasImage: !!t?.imageUrl,
              hasVideo: !!t?.videoUrl,
              hasAudio: !!t?.audioUrl,
              hasResult: !!t?.resultData
            });
            if (n.type === `promptNode`) {
              console.log(`[DEBUG] promptNode exec d.selectedModel=`, t.selectedModel, `d.drawingModel=`, t.drawingModel?.substring(0, 30));
              await Sr(e, t.prompt || ``, `1024x1024`, t.selectedModel ? t.selectedModel : t.drawingModel ? t.drawingModel.split(`
`)[0].trim() : undefined);
            } else if (n.type === `textNode`) {
              console.log(`[InfiniteCanvas] 调用文本节点生成:`, e, {
                prompt: t.prompt || ``,
                selectedModel: t.selectedModel ? t.selectedModel : t.textModel ? t.textModel.split(`
`)[0].trim() : undefined,
                isShareRuntime: we
              });
              a = await Lr(e, t.prompt || ``, t.autoSplit || false, t.selectedModel ? t.selectedModel : t.textModel ? t.textModel.split(`
`)[0].trim() : undefined);
            } else if (n.type === `videoNode`) {
              await kr(e, t.prompt || ``, t.aspectRatio || `16:9`, t.size || `1280x720`, t.selectedModel ? t.selectedModel : t.videoModel ? t.videoModel.split(`
`)[0].trim() : undefined);
            } else if (n.type === `sd2VideoNode`) {
              await Er(e, t.prompt || ``, t.size, t.selectedModel ? t.selectedModel : t.sd2VideoModel ? t.sd2VideoModel.split(`
`)[0].trim() : undefined, t.selectedSeconds || t.seconds);
            } else if (n.type === `discountVideoNode`) {
              await Dr(e, t.prompt || ``, t.size, t.selectedModel ? t.selectedModel : t.discountVideoModel ? t.discountVideoModel.split(`
`)[0].trim() : undefined, t.selectedSeconds || t.seconds, t.resolution);
            } else if (n.type === `audioNode`) {
              if (t.onGenerateAudio) {
                await t.onGenerateAudio(e);
              }
            } else if (n.type === `videoExtractNode`) {
              if (t.onExtractFrames) {
                await t.onExtractFrames(e);
              }
            } else if (n.type === `customNode`) {
              await Qr(e);
            } else if (n.type === `rhWebappNode`) {
              window.dispatchEvent(new CustomEvent(zs, {
                detail: {
                  nodeId: e
                }
              }));
              for (let t = 0; t < 600; t++) {
                await new Promise(e => {
                  return setTimeout(e, 1000);
                });
                let t = String(J().find(t => {
                  return t.id === e;
                })?.data?.status || ``).toUpperCase();
                if (t === `SUCCESS` || t === `FAILED` || t === ``) {
                  break;
                }
              }
            }
          } catch (t) {
            let r = t?.message || `运行失败`;
            l.push({
              nodeId: e,
              type: n.type,
              label: n.data?.label || n.data?.title || ``,
              message: r
            });
            console.error(`Error running node ${e}`, t);
            return;
          }
          await new Promise(e => {
            return setTimeout(e, 150);
          });
          if (J().find(t => {
            return t.id === e;
          })?.data.errorMessage) {
            console.warn(`Node ${e} failed, stopping branch.`);
            k(`节点 ${e} 运行失败，停止后续节点运行`);
            return;
          }
          if (a && a.splitNodes) {
            let e = Y();
            a.splitNodes.forEach(n => {
              c.add(n);
              r.add(n);
              e.filter(e => {
                return e.source === n;
              }).forEach(e => {
                r.add(e.target);
                t.add(e.target);
              });
            });
          } else {
            Y().filter(t => {
              return t.source === e;
            }).forEach(e => {
              return t.add(e.target);
            });
          }
          let s = J().find(t => {
            return t.id === e;
          });
          let u = s?.data?.errorMessage || s?.data?.errorMsg;
          console.log(`[InfiniteCanvas] checkpoint update: nodeId=${e}, hasError=${!!u}, errorMessage=${s?.data?.errorMessage}`);
          if (!u) {
            o.completedNodes.push(e);
            o.updatedAt = Date.now();
            mr(o);
          }
        });
        await Promise.all(n);
        if (t.size > 0 && Array.from(t).some(e => {
          return !c.has(e);
        })) {
          s = Array.from(t).filter(e => {
            return !c.has(e);
          });
        }
      }
    } finally {
      if (l.length === 0) {
        console.log(`[InfiniteCanvas] 工作流完成，清理检查点`);
        hr(e);
      } else {
        console.log(`[InfiniteCanvas] 工作流中断(${l.length}个错误)，保留检查点`);
      }
    }
    k(`依次运行完成`);
    return l;
  }, [J, Y, Sr, Lr, kr, Er, Dr, Qr, k, e]);
  let ti = Z.useCallback(async () => {
    let e = J().filter(e => {
      return e.type !== `stickyNoteNode` && e.type !== `group` && e.type !== `ghostTarget`;
    });
    let t = Y();
    let n = e.filter(e => {
      return !t.some(t => {
        return t.target === e.id;
      });
    }).map(e => {
      return e.id;
    });
    if (n.length === 0) {
      k(`没有可运行的节点`);
      return;
    }
    return await ei(n);
  }, [J, Y, ei, k]);
  let ni = Z.useCallback(async e => {
    let t = e.nodeExecOrder.find(t => {
      return !e.completedNodes.includes(t);
    });
    if (!t) {
      k(`没有需要恢复的节点`);
      return;
    }
    k(`正在恢复工作流...`);
    return await ei(t);
  }, [ei, k]);
  Z.useEffect(() => {
    let e = e => {
      let t = e.detail || {};
      if (!t.projectId || t.projectId === Dn.current) {
        if (t.checkpoint) {
          ni(t.checkpoint);
        }
      }
    };
    window.addEventListener(`canvas-resume-workflow`, e);
    return () => {
      return window.removeEventListener(`canvas-resume-workflow`, e);
    };
  }, [ni, Dn]);
  Z.useEffect(() => {
    let e = e => {
      let t = e.detail || {};
      console.log(`[InfiniteCanvas] 收到工作流运行事件:`, t);
      if (t.targetProjectId && t.targetProjectId !== Dn.current) {
        return;
      }
      let n = Array.isArray(t.injections) ? t.injections : [];
      if (we) {
        Te.current = [];
        let e = new Set([`loading`, `progress`, `status`, `taskId`, `requestData`, `responseData`, `errorMsg`, `errorMessage`, `resultData`, `customResultData`, `text`, `imageUrl`, `videoUrl`, `audioUrl`, `thumbnailUrl`, `imageAvailable`, `imageUrlRef`, `imageUrlThumbRef`, `imageUrlUploaded`, `extractedImages`, `allExtractedImages`, `running`]);
        K(t => {
          return t.map(t => {
            let r = {
              ...(t.data || {})
            };
            for (let t of e) {
              delete r[t];
            }
            let i = n.filter(e => {
              return e.nodeId === t.id;
            });
            for (let e of i) {
              let t = e.path.replace(/^data\./, ``);
              r[t] = e.value;
            }
            if (i.length > 0) {
              r.hasChanged = true;
            }
            return {
              ...t,
              data: r
            };
          });
        });
        window.setTimeout(() => {
          r();
        }, 150);
        return;
      }
      if (n.length > 0) {
        K(e => {
          return e.map(e => {
            let t = n.filter(t => {
              return t.nodeId === e.id;
            });
            if (t.length === 0) {
              return e;
            }
            let r = {
              ...e.data
            };
            for (let e of t) {
              let t = e.path.replace(/^data\./, ``);
              r[t] = e.value;
            }
            r.hasChanged = true;
            return {
              ...e,
              data: r
            };
          });
        });
      }
      async function r() {
        let e = e => {
          let t = e?.data || {};
          return !!t.loading || t.progress !== undefined || !!t.status || !!t.taskId || !!t.requestData || !!t.responseData || !!t.errorMsg || !!t.errorMessage || !!t.resultData || !!t.customResultData || !!t.text || !!t.imageUrl || !!t.videoUrl || !!t.audioUrl || !!t.thumbnailUrl || !!t.imageAvailable || !!t.imageUrlRef || !!t.imageUrlThumbRef || !!t.imageUrlUploaded || !!t.extractedImages || !!t.allExtractedImages || !!t.running;
        };
        let t = [];
        try {
          if (we) {
            let t = Date.now();
            while (Date.now() - t < 3000 && J().some(e)) {
              await new Promise(e => {
                return setTimeout(e, 50);
              });
            }
          }
          console.log(`[InfiniteCanvas] 开始执行工作流，roots will run now`);
          t = (await ti()) || [];
        } finally {
          let e = () => {
            let e = J();
            let t = Y();
            return e.filter(e => {
              return e.type !== `stickyNoteNode` && e.type !== `group` && e.type !== `ghostTarget` && !t.some(t => {
                return t.source === e.id;
              });
            }).map(e => {
              let t = e.data || {};
              return {
                nodeId: e.id,
                type: e.type,
                label: t.label || t.title || ``,
                imageUrl: t.imageUrl || undefined,
                videoUrl: t.videoUrl || undefined,
                audioUrl: t.audioUrl || undefined,
                text: typeof t.text == `string` && t.text ? t.text : typeof t.resultData == `string` ? t.resultData : undefined,
                loading: !!t.loading
              };
            });
          };
          let n = e();
          for (let t = 0; t < 120; t++) {
            let r = n.some(e => {
              return e.loading;
            });
            let i = n.some(e => {
              return e.imageUrl || e.videoUrl || e.audioUrl || e.text;
            });
            if (!r && i || !r && t > 2) {
              break;
            }
            await new Promise(e => {
              return setTimeout(e, 1000);
            });
            n = e();
          }
          let r = n.filter(e => {
            return e.imageUrl || e.videoUrl || e.audioUrl || e.text;
          }).map(({
            loading: e,
            ...t
          }) => {
            return t;
          });
          let i = J().map(e => {
            let t = e.data || {};
            let n = t.errorMessage || t.errorMsg;
            if (n) {
              return {
                nodeId: e.id,
                type: e.type,
                label: t.label || t.title || ``,
                message: String(n)
              };
            } else {
              return null;
            }
          }).filter(Boolean);
          let a = t.length > 0 ? t : i;
          console.log(`[InfiniteCanvas] 派发工作流完成事件, results=`, r.length, `errors=`, a.length);
          window.dispatchEvent(new CustomEvent(M_, {
            detail: {
              targetProjectId: Dn.current,
              results: r,
              errors: a
            }
          }));
        }
      }
      window.setTimeout(r, we ? 120 : 60);
    };
    window.addEventListener(A_, e);
    return () => {
      return window.removeEventListener(A_, e);
    };
  }, [ti, K, J, Y]);
  let ii = (e, t) => {
    let n = `${e.id}:${t.slice().sort().join(`,`)}`;
    let r = Bt.current.get(n);
    if (r) {
      return r;
    }
    let i = (async () => {
      let n = (e.data.shots || []).filter(e => {
        return t.includes(e.id);
      });
      let r = e.data.assets || [];
      let i = Array.from(new Set(n.flatMap(e => {
        let t = `${e.description || ``} ${e.prompt || ``} ${e.videoPrompt || ``} ${e.dialogue || ``}`;
        return r.filter(e => {
          return e?.name && e?.imageUrl && Fa(t, e.name);
        }).map(e => {
          return e.imageUrl;
        });
      })));
      let a = {
        ...(e.data.videoUploadedAssets || {})
      };
      let o = {
        ...(e.data.videoAssetUploadStatus || {})
      };
      let s = {
        ...(e.data.videoAssetUploadErrors || {})
      };
      let c = i.filter(e => {
        return !a[e];
      });
      if (c.length === 0) {
        return a;
      }
      c.forEach(e => {
        o[e] = `uploading`;
        delete s[e];
      });
      K(t => {
        return t.map(t => {
          if (t.id === e.id) {
            return {
              ...t,
              data: {
                ...t.data,
                videoUploadedAssets: a,
                videoAssetUploadStatus: o,
                videoAssetUploadErrors: s,
                videoAssetUploadProgress: {
                  completed: 0,
                  total: c.length,
                  status: `uploading`
                }
              }
            };
          } else {
            return t;
          }
        });
      });
      try {
        let t = 0;
        let n = Math.min(6, c.length);
        let r = 0;
        let i = async () => {
          while (r < c.length) {
            let n = r++;
            let i = c[n];
            let l = await wr(i, `image`, n);
            if (!l) {
              throw Error(`素材上传返回为空`);
            }
            a[i] = l;
            o[i] = `uploaded`;
            t += 1;
            K(n => {
              return n.map(n => {
                if (n.id === e.id) {
                  return {
                    ...n,
                    data: {
                      ...n.data,
                      videoUploadedAssets: {
                        ...a
                      },
                      videoAssetUploadStatus: {
                        ...o
                      },
                      videoAssetUploadErrors: {
                        ...s
                      },
                      videoAssetUploadProgress: {
                        completed: t,
                        total: c.length,
                        status: `uploading`
                      }
                    }
                  };
                } else {
                  return n;
                }
              });
            });
          }
        };
        await Promise.all(Array.from({
          length: n
        }, () => {
          return i();
        }));
        K(t => {
          return t.map(t => {
            if (t.id === e.id) {
              return {
                ...t,
                data: {
                  ...t.data,
                  videoUploadedAssets: a,
                  videoAssetUploadStatus: o,
                  videoAssetUploadErrors: s,
                  videoAssetUploadProgress: {
                    completed: c.length,
                    total: c.length,
                    status: `done`
                  }
                }
              };
            } else {
              return t;
            }
          });
        });
        return a;
      } catch (t) {
        let n = t instanceof Error ? t.message : String(t);
        c.filter(e => {
          return !a[e];
        }).forEach(e => {
          o[e] = `failed`;
          s[e] = n;
        });
        K(t => {
          return t.map(t => {
            if (t.id === e.id) {
              return {
                ...t,
                data: {
                  ...t.data,
                  videoUploadedAssets: a,
                  videoAssetUploadStatus: o,
                  videoAssetUploadErrors: s,
                  videoAssetUploadProgress: {
                    completed: Object.keys(a).length,
                    total: c.length,
                    status: `error`,
                    message: n
                  }
                }
              };
            } else {
              return t;
            }
          });
        });
        throw t;
      }
    })();
    Bt.current.set(n, i);
    i.finally(() => {
      return Bt.current.delete(n);
    });
    return i;
  };
  let ai = e => {
    let t = Tn.current.find(t => {
      return t.id === e;
    });
    if (!t) {
      return;
    }
    let n = (t.data.shots || []).map(e => {
      return e.id;
    });
    let r = (t.data.assets || []).filter(e => {
      return e?.imageUrl;
    });
    if (r.length === 0) {
      k(`暂无已生成的图片资产`);
      return;
    }
    k(`已加入后台上传队列，请留意状态变化`);
    ii({
      ...t,
      data: {
        ...t.data,
        shots: (t.data.shots || []).map(e => {
          return {
            ...e,
            description: `${e.description || ``} ${r.map(e => {
              return `@${e.name}`;
            }).join(` `)}`
          };
        })
      }
    }, n).catch(() => {
      return undefined;
    });
  };
  let oi = (e, t) => {
    let n = Tn.current.find(t => {
      return t.id === e;
    });
    let r = (n?.data.assets || []).find(e => {
      return e.id === t;
    });
    if (!n || !r?.imageUrl) {
      return;
    }
    let i = {
      ...(n.data.videoAssetUploadStatus || {})
    };
    let a = {
      ...(n.data.videoUploadedAssets || {})
    };
    delete a[r.imageUrl];
    i[r.imageUrl] = `uploading`;
    K(t => {
      return t.map(t => {
        if (t.id === e) {
          return {
            ...t,
            data: {
              ...t.data,
              videoUploadedAssets: a,
              videoAssetUploadStatus: i
            }
          };
        } else {
          return t;
        }
      });
    });
    let o = (n.data.shots || []).filter(e => {
      return Fa(`${e.description || ``} ${e.prompt || ``} ${e.videoPrompt || ``} ${e.dialogue || ``}`, r.name);
    }).map(e => {
      return e.id;
    });
    ii({
      ...n,
      data: {
        ...n.data,
        videoUploadedAssets: a,
        videoAssetUploadStatus: i
      }
    }, o).catch(() => {
      return undefined;
    });
  };
  let li = async (e, t, n) => {
    let r = Tn.current.find(t => {
      return t.id === e;
    });
    if (!r) {
      return;
    }
    let i = n === `image` ? 260 : 420;
    let a = Math.max(r.measured?.width || 0, Number(r.style?.width) || 0, 860);
    let o = r.position.x + a + i;
    let s = r.position.y + 80;
    let c = r.data.videoUploadedAssets || {};
    let l = n === `video` ? ii(r, [t]) : null;
    let u = (r.data.shots || []).find(e => {
      return e.id === t;
    });
    let d = String(r.data.aspectRatio || `16:9`);
    let f = d === `custom` ? String(r.data.customAspectRatio || `16:9`) : d === `4:4` ? `1:1` : d;
    let p = String(Math.max(1, Number.parseInt(u?.duration || `5`, 10) || 5));
    let m = {};
    if (n === `image` && S) {
      m.drawingModel = S;
    }
    if (n === `video` && T) {
      m.selectedModel = T;
    }
    let h = di(n === `image` ? `promptNode` : `discountVideoNode`, {
      x: o,
      y: s
    }, {
      expanded: true,
      aspectRatio: f,
      ...m,
      ...(n === `video` ? {
        size: f,
        selectedSeconds: p,
        uploadedAssets: c,
        scriptAssetUploadPending: !!l
      } : {})
    }, {
      source: e,
      sourceHandle: `shot-${t}`
    });
    if (l) {
      l.then(e => {
        return K(t => {
          return t.map(t => {
            if (t.id === h) {
              return {
                ...t,
                data: {
                  ...t.data,
                  uploadedAssets: e,
                  scriptAssetUploadPending: false
                }
              };
            } else {
              return t;
            }
          });
        });
      }).catch(() => {
        return K(e => {
          return e.map(e => {
            if (e.id === h) {
              return {
                ...e,
                data: {
                  ...e.data,
                  scriptAssetUploadPending: false,
                  errorMessage: `素材连接失败，请重试`
                }
              };
            } else {
              return e;
            }
          });
        });
      });
    }
  };
  let ui = async (e, t, n) => {
    let r = Tn.current.find(t => {
      return t.id === e;
    });
    if (!r) {
      return;
    }
    let i = r.data.shots || [];
    let a = t.length ? t : i.map(e => {
      return e.id;
    });
    let o = r.data.videoUploadedAssets || {};
    let s = n === `video` ? ii(r, a) : null;
    let c = Math.max(r.measured?.width || 0, Number(r.style?.width) || 0, 860);
    let l = n === `image` ? 260 : 420;
    let u = r.position.x + c + l;
    let d = n === `image` ? 420 : 500;
    let f = n === `image` ? 100 : 180;
    let p = n === `image` ? 100 : 160;
    let m = String(r.data.projectName || r.data.label || `剧本项目`);
    let h = String(r.data.aspectRatio || `16:9`);
    let g = h === `custom` ? String(r.data.customAspectRatio || `16:9`) : h === `4:4` ? `1:1` : h;
    let _ = `group-script-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`;
    let v = Math.max(1, Math.min(4, a.length));
    let y = Math.max(1, Math.ceil(a.length / 4));
    let b = u - 40;
    let x = r.position.y + 30;
    let C = {
      id: _,
      type: `group`,
      position: {
        x: b,
        y: x
      },
      style: {
        width: v * 420 + Math.max(0, v - 1) * f + 100,
        height: y * d + Math.max(0, y - 1) * p + 120,
        zIndex: -1
      },
      data: {
        name: m
      },
      zIndex: -1
    };
    K(e => {
      return [...e, C];
    });
    let w = [];
    a.forEach((t, a) => {
      let c = a % 4;
      let l = Math.floor(a / 4);
      let m = i.find(e => {
        return e.id === t;
      });
      let h = `镜${m?.index || a + 1}-${n === `image` ? `生图` : `视频`}`;
      let v = String(Math.max(1, Number.parseInt(m?.duration || `5`, 10) || 5));
      let y = {};
      if (n === `image` && S) {
        y.drawingModel = S;
      }
      if (n === `video` && T) {
        y.selectedModel = T;
      }
      let C = n === `image` ? {
        expanded: true,
        label: h,
        aspectRatio: g,
        ...y
      } : {
        expanded: true,
        label: h,
        size: g,
        selectedSeconds: v,
        uploadedAssets: o,
        scriptAssetUploadPending: !!s,
        ...y
      };
      let E = di(n === `image` ? `promptNode` : `discountVideoNode`, {
        x: u + c * (420 + f),
        y: r.position.y + 80 + l * (d + p)
      }, C, {
        source: e,
        sourceHandle: `shot-${t}`
      }, {
        id: _,
        x: b,
        y: x
      });
      w.push(E);
    });
    if (s) {
      s.then(e => {
        return K(t => {
          return t.map(t => {
            if (w.includes(t.id)) {
              return {
                ...t,
                data: {
                  ...t.data,
                  uploadedAssets: e,
                  scriptAssetUploadPending: false
                }
              };
            } else {
              return t;
            }
          });
        });
      }).catch(() => {
        return K(e => {
          return e.map(e => {
            if (w.includes(e.id)) {
              return {
                ...e,
                data: {
                  ...e.data,
                  scriptAssetUploadPending: false,
                  errorMessage: `素材连接失败，请重试`
                }
              };
            } else {
              return e;
            }
          });
        });
      });
    }
  };
  let di = (e, t, n = {}, r, i) => {
    console.log(`>>>>>>>>>>>>>>addNodeWithData`, n);
    let a = {
      ...n
    };
    let o = r ? Tn.current.find(e => {
      return e.id === r.source;
    }) : undefined;
    if (o?.type === `scriptBoxNode` && r?.sourceHandle?.startsWith(`shot-`) && (e === `promptNode` || e === `discountVideoNode`)) {
      let t = r.sourceHandle.slice(5);
      let n = (o.data.shots || []).find(e => {
        return e.id === t;
      });
      let i = String(o.data.aspectRatio || `16:9`);
      let s = i === `custom` ? String(o.data.customAspectRatio || `16:9`) : i;
      if (e === `promptNode`) {
        a = {
          ...a,
          aspectRatio: s === `4:4` ? `1:1` : s
        };
      } else {
        a = {
          ...a,
          size: s === `4:4` ? `1:1` : s,
          selectedSeconds: String(Math.max(1, Number.parseInt(n?.duration || `5`, 10) || 5)),
          durationFromScript: true
        };
      }
    }
    let s = `${e}-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`;
    let c = {
      id: s,
      type: e,
      position: i ? {
        x: t.x - i.x,
        y: t.y - i.y
      } : t,
      parentId: i?.id,
      extent: i ? `parent` : undefined,
      style: e === `promptNode` || e === `imageNode` || e === `sd2VideoNode` || e === `videoNode` || e === `videoExtractNode` || e === `imageBoxNode` || e === `discountVideoNode` ? {
        width: 420,
        height: 420
      } : e === `videoProcessNode` ? {
        width: 560,
        height: 680
      } : e === `scriptBoxNode` ? {
        width: 860
      } : e === `videoToGifNode` ? {
        width: 380,
        height: 270
      } : e === `imageCompressNode` ? {
        width: 360,
        height: 230
      } : e === `faceMosaicNode` ? {
        width: 360,
        height: 300
      } : e === `compareNode` ? {
        width: 420,
        height: 400
      } : e === `panoramaNode` ? {
        width: 512,
        height: 288
      } : e === `director3dNode` ? {
        width: 360,
        height: 300
      } : e === `rhWebappNode` ? {
        width: 820,
        height: 720
      } : undefined,
      data: {
        ...a,
        hasChanged: true,
        expanded: a.expanded === undefined ? e === `promptNode` || e === `textNode` || e === `videoNode` ? true : undefined : a.expanded,
        onGenerate: e === `promptNode` ? Hr : undefined,
        onGenerateText: e === `textNode` ? Ur : undefined,
        onGenerateScript: e === `scriptBoxNode` ? Ar : undefined,
        onGenerateAssetImage: e === `scriptBoxNode` ? Pr : undefined,
        onGenerateAllAssetImages: e === `scriptBoxNode` ? Fr : undefined,
        onRetryVideoAssetUpload: e === `scriptBoxNode` ? oi : undefined,
        onUploadAllVideoAssets: e === `scriptBoxNode` ? ai : undefined,
        onGenerateShotPrompts: e === `scriptBoxNode` ? Ir : undefined,
        onConnectShot: e === `scriptBoxNode` ? li : undefined,
        onConnectShots: e === `scriptBoxNode` ? ui : undefined,
        onStopScriptItem: e === `scriptBoxNode` ? Un : undefined,
        onGenerateVideo: e === `videoNode` ? Gr : undefined,
        onGenerateSD2Video: e === `sd2VideoNode` ? qr : undefined,
        onGenerateDiscountVideo: e === `discountVideoNode` ? Jr : undefined,
        onGenerateCustom: e === `customNode` ? Qr : undefined,
        onAIAssist: e === `customNode` ? Zr : undefined,
        onSaveTemplate: e === `customNode` ? (e, t) => {
          if (te) {
            te({
              id: Date.now().toString(),
              name: e,
              config: t
            });
          }
        } : undefined,
        onCrop: e === `imageNode` || e === `promptNode` ? ar : undefined,
        onSplit: e === `gridSplitNode` ? lr : undefined,
        onSplitOne: e === `gridSplitNode` ? cr : undefined,
        onSplitManual: e === `gridSplitNode` ? ur : undefined,
        onSplitOneManual: e === `gridSplitNode` ? dr : undefined,
        onSpawnImageNode: e === `gridMergeNode` || e === `videoToGifNode` || e === `videoProcessNode` || e === `imageCompressNode` || e === `faceMosaicNode` || e === `compareNode` ? fr : undefined,
        onSpawnAudioNode: e === `videoProcessNode` ? gr : undefined,
        onZoom: Rn,
        onEdit: e === `imageNode` || e === `promptNode` ? zn : undefined,
        onAddImage: e === `promptNode` || e === `textNode` || e === `videoNode` || e === `sd2VideoNode` || e === `discountVideoNode` || e === `videoToGifNode` || e === `imageCompressNode` || e === `faceMosaicNode` ? _r : undefined,
        onPushImagesToImageBox: e === `imageCompressNode` || e === `faceMosaicNode` ? (e, t) => {
          if (En.current.some(t => {
            return t.source === e && Tn.current.find(e => {
              return e.id === t.target;
            })?.type === `imageBoxNode`;
          })) {
            return or.current(e, t);
          } else {
            return null;
          }
        } : undefined,
        onStop: e === `promptNode` || e === `textNode` || e === `videoNode` || e === `sd2VideoNode` || e === `customNode` || e === `videoExtractNode` || e === `discountVideoNode` || e === `scriptBoxNode` ? Vn : undefined,
        onRefresh: e === `videoNode` || e === `sd2VideoNode` || e === `textNode` || e === `promptNode` || e === `discountVideoNode` ? Yr : undefined,
        onShowToast: e === `textNode` || e === `audioNode` || e === `customNode` || e === `videoExtractNode` || e === `videoToGifNode` || e === `videoProcessNode` || e === `imageCompressNode` || e === `faceMosaicNode` || e === `compareNode` || e === `sd2VideoNode` || e === `imageBoxNode` || e === `discountVideoNode` || e === `rhWebappNode` || e === `director3dNode` ? k : undefined,
        onCaptureToBox: e === `director3dNode` || e === `panoramaNode` ? br : undefined,
        onUploadAsset: e === `sd2VideoNode` ? Cr : e === `discountVideoNode` ? wr : undefined,
        aiAppApiUrl: e === `rhWebappNode` ? h : undefined,
        aiAppApiKey: e === `rhWebappNode` ? _ : undefined,
        membershipType: e === `rhWebappNode` ? N?.type : undefined,
        addTransitResource: e === `rhWebappNode` ? j : undefined,
        updateGlobalTasks: e === `rhWebappNode` ? I : undefined,
        presetPrompts: M,
        onSendToActiveTab: e === `promptNode` || e === `imageNode` || e === `imageBoxNode` ? ee : undefined,
        videoModel: e === `videoNode` ? C : undefined,
        drawingModel: e === `promptNode` ? S : undefined,
        textModel: e === `textNode` || e === `scriptBoxNode` || e === `customNode` ? x : undefined,
        drawingModelForScript: e === `scriptBoxNode` ? S : undefined,
        transitResources: e === `scriptBoxNode` ? A : undefined,
        audioApiUrl: e === `audioNode` ? y : undefined,
        audioApiKey: e === `audioNode` ? b : undefined,
        audioModel: e === `audioNode` ? O : undefined,
        videoDurations: e === `videoNode` || e === `sd2VideoNode` || e === `discountVideoNode` ? D : undefined,
        sd2VideoModel: e === `sd2VideoNode` ? w : undefined,
        discountVideoModel: e === `discountVideoNode` ? T : undefined
      }
    };
    console.log(`newNode>>>>`, c);
    K(e => {
      return e.concat(c);
    });
    if (r) {
      let t = {
        id: `e-${r.source}-${s}`,
        source: r.source,
        sourceHandle: r.sourceHandle,
        target: s,
        targetHandle: null
      };
      if (Tn.current.find(e => {
        return e.id === r.source;
      })?.type === `imageNode` && (e === `promptNode` || e === `textNode` || e === `videoNode`)) {
        t.animated = true;
      }
      q(e => {
        return e.concat(t);
      });
    }
    Ie(null);
    return s;
  };
  let fi = Z.useRef(li);
  fi.current = li;
  let pi = Z.useRef(ui);
  pi.current = ui;
  Z.useEffect(() => {
    K(e => {
      return e.map(e => {
        if (e.type === `scriptBoxNode`) {
          return {
            ...e,
            data: {
              ...e.data,
              onConnectShot: fi.current,
              onConnectShots: pi.current
            }
          };
        } else {
          return e;
        }
      });
    });
  }, [K]);
  Z.useEffect(() => {
    let e = e => {
      let t = e.detail;
      if (t?.nodeId && t.shotId && t.target) {
        fi.current(t.nodeId, t.shotId, t.target);
      }
    };
    let t = e => {
      let t = e.detail;
      if (t?.nodeId && t.target) {
        pi.current(t.nodeId, t.shotIds || [], t.target);
      }
    };
    window.addEventListener(`script-box-connect-shot`, e);
    window.addEventListener(`script-box-connect-shots`, t);
    return () => {
      window.removeEventListener(`script-box-connect-shot`, e);
      window.removeEventListener(`script-box-connect-shots`, t);
    };
  }, [K]);
  Z.useEffect(() => {
    let e = async e => {
      let t = e.detail;
      if (!t?.url || t.type === `folder`) {
        return;
      }
      let n = Le.current?.getBoundingClientRect();
      let {
        open: r,
        width: i
      } = ze.current;
      let a = n ? n.width - (r ? i : 0) : 0;
      let o = n ? Ve({
        x: n.left + a / 2,
        y: n.top + n.height / 2
      }) : {
        x: 0,
        y: 0
      };
      if (t.type === `text` || t.type.startsWith(`text/`)) {
        let e = t.url;
        if (/^https?:\/\//i.test(t.url)) {
          try {
            let n = await fetch(t.url);
            if (n.ok) {
              e = await n.text();
            }
          } catch {}
        }
        di(`textNode`, o, {
          text: e,
          label: t.pageTitle || `文本素材`
        });
      } else if (t.type === `video` || t.type.startsWith(`video/`)) {
        di(`videoExtractNode`, o, {
          videoUrl: t.url,
          videoName: t.pageTitle || `视频素材`
        });
      } else if (t.type === `audio` || t.type.startsWith(`audio/`)) {
        di(`audioPlayerNode`, o, {
          audioUrl: t.url,
          audioName: t.pageTitle || `音频素材`
        });
      } else {
        di(`imageNode`, o, {
          imageUrl: t.url,
          label: t.pageTitle || `图片素材`
        });
      }
    };
    window.addEventListener(Hn, e);
    return () => {
      return window.removeEventListener(Hn, e);
    };
  }, [Ve]);
  Z.useEffect(() => {
    if (ve) {
      ve.current = {
        listNodes: () => {
          return Tn.current.map(e => {
            return {
              id: e.id,
              type: e.type,
              position: e.position,
              label: e.data?.label,
              selected: e.selected,
              data: e.data
            };
          });
        },
        listEdges: () => {
          return En.current.map(e => {
            return {
              id: e.id,
              source: e.source,
              target: e.target,
              sourceHandle: e.sourceHandle,
              targetHandle: e.targetHandle
            };
          });
        },
        addNode: (e, t, n = {}, r) => {
          let i = t || (() => {
            let e = Le.current?.getBoundingClientRect();
            if (!e) {
              return {
                x: 0,
                y: 0
              };
            }
            let {
              open: t,
              width: n
            } = ze.current;
            let r = e.width - (t ? n : 0);
            return Ve({
              x: e.left + r / 2,
              y: e.top + e.height / 2
            });
          })();
          let a = `${e}-${Date.now()}`;
          let o = {
            id: a,
            type: e,
            position: i,
            style: e === `promptNode` || e === `imageNode` || e === `discountVideoNode` || e === `videoNode` ? {
              width: 420,
              height: 420
            } : undefined,
            data: {
              ...n,
              hasChanged: true,
              expanded: n.expanded === undefined ? e === `promptNode` || e === `textNode` ? true : undefined : n.expanded,
              onGenerate: e === `promptNode` ? Hr : undefined,
              onGenerateText: e === `textNode` ? Ur : undefined,
              onGenerateDiscountVideo: e === `discountVideoNode` ? Jr : undefined,
              onCrop: e === `imageNode` || e === `promptNode` ? ar : undefined,
              onZoom: Rn,
              onEdit: e === `imageNode` || e === `promptNode` ? zn : undefined,
              onAddImage: e === `promptNode` || e === `textNode` || e === `discountVideoNode` ? _r : undefined,
              onStop: e === `promptNode` || e === `textNode` || e === `discountVideoNode` ? Vn : undefined,
              onRefresh: e === `textNode` || e === `promptNode` || e === `discountVideoNode` ? Yr : undefined,
              onShowToast: k,
              onUploadAsset: e === `discountVideoNode` ? wr : undefined,
              presetPrompts: M,
              drawingModel: e === `promptNode` ? S : undefined,
              textModel: e === `textNode` ? x : undefined,
              discountVideoModel: e === `discountVideoNode` ? T : undefined,
              videoDurations: e === `discountVideoNode` ? D : undefined
            }
          };
          K(e => {
            return e.concat(o);
          });
          if (r) {
            let e = {
              id: `e-${r.source}-${a}`,
              source: r.source,
              sourceHandle: r.sourceHandle ?? null,
              target: a,
              targetHandle: null
            };
            q(t => {
              return t.concat(e);
            });
          }
          return a;
        },
        updateNode: (e, t) => {
          let n = false;
          K(r => {
            return r.map(r => {
              if (r.id === e) {
                n = true;
                return {
                  ...r,
                  data: {
                    ...r.data,
                    ...t,
                    hasChanged: true
                  }
                };
              } else {
                return r;
              }
            });
          });
          return n;
        },
        deleteNode: e => {
          let t = Tn.current.some(t => {
            return t.id === e;
          });
          K(t => {
            return t.filter(t => {
              return t.id !== e;
            });
          });
          q(t => {
            return t.filter(t => {
              return t.source !== e && t.target !== e;
            });
          });
          return t;
        },
        connectNodes: (e, t, n) => {
          if (e === t) {
            return false;
          }
          if (En.current.some(n => {
            return n.source === e && n.target === t;
          })) {
            return true;
          }
          let r = {
            id: `e-${e}-${t}-${Date.now()}`,
            source: e,
            sourceHandle: n?.sourceHandle ?? null,
            target: t,
            targetHandle: n?.targetHandle ?? null,
            type: `default`
          };
          q(e => {
            return e.concat(r);
          });
          return true;
        },
        triggerGeneration: e => {
          let t = Tn.current.find(t => {
            return t.id === e;
          });
          if (!t) {
            return false;
          }
          let n = t.data;
          try {
            if (t.type === `promptNode` && typeof n.onGenerate == `function`) {
              n.onGenerate(e, n.prompt || ``, n.aspectRatio || `1024x1024`, n.selectedModel, n.apiFormat || `auto`, n.batchSize || 1);
              return true;
            } else if (t.type === `textNode` && typeof n.onGenerateText == `function`) {
              n.onGenerateText(e, n.prompt || ``, n.autoSplit || false, n.selectedModel);
              return true;
            } else if (t.type === `discountVideoNode` && typeof n.onGenerateDiscountVideo == `function`) {
              n.onGenerateDiscountVideo(e, n.prompt || ``, n.size || `16:9`, n.selectedModel, n.selectedSeconds, n.resolution);
              return true;
            } else {
              return false;
            }
          } catch (e) {
            console.error(`[Agent] triggerGeneration 失败:`, e);
            return false;
          }
        },
        selectNode: (e, t = true) => {
          K(n => {
            return n.map(n => {
              if (n.id === e) {
                return {
                  ...n,
                  selected: t
                };
              } else {
                return n;
              }
            });
          });
        },
        selectNodes: (e, t = true) => {
          let n = new Set(e);
          let r = 0;
          K(e => {
            return e.map(e => {
              if (n.has(e.id)) {
                r++;
                return {
                  ...e,
                  selected: t
                };
              } else {
                return e;
              }
            });
          });
          return r;
        },
        clearSelection: () => {
          K(e => {
            return e.map(e => {
              return {
                ...e,
                selected: false
              };
            });
          });
          q(e => {
            return e.map(e => {
              return {
                ...e,
                selected: false
              };
            });
          });
        },
        deleteEdge: (e, t, n) => {
          let r = false;
          q(i => {
            return i.filter(i => {
              if (e ? i.id === e : i.source === t && i.target === n) {
                r = true;
                return false;
              } else {
                return true;
              }
            });
          });
          return r;
        },
        moveNode: (e, t) => {
          let n = false;
          K(r => {
            return r.map(r => {
              if (r.id === e) {
                n = true;
                return {
                  ...r,
                  position: t,
                  data: {
                    ...r.data,
                    hasChanged: true
                  }
                };
              } else {
                return r;
              }
            });
          });
          return n;
        },
        duplicateNode: (e, t) => {
          let n = Tn.current.find(t => {
            return t.id === e;
          });
          if (!n) {
            return null;
          }
          let r = t?.x ?? 50;
          let i = t?.y ?? 50;
          let a = `${n.type || `node`}-${Date.now()}`;
          let o = {};
          if (n.data) {
            for (let [e, t] of Object.entries(n.data)) {
              if (typeof t != `function`) {
                o[e] = t;
              }
            }
          }
          o.hasChanged = true;
          delete o.imageUrl;
          delete o.taskId;
          delete o.loading;
          delete o.errorMsg;
          let s = {
            ...n,
            id: a,
            position: {
              x: n.position.x + r,
              y: n.position.y + i
            },
            selected: false,
            data: o
          };
          K(e => {
            return e.concat(s);
          });
          return a;
        },
        setNodeLocked: (e, t) => {
          let n = false;
          K(r => {
            return r.map(r => {
              if (r.id === e) {
                n = true;
                return {
                  ...r,
                  draggable: !t,
                  selectable: !t,
                  data: {
                    ...r.data,
                    _locked: t
                  }
                };
              } else {
                return r;
              }
            });
          });
          return n;
        },
        isNodeLocked: e => {
          return !!Tn.current.find(t => {
            return t.id === e;
          })?.data?._locked;
        },
        groupNodes: (e, t) => {
          let n = Tn.current.filter(t => {
            return e.includes(t.id) && t.type !== `group`;
          });
          if (n.length < 2) {
            return null;
          }
          let r = Math.min(...n.map(e => {
            return e.position.x;
          }));
          let i = Math.min(...n.map(e => {
            return e.position.y;
          }));
          let a = Math.max(...n.map(e => {
            return e.position.x + (e.measured?.width || 420);
          }));
          let o = Math.max(...n.map(e => {
            return e.position.y + (e.measured?.height || 420);
          }));
          let s = t || `group-${Date.now()}`;
          let c = {
            id: s,
            type: `group`,
            position: {
              x: r - 40,
              y: i - 40
            },
            style: {
              width: a - r + 80,
              height: o - i + 80
            },
            data: {
              label: `分组`
            }
          };
          K(t => {
            let n = new Set(e);
            return [c, ...t.map(e => {
              if (n.has(e.id)) {
                return {
                  ...e,
                  parentId: s,
                  position: {
                    x: e.position.x - (r - 40),
                    y: e.position.y - (i - 40)
                  },
                  selected: false
                };
              } else {
                return e;
              }
            })];
          });
          return s;
        },
        ungroupNodes: e => {
          let t = Tn.current.find(t => {
            return t.id === e;
          });
          if (t) {
            K(n => {
              return n.filter(t => {
                return t.id !== e;
              }).map(n => {
                if (n.parentId === e) {
                  return {
                    ...n,
                    parentId: undefined,
                    extent: undefined,
                    position: {
                      x: n.position.x + t.position.x,
                      y: n.position.y + t.position.y
                    }
                  };
                } else {
                  return n;
                }
              });
            });
            return true;
          } else {
            return false;
          }
        },
        undo: () => {
          if (hn <= 0) {
            return false;
          }
          _n.current = true;
          let e = fn[hn - 1];
          if (e) {
            K(e.nodes);
            q(e.edges);
            gn(hn - 1);
            vn.current = hn - 1;
            setTimeout(() => {
              _n.current = false;
            }, 600);
            return true;
          } else {
            return false;
          }
        },
        redo: () => {
          if (hn >= fn.length - 1) {
            return false;
          }
          _n.current = true;
          let e = fn[hn + 1];
          if (e) {
            K(e.nodes);
            q(e.edges);
            gn(hn + 1);
            vn.current = hn + 1;
            setTimeout(() => {
              _n.current = false;
            }, 600);
            return true;
          } else {
            return false;
          }
        },
        focusNode: e => {
          let t = Tn.current.find(t => {
            return t.id === e;
          });
          if (t) {
            try {
              Ye(t.position.x, t.position.y, {
                zoom: 1,
                duration: 400
              });
            } catch {}
          }
        },
        getViewport: () => {
          return Je();
        },
        setViewport: e => {
          return qe(e);
        },
        fitView: e => {
          Ue({
            padding: e?.padding ?? 0.1,
            duration: e?.duration ?? 300
          });
        },
        zoomIn: () => {
          return Ge();
        },
        zoomOut: () => {
          return Ke();
        },
        showToast: (e, t) => {
          return k(e);
        },
        uploadAsset: wr
      };
      return () => {
        if (ve) {
          ve.current = null;
        }
      };
    }
  }, [ve, K, q, qe, Je, Ye, Ve, Hr, Ur, Jr, ar, Rn, zn, _r, Vn, Yr, wr, k, M, S, x, T, D, fn, hn, Ue, Ge, Ke]);
  let gi = (e = 0) => {
    return Ve({
      x: (Fe?.x || 0) + (Le.current?.getBoundingClientRect().left || 0) + e,
      y: (Fe?.y || 0) + (Le.current?.getBoundingClientRect().top || 0)
    });
  };
  let _i = [{
    type: `textConcatNode`,
    label: `文本拼接`,
    icon: <_Component120 size={15} className={`text-white`} />,
    cat: `text`
  }, {
    type: `audioNode`,
    label: `听音断句`,
    icon: <Be size={15} className={`text-white`} />,
    cat: `text`
  }, {
    type: `imageBoxNode`,
    label: `图片盒子`,
    icon: <_Component30 size={15} className={`text-white`} />,
    cat: `image`,
    data: {
      images: [],
      activeIndex: 0,
      expanded: false
    }
  }, {
    type: `gridSplitNode`,
    label: `图片切分`,
    icon: <Et size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `gridMergeNode`,
    label: `图片拼图`,
    icon: <_Component45 size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `panoramaNode`,
    label: `全景图`,
    icon: <_Component2 size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `director3dNode`,
    label: `3D导演台`,
    icon: <_Component29 size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `imageCompressNode`,
    label: `图片压缩`,
    icon: <_Component26 size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `faceMosaicNode`,
    label: `人脸打码`,
    icon: <_shared.R size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `compareNode`,
    label: `对比工具`,
    icon: <_Component56 size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `urlToImageNode`,
    label: `网址转图片`,
    icon: <_Component113 size={15} className={`text-white`} />,
    cat: `image`
  }, {
    type: `videoNode`,
    label: `其他视频`,
    icon: <Pt size={15} className={`text-white`} />,
    cat: `video`,
    data: {
      prompt: ``
    }
  }, {
    type: `videoExtractNode`,
    label: `视频抽帧`,
    icon: <Pt size={15} className={`text-white`} />,
    cat: `video`
  }, {
    type: `videoToGifNode`,
    label: `视频转GIF`,
    icon: <_Component49 size={15} className={`text-white`} />,
    cat: `video`
  }, {
    type: `videoProcessNode`,
    label: `视频处理`,
    icon: <_Component49 size={15} className={`text-white`} />,
    cat: `video`,
    badge: {
      text: `NEW`,
      tone: `new`
    },
    data: {
      mode: `trim`,
      sourceOrder: [],
      audioFormat: `m4a`,
      trimStart: 0,
      trimEnd: 4,
      resizeWidth: 1280,
      resizeHeight: 720,
      targetFps: 30
    }
  }, {
    type: `customNode`,
    label: `万能节点`,
    icon: <_Component45 size={15} className={`text-white`} />,
    cat: `other`
  }, {
    type: `stickyNoteNode`,
    label: `便签`,
    icon: <_Component121 size={15} className={`text-white`} />,
    cat: `other`,
    data: {
      text: ``,
      fontSize: 24,
      bgColor: `rgba(180,160,60,0.25)`,
      textColor: `rgba(255,255,255,0.92)`
    }
  }, {
    type: `fileToUrlNode`,
    label: `文件转链接`,
    icon: <_Component113 size={15} className={`text-white`} />,
    cat: `other`
  }];
  let vi = [{
    key: `text`,
    label: `文本工具`
  }, {
    key: `image`,
    label: `图片工具`
  }, {
    key: `video`,
    label: `视频工具`
  }, {
    key: `other`,
    label: `其他工具`
  }];
  let yi = e => {
    let t = e.type === `fileToUrlNode` ? {
      cloudStorageConfig: U,
      ...(e.data || {})
    } : e.data || {};
    let n = e.type === `stickyNoteNode` ? undefined : Fe?.connection;
    di(e.type, gi(), t, n);
  };
  let bi = e => {
    if (!!e.target.files && e.target.files.length !== 0) {
      Array.from(e.target.files).forEach(async (e, t) => {
        if (!Fe) {
          return;
        }
        let n = Ve({
          x: Fe.x + (Le.current?.getBoundingClientRect().left || 0) + t * 50,
          y: Fe.y + (Le.current?.getBoundingClientRect().top || 0) + t * 50
        });
        if (e.type.startsWith(`text/`)) {
          let t = new FileReader();
          t.onload = t => {
            let r = t.target?.result;
            di(`textNode`, n, {
              text: r,
              label: e.name
            }, Fe.connection);
          };
          t.readAsText(e);
          return;
        }
        let r = (e.name || ``).toLowerCase();
        let i = e.type.startsWith(`audio/`) || /\.(mp3|wav|ogg|m4a|flac|aac|opus|wma|aiff)$/i.test(r);
        try {
          let t = await hi(e, {
            subfolder: `canvas/upload`,
            preferThumbnail: e.type.startsWith(`image/`),
            thumbMaxDim: 480,
            thumbQuality: 75
          });
          console.log(`[InfiniteCanvas] urlifyAsset`, t);
          if (t.url) {
            if (i) {
              di(`audioPlayerNode`, n, {
                audioUrl: t.url,
                audioName: e.name,
                label: e.name
              }, Fe.connection);
            } else {
              di(`imageNode`, n, {
                imageUrl: t.url,
                thumbnailUrl: t.thumbnailUrl,
                label: e.name
              }, Fe.connection);
            }
            return;
          }
        } catch (e) {
          console.warn(`[InfiniteCanvas.handleFileUpload] urlifyAsset failed, fallback to base64:`, e);
        }
        let a = new FileReader();
        a.onload = t => {
          let r = t.target?.result;
          if (i) {
            di(`audioPlayerNode`, n, {
              audioUrl: r,
              audioName: e.name,
              label: e.name
            }, Fe.connection);
          } else {
            di(`imageNode`, n, {
              imageUrl: r,
              label: e.name
            }, Fe.connection);
          }
        };
        a.readAsDataURL(e);
      });
      e.target.value = ``;
    }
  };
  let xi = Z.useCallback(async (e, t, n, r) => {
    let i = e;
    let a = t || Fe?.connection;
    if (!i && Fe) {
      i = Ve({
        x: Fe.x + (Le.current?.getBoundingClientRect().left || 0),
        y: Fe.y + (Le.current?.getBoundingClientRect().top || 0)
      });
    }
    if (!i) {
      let e = Le.current?.getBoundingClientRect();
      if (e) {
        i = Ve({
          x: e.left + e.width / 2,
          y: e.top + e.height / 2
        });
      } else {
        i = {
          x: 0,
          y: 0
        };
      }
    }
    try {
      let e = n || (await navigator.clipboard.readText().catch(() => {
        return ``;
      }));
      if (e && e.trim()) {
        try {
          let t = JSON.parse(e.trim());
          if (t && t.type === `mutiwindow-nodes`) {
            let {
              nodes: e,
              edges: n
            } = t;
            if (!e || e.length === 0) {
              return;
            }
            let o = Math.min(...e.map(e => {
              return e.position.x;
            }));
            let s = Math.min(...e.map(e => {
              return e.position.y;
            }));
            let c = Math.max(...e.map(e => {
              return e.position.x + (e.measured?.width || 300);
            }));
            let l = Math.max(...e.map(e => {
              return e.position.y + (e.measured?.height || 300);
            }));
            let u = (o + c) / 2;
            let d = (s + l) / 2;
            let f = new Map();
            let p = e.map(e => {
              let t = `${e.type}-${Date.now()}-${Math.random().toString(36).substr(2, 5)}`;
              f.set(e.id, t);
              let n = JSON.parse(JSON.stringify(e.data));
              return {
                ...e,
                id: t,
                position: {
                  x: i.x + (e.position.x - u),
                  y: i.y + (e.position.y - d)
                },
                selected: true,
                data: n
              };
            });
            let m = (n || []).map(e => {
              return {
                ...e,
                id: `e-${f.get(e.source)}-${f.get(e.target)}`,
                source: f.get(e.source),
                target: f.get(e.target),
                selected: true,
                type: `default`
              };
            });
            let h = [];
            if (r && t.originalIds) {
              let n = En.current;
              e.forEach((e, r) => {
                let i = t.originalIds[r];
                let a = f.get(e.id);
                if (i && a) {
                  n.filter(e => {
                    return e.target === i;
                  }).forEach(e => {
                    if (!f.has(e.source)) {
                      h.push({
                        id: `e-auto-in-${e.source}-${a}`,
                        source: e.source,
                        sourceHandle: e.sourceHandle,
                        target: a,
                        targetHandle: e.targetHandle,
                        type: `default`
                      });
                    }
                  });
                  n.filter(e => {
                    return e.source === i;
                  }).forEach(e => {
                    if (!f.has(e.target)) {
                      h.push({
                        id: `e-auto-out-${a}-${e.target}`,
                        source: a,
                        sourceHandle: e.sourceHandle,
                        target: e.target,
                        targetHandle: e.targetHandle,
                        type: `default`
                      });
                    }
                  });
                }
              });
            }
            K(e => {
              return e.map(e => {
                return {
                  ...e,
                  selected: false
                };
              }).concat(p);
            });
            q(e => {
              return e.map(e => {
                return {
                  ...e,
                  selected: false
                };
              }).concat(m).concat(h);
            });
            if (a && p.length > 0) {
              let e = p.reduce((e, t) => {
                if (e.position.x < t.position.x) {
                  return e;
                } else {
                  return t;
                }
              });
              let t = {
                id: `e-${a.source}-${e.id}`,
                source: a.source,
                sourceHandle: a.sourceHandle,
                target: e.id,
                targetHandle: null,
                type: `default`
              };
              q(e => {
                return e.concat(t);
              });
            }
            Ie(null);
            k(`已粘贴 ${p.length} 个节点`);
            return;
          } else if (t && t.type === `mutiwindow-images`) {
            let {
              images: e
            } = t;
            if (!e || e.length === 0) {
              return;
            }
            let n = e.map((e, t) => {
              let n = t % 6;
              let r = Math.floor(t / 6);
              return {
                id: `imageNode-${Date.now()}-${t}`,
                type: `imageNode`,
                position: {
                  x: i.x + n * 150,
                  y: i.y + r * 150
                },
                data: {
                  imageUrl: e,
                  label: `提取帧 ${t + 1}`
                },
                selected: true,
                style: {
                  width: 420,
                  height: 420
                }
              };
            });
            K(e => {
              return e.map(e => {
                return {
                  ...e,
                  selected: false
                };
              }).concat(n);
            });
            Ie(null);
            k(`已粘贴 ${e.length} 张提取的图片`);
            return;
          }
        } catch {
          let e = localStorage.getItem(`mutiwindow-clipboard`);
          if (e) {
            try {
              let t = JSON.parse(e);
              if (t && t.type === `mutiwindow-images`) {
                let {
                  images: e
                } = t;
                if (e && e.length > 0) {
                  let t = e.map((e, t) => {
                    let n = t % 6;
                    let r = Math.floor(t / 6);
                    return {
                      id: `imageNode-${Date.now()}-${t}`,
                      type: `imageNode`,
                      position: {
                        x: i.x + n * 150,
                        y: i.y + r * 150
                      },
                      data: {
                        imageUrl: e,
                        label: `提取帧 ${t + 1}`
                      },
                      selected: true,
                      style: {
                        width: 420,
                        height: 420
                      }
                    };
                  });
                  K(e => {
                    return e.map(e => {
                      return {
                        ...e,
                        selected: false
                      };
                    }).concat(t);
                  });
                  Ie(null);
                  k(`已粘贴 ${e.length} 张提取的图片`);
                  return;
                }
              }
            } catch {}
          }
        }
      }
    } catch (e) {
      console.error(`Failed to read clipboard text`, e);
    }
    try {
      let e = await navigator.clipboard.read().catch(() => {
        return [];
      });
      if (e && e.length > 0) {
        for (let t of e) {
          if (t.types.some(e => {
            return e.startsWith(`image/`);
          })) {
            let e = await t.getType(t.types.find(e => {
              return e.startsWith(`image/`);
            }));
            try {
              let t = await hi(e, {
                subfolder: `canvas/paste`,
                preferThumbnail: true,
                thumbMaxDim: 480,
                thumbQuality: 75
              });
              if (t.url && /^https?:\/\//i.test(t.url)) {
                di(`imageNode`, i, {
                  imageUrl: t.url,
                  thumbnailUrl: t.thumbnailUrl
                }, a);
                Ie(null);
                return;
              }
            } catch (e) {
              console.warn(`[InfiniteCanvas.handlePaste] urlifyAsset failed, fallback to base64:`, e);
            }
            let n = new FileReader();
            n.onload = e => {
              let t = e.target?.result;
              di(`imageNode`, i, {
                imageUrl: t
              }, a);
            };
            n.readAsDataURL(e);
            Ie(null);
            return;
          }
          if (t.types.includes(`text/plain`)) {
            let e = await (await t.getType(`text/plain`)).text();
            if (e && e.trim()) {
              try {
                let t = JSON.parse(e);
                if (t && t.type === `mutiwindow-nodes`) {
                  return;
                }
              } catch {}
              di(`textNode`, i, {
                text: e.trim(),
                expanded: false
              }, a);
              Ie(null);
              return;
            }
          }
        }
      }
    } catch (e) {
      console.error(`Failed to read clipboard contents: `, e);
    }
    if (!Fe && !n) {
      k(`无法读取剪贴板或格式不支持，请尝试使用 Ctrl+V 快捷键`);
    } else if (Fe && !n) {
      k(`读取剪贴板失败，请使用 Ctrl+V 快捷键粘贴`);
    }
    Ie(null);
  }, [Fe, Ve, Sr, Lr, ar, Rn, K, q, k]);
  let Si = () => {
    if (Fe?.nodeId) {
      K(e => {
        return e.filter(e => {
          return e.id !== Fe.nodeId;
        });
      });
      q(e => {
        return e.filter(e => {
          return e.source !== Fe.nodeId && e.target !== Fe.nodeId;
        });
      });
    }
    Ie(null);
  };
  let Ci = async () => {
    let e = Tn.current.filter(e => {
      return e.selected;
    });
    let t = e;
    if (Fe?.nodeId && !e.find(e => {
      return e.id === Fe.nodeId;
    })) {
      let e = Tn.current.find(e => {
        return e.id === Fe.nodeId;
      });
      if (e) {
        t = [e];
      }
    }
    if (t.length > 0) {
      let e = En.current.filter(e => {
        return t.some(t => {
          return t.id === e.source;
        }) && t.some(t => {
          return t.id === e.target;
        });
      });
      let n = {
        type: `mutiwindow-nodes`,
        nodes: t.map(e => {
          let t = {
            ...e.data
          };
          Object.keys(t).forEach(e => {
            if (typeof t[e] == `function`) {
              delete t[e];
            }
          });
          delete t.loading;
          delete t.progress;
          delete t.errorMessage;
          delete t.imageUrlRef;
          delete t.imageUrlThumbRef;
          delete t.imageUrlUploaded;
          return {
            ...e,
            data: t
          };
        }),
        edges: e,
        originalIds: t.map(e => {
          return e.id;
        })
      };
      try {
        await navigator.clipboard.writeText(JSON.stringify(n));
        k(`已复制 ${t.length} 个节点`);
      } catch (e) {
        console.error(`Copy failed`, e);
        k(`复制失败，请检查浏览器权限`);
      }
    }
    Ie(null);
  };
  let Ti = async () => {
    await Ci();
    setTimeout(async () => {
      try {
        let e = await navigator.clipboard.readText().catch(() => {
          return ``;
        });
        if (e && e.trim()) {
          let t = JSON.parse(e.trim());
          if (t && t.type === `mutiwindow-nodes`) {
            await xi(undefined, undefined, e.trim(), true);
          }
        }
      } catch (e) {
        console.error(`Duplicate paste failed: `, e);
      }
    }, 50);
  };
  let Ei = async () => {
    if (Fe?.nodeId) {
      let e = De.find(e => {
        return e.id === Fe.nodeId;
      });
      if (e && e.data.imageUrl) {
        try {
          let t = e.data.imageUrl;
          let n = new Image();
          n.crossOrigin = `anonymous`;
          n.src = t;
          await new Promise((e, t) => {
            n.onload = e;
            n.onerror = t;
          });
          let r = document.createElement(`canvas`);
          r.width = n.width;
          r.height = n.height;
          let i = r.getContext(`2d`);
          if (!i) {
            throw Error(`Could not get canvas context`);
          }
          i.drawImage(n, 0, 0);
          r.toBlob(async e => {
            if (e) {
              try {
                await navigator.clipboard.write([new ClipboardItem({
                  'image/png': e
                })]);
                k(`图片已复制，可前往其他AI平台粘贴`);
              } catch (e) {
                console.error(`Clipboard write failed:`, e);
                k(`写入剪贴板失败，请检查浏览器权限`);
              }
            }
          }, `image/png`);
        } catch (t) {
          console.error(`Canvas copy failed, trying fetch fallback:`, t);
          try {
            let t = e.data.imageUrl;
            let n = await (await Bl(t, {
              ...Ee
            })).blob();
            if (n.type !== `image/png`) {
              throw Error(`Image is not PNG, cannot copy via fetch fallback`);
            }
            await navigator.clipboard.write([new ClipboardItem({
              'image/png': n
            })]);
            k(`图片已复制，可前往其他AI平台粘贴`);
          } catch (e) {
            console.error(`Fallback copy failed:`, e);
            k(`复制图片失败，可能因跨域或格式限制`);
          }
        }
      } else {
        k(`节点没有图片`);
      }
    }
    Ie(null);
  };
  let Di = () => {
    if (Fe?.nodeId) {
      let e = De.find(e => {
        return e.id === Fe.nodeId;
      });
      if (e && (e.type === `imageNode` || e.type === `promptNode`)) {
        let t = `gridSplitNode-${Date.now()}`;
        let n = {
          id: t,
          type: `gridSplitNode`,
          position: {
            x: e.position.x + (e.measured?.width || 300) + 50,
            y: e.position.y
          },
          data: {
            onSplit: lr,
            onSplitOne: cr,
            onSplitManual: ur,
            onSplitOneManual: dr
          }
        };
        K(e => {
          return e.concat(n);
        });
        let r = {
          id: `e-${e.id}-${t}`,
          source: e.id,
          target: t
        };
        q(e => {
          return e.concat(r);
        });
      } else {
        k(`该节点不支持图片切分/拼图`);
      }
    }
    Ie(null);
  };
  let Oi = Z.useCallback((e, t) => {
    if (!t.isValid && t.fromNode && t.fromHandle) {
      let {
        clientX: n,
        clientY: r
      } = `changedTouches` in e ? e.changedTouches[0] : e;
      let i = Le.current?.getBoundingClientRect();
      if (i) {
        setTimeout(() => {
          let e = Ve({
            x: n,
            y: r
          });
          K(t => {
            return t.filter(e => {
              return e.id !== `ghost-target`;
            }).concat({
              id: `ghost-target`,
              type: `ghostTarget`,
              position: e,
              style: {
                opacity: 0,
                pointerEvents: `none`,
                width: 1,
                height: 1
              },
              data: {
                label: ``
              },
              selectable: false,
              draggable: false
            });
          });
          q(e => {
            return e.filter(e => {
              return e.id !== `ghost-edge`;
            }).concat({
              id: `ghost-edge`,
              source: t.fromNode.id,
              sourceHandle: t.fromHandle.id || null,
              target: `ghost-target`
            });
          });
          Ie({
            x: n - i.left,
            y: r - i.top,
            type: `connection`,
            connection: {
              source: t.fromNode.id,
              sourceHandle: t.fromHandle.id || null,
              dropPosition: e
            }
          });
          it(false);
        }, 50);
      }
    }
  }, [Ie]);
  let ki = Z.useCallback(e => {
    e.preventDefault();
    e.dataTransfer.dropEffect = `copy`;
  }, []);
  let Ai = Z.useCallback(e => {
    e.preventDefault();
    let t = Ve({
      x: e.clientX,
      y: e.clientY
    });
    let n = e.dataTransfer.getData(`application/x-yimao-template`) || e.dataTransfer.getData(`text/plain`);
    if (n) {
      try {
        let e = JSON.parse(n);
        if (e && typeof e == `object` && typeof e.id == `number` && typeof e.name == `string` && e.graphData && Array.isArray(e.graphData.nodes)) {
          Nn.current(e, t);
          Rt(false);
          return;
        }
      } catch (e) {
        console.warn(`[template-drop] 解析失败`, e);
      }
    }
    let r = e.dataTransfer.getData(`application/x-mutiwindow-task`);
    if (r) {
      try {
        let e = JSON.parse(r);
        if (e?.url) {
          let n = e.url;
          try {
            let e = JSON.parse(n);
            if (typeof e == `object`) {
              n = JSON.stringify(e);
            }
          } catch {}
          if (e.mediaType === `video`) {
            di(`discountVideoNode`, t, {
              videoUrl: n,
              prompt: e.prompt || ``,
              label: `来自任务清单`
            });
          } else if (e.mediaType === `audio`) {
            di(`audioPlayerNode`, t, {
              audioUrl: n,
              label: `来自任务清单`
            });
          } else if (e.mediaType === `text`) {
            di(`textNode`, t, {
              text: n,
              label: `来自任务清单`
            });
          } else {
            di(`imageNode`, t, {
              imageUrl: n,
              label: `来自任务清单`
            });
          }
          return;
        }
      } catch (e) {
        console.warn(`[task-drop] 解析失败`, e);
      }
    }
    if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
      Array.from(e.dataTransfer.files).forEach(async (e, n) => {
        let r = (e.name || ``).toLowerCase();
        let i = /\.(mp3|wav|ogg|m4a|flac|aac|opus|wma|aiff)$/i.test(r);
        let a = /\.(mp4|webm|mov|mkv|avi|m4v)$/i.test(r);
        let o = /\.(png|jpe?g|webp|gif|bmp|svg|avif)$/i.test(r);
        let s = e.type.startsWith(`audio/`) || i;
        let c = e.type.startsWith(`video/`) || a;
        let l = e.type.startsWith(`image/`) || o;
        if (!l && !c && !s && !e.type.startsWith(`text/`)) {
          return;
        }
        let u = {
          x: t.x + n * 50,
          y: t.y + n * 50
        };
        if (e.type.startsWith(`text/`)) {
          let t = new FileReader();
          t.onload = t => {
            let n = t.target?.result;
            di(`textNode`, u, {
              text: n,
              label: e.name
            });
          };
          t.readAsText(e);
          return;
        }
        try {
          let t = await hi(e, {
            subfolder: `canvas/drop`,
            preferThumbnail: l,
            thumbMaxDim: 480,
            thumbQuality: 75
          });
          if (t.url && /^https?:\/\//i.test(t.url)) {
            if (s) {
              di(`audioPlayerNode`, u, {
                audioUrl: t.url,
                audioName: e.name,
                label: e.name
              });
            } else {
              di(`imageNode`, u, {
                imageUrl: t.url,
                thumbnailUrl: t.thumbnailUrl,
                label: e.name
              });
            }
            return;
          }
        } catch (e) {
          console.warn(`[InfiniteCanvas.onDrop] urlifyAsset failed, fallback to base64:`, e);
        }
        let d = new FileReader();
        d.onload = t => {
          let n = t.target?.result;
          if (s) {
            di(`audioPlayerNode`, u, {
              audioUrl: n,
              audioName: e.name,
              label: e.name
            });
          } else {
            di(`imageNode`, u, {
              imageUrl: n,
              label: e.name
            });
          }
        };
        d.readAsDataURL(e);
      });
      return;
    }
    let i = e.dataTransfer.getData(`text/plain`);
    if (i) {
      if (i.startsWith(`http`) || i.startsWith(`data:image`) || i.startsWith(`blob:`)) {
        di(`imageNode`, t, {
          imageUrl: i
        });
      } else {
        di(`textNode`, t, {
          text: i
        });
      }
    }
  }, [Ve, di]);
  let ji = Z.useRef(new Map());
  let Mi = Z.useRef(new Map());
  let Ni = e => {
    return typeof e == `string` && (e.startsWith(`data:`) || e.startsWith(`blob:`) || /^https?:\/\//i.test(e));
  };
  let Pi = e => {
    return typeof e == `string` && /^https?:\/\//i.test(e) && !/:\d+\/files\//.test(e) && !e.includes(`localhost`);
  };
  let Fi = Z.useCallback(async e => {
    if (!Ni(e)) {
      return e;
    }
    let t = ji.current.get(e);
    if (t) {
      return t;
    }
    let n = (async () => {
      try {
        let t = await (await fetch(e)).blob();
        let n = t;
        if (t.type.startsWith(`image/`)) {
          try {
            n = (await Or(t, 1000, 0.85)).blob;
          } catch (t) {
            console.warn(`[template] image compress failed, keep original blob:`, e, t);
          }
        }
        let r = e.split(`/`);
        let i = (r[r.length - 1] || `asset_${Date.now()}`).split(`?`)[0];
        return await g_(n, i, {
          folder: `templates`
        });
      } catch (t) {
        console.warn(`[template] upload asset to cloud failed:`, e, t);
        return e;
      }
    })();
    ji.current.set(e, n);
    try {
      return await n;
    } finally {
      ji.current.delete(e);
    }
  }, []);
  let Ii = Z.useCallback(async e => {
    if (!Pi(e)) {
      return e;
    }
    let t = Mi.current.get(e);
    if (t) {
      return t;
    }
    let n = (async () => {
      try {
        return (await ci(e, {
          subfolder: `canvas/template`
        }))?.url || e;
      } catch (t) {
        console.warn(`[template] upload remote asset failed:`, e, t);
        return e;
      }
    })();
    Mi.current.set(e, n);
    try {
      return await n;
    } finally {
      Mi.current.delete(e);
    }
  }, []);
  let Li = Z.useCallback(async (e, t, n) => {
    if (e == null || typeof e == `function`) {
      return e;
    }
    let r = !n || n === `url` || n.endsWith(`Url`) || n === `images` || n === `extractedImages` || n === `allExtractedImages` || n === `resultData`;
    if (typeof e == `string`) {
      if (r && Ni(e)) {
        return await t(e);
      } else {
        return e;
      }
    }
    if (Array.isArray(e)) {
      let r = [];
      for (let i of e) {
        r.push(await Li(i, t, n));
      }
      return r;
    }
    if (typeof e == `object`) {
      let n = {};
      for (let [r, i] of Object.entries(e)) {
        n[r] = await Li(i, t, r);
      }
      return n;
    }
    return e;
  }, []);
  let Ri = e => {
    let t = new Set(e.map(e => {
      return e.type;
    }));
    if ([...t].some(e => {
      return e === `videoNode` || e === `sd2VideoNode` || e === `discountVideoNode` || e === `videoExtractNode`;
    })) {
      return `video`;
    } else if ([...t].some(e => {
      return e === `imageNode` || e === `promptNode` || e === `imageBoxNode`;
    })) {
      return `image`;
    } else {
      return `text`;
    }
  };
  let zi = Z.useCallback(async e => {
    let t = Nt.length > 0 ? Nt : Tn.current.filter(e => {
      return e.selected && e.type !== `group`;
    });
    if (t.length === 0) {
      throw Error(`请先框选要保存的节点`);
    }
    let n = new Set(t.map(e => {
      return e.id;
    }));
    let r = En.current.filter(e => {
      return n.has(e.source) && n.has(e.target);
    });
    let i = Math.min(...t.map(e => {
      return e.position.x;
    }));
    let a = Math.min(...t.map(e => {
      return e.position.y;
    }));
    let o = [];
    let s = ``;
    for (let e of t) {
      let t = JSON.parse(JSON.stringify(e.data || {}, (e, t) => {
        if (typeof t == `function`) {
          return undefined;
        } else {
          return t;
        }
      }));
      delete t.loading;
      delete t.progress;
      delete t.errorMessage;
      delete t.imageUrlRef;
      delete t.imageUrlThumbRef;
      delete t.imageUrlUploaded;
      delete t.hasChanged;
      let n = await Li(t, Fi);
      if (!s) {
        let e = n.imageUrl || n.thumbnailUrl || Array.isArray(n.images) && n.images[0]?.url;
        if (typeof e == `string` && /^https?:\/\//i.test(e)) {
          s = e;
        }
      }
      o.push({
        id: e.id,
        type: e.type,
        position: {
          x: e.position.x - i,
          y: e.position.y - a
        },
        style: e.style,
        data: n
      });
    }
    await __({
      name: e,
      category: Ri(t),
      coverUrl: s,
      graphData: {
        nodes: o,
        edges: r
      },
      nodeCount: o.length,
      isPublic: false
    });
    k(`模板已保存到「我的模板」`);
  }, [Nt, k, Li, Fi]);
  let Bi = Z.useCallback(async (e, t) => {
    let n = e.graphData;
    if (!n || !Array.isArray(n.nodes) || n.nodes.length === 0) {
      k(`模板数据为空`);
      return;
    }
    let r = new Map();
    let i = Date.now();
    let a = await Promise.all(n.nodes.map(async e => {
      let n = `${e.type}-${i}-${Math.random().toString(36).substr(2, 5)}`;
      r.set(e.id, n);
      let a = await Li(JSON.parse(JSON.stringify(e.data || {})), Ii);
      return {
        ...e,
        id: n,
        position: {
          x: t.x + (e.position?.x || 0),
          y: t.y + (e.position?.y || 0)
        },
        selected: true,
        data: {
          ...a,
          hasChanged: true
        }
      };
    }));
    let o = (n.edges || []).map(e => {
      return {
        ...e,
        id: `e-${r.get(e.source)}-${r.get(e.target)}-${Math.random().toString(36).substr(2, 4)}`,
        source: r.get(e.source),
        target: r.get(e.target),
        selected: false,
        type: `default`
      };
    }).filter(e => {
      return e.source && e.target;
    });
    K(e => {
      return e.map(e => {
        return {
          ...e,
          selected: false
        };
      }).concat(a);
    });
    q(e => {
      return e.concat(o);
    });
    S_(e.id);
    k(`已插入模板「${e.name}」`);
  }, [K, q, k, Li, Ii]);
  Z.useEffect(() => {
    K(e => {
      return e.map(e => {
        let t = {
          ...e.data
        };
        let n = false;
        if ((e.type === `promptNode` || e.type === `textNode` || e.type === `videoNode`) && JSON.stringify(t.presetPrompts) !== JSON.stringify(M)) {
          t.presetPrompts = M;
          n = true;
        }
        if (e.type === `videoExtractNode`) {
          if (t.onExtractFrames !== $r) {
            t.onExtractFrames = $r;
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
        }
        if (e.type === `videoProcessNode`) {
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
          if (t.onSpawnImageNode !== fr) {
            t.onSpawnImageNode = fr;
            n = true;
          }
          if (t.onSpawnAudioNode !== gr) {
            t.onSpawnAudioNode = gr;
            n = true;
          }
        }
        if (e.type === `videoToGifNode` || e.type === `imageCompressNode` || e.type === `faceMosaicNode`) {
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
          if (t.onSpawnImageNode !== fr) {
            t.onSpawnImageNode = fr;
            n = true;
          }
          if (t.onAddImage !== _r) {
            t.onAddImage = _r;
            n = true;
          }
          if ((e.type === `imageCompressNode` || e.type === `faceMosaicNode`) && !t.onPushImagesToImageBox) {
            t.onPushImagesToImageBox = (e, t) => {
              if (En.current.some(t => {
                return t.source === e && Tn.current.find(e => {
                  return e.id === t.target;
                })?.type === `imageBoxNode`;
              })) {
                return or.current(e, t);
              } else {
                return null;
              }
            };
            n = true;
          }
        }
        if (e.type === `promptNode`) {
          if (t.onGenerate !== Hr) {
            t.onGenerate = Hr;
            n = true;
          }
          if (!t.drawingModel && S) {
            t.drawingModel = S;
            n = true;
          }
        }
        if (e.type === `director3dNode` || e.type === `panoramaNode`) {
          if (t.onCaptureToBox !== br) {
            t.onCaptureToBox = br;
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
        }
        if (e.type === `textNode`) {
          if (t.onGenerateText !== Ur) {
            t.onGenerateText = Ur;
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
          if (!t.textModel && x) {
            t.textModel = x;
            n = true;
          }
        }
        if (e.type === `sd2VideoNode`) {
          if (t.onGenerateSD2Video !== qr) {
            t.onGenerateSD2Video = qr;
            n = true;
          }
          if (t.onUploadAsset !== Cr) {
            t.onUploadAsset = Cr;
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
          if (!t.sd2VideoModel && w) {
            t.sd2VideoModel = w;
            n = true;
          }
          if (!t.videoDurations && D) {
            t.videoDurations = D;
            n = true;
          }
        }
        if (e.type === `discountVideoNode`) {
          if (t.onGenerateDiscountVideo !== Jr) {
            t.onGenerateDiscountVideo = Jr;
            n = true;
          }
          if (t.onUploadAsset !== wr) {
            t.onUploadAsset = wr;
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
          if (!t.discountVideoModel && T) {
            t.discountVideoModel = T;
            n = true;
          }
          if (!t.videoDurations && D) {
            t.videoDurations = D;
            n = true;
          }
        }
        if (e.type === `videoNode`) {
          if (t.onGenerateVideo !== Gr) {
            t.onGenerateVideo = Gr;
            n = true;
          }
          if (!t.videoModel && C) {
            t.videoModel = C;
            n = true;
          }
          if (!t.videoDurations && D) {
            t.videoDurations = D;
            n = true;
          }
        }
        if (e.type === `audioNode`) {
          if (t.audioApiUrl !== y) {
            t.audioApiUrl = y;
            n = true;
          }
          if (t.audioApiKey !== b) {
            t.audioApiKey = b;
            n = true;
          }
          if (t.audioModel !== O) {
            t.audioModel = O;
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
        }
        if (e.type === `imageBoxNode`) {
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
          if (t.onZoom !== Rn) {
            t.onZoom = Rn;
            n = true;
          }
          if (t.onSendToActiveTab !== ee) {
            t.onSendToActiveTab = ee;
            n = true;
          }
        }
        if (e.type === `rhWebappNode`) {
          if (t.aiAppApiUrl !== h) {
            t.aiAppApiUrl = h;
            n = true;
          }
          if (t.aiAppApiKey !== _) {
            t.aiAppApiKey = _;
            n = true;
          }
          if (t.membershipType !== N?.type) {
            t.membershipType = N?.type;
            n = true;
          }
          if (t.addTransitResource !== j) {
            t.addTransitResource = j;
            n = true;
          }
          if (t.updateGlobalTasks !== I) {
            t.updateGlobalTasks = I;
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
        }
        if (e.type === `customNode`) {
          if (t.textModel !== x) {
            t.textModel = x;
            n = true;
          }
          if (t.onGenerateCustom !== Qr) {
            t.onGenerateCustom = Qr;
            n = true;
          }
          if (t.onAIAssist !== Zr) {
            t.onAIAssist = Zr;
            n = true;
          }
          if (typeof t.onSaveTemplate != `function`) {
            t.onSaveTemplate = (e, t) => {
              if (te) {
                te({
                  id: Date.now().toString(),
                  name: e,
                  config: t
                });
              }
            };
            n = true;
          }
          if (t.onShowToast !== k) {
            t.onShowToast = k;
            n = true;
          }
        }
        if ((e.type === `promptNode` || e.type === `textNode` || e.type === `videoNode`) && t.onAddImage !== _r) {
          t.onAddImage = _r;
          n = true;
        }
        if ((e.type === `promptNode` || e.type === `textNode` || e.type === `videoNode` || e.type === `sd2VideoNode` || e.type === `discountVideoNode`) && t.onRefresh !== Yr) {
          t.onRefresh = Yr;
          n = true;
        }
        if ((e.type === `promptNode` || e.type === `textNode` || e.type === `videoNode` || e.type === `customNode` || e.type === `sd2VideoNode` || e.type === `videoExtractNode` || e.type === `discountVideoNode`) && t.onStop !== Vn) {
          t.onStop = Vn;
          n = true;
        }
        if ((e.type === `imageNode` || e.type === `promptNode`) && t.onCrop !== ar) {
          t.onCrop = ar;
          n = true;
        }
        if ((e.type === `imageNode` || e.type === `promptNode`) && t.onEdit !== zn) {
          t.onEdit = zn;
          n = true;
        }
        if (e.type === `gridSplitNode`) {
          if (t.onSplit !== lr) {
            t.onSplit = lr;
            n = true;
          }
          if (t.onSplitOne !== cr) {
            t.onSplitOne = cr;
            n = true;
          }
          if (t.onSplitManual !== ur) {
            t.onSplitManual = ur;
            n = true;
          }
          if (t.onSplitOneManual !== dr) {
            t.onSplitOneManual = dr;
            n = true;
          }
        }
        if (e.type === `gridMergeNode` && t.onSpawnImageNode !== fr) {
          t.onSpawnImageNode = fr;
          n = true;
        }
        if (e.type === `cropNode`) {
          if (t.onCropComplete !== rr) {
            t.onCropComplete = rr;
            n = true;
          }
          if (t.onCancel !== ir) {
            t.onCancel = ir;
            n = true;
          }
        }
        if (t.onZoom !== Rn) {
          t.onZoom = Rn;
          n = true;
        }
        if ((e.type === `promptNode` || e.type === `imageNode`) && t.onSendToActiveTab !== ee) {
          t.onSendToActiveTab = ee;
          n = true;
        }
        if (n) {
          return {
            ...e,
            data: t
          };
        } else {
          return e;
        }
      });
    });
  }, [Ne, Hr, Ur, Gr, qr, Jr, Qr, Zr, te, ar, lr, cr, ur, dr, fr, gr, rr, ir, K, M, _r, Rn, k, Vn, zn, ee, h, _, N?.type, j, I, br]);
  Z.useEffect(() => {
    let e = e => {
      let t = e.detail?.task;
      if (!t?.nodeId) {
        return;
      }
      let n = J().find(e => {
        return e.id === t.nodeId;
      });
      if (!n) {
        k?.(`原节点已被删除,无法重新执行`);
        return;
      }
      let r = n.data || {};
      let i = r.prompt || t.prompt || ``;
      try {
        switch (t.type) {
          case `image`:
            {
              Sr(n.id, i, r.size, t.modelName);
              break;
            }
          case `text`:
            {
              Lr(n.id, i, !!r.autoSplit, t.modelName);
              break;
            }
          case `video`:
            {
              kr(n.id, i, r.aspectRatio, r.size, t.modelName, r.selectedSeconds || r.seconds);
              break;
            }
          case `sd2Video`:
            {
              Er(n.id, i, r.size, t.modelName, r.selectedSeconds || r.seconds);
              break;
            }
          case `discountVideo`:
            {
              Dr(n.id, i, r.size, t.modelName, r.selectedSeconds || r.seconds, r.resolution);
              break;
            }
          case `custom`:
            {
              Qr(n.id);
              break;
            }
          case `rhWebapp`:
            {
              window.dispatchEvent(new CustomEvent(zs, {
                detail: {
                  nodeId: n.id
                }
              }));
              break;
            }
        }
      } catch (e) {
        console.error(`[rerun-task] 失败`, e);
      }
    };
    window.addEventListener(`mutiwindow-rerun-task`, e);
    return () => {
      return window.removeEventListener(`mutiwindow-rerun-task`, e);
    };
  }, [J, Sr, Lr, kr, Er, Dr, Qr, k]);
  let Vi = Z.useCallback((e, t) => {
    e.stopPropagation();
    q(e => {
      return e.filter(e => {
        return e.id !== t.id;
      });
    });
  }, [q]);
  Z.useEffect(() => {
    if (!Fe || Fe.type !== `connection`) {
      K(e => {
        return e.filter(e => {
          return e.id !== `ghost-target`;
        });
      });
      q(e => {
        return e.filter(e => {
          return e.id !== `ghost-edge`;
        });
      });
    }
  }, [Fe, K, q]);
  let Ui = Z.useCallback(() => {
    nt({
      nodes: J(),
      edges: Y()
    });
    let e = new l_.default.graphlib.Graph({
      compound: true
    });
    e.setDefaultEdgeLabel(() => {
      return {};
    });
    e.setGraph({
      rankdir: `LR`,
      nodesep: 300,
      ranksep: 300,
      align: `UL`
    });
    let t = Tn.current;
    let n = En.current;
    t.forEach(t => {
      let n = t.type === `group`;
      let r = n ? t.measured?.height || 200 : 80;
      let i = n ? t.measured?.width || 300 : 320;
      e.setNode(t.id, {
        width: i,
        height: r
      });
      if (t.parentId) {
        e.setParent(t.id, t.parentId);
      }
    });
    n.forEach(t => {
      e.setEdge(t.source, t.target);
    });
    l_.default.layout(e);
    let r = [];
    let i = new Set();
    let a = new Map();
    t.forEach(e => {
      return a.set(e.id, []);
    });
    n.forEach(e => {
      if (a.has(e.source)) {
        a.get(e.source).push(e.target);
      }
      if (a.has(e.target)) {
        a.get(e.target).push(e.source);
      }
    });
    t.forEach(e => {
      if (!i.has(e.id)) {
        let n = [];
        let o = [e.id];
        for (i.add(e.id); o.length > 0;) {
          let e = o.shift();
          let r = t.find(t => {
            return t.id === e;
          });
          if (r) {
            n.push(r);
          }
          a.get(e)?.forEach(e => {
            if (!i.has(e)) {
              i.add(e);
              o.push(e);
            }
          });
        }
        r.push(n);
      }
    });
    let o = 0;
    let s = 0;
    let c = 0;
    let l = [];
    r.forEach(t => {
      let n = Infinity;
      let r = Infinity;
      let i = -Infinity;
      let a = -Infinity;
      t.forEach(t => {
        let o = e.node(t.id);
        if (o) {
          n = Math.min(n, o.x - o.width / 2);
          r = Math.min(r, o.y - o.height / 2);
          i = Math.max(i, o.x + o.width / 2);
          a = Math.max(a, o.y + o.height / 2);
        }
      });
      let u = i - n;
      let d = a - r;
      if (o + u > 2500 && o > 0) {
        o = 0;
        s += c + 400;
        c = 0;
      }
      t.forEach(t => {
        let i = e.node(t.id);
        if (i) {
          let a = 0;
          let c = 0;
          if (t.parentId) {
            let n = e.node(t.parentId);
            if (n) {
              a = i.x - i.width / 2 - (n.x - n.width / 2);
              c = i.y - i.height / 2 - (n.y - n.height / 2);
            }
          } else {
            let e = i.x - i.width / 2 - n;
            let t = i.y - i.height / 2 - r;
            a = o + e;
            c = s + t;
          }
          l.push({
            ...t,
            position: {
              x: a,
              y: c
            },
            data: {
              ...t.data,
              expanded: false
            },
            style: t.type === `group` ? {
              ...t.style,
              width: i.width,
              height: i.height
            } : t.style
          });
        }
      });
      o += u + 300;
      c = Math.max(c, d);
    });
    K(l);
    k(`已自动排版，节点已收起`);
    setTimeout(() => {
      return Ue({
        padding: 0.2,
        duration: 800,
        maxZoom: 1
      });
    }, 100);
  }, [K, Ue, k, J, Y]);
  let Wi = Z.useCallback(async e => {
    let t = Tn.current.filter(t => {
      return e.includes(t.id) && t.type === `imageNode` && t.data?.imageUrl;
    });
    if (t.length < 2) {
      k(`至少选择 2 个图片节点`);
      return;
    }
    let n = t.reduce((e, t) => {
      return e + t.position.x;
    }, 0) / t.length;
    let r = t.reduce((e, t) => {
      return e + t.position.y;
    }, 0) / t.length;
    let i = await Promise.all(t.map(async e => {
      let t = e.data.imageUrl;
      let n;
      try {
        n = await _cmp_Tr(t, 256, 0.7);
      } catch {
        n = undefined;
      }
      return {
        id: `img-${e.id}-${Math.random().toString(36).slice(2, 8)}`,
        url: t,
        thumb: n,
        label: e.data.label,
        source: `merge`,
        createdAt: Date.now()
      };
    }));
    let a = new Set(t.map(e => {
      return e.id;
    }));
    let o = En.current.filter(e => {
      return a.has(e.target) && !a.has(e.source);
    });
    let s = En.current.filter(e => {
      return a.has(e.source) && !a.has(e.target);
    });
    di(`imageBoxNode`, {
      x: n,
      y: r
    }, {
      images: i,
      activeIndex: 0,
      expanded: false,
      label: `图片盒子 (${i.length})`
    });
    let c = `imageBoxNode-${Date.now()}`;
    setTimeout(() => {
      let e = Tn.current.filter(e => {
        return e.type === `imageBoxNode`;
      }).slice(-1)[0]?.id || c;
      q(t => {
        let n = t.filter(e => {
          return !a.has(e.source) && !a.has(e.target);
        });
        o.forEach(t => {
          if (!n.find(n => {
            return n.source === t.source && n.target === e;
          })) {
            n = n.concat({
              ...t,
              id: `e-${t.source}-${e}-${Math.random().toString(36).slice(2, 6)}`,
              target: e,
              targetHandle: `in`
            });
          }
        });
        s.forEach(t => {
          if (!n.find(n => {
            return n.source === e && n.target === t.target;
          })) {
            n = n.concat({
              ...t,
              id: `e-${e}-${t.target}-${Math.random().toString(36).slice(2, 6)}`,
              source: e,
              sourceHandle: `active`
            });
          }
        });
        return n;
      });
      K(e => {
        return e.filter(e => {
          return !a.has(e.id);
        });
      });
      k(`已合并 ${i.length} 张图到图片盒子`);
    }, 30);
  }, [di, K, q, k]);
  let Gi = Z.useCallback(() => {
    let e = Tn.current.filter(e => {
      return e.selected && e.type !== `group`;
    });
    if (e.length < 2) {
      k(`请至少选择两个节点进行打组`);
      return;
    }
    let t = Math.min(...e.map(e => {
      return e.position.x;
    }));
    let n = Math.min(...e.map(e => {
      return e.position.y;
    }));
    let r = Math.max(...e.map(e => {
      return e.position.x + (e.measured?.width || 300);
    }));
    let i = Math.max(...e.map(e => {
      return e.position.y + (e.measured?.height || 200);
    }));
    let a = `group-${Date.now()}`;
    let o = {
      id: a,
      type: `group`,
      position: {
        x: t - 40,
        y: n - 40
      },
      style: {
        width: r - t + 80,
        height: i - n + 80,
        zIndex: -1
      },
      data: {
        name: `编组`
      },
      zIndex: -1
    };
    K(r => {
      return [o, ...r.map(r => {
        if (e.some(e => {
          return e.id === r.id;
        })) {
          return {
            ...r,
            parentId: a,
            position: {
              x: r.position.x - (t - 40),
              y: r.position.y - (n - 40)
            },
            selected: false
          };
        } else {
          return r;
        }
      })];
    });
    k(`打组成功`);
  }, [K, k]);
  let Ki = Z.useCallback(async () => {
    if (!(await ri())) {
      k(`Local Tool 未连接，无法清理缓存`);
      return;
    }
    k(`正在扫描画布缓存...`);
    let e = new Map();
    let t = 0;
    let n = 0;
    let r = 0;
    let i = e => {
      return typeof e == `string` && (e.startsWith(`data:image`) || e.startsWith(`data:video`) || e.startsWith(`data:audio`));
    };
    let a = async i => {
      r++;
      if (e.has(i)) {
        t++;
        return e.get(i);
      }
      try {
        let r = await hi(i, {
          subfolder: `canvas/cleaned`,
          preferThumbnail: i.startsWith(`data:image`),
          thumbMaxDim: 480,
          thumbQuality: 75
        });
        if (r.url && /^https?:\/\//i.test(r.url)) {
          e.set(i, r.url);
          t++;
          return r.url;
        } else {
          n++;
          return i;
        }
      } catch (e) {
        console.warn(`[handleCleanCanvasCache] urlify failed:`, e);
        n++;
        return i;
      }
    };
    let o = async e => {
      if (e == null || typeof e == `function`) {
        return e;
      }
      if (typeof e == `string`) {
        if (i(e)) {
          return await a(e);
        } else {
          return e;
        }
      }
      if (Array.isArray(e)) {
        let t = [];
        for (let n of e) {
          t.push(await o(n));
        }
        return t;
      }
      if (typeof e == `object`) {
        let t = {};
        for (let [n, r] of Object.entries(e)) {
          t[n] = await o(r);
        }
        return t;
      }
      return e;
    };
    let s = Tn.current;
    let c = [];
    for (let e of s) {
      let t = await o(e.data);
      let n = t?.imageUrl;
      if (typeof n == `string` && /^https?:\/\//i.test(n)) {
        if (t.imageUrlRef) {
          delete t.imageUrlRef;
        }
        if (t.imageUrlThumbRef) {
          delete t.imageUrlThumbRef;
        }
        if (t.imageUrlUploaded) {
          delete t.imageUrlUploaded;
        }
      }
      c.push({
        ...e,
        data: t
      });
    }
    if (r === 0) {
      k(`画布中没有需要清理的内联资源`);
      return;
    }
    Tn.current = c;
    K(c);
    bn.current.clear();
    try {
      await Wn();
    } catch (e) {
      console.warn(`[handleCleanCanvasCache] saveToStorage after clean failed:`, e);
    }
    k(`画布缓存清理完成：成功 ${t} / 失败 ${n}`);
  }, [K, k, Wn]);
  let qi = Z.useCallback(e => {
    K(t => {
      let n = t.find(t => {
        return t.id === e;
      });
      if (n) {
        return t.filter(t => {
          return t.id !== e;
        }).map(t => {
          if (t.parentId === e) {
            return {
              ...t,
              parentId: undefined,
              extent: undefined,
              position: {
                x: t.position.x + n.position.x,
                y: t.position.y + n.position.y
              }
            };
          } else {
            return t;
          }
        });
      } else {
        return t;
      }
    });
    k(`已取消编组`);
  }, [K, k]);
  Z.useEffect(() => {
    let e = e => {
      let t = e || document.activeElement;
      if (!t) {
        return false;
      }
      let n = t.tagName;
      return n === `INPUT` || n === `TEXTAREA` || n === `SELECT` || !!t.isContentEditable;
    };
    let t = () => {
      if (document.querySelector(`.director3d-overlay`)) {
        return false;
      }
      let e = Le.current?.getBoundingClientRect();
      return !!e && e.width > 0 && e.height > 0;
    };
    let n = () => {
      try {
        let e = window.getSelection();
        return !!e && e.toString().length > 0;
      } catch {
        return false;
      }
    };
    let r = r => {
      if (!e(r.target) && t()) {
        if (!r.ctrlKey && !r.metaKey && !r.altKey && !r.shiftKey) {
          let e = Le.current?.getBoundingClientRect();
          if (e) {
            let t = Fe?.type === `connection` && !!Fe.connection && !!Fe.connection.dropPosition;
            let n = t ? Fe.connection.dropPosition : Ve({
              x: e.left + Jt.current.x,
              y: e.top + Jt.current.y
            });
            let i = t ? Fe.connection : undefined;
            let a = () => {
              if (t) {
                K(e => {
                  return e.filter(e => {
                    return e.id !== `ghost-target`;
                  });
                });
                q(e => {
                  return e.filter(e => {
                    return e.id !== `ghost-edge`;
                  });
                });
                Ie(null);
              }
            };
            if (r.key.toLowerCase() === `q`) {
              di(`textNode`, n, {
                text: ``
              }, i);
              k(t ? `已添加并连接文本节点` : `添加文本节点`);
              a();
              return;
            } else if (r.key.toLowerCase() === `w`) {
              di(`promptNode`, n, {
                prompt: ``
              }, i);
              k(t ? `已添加并连接生图节点` : `添加生图节点`);
              a();
              return;
            } else if (r.key.toLowerCase() === `e`) {
              di(`discountVideoNode`, n, {
                prompt: ``
              }, i);
              k(t ? `已添加并连接特惠视频节点` : `添加特惠视频节点`);
              a();
              return;
            }
          }
        }
        if ((r.ctrlKey || r.metaKey) && r.key === `z`) {
          if (r.shiftKey) {
            Kn();
          } else {
            Gn();
          }
        } else if ((r.ctrlKey || r.metaKey) && r.key === `y`) {
          Kn();
        } else if ((r.ctrlKey || r.metaKey) && (r.key === `a` || r.key === `A`)) {
          if (n()) {
            return;
          }
          r.preventDefault();
          K(e => {
            return e.map(e => {
              return {
                ...e,
                selected: true
              };
            });
          });
          q(e => {
            return e.map(e => {
              return {
                ...e,
                selected: true
              };
            });
          });
        } else if ((r.ctrlKey || r.metaKey) && r.key === `c`) {
          if (n()) {
            return;
          }
          Ci();
        } else if ((r.ctrlKey || r.metaKey) && r.key === `d`) {
          r.preventDefault();
          Ti();
        } else if ((r.ctrlKey || r.metaKey) && r.key === `g`) {
          r.preventDefault();
          Gi();
        } else if ((r.ctrlKey || r.metaKey) && r.key === `l`) {
          r.preventDefault();
          Ui();
        }
      }
    };
    let i = n => {
      if (e(n.target) || !t()) {
        return;
      }
      let r = n.clipboardData?.getData(`text/plain`);
      if (r && r.trim()) {
        try {
          let e = JSON.parse(r.trim());
          if (e && e.type === `mutiwindow-nodes`) {
            n.preventDefault();
            xi(undefined, undefined, r.trim());
            return;
          }
        } catch {}
      }
      let i = n.clipboardData?.items;
      if (i && i.length > 0) {
        for (let e = 0; e < i.length; e++) {
          let t = i[e];
          if (t.kind === `file` && t.type.startsWith(`image/`)) {
            let e = t.getAsFile();
            if (e) {
              n.preventDefault();
              let t = new FileReader();
              t.onload = e => {
                let t = e.target?.result;
                if (!t) {
                  return;
                }
                let n = Le.current?.getBoundingClientRect();
                di(`imageNode`, n ? Ve({
                  x: n.left + Jt.current.x,
                  y: n.top + Jt.current.y
                }) : {
                  x: 0,
                  y: 0
                }, {
                  imageUrl: t
                });
                k(`已粘贴图片`);
              };
              t.readAsDataURL(e);
              return;
            }
          }
        }
      }
      n.preventDefault();
      xi();
    };
    window.addEventListener(`keydown`, r);
    window.addEventListener(`paste`, i);
    return () => {
      window.removeEventListener(`keydown`, r);
      window.removeEventListener(`paste`, i);
    };
  }, [Gn, Kn, xi, Ci, Ti]);
  let Ji = () => {
    if (!Fe) {
      return {
        top: 0,
        left: 0
      };
    }
    let {
      x: e,
      y: t
    } = Fe;
    let n = Fe.type === `node` || Fe.type === `selection` ? 150 : 550;
    if (Le.current) {
      let r = Le.current.getBoundingClientRect();
      if (e + 160 > r.width) {
        e = r.width - 160 - 10;
      }
      if (t + n > r.height) {
        t = Math.max(10, r.height - n - 10);
      }
    }
    return {
      top: t,
      left: e
    };
  };
  let Yi = De.length > 100;
  let Xi = Z.useMemo(() => {
    let e = new Set(De.filter(e => {
      return e.type === `group` && e.data?.collapsed;
    }).map(e => {
      return e.id;
    }));
    if (e.size === 0) {
      return De;
    } else {
      return De.map(t => {
        if (t.parentId && e.has(t.parentId)) {
          return {
            ...t,
            style: {
              ...t.style,
              opacity: 0,
              pointerEvents: `none`
            },
            className: `${t.className || ``} nodrag nopan`
          };
        } else {
          return t;
        }
      });
    }
  }, [De]);
  let Zi = Z.useMemo(() => {
    return De.filter(e => {
      return e.selected;
    }).map(e => {
      return e.id;
    }).sort().join(`|`);
  }, [De]);
  let $i = Z.useMemo(() => {
    return De.filter(e => {
      return e.type === `group` && e.data?.collapsed;
    }).map(e => {
      return e.id;
    }).sort().join(`|`);
  }, [De]);
  let ea = Z.useMemo(() => {
    return De.map(e => {
      return `${e.id}:${e.parentId || ``}`;
    }).sort().join(`|`);
  }, [De]);
  let ta = Z.useMemo(() => {
    let e = new Set(Zi ? Zi.split(`|`) : []);
    let t = new Set($i ? $i.split(`|`) : []);
    let n = t.size > 0;
    let r = e.size > 0 || Yi;
    if (!n && !r) {
      return Ae;
    }
    let i = n ? new Map(ea.split(`|`).filter(Boolean).map(e => {
      let t = e.indexOf(`:`);
      return [e.slice(0, t), e.slice(t + 1) || undefined];
    })) : null;
    return Ae.map(r => {
      let a = r.hidden;
      let o = r.source;
      let s = r.target;
      let c = r.sourceHandle;
      let l = r.targetHandle;
      if (n && i) {
        let e = i.get(r.source);
        let n = i.get(r.target);
        let u = e && t.has(e);
        let d = n && t.has(n);
        if (u && d && e === n) {
          a = true;
        } else if (u && !d) {
          o = e;
          c = null;
        } else if (d && !u) {
          s = n;
          l = null;
        } else if (u && d) {
          o = e;
          s = n;
          c = null;
          l = null;
        }
      }
      let u = e.has(r.source) || e.has(r.target);
      let d = {
        ...(r.data || {}),
        relatedToSelected: u,
        nodeCount: De.length,
        edgeFxLimit: 50
      };
      if (a === r.hidden && o === r.source && s === r.target && c === r.sourceHandle && l === r.targetHandle && r.data?.relatedToSelected === u && r.data?.nodeCount === De.length && r.data?.edgeFxLimit === 50) {
        return r;
      } else {
        return {
          ...r,
          source: o,
          target: s,
          sourceHandle: c,
          targetHandle: l,
          hidden: a,
          data: d
        };
      }
    });
  }, [Ae, Zi, $i, ea, De.length, Yi]);
  const Component2910 = `input`;
  const Component2911 = `div`;
  const Component2912 = `path`;
  const Component2913 = `path`;
  const Component2914 = `path`;
  const Component2915 = `svg`;
  const Component2916 = `div`;
  const Component2917 = `div`;
  const Component2918 = `p`;
  const Component2919 = `button`;
  const Component2920 = `button`;
  const Component2921 = `div`;
  const Component2922 = `div`;
  const Component2923 = `polygon`;
  const Component2924 = `svg`;
  const Component2925 = `button`;
  const Component2926 = `div`;
  const Component2927 = `button`;
  const Component2928 = `button`;
  const Component2929 = `button`;
  const Component2930 = `path`;
  const Component2931 = `path`;
  const Component2932 = `path`;
  const Component2933 = `path`;
  const Component2934 = `svg`;
  const Component2935 = `button`;
  const Component2936 = `button`;
  const Component2937 = `div`;
  const Component2938 = `button`;
  const Component2939 = `span`;
  const Component2940 = `button`;
  const Component2941 = `div`;
  const Component2942 = `button`;
  const Component2943 = `a`;
  const Component2944 = `button`;
  const Component2945 = `div`;
  const Component2946 = `a`;
  const Component2947 = `div`;
  const Component2948 = `div`;
  const Component2949 = `div`;
  const Component2950 = `div`;
  const Component2951 = `div`;
  const Component2952 = `div`;
  const Component2953 = `button`;
  const Component2954 = `h3`;
  const Component2955 = `span`;
  const Component2956 = `span`;
  const Component2957 = `span`;
  const Component2958 = `div`;
  const Component2959 = `div`;
  const Component2960 = `span`;
  const Component2961 = `span`;
  const Component2962 = `span`;
  const Component2963 = `div`;
  const Component2964 = `div`;
  const Component2965 = `span`;
  const Component2966 = `span`;
  const Component2967 = `rect`;
  const Component2968 = `path`;
  const Component2969 = `svg`;
  const Component2970 = `span`;
  const Component2971 = `div`;
  const Component2972 = `div`;
  const Component2973 = `div`;
  const Component2974 = `h3`;
  const Component2975 = `span`;
  const Component2976 = `rect`;
  const Component2977 = `path`;
  const Component2978 = `svg`;
  const Component2979 = `span`;
  const Component2980 = `div`;
  const Component2981 = `div`;
  const Component2982 = `div`;
  const Component2983 = `div`;
  const Component2984 = `h3`;
  const Component2985 = `span`;
  const Component2986 = `span`;
  const Component2987 = `div`;
  const Component2988 = `div`;
  const Component2989 = `span`;
  const Component2990 = `span`;
  const Component2991 = `div`;
  const Component2992 = `div`;
  const Component2993 = `span`;
  const Component2994 = `span`;
  const Component2995 = `div`;
  const Component2996 = `div`;
  const Component2997 = `div`;
  const Component2998 = `h3`;
  const Component2999 = `span`;
  const Component3000 = `span`;
  const Component3001 = `span`;
  const Component3002 = `div`;
  const Component3003 = `div`;
  const Component3004 = `span`;
  const Component3005 = `span`;
  const Component3006 = `div`;
  const Component3007 = `div`;
  const Component3008 = `span`;
  const Component3009 = `span`;
  const Component3010 = `span`;
  const Component3011 = `div`;
  const Component3012 = `div`;
  const Component3013 = `span`;
  const Component3014 = `span`;
  const Component3015 = `span`;
  const Component3016 = `div`;
  const Component3017 = `div`;
  const Component3018 = `span`;
  const Component3019 = `span`;
  const Component3020 = `span`;
  const Component3021 = `div`;
  const Component3022 = `div`;
  const Component3023 = `span`;
  const Component3024 = `span`;
  const Component3025 = `span`;
  const Component3026 = `div`;
  const Component3027 = `div`;
  const Component3028 = `div`;
  const Component3029 = `div`;
  const Component3030 = `div`;
  const Component3031 = `div`;
  const Component3032 = `div`;
  const Component3033 = `span`;
  const Component3034 = `div`;
  const Component3035 = `span`;
  const Component3036 = `button`;
  const Component3037 = `span`;
  const Component3038 = `div`;
  const Component3039 = `span`;
  const Component3040 = `button`;
  const Component3041 = `span`;
  const Component3042 = `div`;
  const Component3043 = `span`;
  const Component3044 = `button`;
  const Component3045 = `span`;
  const Component3046 = `span`;
  const Component3047 = `span`;
  const Component3048 = `button`;
  const Component3049 = `span`;
  const Component3050 = `button`;
  const Component3051 = `div`;
  const Component3052 = `span`;
  const Component3053 = `span`;
  const Component3054 = `span`;
  const Component3055 = `button`;
  const Component3056 = `div`;
  const Component3057 = `div`;
  const Component3063 = `div`;
  const Component3064 = `div`;
  const Component3065 = `div`;
  const Component3066 = `div`;
  const Component3067 = `span`;
  const Component3068 = `span`;
  const Component3069 = `span`;
  const Component3070 = `button`;
  const Component3071 = `button`;
  const Component3072 = `button`;
  const Component3073 = `div`;
  const Component3074 = `div`;
  const Component3075 = `div`;
  const Component3076 = `span`;
  const Component3077 = `span`;
  const Component3078 = `button`;
  const Component3079 = `div`;
  const Component3080 = `span`;
  const Component3081 = `button`;
  const Component3082 = `span`;
  const Component3083 = `button`;
  const Component3084 = `span`;
  const Component3085 = `button`;
  const Component3086 = `div`;
  const Component3119 = `div`;
  const Component3120 = `path`;
  const Component3121 = `path`;
  const Component3122 = `svg`;
  const Component3123 = `span`;
  const Component3124 = `div`;
  const Component3125 = `span`;
  const Component3126 = `button`;
  const Component3127 = `span`;
  const Component3128 = `button`;
  const Component3129 = `span`;
  const Component3130 = `button`;
  const Component3131 = `div`;
  const Component3132 = `div`;
  const Component3133 = `div`;
  const Component3134 = `circle`;
  const Component3135 = `line`;
  const Component3136 = `line`;
  const Component3137 = `svg`;
  const Component3138 = `div`;
  const Component3139 = `h3`;
  const Component3140 = `p`;
  const Component3141 = `div`;
  const Component3142 = `div`;
  const Component3143 = `p`;
  const Component3144 = `button`;
  const Component3145 = `button`;
  const Component3146 = `div`;
  const Component3147 = `div`;
  const Component3148 = `div`;
  const Component3149 = `div`;
  return <Component3149 style={{
    width: `100%`,
    height: `100%`
  }} ref={Le} onMouseMove={e => {
    if (Zt.current !== null) {
      return;
    }
    let t = e.clientX;
    let n = e.clientY;
    Zt.current = requestAnimationFrame(() => {
      Zt.current = null;
      let e = Yt.current;
      Qt();
      e ||= Yt.current;
      if (e) {
        Jt.current = {
          x: t - e.left,
          y: n - e.top
        };
      }
    });
  }}>
      <_cmp__Component131 value={Z.useMemo(() => {
      return {
        useThumbnail: le,
        lodLevel: yt,
        viewportMoving: false,
        nodeCount: De.length,
        handleFollowLimit: 60,
        edgeFxLimit: 50
      };
    }, [le, yt, De.length])}>
        <Component2910 type={`file`} ref={Re} style={{
        display: `none`
      }} accept={`image/*,video/*,audio/*,text/plain`} onChange={bi} />
        <_Component128 nodes={Xi} edges={ta} onNodesChange={ke} onEdgesChange={Me} onConnect={Yn} onEdgeDoubleClick={Vi} nodeTypes={O_} edgeTypes={k_} connectionLineComponent={_cmp_Pg} connectionRadius={60} onNodeClick={tr} onPaneContextMenu={Zn} onNodeContextMenu={Qn} onSelectionContextMenu={$n} onSelectionEnd={er} onPaneClick={nr} onDragOver={ki} onDrop={Ai} onConnectEnd={Oi} onMoveStart={Wt} onMoveEnd={Kt} onNodeDragStop={on} onSelectionDragStop={cn} deleteKeyCode={[`Backspace`, `Delete`]} fitView={true} fitViewOptions={{
        padding: 0.2,
        maxZoom: 1,
        minZoom: 0.05
      }} minZoom={0.05} maxZoom={4} elevateNodesOnSelect={false} elevateEdgesOnSelect={false} nodeOrigin={[0, 0]} className={`bg-[#0d0c0c] ${Yi ? `performance-large-canvas` : ``}`} proOptions={{
        hideAttribution: true
      }} onlyRenderVisibleElements={De.length > 20} nodesDraggable={true} nodesConnectable={true} selectionOnDrag={De.length <= 80} panOnDrag={true}>
          <_Component122 onLodChange={xt} enablePerformanceMode={ge} />
          {ge && yt >= 2 && <_Component123 position={`top-center`} className={`mt-4 pointer-events-none`}>
              <Component2911 className={`bg-yellow-500/20 border border-yellow-500/50 text-yellow-200 px-4 py-2 rounded-full text-xs font-bold shadow-lg backdrop-blur-sm flex items-center gap-2 animate-pulse`}>
                <_Component20 size={14} className={`text-yellow-400`} />
                {yt === 3 ? `已进入全局性能模式 (图片视频已隐藏)` : `低缩放性能模式 (图片已隐藏)`}
              </Component2911>
            </_Component123>}
          {De.length > 300 && <_Component123 position={`top-left`} className={`mt-4 ml-4 pointer-events-none opacity-60 hover:opacity-100 transition-opacity`}>
              <Component2916 className={`bg-orange-900/40 border border-orange-500/40 text-orange-200 px-3 py-1.5 rounded text-xs shadow-lg flex items-center gap-2 backdrop-blur-sm`}>
                <Component2915 xmlns={`http://www.w3.org/2000/svg`} width={`14`} height={`14`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`} className={`text-orange-400`}>
                  <Component2912 d={`m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z`} />
                  <Component2913 d={`M12 9v4`} />
                  <Component2914 d={`M12 17h.01`} />
                </Component2915>
                {`当前节点数(`}
                {De.length}
                {`)较多，可能影响流畅度，建议分拆新建项目。`}
              </Component2916>
            </_Component123>}
          {Sn && <_Component123 position={`top-center`} className={`mt-16 pointer-events-none`}>
              <Component2917 className={`bg-red-500/20 border border-red-500/50 text-red-200 px-4 py-2 rounded-full text-xs font-bold shadow-lg backdrop-blur-sm flex items-center gap-2 pointer-events-auto cursor-pointer`} onClick={() => {
            return window.location.reload();
          }} title={`点击刷新页面`}>
                <_Component17 size={14} className={`text-red-400 animate-pulse`} />
                {`检测到该画布在其他窗口被修改，继续保存可能会覆盖数据，强烈建议点击此处刷新页面！`}
              </Component2917>
            </_Component123>}
          <_Component123 position={`bottom-left`} className={`flex flex-col gap-2 mb-2 ml-2`}>
            {tt && <Component2922 className={`absolute bottom-full left-0 mb-4 bg-[#222] border border-[#333] rounded-xl shadow-2xl p-4 w-[240px] text-gray-300 animate-slide-up origin-bottom-left z-[1000] pointer-events-auto`}>
                <Component2918 className={`text-sm font-medium mb-4 text-center`}>{`是否保留此次整理结果？`}</Component2918>
                <Component2921 className={`flex items-center gap-3`}>
                  <Component2919 onClick={() => {
                K(tt.nodes);
                q(tt.edges);
                nt(null);
                setTimeout(() => {
                  return Ue({
                    padding: 0.2,
                    duration: 800,
                    maxZoom: 1
                  });
                }, 100);
              }} className={`flex-1 py-1.5 rounded-lg text-sm transition-colors text-gray-400 hover:text-white hover:bg-[#333] border border-[#444]`}>{`还原`}</Component2919>
                  <Component2920 onClick={() => {
                return nt(null);
              }} className={`flex-1 py-1.5 rounded-lg text-sm font-medium transition-colors bg-[#333] text-white hover:bg-[#444] border border-[#555]`}>{`保留`}</Component2920>
                </Component2921>
              </Component2922>}
            <Component2950 className={`flex items-center gap-3`}>
              <Component2941 className={`flex items-center bg-[#222] border border-[#333] rounded-full px-2 py-1 shadow-xl`}>
                <Component2925 onClick={() => {
                ti();
              }} className={`p-2 rounded-full transition-colors flex items-center justify-center text-green-400 hover:text-white hover:bg-[#333]`} title={`运行整个工作流（从所有起点依次执行）`}>
                  <Component2924 xmlns={`http://www.w3.org/2000/svg`} width={`16`} height={`16`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`}>
                    <Component2923 points={`5 3 19 12 5 21 5 3`} />
                  </Component2924>
                </Component2925>
                <Component2926 className={`w-[1px] h-4 bg-[#444] mx-2`} />
                <Component2927 onClick={Ui} className={`p-2 rounded-full transition-colors flex items-center justify-center text-gray-400 hover:text-white hover:bg-[#333]`} title={`整理画布`}>
                  <Et size={16} />
                </Component2927>
                <Component2928 onClick={() => {
                return dn(!un);
              }} className={`p-2 rounded-full transition-colors flex items-center justify-center ${un ? `text-white` : `text-gray-400 hover:text-white hover:bg-[#333]`}`} title={`画布小地图`}>
                  <_Component124 size={16} />
                </Component2928>
                <Component2929 onClick={Ki} className={`p-2 rounded-full transition-colors flex items-center justify-center text-gray-400 hover:text-white hover:bg-[#333]`} title={`清理缓存：将内联大资源转为本地URL`}>
                  <He size={16} />
                </Component2929>
                <Component2935 onClick={() => {
                return Ue({
                  padding: 0.2,
                  duration: 800
                });
              }} className={`p-2 rounded-full transition-colors flex items-center justify-center text-gray-400 hover:text-white hover:bg-[#333]`} title={`适合视图`}>
                  <Component2934 xmlns={`http://www.w3.org/2000/svg`} width={`16`} height={`16`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`}>
                    <Component2930 d={`M3 7V5a2 2 0 0 1 2-2h2`} />
                    <Component2931 d={`M17 3h2a2 2 0 0 1 2 2v2`} />
                    <Component2932 d={`M21 17v2a2 2 0 0 1-2 2h-2`} />
                    <Component2933 d={`M7 21H5a2 2 0 0 1-2-2v-2`} />
                  </Component2934>
                </Component2935>
                {_e && <Component2936 onClick={_e} className={`p-2 rounded-full transition-colors flex items-center justify-center ${ge ? `text-yellow-400 hover:text-yellow-300` : `text-gray-400 hover:text-white hover:bg-[#333]`}`} title={ge ? `缩放性能模式已开启` : `缩放性能模式已关闭`}>
                    <_Component20 size={16} />
                  </Component2936>}
                <Component2937 className={`w-[1px] h-4 bg-[#444] mx-2`} />
                <Component2938 onClick={() => {
                return Ke({
                  duration: 300
                });
              }} className={`p-1.5 rounded-full transition-colors text-gray-400 hover:text-white hover:bg-[#333]`}>
                  <_Component47 size={14} />
                </Component2938>
                <Component2939 className={`text-xs text-white font-medium min-w-[36px] text-center cursor-default select-none`}>
                  <_cmp_B_ />
                </Component2939>
                <Component2940 onClick={() => {
                return Ge({
                  duration: 300
                });
              }} className={`p-1.5 rounded-full transition-colors text-gray-400 hover:text-white hover:bg-[#333]`}>
                  <Xt size={14} />
                </Component2940>
              </Component2941>
              <Component2949 className={`flex items-center bg-[#222] border border-[#333] rounded-full px-1 py-1 shadow-xl`}>
                <Component2948 className={`relative group/help`}>
                  <Component2942 className={`p-2 rounded-full transition-colors flex items-center justify-center text-gray-400 hover:text-white hover:bg-[#333]`}>
                    <_Component125 size={16} />
                  </Component2942>
                  <Component2947 className={`absolute bottom-full left-0 mb-2 w-32 bg-[#1a1a1a] border border-[#333] rounded-xl shadow-xl opacity-0 invisible group-hover/help:opacity-100 group-hover/help:visible transition-all duration-200 z-50 overflow-hidden py-1`}>
                    <Component2943 href={qn(`/docs`)} target={`_blank`} className={`flex items-center gap-2 px-3 py-2 text-sm text-gray-300 hover:bg-[#333] hover:text-white`}>
                      <Dt size={14} />
                      {` 使用教程`}
                    </Component2943>
                    <Component2944 onClick={() => {
                    window.dispatchEvent(new CustomEvent(`open-shortcuts-modal`));
                  }} className={`w-full flex items-center gap-2 px-3 py-2 text-sm text-gray-300 hover:bg-[#333] hover:text-white text-left`}>
                      <V size={14} />
                      {` 快捷键`}
                    </Component2944>
                    <Component2945 className={`h-[1px] bg-[#333] my-1`} />
                    <Component2946 href={`https://test-cyfyd24zfbua.feishu.cn/share/base/form/shrcnhVA6iRUBLQPfF3ej9yMzKf`} target={`_blank`} className={`flex items-center gap-2 px-3 py-2 text-sm text-gray-300 hover:bg-[#333] hover:text-white`}>
                      <_Component10 size={14} />
                      {` 提交反馈`}
                    </Component2946>
                  </Component2947>
                </Component2948>
              </Component2949>
            </Component2950>
          </_Component123>
          <Component2952 className={`absolute left-4 bottom-16 z-[990] flex flex-col items-start gap-2 pointer-events-none`}>
            <Component2951 className={`transition-all duration-300 origin-bottom-left ${un ? `scale-100 opacity-100 pointer-events-auto` : `scale-90 opacity-0 pointer-events-none absolute bottom-0 left-0`}`}>
              {De.length < 100 && <_shared.P pannable={true} zoomable={true} maskColor={`#0d0c0c80`} className={`!bg-[#222] !m-0 !relative !bottom-0 !left-0 shadow-2xl rounded overflow-hidden border border-[#333] ${un ? `!pointer-events-auto` : `!pointer-events-none`}`} nodeColor={`#444`} />}
            </Component2951>
          </Component2952>
          <Oe variant={E.Dots} gap={20} size={1} color={`#333`} />
          {$e && <Component3032 className={`absolute inset-0 z-[1000] flex items-center justify-center bg-black/40 backdrop-blur-sm`} onClick={() => {
          return et(false);
        }}>
              <Component3031 className={`bg-[#222] border border-[#333] rounded-xl shadow-2xl p-6 w-[560px] text-gray-300 relative`} onClick={e => {
            return e.stopPropagation();
          }}>
                <Component2953 className={`absolute top-4 right-4 text-gray-500 hover:text-white transition-colors`} onClick={() => {
              return et(false);
            }}>
                  <Gt size={16} />
                </Component2953>
                <Component3030 className={`grid grid-cols-2 gap-8 mt-2`}>
                  <Component2983>
                    <Component2954 className={`text-gray-500 text-sm mb-4 font-bold`}>{`缩放`}</Component2954>
                    <Component2973 className={`space-y-3 text-sm`}>
                      <Component2959 className={`flex justify-between items-center`}>
                        <Component2955>{`放大`}</Component2955>
                        <Component2958 className={`flex items-center gap-1`}>
                          <Component2956 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component2956>
                          <Component2957 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`+`}</Component2957>
                        </Component2958>
                      </Component2959>
                      <Component2964 className={`flex justify-between items-center`}>
                        <Component2960>{`缩小`}</Component2960>
                        <Component2963 className={`flex items-center gap-1`}>
                          <Component2961 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component2961>
                          <Component2962 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`-`}</Component2962>
                        </Component2963>
                      </Component2964>
                      <Component2972 className={`flex justify-between items-center`}>
                        <Component2965>{`鼠标`}</Component2965>
                        <Component2971 className={`flex items-center gap-1`}>
                          <Component2966 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component2966>
                          <Component2970 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs flex items-center`}>
                            <Component2969 width={`12`} height={`12`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`}>
                              <Component2967 x={`5`} y={`2`} width={`14`} height={`20`} rx={`7`} />
                              <Component2968 d={`M12 6v4`} />
                            </Component2969>
                          </Component2970>
                        </Component2971>
                      </Component2972>
                    </Component2973>
                    <Component2974 className={`text-gray-500 text-sm mt-8 mb-4 font-bold`}>{`移动画布`}</Component2974>
                    <Component2982 className={`space-y-3 text-sm`}>
                      <Component2981 className={`flex justify-between items-center`}>
                        <Component2975>{`鼠标`}</Component2975>
                        <Component2980 className={`flex items-center gap-1`}>
                          <Component2979 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs flex items-center gap-1`}>
                            <Component2978 width={`12`} height={`12`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`}>
                              <Component2976 x={`5`} y={`2`} width={`14`} height={`20`} rx={`7`} />
                              <Component2977 d={`M12 6v4`} />
                            </Component2978>
                            {`拖拽`}
                          </Component2979>
                        </Component2980>
                      </Component2981>
                    </Component2982>
                  </Component2983>
                  <Component3029>
                    <Component2984 className={`text-gray-500 text-sm mb-4 font-bold`}>{`快速添加节点`}</Component2984>
                    <Component2997 className={`space-y-3 text-sm`}>
                      <Component2988 className={`flex justify-between items-center`}>
                        <Component2985>{`文本节点`}</Component2985>
                        <Component2987 className={`flex items-center gap-1`}>
                          <Component2986 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs font-bold`}>{`Q`}</Component2986>
                        </Component2987>
                      </Component2988>
                      <Component2992 className={`flex justify-between items-center`}>
                        <Component2989>{`生图节点`}</Component2989>
                        <Component2991 className={`flex items-center gap-1`}>
                          <Component2990 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs font-bold`}>{`W`}</Component2990>
                        </Component2991>
                      </Component2992>
                      <Component2996 className={`flex justify-between items-center`}>
                        <Component2993>{`特惠视频节点`}</Component2993>
                        <Component2995 className={`flex items-center gap-1`}>
                          <Component2994 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs font-bold`}>{`E`}</Component2994>
                        </Component2995>
                      </Component2996>
                    </Component2997>
                    <Component2998 className={`text-gray-500 text-sm mt-8 mb-4 font-bold`}>{`其他`}</Component2998>
                    <Component3028 className={`space-y-3 text-sm`}>
                      <Component3003 className={`flex justify-between items-center`}>
                        <Component2999>{`克隆`}</Component2999>
                        <Component3002 className={`flex items-center gap-1`}>
                          <Component3000 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component3000>
                          <Component3001 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`D`}</Component3001>
                        </Component3002>
                      </Component3003>
                      <Component3007 className={`flex justify-between items-center`}>
                        <Component3004>{`删除`}</Component3004>
                        <Component3006 className={`flex items-center gap-1`}>
                          <Component3005 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Delete`}</Component3005>
                        </Component3006>
                      </Component3007>
                      <Component3012 className={`flex justify-between items-center`}>
                        <Component3008>{`撤销`}</Component3008>
                        <Component3011 className={`flex items-center gap-1`}>
                          <Component3009 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component3009>
                          <Component3010 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Z`}</Component3010>
                        </Component3011>
                      </Component3012>
                      <Component3017 className={`flex justify-between items-center`}>
                        <Component3013>{`重做`}</Component3013>
                        <Component3016 className={`flex items-center gap-1`}>
                          <Component3014 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component3014>
                          <Component3015 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Y`}</Component3015>
                        </Component3016>
                      </Component3017>
                      <Component3022 className={`flex justify-between items-center`}>
                        <Component3018>{`复制`}</Component3018>
                        <Component3021 className={`flex items-center gap-1`}>
                          <Component3019 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component3019>
                          <Component3020 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`C`}</Component3020>
                        </Component3021>
                      </Component3022>
                      <Component3027 className={`flex justify-between items-center`}>
                        <Component3023>{`粘贴`}</Component3023>
                        <Component3026 className={`flex items-center gap-1`}>
                          <Component3024 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`Ctrl`}</Component3024>
                          <Component3025 className={`bg-[#1a1a1a] border border-[#333] px-2 py-1 rounded text-xs`}>{`V`}</Component3025>
                        </Component3026>
                      </Component3027>
                    </Component3028>
                  </Component3029>
                </Component3030>
              </Component3031>
            </Component3032>}
          {Fe && <Component3119 style={Ji()} className={`absolute z-50 bg-[#1c1c1e] border border-white/[0.04] rounded-2xl shadow-[0_20px_60px_-10px_rgba(0,0,0,0.85)] p-2 flex flex-col min-w-[208px]`} onClick={e => {
          return e.stopPropagation();
        }}>
              {Fe.type === `canvas` || Fe.type === `connection` ? <Q.Fragment>
                  <Component3036 className={`w-full text-left px-3.5 py-2.5 text-sm text-gray-200 hover:bg-white/10 hover:text-white rounded-xl flex items-center justify-between transition-colors`} onClick={() => {
              return di(`textNode`, gi(), {
                text: ``
              }, Fe.connection);
            }}>
                    <Component3034 className={`flex items-center gap-2.5`}>
                      <_Component18 size={16} className={`text-green-500`} />
                      <Component3033>{`文本`}</Component3033>
                    </Component3034>
                    <Component3035 className={`text-[11px] text-gray-500 ml-3 font-mono`}>{`Q`}</Component3035>
                  </Component3036>
                  <Component3040 className={`w-full text-left px-3.5 py-2.5 text-sm text-gray-200 hover:bg-white/10 hover:text-white rounded-xl flex items-center justify-between transition-colors`} onClick={() => {
              return di(`promptNode`, gi(), {
                prompt: ``
              }, Fe.connection);
            }}>
                    <Component3038 className={`flex items-center gap-2.5`}>
                      <_Component2 size={16} className={`text-blue-400`} />
                      <Component3037>{`图片`}</Component3037>
                    </Component3038>
                    <Component3039 className={`text-[11px] text-gray-500 ml-3 font-mono`}>{`W`}</Component3039>
                  </Component3040>
                  <Component3044 className={`w-full text-left px-3.5 py-2.5 text-sm text-gray-200 hover:bg-white/10 hover:text-white rounded-xl flex items-center justify-between transition-colors`} onClick={() => {
              return di(`discountVideoNode`, gi(), {
                prompt: ``
              }, Fe.connection);
            }}>
                    <Component3042 className={`flex items-center gap-2.5`}>
                      <_Component41 size={16} className={`text-yellow-500`} />
                      <Component3041>{`视频`}</Component3041>
                    </Component3042>
                    <Component3043 className={`text-[11px] text-gray-500 ml-3 font-mono`}>{`E`}</Component3043>
                  </Component3044>
                  <Component3048 className={`w-full text-left px-3.5 py-2.5 text-sm text-gray-200 hover:bg-white/10 hover:text-white rounded-xl flex items-center justify-between transition-colors`} onClick={() => {
              return di(`scriptBoxNode`, gi(), {
                step: 1,
                shots: [],
                assets: []
              }, Fe.connection);
            }}>
                    <Component3046 className={`flex items-center gap-2.5`}>
                      <Dt size={16} className={`text-fuchsia-300`} />
                      <Component3045>{`剧本盒子`}</Component3045>
                    </Component3046>
                    <Component3047 className={`text-[10px] text-gray-500`}>{`Beta`}</Component3047>
                  </Component3048>
                  <Component3050 className={`w-full text-left px-3.5 py-2.5 text-sm text-gray-200 hover:bg-white/10 hover:text-white rounded-xl flex items-center gap-2.5 transition-colors`} onClick={() => {
              return di(`rhWebappNode`, gi(), {
                webappName: `AI应用`,
                webappDesc: ``,
                openAppSelectorOnMount: true
              }, Fe.connection);
            }}>
                    <_Component48 size={16} className={`text-cyan-400`} />
                    <Component3049>{`AI应用`}</Component3049>
                  </Component3050>
                  <Component3051 className={`h-[1px] bg-white/[0.04] my-2 mx-1`} />
                  <Component3066 className={`group/tools`} onMouseEnter={dt} onMouseLeave={ft}>
                    <Component3055 className={`w-full text-left px-3.5 py-2 text-[11px] text-gray-400 hover:text-gray-200 rounded-xl flex items-center gap-2 justify-between transition-colors`} onClick={e => {
                return e.stopPropagation();
              }}>
                      <Component3053 className={`flex items-center gap-2`}>
                        <Ft size={13} className={`text-gray-400`} />
                        <Component3052>{`小工具`}</Component3052>
                      </Component3053>
                      <Component3054 className={`text-xl text-gray-400 leading-none`}>{`›`}</Component3054>
                    </Component3055>
                    {at && <Component3065 className={`absolute left-full top-0 ml-2 bg-[#1c1c1e] border border-white/[0.04] rounded-2xl shadow-[0_20px_60px_-10px_rgba(0,0,0,0.85)] p-2 w-[360px] z-50 before:content-[''] before:absolute before:-left-3 before:top-0 before:w-3 before:h-full`} onMouseEnter={dt} onMouseLeave={ft}>
                        <Component3056 className={`px-1 pb-1 text-[10px] text-gray-500`}>{`勾选可固定到右键菜单`}</Component3056>
                        {vi.map(e => {
                  return <Component3064 className={`mb-1.5 last:mb-0`} key={e.key}>
                              <Component3057 className={`px-1 py-1 text-[10px] text-gray-500`}>
                                {e.label}
                              </Component3057>
                              <Component3063 className={`grid grid-cols-2 gap-0.5`}>
                                {_i.filter(t => {
                        return t.cat === e.key;
                      }).map(e => {
                        let t = pt.includes(e.type);
                        const Component3058 = `span`;
                        const Component3059 = `span`;
                        const Component3060 = `button`;
                        const Component3061 = `button`;
                        const Component3062 = `div`;
                        return <Component3062 className={`group/titem flex items-center rounded-lg hover:bg-white/10 transition-colors`} key={e.type}>
                                        <Component3060 className={`flex-1 min-w-0 text-left px-2.5 py-1.5 text-[13px] text-gray-200 hover:text-white flex items-center gap-2`} onClick={() => {
                            return yi(e);
                          }}>
                                          {e.icon}
                                          <Component3058 className={`truncate`}>
                                            {e.label}
                                          </Component3058>
                                          {e.badge && <Component3059 className={`rounded px-1 py-0.5 text-[8px] font-semibold ${e.badge.tone === `new` ? `bg-emerald-500/20 text-emerald-300` : `bg-fuchsia-500/20 text-fuchsia-300`}`}>
                                              {e.badge.text}
                                            </Component3059>}
                                        </Component3060>
                                        <Component3061 className={`p-1 mr-1 rounded transition-colors ${t ? `text-white hover:text-gray-200` : `text-gray-500 hover:text-gray-300`}`} title={t ? `已固定，点击取消` : `固定到右键菜单`} onClick={t => {
                            t.stopPropagation();
                            vt(e.type);
                          }}>
                                          <Se size={14} className={t ? `opacity-100` : `opacity-30`} />
                                        </Component3061>
                                      </Component3062>;
                      })}
                              </Component3063>
                            </Component3064>;
                })}
                      </Component3065>}
                  </Component3066>
                  {L && L.length > 0 && <Component3075 className={`relative group/custom`}>
                      <Component3070 className={`w-full text-left px-3.5 py-2.5 text-sm text-gray-200 hover:bg-white/10 rounded-xl flex items-center gap-2.5 justify-between transition-colors`} onClick={e => {
                return e.stopPropagation();
              }}>
                        <Component3068 className={`flex items-center gap-2.5`}>
                          <_Component45 size={16} className={`text-white`} />
                          <Component3067>{`自定义`}</Component3067>
                        </Component3068>
                        <Component3069 className={`text-xl text-gray-400 leading-none`}>{`›`}</Component3069>
                      </Component3070>
                      <Component3074 className={`absolute left-full top-0 ml-2 bg-[#1c1c1e] border border-white/[0.04] rounded-2xl shadow-[0_20px_60px_-10px_rgba(0,0,0,0.85)] p-2 min-w-[200px] hidden group-hover/custom:block z-50 before:content-[''] before:absolute before:-left-3 before:top-0 before:w-3 before:h-full`}>
                        {L.map(e => {
                  return <Component3073 className={`group/item flex items-center justify-between hover:bg-white/10 rounded-xl`} key={e.id}>
                              <Component3071 className={`text-left px-3 py-2 text-sm text-gray-200 hover:text-white flex-1`} onClick={t => {
                      t.stopPropagation();
                      let n = e.config?.variables || {};
                      di(`customNode`, gi(150), {
                        label: e.name,
                        config: e.config,
                        configMode: false,
                        variables: n
                      }, Fe.connection);
                      Ie(null);
                    }}>
                                {e.name}
                              </Component3071>
                              <Component3072 className={`p-1.5 text-gray-500 hover:text-red-400 opacity-0 group-hover/item:opacity-100 transition-opacity`} onClick={t => {
                      t.stopPropagation();
                      if (window.confirm(`确定要删除自定义节点 "${e.name}" 吗？`)) {
                        ne?.(e.id);
                      }
                    }} title={`删除`}>
                                <Ot size={14} />
                              </Component3072>
                            </Component3073>;
                })}
                      </Component3074>
                    </Component3075>}
                  {_i.filter(e => {
              return pt.includes(e.type);
            }).length > 0 && <Q.Fragment>
                      {_i.filter(e => {
                return pt.includes(e.type);
              }).map(e => {
                return <Component3078 className={`w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] hover:text-white rounded flex items-center gap-2`} onClick={() => {
                  return yi(e);
                }} key={e.type}>
                              {e.icon}
                              <Component3076>{e.label}</Component3076>
                              {e.badge && <Component3077 className={`ml-auto rounded px-1 py-0.5 text-[8px] font-semibold ${e.badge.tone === `new` ? `bg-emerald-500/20 text-emerald-300` : `bg-fuchsia-500/20 text-fuchsia-300`}`}>
                                  {e.badge.text}
                                </Component3077>}
                            </Component3078>;
              })}
                    </Q.Fragment>}
                  <Component3079 className={`h-[1px] bg-white/[0.04] my-2 mx-1`} />
                  <Component3081 className={`w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] hover:text-white rounded flex items-center gap-2`} onClick={() => {
              return Re.current?.click();
            }}>
                    <_Component0 size={16} className={`text-gray-400`} />
                    <Component3080>{`上传`}</Component3080>
                  </Component3081>
                  <Component3083 className={`w-full text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] hover:text-white rounded flex items-center gap-2`} onClick={() => {
              Ln();
              Ie(null);
            }}>
                    <_Component126 size={16} className={`text-white`} />
                    <Component3082>{`模板库`}</Component3082>
                  </Component3083>
                  <Component3086 className={`relative`}>
                    <Component3085 className={`w-full text-left px-3.5 py-2.5 text-sm text-gray-200 hover:bg-white/10 hover:text-white rounded-xl flex items-center gap-2.5 transition-colors`} onClick={e => {
                e.stopPropagation();
                it(true);
              }}>
                      <_Component2 size={16} className={`text-gray-400`} />
                      <Component3084>{`导入`}</Component3084>
                    </Component3085>
                    <_cmp__Component118 isOpen={rt} onClose={() => {
                it(false);
              }} transitResources={A} canvasNodes={De} onImportToCanvas={async e => {
                let t = Ve({
                  x: Fe.x + (Le.current?.getBoundingClientRect().left || 0) + 100,
                  y: Fe.y + (Le.current?.getBoundingClientRect().top || 0)
                });
                if (e.type === `text`) {
                  let n = e.url;
                  if (/^https?:\/\//i.test(e.url)) {
                    try {
                      let t = await fetch(e.url);
                      if (t.ok) {
                        n = await t.text();
                      }
                    } catch {}
                  }
                  di(`textNode`, t, {
                    text: n,
                    label: e.name || `文本素材`
                  }, undefined);
                } else if (e.type === `video`) {
                  di(`discountVideoNode`, t, {
                    videoUrl: e.url,
                    videoName: e.name || `视频素材`,
                    label: e.name || `视频素材`,
                    expanded: true
                  }, undefined);
                } else if (e.type === `audio`) {
                  di(`audioPlayerNode`, t, {
                    audioUrl: e.url,
                    audioName: e.name || `音频素材`
                  }, undefined);
                } else {
                  di(`imageNode`, t, {
                    imageUrl: e.url,
                    label: e.name || `图片素材`
                  }, undefined);
                }
                it(false);
                Ie(null);
              }} />
                  </Component3086>
                </Q.Fragment> : (() => {
            let e = De.find(e => {
              return e.id === Fe?.nodeId;
            });
            let t = De.filter(e => {
              return e.selected;
            });
            if (t.length > 1 && (e?.selected || Fe?.type === `selection`)) {
              let e = t.length >= 2 && t.every(e => {
                return e.type === `imageNode` && e.data?.imageUrl;
              });
              const Component3087 = `span`;
              const Component3088 = `button`;
              const Component3089 = `span`;
              const Component3090 = `button`;
              const Component3091 = `span`;
              const Component3092 = `button`;
              const Component3093 = `path`;
              const Component3094 = `path`;
              const Component3095 = `svg`;
              const Component3096 = `span`;
              const Component3097 = `button`;
              const Component3098 = `span`;
              const Component3099 = `button`;
              const Component3100 = `div`;
              const Component3101 = `span`;
              const Component3102 = `button`;
              return <Q.Fragment>
                        <Component3088 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={e => {
                  e.stopPropagation();
                  Ci();
                  Ie(null);
                }}>
                          <_Component7 size={16} className={`text-gray-400`} />
                          <Component3087>
                            {`复制 (`}
                            {t.length}
                            {`)`}
                          </Component3087>
                        </Component3088>
                        <Component3090 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={e => {
                  e.stopPropagation();
                  Gi();
                  Ie(null);
                }}>
                          <Et size={16} className={`text-gray-400`} />
                          <Component3089>{`组合`}</Component3089>
                        </Component3090>
                        {G && <Component3092 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={e => {
                  e.stopPropagation();
                  In();
                  Ie(null);
                }}>
                            <_Component126 size={16} className={`text-gray-400`} />
                            <Component3091>{`创建模板`}</Component3091>
                          </Component3092>}
                        <Component3097 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={e => {
                  e.stopPropagation();
                  tn(t.map(e => {
                    return e.id;
                  }));
                  k(`请点击要连接到的目标节点`);
                  Ie(null);
                }}>
                          <Component3095 xmlns={`http://www.w3.org/2000/svg`} width={`16`} height={`16`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`} className={`text-blue-400`}>
                            <Component3093 d={`M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71`} />
                            <Component3094 d={`M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71`} />
                          </Component3095>
                          <Component3096>{`多项连接`}</Component3096>
                        </Component3097>
                        {e && <Component3099 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={e => {
                  e.stopPropagation();
                  Wi(t.map(e => {
                    return e.id;
                  }));
                  Ie(null);
                }}>
                            <_Component30 size={16} className={`text-gray-400`} />
                            <Component3098>
                              {`合并为图片盒子 (`}
                              {t.length}
                              {`)`}
                            </Component3098>
                          </Component3099>}
                        <Component3100 className={`h-[1px] bg-[#333] my-1`} />
                        <Component3102 className={`text-left px-3 py-2 text-sm text-red-400 hover:bg-[#333] rounded flex items-center gap-2`} onClick={e => {
                  e.stopPropagation();
                  Si();
                  Ie(null);
                }}>
                          <Ot size={16} className={`text-red-400`} />
                          <Component3101>{`删除`}</Component3101>
                        </Component3102>
                      </Q.Fragment>;
            }
            const Component3103 = `span`;
            const Component3104 = `button`;
            const Component3105 = `div`;
            const Component3106 = `polygon`;
            const Component3107 = `svg`;
            const Component3108 = `span`;
            const Component3109 = `button`;
            const Component3110 = `div`;
            const Component3111 = `span`;
            const Component3112 = `button`;
            const Component3113 = `span`;
            const Component3114 = `button`;
            const Component3115 = `span`;
            const Component3116 = `button`;
            const Component3117 = `span`;
            const Component3118 = `button`;
            return <Q.Fragment>
                      {e?.type === `group` && <Q.Fragment>
                          <Component3104 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={e => {
                  e.stopPropagation();
                  if (Fe?.nodeId) {
                    qi(Fe.nodeId);
                  }
                  Ie(null);
                }}>
                            <Et size={16} className={`text-gray-400`} />
                            <Component3103>{`取消编组`}</Component3103>
                          </Component3104>
                          <Component3105 className={`h-[1px] bg-[#333] my-1`} />
                        </Q.Fragment>}
                      <Component3109 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={() => {
                if (Fe?.nodeId) {
                  ei(Fe.nodeId);
                }
                Ie(null);
              }}>
                        <Component3107 xmlns={`http://www.w3.org/2000/svg`} width={`16`} height={`16`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`} className={`text-green-400`}>
                          <Component3106 points={`5 3 19 12 5 21 5 3`} />
                        </Component3107>
                        <Component3108>{`依次运行后续节点`}</Component3108>
                      </Component3109>
                      <Component3110 className={`h-[1px] bg-[#333] my-1`} />
                      <Component3112 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={Ci}>
                        <_Component7 size={16} className={`text-gray-400`} />
                        <Component3111>{`复制节点`}</Component3111>
                      </Component3112>
                      {(e?.type === `imageNode` || e?.type === `promptNode`) && <Q.Fragment>
                          <Component3114 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={Ei}>
                            <_Component2 size={16} className={`text-gray-400`} />
                            <Component3113>{`复制图片`}</Component3113>
                          </Component3114>
                          <Component3116 className={`text-left px-3 py-2 text-sm text-gray-300 hover:bg-[#333] rounded flex items-center gap-2`} onClick={Di}>
                            <Et size={16} className={`text-gray-400`} />
                            <Component3115>{`图片切分`}</Component3115>
                          </Component3116>
                        </Q.Fragment>}
                      <Component3118 className={`text-left px-3 py-2 text-sm text-red-400 hover:bg-[#333] rounded flex items-center gap-2`} onClick={Si}>
                        <Ot size={16} className={`text-red-400`} />
                        <Component3117>{`删除`}</Component3117>
                      </Component3118>
                    </Q.Fragment>;
          })()}
            </Component3119>}
          <_cmp__Component127 visible={!Ne || St} />
        </_Component128>
        {Ne && De.length === 0 && !St && !Fe && <Component3133 className={`absolute inset-0 z-10 flex flex-col items-center justify-center pointer-events-none`}>
            <Component3132 className={`flex flex-col items-center pointer-events-auto transform -translate-y-10`}>
              <Component3124 className={`bg-[#1c1c1c] rounded-full px-5 py-2.5 flex items-center gap-3 mb-10 border border-[#333] shadow-lg`}>
                <Component3122 xmlns={`http://www.w3.org/2000/svg`} width={`18`} height={`18`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`} className={`text-gray-300`}>
                  <Component3120 d={`M3 3v18h18`} />
                  <Component3121 d={`m19 9-5 5-4-4-3 3`} />
                </Component3122>
                <Component3123 className={`text-base font-medium text-gray-200 tracking-wide`}>{`右键自由生成你的想象`}</Component3123>
              </Component3124>
              <Component3131 className={`flex items-center gap-4`}>
                <Component3126 onClick={() => {
              let e = Le.current?.getBoundingClientRect();
              let t = e ? e.width - (ye ? be : 0) : 0;
              di(`textNode`, e ? Ve({
                x: e.left + t / 2 - 150,
                y: e.top + e.height / 2
              }) : {
                x: 0,
                y: 0
              }, {
                text: ``
              });
            }} className={`flex items-center gap-2 px-6 py-3.5 bg-transparent hover:bg-[#222] border border-[#333] hover:border-gray-500 rounded-2xl transition-all text-gray-400 hover:text-gray-200`}>
                  <_Component18 size={18} className={`text-gray-500`} />
                  <Component3125 className={`text-sm font-medium`}>{`文字生成`}</Component3125>
                </Component3126>
                <Component3128 onClick={() => {
              let e = Le.current?.getBoundingClientRect();
              let t = e ? e.width - (ye ? be : 0) : 0;
              di(`promptNode`, e ? Ve({
                x: e.left + t / 2,
                y: e.top + e.height / 2
              }) : {
                x: 0,
                y: 0
              }, {
                prompt: ``
              });
            }} className={`flex items-center gap-2 px-6 py-3.5 bg-transparent hover:bg-[#222] border border-[#333] hover:border-gray-500 rounded-2xl transition-all text-gray-400 hover:text-gray-200`}>
                  <_Component2 size={18} className={`text-gray-500`} />
                  <Component3127 className={`text-sm font-medium`}>{`图片生成`}</Component3127>
                </Component3128>
                <Component3130 onClick={() => {
              let e = Le.current?.getBoundingClientRect();
              let t = e ? e.width - (ye ? be : 0) : 0;
              di(`discountVideoNode`, e ? Ve({
                x: e.left + t / 2 + 150,
                y: e.top + e.height / 2
              }) : {
                x: 0,
                y: 0
              }, {
                prompt: ``
              });
            }} className={`flex items-center gap-2 px-6 py-3.5 bg-transparent hover:bg-[#222] border border-[#333] hover:border-gray-500 rounded-2xl transition-all text-gray-400 hover:text-gray-200`}>
                  <Pt size={18} className={`text-gray-500`} />
                  <Component3129 className={`text-sm font-medium`}>{`特惠视频`}</Component3129>
                </Component3130>
              </Component3131>
            </Component3132>
          </Component3133>}
        {Xe && Fn.createPortal(<_cmp_D_ imageUrl={Xe} onClose={() => {
        return Ze(null);
      }} />, document.body)}
        {X && <_cmp__Component129 imageUrl={X.url} initialTool={X.initialTool} onSave={Bn} onClose={() => {
        return Qe(null);
      }} />}
        <_cmp__Component130 open={At} nodeCount={Nt.length} onClose={() => {
        return Mt(false);
      }} onSave={zi} />
        <_cmp_E_ open={Lt} onClose={() => {
        return Rt(false);
      }} onUse={async e => {
        let t = Le.current?.getBoundingClientRect();
        let n = t ? t.width - (ye ? be : 0) : 0;
        await Bi(e, t ? Ve({
          x: t.left + n / 2,
          y: t.top + t.height / 2
        }) : {
          x: 0,
          y: 0
        });
      }} onToast={k} />
        {Tt && <Component3148 className={`fixed inset-0 z-[9999] flex items-center justify-center bg-black/60`} onClick={e => {
        return e.stopPropagation();
      }}>
            <Component3147 className={`bg-[#1a1a1a] border border-[#333] rounded-2xl shadow-2xl p-6 w-[380px] max-w-[90vw]`}>
              <Component3142 className={`flex items-center gap-3 mb-4`}>
                <Component3138 className={`w-10 h-10 rounded-full bg-yellow-500/20 flex items-center justify-center`}>
                  <Component3137 xmlns={`http://www.w3.org/2000/svg`} width={`20`} height={`20`} viewBox={`0 0 24 24`} fill={`none`} stroke={`currentColor`} strokeWidth={`2`} strokeLinecap={`round`} strokeLinejoin={`round`} className={`text-yellow-400`}>
                    <Component3134 cx={`12`} cy={`12`} r={`10`} />
                    <Component3135 x1={`12`} y1={`8`} x2={`12`} y2={`12`} />
                    <Component3136 x1={`12`} y1={`16`} x2={`12.01`} y2={`16`} />
                  </Component3137>
                </Component3138>
                <Component3141>
                  <Component3139 className={`text-white font-medium`}>{`检测到中断的工作流`}</Component3139>
                  <Component3140 className={`text-gray-400 text-sm`}>
                    {`已完成 `}
                    {Tt.completedNodes.length}
                    {`/`}
                    {Tt.nodeExecOrder.length}
                    {` 个节点`}
                  </Component3140>
                </Component3141>
              </Component3142>
              <Component3143 className={`text-gray-400 text-sm mb-6`}>{`上次执行未正常完成，是否继续执行剩余节点？`}</Component3143>
              <Component3146 className={`flex gap-3`}>
                <Component3144 onClick={() => {
              kt(null);
              hr(e);
            }} className={`flex-1 py-2.5 rounded-xl text-sm font-medium text-gray-400 bg-[#333] hover:bg-[#444] transition-colors`}>{`取消`}</Component3144>
                <Component3145 onClick={() => {
              let e = Tt;
              kt(null);
              if (e) {
                ni(e);
              }
            }} className={`flex-1 py-2.5 rounded-xl text-sm font-medium text-white bg-blue-600 hover:bg-blue-500 transition-colors`}>{`继续执行`}</Component3145>
              </Component3146>
            </Component3147>
          </Component3148>}
      </_cmp__Component131>
    </Component3149>;
}