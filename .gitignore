# ════════════════════════════════════════════════════════════════
# .gitignore · 猫猫AI画布多端合一（自研后端替代版）
# 依据 CLAUDE.md §四 开发红线与分层铁律整理：
#   - 运行只读 dist/，src/bundle/ 是可编辑工程源码（应入库）
#   - localTool/dist/ 是 tsc 产物（不入库）
#   - localTool/.env 含真实 Lovart AK/SK（绝不入库）
# ════════════════════════════════════════════════════════════════

# ── 仓库根禁放生成产物（TD-23-1 · 2026-09-15）──
# 【为什么是一条反向判据，而不是逐文件补 ignore】根目录是"项目门面"，不是输出目录。
#   审计/闸报告常以 `npm run xxx > gate.txt` 这类**一次性 shell 重定向**落在根 ——
#   脚本本身并不写文件（所以「grep 生成器」是 0 命中），逐文件补 ignore 必漏，且下一次 `>` 又生成一份。
#   ⇒ 反向判据：**根级 .txt 一律不入库**（根连一个 .txt 都不该有）。
#   要留档的报告请放独立的产物目录（或直接看终端输出），**不要放仓库根**。
#   已按此清理：`agent-report.txt` / `gate.txt`（均为 2026-09-12 的一次性重定向快照，数字早已过期）。
/*.txt

# ── 依赖（体积大、可重装，不纳入版本库）──
node_modules/
.eslintcache

# ── 构建产物 ──
# localTool 的 tsc 编译产物（由 localTool/src 重建，不入库）
localTool/dist/
# 根前端 dist/：src/bundle/ 经 `npm run build` 回灌的运行时产物，
# 运行只读、每次 build 必变，属构建产物，不入库（见 CLAUDE.md §四.2/§四.5）。
dist/
# 回归测试 esbuild 临时 bundle（scripts/regression_test.cjs 运行期生成，不入库）
prototypes/react-nodes/.regression-entry.cjs
.regression-entry.cjs
.regression-entry.css
# Playwright E2E 运行产物（test-results/ / playwright-report/ / results.json 每次跑都会生成，不入库）
test-results/
playwright-report/
tests/e2e/results.json

# Vite / TS 构建缓存与类型增量
.vite/
*.tsbuildinfo

# ── Python ──
__pycache__/
*.pyc
# 依赖变更标记（启动脚本运行期生成，非源码）

# ── 环境变量 / 密钥（含真实 Lovart AK/SK，绝不入库）──
.env
.env.*

# ── 闭源引擎二进制交付物（无源码、体积大，仅本地运行用）──
*.exe
一毛AI画布引擎Mac端Arm版
一毛AI画布引擎Mac端Intel版

# ── dist 原版备份（美化前的单行压缩版，仅本地回退用）──
dist-orig/

# ── readable 阅读副本（rename.cjs 生成、与 src/bundle 1:1、可随时重建，不入库）──
# 由 scripts/rename.cjs 递归生成；_smoke_checks.checkReadableParity 缺失时会自动重建。
readable/

# ── 逆向还原目录：整目录入库（含中间产物 pipeline/dist/step0_raw/verifiers），仅忽略其下的系统文件 ──
# 注：CLAUDE.md §四.4 标注此目录「AI 禁止读」，但需随仓库分发，故纳入版本库。

# ── 一键启动脚本（Mac .command）──
# 本机需可执行权限(755)以支持 ./ 双击启动；该权限为本地环境特定、易与远端
# 100644 冲突反复刷屏，故整体忽略，本地自行 chmod +x（见 docs 提及的启动方式）。
# 2026-09-06 迁入 packaging/mac/，路径同步更新。
launch-all.command
packaging/mac/launch-all.command

# ── macOS 菜单栏/Dock 常驻工具编译产物（猫猫画布.app）──
# 由 packaging/mac/menubar/build.sh 从 main.swift 编译生成（含 .icns/.png/launcher），
# 属构建产物不入库；要分发只提交源码 packaging/mac/menubar/main.swift。
*.app

# ── 测试运行产物（AI 跑 vitest 时重定向/扫描生成的日志与清单，非源码，不入库）──
vitest_*.txt
out_*.txt
hastest.txt
# 其它测试输出重定向日志（形如 xxx_test.log / coverage 报告等）
*.test.log
# vitest 运行期重定向日志（tests/unit/ 下 .vitest_*.txt，非源码）
tests/unit/.vitest_*.txt
coverage/
# 临时调试探针测试（排查 OOM/挂起时生成的 __probe*.test.jsx，非正式用例，不入库）
__probe*.test.jsx
__probe*.test.js
# 一次性临时产物（AI 调试/重定向生成的 tmp_*.txt、msg.txt 等，非源码不入库）
tmp_*.txt
msg.txt
*.tmp

# ── 运行时日志（启动脚本落盘，含 [proxy] 转发明细）──
logs/
*.log
localTool/localtool_18080.log
localTool/localtool_18080.err.log

# ── 系统 / 编辑器 / 项目数据 ──
.DS_Store
Thumbs.db
# vitest 运行期重定向输出（vitest 结果收集文件，非源码不入库）
vitest-out.txt
vitest-out_*.txt

# ── 守护进程 PID 锁文件（launch-all.ps1 单实例保护，运行期生成）──
.watchdog.lock

# ── 回滚快照（snapshot.cjs 运行期生成，含完整 public 副本与 .glb/.wasm 二进制，约 60M，绝不入库）──
scripts/rollback/snapshot-*/

# ── 运行产物（每次运行/验收自动生成，随时间变化，不入库）──
# ts-migrate.mjs report 生成的 Excel 全景迁移作战表（随时可重建）
ts-migration-view.csv
# ts-tests.mjs status --export 生成的测试类型消化进度表（随时可重建）
tests/type_migration_report.csv
tests/TYPE_MIGRATION_TODO.md
# safety-net.cjs --save 的 dist 基线快照
scripts/dist-snapshot.json
# 探针 journal（scripts/probe.mjs 运行期生成：临时注入前的原文备份，正常用完即删；
# 残留 = 上次探针被 Ctrl+C/崩溃打断 → 下次启动 recoverStale() 自愈还原）
scripts/.probe/
# verify-ext.cjs 真机验收结果
scripts/report.json
# verify-chunks.cjs 异步 chunk 验收结果
scripts/report-chunks.json

# 注：scripts/archived/{tools,beautify,semantic-map}/ 是归档的原始脚本源码（非副本），
# 应随仓库分发供后续 AI 查阅，故不在此忽略（见 scripts/archived/README.md）。

# ── 临时产物目录（本地生成、可随时重建，不入库）──
Temp/
tmp/

# ── 代码审计：audit/ 沙盒已退役（2026-09-13 · TD-17-1）──
# 原 audit/ 把 knip/jscpd/oxlint/depcruise/madge 装在 .gitignore 的沙盒 → CI 装不到、工具不生效。
# 现把唯一主工程未覆盖的能力（死代码 knip）并进主工程：devDependency + 根 knip.json +
# scripts/check-dead-code.mjs（挂 gates.manifest push 层，CI 同跑）。其余工具不并：架构规则已在
# scripts/check-arch.mjs 自包含实现、oxlint 与 eslint 重叠、jscpd 无迫切性、ast-grep 是重写器非闸。
# 历史报告归档 docs/audit-archive/。

# ── 外部学习仓库 / 第三方工具（他人仓库与嵌套 git 仓库，仅本地参考用，不入库）──
infinite-canvas别人仓库学习专用/
# tools/ 下均为第三方仓库副本（ProPainter-Webui 等嵌套 git 仓库 + ProPainter 空仓库），
# 体积大且非本工程源码，整体忽略，避免嵌套仓库导致 add 失败。
tools/

# ── 本机推理模型资源（体积大/可重拉，不入库；仅 MANIFEST 与文档入库）──
# localTool/runtime-models/ 下的大体积二进制：模型权重(.onnx) + 推理运行时(.wasm/.mjs/.js)。
# 这些文件由 localTool/scripts/fetch-runtime-models.mjs 按 MANIFEST.json 重建，不纳入版本库。
# 规范见 docs/95-本机推理模型资源-GitHub规范.md
localTool/runtime-models/**/*.onnx
localTool/runtime-models/**/*.wasm
localTool/runtime-models/**/transformers*.js
localTool/runtime-models/**/ort*.mjs

# .codebuddy：保留 commands/(命令) 与 memory/ 入库；仅忽略 teams/ 运行态数据
.codebuddy/teams/
localTool/runtime-models/depth-video.zip
localTool/.lovart_project.json
