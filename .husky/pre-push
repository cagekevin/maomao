#!/bin/sh
# 推送门禁：全量单元测试（push 前才跑，避免每次 commit 都等全量测试）。
# commit 阶段已做快速类型检查；全量测试放 push 前兜底。
#
# 同 pre-commit：`set +e` 防坏 fd 环境下 echo 被 husky 的 `sh -e` 误判；门禁结论来自命令退出码。
set +e
npm run test:unit
tc=$?
[ "$tc" -eq 0 ]