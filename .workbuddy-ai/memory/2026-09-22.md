# 2026-09-22 · 工作日志（maomao）

## 复核「skill 债务偿还六批」（00:18–00:40 · 只读轮）· 轮次文件 `daily/架构日志/11-跨区-skill债务偿还六批复核-2026-09-22.md`

- **触发**：用户「他已经完成 6 批了，你去看看他的 6 批做得怎么样」。对象 = 另一会话的 `11-跨区-skill债务偿还第一~六批-*.md`（把我 09-21 审计登记的 22 条债逐批偿还）。
- **方法**：**不采信自我声明**，逐条回代码取证；对可机器判定的声明（lint）做一次**定点静态检查**。未跑任何测试。
- **总判定：6 批是真施工**（抽样取证 20 项，**19 项为真**）：`freezeSkillTurn(ids)` 装配收口 · 内置常量迁入 `skillBuiltins.ts`（legacy 层已无副本）· `skillStore` 降纯转发壳且**零深引** · 三态分组意图 + `resolveGroupName` · `execFileSync` 改 argv（skills/files 共 3 处）· 二进制改「往返无损」· `.trash` 上限 + `pruneTrash` · `only=SKILL.md` · 装载期引用对账 · `undecided` 三态 · 不可信条款共用一份 · `origin` 有读者 · `normalizeSkillIds` 四写路径收口 · 空值行口径 · `description` 必填 · 预算超界警告 · `trashKeep` 由生产者给全 · `onReloaded` 经 ref · 死代码基线**只删不增**（`git diff`：70→68）。
- **🔴 唯一硬伤（已登记 TD-11-45 · 高）**：**假凭证 + 挡提交**。批次一 §三 与批次六 §四 都写「`lint` 0 问题」，实测 `npm run lint` = **1 error**（`react-hooks/rules-of-hooks`，唯一一条就在新代码上）。根因 = 非 hook 却命名 `useSkillInCurrentConversation`（`skillSelection.ts:30`）且调在事件处理器里（`SkillSettings.tsx:493`）。**爆炸半径**：`.husky/pre-commit` 跑 `lint-staged`→`eslint --fix`，该 error **无 fixer** ⇒ **任何提交 skill 相关改动的人会被挡在 pre-commit**。
- **另 4 笔（低/中）**：TD-11-46 `HydrateResult.failures` 一字段两义（包未纳入 vs 已纳入但引用缺失）而三处消费者全按「未纳入」措辞上报 ⇒ **谎报**；TD-11-47 陈旧注释（`skillCatalog.ts:358` 仍写 `truncateSkillContent(_,0)=「不限」`，与同文件新口径矛盾；`AgentPanel.tsx:441-443` 同类）；TD-11-48 新闸单行正则覆盖不全（多行/变量间接漏检，而文案宣称"结构上不存在"）；TD-11-49 README 不变量 17「空值行=不存在」强于实现（布尔字段被 `toBool('')` 物化成 `false`）。
- **他的自我修正成立**：TD-11-19「两个写 API」实为**两种粒度**（单/批），现 `setSkillEnabled` 委托 `setSkillsEnabled([id],…)` ⇒ 实现只一份。措辞我接受。
- **他做得好的一点**：TD-11-43（`files.ts handleOpenDir` 未过越根守卫、`filepath` 用户可控）**自报**、如实标跨区未修、不顺手改 —— 越区边界自守。我复核为真。
- ⚠️ **债号可能被并行会话占用**：本轮 5 笔实际落 **TD-11-45 ~ TD-11-49**（`TD-11-44` 已被另一会话占：设置页面板 vs Skills 列表两处并存未收口）。**以 `debt.mjs add` 回显的号为准**，别按自己的顺序推。
- **账本收口**：主表待办 17 条（11 区 7 条：TD-11-43~49）；`debt.mjs audit` 违规 **3 项全为归档区既有**，本轮零新增。

## skill 越权与复杂度专项（00:31–00:50 · 只读轮）· 轮次文件 `daily/架构日志/11-跨区-skill越权与复杂度专项-2026-09-22.md`

- **触发**：用户连问「有没有越权？生产者-消费者有没有越权复杂度？有没有把简单事情写复杂？」并对上一轮只给 3 处候选不满 ⇒ 铺开重查。
- **先纠正一个我的误判**：`docs/plan/141`（设置页重设计「列表即磁盘」）**不是外观活**，它的根是 §8 末行「**列表骨架 = 索引 → 磁盘**」+ §10.1「行数 == 磁盘包数 + 内置数（不是索引数）」= `ADR-0056 Q2`「副本不得参与存在性判定」在界面层的同款 ⇒ 属越权类，**不能跳过**。
- **🔴 母体（3 条同根）**：**存储收口那一轮只把「键」搬进模块，没搬「键上的语义与域内实现」**。legacy 壳 `runtime/skillStore.ts` 至今持有三样本该属于模块的东西 —— ① 「默认启用」判据（`:195`）② 「全部可用 skill」的组合（`:160` `getAllSkills`）③ 文件白名单 + 40 行乱码修复（`:71/89`）；门面又不导出它们 ⇒ 消费者只能从壳取，**模块内的新代码（141 视图层）被迫抄第二份**（`skillLibraryView.ts:196` `enabledOf`，注释还自认"与 isSkillEnabled 同口径"）。⇒ 越权的实况不是"消费者抢活"，而是**生产者的活留在消费者侧**。
- **复杂度**：141 方向对（拆掉第二真相），但把复杂度集中到 `SkillRow`（21 字段 × 5 态 × 6 计数），且新代码里冒出 3 处"同一判据两副本"：`rowFromIndex` **先造错的行再覆盖三字段**（漏改即回归成"仅索引行可用可编辑"= 它要修的 bug 类）· `SkillRow.usable` 与 `isUsableState()` 各写一遍 `=== 'ok'`（而函数自称"三处共用唯一判据"）· 组名→显示名映射散成三处（`skillGroupLabel` 自称唯一措辞源 + view 两个伪组特例 + 消费者 `prefillCategory`）。另有 `'SKILL.md'` 代码字面量 **15 处/8 文件**（同一 `find(relPath==='SKILL.md')` 惯用法抄 7 遍）。
- **实测为真的边界（非债）**：媒体域**确实没越界** —— `orphanGc` 只扫 `getUploadDir()`、静态路由有越根守卫（`resolvedPath.startsWith(resolve(uploadDir))`）、`backupDb` 只备 `localtoon.db` ⇒ 方案 §1.1.1 的三条硬事实全部成立。这是本轮唯一"声称 + 实测都对上"的一组。
- **账本**：新登 **TD-11-50/51/52/53/56/57/58/59/60**（9 笔待还）；**主动判掉 1 条重复**（我的 TD-11-54 与另一会话的 TD-11-55 是同一事实 ⇒ resolve 指向 TD-11-55，不重复计账）。11 区主表待办 **17** 条。
- ⚠️ **`SkillSettings.tsx` 在改写中**：取证期间两次读到不同内容（141 施工）⇒ 该文件结论标"回查"，必须在施工完成后复核。

## skill「补丁思维 vs 架构师思维」判定审计（00:46–01:00 · 只读轮）· 轮次文件 `daily/架构日志/11-跨区-skill补丁思维判定-2026-09-22.md`

- **触发**：用户「审计他，看哪些是**补丁思维**而不是架构师思维 —— 从**正确性、复杂度、SSOT** 去看」。
- **我立了六条试金石**（从证据反推，可复用）：① 第二真相还在吗 ② 是拆成两个还是给一个加语义 ③ 约束落在结构还是文案/注释 ④ 结构上不可能漏 vs 靠调用点记得改 ⑤ 修的是**根因所在的结构**还是**症状所在的结构** ⑥ 判据面 ≥ 问题面吗。
- **度量**：他 24 笔「已解决」里 **16 笔架构师思维**（收口唯一入口/唯一判据、拆态、让结构上不可能违反）· **7 笔带补丁性** · **2 笔半**。**没有一笔是假修复**（都有真实改动 + 探针）。
- **🔴 最硬的补丁证据（产出归零）**：`TD-11-40`（给面板加 `onReloaded`）—— 组件 `SkillLibraryPanel.tsx` **已被删除**，`onReloaded` 全仓 0 命中；`TD-11-41`（`reconcileSkillGroups` 骨架唯一）—— 函数**已被整个删除**，同一原则在 `buildSkillLibraryView` 重做。⇒ 两笔"已解决"的**净产出为零**，因为修在"即将被自己下一个方案替换的结构"上。
- **最贵的补丁（母体）**：`TD-11-19`「拔掉域外深引」但**不归位职责** ⇒ 三样东西仍住 legacy 壳（默认启用判据 / 全部 skill 的组合 / 文件白名单+乱码修复），门面不导出 ⇒ 141 的新代码被迫抄第二份（`enabledOf`）。**不是作者退步，是上一轮收口没做完**。
- **⚠️ 结论重心（00:55 复核后修正，很重要）**：**他的修复能力是架构师级的，短板在"选落点"**。写轮次文件期间他把我判的 4 笔全部以**结构方式**修掉：`failures` **拆成两个字段**（不是借壳）· `rowFromIndex` **增 state 形参**（不是先写死再覆盖）· `isUsableState` **收成文件内唯一判据并去掉导出** · `selectSkillInCurrentConversation` 改名 + **承认"lint 0 问题"是凭证失实、口径已改**（没辩解）。⇒ 对这个人，**"动手前先问落点"比"教怎么修"值钱**。
- **仍未结（补丁类剩余）**：**TD-11-50/51/52**（P2 母体，且是 141 抄第二份的原因）· **TD-11-48**（新闸单行正则）· **TD-11-62**（消费者直接比哨兵字符串 `__index-only`/`__official` 做渲染判定 ⇒ 改哨兵值即静默失效）。
- **账本**：11 区主表待办 **12** 条（TD-11-43/48/50/51/52/53/55/58/59/60/61/62；其中 55/61 为另一会话自登）；`audit` 违规 3 项全为归档区既有。

## 把「选落点」写进 `架构师改码7步法`（01:00–01:05 · 用户指令）

- **触发**：用户「在'架构师写代码七步法'里，要写清楚的那个步骤那里，加一个'选落点'」—— 把上一轮判定审计（补丁思维）里提炼的**落点判据**固化成流程。
- **落点选择**：`架构师改码7步法.md` 的 **Step 3 · 定位 —— 复杂度该落到哪个物理位置？** 就是"落点"那一步。改动 4 处（**+25 行**，1046 → 1071）：
  1. **Step 0 · 全库检索** 加一条：**查在飞方案**（`ls docs/plan/` 最新一期 + 它有没有点名我要改/新建的结构）—— 否则时效判据无从执行。
  2. **Step 3 动作** 加第 7 条：**落点时效（会不会白修）**。
  3. **Step 3 新增「选落点」判据块**：三问（**① 时效** 会不会被手上正在写的方案替换 / **② 层次** 根因住哪层，症状在 UI ⇒ 根因多在契约·类型层 / **③ 收口** 搬的是「键」还是「键+语义」）+ 每条带**本仓实证**（面板被删致产出归零 · description 必填被自己的下一个设计推翻 · 只搬键不搬语义致新代码抄第二份）+ **不过关怎么办** + **凭证（强制一行）「在飞方案检查」**。
  4. **两张速查附录各补**：退回速查 2 行（在飞方案点名该结构 → 退回重选落点；判据留症状层 / 只搬键不搬语义 → 退回或视为未完成）· 决策树 Step 3 下补"🔥 先选落点"分支。
- **一句话内核（写进文档的原话）**：**落点 = 根因所在的结构，不是症状所在的结构；且先判时效 —— 落在"即将被替换的结构"上，产出归零。**
- **为什么落这两处**：七步法自带"退回与熔断速查 + 速查决策树"两张附录，是同一规则的**索引面**；只改正文不补附录 = 新判据悬空（该文档自身的"注释/索引与实现同步"纪律）。

## 把七步法做成 skill（01:00–01:12 · 用户指令「直接新建一个 skill」+ 两次纠正）

- **落点**：**项目级 skill** = `.workbuddy-ai/skills/architect-7-steps/`（系统提示词定义的项目级路径；`~/.workbuddy-ai/skills/` 下已有 `_bm_skillid_migration.json` 佐证该目录就是本产品的 skill 根）。**命令文件与 ADR 指向未动**（用户裁定：「skill 是 skill，command 是 command，你不要管」）。
- **两次被纠正（值得记的教训）**：
  1. **我跑偏成"逐字保真"**：用脚本机械切分 1071 行、还写保真核验脚本去比字节。用户：「**为什么要逐字保留？咱的目标是把它变成一个 skill**…你肯定不能保证给它一个字不变，那我要你那个 skill 有啥用？」⇒ **skill 的目标是「更结构化、AI 更好读」，不是搬运**。
  2. **我设计了"12 个分步文件 + 每步按需加载"，但那是假按需**：用户反问「**一个 AI 去写代码的时候，他会反反复复回来读这个 skill 吗？**」⇒ **不会**。AI 一旦进入改码循环就不再回来重读 ⇒ **SKILL.md 必须自足**（全部可执行内容一次载入）；references 只放"在**某个可识别的时刻**才会主动去读"的深水区。
- **最终形态**（4 份参考，非 12）：`SKILL.md`（313 行 / 34KB，自足全流程：三条不可违反 + 六闸表 + Step 0–7 动作与判据 + 用户报症状浓缩 + 退回速查 + 按需加载指引）· `references/00-principles.md`（五底线全文 / 七铁律 / 生产者↔消费者三铁律完整清单+实证表 / 拆兜底六形态）· `01-gates.md`（手段优先级 / 六种不要建的闸 / 建闸 6 问 / 删闸规则 / 闸的判据与成本守恒 / A10 工具债 / 五闸凭证要求）· `02-diagnosis.md`（报症状完整版：9 条"做完了"对照表 + 五种滑法 + 弯路留痕）· `99-appendix.md`（附 A 15 条踩坑 + 手法排行 + TS/React 异常容错禁止项 6 类）。**合计 616 行 / 68KB**（原 1071 行；首次载入从 ~60KB 降到 34KB，其余按需）。
- **skill-creator 的硬门**：`quick_validate.py` 要求 `name` **hyphen-case** ⇒ 用 `architect-7-steps`（本仓既有 `电影感图片` 用中文名也能跑，但取安全侧）；frontmatter 必须带 `agent_created: true`（否则 `skill_manage` 改不了它）。校验：**Skill is valid!**
- **一次性脚本已删**：`tmp/split-7step.mjs` + `tmp/split-report.txt`（机械切分那轮的产物）—— 用户「那肯定要删呀」⇒ 删前按四口径证死（全仓 0 引用）后 `rm`，`tmp/` 只剩别人的 `eslintprobe/`。

## skill「补丁思维整改」批复核（01:19–01:45 · 只读轮 · **全量读**）· 轮次文件 `daily/架构日志/11-跨区-skill整改批复核-2026-09-22.md`

- **触发**：用户「他现在又改得差不多了，继续用刚那些审计方法审」+ 上一轮被批「才看几秒就下结论」⇒ 本轮**先读完全部再说话**（模块 14 文件全读、含 `skillLibraryView` 507 行；2 道新闸全读；设置页数据/动作/渲染判据段；组件测试；README；账本）。
- **总判定：这一批是高质量整改** —— 他声明的 8 条结清（TD-11-48/50/51/52/53/58/61/62）**全部为真**，且我上轮判的 3 条未结（P2/P6/P7）**全部按结构方式**结掉（判据跟键走 `isSkillEnabledIn` / 整类禁 `exec` / 语义提成 `kind` + 哨兵不导出 + 闸）；`lint` 这次**真是 0**（上轮是假凭证）；自曝并修掉一处**真功能空洞**（面板拖 `.md` 导入是"谎报"——TD-11-16 收口留下的回归，我上轮没查到）。
- **🔴 本轮唯一"形态级"问题：判据没横向铺开（三处同形复发）**
  1. **TD-11-64**（中）新闸「入口文件名字面量只许住定义文件」**大小写敏感**（`/['"\`]SKILL\.md['"\`]/`），而 `skillImport.ts:121/144` 为大小写不敏感比较**必须写 `'skill.md'`** ⇒ **2 处真实例当场漏检**、闸永远绿。正是他刚在 shell 闸上批过的"枚举必漏"。
  2. **TD-11-65**（中）`skillCloudSync.ts:60` 云写回只比入口指纹却**拉全量 `references/**`** —— `detectSkillDrift` 已改用 `readDiskSkillPackages()`（TD-11-39），**同一判据只改了一处**。
  3. **TD-11-68**（低）`skillLibraryView.ts` **507 行 5+ 职责**混居，与被登记的 TD-11-60（skillCatalog 325 行）**同形且更大**，却未登记。
- **另 5 笔**：TD-11-66（`listAllSkills` 吞缓存损坏 ⇒ 面板静默"只剩内置"，M5 失败不可见）· TD-11-67（组开关可开行判据两份：view 的 `toggleable` vs 消费者 `filter(state==='ok')`）· TD-11-69（`skillGroupLabel` 注释仍自称"唯一措辞源"，实际已迁 `labelOfGroup`）· TD-11-70（导出只带 5 字段 ⇒ 丢 allowed-tools/布尔/未知字段，与导入"原样带过"不对称）· TD-11-71（picker 用 `enabledMap:{}` + `filter(r.enabled)` 表达"不判启用"⇒ 依赖默认启用恒真，改默认即静默空下拉）。
- **⚠️ 我自己的两次错判（已自我推翻，写进轮次文件）**：① 据**过期读数**判 `buildSkillPickerGroups(disk)` 的 disk 参数"生产零传值 = 幽灵" ⇒ 重读 `AgentPanel:585` 发现**真传了**（TD-11-55 其实已实现）② 据 01:19 的账本判 TD-11-59"账实不符" ⇒ 01:35 复核发现他 ~01:30 刚自行 resolve。
  - **我的 resolve 一度覆盖了他带 3 个探针 sha 的原注** ⇒ 已用 `edit --field solution` **还原 + 追加复核结论**（教训：**动别人的账本行前先 `show` 看它有没有凭证**）。
- **账目**：新登 TD-11-64~71（8 笔）· 纠正 TD-11-55（旧状态确为待还）· 归档 2 条 · 11 区待办 **11** 条 · `audit` 违规 3 项全为归档区既有。
- **给他的改进建议（写进轮次文件 §七）**：他修**一个点**很扎实，短板是**没问"同一条判据还有哪些地方该一起改"** —— 与上轮"选落点"同根：**动手前先问"这条判据的适用面有多大"**。


## Skill 设置页 Mockup 按新版代码重做（06:18 · 用户选方案 A）

- **触发**：用户「根据他最新的代码重新去设计 mockup…我选方案 A。现在他已经去掉了两种情况并列的内容」。
- **代码已重写（TD-11-44）**：`SkillSettings.tsx` 从 1038 → **1288 行**（mtime 09-22 01:41）。旧版「上方磁盘技能库面板 ＋ 左栏 Skills 列表」的**两个并列列表已删除**，收敛为**一个列表 · 一个编辑器 · 一条工具条**；列表骨架取磁盘包（真相源），索引只补状态。新增 `skillLibraryView.ts`（507 行，纯函数判定中枢）· `skillPickerView.ts` · `skillGroup.ts`（哨兵与措辞唯一出生地）。
- **新版结构（照此出图）**：头部（标题 + 芯片 磁盘 N 包 / 启用 x/y）→ 工具条（分组下拉 · 导入⌄ · 新建 · 分组 · 右：重读/打开文件夹/注入预算）→ 注入预算折叠条（摘要：清单关 · 单篇 12,000 · 合计 24,000 · 一次最多 4）→ 主体 `h-[600px]` 左列表（46%）+ 右详情/编辑 → 底部官方说明。
- **新版关键新增（mockup 必须体现）**：`TriToggle` 三态组开关 · `RowBadges` 五种行状态（ok / missing-id 缺 id / not-indexed 未纳入索引 / unreadable 读不到 SKILL.md / index-only 磁盘上已删除）+ 附加标记（无描述 / 引用缺失 N）+ 行上快捷动作（补齐 id / 重读 / 恢复 / 丢弃）· 编辑态**冲突条**（磁盘版被外部改过 ⇒ 一个字都没写）· 三条页面级横幅（未决 / 索引损坏 / notice）。
- **产出**：`mockup/skill-settings-mockup.html`（**已整文件重写**，旧版并列对照全部删除）。四段：主视图详情态 · 编辑态（含冲突条 + 超限预算条）· 行状态速查表 · 异常态横幅；末附 6 条设计建议。
- **我提的 6 条建议**：① **搜索框被删了**（新版只有分组下拉）⇒ 加回工具条最左 ② 「分组」与「新建」同为 `Plus` 图标 ⇒ 改 `FolderPlus` ③ **组头计数口径不符肉眼**：`toggleable` 只数 `state==='ok'` ⇒ 组里可见 4 行组头写 `2/2` ④ `BudgetBar` 只有文字没有条 ⇒ 补 4px 进度条（蓝/琥珀/红三档）⑤ 自定义行图标位是孤立 `·` ⇒ 统一 `Bot` ⑥ 详情 5 按钮平铺主次不分 + 左栏 46% 偏宽 ⇒ 收到 340px、删除右移。
- **口径**：卡片宽仍按 `max-w-4xl` = **896px**；只 link `maomao-kit.css` + 自写 `<style>`，**不依赖 Tailwind CDN**（实测全仓 mockup 无一引用 CDN）。


## skill「六类拆分 + 八笔结构收口」复核（06:5x · 用户「又要快做完了，重新全面审计」）

- **对象**：他对我 `整改批复核` 的处置（`复核八笔结构收口` + `六类混居拆分`）· 模块 24 文件（3414 行）在 01:48–01:55 的改动。
- **总判定**：**这一批真做完了且做对了，12 项声明全为真，无功能错**。旧 `skillCatalog.ts` 真删 · 6 个新文件忠实搬家 · `skillGroup.ts` 真成两个用例共用的家 · 闸 owner 真跟着定义走（`check-arch.mjs:2308`）· TD-11-64 两头都改（闸 `/i` + `skillImport` 派生 `SKILL_ENTRY_FILE.toLowerCase()`）· 65/67/70 全真 · **TD-11-47 另一半（`skillInjectText:106-109`）已随 TD-11-26 回改 ⇒ 非债**。
- **重跑静态闸（凭证）**：`type-check` 0 错 · `lint` 0 问题 · `check:arch` ✅（跨栈对账 3 项 + 三道 skill 闸）。**未跑测试**（用户明令）。
- **🔴 新登 5 笔，全是「声明面没收口」，全低息、全孤例**：
  - **TD-11-72** 门面 `index.ts:5` 写「当前承载**五类**用例」却列了 ①–⑦（7 类），且称「其余用例在实现那一期加入」而读资料/云写回的导出就在 `:41-42`。
  - **TD-11-73** 用例编号**三处互不一致**：plan140 §2.2（①=注入）· README §2（skillInject=① **且** skillLibraryView=①（新），**自相冲突**）· 门面 index.ts（①=渲染库、③=注入）。
  - **TD-11-74** `skillGroup.ts` 头自称「排序」唯一家，但「官方恒排最前」/`skillLibraryView:402` 与「仅索引恒排最后」`:411` 两条规则住在视图文件，`compareSkillGroups` 只认 `_未分类`。
  - **TD-11-75** 视图入参**允许编值且缺省=危险值**：`enabledMap` 必填而选用入口无值可给（`skillPickerView:62` 传 `{}`）· `rowFromIndex` 的 `state` 默认 `'ok'`（`:203`）⇒ 漏传即静默 `usable+editable`，与本函数 `:195-197` 自注（TD-11-56「先造错再覆盖」）**直接矛盾**。TD-11-71 只删了消费端过滤，没动根因所在的签名。
  - **TD-11-76** `docs/plan/140 §3.2`（:287-302）仍把 5 个函数挂在已删的 `skillCatalog.ts` 名下且**无对账指针**（同文件 §2.1 补了、§3.2 没补）。
- **⭐ 五笔的共同点 = 他刚写进 SOP 的第 8 条「判据适用面」的命中项，只是方向反过来**：他修的是**代码实例**，没收的是**描述同一条判据的所有地方**。⇒ **第 8 条目前只在代码面生效，没覆盖声明面**（头注释 / 用例编号 / 模块 README / 方案规格块 / 类型签名）。
- **我自己也有同一条病，已当场修**：SOP `架构师改码7步法.md` 01:35 加了 Step 3 第 8 条，而我 01:08 建的 skill `architect-7-steps/SKILL.md` **只有 1–7 条** ⇒ 已同步补上（含"声明面五处清单"）。不进账本（当场修完），但在轮次文件 §七 留痕。
- **账本**：新登 TD-11-72~76（号与我预测一致，未被并行会话占用）· `audit` 复跑**无新增违规**（仅 3 条 01/02/04 区历史断链，非本轮）· 11 区已解决 70 条、**只剩 TD-11-63**（面板 `.md` 导入路径不可达，等用户拍板）。
- **产出**：`daily/架构日志/11-跨区-skill六类拆分与结构收口复核-2026-09-22.md`（已 `arch-index --write`，11 区共 19 轮）。
- **⚠️ 又踩一次老坑**：shell `grep` 对 `check-arch.mjs` **静默返回空**（明明有 12 处命中）⇒ 必须用 Grep 工具。**shell grep 空结果 = 假证据**（本仓第 3 次实证）。

### 深挖第二轮（补读 skillInject / skillRegistry / skillResource / skillResourcePath / skillDirName）

- **新增第 6 笔 TD-11-77（本轮唯一影响使用体验的）**：`skillResourcePath.ts:71` 的 `extractResourcePaths` **要求路径含 `/`**，而 `assertSafeSkillRelativePath`（:41-55）**允许** `style.md` 这类裸文件名 ⇒ 写在包根的 `style.md` **正文里明明引用了却提取不到**；模型来读时 `skillResource.ts:85` 回「「X」正文里没有引用这个文件」⇒ **报错给了一个不成立的原因**（M5 近亲：失败信息说谎），模型据此无法自纠。且 `README §3` 不变量 4（"只有正文里显式写出的路径才可读"）**比实现宽**。修法二选一：去掉"必须含 `/`" / 保���它但改报错口径 + 补 README。
- **另两条判过但判非债（留痕）**：
  - `skillRegistry.ts:32` 还留着 `const str = (…)`，与 `skillLibraryView.ts:189` 导出的 `asText` **逐字同形** —— 他那轮"批量改名 `str(`→`asText(` 只改调用点没改定义行"的弯路**没收干净**。判**非债**：消它要 `skillRegistry` import `skillLibraryView` ⇒ **组合层依赖视图层**，比留着这 1 行更糟。
  - `skillResource.ts:71` 歧义报错在 `ref` 为空时输出「本轮有多个 Skill 匹配「」」（空引号）。判**非债**：修辞退化但信息完整（给了候选列表 + 怎么改），模型能自纠 ⇒ 与 77「原因说谎」不同族。
- **复核为真**：`skillInject.freezeSkillTurn` 只吃 id（TD-11-16 根因已断）、去掉了冗余预读（TD-11-66）· `skillRegistry.listAllSkills` 真返 `{ok,list,error}` · `skillResource` 两道判据齐全（本轮绑定 + 正文引用 + `assertSafeSkillRelativePath`）· `TEXT_RESOURCE_EXT` 一份两用（读侧/导入侧，TD-11-34）· `slugifySkillName` 与 `isLegalDirSegment` 职责分离、前导点与尾空格都处理了。
- **账本**：新登 **6 笔**（72–77，全孤例全低息）· `audit` 无新增违规 · 11 区待还 **7 条**（63 + 72~77）。

## Skill 设置页「设计师视角」重做（06:51 · 用户批「你这是在打补丁」）

- **批评（要记住）**：前两版都是「现状复刻 + 病灶标注 + 小修小补」⇒ 用户要的是**从交互模型重做**，不是在原结构上贴建议。教训：**用户说"重新设计"时，先问"这个对象的本质是什么、用户在这里真正做哪两件事"，再动手；不要在现有控件上提改进项。**
- **产出**：`mockup/skill-settings-redesign.html`（新建，与上一版 `skill-settings-mockup.html` 并存）。
- **重新定位**：这个对象不是「设置卡片」，是**一柜子会被原样塞进 AI 上下文的指令文档**；用户只做两件事 = **挑哪些参与本轮对话** + **写/改那段指令** ⇒ 设计围绕这两件事重排。
- **8 条设计决策**：① 主-从双栏 → **导航条(176px) + 内容区(720px)**，编辑时内容区整体换成编辑器 ② **预算常驻仪表**（启用个数 5/4 分段条 + 字数 18,240/24,000 按技能分段），把硬约束可见化 ③ **删掉详情态**（与编辑表单是同一组字段的第二份渲染）④ 异常从行内徽标收进 **「待处置」页**（正常行只留琥珀点）⑤ 分组升级为**导航条**（分组=磁盘文件夹）⑥ 去掉 `h-[600px]` 死锁 ⑦ 顶栏只留一个主按钮，维护类降为导航条底部文字链 ⑧ 建议挪出「AI 助手」子分区独立成页（待拍板）。
- **口径**：卡片仍 896px（`max-w-4xl`）；只 link `maomao-kit.css` + 自写 `<style>`，不依赖 CDN。

## Skill 设置页布局重排 · 落地（07:05–07:30 · 改码轮）· 轮次文件 `daily/架构日志/11-跨区-skill设置页布局重排-2026-09-22.md`

- **只改 1 个文件**：`src/components/settings/sections/SkillSettings.tsx`（1288 → 1584 行）。判据层 `src/components/agent/skill/**` · `tests/**` · `SettingsFrame` **一行未动**。
- **落地的 7 条**：① 左列表 46% → **导航条 172px + 内容区**（列表⇄编辑器整体切换，编辑器 484→724px）② **删掉详情态**：改成同一份编辑器的两态（只读 = 字段 `disabled`，点「编辑」放开）⇒ 名称/描述/分组/正文 渲染处数 **2→1** ③ 工具条 7 按钮 → 4（重读/打开文件夹下沉导航条底部）④ `h-[600px]` → `min-h-[420px]` ⑤ 预算常驻一行（已启用 N · 一次最多 M，超限标琥珀）⑥ `BudgetBar` 补 4px 进度条 ⑦ 新增**待处置**聚合视图（problemOf 映射，同源判据）。
- **⚠️ 两条被现实纠正（强可复用）**：
  1. **我提的「加回搜索框」是错的** —— `tests/unit/skillSettings.test.tsx` 第 3 例标题就写着「**没有搜索框**（用户裁定：技能就几个，搜索是噪声）」，并用 `queryByPlaceholderText(/搜索/)` 锁死。**先查它为什么不在，再提"补回来"**。
  2. **「删详情态」与测试撞墙** —— 若"点行直接进编辑器"，「编辑」按钮消失 ⇒ 测试第 3 例（选中→点编辑→表单）必红。**解法不是改测试，而是把只读态做成编辑器的 `disabled` 态**（一份渲染两种可编辑性，「编辑」按钮自然还在）。⇒ 这反而比原设计更省。
  3. **「异常收进待处置」不能是替换，只能是双层** —— 行徽标+快捷动作是「状态在行上」铁律的落地，测试第 1 例锁死。改为行上保留 + 待处置给完整解释，两者同源（都读 `row.state`）。
- **未做**：字数分段仪表 —— 需要"哪些会被注入/合计怎么算"的判据，那是 `skillBudget`（生产者）的契约；**UI 自算 = 消费者越权 = 第二份判据**。不登记为债（现有代码无缺陷）。
- **凭证**：`npx vitest run` 9 套件 **110 passed**（含 skillSettings 7 例）· `tsc --noEmit` **exit 0 / 0 error** · `eslint` **exit 0** · `check:arch` ✅（含新闸「组哨兵只许住 skillGroup.ts」→ 0 手抄）。**未跑探针先红后绿**（纯 UI 重排无行为修复，按 §7.2 走现有测试 + 反向探针）。
- **行数 +296 不粉饰**：新增了两个能力（导航条 ~90 / 待处置 ~110），是在飞设计里被批准的新增。
- **待用户拍板**：整页挪出「AI 助手」子分区独立成设置项（或放宽到 `max-w-6xl`）—— 动 `SettingsFrame`，未擅改。
- ⚠️ **环境**：`mockup/*.html` 会被外部工具注入 `data-page-node-id` 属性（并曾吞掉一个块）⇒ 用 python 改 mockup 后要回读确认结构还在。

## ⚠️ 用户提问「为什么一次最多只能启用几个」→ 查出我自己引入的假宣称（07:41–07:46）

- **概念区分（强可复用，别再搞混）**：
  - **设置页的开关（启用 / `agent_skill_enabled`）** = 这个技能进不进 AI 助手的**可选列表**（候选池）。取证：`AgentPanel.tsx:403` `skillsRead.list.filter((s) => isSkillEnabled(s.id))` 先过滤，再交给 `skillPickerView`（**显式不读 `enabled`**，只按 `usable`，见 TD-11-71）。⇒ **开多少个都合法**。
  - **`maxExplicitBindings: 4`** = **一次对话里最多同时挂几个**。取证：`freezeSkillTurn(activeSkills)` → `skillInjectText.ts:94` `list.slice(0, maxItems)`；超出**不注入**，但尾部注明「另有 N 项因「单次最多 4 项」未注入」（不静默）。
  - 三层预算：① `maxExplicitBindings` 限条数 ② `singleSkillChars` 限单篇 ③ `expansionTotalChars` 限合计（逐项递减，后面的先被压，`room<=0` ⇒ 跳过并记 `starvedNames`）。
- **我犯的错**：本轮把 `view.counts.enabled`（候选池）去比 `maxExplicitBindings`（单次装载量），超限时标琥珀「超出上限 N 个」⇒ **拿两件不可比的事造了一个告警**（假宣称）。已回改 3 处：删 `overBindings`、常驻行改「可选 N 个 · 一次对话最多带 M 个」、标签改「一次对话最多带几个」+ 补说明。
- ⚠️ **教训**：**把「某个数字 > 某个上限」画成告警之前，先验证两个数是同一个量纲**。本仓同名近义的量不止这一对。顶栏芯片「启用 x/y」**不能改措辞**（测试第 2 例按 `启用 2/2` 精确匹配锁定）。
- 落账：轮次文件 §八 后记（含取证链 + 凭证）。回改后重跑：7 passed · tsc 0 · eslint 0。
