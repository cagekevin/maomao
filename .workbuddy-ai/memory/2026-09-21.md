# 2026-09-21

## 按 ADR 审计 2026-09-20 性能优化（只读轮 · 轮次文件 `daily/架构日志/25-跨区-性能优化ADR合规审计-2026-09-21.md`）

- **对象**：① 1B `EditorShell` 改 `React.lazy`（d233a506，首屏 gzip −19.5%）；② 04 区 React 重渲偿债 6 轮（已结清 14 笔、改判 2 笔、04 区待还 8 笔）。
- **判定**：13 条 ADR 逐条过 —— 合规 7（0041/0047/0030/0045/0046/0033/0016，0040 落点算合规）· 不符 1（**0019 净减口径**：验收停在"探针先红后绿"＝症状消失，仅 TD-04-56 给了 −135 行；Step 0 普查的 87/1212/146/213 口径未固化，今复测裸 `memo(` 37→52 已不可比）· 待拍板 2（0049 边界用例 3 处 · 0048 `Suspense fallback={null}` 白屏窗口）· 观察 2（TD-04-38/44/47 区归属错位应属 13/18；`useStoreSelector.ts:69` 的 `null as T`）。
- **独立探针（不复述昨天声索）**：① 去掉 `memo(LeftPanel)` ⇒ `panelMemoBail` 红（1→2）② 注释掉 `selectorRef` 渲染期赋值 ⇒ `useStoreSelector` 红（陈旧值）⇒ 两条都是**真会红**的闸，非自证式。均已还原。定点 10 文件 / 89 测试全绿。
- **ADR-0049 适用性的裁定**：该 ADR 21:50 裁定、9-21 落盘，晚于 20:00–21:00 写的用例 ⇒ 判**待清点**不判违规。建议删 `NodeTitle.test.tsx:54-61`（图标尺寸/类名＝UI 样式细节，类型层已拦）、`useImageHoverActions` 存在性断言改行为断言；`panelMemoBail`（数渲染次数＝行为）与 `useStoreSelector`（正确性）**保留**。
### 登记结果（用户「那你去登记把」· 走债务登记5步法）

- **TD-25-42**（25 区 · 增债/中/结构债）：性能轮验收停在「探针先红后绿」＝症状消失，M1 普查口径（87 memo/1212 内联箭头/146 数组/213 对象）**未固化**，今复测裸 `memo(` 37→52 口径已不可比 ⇒ 违 ADR-0019 净减口径。解法一句话：口径固化成脚本，施工前后各跑一次。
- **TD-04-58**（04 区 · 增债/中/**业务债·待拍板**）：`src/App.tsx:1697` `Suspense fallback={null}` ⇒ 打开剪辑器在 489.79 kB chunk 下载期渲染空白；提交 `d233a506` 称「无行为差异」不成立。选项 A 补加载反馈 / B 维持现状用户显式接受（须回改提交说明）/ C 空闲预取 chunk。
- 两笔均走 `debt.mjs add`（锚点 = 本轮次文件）；`index.md` 随之刷新：25 区 `待翻新` → `已验证(有债待还) 🔴`。
- **未处置（留痕）**：`debt.mjs audit` 另有 4 项**历史**违规（锚点断链 TD-01-27 / TD-02-67 / TD-04-55 + 归类 TD-22-68），与本轮新债无关，已在轮次文件 §七 旁注记给下一轮。

### 方法沉淀

1. **审计性能优化时的判据顺序**：先问「它删/改了什么」（ADR-0047）→ 再问「新原语有没有真消费」（ADR-0030 · refs）→ 再问「是不是第二份入口」（ADR-0045）→ 最后问「验收口径是不是净减」（ADR-0019）。前三条昨天都过了，**第四条是本轮唯一实质不符**。
2. **别复述前一轮的"探针先红后绿"** —— 审计要自己注入一次负例；本轮两条探针各 20 秒，就把"声索"变成了"证据"。
3. **判据晚于行为的用例不能判违规** —— 先核 ADR 日期与被审代码日期，晚 ⇒ 判"待清点"并交用户，而不是追责。

## ADR 合并判定（只读 · 未执行 status/add）· 报告 `daily/架构日志/跨区-ADR合并判定-同族两面审计-2026-09-21.md`

- **结论**：现行 40 条里**需合并 2 组**：① `0041+0046`（判据的防线形态，两条从"闸/规则"与"AI 会不会猜"两个入口问同一张落点表，且 0041 的"写明不能+人工判据"出口 = 0046 判违规的"停在文档层"，**真对撞**，现靠互相打补丁压住）→ 合并为**六级落点表**（删>类型层>唯一入口>目录>检查+负例探针>诚实声明）；② `0030+0047`（"什么情况可以删"被切成两条，且 0030 头部已把 0047 两关抄回 = 反向补丁）→ 一条线（不许造→判→删前三关）。
- **另 1 组收编不是合并**：`0032` 的"注入主世界物理不可收口"是通用判据，应上提进 `0031` 边界表作第三类出口，0032 转已取代；**且 0031→0032 引用为 0 而反向 5 处 = 单向补丁，须补回指**。
- **禁合并**：`0029×0031`（互相声明禁合并，已双向钉边界）· `0002/0005/0006/0033/0034`（唯一出口分属不同能力）· `0016×0041`（母体/实例化）。`0050×0051` 邻族不合并，但 `0050:39` 把 A/B/C 当默认形态与 `0051` 冲突 ⇒ 补一行指针即可。
- **⚠️ 方法沉淀（重要）**：**六节模板仓里，正文级 Dice 一律失效**（实测 p50 0.562 > 同义参考值 0.55，模板词抬高底噪）⇒ 只比结论。**且高 Dice ≠ 该合并**：本仓结论级最高对（0.443）恰是明令禁合并的 0029×0031 ⇒ 相似度只能用于**排除**（max 0.443 < 0.55 ⇒ 确无同义对，证明 `hygiene` 报"近义簇 0"不是漏检），**不能用于定罪**；定罪只能靠切分维度。已回写进 `adr-dedup-consolidate` skill。
- **顺带发现（比合并更能降现行集）**：`hygiene` 报未毕业 40/40，其中 `0002/0005/0006/0033/0034` 与已毕业的 `0035/0036` 同形（唯一出口，已由结构/类型守住）⇒ 应先核载体走 `--to 已毕业`，零削能力风险，现行 40→35。
- **未做的事**：未执行任何 `status` / `add` / `index --write`；未回改引用面。全部待用户确认后另开一轮。

## ADR 合并执行完毕（同日 11:44 批准后落盘）· 记录在同上的轮次文件「执行结果」节

- **新建**：`ADR-0052` 判据的防线形态（六级落点：删>类型层>唯一入口>目录>检查+负例探针>诚实声明）= 合并 0041+0046；`ADR-0053` 幽灵预留与删除的判据（不许造→判→删前三关）= 合并 0030+0047。
- **退役**：0041/0046/0030/0047 → 已取代；0032 → 已取代 by 0031（「物理不可收口」上提为 0031 §决议7 第 1 步，并补上 0031→0032 的单向回指）。0050 §② 补指 0051。
- **回改**：29 文件 · 53 处（①② 全改，daily/ 未动）。**audit 0 问题 · refs --check 0 断链 · doctor 四项全绿 · 现行 40 → 37**。
- ⚠️ **批量改 ADR 编号的两个坑（强可复用）**：① 必须排除 `**取代**` / `**被取代于**` 头部状态行 —— 它是 `status --by` 自动写的，批量替换会变成**自指**；② 必须跳过 README 的自动索引块，其标记是 `<!-- ADR-INDEX:BEGIN -->`（**不是 START**），块内自写「禁手改」，改了也会被 `index --write` 覆盖。
- ⚠️ 另一条：`audit` 的 `bodyLines = 整个文件行数`（不是正文段落），上限 80 ⇒ 写新 ADR 时**含头部状态行一起算**，很容易超；超了就把取证过程挪去区域日志。
- 已回写 `adr-dedup-consolidate` skill（正文级 Dice 在六节模板仓失效 / 高 Dice ≠ 该合并）。
- **下一轮可做（未做）**：`hygiene` 报未毕业 37/37。

## 毕业复核（12:09）—— 推翻我自己上一轮的建议：0002/0005/0006/0033/0034 **一条都不该毕业**

- **⚠️ 关键教训（强可复用）**：我上一轮据「与已毕业的 0035/0036 **同形**（都是唯一出口类）」建议批量判毕业 —— **错了**。同形 ≠ 同载体。**判毕业前必须实测载体，不能看标题归类。**
- **本仓毕业口径**（从已毕业的 3 条反推，不要看 README 的示例）：`0027`=真闸 `check-upload-dirs.mjs`；`0035`=类型层 `timeoutMs` 必填；`0036`=`check-arch.mjs` 规则 6/10/11/12。⇒ **违规即红 / 编译不过** 才算；**行为单元测试不算**（它测"原语对不对"，不测"有没有人绕过"）。
- **实测**：0002 的 `confirmPersist.test.ts:70`、0005 的 `utils.test.ts:25`、0006 的 `utils.test.ts:69` 全是**行为测试** —— 别处新写裸三元 / 改用 JSON 往返 / 裸调 `toDataURL`，它们**一条都不红** ⇒ 不毕业。0034 只有 `check-node-handles.mjs` 守**端口渲染**那一小块，判据 1/2/3/4 无闸 ⇒ 不毕业。
- **两处假宣称**：① `ADR-0033:66` 自称「裸读 import.meta.env 会被 check-any-honesty.mjs 拦」—— 该闸只管 `as any`/`as never`/`useRef(null)`，**完全不碰 env**；已就地改写为诚实边界 + 毕业门槛。② `docs/adr/README.md:209` 的毕业示例引用 `tests/unit/degrade.test.ts`，**该文件不存在**（只有 `useAssetDegrade.test.ts`，且测的是 LOD）—— **未修**（守护者边界：不改 README 口径），列为待办。
- **已落盘防护**：5 条全部补了 ADR-0052 第 6 级「诚实边界」（写明**不能机器判定** + 人工判据 + 不许硬凑 grep + 「不许判毕业」+ 将来毕业的门槛）。⇒ **这次的近失已编码进 ADR**。
- 结果：`audit` 0 问题，**现行仍 37**（不降是对的 —— 宁可占编号，也不发没人守的"已毕业"证）。

## 全量健康度体检（12:17）· 记录在同上的轮次文件 §⑧

- **唯一实质命中 = ADR-0044 的 ⓿ 问**：正文 §9 拆门面是**三关**（正确性不退 ⇒ 禁做 / 复杂度变低 / SSOT 变好），**结论句只写了后两关**，漏掉唯一带「禁做」的必要条件 ⇒ 只读结论的 AI 会以为凑齐 ②③ 就能拆 —— 与 2026-09-20 白删事故同一个病。**已修结论句**。
- **落点失效 2 处已修**：`0015` 的 `engine/commands/CommandManager.ts` → `src/components/videoEditor/engine/core/managers/commands.ts:8,33`；`0026` 的 `generationContract.ts` → `src/components/generate/lib/generationOrchestration.ts:68,151`。`0039` 的 N2/N7 三处**举例**路径失效（是被本条自己改名的）⇒ 已标注「旧名/已改名」，不删。
- **幽灵闸**：全仓扫 ADR 引用的 `scripts/*` 与 `npm run X`，除已修的 0033 外**无假宣称**；0011/0049 引用已删脚本是**有意的历史记录**（且 0011 有「复活即失败」守卫）⇒ 别误伤。
- ⚠️ **方法沉淀**：① 扫 ADR 里的代码落点必须**按 basename 建全仓索引**解析（ADR 大量写 `contracts.ts` 简写），否则全是假红。② **单向补丁检查机器判不了方向** —— 真正缺陷是「两边都没指针」，而那没有引用可查 ⇒ 只能当线索不能当结论（本轮 49 处原始命中全部判为噪音）。③ 正则 `scripts/xxx.(mjs|js)` 会把 `gates.manifest.json` 误匹配成 `.js`（贪心无边界）。
- **别误伤**：ADR 提到已删除的脚本/文件，若该 ADR 本身就是**记录删除**或**防复活**，那是健康的，不是幽灵落点。
- 结果：现行 37 · audit 0 问题 · 无新增可合并对 · 无新增可毕业。

## 合并 ADR-0049 + ADR-0054（12:38 用户指令）· 记录在同上的轮次文件 §⑨

- **`ADR-0054` 是另一个会话在本轮体检期间新建的**（标题「ADR-0049 的适用范围：判据只约束新增用例，存量一律不动」）⇒ 印证「本仓可能同时有另一会话在写」，动 ADR 前先看 `ls docs/adr/` 与 mtime。
- **方向：把 0054 并进 0049，不开新条**（铁律 1）。0054 是纯**适用范围补充**不是同义重复；0049 是判据本体且被多处引用，并进去**引用面零 churn**（实测 0054 引用数 = 0）。
- **该合的真实理由（命中 ⓿ 问）**：0049 结论句原本没写适用范围，只在决议挂「见 ADR-0054」指针 ⇒ 只读结论的 AI 会拿「禁 UI 存在性测试」去删存量 ~430 处 —— 正是用户裁定要防的误删。**与 ADR-0044 同一个病**（结论句漏掉唯一带「禁做」的那道关）。
- 合入：★适用范围表（新增违规即删 / 存量一律不动）+ 2026-09-21 存量实测表 + 四条豁免理由 + 「判存量不算违规，但拿豁免为新写开脱 ⇒ 违规」+ 结论句带上适用范围 + 补 Lessons Learned（audit 拦下：取代方必须写）。
- 结果：0054 → 已取代 by 0049 · 引用面 0 处 · **audit 0 问题 · 断链 0 · doctor 四项全绿 · 现行仍 37**。
- ⚠️ **信号**：0054 从新建到被合并**不到 1 小时**，它解决的问题（适用范围）本可在 0049 里改一行 ⇒ 落笔前先问「这是新判据，还是既有判据的边界？」后者改现有条，不占编号。
- ⚠️ 复核过一回的坑：`status N --to 已取代 --by M` 会在 **M** 的头部写 `- **取代**：ADR-N`，于是 `refs N` 会显示「须回改 1」—— **那是工具字段，不要改**（改了就断掉取代链）。

## 六问闸修正（12:46–12:53 用户指令）· 记录在轮次文件 §⑩（**含我一次误判与回退**）

- **⚠️ 我读反了一次**：`adr.mjs:434` 有句注释写「六问的**答卷人是用户**，不是 AI」，我照它把 `--q6` 改成「用户答」⇒ **用户当场纠正**：「AI 展示给用户，用户来**决策**」。**6 问是 AI 的自检，用户只做同意/不同意。** 已回退。⇒ 教训：**ADR/工具里的注释也会写错，别拿它当依据**（同 ADR-0001：既有约定不是判据来源）。
- **真实缺口**：门槛只要求「答过 + 自报同意」⇒ AI 把答卷塞进 `--q6` 就过闸，**用户从没见过这 6 问** = 自问自批（2026-09-20 误落 ADR-0050 的同一路径）。
- **用户追问「既然给用户看过了，为什么还再发给 CLI，是否重复」→ 查证属实**：`q6` 只在 `:480` 校验标记、`:513` 回显，**不落盘、不进索引 ⇒ 打完就丢**；且「含 6 个标记」只证明敲了 6 个符号 = **假防线**。
- **用户裁定：删掉 `--q6`**。已改：`adr.mjs` 删 `--q6` + 6 标记检查，门槛只剩 `--user-approved`；`:434` 错注释改正；打印/错误文案改为三步流程（AI 答 → **贴进对话** → 用户决策）；`README.md` 4 处 + `ADR守护者.md`（「5 问」→「6 问」+ 口径）。
- **探针**：无 `--user-approved` → exit 2；**传 `--q6` 也照样 exit 2（已完全失效）**；`--dry` 仍打印 6 问。
- ⚠️ **`ADR-0055` 是删号作废号，勿复用**（回退前的正例探针真建过，验证后 rm）。
- ⚠️ **改 `adr.mjs` 的坑**：模板字符串里写 `` `--q6` `` **必须转义成 `` \`--q6\` ``** —— 漏了会让反引号提前终止模板串，`--q6` 被解析成后缀自减 ⇒ `SyntaxError: Invalid left-hand side expression in postfix operation`。**改完先跑 `node --check scripts/adr.mjs`**。
- **诚实边界**：拦得住「没声明用户同意」；**拦不住「AI 答完不贴给用户看就自报同意」**（无法机器验证）⇒ 按 `ADR-0052` 第 6 级诚实声明，**不硬凑检查**；真防线是「贴进对话」这个用户当场能看见的动作。

## 性能优化审计债务清偿（12:00–12:15）· 轮次文件 `25-跨区-性能优化审计债务清偿-2026-09-21.md`

- **销 3 笔**：TD-04-46 / TD-04-56（全量零炸达成：252 files / **3073 → 3072 passed**，净 −1 恰为删掉的那条用例）；**TD-25-42**（口径固化为新脚本 `scripts/m1-count.mjs`）。
- **新工具 `scripts/m1-count.mjs`**（AST 口径 · 只读 · **不是闸**，不挂 CI）：`--json` 落基线、`--diff` 自动算净减。**它取代 `04-跨区-M1母体建闸前置评审` §一 的 7 行手写 grep。**
  - ⚠️ **强可复用**：旧 grep 口径**不可复算**的根因实测 = ① 把 **30 行注释**里的 `memo(` 计入（改注释即改数字）② `{0,60}` 是任意上界。⇒ **凡"计数当验收"的口径，必须钉进 AST，禁停在 grep。**
  - ⚠️ **「区间差值 ≠ 轮次净减」**：想用脚本给某轮出净减，必须在**该轮开工前**跑 `--json`；事后用 `git worktree` 检出改前提交**不可行**（提交混装 + 域模块化搬迁同区间）。
- **TD-04-58 改判非债**（用户质疑「加载期需要那么久吗」触发实测）：审计的「489.79 kB 下载期空白 ⇒ 可感知退化」**被推翻** —— 真实 Chromium 冷缓存 ×3：**网络仅 3–17 ms**，总空白 58.4/58.8/117.4 ms（多为编辑器自身 JS 的**解析+执行**，属 1B **搬家**而非新增）；热缓存 0.3 ms。⇒ 保留 1B + `fallback={null}`。已按 A7 回改 `25-跨区-待拍板清单-2026-09-20.md:186` 的「无行为差异」。
- **ADR-0049 边界用例**：删 `NodeTitle` 图标样式用例（连 `Orbit` import）+ 删 `useImageHoverActions` 两条被行为断言覆盖的断言（保留 `show`）。**其余存量未清点** ⇒ 新登记 **TD-25-43**（36 文件 / `querySelector` 173 行 / `className` 23 / `getAttribute` 54）。
- **两条未做留痕**：① B 簇 4 笔（TD-04-40/45/54/57，审计已划出边界）；② `debt.mjs audit` 4 项历史违规 —— **实测其修复被工具卡住**：`debt.mjs edit` 只支持 `summary`/`solution`，**锚点/归类改不了**，而账本禁手改 ⇒ 需先给 CLI 加 `--field anchor|classify`。
- ⚠️ **环境观察**：本仓**同时有另一会话在写**（`docs/adr/ADR-0053`、`docs/plan/123`、`AI提示词进阶…md`、`DOMAIN-*`，mtime 11:55/12:00）。本轮**未触碰、未回退**其改动。

## B 簇 4 笔未施工债清偿（12:20–12:35）· 轮次文件 `04-跨区-B簇未施工债清偿-2026-09-21.md`

- **销 3 笔**：**TD-04-54**（测试重复覆盖）· **TD-04-45**（4 处内联 getSnapshot/subscribe）· **TD-04-57**（卡片操作浮层 40 行逐字重复）。**TD-04-40 单独立轮**（实测波及 **49 处 `useEditor()` / 37 文件**）。
- **TD-04-54**：收口到 `useCanvasHistory.test.ts`（7→9 例）+ **删** `canvasHooks.test.ts`（5 例，4 例重复）。删前走 ADR-0047 两关 + 四口径（AST 0 / 字符串仅自指 / 无桶 / glob 收集）。
  - ⚠️ **强可复用**：该文件那条「启动 600ms suppress」断言是 **`expect(typeof canRedo).toBe('boolean')` = 同义反复**（`canRedo` 本就声明为 boolean）⇒ 按 §7.2 换成真断言。**"断言存在 ≠ 契约被覆盖"**。
- **TD-04-45**：3 处逐字重复收口为 `useSelectionSnapshot`（`use-element-selection.ts`）+ 导出**窄** `useSelectedElements()`；`nodeRuntimeStore.ts:76` 就地 `useCallback`。
  - ⚠️ **强可复用（React 语义）**：`useSyncExternalStore` 的**重订阅依赖只有 `subscribe`** —— 去掉 `getSnapshot` 的 `useCallback` **探针不红**（实测），去掉 `subscribe` 的才红。**别把 getSnapshot 的身份当重订阅载荷。**
- **TD-04-57**：新建 `src/components/resource/ResourceCardActions.tsx`（**素材域**：有业务语义+仅 2 域消费 ⇒ 按 ADR-0040 **不算横切**，不进 `base/ui`）。两消费方传 `item` + 稳定 handler（签名本为 `(item: ResourceItem) => void`），类名字符串逐字保留 ⇒ 渲染零差异。
- **零炸逐笔对照（用例数差值与改动一一对应）**：开工前 252/3072 → TD-04-54 后 **251/3069** → TD-04-45 后 **252/3072** → TD-04-57 后 **252/3072**。闸：tsc 0 错 · check:arch ✅ · check:dead-code ✅ 无新增 · eslint 0 error。
- **未做留痕**：`check:dead-code` 报基线内 1 条已消失（`genErrors.ts::timeoutMessage`，**非本轮**）⇒ 不顺手 `--update-baseline`。

## TD-01-27 复检 → 改判非债（12:36–12:45）· 轮次文件 `01-跨区-前端persist泄漏执行态列复检-2026-09-21.md`

- **结论**：债的**现象属实**（前端对 backend-owned 任务发整份 Task 快照），但**不是缺陷** ⇒ **改判非债**。01 区由此转 `已验证(无债)`。
- **取证链（可复用）**：① 执行态归属 = **后端那一行的逐行事实**（`request_data._relayPoll`），正确性由拥有真相的那层保证；② **前端无法观测该事实** —— `grep -rn "requestData" src/` = **0 处**，`reportGenerate():282` 本地造任务（无句柄）后**立即** `persist`，后续 persist 都作用于同一内存对象；③ 后端逐行判据：行不存在/无句柄**不剥离**（文本·chat 前端本就是执行方，一刀切会造「文本任务永远 running」回归）。
  - ⚠️ **别据 `type` 在前端剥离执行态列** —— 那是**第二份判据**（后端已明确拒绝全局假定）。要消冗余只有一条路：**加跨栈契约字段**（Step 5 交用户）。
- **A7 回改**：`01-跨区-前端persist泄漏执行态列-2026-09-20.md` 的「逐次打 warn」**不成立** —— HEAD `aa0121f1`「任务状态收敛」已把冗余写降为 **`debug`**（`[upsertTask:client-redundant]`），`warn` 只留真竞态（`blocksRealDamage`）。
- **闭合原轮开口**：实测 `GET /api/tasks?pageSize=200` 查 `task_mu9mp6di_dhf736` → **`status=completed` + 真实 `result_url`** ⇒ 属「纯前端越权写噪音」，**非**「缺执行方真 bug」（守门生效，结果未被抹）。
- **旁注（观察·不立债）**：该行 `completed` 却残留 TLS 断连 `errorMsg`；实测 `TaskCenter.tsx:342` 只在 `failed || unknown` 渲染 `errorMsg` ⇒ 对用户不可见。
- **用户裁定（12:50 · 原话「那直接这个判定为非债」）**：TD-01-27 **判非债结案**，**不追**跨栈契约改动 ⇒ 冗余载荷（debug 级日志）作为既定成本接受，不立债、不回潮。**同时禁**前端按 `type` 剥离执行态列（那会是"归属"的第二份判据）。

## TD-04-40 单独立轮：`useEditor()` 订阅面收窄（13:00–13:12）· 轮次文件 `04-跨区-useEditor订阅面收窄-2026-09-21.md`

- **做法**：`useEditor(...stores)` —— 订阅面**由消费方声明**（取该文件 `editor.<store>` 引用的**并集** = 上界 ⇒ 构造上不欠订阅）；不传参 = 全部 7 个（向后兼容）。48 个调用点全部迁移；`useEditorInstance()`（只取实例不订阅）唯一消费方 = `use-element-selection`。
- **度量**：订阅总数 **336 → 105（−68.8%）**；**28/46** 调用点不再订阅 `playback` ⇒ 免**每帧连坐重渲**。
- ⚠️ **强可复用（先证伪朴素解法）**：债的解法「拆成 instance + 渲染方」**不成立** —— 实测 36 个消费方**全部**至少引用 1 个 store（零引用的 = 0）⇒ 通用实例入口**没有消费方**，加了就是 ADR-0030 **幽灵预留**。真正缺的是"**订阅面不可声明**"，不是"实例 vs 订阅"的二分。
- ⚠️ **`command` / `save` / `audio` 不是订阅缺口**：实测它们**无自己的 `subscribe`**（`save-manager` 自己订阅 scenes/timeline、`audio-manager` 订阅 playback/timeline/media）⇒ 是**派生**件，订阅那 7 个即覆盖。**别给它们加订阅面**（= 造第二份真相）。
- ⚠️ **两条弯路（都可复用）**：① **审计脚本的输出本身也是"描述"，要自检** —— 首版分类脚本把 manager 集合藏了，把 `use-editor-actions.ts` 误显示成「零 manager 引用」（它其实有 `editor.project.getActive()` **渲染期读**）⇒ 照它迁移会**静默不更新**。② **改被 mock 模块的导出面，必须同步它的所有 mock 点** —— 把 `use-element-selection` 从 `useEditor` 改到 `useEditorInstance` 后，TD-04-45 那个测试的 `vi.mock` 未同步 ⇒ 3 例红（全量首跑 1 文件/3 例失败），已修。
- **零炸**：全量 **253 files / 3075 passed**；tsc 0 错 · check:arch ✅ · eslint（52 文件）0 error；探针真红。
- **边界（诚实声明）**：粒度 = **manager（7 个）非字段**；字段级需值选择器（`useStoreSelector` 那条路），而 `useEditor` 是版本计数形态、无 snapshot 可选 ⇒ 属另一轮，**不假装做了**。
- **主表待还降至 10 条**。

## TD-25-43 改判非债（13:50 · 用户裁定）—— 存量 UI 存在性用例不追

- **用户原话**：「存量 ui 测试这个不查，把这个列为非债」（"肥宅"=非债 谐音）⇒ **存量不做清点**，已归档。
- ⚠️ **判据侧已同步收窄（强可复用）**：**ADR-0049 于同日 12:42 被改写** —— 判据**默认只约束新增/新改**（明文「**判存量不算违规**」）+ 待办②「存量待清点」**作废** + 新增 Lessons Learned：
  「**正确的判据溯及存量，就变成大面积误删的授权书 ⇒ 立判据默认只约束新增**；要溯及存量须单独举证（真违规数可数 + 误删可逆）」。
- ⚠️ **对已做工作的影响（A7 已回改）**：我在 B 簇轮删掉的 2 处断言（`NodeTitle` 图标样式 / `useImageHoverActions` 的 `toBeTruthy`+`typeof onClick`）**结论不变**，但**依据要重新表述** ——
  不再靠 ADR-0049 的适用范围，而靠：① 契约**已在类型层**（`icon?: LucideIcon`）⇒ 第二道冗余；② 被**同文件点击行为断言完全覆盖**（探针已证）。
  **不得**把理由回写成「ADR-0049 要求」（= 把已收窄的判据又扩回去）。已在 `25-跨区-性能优化审计债务清偿-2026-09-21.md` §3.2 与 `25-跨区-性能优化审计合规审计-2026-09-21.md` §四 各留后记。
- **主表待还降至 9 条**；`debt audit` 现为「可归一/修复 **0** 项 · 需人工 3 项」（TD-22-68 的归类非规范已由他人修掉，仅剩 3 处历史锚点断链）。

## A7 回改：轮次文件里的「⏳ 未做」清单会过期（13:52 · 用户发现）

- **用户发现**：`25-跨区-性能优化审计债务清偿-2026-09-21.md` §一 第 7 行仍写「B 簇未施工 4 笔 ⏳ 未做」，而这 4 笔（TD-04-40/45/54/57）**早已全部清偿**（分属 `04-跨区-B簇未施工债清偿` 与 `04-跨区-useEditor订阅面收窄` 两个轮次）。
- **已回改 5 处**（都加 `▶ 后记`，保留原表作快照）：① 该文件 §一 第 6+7 行（合并一条后记，覆盖 TD-25-43 判非债 与 B 簇已完成）；② 该文件 §六 表；③ `04-跨区-B簇…` §一 第 4 行；④ 同文件 §五；⑤ 审计文件 §六 覆盖账。
- ⚠️ **强可复用（新坑）**：**轮次文件的「未做 / ⏳」清单是"状态"，会随账本漂移** —— 它与 A8「状态不许**预支**」是一对：**状态也不许滞留**。
  本轮 5 处全靠**人眼发现**；**机器可判**（轮次文件里 `TD-xx` + 附近有 `⏳/未做`，而账本该号已 `已解决` ⇒ 漂移）。
  ⇒ **未建闸**（ADR-0016 默认不建 + 建闸前置评审需用户买账），仅留痕待用户定。

## 解答「m1-count.mjs 取代什么」（12:22，只读 · 用户没看懂上一条轮次文件 §二）

- **用户问题**：「他说他那个脚本 M1 那个能取代什么，我没看明白」。
- **答案**：取代的不是某个功能/守卫，而是**验收口径** —— `04-跨区-M1母体建闸前置评审-2026-09-20.md` §一 那张普查表里的**手写 grep 口径**（表 7 行数字，其中 **6 行**是 `grep -rEo`，第 16 行是"上两行相加"）。
- **实跑复现（本轮亲自跑，未复述原文）**：
  - `grep -rEo '(^|[^.A-Za-z])memo\(' src` → **52**；`node scripts/m1-count.mjs` → **20**（差 2.6 倍，同一份代码）。
  - `grep -rEo 'React\.memo\(' src` → **50**；AST → **49**。
  - `grep -rn '//.*memo(\|^\s*\*.*memo(' src` → **30 行**注释命中。
  - 脚本实时输出：扫描 592 文件（181 含命中，**2 个文件有 TS 解析诊断**）；裸 memo 20 / React.memo 49 / 合计 **69** / 带比较器 3；内联箭头 1188 / 数组 145 / 对象 210。
- **易误读点（用户卡住的真正原因）**：「取代」听起来像取代一道**检查/闸**。实际相反 —— M1 评审已裁定**不建闸**，脚本头明写"无 pass/fail、退出码恒 0、不挂 CI"。它取代的是"**怎么数**"。
- ⚠️ **发现一处凭证瑕疵（未改，已报用户）**：轮次文件 §八「一归」行的凭证 `grep -c "grep -rEo" 04-跨区-M1母体建闸前置评审-2026-09-20.md` → 声明 **7**，**实测输出 6**。「7」实为那张**7 行数字表**（含"上两行相加"），措辞混淆。该文件通篇讲"凭证可复现"，此行自相矛盾 ⇒ 建议改 6 或改措辞。**按"不改没人让我改的文件"原则，只报不动手。**
- **身份落定**：用户 = **Kevin**，沟通偏好 **结论先行**。已写 `~/.workbuddy-ai/{IDENTITY,SOUL,USER,MEMORY}.md` + 本仓 `memory/MEMORY.md`（长期约定与踩坑），并删除 `BOOTSTRAP.md`。

## 工具债三实例（13:00–14:00 · 用户指令「看看有什么工具债，去修，只做工具债」）· 轮次文件 `daily/架构日志/17-跨区-工具债声明与实况不符-2026-09-21.md`

- **Step 0 普查**：`node --check` 全 scripts 全绿（无语法债）；幽灵依赖扫描（scripts 的 bare import × package.json 声明取差集）→ **唯一命中 `@babel/parser`**；账本 `list --status 待还` 11 条**无一条工具债**（工具债此前从未入账）。
- **Step 2 归因（关键）**：3 个实例形态不同，**根因同一条 = M4「描述层无对账」** —— 工具的「声明/文案/小结」说了算、不过对账。⇒ 登 **1 笔母体 TD-17-35**（17 区「审计工具链治理」），当场 resolve + archive，**不进主表待办**（A10）。
  - ① `check-arch.mjs:47` 自称「@babel/parser（**devDependencies 已声明**）」= **假**（靠 vite/@babel/core 的传递依赖活着）→ `package.json` 补 `^7.29.8` + `npm install` 同步 lock（**diff 仅 2 行**）。
  - ② `debt.mjs audit` 小结报「**可归一/修复** N 项」，但归类/利息率**无写入口**（`edit --field` 只收 summary｜solution）⇒ 假指引 → **给 `edit` 补 `--field classify` 与 `rate`**（枚举只收规范值，**不替调用方归一** —— 归一归 `normClass`/`normRate`）。
  - ③ `adr.mjs audit` 报「**正文** N 行」，实为 `bodyLines = text.split('\n').length` = **整个文件行数** → 只对齐**文案**，**口径未改**（改口径 = 放宽红线，越界）。
- **⚠️ 强可复用教训（本轮第二次弯路）**：`debt.mjs add` 之后**必须复跑 `debt.mjs audit`** —— 新债的**锚点文件里没有该债号** ⇒ 立刻制造一条「锚点断链」违规（违规 3 → 4）。**只看 `add` 的 ✅ 会漏**。补上区域日志的「新登记债」节即消。
- **⚠️ 另一条**：`debt.mjs` 的 `AUTO_KINDS` 含「多余表头」，但 `fix` 只修**列错位**（`cmdFix` 只处理 mode A/B/C 行）⇒ 该项**无入口**（当前 0 实例）。未修：扩 `fix` = 为 0 实例建能力（撞 ADR-0053 第 0 关），收窄 `AUTO_KINDS` 又近「改声索」⇒ 只登记。
- **未做（A11 判不准 → 只登记）**：`adr.mjs --q6` 答卷**落盘即蒸发**（只 console 回显，ADR 里查不到）；`--user-approved` 是**自证 flag**（只校验"有没有写它"，不校验用户是否真同意）。两条都属**记录纪律形态**，需用户裁定。
- **凭证**：先红后绿（`edit --field classify` exit 1 → exit 0，`audit` 4 → 3 违规）；反向探针（用不存在的 `TD-99-99` 探四分支全通 + 三个非法值全被拒，**零写入**）；`check:push` **8 道闸全绿**；`npm ls @babel/parser` 从 deduped 幽灵变顶层。
- **范围自守**：只动 `scripts/**` + `package.json` + 本区日志。`src/`·`tests/` 零改动（已用 mtime 核过：那批 src 改动 mtime 11:55/12:30，是**并行会话**的，非本轮）。
- **顺带观察**：`adr.mjs audit` 现报 **0 问题**（ADR-0049 的 81 行超限已消失，另一会话动过）；`ADR-0054` 仍在（12:40）。用户说「我让别的 ai 合并」⇒ **本轮未触碰**。

## Skill 系统（docs/plan/140）落地审计（23:00–23:10 · 只读轮）· 轮次文件 `daily/架构日志/11-跨区-skill系统方案落地审计-2026-09-21.md`

- **归属区**：**11（AI 助手）** —— skill 首审（此前无 skill 轮次、`debt.mjs search skill` 零命中）。区域状态 `已验证(无债)` 🟢 → **`已验证(有债待还)`** 🔴。
- **审计口径**：对 `docs/plan/140` 的 P0–P4 施工单 + §6 验收 28 条**逐条对账**（架构形态 / UI / 功能落地 / 代码严谨性 / 逻辑错误 / 用户视角）。覆盖账 **26 点位 / 审 26 / 未审 0**。
- **总判定**：**P0–P3 基本按方案落地（后端 6 端点 · 磁盘真相源 · 发送起点冻结 · 漂移侦测 · 整目录导入 · 预算配额 · 写路径收口均取证为真）；架构形态符合方案（模块落点 / 门面按用例 / 唯一读写口 / 域外零深引）；缺陷集中在「装配层」与「文档层」。**
- **🔴 头号发现（高息母体）**：**P4 `skill_read_file` 功能等于零落地** —— `AgentPanel.tsx:415` 的 `applySkill` 只镜像 `id/name/description/content` **四字段**，丢掉 `category/slug`；`freezeSkillTurn` 收到后写出的 binding 永远缺落点 ⇒ `skillResource.ts:91` **恒走「不在磁盘上」分支**（对所有 skill 都失败）。`as SkillPick[]` 断言把类型不匹配压住了；单测 `skillResource.test.ts:27-28` **直接构造**带 `category/slug` 的 binding ⇒ **全绿而功能全断**（判据↔实况差集，第 6 问的又一实证）。
- **其余 14 笔待还**：① 方案 §1.4「不可信条款须覆盖块①」未落地（`agentCore.ts:428` 块① 不附规则、`agentConfig.ts:155` 无该条款，§9 却标 ✅）② 「空库=未决」被谎报成「磁盘与缓存一致」③ `skillStore.ts:15` 深引 `skillRepository`（门面唯一出口被绕过 + 启用态写两份 API）④ README 三处漂移（`RuntimeSkill` 已删仍写 / 「待 P3 收拾」未消 / 「云同步回写未做」实已做）⑤ 方案 §7 承诺的「references 引用断裂 → hydrate 报警」未落地 ⑥ 「去使用」是假动作（只 showToast）⑦ 清空「分组」框**不能**移回未分组（`undefined` 折叠了「未提供」与「清空」）⑧ `skillCloudSync.ts:84` 静默跳过形状不全条目 ⑨ 目录名合法性**两份判据**（前端 slugify 剥 `*?<>|` vs 后端放行）+ `execSync` 拼串（`files.ts` 同款复发）⑩ `truncateSkillContent(_,0)=不限` 反直觉契约 ⑪ `MAX_IMPORT_FILE_BYTES` 与后端 `MAX_FILE_BYTES` 两份 ⑫ 导出静默降级为仅正文 ⑬ `SkillBinding.origin` 只写不读 ⑭ 组清单取材偏离 §1.6（空组侧栏不可见）。→ 账本 **TD-11-16 ~ TD-11-30**。
- **已裁定非债（入账）**：`TD-11-31`（「空库不写空」闸门为有意设计，只挡「全不可纳入」；后端不可达已由 `!r.ok` 早退）· `TD-11-32`（门面未导出 `openSkillFolder` —— ADR-0053 零消费者不导出，属方案文档滞后）。另 2 条仅轮次留痕（块② 结束标记带「（N 项）」；`splitFrontmatter` 导出）。
- **⚠️ 新踩到的工具约束（SOP 未写，强可复用）**：`debt.mjs add` 的 `--summary` **≤120 字**（超了报「摘要 N 字 > 120」），且**禁裸竖线 `|`**（会撑破表格列，须换「／」）。首次三条被连拒两次。
- **范围自守**：本轮**只写 3 个文件** —— 轮次文件（23:07）· `债务.md`（23:06）· `index.md`（23:07）。`src/**` / `localTool/**` 一行未改（工作树里那批 `M src/...`、`?? src/components/agent/skill/` 是 **skill 功能本身**的未提交改动，非本轮产物）。

### 深审二轮 + SOP 补齐（23:10–23:20 · 同轮次文件 §五）

- **🔴 最贵的一课（已写进 MEMORY.md）**：**审计期间目标在动**。二轮取证时发现**另一会话正按文件顺序施工同一模块**（mtime 22:09 `skillMigration` → 22:11 `skillManifest` → … → 23:01 `README` → 23:08 `skillCatalog`/`skillApi`/`index` → 23:08:50 `SkillSettings` → 23:09:08 `AgentPanel` → 23:09:54 `skillCatalog.test.ts`）。一轮读数（~23:00）**有 6 个文件已过期** ⇒ 二轮第一件事改成**逐条复核**，而不是继续往下挖。
  - ⚠️ 顺手踩到：**shell `grep` 在本仓不可靠**（`--include=*.ts` 被 zsh 当 glob、`\|` 交替静默返回空）—— 实测"README 三处漂移"用 shell grep 查是 0 命中，**读文件才发现全在**。⇒ **取证一律用 Grep 工具（ripgrep），别用 shell grep**。
- **复核结果**：15 笔中 **13 笔仍成立**；`TD-11-30`（组清单取材）**已被并行会话修复**（新增 `listSkillGroups` + `buildSkillGroupTree(skills, diskGroups)` 并集、`_未分类` 例外、README 不变量 15）⇒ 账本 `resolve` 并留凭证 + `archive`；`TD-11-20`（README）**部分修复**（云同步那条已改正确，`:15 RuntimeSkill` 与 `:73 已知例外` 仍在，且新增 `:85-86` 同义重复）⇒ `edit --field summary` 就地更正。
- **二轮新债 8 笔（TD-11-33 ~ TD-11-40）**：① `description` 必填（§1.2）未校验 ⇒ 空描述技能被 `skillCatalog.ts:210` 静默滤出块①（中）② 文本扩展名白名单**两份且不一致**（`skillImport.ts:42` vs `skillCatalog.ts:44`，tsv/html/htm 可读不可导入）③ `.trash/` **只写不清**（每次覆盖保存 + 每次删除各留一份整包，无 GC/上限/文档/UI）④ 二进制判据 = **含 NUL**（`skills.ts:132`）⇒ 无 NUL 二进制被当 utf8 读、整包写回**静默损坏**（可达性 ≈2%/小文件，已标前提）⑤ `serializeSkillMarkdown` 空值已知字段往返丢行（`''`→`undefined`）而 `unknown` 无守卫；注释称"幂等已锁死"但单测只覆盖非空样例 ⑥ 预算旋钮**无上界**（可调到形同关闭、顶爆上下文）⑦ 漂移侦测**拿全量正文比签名**（每次焦点全量拉全库 `withContent`）⑧ 新组树残留洞：Finder 建**空**目录 + 面板「重读」→ 侧栏不刷新（`diskGroups` 只随 `libRefresh` 变）。
- **SOP 就地补齐（用户指令「那你写一下」）**：`.codebuddy/commands/债务登记5步法.md` §4.2 字段约束补上 `debt.mjs add` 的三条硬拒（**摘要 ≤120 字** · **禁裸竖线 `|`** · **利息只有 高/中/低**）+ 一句"写前先裁"。属 SOP §零.3 例外② 允许的「当场修工具债」。
- **账本收口**：主表待办 **10 → 32 条**（本轮累计登记 25 行：待还 22 + 已解决 1 + 非债 2）；`debt.mjs audit` 违规 **3 项全为归档区既有**（TD-01-27 / TD-02-67 / TD-04-55），**本轮零新增**；`archive` 归档 3 条。

## Skill 设置页 Mockup 重设计（23:00–23:15 · 只产出 mockup，不动 src）

- **产出**：`mockup/skill-settings-mockup.html`（自包含，只 link `maomao-kit.css` + 自写 `<style>`，**不依赖 Tailwind CDN** —— 实测全仓 mockup HTML 无一引用 CDN，离线也能开）。
- **现状对象**：`src/components/settings/sections/SkillSettings.tsx`（1038 行）。它**不在** `SettingsFrame` 的 SECTIONS 里，而是嵌在「AI 助手」分区底部（`<SkillSettings />` 在 `AgentChatSettings.tsx:221`）。
- **诊断出的 7 个病灶**（按影响排序）：① 磁盘库面板与左栏缓存列表 = **两个「有什么技能」的清单**（真相是缓存=磁盘投影，UI 画成了两个真相源）② 首屏被设置项吃掉 ~260px，列表被推到 `h-[760px]` 分栏里 ③ 「单篇 12000 字」预算与编辑器脱节（textarea 只有 `Skill Prompt` 角标）④ 组开关用二态控件表达三态，靠 `部分 2/4` 文字救场 ⑤ `bg-white text-black`「去使用」裸色按钮、权重反了 ⑥ 详情态底部 57px 空占位 div ⑦ 导入导出三个等宽技术黑话按钮（`.md`/文件夹/导出）。
- **方案 A（推荐）**：不改 IA，只做分层收拢 —— 顶部统计芯片（启用 5/12 · 注入上限 4）+ toolbar 一行（搜索/分组/导入▾/新建）+ 磁盘行压成一行（路径+重读+打开，不列技能）+ 高级预算折叠成摘要行；详情区加**预算条**（8,432 / 12,000）；组开关改**三态**（半填充=部分）；底部空占位换成路径/版本/修改时间元信息条。
- **方案 B（备选）**：卡片网格（3 列）+ 右侧抽屉。**不推荐为默认**的理由：本仓技能库心智是「分组=磁盘目录」，网格把目录层级压成筛选 chip，20+ 技能后退化为长滚动。但它的抽屉/预算条/缺 id 告警条可搬进 A。
- ⚠️ **写 mockup 的口径**：一律用 `--mao-*` 令牌别名（照抄 `leftpanel-tabs-mockup.html` 的 `:root` 别名块），禁裸色值；卡片宽度按设置内容区 `max-w-4xl` = **896px**。
