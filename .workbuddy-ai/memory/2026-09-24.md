# 2026-09-24

## 复检 plan 147（本机模型资产统一）执行结果 → 登记 6 笔债

用户指令：别的 AI 已做完 `docs/plan/147`，只检查、有问题就登记债，**不跑任何测试**。

**复检结论**：主体施工成立（`paths.ts::getRuntimeModelDir` 落点真源唯一；`index.ts` 已销
`/^\/depth-video\//` 物理目录名副本；`check:arch` 实跑通过含新增 `PREFIX_MODELS` 对账；
`git check-ignore -v` 证实入库规则生效；sha256 抽验 2/8 一致；`dist/` 已无 mediapipe/models）。
但原轮次「本轮未新登记债」**不成立**。

**新登记 6 笔**（账本 `debt.mjs`，锚点 `27-跨区-本机模型资产落点统一复检-2026-09-24.md`）：
- `TD-17-37`（中·母体·复发）区号 27 未登记进 `scripts/arch-index.mjs` 的 `AREAS`（止于 25）
  ⇒ 轮次文件对 `index.md` 不可见；26 号（2026-09-21）同形。
- `TD-17-38`（中）原轮次覆盖度表 6 行 vs 覆盖账「共12/审12/未审0」，改动文件无覆盖行。
- `TD-08-72`（中）plan147 §2.2 的 MANIFEST schema 未回填（path 基准／modelId↔tool／purpose↔description／source 空串）。
- `TD-24-11`（低）`localOnlyPaths.ts:27` 注释指向不存在的 `runtimeModels.ts`，实为 `runtimeModelUrl.ts`。
- `TD-23-6`（低）`.eslintrc.cjs:55` 的 `public/mediapipe/wasm/**` 搬迁后成死配置。
- `TD-08-73`（低·工具债）`runtime-model.mjs` 的 `init --from` 缺路径时静默降级。

**就地改正**：`spec/判据登记表.md` §九#14 载体数 N 3→4（漏计新增的 `runtime-model.mjs:38` 锚点）。

**裁定非债（留痕）**：`index.ts:260` 的 `try{decodeURIComponent}catch{}`（失败经 404 可见，与
`handleStaticFile:191` 同款）；`index.ts:268` 的 `startsWith` 前缀判据（仅可达同前缀兄弟目录，零提权）；
`public/manifest.json:50-51` 死声明（用户已裁定接受 Chrome 扩展形态代价）。

**环境事实（复用价值高）**：
- 跨区轮次文件用**不存在**的区号（26/27）时 `arch-index.mjs` 不报错、`index.md` 静默漏掉该轮。
- `debt.mjs add` 的摘要硬上限 **120 字**且禁裸 `|`（本轮被拒 3 次，先裁再敲）。
- `npm run lint` 只扫 `src tests`，不扫 `localTool`。
