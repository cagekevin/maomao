# Agent 独立服务的 API Key 配置（仅 OpenAI 兼容协议）
# 复制本文件为 .env 并填入真实 key。key 也可直接写在 api_providers.json 的 api_key 字段。
# 变量名规则：API_PROVIDER_<PROVIDER_ID大写，非字母数字转_>_KEY
# 例如 provider id 为 "my-image" -> API_PROVIDER_MY_IMAGE_KEY

# 示例：与 api_providers.example.json 的 my-image 对应
API_PROVIDER_MY_IMAGE_KEY=sk-your-key-here
